# Documentation Coverage Report

## Summary
- Total functions found: **12258**
- Total undocumented: **8750**
- Documentation coverage: **2.6%**

## Critical gaps
Exported/public entries with no JSDoc at all, grouped by file.

### `packages/daemon/src/channel-manager.ts`
- `packages/daemon/src/channel-manager.ts:96` — `class ChannelManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/daemon/src/channel-manager.ts:104` — `ChannelManager.constructor(opts: ChannelManagerOptions)` — exported: **yes** — Implements logic starting with: { this.guild = opts.guild; this.logger = opts.logger; this.categoryName = opts.categoryName ?? DEFAULT_CATEGORY_NAME; } (missing: jsdoc)

### `packages/daemon/src/daemon.ts`
- `packages/daemon/src/daemon.ts:24` — `Daemon.constructor(private readonly config: DaemonConfig, private readonly logger: Logger, private readonly healthIntervalMs: number = 300_000)` — exported: **yes** — Implements logic starting with: { this.onSigterm = () => void this.shutdown(); this.onSigint = () => void this.shutdown(); } (missing: jsdoc)

### `packages/daemon/src/discord-bot.ts`
- `packages/daemon/src/discord-bot.ts:73` — `class DiscordBot` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/daemon/src/discord-bot.ts:84` — `DiscordBot.constructor(opts: DiscordBotOptions)` — exported: **yes** — Implements logic starting with: { this.config = opts.config; this.logger = opts.logger; this.sessionManager = opts.sessionManager; this.scanProjects = opts.scanProjects; } (missing: jsdoc)

### `packages/daemon/src/event-bridge.ts`
- `packages/daemon/src/event-bridge.ts:62` — `class EventBridge` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/daemon/src/event-bridge.ts:91` — `EventBridge.constructor(opts: EventBridgeOptions)` — exported: **yes** — Implements logic starting with: { this.sessionManager = opts.sessionManager; this.channelManager = opts.channelManager; this.client = opts.client; this.config = opts.config; this.… (missing: jsdoc)

### `packages/daemon/src/event-formatter.ts`
- `packages/daemon/src/event-formatter.ts:62` — `formatToolStart(event: SdkAgentEvent)` — exported: **yes** — Implements logic starting with: { const toolName = str(event.name || event.toolName, 'unknown'); const embed = new EmbedBuilder() .setColor(COLOR.tool) .setTitle(`🔧 ${truncate(to… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:77` — `formatToolEnd(event: SdkAgentEvent)` — exported: **yes** — Implements logic starting with: { const toolName = str(event.name || event.toolName, 'unknown'); const isError = event.isError === true || event.error != null; const color = isErr… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:101` — `formatMessage(event: SdkAgentEvent)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:262` — `formatCompletion(event: SdkAgentEvent)` — exported: **yes** — Implements logic starting with: { const status = str(event.status, 'completed'); const isError = status === 'error' || status === 'cancelled'; const color = isError ? COLOR.error… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:290` — `formatError(sessionId: string, error: string)` — exported: **yes** — Implements logic starting with: { const embed = new EmbedBuilder() .setColor(COLOR.error) .setTitle('❌ Session Error') .setDescription(`\`\`\`\n${truncate(error, 2000)}\n\`\`\``)… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:301` — `formatCostUpdate(event: SdkAgentEvent)` — exported: **yes** — Implements logic starting with: { const cost = num(event.cumulativeCost ?? event.totalCost); const tokens = event.tokens as | { input?: number; output?: number; cacheRead?: number… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:325` — `formatSessionStarted(projectName: string)` — exported: **yes** — Implements logic starting with: { const embed = new EmbedBuilder() .setColor(COLOR.info) .setTitle('🚀 Session Started') .setDescription(`Project: **${truncate(projectName, 200)}*… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:335` — `formatTaskTransition(event: SdkAgentEvent)` — exported: **yes** — Implements logic starting with: { const taskId = str(event.taskId || event.task); const sliceId = str(event.sliceId || event.slice); const status = str(event.status || event.state… (missing: jsdoc)
- `packages/daemon/src/event-formatter.ts:355` — `formatGenericEvent(event: SdkAgentEvent)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `packages/daemon/src/logger.ts`
- `packages/daemon/src/logger.ts:28` — `Logger.constructor(opts: LoggerOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/daemon/src/logger.ts:43` — `Logger.debug(msg: string, data?: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { this.write('debug', msg, data); } (missing: jsdoc)
- `packages/daemon/src/logger.ts:47` — `Logger.info(msg: string, data?: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { this.write('info', msg, data); } (missing: jsdoc)
- `packages/daemon/src/logger.ts:51` — `Logger.warn(msg: string, data?: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { this.write('warn', msg, data); } (missing: jsdoc)
- `packages/daemon/src/logger.ts:55` — `Logger.error(msg: string, data?: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { this.write('error', msg, data); } (missing: jsdoc)

### `packages/daemon/src/message-batcher.ts`
- `packages/daemon/src/message-batcher.ts:58` — `class MessageBatcher` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/daemon/src/message-batcher.ts:69` — `MessageBatcher.constructor(send: SendFn, logger?: BatcherLogger, options?: BatcherOptions)` — exported: **yes** — Implements logic starting with: { this.send = send; this.logger = logger ?? noopLogger; this.flushIntervalMs = options?.flushIntervalMs ?? 1500; this.maxBatchSize = options?.maxBa… (missing: jsdoc)

### `packages/daemon/src/orchestrator.ts`
- `packages/daemon/src/orchestrator.ts:166` — `class Orchestrator` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/daemon/src/orchestrator.ts:175` — `Orchestrator.constructor(deps: OrchestratorDeps, client?: Anthropic)` — exported: **yes** — Implements logic starting with: { this.deps = deps; this.client = client ?? null; } (missing: description)

### `packages/daemon/src/session-manager.ts`
- `packages/daemon/src/session-manager.ts:61` — `class SessionManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/daemon/src/session-manager.ts:65` — `SessionManager.constructor(private readonly logger: Logger)` — exported: **yes** — Implements logic starting with: { super(); } (missing: jsdoc)

### `packages/daemon/src/verbosity.ts`
- `packages/daemon/src/verbosity.ts:50` — `class VerbosityManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)

### `packages/mcp-server/src/env-writer.ts`
- `packages/mcp-server/src/env-writer.ts:139` — `isSafeEnvVarKey(key: string)` — exported: **yes** — Implements logic starting with: { return /^[A-Za-z_][A-Za-z0-9_]*$/.test(key); } (missing: jsdoc)
- `packages/mcp-server/src/env-writer.ts:162` — `isSecuritySensitiveEnvKey(key: string)` — exported: **yes** — Implements logic starting with: { return SECURITY_SENSITIVE_KEYS.has(key.toUpperCase()); } (missing: jsdoc)
- `packages/mcp-server/src/env-writer.ts:166` — `isSupportedDeploymentEnvironment(env: string)` — exported: **yes** — Implements logic starting with: { return env === "development" || env === "preview" || env === "production"; } (missing: jsdoc)
- `packages/mcp-server/src/env-writer.ts:175` — `resolveProjectEnvFilePath(projectDir: string, envFilePath = ".env")` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/mcp-server/src/env-writer.ts:200` — `shellEscapeSingle(value: string)` — exported: **yes** — Implements logic starting with: { return `'${value.replace(/'/g, `'\\''`)}'`; } (missing: jsdoc)

### `packages/mcp-server/src/readers/captures.ts`
- `packages/mcp-server/src/readers/captures.ts:85` — `readCaptures(projectDir: string, filter: 'all' | 'pending' | 'actionable' = 'all')` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/mcp-server/src/readers/doctor-lite.ts`
- `packages/mcp-server/src/readers/doctor-lite.ts:183` — `runDoctorLite(projectDir: string, scope?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/mcp-server/src/readers/knowledge.ts`
- `packages/mcp-server/src/readers/knowledge.ts:92` — `readKnowledge(projectDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/mcp-server/src/readers/metrics.ts`
- `packages/mcp-server/src/readers/metrics.ts:74` — `readHistory(projectDir: string, limit?: number)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/mcp-server/src/readers/paths.ts`
- `packages/mcp-server/src/readers/paths.ts:104` — `_resetReaderCaches()` — exported: **yes** — Implements logic starting with: { gsdRootCache.clear(); milestoneIdsCache.clear(); milestoneDirCache.clear(); sliceIdsCache.clear(); sliceDirCache.clear(); taskFilesCache.clear();… (missing: description)

### `packages/mcp-server/src/readers/roadmap.ts`
- `packages/mcp-server/src/readers/roadmap.ts:184` — `readRoadmap(projectDir: string, filterMilestoneId?: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/mcp-server/src/readers/state.ts`
- `packages/mcp-server/src/readers/state.ts:160` — `readProgress(projectDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/mcp-server/src/server.ts`
- `packages/mcp-server/src/server.ts:400` — `buildAskUserQuestionsElicitRequest(questions: AskUserQuestion[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/mcp-server/src/server.ts:451` — `formatAskUserQuestionsElicitResult(questions: AskUserQuestion[], result: AskUserQuestionsElicitResult)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `packages/mcp-server/src/server.ts:629` — `askUserQuestionsHandler(questions: AskUserQuestion[], extra: McpToolExtra | undefined, deps: AskUserQuestionsHandlerDeps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/mcp-server/src/server.ts:754` — `secureEnvCollectHandler(args: Record<string, unknown>, elicitInput: ElicitInputFn)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/mcp-server/src/session-manager.ts`
- `packages/mcp-server/src/session-manager.ts:76` — `class SessionManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)

### `packages/mcp-server/src/tool-credentials.ts`
- `packages/mcp-server/src/tool-credentials.ts:63` — `resolveAuthPath(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { const agentDir = env.GSD_CODING_AGENT_DIR?.trim(); if (agentDir) return join(expandHome(agentDir), "auth.json"); return join(homedir(), ".gsd", "… (missing: jsdoc)
- `packages/mcp-server/src/tool-credentials.ts:69` — `loadStoredCredentialEnvKeys(options: { env?: NodeJS.ProcessEnv; authPath?: string; } = {})` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/mcp-server/src/workflow-tools.ts`
- `packages/mcp-server/src/workflow-tools.ts:328` — `validateProjectDir(projectDir: string, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/mcp-server/src/workflow-tools.ts:453` — `_parseWorkflowArgsForTest(schema: z.ZodType<T>, args: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { return parseWorkflowArgs(schema, args); } (missing: jsdoc)
- `packages/mcp-server/src/workflow-tools.ts:592` — `_buildImportCandidates(relativePath: string)` — exported: **yes** — Implements logic starting with: { // Build candidate paths: prefer source first, including the .ts source // variant, before falling back to compiled dist. In source/dev execution… (missing: description, @param relativePath, @returns)
- `packages/mcp-server/src/workflow-tools.ts:1559` — `registerWorkflowTools(realServer: McpToolServer)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/native/src/ast/index.ts`
- `packages/native/src/ast/index.ts:6` — `astGrep(options: AstFindOptions)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).astGrep(options) as AstFindResult; } (missing: jsdoc)
- `packages/native/src/ast/index.ts:10` — `astEdit(options: AstReplaceOptions)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).astEdit(options) as AstReplaceResult; } (missing: jsdoc)

### `packages/pi-agent-core/src/agent.ts`
- `packages/pi-agent-core/src/agent.ts:137` — `class Agent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:174` — `Agent.constructor(opts: AgentOptions = {})` — exported: **yes** — Implements logic starting with: { this._state = { ...this._state, ...opts.initialState }; this.convertToLlm = opts.convertToLlm || defaultConvertToLlm; this.transformContext = opt… (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:266` — `Agent.get state()` — exported: **yes** — Implements logic starting with: { return this._state; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:270` — `Agent.subscribe(fn: (e: AgentEvent) => void)` — exported: **yes** — Implements logic starting with: { this.listeners.add(fn); return () => this.listeners.delete(fn); } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:276` — `Agent.setSystemPrompt(v: string)` — exported: **yes** — Implements logic starting with: { this._state.systemPrompt = v; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:280` — `Agent.setModel(m: Model<any>)` — exported: **yes** — Implements logic starting with: { this._state.model = m; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:284` — `Agent.setThinkingLevel(l: ThinkingLevel)` — exported: **yes** — Implements logic starting with: { this._state.thinkingLevel = l; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:288` — `Agent.setSteeringMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Implements logic starting with: { this.steeringMode = mode; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:292` — `Agent.getSteeringMode()` — exported: **yes** — Implements logic starting with: { return this.steeringMode; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:296` — `Agent.setFollowUpMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Implements logic starting with: { this.followUpMode = mode; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:300` — `Agent.getFollowUpMode()` — exported: **yes** — Implements logic starting with: { return this.followUpMode; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:304` — `Agent.setTools(t: AgentTool<any>[])` — exported: **yes** — Implements logic starting with: { this._state.tools = t; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:308` — `Agent.replaceMessages(ms: AgentMessage[])` — exported: **yes** — Implements logic starting with: { this._state.messages = ms.slice(); } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:312` — `Agent.appendMessage(m: AgentMessage)` — exported: **yes** — Implements logic starting with: { this._state.messages = [...this._state.messages, m]; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:332` — `Agent.clearSteeringQueue()` — exported: **yes** — Implements logic starting with: { this.steeringQueue = []; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:336` — `Agent.clearFollowUpQueue()` — exported: **yes** — Implements logic starting with: { this.followUpQueue = []; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:340` — `Agent.clearAllQueues()` — exported: **yes** — Implements logic starting with: { this.steeringQueue = []; this.followUpQueue = []; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:357` — `Agent.hasQueuedMessages()` — exported: **yes** — Implements logic starting with: { return this.steeringQueue.length > 0 || this.followUpQueue.length > 0; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:391` — `Agent.clearMessages()` — exported: **yes** — Implements logic starting with: { this._state.messages = []; } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:395` — `Agent.abort(origin: AgentAbortOrigin = "unknown")` — exported: **yes** — Implements logic starting with: { this.abortOrigin = origin; this.abortController?.abort(); } (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:400` — `Agent.waitForIdle()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:404` — `Agent.reset()` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:416` — `Agent.prompt(input: string, images?: ImageContent[])` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-agent-core/src/agent.ts:417` — `Agent.prompt(input: string | AgentMessage | AgentMessage[], images?: ImageContent[])` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-agent-core/src/token-audit.ts`
- `packages/pi-agent-core/src/token-audit.ts:57` — `buildTokenAuditSummary(context: Context, sourceMessages: AgentMessage[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-agent-core/src/token-audit.ts:103` — `maybeLogTokenAudit(context: Context, sourceMessages: AgentMessage[])` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `packages/pi-agent-core/src/token-audit.ts:109` — `buildProviderPayloadAuditSummary(payload: unknown)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-agent-core/src/token-audit.ts:137` — `maybeLogProviderPayloadAudit(payload: unknown, phase: string)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `packages/pi-ai/src/api-registry.ts`
- `packages/pi-ai/src/api-registry.ts:66` — `registerApiProvider(provider: ApiProvider<TApi, TOptions>, sourceId?: string)` — exported: **yes** — Implements logic starting with: { apiProviderRegistry.set(provider.api, { provider: { api: provider.api, stream: wrapStream(provider.api, provider.stream), streamSimple: wrapStrea… (missing: jsdoc)
- `packages/pi-ai/src/api-registry.ts:80` — `getApiProvider(api: Api)` — exported: **yes** — Implements logic starting with: { return apiProviderRegistry.get(api)?.provider; } (missing: jsdoc)
- `packages/pi-ai/src/api-registry.ts:84` — `clearApiProviders()` — exported: **yes** — Implements logic starting with: { apiProviderRegistry.clear(); } (missing: jsdoc)

### `packages/pi-ai/src/env-api-keys.ts`
- `packages/pi-ai/src/env-api-keys.ts:64` — `getEnvApiKey(provider: string)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-ai/src/env-api-keys.ts:65` — `getEnvApiKey(provider: any)` — exported: **yes** — Implements logic starting with: { // Fall back to environment variables if (provider === "github-copilot") { return process.env.COPILOT_GITHUB_TOKEN || process.env.GH_TOKEN || pro… (missing: jsdoc)

### `packages/pi-ai/src/models/index.ts`
- `packages/pi-ai/src/models/index.ts:64` — `getModel(provider: TProvider, modelId: TModelId)` — exported: **yes** — Implements logic starting with: { const providerModels = modelRegistry.get(provider); return providerModels?.get(modelId as string) as Model<ModelApi<TProvider, TModelId>>; } (missing: jsdoc)
- `packages/pi-ai/src/models/index.ts:72` — `getProviders()` — exported: **yes** — Implements logic starting with: { return Array.from(modelRegistry.keys()) as KnownProvider[]; } (missing: jsdoc)
- `packages/pi-ai/src/models/index.ts:76` — `getModels(provider: TProvider)` — exported: **yes** — Implements logic starting with: { const models = modelRegistry.get(provider); return models ? (Array.from(models.values()) as Model<Api>[]) : []; } (missing: jsdoc)
- `packages/pi-ai/src/models/index.ts:83` — `calculateCost(model: Model<TApi>, usage: Usage)` — exported: **yes** — Implements logic starting with: { usage.cost.input = (model.cost.input / 1000000) * usage.input; usage.cost.output = (model.cost.output / 1000000) * usage.output; usage.cost.cache… (missing: jsdoc)

### `packages/pi-ai/src/providers/amazon-bedrock.ts`
- `packages/pi-ai/src/providers/amazon-bedrock.ts:65` — `streamBedrock(model: Model<"bedrock-converse-stream">, context: Context, options: BedrockOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:223` — `streamSimpleBedrock(model: Model<"bedrock-converse-stream">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Implements logic starting with: { const base = buildBaseOptions(model, options, undefined); if (!options?.reasoning) { return streamBedrock(model, context, { ...base, reasoning: u… (missing: jsdoc)

### `packages/pi-ai/src/providers/anthropic-shared.ts`
- `packages/pi-ai/src/providers/anthropic-shared.ts:77` — `toClaudeCodeName(name: string)` — exported: **yes** — Implements logic starting with: ccToolLookup.get(name.toLowerCase()) ?? name (missing: jsdoc)
- `packages/pi-ai/src/providers/anthropic-shared.ts:79` — `fromClaudeCodeName(name: string, tools?: Tool[])` — exported: **yes** — Implements logic starting with: { if (tools && tools.length > 0) { const lowerName = name.toLowerCase(); const matchedTool = tools.find((tool) => tool.name.toLowerCase() === lower… (missing: jsdoc)

### `packages/pi-ai/src/providers/anthropic-vertex.ts`
- `packages/pi-ai/src/providers/anthropic-vertex.ts:69` — `streamAnthropicVertex(model: Model<"anthropic-vertex">, context: Context, options?: AnthropicOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/providers/anthropic-vertex.ts:93` — `streamSimpleAnthropicVertex(model: Model<"anthropic-vertex">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/anthropic.ts`
- `packages/pi-ai/src/providers/anthropic.ts:65` — `usesAnthropicBearerAuth(provider: Model<"anthropic-messages">["provider"])` — exported: **yes** — Implements logic starting with: { return provider === "alibaba-coding-plan" || provider === "minimax" || provider === "minimax-cn"; } (missing: jsdoc)
- `packages/pi-ai/src/providers/anthropic.ts:74` — `buildAnthropicClientOptions(model: Model<"anthropic-messages">, apiKey: string, interleavedThinking: boolean, optionsHeaders?: Record<string, string>, dynamicHeaders?: Record<string, string>)` — exported: **yes** — Implements logic starting with: { // Adaptive thinking models (Opus 4.6, Sonnet 4.6) have interleaved thinking built-in. // The beta header is deprecated on Opus 4.6 and redundant… (missing: jsdoc)
- `packages/pi-ai/src/providers/anthropic.ts:161` — `streamAnthropic(model: Model<"anthropic-messages">, context: Context, options?: AnthropicOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/providers/anthropic.ts:201` — `streamSimpleAnthropic(model: Model<"anthropic-messages">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/azure-openai-responses.ts`
- `packages/pi-ai/src/providers/azure-openai-responses.ts:71` — `streamAzureOpenAIResponses(model: Model<"azure-openai-responses">, context: Context, options?: AzureOpenAIResponsesOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/providers/azure-openai-responses.ts:110` — `streamSimpleAzureOpenAIResponses(model: Model<"azure-openai-responses">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/github-copilot-headers.ts`
- `packages/pi-ai/src/providers/github-copilot-headers.ts:11` — `hasCopilotVisionInput(messages: Message[])` — exported: **yes** — Implements logic starting with: { return messages.some((msg) => { if (msg.role === "user" && Array.isArray(msg.content)) { return msg.content.some((c) => c.type === "image"); } if… (missing: jsdoc)
- `packages/pi-ai/src/providers/github-copilot-headers.ts:23` — `buildCopilotDynamicHeaders(params: { messages: Message[]; hasImages: boolean; })` — exported: **yes** — Implements logic starting with: { const headers: Record<string, string> = { "X-Initiator": inferCopilotInitiator(params.messages), "Openai-Intent": "conversation-edits", }; if (pa… (missing: jsdoc)

### `packages/pi-ai/src/providers/google-gemini-cli.ts`
- `packages/pi-ai/src/providers/google-gemini-cli.ts:320` — `streamGoogleGeminiCli(model: Model<"google-gemini-cli">, context: Context, options?: GoogleGeminiCliOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/google-gemini-cli.ts:813` — `streamSimpleGoogleGeminiCli(model: Model<"google-gemini-cli">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/google-vertex.ts`
- `packages/pi-ai/src/providers/google-vertex.ts:72` — `streamGoogleVertex(model: Model<"google-vertex">, context: Context, options?: GoogleVertexOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/google-vertex.ts:298` — `streamSimpleGoogleVertex(model: Model<"google-vertex">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Implements logic starting with: { const base = buildBaseOptions(model, options, undefined); if (!options?.reasoning) { return streamGoogleVertex(model, context, { ...base, thinkin… (missing: jsdoc)

### `packages/pi-ai/src/providers/google.ts`
- `packages/pi-ai/src/providers/google.ts:59` — `streamGoogle(model: Model<"google-generative-ai">, context: Context, options?: GoogleOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/google.ts:285` — `streamSimpleGoogle(model: Model<"google-generative-ai">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/mistral.ts`
- `packages/pi-ai/src/providers/mistral.ts:58` — `streamMistral(model: Model<"mistral-conversations">, context: Context, options?: MistralOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/mistral.ts:117` — `streamSimpleMistral(model: Model<"mistral-conversations">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/openai-codex-responses.ts`
- `packages/pi-ai/src/providers/openai-codex-responses.ts:110` — `streamOpenAICodexResponses(model: Model<"openai-codex-responses">, context: Context, options?: OpenAICodexResponsesOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/openai-codex-responses.ts:265` — `streamSimpleOpenAICodexResponses(model: Model<"openai-codex-responses">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/openai-completions.ts`
- `packages/pi-ai/src/providers/openai-completions.ts:69` — `streamOpenAICompletions(model: Model<"openai-completions">, context: Context, options?: OpenAICompletionsOptions)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `packages/pi-ai/src/providers/openai-completions.ts:333` — `streamSimpleOpenAICompletions(model: Model<"openai-completions">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-ai/src/providers/openai-completions.ts:477` — `convertMessages(model: Model<"openai-completions">, context: Context, compat: Required<OpenAICompletionsCompat>)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)

### `packages/pi-ai/src/providers/openai-responses-shared.ts`
- `packages/pi-ai/src/providers/openai-responses-shared.ts:85` — `convertResponsesMessages(model: Model<TApi>, context: Context, allowedToolCallProviders: ReadonlySet<string>, options?: ConvertResponsesMessagesOptions)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `packages/pi-ai/src/providers/openai-responses-shared.ts:262` — `convertResponsesTools(tools: Tool[], options?: ConvertResponsesToolsOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-ai/src/providers/openai-responses-shared.ts:277` — `processResponsesStream(openaiStream: AsyncIterable<ResponseStreamEvent>, output: AssistantMessage, stream: AssistantMessageEventStream, model: Model<TApi>, options?: OpenAIResponsesStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/openai-responses.ts`
- `packages/pi-ai/src/providers/openai-responses.ts:67` — `streamOpenAIResponses(model: Model<"openai-responses">, context: Context, options?: OpenAIResponsesOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/providers/openai-responses.ts:110` — `streamSimpleOpenAIResponses(model: Model<"openai-responses">, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/providers/register-builtins.ts`
- `packages/pi-ai/src/providers/register-builtins.ts:37` — `setBedrockProviderModule(module: BedrockProviderModule)` — exported: **yes** — Implements logic starting with: { bedrockProviderModuleOverride = module; } (missing: jsdoc)
- `packages/pi-ai/src/providers/register-builtins.ts:189` — `resetApiProviders()` — exported: **yes** — Implements logic starting with: { clearApiProviders(); registerBuiltInApiProviders(); registerFakeProviderIfEnabled(); } (missing: jsdoc)

### `packages/pi-ai/src/providers/simple-options.ts`
- `packages/pi-ai/src/providers/simple-options.ts:19` — `buildBaseOptions(model: Model<Api>, options?: SimpleStreamOptions, apiKey?: string)` — exported: **yes** — Implements logic starting with: { return { temperature: options?.temperature, maxTokens: options?.maxTokens || defaultMaxTokens(model), signal: options?.signal, apiKey: apiKey ||… (missing: jsdoc)
- `packages/pi-ai/src/providers/simple-options.ts:34` — `clampReasoning(effort: ThinkingLevel | undefined)` — exported: **yes** — Implements logic starting with: { return effort === "xhigh" ? "high" : effort; } (missing: jsdoc)
- `packages/pi-ai/src/providers/simple-options.ts:38` — `adjustMaxTokensForThinking(baseMaxTokens: number, modelMaxTokens: number, reasoningLevel: ThinkingLevel, customBudgets?: ThinkingBudgets)` — exported: **yes** — Implements logic starting with: { const defaultBudgets: ThinkingBudgets = { minimal: 1024, low: 2048, medium: 8192, high: 16384, }; const budgets = { ...defaultBudgets, ...customB… (missing: jsdoc)

### `packages/pi-ai/src/providers/think-tag-parser.ts`
- `packages/pi-ai/src/providers/think-tag-parser.ts:32` — `ThinkTagParser.consume(delta: string)` — exported: **yes** — Implements logic starting with: { this.state.pending += delta; return this.drain(false); } (missing: jsdoc)
- `packages/pi-ai/src/providers/think-tag-parser.ts:37` — `ThinkTagParser.flush()` — exported: **yes** — Implements logic starting with: { return this.drain(true); } (missing: jsdoc)

### `packages/pi-ai/src/stream.ts`
- `packages/pi-ai/src/stream.ts:25` — `stream(model: Model<TApi>, context: Context, options?: ProviderStreamOptions)` — exported: **yes** — Implements logic starting with: { const provider = resolveApiProvider(model.api); return provider.stream(model, context, options as StreamOptions); } (missing: jsdoc)
- `packages/pi-ai/src/stream.ts:34` — `complete(model: Model<TApi>, context: Context, options?: ProviderStreamOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/stream.ts:43` — `streamSimple(model: Model<TApi>, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Implements logic starting with: { const provider = resolveApiProvider(model.api); return provider.streamSimple(model, context, options); } (missing: jsdoc)
- `packages/pi-ai/src/stream.ts:52` — `completeSimple(model: Model<TApi>, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-ai/src/utils/event-stream.ts`
- `packages/pi-ai/src/utils/event-stream.ts:4` — `class EventStream` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:11` — `EventStream.constructor(private isComplete: (event: T) => boolean, private extractResult: (event: T) => R)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:20` — `EventStream.push(event: T)` — exported: **yes** — Implements logic starting with: { if (this.done) return; if (this.isComplete(event)) { this.done = true; this.resolveFinalResult(this.extractResult(event)); } // Deliver to waitin… (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:37` — `EventStream.end(result?: R)` — exported: **yes** — Implements logic starting with: { this.done = true; if (result !== undefined) { this.resolveFinalResult(result); } // Notify all waiting consumers that we're done while (this.wait… (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:49` — `EventStream.[Symbol.asyncIterator]()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:63` — `EventStream.result()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:68` — `class AssistantMessageEventStream` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-ai/src/utils/event-stream.ts:69` — `AssistantMessageEventStream.constructor()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-ai/src/utils/oauth/github-copilot.ts`
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:55` — `normalizeDomain(input: string)` — exported: **yes** — Implements logic starting with: { const trimmed = input.trim(); if (!trimmed) return null; try { const url = trimmed.includes("://") ? new URL(trimmed) : new URL(`https://${trimme… (missing: jsdoc)
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:92` — `getGitHubCopilotBaseUrl(token?: string, enterpriseDomain?: string)` — exported: **yes** — Implements logic starting with: { // If we have a token, extract the base URL from proxy-ep if (token) { const urlFromToken = getBaseUrlFromToken(token); if (urlFromToken) return… (missing: jsdoc)

### `packages/pi-ai/src/web-runtime-env-api-keys.ts`
- `packages/pi-ai/src/web-runtime-env-api-keys.ts:31` — `getEnvApiKey(provider: string)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-ai/src/web-runtime-env-api-keys.ts:32` — `getEnvApiKey(provider: string)` — exported: **yes** — Implements logic starting with: { if (provider === "github-copilot") { return process.env.COPILOT_GITHUB_TOKEN || process.env.GH_TOKEN || process.env.GITHUB_TOKEN; } if (provider… (missing: jsdoc)

### `packages/pi-coding-agent/src/cli/args.ts`
- `packages/pi-coding-agent/src/cli/args.ts:64` — `isValidThinkingLevel(level: string)` — exported: **yes** — Implements logic starting with: { return VALID_THINKING_LEVELS.includes(level as ThinkingLevel); } (missing: jsdoc)
- `packages/pi-coding-agent/src/cli/args.ts:68` — `parseArgs(args: string[], extensionFlags?: Map<string, { type: "boolean" | "string" }>)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/cli/args.ts:215` — `printHelp()` — exported: **yes** — Logs diagnostic information. (missing: jsdoc)

### `packages/pi-coding-agent/src/config.ts`
- `packages/pi-coding-agent/src/config.ts:52` — `getUpdateInstruction(packageName: string)` — exported: **yes** — Implements logic starting with: { const method = detectInstallMethod(); switch (method) { case "bun-binary": return `Download from: https://github.com/badlogic/pi-mono/releases/la… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/agent-session.ts`
- `packages/pi-coding-agent/src/core/agent-session.ts:239` — `class AgentSession` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/agent-session.ts:316` — `AgentSession.constructor(config: AgentSessionConfig)` — exported: **yes** — Implements logic starting with: { this.agent = config.agent; this.sessionManager = config.sessionManager; this.settingsManager = config.settingsManager; this._scopedModels = confi… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/agent-session.ts:1391` — `AgentSession.getRenderableToolDefinition(toolName: string)` — exported: **yes** — Implements logic starting with: { const normalizedToolName = toolName.toLowerCase(); return [...this._getBuiltinToolDefinitions(), ...this._getRegisteredToolDefinitions()].find( (… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/agent-session.ts:1627` — `AgentSession.get resourceLoader()` — exported: **yes** — Implements logic starting with: { return this._resourceLoader; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/agent-session.ts:2080` — `AgentSession.bindExtensions(bindings: ExtensionBindings)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/agent-session.ts:2426` — `AgentSession.reload()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/agent-session.ts:3033` — `AgentSession.getContextUsage()` — exported: **yes** — Implements logic starting with: { const model = this.model; if (!model) return undefined; const contextWindow = model.contextWindow ?? 0; if (contextWindow <= 0) return undefined;… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/artifact-manager.ts`
- `packages/pi-coding-agent/src/core/artifact-manager.ts:25` — `ArtifactManager.constructor(sessionFile: string)` — exported: **yes** — Implements logic starting with: { // Artifact directory is session file path without .jsonl extension this.#dir = sessionFile.slice(0, -6); } (missing: description)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:38` — `ArtifactManager.#ensureDir()` — exported: **yes** — Implements logic starting with: { if (!this.#dirCreated) { mkdirSync(this.#dir, { recursive: true }); this.#dirCreated = true; } if (!this.#initialized) { this.#scanExistingIds();… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/auth-storage.ts`
- `packages/pi-coding-agent/src/core/auth-storage.ts:93` — `class FileAuthStorageBackend` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:94` — `FileAuthStorageBackend.constructor(private authPath: string = join(getAgentDir(), "auth.json"))` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:110` — `FileAuthStorageBackend.withLock(fn: (current: string | undefined) => LockResult<T>)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:131` — `FileAuthStorageBackend.withLockAsync(fn: (current: string | undefined) => Promise<LockResult<T>>)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:175` — `class InMemoryAuthStorageBackend` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:178` — `InMemoryAuthStorageBackend.withLock(fn: (current: string | undefined) => LockResult<T>)` — exported: **yes** — Implements logic starting with: { const { result, next } = fn(this.value); if (next !== undefined) { this.value = next; } return result; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:186` — `InMemoryAuthStorageBackend.withLockAsync(fn: (current: string | undefined) => Promise<LockResult<T>>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:270` — `AuthStorage.create(authPath?: string)` — exported: **yes** — Implements logic starting with: { return new AuthStorage(new FileAuthStorageBackend(authPath ?? join(getAgentDir(), "auth.json"))); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:274` — `AuthStorage.fromStorage(storage: AuthStorageBackend)` — exported: **yes** — Implements logic starting with: { return new AuthStorage(storage); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:278` — `AuthStorage.inMemory(data: AuthStorageData = {})` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/auth-storage.ts:520` — `AuthStorage.drainErrors()` — exported: **yes** — Implements logic starting with: { const drained = [...this.errors]; this.errors = []; return drained; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/blob-store.ts`
- `packages/pi-coding-agent/src/core/blob-store.ts:21` — `class BlobStore` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/blob-store.ts:23` — `BlobStore.constructor(dir: string)` — exported: **yes** — Implements logic starting with: { this.dir = dir; mkdirSync(dir, { recursive: true }); } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/compaction-orchestrator.ts`
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:47` — `class CompactionOrchestrator` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:53` — `CompactionOrchestrator.constructor(private readonly _deps: CompactionOrchestratorDeps)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:78` — `CompactionOrchestrator.set branchSummaryAbortController(controller: AbortController | undefined)` — exported: **yes** — Implements logic starting with: { this._branchSummaryAbortController = controller; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/compaction/compaction.ts`
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:712` — `CompactionProducedNoSummaryError.constructor(message: string)` — exported: **yes** — Implements logic starting with: { super(message); this.name = "CompactionProducedNoSummaryError"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:791` — `prepareCompaction(pathEntries: SessionEntry[], settings: CompactionSettings)` — exported: **yes** — Implements logic starting with: { if (pathEntries.length > 0 && pathEntries[pathEntries.length - 1].type === "compaction") { return undefined; } let prevCompactionIndex = -1; for… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/compaction/utils.ts`
- `packages/pi-coding-agent/src/core/compaction/utils.ts:31` — `createFileOps()` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/contextual-tips.ts`
- `packages/pi-coding-agent/src/core/contextual-tips.ts:188` — `class ContextualTips` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/contextual-tips.ts:199` — `ContextualTips.get bashIncludedCount()` — exported: **yes** — Implements logic starting with: { return this._bashIncludedCount; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/discovery-cache.ts`
- `packages/pi-coding-agent/src/core/discovery-cache.ts:22` — `class ModelDiscoveryCache` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:26` — `ModelDiscoveryCache.constructor(cachePath?: string)` — exported: **yes** — Implements logic starting with: { this.cachePath = cachePath ?? join(getAgentDir(), "discovery-cache.json"); this.data = { version: 1, entries: {} }; this.load(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:32` — `ModelDiscoveryCache.get(provider: string)` — exported: **yes** — Implements logic starting with: { const entry = this.data.entries[provider]; return entry; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:37` — `ModelDiscoveryCache.set(provider: string, models: DiscoveredModel[], ttlMs?: number)` — exported: **yes** — Implements logic starting with: { // Re-read from disk to get the latest state before modifying this.load(); this.data.entries[provider] = { models, fetchedAt: Date.now(), ttlMs:… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:48` — `ModelDiscoveryCache.isStale(provider: string)` — exported: **yes** — Implements logic starting with: { const entry = this.data.entries[provider]; if (!entry) return true; return Date.now() - entry.fetchedAt > entry.ttlMs; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:54` — `ModelDiscoveryCache.clear(provider?: string)` — exported: **yes** — Implements logic starting with: { // Re-read from disk to get the latest state before modifying this.load(); if (provider) { delete this.data.entries[provider]; } else { this.data… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:65` — `ModelDiscoveryCache.getAll(includeStale = false)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:75` — `ModelDiscoveryCache.load()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/discovery-cache.ts:90` — `ModelDiscoveryCache.save()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/event-bus.ts`
- `packages/pi-coding-agent/src/core/event-bus.ts:12` — `createEventBus()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/extensions/extension-registry.ts`
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:71` — `getRegistryPath()` — exported: **yes** — Implements logic starting with: { return join(dirname(getAgentDir()), "extensions", "registry.json"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:81` — `loadRegistry()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:93` — `saveRegistry(registry: ExtensionRegistry)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:116` — `enableExtension(registry: ExtensionRegistry, id: string)` — exported: **yes** — Implements logic starting with: { const entry = registry.entries[id]; if (entry) { entry.enabled = true; delete entry.disabledAt; delete entry.disabledReason; } else { registry.en… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/extensions/loader.ts`
- `packages/pi-coding-agent/src/core/extensions/loader.ts:365` — `importExtensionModule(parentModuleUrl: string, specifier: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/extensions/project-trust.ts`
- `packages/pi-coding-agent/src/core/extensions/project-trust.ts:30` — `isProjectTrusted(projectPath: string, agentDir: string)` — exported: **yes** — Implements logic starting with: { const canonical = path.resolve(projectPath); return readTrustedProjects(agentDir).has(canonical); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/project-trust.ts:35` — `trustProject(projectPath: string, agentDir: string)` — exported: **yes** — Implements logic starting with: { const canonical = path.resolve(projectPath); const trusted = readTrustedProjects(agentDir); trusted.add(canonical); writeTrustedProjects(agentDir… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/project-trust.ts:42` — `getUntrustedExtensionPaths(projectPath: string, extensionPaths: string[], agentDir: string)` — exported: **yes** — Implements logic starting with: { if (isProjectTrusted(projectPath, agentDir)) { return []; } return extensionPaths; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/extensions/runner.ts`
- `packages/pi-coding-agent/src/core/extensions/runner.ts:224` — `class ExtensionRunner` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:260` — `ExtensionRunner.constructor(extensions: Extension[], runtime: ExtensionRuntime, cwd: string, sessionManager: SessionManager, modelRegistry: ModelRegistry)` — exported: **yes** — Implements logic starting with: { this.extensions = extensions; this.runtime = runtime; this.uiContext = noOpUIContext; this.cwd = cwd; this.sessionManager = sessionManager; this.… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:403` — `ExtensionRunner.bindCore(actions: ExtensionActions, contextActions: ExtensionContextActions)` — exported: **yes** — Implements logic starting with: { // Copy actions into the shared runtime (all extension APIs reference this) this.runtime.sendMessage = actions.sendMessage; this.runtime.sendUser… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:446` — `ExtensionRunner.bindCommandContext(actions?: ExtensionCommandContextActions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:465` — `ExtensionRunner.setUIContext(uiContext?: ExtensionUIContext)` — exported: **yes** — Implements logic starting with: { this.uiContext = uiContext ?? noOpUIContext; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:469` — `ExtensionRunner.getUIContext()` — exported: **yes** — Implements logic starting with: { return this.uiContext; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:473` — `ExtensionRunner.hasUI()` — exported: **yes** — Implements logic starting with: { return this.uiContext !== noOpUIContext; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:477` — `ExtensionRunner.getExtensionPaths()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:505` — `ExtensionRunner.getFlags()` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:517` — `ExtensionRunner.setFlagValue(name: string, value: boolean | string)` — exported: **yes** — Implements logic starting with: { this.runtime.flagValues.set(name, value); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:521` — `ExtensionRunner.getFlagValues()` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:525` — `ExtensionRunner.getShortcuts(effectiveKeybindings: Required<KeybindingsConfig>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:570` — `ExtensionRunner.getShortcutDiagnostics()` — exported: **yes** — Implements logic starting with: { return this.shortcutDiagnostics; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:574` — `ExtensionRunner.onError(listener: ExtensionErrorListener)` — exported: **yes** — Implements logic starting with: { this.errorListeners.add(listener); return () => this.errorListeners.delete(listener); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:579` — `ExtensionRunner.emitError(error: ExtensionError)` — exported: **yes** — Implements logic starting with: { for (const listener of this.errorListeners) { listener(error); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:585` — `ExtensionRunner.hasHandlers(eventType: string)` — exported: **yes** — Implements logic starting with: { for (const ext of this.extensions) { const handlers = ext.handlers.get(eventType); if (handlers && handlers.length > 0) { return true; } } return… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:595` — `ExtensionRunner.getMessageRenderer(customType: string)` — exported: **yes** — Implements logic starting with: { for (const ext of this.extensions) { const renderer = ext.messageRenderers.get(customType); if (renderer) { return renderer; } } return undefined… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:605` — `ExtensionRunner.getRegisteredCommands(reserved?: Set<string>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:657` — `ExtensionRunner.getCommandDiagnostics()` — exported: **yes** — Implements logic starting with: { return this.commandDiagnostics; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:661` — `ExtensionRunner.getRegisteredCommandsWithPaths()` — exported: **yes** — Implements logic starting with: { const result: Array<{ command: RegisteredCommand; extensionPath: string }> = []; for (const ext of this.extensions) { for (const command of ext.c… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:671` — `ExtensionRunner.getCommand(name: string)` — exported: **yes** — Implements logic starting with: { let protectedCommand: RegisteredCommand | undefined; for (const ext of this.extensions) { const command = ext.commands.get(name); if (command) {… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:736` — `ExtensionRunner.createCommandContext()` — exported: **yes** — Implements logic starting with: { return { ...this.createContext(), waitForIdle: () => this.waitForIdleFn(), newSession: (options) => this.newSessionHandler(options), fork: (entry… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:804` — `ExtensionRunner.emit(event: TEvent)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:819` — `ExtensionRunner.emitToolResult(event: ToolResultEvent)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:837` — `ExtensionRunner.emitToolCall(event: ToolCallEvent)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:851` — `ExtensionRunner.emitBashTransform(command: string, cwd: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:869` — `ExtensionRunner.emitUserBash(event: UserBashEvent)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:883` — `ExtensionRunner.emitContext(messages: AgentMessage[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:896` — `ExtensionRunner.emitBeforeProviderRequest(payload: unknown, model?: { provider: string; id: string; api?: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:914` — `ExtensionRunner.emitBeforeModelSelect(event: Omit<BeforeModelSelectEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:929` — `ExtensionRunner.emitAdjustToolSet(event: Omit<AdjustToolSetEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:944` — `ExtensionRunner.emitBeforeAgentStart(prompt: string, images: ImageContent[] | undefined, systemPrompt: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:979` — `ExtensionRunner.emitResourcesDiscover(cwd: string, reason: ResourcesDiscoverEvent["reason"])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1042` — `ExtensionRunner.emitStop(event: Omit<StopEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1050` — `ExtensionRunner.emitNotification(event: Omit<NotificationEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1058` — `ExtensionRunner.emitSessionEnd(event: Omit<SessionEndEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1066` — `ExtensionRunner.emitBeforeCommit(event: Omit<BeforeCommitEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1091` — `ExtensionRunner.emitCommit(event: Omit<CommitEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1099` — `ExtensionRunner.emitBeforePush(event: Omit<BeforePushEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1118` — `ExtensionRunner.emitPush(event: Omit<PushEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1126` — `ExtensionRunner.emitBeforePr(event: Omit<BeforePrEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1153` — `ExtensionRunner.emitPrOpened(event: Omit<PrOpenedEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1161` — `ExtensionRunner.emitBeforeVerify(event: Omit<BeforeVerifyEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1180` — `ExtensionRunner.emitVerifyResult(event: Omit<VerifyResultEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1188` — `ExtensionRunner.emitBudgetThreshold(event: Omit<BudgetThresholdEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1207` — `ExtensionRunner.emitMilestoneStart(event: Omit<MilestoneStartEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1215` — `ExtensionRunner.emitMilestoneEnd(event: Omit<MilestoneEndEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1223` — `ExtensionRunner.emitUnitStart(event: Omit<UnitStartEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1231` — `ExtensionRunner.emitUnitEnd(event: Omit<UnitEndEvent, "type">)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/extensions/types.ts`
- `packages/pi-coding-agent/src/core/extensions/types.ts:1106` — `isToolResultEventType(toolName: "read", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1107` — `isToolResultEventType(toolName: "edit", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1108` — `isToolResultEventType(toolName: "write", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1109` — `isToolResultEventType(toolName: "grep", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1110` — `isToolResultEventType(toolName: "find", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1111` — `isToolResultEventType(toolName: "ls", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1112` — `isToolResultEventType(toolName: TName, event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1116` — `isToolResultEventType(toolName: string, event: ToolResultEvent)` — exported: **yes** — Implements logic starting with: { return event.toolName === toolName; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1141` — `isToolCallEventType(toolName: "read", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1142` — `isToolCallEventType(toolName: "edit", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1143` — `isToolCallEventType(toolName: "write", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1144` — `isToolCallEventType(toolName: "grep", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1145` — `isToolCallEventType(toolName: "find", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1146` — `isToolCallEventType(toolName: "ls", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1147` — `isToolCallEventType(toolName: TName, event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1151` — `isToolCallEventType(toolName: string, event: ToolCallEvent)` — exported: **yes** — Implements logic starting with: { return event.toolName === toolName; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/fallback-resolver.ts`
- `packages/pi-coding-agent/src/core/fallback-resolver.ts:23` — `class FallbackResolver` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/fallback-resolver.ts:24` — `FallbackResolver.constructor(private settingsManager: SettingsManager, private authStorage: AuthStorage, private modelRegistry: ModelRegistry)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)

### `packages/pi-coding-agent/src/core/footer-data-provider.ts`
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:47` — `FooterDataProvider.constructor()` — exported: **yes** — Implements logic starting with: { this.setupGitWatcher(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/hooks-runner.ts`
- `packages/pi-coding-agent/src/core/hooks-runner.ts:85` — `isProjectHooksTrusted(cwd: string)` — exported: **yes** — Implements logic starting with: { return existsSync(join(cwd, CONFIG_DIR_NAME, TRUST_MARKER)); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/hooks-runner.ts:250` — `createHooksRunner(options: HooksRunnerOptions)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lifecycle-hooks.ts`
- `packages/pi-coding-agent/src/core/lifecycle-hooks.ts:65` — `readManifestRuntimeDeps(dir: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lifecycle-hooks.ts:76` — `collectRuntimeDependencies(installedPath: string, entryPaths: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lifecycle-hooks.ts:87` — `verifyRuntimeDependencies(runtimeDeps: string[], source: string, appName: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lifecycle-hooks.ts:102` — `resolveLocalSourcePath(source: string, cwd: string)` — exported: **yes** — Implements logic starting with: { const trimmed = source.trim(); if (!trimmed) return undefined; if (trimmed.startsWith("npm:")) return undefined; if (parseGitUrl(trimmed)) return… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lifecycle-hooks.ts:139` — `prepareLifecycleHooks(options: PackageLifecycleHooksOptions, target: LifecycleHooksTarget, prepareOptions?: PrepareLifecycleHooksOptions)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lifecycle-hooks.ts:221` — `runLifecycleHooks(loaded: LoadedLifecycleHooks | null, phase: LifecycleHookPhase)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lsp/client.ts`
- `packages/pi-coding-agent/src/core/lsp/client.ts:790` — `sendRequest(client: LspClient, method: string, params: unknown, signal?: AbortSignal, timeoutMs: number = DEFAULT_REQUEST_TIMEOUT_MS)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/client.ts:932` — `getActiveClients()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lsp/config.ts`
- `packages/pi-coding-agent/src/core/lsp/config.ts:150` — `hasRootMarkers(cwd: string, markers: string[])` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/config.ts:199` — `resolveLocalBinaryPath(command: string, cwd: string, isWindows: boolean)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/config.ts:218` — `which(command: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/config.ts:231` — `resolveCommand(command: string, cwd: string)` — exported: **yes** — Implements logic starting with: { const localPath = resolveLocalBinaryPath(command, cwd, process.platform === "win32"); if (localPath) return localPath; return which(command); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/config.ts:330` — `getServersForFile(config: LspConfig, filePath: string)` — exported: **yes** — Implements logic starting with: { const ext = path.extname(filePath).toLowerCase(); const fileName = path.basename(filePath).toLowerCase(); const matches: Array<[string, ServerCon… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/config.ts:354` — `getServerForFile(config: LspConfig, filePath: string)` — exported: **yes** — Implements logic starting with: { return getServersForFile(config, filePath)[0] ?? null; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lsp/helpers.ts`
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:6` — `ToolAbortError.constructor()` — exported: **yes** — Implements logic starting with: { super("Tool execution aborted"); this.name = "ToolAbortError"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:12` — `throwIfAborted(signal?: AbortSignal)` — exported: **yes** — Implements logic starting with: { if (signal?.aborted) { throw new ToolAbortError(); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:18` — `isEnoent(err: unknown)` — exported: **yes** — Implements logic starting with: { return (err as any)?.code === "ENOENT"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:22` — `isRecord(v: unknown)` — exported: **yes** — Implements logic starting with: { return typeof v === "object" && v !== null && !Array.isArray(v); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:26` — `clampTimeout(timeout?: number)` — exported: **yes** — Implements logic starting with: { return Math.max(5, Math.min(60, timeout ?? 20)); } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lsp/index.ts`
- `packages/pi-coding-agent/src/core/lsp/index.ts:88` — `warmupLspServers(cwd: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/index.ts:123` — `getLspStatus()` — exported: **yes** — Implements logic starting with: { return getActiveClients(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lsp/lspmux.ts`
- `packages/pi-coding-agent/src/core/lsp/lspmux.ts:116` — `detectLspmux()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/lspmux.ts:147` — `isLspmuxSupported(command: string)` — exported: **yes** — Implements logic starting with: { const baseName = command.split("/").pop() ?? command; return DEFAULT_SUPPORTED_SERVERS.has(baseName); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/lspmux.ts:187` — `getLspmuxCommand(command: string, args?: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/lsp/utils.ts`
- `packages/pi-coding-agent/src/core/lsp/utils.ts:160` — `fileToUri(filePath: string)` — exported: **yes** — Implements logic starting with: { const resolved = path.resolve(filePath); if (process.platform === "win32") { return `file:///${resolved.replace(/\\/g, "/")}`; } return `file://$… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:170` — `uriToFile(uri: string)` — exported: **yes** — Implements logic starting with: { if (!uri.startsWith("file://")) { return uri; } let filePath = decodeURIComponent(uri.slice(7)); if (process.platform === "win32" && filePath.sta… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:199` — `sortDiagnostics(diagnostics: Diagnostic[])` — exported: **yes** — Implements logic starting with: { return diagnostics.sort((a, b) => { const aSeverity = a.severity ?? 1; const bSeverity = b.severity ?? 1; if (aSeverity !== bSeverity) return aSe… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:227` — `formatDiagnostic(diagnostic: Diagnostic, filePath: string)` — exported: **yes** — Implements logic starting with: { const severity = severityToString(diagnostic.severity); const line = diagnostic.range.start.line + 1; const col = diagnostic.range.start.characte… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:240` — `formatGroupedDiagnosticMessages(messages: string[])` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:311` — `formatDiagnosticsSummary(diagnostics: Diagnostic[])` — exported: **yes** — Implements logic starting with: { const counts = { error: 0, warning: 0, info: 0, hint: 0 }; for (const d of diagnostics) { const sev = severityToString(d.severity); if (sev in co… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:334` — `formatLocation(location: Location, cwd: string)` — exported: **yes** — Implements logic starting with: { const file = path.relative(cwd, uriToFile(location.uri)); const line = location.range.start.line + 1; const col = location.range.start.character… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:345` — `formatWorkspaceEdit(edit: WorkspaceEdit, cwd: string)` — exported: **yes** — Implements logic starting with: { const results: string[] = []; if (edit.changes) { for (const [uri, textEdits] of Object.entries(edit.changes)) { const file = path.relative(cwd,… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:414` — `symbolKindToIcon(kind: SymbolKind)` — exported: **yes** — Implements logic starting with: { return `[${SYMBOL_KIND_LABELS[kind] ?? "?"}]`; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:418` — `formatDocumentSymbol(symbol: DocumentSymbol, indent = 0)` — exported: **yes** — Implements logic starting with: { const prefix = " ".repeat(indent); const icon = symbolKindToIcon(symbol.kind); const line = symbol.range.start.line + 1; const detail = symbol.de… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:434` — `formatSymbolInformation(symbol: SymbolInformation, cwd: string)` — exported: **yes** — Implements logic starting with: { const icon = symbolKindToIcon(symbol.kind); const location = formatLocation(symbol.location, cwd); const container = symbol.containerName ? ` (${… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:441` — `filterWorkspaceSymbols(symbols: SymbolInformation[], query: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:450` — `dedupeWorkspaceSymbols(symbols: SymbolInformation[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:469` — `formatCodeAction(action: CodeAction | Command, index: number)` — exported: **yes** — Implements logic starting with: { const kind = "kind" in action && action.kind ? action.kind : "action"; const preferred = "isPreferred" in action && action.isPreferred ? " (prefe… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:492` — `applyCodeAction(action: CodeAction | Command, dependencies: CodeActionApplyDependencies)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:526` — `hasGlobPattern(value: string)` — exported: **yes** — Implements logic starting with: { return GLOB_PATTERN_CHARS.test(value); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:530` — `collectGlobMatches(pattern: string, cwd: string, maxMatches: number)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:547` — `extractHoverText(contents: string | { kind: string; value: string } | { language: string; value: string } | unknown[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:596` — `resolveSymbolColumn(filePath: string, line: number, symbol?: string, occurrence?: number)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:631` — `readLocationContext(filePath: string, line: number, contextLines = 1)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:659` — `formatCallHierarchyItem(item: CallHierarchyItem, cwd: string)` — exported: **yes** — Implements logic starting with: { const icon = symbolKindToIcon(item.kind); const detail = item.detail ? ` ${item.detail}` : ""; const relPath = path.relative(cwd, uriToFile(item.… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/lsp/utils.ts:677` — `formatSignatureHelp(result: SignatureHelp)` — exported: **yes** — Implements logic starting with: { if (!result.signatures || result.signatures.length === 0) { return "No signature information"; } const activeIdx = result.activeSignature ?? 0; c… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/messages.ts`
- `packages/pi-coding-agent/src/core/messages.ts:106` — `createBranchSummaryMessage(summary: string, fromId: string, timestamp: string)` — exported: **yes** — Implements logic starting with: { return { role: "branchSummary", summary, fromId, timestamp: new Date(timestamp).getTime(), }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/messages.ts:115` — `createCompactionSummaryMessage(summary: string, tokensBefore: number, timestamp: string)` — exported: **yes** — Implements logic starting with: { return { role: "compactionSummary", summary: summary, tokensBefore, timestamp: new Date(timestamp).getTime(), }; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/model-discovery.ts`
- `packages/pi-coding-agent/src/core/model-discovery.ts:46` — `getDefaultTTL(provider: string)` — exported: **yes** — Implements logic starting with: { return DISCOVERY_TTLS[provider] ?? DISCOVERY_TTLS.default; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/model-discovery.ts:294` — `supportsDiscoveryForApi(api: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!api) return false; return OPENAI_COMPAT_DISCOVERY_APIS.has(api); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/model-discovery.ts:299` — `getDiscoveryAdapter(provider: string, providerApis?: Iterable<string>)` — exported: **yes** — Implements logic starting with: { const known = adapters[provider]; if (known) return known; if (providerApis) { for (const api of providerApis) { if (supportsDiscoveryForApi(api)… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/model-discovery.ts:314` — `getDiscoverableProviders()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/model-registry.ts`
- `packages/pi-coding-agent/src/core/model-registry.ts:253` — `ModelRegistry.constructor(readonly authStorage: AuthStorage, readonly modelsJsonPath: string | undefined = join(getAgentDir(), "models.json"))` — exported: **yes** — Implements logic starting with: { this.discoveryCache = new ModelDiscoveryCache(); // Set up fallback resolver for custom provider API keys this.authStorage.setFallbackResolver((p… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/models-json-writer.ts`
- `packages/pi-coding-agent/src/core/models-json-writer.ts:37` — `class ModelsJsonWriter` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/models-json-writer.ts:40` — `ModelsJsonWriter.constructor(modelsJsonPath?: string)` — exported: **yes** — Implements logic starting with: { this.modelsJsonPath = modelsJsonPath ?? join(getAgentDir(), "models.json"); } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/package-commands.ts`
- `packages/pi-coding-agent/src/core/package-commands.ts:41` — `getPackageCommandUsage(appName: string, command: PackageCommand)` — exported: **yes** — Implements logic starting with: { switch (command) { case "install": return `${appName} install <source> [-l]`; case "remove": return `${appName} remove <source> [-l]`; case "upda… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-commands.ts:109` — `parsePackageCommand(args: string[], allowedCommands?: ReadonlySet<PackageCommand>)` — exported: **yes** — Implements logic starting with: { const [command, ...rest] = args; if (command !== "install" && command !== "remove" && command !== "update" && command !== "list") { return undefi… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-commands.ts:151` — `runPackageCommand(options: PackageCommandRunnerOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/package-manager.ts`
- `packages/pi-coding-agent/src/core/package-manager.ts:636` — `class DefaultPackageManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:643` — `DefaultPackageManager.constructor(options: PackageManagerOptions)` — exported: **yes** — Implements logic starting with: { this.cwd = options.cwd; this.agentDir = options.agentDir; this.settingsManager = options.settingsManager; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:649` — `DefaultPackageManager.setProgressCallback(callback: ProgressCallback | undefined)` — exported: **yes** — Implements logic starting with: { this.progressCallback = callback; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:653` — `DefaultPackageManager.addSourceToSettings(source: string, options?: { local?: boolean })` — exported: **yes** — Implements logic starting with: { const scope: SourceScope = options?.local ? "project" : "user"; const currentSettings = scope === "project" ? this.settingsManager.getProjectSett… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:672` — `DefaultPackageManager.removeSourceFromSettings(source: string, options?: { local?: boolean })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:690` — `DefaultPackageManager.getInstalledPath(source: string, scope: "user" | "project")` — exported: **yes** — Implements logic starting with: { const parsed = this.parseSource(source); if (parsed.type === "npm") { const path = this.getNpmInstallPath(parsed, scope); return existsSync(path)… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:729` — `DefaultPackageManager.resolve(onMissing?: (source: string) => Promise<MissingSourceAction>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:783` — `DefaultPackageManager.resolveExtensionSources(sources: string[], options?: { local?: boolean; temporary?: boolean })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:794` — `DefaultPackageManager.install(source: string, options?: { local?: boolean })` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:817` — `DefaultPackageManager.remove(source: string, options?: { local?: boolean })` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/package-manager.ts:836` — `DefaultPackageManager.update(source?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/redact-secrets.ts`
- `packages/pi-coding-agent/src/core/redact-secrets.ts:36` — `redactSecrets(input: string)` — exported: **yes** — Implements logic starting with: { // Short-circuit: skip regex work on strings with no plausible secret markers. // Cheap heuristic — if none of these substrings are present, no p… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/resource-loader.ts`
- `packages/pi-coding-agent/src/core/resource-loader.ts:159` — `class DefaultResourceLoader` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:213` — `DefaultResourceLoader.constructor(options: DefaultResourceLoaderOptions)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:260` — `DefaultResourceLoader.getExtensions()` — exported: **yes** — Implements logic starting with: { return this.extensionsResult; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:264` — `DefaultResourceLoader.getSkills()` — exported: **yes** — Implements logic starting with: { return { skills: this.skills, diagnostics: this.skillDiagnostics }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:268` — `DefaultResourceLoader.getPrompts()` — exported: **yes** — Implements logic starting with: { return { prompts: this.prompts, diagnostics: this.promptDiagnostics }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:272` — `DefaultResourceLoader.getThemes()` — exported: **yes** — Implements logic starting with: { return { themes: this.themes, diagnostics: this.themeDiagnostics }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:276` — `DefaultResourceLoader.getAgentsFiles()` — exported: **yes** — Implements logic starting with: { return { agentsFiles: this.agentsFiles }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:280` — `DefaultResourceLoader.getSystemPrompt()` — exported: **yes** — Implements logic starting with: { return this.systemPrompt; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:284` — `DefaultResourceLoader.getAppendSystemPrompt()` — exported: **yes** — Implements logic starting with: { return this.appendSystemPrompt; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:288` — `DefaultResourceLoader.getPathMetadata()` — exported: **yes** — Implements logic starting with: { return this.pathMetadata; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:292` — `DefaultResourceLoader.extendResources(paths: ResourceExtensionPaths)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/resource-loader.ts:322` — `DefaultResourceLoader.reload()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/retry-handler.ts`
- `packages/pi-coding-agent/src/core/retry-handler.ts:39` — `class RetryHandler` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/retry-handler.ts:47` — `RetryHandler.constructor(private readonly _deps: RetryHandlerDeps)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)

### `packages/pi-coding-agent/src/core/sdk.ts`
- `packages/pi-coding-agent/src/core/sdk.ts:26` — `CredentialCooldownError.constructor(provider: string, retryAfterMs?: number)` — exported: **yes** — Implements logic starting with: { super( `All credentials for "${provider}" are in a cooldown window. ` + `Please wait a moment and try again, or switch to a different provider.`,… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/sdk.ts:36` — `canRestoreSessionModel(modelRegistry: Pick<ModelRegistry, "isProviderRequestReady">, model: Model<any>)` — exported: **yes** — Implements logic starting with: { return modelRegistry.isProviderRequestReady(model.provider); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/sdk.ts:57` — `filterToolsForProviderRequest(tools: AgentTool[], model: Pick<Model<any>, "api" | "provider">)` — exported: **yes** — Implements logic starting with: { const providerCaps = getProviderCapabilities(model.api); if (!providerCaps.toolCalling) { return { compatible: [], filtered: tools }; } const com… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/sdk.ts:133` — `getAdjustToolSetRequestCustomMessages(messages: readonly AgentMessage[] | undefined)` — exported: **yes** — Implements logic starting with: { if (!messages) return []; const requestMessages: Array<{ index: number; customType: string }> = []; for (let index = messages.length - 1; index >… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/session-manager.ts`
- `packages/pi-coding-agent/src/core/session-manager.ts:339` — `getLatestCompactionEntry(entries: SessionEntry[])` — exported: **yes** — Implements logic starting with: { for (let i = entries.length - 1; i >= 0; i--) { if (entries[i].type === "compaction") { return entries[i] as CompactionEntry; } } return null; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:912` — `SessionManager.newSession(options?: NewSessionOptions)` — exported: **yes** — Implements logic starting with: { this.sessionId = randomUUID(); const timestamp = new Date().toISOString(); const header: SessionHeader = { type: "session", version: CURRENT_SESS… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:972` — `SessionManager.isPersisted()` — exported: **yes** — Implements logic starting with: { return this.persist; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:976` — `SessionManager.getCwd()` — exported: **yes** — Implements logic starting with: { return this.cwd; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:980` — `SessionManager.getSessionDir()` — exported: **yes** — Implements logic starting with: { return this.sessionDir; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:984` — `SessionManager.getSessionId()` — exported: **yes** — Implements logic starting with: { return this.sessionId; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:988` — `SessionManager.getSessionFile()` — exported: **yes** — Implements logic starting with: { return this.sessionFile; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:992` — `SessionManager._persist(entry: SessionEntry)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:1186` — `SessionManager.getLeafId()` — exported: **yes** — Implements logic starting with: { return this.leafId; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:1190` — `SessionManager.getLeafEntry()` — exported: **yes** — Implements logic starting with: { return this.leafId ? this.byId.get(this.leafId) : undefined; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:1194` — `SessionManager.getEntry(id: string)` — exported: **yes** — Implements logic starting with: { return this.byId.get(id); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/session-manager.ts:1285` — `SessionManager.getUsageTotals()` — exported: **yes** — Implements logic starting with: { return { ...this.usageTotals }; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/settings-manager.ts`
- `packages/pi-coding-agent/src/core/settings-manager.ts:366` — `class SettingsManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:494` — `SettingsManager.getGlobalSettings()` — exported: **yes** — Implements logic starting with: { return structuredClone(this.globalSettings); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:498` — `SettingsManager.getProjectSettings()` — exported: **yes** — Implements logic starting with: { return structuredClone(this.projectSettings); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:502` — `SettingsManager.getBashInterceptorEnabled()` — exported: **yes** — Implements logic starting with: { return this.settings.bashInterceptor?.enabled ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:506` — `SettingsManager.getBashInterceptorRules()` — exported: **yes** — Implements logic starting with: { return this.settings.bashInterceptor?.rules; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:510` — `SettingsManager.reload()` — exported: **yes** — Implements logic starting with: { const globalLoad = SettingsManager.tryLoadFromStorage(this.storage, "global"); if (!globalLoad.error) { this.globalSettings = globalLoad.settings… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:672` — `SettingsManager.flush()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:676` — `SettingsManager.drainErrors()` — exported: **yes** — Implements logic starting with: { const drained = [...this.errors]; this.errors = []; return drained; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:726` — `SettingsManager.getLastChangelogVersion()` — exported: **yes** — Implements logic starting with: { return this.settings.lastChangelogVersion; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:730` — `SettingsManager.setLastChangelogVersion(version: string)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("lastChangelogVersion", version); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:734` — `SettingsManager.getDefaultProvider()` — exported: **yes** — Implements logic starting with: { return this.settings.defaultProvider; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:738` — `SettingsManager.getDefaultModel()` — exported: **yes** — Implements logic starting with: { return this.settings.defaultModel; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:742` — `SettingsManager.setDefaultProvider(provider: string)` — exported: **yes** — Implements logic starting with: { this.setScopedSetting("defaultProvider", provider); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:746` — `SettingsManager.setDefaultModel(modelId: string)` — exported: **yes** — Implements logic starting with: { this.setScopedSetting("defaultModel", modelId); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:750` — `SettingsManager.setDefaultModelAndProvider(provider: string, modelId: string)` — exported: **yes** — Implements logic starting with: { if (this.hasProjectSettings()) { this.projectSettings.defaultProvider = provider; this.projectSettings.defaultModel = modelId; this.markProjectMo… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:766` — `SettingsManager.getSteeringMode()` — exported: **yes** — Implements logic starting with: { return this.settings.steeringMode || "one-at-a-time"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:770` — `SettingsManager.setSteeringMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("steeringMode", mode); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:774` — `SettingsManager.getFollowUpMode()` — exported: **yes** — Implements logic starting with: { return this.settings.followUpMode || "one-at-a-time"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:778` — `SettingsManager.setFollowUpMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("followUpMode", mode); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:782` — `SettingsManager.getTheme()` — exported: **yes** — Implements logic starting with: { return this.settings.theme; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:786` — `SettingsManager.setTheme(theme: string)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("theme", theme); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:790` — `SettingsManager.getDefaultThinkingLevel()` — exported: **yes** — Implements logic starting with: { return this.settings.defaultThinkingLevel; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:794` — `SettingsManager.setDefaultThinkingLevel(level: "off" | "minimal" | "low" | "medium" | "high" | "xhigh")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("defaultThinkingLevel", level); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:798` — `SettingsManager.getTransport()` — exported: **yes** — Implements logic starting with: { return this.settings.transport ?? "sse"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:802` — `SettingsManager.setTransport(transport: TransportSetting)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("transport", transport); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:806` — `SettingsManager.getCompactionEnabled()` — exported: **yes** — Implements logic starting with: { return this.settings.compaction?.enabled ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:810` — `SettingsManager.setCompactionEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("compaction", "enabled", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:814` — `SettingsManager.getCompactionReserveTokens()` — exported: **yes** — Implements logic starting with: { return this.settings.compaction?.reserveTokens ?? COMPACTION_RESERVE_TOKENS; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:818` — `SettingsManager.getCompactionKeepRecentTokens()` — exported: **yes** — Implements logic starting with: { return this.settings.compaction?.keepRecentTokens ?? COMPACTION_KEEP_RECENT_TOKENS; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:822` — `SettingsManager.getCompactionThresholdPercent()` — exported: **yes** — Implements logic starting with: { return this.settings.compaction?.thresholdPercent; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:847` — `SettingsManager.getCompactionSettings()` — exported: **yes** — Implements logic starting with: { return { enabled: this.getCompactionEnabled(), reserveTokens: this.getCompactionReserveTokens(), keepRecentTokens: this.getCompactionKeepRecentTo… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:861` — `SettingsManager.getBranchSummarySettings()` — exported: **yes** — Implements logic starting with: { return { reserveTokens: this.settings.branchSummary?.reserveTokens ?? COMPACTION_RESERVE_TOKENS, skipPrompt: this.settings.branchSummary?.skipPro… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:868` — `SettingsManager.getBranchSummarySkipPrompt()` — exported: **yes** — Implements logic starting with: { return this.settings.branchSummary?.skipPrompt ?? false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:872` — `SettingsManager.getRetryEnabled()` — exported: **yes** — Implements logic starting with: { return this.settings.retry?.enabled ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:876` — `SettingsManager.setRetryEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("retry", "enabled", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:880` — `SettingsManager.getRetrySettings()` — exported: **yes** — Implements logic starting with: { return { enabled: this.getRetryEnabled(), maxRetries: this.settings.retry?.maxRetries ?? 3, baseDelayMs: this.settings.retry?.baseDelayMs ?? RETR… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:889` — `SettingsManager.getHideThinkingBlock()` — exported: **yes** — Implements logic starting with: { return this.settings.hideThinkingBlock ?? false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:893` — `SettingsManager.setHideThinkingBlock(hide: boolean)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("hideThinkingBlock", hide); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:897` — `SettingsManager.getShellPath()` — exported: **yes** — Implements logic starting with: { return this.settings.shellPath; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:901` — `SettingsManager.setShellPath(path: string | undefined)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("shellPath", path); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:905` — `SettingsManager.getQuietStartup()` — exported: **yes** — Implements logic starting with: { return this.settings.quietStartup ?? false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:909` — `SettingsManager.setQuietStartup(quiet: boolean)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("quietStartup", quiet); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:913` — `SettingsManager.getShellCommandPrefix()` — exported: **yes** — Implements logic starting with: { return this.settings.shellCommandPrefix; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:917` — `SettingsManager.setShellCommandPrefix(prefix: string | undefined)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("shellCommandPrefix", prefix); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:921` — `SettingsManager.getCollapseChangelog()` — exported: **yes** — Implements logic starting with: { return this.settings.collapseChangelog ?? false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:925` — `SettingsManager.setCollapseChangelog(collapse: boolean)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("collapseChangelog", collapse); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:929` — `SettingsManager.getPackages()` — exported: **yes** — Implements logic starting with: { return [...(this.settings.packages ?? [])]; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:933` — `SettingsManager.setPackages(packages: PackageSource[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("packages", packages); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:937` — `SettingsManager.setProjectPackages(packages: PackageSource[])` — exported: **yes** — Implements logic starting with: { this.setProjectSetting("packages", packages); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:941` — `SettingsManager.getExtensionPaths()` — exported: **yes** — Implements logic starting with: { return [...(this.settings.extensions ?? [])]; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:945` — `SettingsManager.setExtensionPaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("extensions", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:949` — `SettingsManager.setProjectExtensionPaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setProjectSetting("extensions", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:953` — `SettingsManager.getSkillPaths()` — exported: **yes** — Implements logic starting with: { return [...(this.settings.skills ?? [])]; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:957` — `SettingsManager.setSkillPaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("skills", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:961` — `SettingsManager.setProjectSkillPaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setProjectSetting("skills", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:965` — `SettingsManager.getPromptTemplatePaths()` — exported: **yes** — Implements logic starting with: { return [...(this.settings.prompts ?? [])]; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:969` — `SettingsManager.setPromptTemplatePaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("prompts", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:973` — `SettingsManager.setProjectPromptTemplatePaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setProjectSetting("prompts", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:977` — `SettingsManager.getThemePaths()` — exported: **yes** — Implements logic starting with: { return [...(this.settings.themes ?? [])]; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:981` — `SettingsManager.setThemePaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("themes", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:985` — `SettingsManager.setProjectThemePaths(paths: string[])` — exported: **yes** — Implements logic starting with: { this.setProjectSetting("themes", paths); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:989` — `SettingsManager.getEnableSkillCommands()` — exported: **yes** — Implements logic starting with: { return this.settings.enableSkillCommands ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:993` — `SettingsManager.setEnableSkillCommands(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("enableSkillCommands", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:997` — `SettingsManager.getThinkingBudgets()` — exported: **yes** — Implements logic starting with: { return this.settings.thinkingBudgets; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1001` — `SettingsManager.getShowImages()` — exported: **yes** — Implements logic starting with: { return this.settings.terminal?.showImages ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1005` — `SettingsManager.setShowImages(show: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("terminal", "showImages", show); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1009` — `SettingsManager.getClearOnShrink()` — exported: **yes** — Implements logic starting with: { // Settings takes precedence, then env var, then default false if (this.settings.terminal?.clearOnShrink !== undefined) { return this.settings.te… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1017` — `SettingsManager.setClearOnShrink(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("terminal", "clearOnShrink", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1021` — `SettingsManager.getAdaptiveMode()` — exported: **yes** — Implements logic starting with: { const mode = this.settings.terminal?.adaptiveMode; const valid: AdaptiveTuiMode[] = ["auto", "chat", "workflow", "validation", "debug", "compact"… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1027` — `SettingsManager.setAdaptiveMode(mode: AdaptiveTuiMode)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("terminal", "adaptiveMode", mode); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1031` — `SettingsManager.getImageAutoResize()` — exported: **yes** — Implements logic starting with: { return this.settings.images?.autoResize ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1035` — `SettingsManager.setImageAutoResize(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("images", "autoResize", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1039` — `SettingsManager.getBlockImages()` — exported: **yes** — Implements logic starting with: { return this.settings.images?.blockImages ?? false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1043` — `SettingsManager.setBlockImages(blocked: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("images", "blockImages", blocked); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1047` — `SettingsManager.getEnabledModels()` — exported: **yes** — Implements logic starting with: { return this.settings.enabledModels; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1051` — `SettingsManager.setEnabledModels(patterns: string[] | undefined)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("enabledModels", patterns); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1055` — `SettingsManager.getDoubleEscapeAction()` — exported: **yes** — Implements logic starting with: { return this.settings.doubleEscapeAction ?? "tree"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1059` — `SettingsManager.setDoubleEscapeAction(action: "fork" | "tree" | "none")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("doubleEscapeAction", action); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1063` — `SettingsManager.getTreeFilterMode()` — exported: **yes** — Implements logic starting with: { const mode = this.settings.treeFilterMode; const valid = ["default", "no-tools", "user-only", "labeled-only", "all"]; return mode && valid.includ… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1069` — `SettingsManager.setTreeFilterMode(mode: "default" | "no-tools" | "user-only" | "labeled-only" | "all")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("treeFilterMode", mode); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1073` — `SettingsManager.getShowHardwareCursor()` — exported: **yes** — Implements logic starting with: { return this.settings.showHardwareCursor ?? process.env.PI_HARDWARE_CURSOR === "1"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1077` — `SettingsManager.setShowHardwareCursor(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("showHardwareCursor", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1081` — `SettingsManager.getEditorPaddingX()` — exported: **yes** — Implements logic starting with: { return this.settings.editorPaddingX ?? 0; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1085` — `SettingsManager.setEditorPaddingX(padding: number)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("editorPaddingX", Math.max(0, Math.min(3, Math.floor(padding)))); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1089` — `SettingsManager.getAutocompleteMaxVisible()` — exported: **yes** — Implements logic starting with: { return this.settings.autocompleteMaxVisible ?? 5; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1093` — `SettingsManager.setAutocompleteMaxVisible(maxVisible: number)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("autocompleteMaxVisible", Math.max(3, Math.min(20, Math.floor(maxVisible)))); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1097` — `SettingsManager.getRespectGitignoreInPicker()` — exported: **yes** — Implements logic starting with: { return this.settings.respectGitignoreInPicker ?? true; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1101` — `SettingsManager.setRespectGitignoreInPicker(value: boolean)` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("respectGitignoreInPicker", value); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1105` — `SettingsManager.getSearchExcludeDirs()` — exported: **yes** — Implements logic starting with: { return this.settings.searchExcludeDirs ?? []; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1109` — `SettingsManager.setSearchExcludeDirs(dirs: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1113` — `SettingsManager.getCodeBlockIndent()` — exported: **yes** — Implements logic starting with: { return this.settings.markdown?.codeBlockIndent ?? " "; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1117` — `SettingsManager.getMemorySettings()` — exported: **yes** — Implements logic starting with: { return { enabled: this.settings.memory?.enabled ?? false, maxRolloutsPerStartup: this.settings.memory?.maxRolloutsPerStartup ?? 64, maxRolloutAge… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1135` — `SettingsManager.getAsyncEnabled()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1139` — `SettingsManager.getAsyncMaxJobs()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1143` — `SettingsManager.getTaskIsolationMode()` — exported: **yes** — Implements logic starting with: { return this.settings.taskIsolation?.mode ?? "none"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1147` — `SettingsManager.getTaskIsolationMerge()` — exported: **yes** — Implements logic starting with: { return this.settings.taskIsolation?.merge ?? "patch"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1151` — `SettingsManager.getFallbackEnabled()` — exported: **yes** — Implements logic starting with: { return this.settings.fallback?.enabled ?? false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1155` — `SettingsManager.setFallbackEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("fallback", "enabled", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1159` — `SettingsManager.getFallbackChains()` — exported: **yes** — Implements logic starting with: { return this.settings.fallback?.chains ?? {}; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1163` — `SettingsManager.getFallbackChain(name: string)` — exported: **yes** — Implements logic starting with: { return this.settings.fallback?.chains?.[name]; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1167` — `SettingsManager.setFallbackChain(name: string, entries: FallbackChainEntry[])` — exported: **yes** — Implements logic starting with: { if (!this.globalSettings.fallback) { this.globalSettings.fallback = {}; } if (!this.globalSettings.fallback.chains) { this.globalSettings.fallbac… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1180` — `SettingsManager.removeFallbackChain(name: string)` — exported: **yes** — Implements logic starting with: { if (!this.globalSettings.fallback?.chains?.[name]) { return false; } delete this.globalSettings.fallback.chains[name]; if (Object.keys(this.globa… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1193` — `SettingsManager.getFallbackSettings()` — exported: **yes** — Implements logic starting with: { return { enabled: this.getFallbackEnabled(), chains: this.getFallbackChains(), }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1200` — `SettingsManager.getModelDiscoverySettings()` — exported: **yes** — Implements logic starting with: { return this.settings.modelDiscovery ?? {}; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1204` — `SettingsManager.setModelDiscoveryEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.setNestedGlobalSetting("modelDiscovery", "enabled", enabled); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1208` — `SettingsManager.getEditMode()` — exported: **yes** — Implements logic starting with: { return this.settings.editMode ?? "standard"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1212` — `SettingsManager.setEditMode(mode: "standard" | "hashline")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("editMode", mode); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1216` — `SettingsManager.getTimestampFormat()` — exported: **yes** — Implements logic starting with: { return this.settings.timestampFormat ?? "date-time-iso"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1220` — `SettingsManager.setTimestampFormat(format: "date-time-iso" | "date-time-us")` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("timestampFormat", format); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1232` — `SettingsManager.setAllowedCommandPrefixes(prefixes: string[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("allowedCommandPrefixes", prefixes); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1244` — `SettingsManager.setFetchAllowedUrls(urls: string[])` — exported: **yes** — Implements logic starting with: { this.setGlobalSetting("fetchAllowedUrls", urls); } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/skills.ts`
- `packages/pi-coding-agent/src/core/skills.ts:104` — `getLoadedSkills()` — exported: **yes** — Implements logic starting with: { return [...loadedSkills]; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/timings.ts`
- `packages/pi-coding-agent/src/core/timings.ts:10` — `time(label: string)` — exported: **yes** — Implements logic starting with: { if (!ENABLED) return; const now = Date.now(); timings.push({ label, ms: now - lastTime }); lastTime = now; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/timings.ts:17` — `printTimings()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/bash.ts`
- `packages/pi-coding-agent/src/core/tools/bash.ts:274` — `createBashTool(cwd: string, options?: BashToolOptions)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/edit-diff.ts`
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:14` — `detectLineEnding(content: string)` — exported: **yes** — Implements logic starting with: { const crlfIdx = content.indexOf("\r\n"); const lfIdx = content.indexOf("\n"); if (lfIdx === -1) return "\n"; if (crlfIdx === -1) return "\n"; ret… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:22` — `normalizeToLF(text: string)` — exported: **yes** — Implements logic starting with: { return text.replace(/\r\n/g, "\n").replace(/\r/g, "\n"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:26` — `restoreLineEndings(text: string, ending: "\r\n" | "\n")` — exported: **yes** — Implements logic starting with: { return ending === "\r\n" ? text.replace(/\n/g, "\r\n") : text; } (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/edit.ts`
- `packages/pi-coding-agent/src/core/tools/edit.ts:58` — `createEditTool(cwd: string, options?: EditToolOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/find.ts`
- `packages/pi-coding-agent/src/core/tools/find.ts:53` — `createFindTool(cwd: string, options?: FindToolOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/grep.ts`
- `packages/pi-coding-agent/src/core/tools/grep.ts:65` — `createGrepTool(cwd: string, options?: GrepToolOptions)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/hashline-edit.ts`
- `packages/pi-coding-agent/src/core/tools/hashline-edit.ts:142` — `createHashlineEditTool(cwd: string, options?: HashlineEditToolOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/hashline-read.ts`
- `packages/pi-coding-agent/src/core/tools/hashline-read.ts:56` — `createHashlineReadTool(cwd: string, options?: HashlineReadToolOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/hashline.ts`
- `packages/pi-coding-agent/src/core/tools/hashline.ts:117` — `HashlineMismatchError.constructor(mismatches: HashMismatch[], fileLines: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:133` — `HashlineMismatchError.formatMessage(mismatches: HashMismatch[], fileLines: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/ls.ts`
- `packages/pi-coding-agent/src/core/tools/ls.ts:48` — `createLsTool(cwd: string, options?: LsToolOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/path-utils.ts`
- `packages/pi-coding-agent/src/core/tools/path-utils.ts:39` — `expandPath(filePath: string)` — exported: **yes** — Implements logic starting with: { const normalized = normalizeUnicodeSpaces(normalizeAtPrefix(filePath)); if (normalized === "~") { return os.homedir(); } if (normalized.startsWit… (missing: jsdoc)
- `packages/pi-coding-agent/src/core/tools/path-utils.ts:74` — `resolveReadPath(filePath: string, cwd: string)` — exported: **yes** — Implements logic starting with: { const resolved = resolveToCwd(filePath, cwd); if (fileExists(resolved)) { return resolved; } // Try macOS AM/PM variant (narrow no-break space be… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/read.ts`
- `packages/pi-coding-agent/src/core/tools/read.ts:51` — `createReadTool(cwd: string, options?: ReadToolOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/tool-target.ts`
- `packages/pi-coding-agent/src/core/tools/tool-target.ts:23` — `createToolTarget(target: ToolTargetMetadata)` — exported: **yes** — Implements logic starting with: { return target; } (missing: jsdoc)
- `packages/pi-coding-agent/src/core/tools/tool-target.ts:27` — `createReadFileTarget(inputPath: string, resolvedPath: string, offset?: number, limit?: number)` — exported: **yes** — Implements logic starting with: { const effectiveOffset = Math.max(1, offset ?? 1); return createToolTarget({ kind: "file", action: "read", inputPath, resolvedPath, range: offset… (missing: jsdoc)

### `packages/pi-coding-agent/src/core/tools/write.ts`
- `packages/pi-coding-agent/src/core/tools/write.ts:41` — `createWriteTool(cwd: string, options?: WriteToolOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/main.ts`
- `packages/pi-coding-agent/src/main.ts:353` — `main(args: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/adaptive-layout.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/adaptive-layout.ts:19` — `class AdaptiveLayoutComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/adaptive-layout.ts:20` — `AdaptiveLayoutComponent.constructor(private readonly getState: () => AdaptiveLayoutState)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/adaptive-layout.ts:22` — `AdaptiveLayoutComponent.invalidate()` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/adaptive-layout.ts:24` — `AdaptiveLayoutComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { const state = this.getState(); const mode = resolveTuiMode({ terminalWidth: width, override: state.override, activeToolCount: state.activeToolCou… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/armin.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/armin.ts:60` — `class ArminComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/armin.ts:72` — `ArminComponent.constructor(ui: TUI)` — exported: **yes** — Implements logic starting with: { this.ui = ui; this.effect = EFFECTS[Math.floor(Math.random() * EFFECTS.length)]; this.finalGrid = buildFinalGrid(); this.currentGrid = this.creat… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/armin.ts:82` — `ArminComponent.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedWidth = 0; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/armin.ts:86` — `ArminComponent.render(width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/armin.ts:379` — `ArminComponent.dispose()` — exported: **yes** — Implements logic starting with: { this.stopAnimation(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:29` — `AssistantMessageComponent.constructor(message?: AssistantMessage, hideThinkingBlock = false, markdownTheme: MarkdownTheme = getMarkdownTheme(), timestampFormat: TimestampFormat = "date-time-iso", range?: ContentRange)` — exported: **yes** — Implements logic starting with: { super(); this.hideThinkingBlock = hideThinkingBlock; this.markdownTheme = markdownTheme; this.timestampFormat = timestampFormat; this.range = ran… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:56` — `AssistantMessageComponent.setRange(range: ContentRange | undefined)` — exported: **yes** — Implements logic starting with: { this.range = range; if (this.lastMessage) { this.updateContent(this.lastMessage); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:63` — `AssistantMessageComponent.setShowMetadata(show: boolean)` — exported: **yes** — Implements logic starting with: { this.showMetadata = show; if (this.lastMessage) { this.updateContent(this.lastMessage); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:70` — `AssistantMessageComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); if (this.lastMessage) { this.updateContent(this.lastMessage); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:77` — `AssistantMessageComponent.setHideThinkingBlock(hide: boolean)` — exported: **yes** — Implements logic starting with: { this.hideThinkingBlock = hide; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:81` — `AssistantMessageComponent.updateContent(message: AssistantMessage)` — exported: **yes** — Implements logic starting with: { this.lastMessage = message; // Clear content container this.contentContainer.clear(); const start = this.range?.startIndex ?? 0; const end = this… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:166` — `AssistantMessageComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { const frameWidth = Math.max(20, width); const contentWidth = Math.max(1, frameWidth - 2); const lines = super.render(contentWidth); const metaPar… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:21` — `class BashExecutionComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:34` — `BashExecutionComponent.constructor(command: string, ui: TUI, excludeFromContext = false)` — exported: **yes** — Implements logic starting with: { super(); this.command = command; this.ui = ui; // Use dim border for excluded-from-context commands (!! prefix) const colorKey = excludeFromConte… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:79` — `BashExecutionComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); this.updateDisplay(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:84` — `BashExecutionComponent.appendOutput(chunk: string)` — exported: **yes** — Implements logic starting with: { // Strip ANSI codes and normalize line endings // Note: binary data is already sanitized in tui-renderer.ts executeBashCommand const clean = stri… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:102` — `BashExecutionComponent.setComplete(exitCode: number | undefined, cancelled: boolean, truncationResult?: TruncationResult, fullOutputPath?: string)` — exported: **yes** — Implements logic starting with: { this.exitCode = exitCode; this.status = cancelled ? "cancelled" : exitCode !== 0 && exitCode !== undefined && exitCode !== null ? "error" : "comp… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:123` — `BashExecutionComponent.render(width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts:12` — `BorderedLoader.constructor(tui: TUI, theme: Theme, message: string, options?: { cancellable?: boolean })` — exported: **yes** — Implements logic starting with: { super(); this.cancellable = options?.cancellable ?? true; const borderColor = (s: string) => theme.fg("border", s); this.addChild(new DynamicBord… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts:42` — `BorderedLoader.get signal()` — exported: **yes** — Implements logic starting with: { if (this.cancellable) { return (this.loader as CancellableLoader).signal; } return this.signalController?.signal ?? new AbortController().signal;… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts:49` — `BorderedLoader.set onAbort(fn: (() => void) | undefined)` — exported: **yes** — Implements logic starting with: { if (this.cancellable) { (this.loader as CancellableLoader).onAbort = fn; } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts:55` — `BorderedLoader.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if (this.cancellable) { (this.loader as CancellableLoader).handleInput(data); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts:61` — `BorderedLoader.dispose()` — exported: **yes** — Implements logic starting with: { if ("dispose" in this.loader && typeof this.loader.dispose === "function") { this.loader.dispose(); } } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/branch-summary-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/branch-summary-message.ts:15` — `BranchSummaryMessageComponent.constructor(message: BranchSummaryMessage, markdownTheme: MarkdownTheme = getMarkdownTheme())` — exported: **yes** — Implements logic starting with: { super(1, 1, (t) => theme.bg("customMessageBg", t)); this.message = message; this.markdownTheme = markdownTheme; this.updateDisplay(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/branch-summary-message.ts:22` — `BranchSummaryMessageComponent.setExpanded(expanded: boolean)` — exported: **yes** — Implements logic starting with: { this.expanded = expanded; this.updateDisplay(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/branch-summary-message.ts:27` — `BranchSummaryMessageComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); this.updateDisplay(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/chat-frame.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/chat-frame.ts:16` — `renderChatFrame(contentLines: string[], width: number, opts: { label: string; tone: FrameTone; timestamp?: number; timestampFormat: TimestampFormat; showTimestamp?: boolean; })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts:18` — `CompactionSummaryMessageComponent.constructor(message: CompactionSummaryMessage, markdownTheme: MarkdownTheme = getMarkdownTheme())` — exported: **yes** — Implements logic starting with: { super(); this.message = message; this.markdownTheme = markdownTheme; this.rebuild(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts:28` — `CompactionSummaryMessageComponent.setExpanded(expanded: boolean)` — exported: **yes** — Implements logic starting with: { if (this.expanded !== expanded) { this.expanded = expanded; this.rebuild(); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts:35` — `CompactionSummaryMessageComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); this.rebuild(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts:68` — `CompactionSummaryMessageComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { const frameWidth = Math.max(20, width); const contentWidth = Math.max(1, frameWidth - 4); const lines = super.render(contentWidth); const framed… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts:550` — `class ConfigSelectorComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts:554` — `ConfigSelectorComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts:557` — `ConfigSelectorComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.resourceList.focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts:562` — `ConfigSelectorComponent.constructor(resolvedPaths: ResolvedPaths, settingsManager: SettingsManager, cwd: string, agentDir: string, onClose: () => void, onExit: () => void, requestRender: () => void)` — exported: **yes** — Implements logic starting with: { super(); const groups = buildGroups(resolvedPaths); // Add header this.addChild(new Spacer(1)); this.addChild(new DynamicBorder()); this.addChild… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts:594` — `ConfigSelectorComponent.getResourceList()` — exported: **yes** — Implements logic starting with: { return this.resourceList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/countdown-timer.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/countdown-timer.ts:7` — `class CountdownTimer` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/countdown-timer.ts:12` — `CountdownTimer.constructor(timeoutMs: number, private tui: TUI | undefined, private onTick: (seconds: number) => void, private onExpire: () => void)` — exported: **yes** — Implements logic starting with: { this.remainingSeconds = Math.ceil(timeoutMs / 1000); this.onTick(this.remainingSeconds); this.intervalId = setInterval(() => { if (this._disposed… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/countdown-timer.ts:34` — `CountdownTimer.dispose()` — exported: **yes** — Implements logic starting with: { this._disposed = true; if (this.intervalId) { clearInterval(this.intervalId); this.intervalId = undefined; } } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/custom-editor.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/custom-editor.ts:18` — `CustomEditor.constructor(tui: TUI, theme: EditorTheme, keybindings: KeybindingsManager, options?: EditorOptions)` — exported: **yes** — Implements logic starting with: { super(tui, theme, options); this.keybindings = keybindings; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/custom-editor.ts:30` — `CustomEditor.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { // Check extension-registered shortcuts first if (this.onExtensionShortcut?.(data)) { return; } // Check for paste image keybinding if (this.keyb… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/custom-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/custom-message.ts:20` — `CustomMessageComponent.constructor(message: CustomMessage<unknown>, customRenderer?: MessageRenderer, markdownTheme: MarkdownTheme = getMarkdownTheme())` — exported: **yes** — Implements logic starting with: { super(); this.message = message; this.customRenderer = customRenderer; this.markdownTheme = markdownTheme; this.addChild(new Spacer(1)); // Creat… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/custom-message.ts:38` — `CustomMessageComponent.setExpanded(expanded: boolean)` — exported: **yes** — Implements logic starting with: { if (this._expanded !== expanded) { this._expanded = expanded; this.rebuild(); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/custom-message.ts:45` — `CustomMessageComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); this.rebuild(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/daxnuts.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/daxnuts.ts:57` — `class DaxnutsComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/daxnuts.ts:67` — `DaxnutsComponent.constructor(ui: TUI)` — exported: **yes** — Implements logic starting with: { this.ui = ui; this.image = buildImage(); this.startAnimation(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/daxnuts.ts:73` — `DaxnutsComponent.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedWidth = 0; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/daxnuts.ts:95` — `DaxnutsComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { if (width === this.cachedWidth && this.cachedTick === this.tick) { return this.cachedLines; } const t = theme; const lines: string[] = []; const… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/daxnuts.ts:162` — `DaxnutsComponent.dispose()` — exported: **yes** — Implements logic starting with: { this.stopAnimation(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:22` — `DynamicBorder.constructor(color: (str: string) => string = (str) => { try { return theme.fg("border", str); } catch { return str; } }, label?: string)` — exported: **yes** — Implements logic starting with: { this.color = color; this.label = label; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:29` — `DynamicBorder.setLabel(label: string | undefined)` — exported: **yes** — Implements logic starting with: { this.label = label; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:63` — `DynamicBorder.get isSpinning()` — exported: **yes** — Implements logic starting with: { return this.spinnerInterval !== null; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:67` — `DynamicBorder.invalidate()` — exported: **yes** — Implements logic starting with: { // No cached state to invalidate currently } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:71` — `DynamicBorder.render(width: number)` — exported: **yes** — Implements logic starting with: { this.lastExternalRender = Date.now(); const spinnerPrefix = this.spinnerInterval && this.spinnerColorFn ? this.spinnerColorFn(this.spinnerFrames[… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts:25` — `class ExtensionEditorComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts:33` — `ExtensionEditorComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts:36` — `ExtensionEditorComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.editor.focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts:41` — `ExtensionEditorComponent.constructor(tui: TUI, keybindings: KeybindingsManager, title: string, prefill: string | undefined, onSubmit: (value: string) => void, onCancel: () => void, options?: EditorOptions)` — exported: **yes** — Implements logic starting with: { super(); this.tui = tui; this.keybindings = keybindings; this.onSubmitCallback = onSubmit; this.onCancelCallback = onCancel; // Add top border th… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts:95` — `ExtensionEditorComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); // Escape or Ctrl+C to cancel if (kb.matches(keyData, "selectCancel")) { this.onCancelCallback(); return; } //… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts:17` — `class ExtensionInputComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts:27` — `ExtensionInputComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts:30` — `ExtensionInputComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.input.focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts:35` — `ExtensionInputComponent.constructor(title: string, placeholder: string | undefined, onSubmit: (value: string) => void, onCancel: () => void, opts?: ExtensionInputOptions)` — exported: **yes** — Implements logic starting with: { super(); this.onSubmitCallback = onSubmit; this.onCancelCallback = onCancel; this.baseTitle = title; this.addChild(new DynamicBorder()); this.add… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts:76` — `ExtensionInputComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); if (kb.matches(keyData, "selectConfirm") || keyData === "\n") { if (this.input.getValue().trim() === "") retur… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts:88` — `ExtensionInputComponent.dispose()` — exported: **yes** — Implements logic starting with: { this.countdown?.dispose(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/extension-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/extension-selector.ts:21` — `class ExtensionSelectorComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-selector.ts:31` — `ExtensionSelectorComponent.constructor(title: string, options: string[], onSelect: (option: string) => void, onCancel: () => void, opts?: ExtensionSelectorOptions)` — exported: **yes** — Implements logic starting with: { super(); this.options = options; this.onSelectCallback = onSelect; this.onCancelCallback = onCancel; this.baseTitle = title; this.addChild(new Dy… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-selector.ts:122` — `ExtensionSelectorComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); if (kb.matches(keyData, "selectUp") || keyData === "k") { let next = this.selectedIndex - 1; if (next < 0) nex… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/extension-selector.ts:152` — `ExtensionSelectorComponent.dispose()` — exported: **yes** — Implements logic starting with: { this.countdown?.dispose(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/footer.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:53` — `FooterComponent.constructor(private session: AgentSession, private footerData: ReadonlyFooterDataProvider)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:58` — `FooterComponent.setAutoCompactEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.autoCompactEnabled = enabled; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:78` — `FooterComponent.render(width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:19` — `buildAuthUrlPresentation(url: string, terminalColumns: number)` — exported: **yes** — Implements logic starting with: { const maxUrlWidth = Math.max(20, terminalColumns - 4); const displayUrl = truncateToWidth(url, maxUrlWidth); return { displayUrl, fullUrlLines: d… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:50` — `LoginDialogComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:53` — `LoginDialogComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.input.focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:58` — `LoginDialogComponent.constructor(tui: TUI, providerId: string, private onComplete: (success: boolean, message?: string) => void)` — exported: **yes** — Implements logic starting with: { super(); this.tui = tui; const providerInfo = getOAuthProviders().find((p) => p.id === providerId); const providerName = providerInfo?.name || pr… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:101` — `LoginDialogComponent.get signal()` — exported: **yes** — Implements logic starting with: { return this.abortController.signal; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:243` — `LoginDialogComponent.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if (this.disposed) return; const kb = getEditorKeybindings(); if (kb.matches(data, "selectCancel")) { this.cancel(); return; } // Pass to input t… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:18` — `providerDisplayName(provider: string)` — exported: **yes** — Implements logic starting with: { return provider; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:85` — `ModelSelectorComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:88` — `ModelSelectorComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.searchInput.focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:119` — `ModelSelectorComponent.constructor(tui: TUI, currentModel: Model<any> | undefined, settingsManager: SettingsManager, modelRegistry: ModelRegistry, scopedModels: ReadonlyArray<ScopedModelItem>, onSelect: (model: Model<any>) => void, onCancel: () => void, initialSearchInput?: string)` — exported: **yes** — Implements logic starting with: { super(); this.tui = tui; this.currentModel = currentModel; this.settingsManager = settingsManager; this.modelRegistry = modelRegistry; this.scope… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:560` — `ModelSelectorComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); // Tab: scope toggle if (kb.matches(keyData, "tab")) { if (this.scopedModelItems.length > 0) { const nextScope… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:687` — `ModelSelectorComponent.getSearchInput()` — exported: **yes** — Implements logic starting with: { return this.searchInput; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/oauth-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/oauth-selector.ts:20` — `OAuthSelectorComponent.constructor(mode: "login" | "logout", authStorage: AuthStorage, onSelect: (providerId: string) => void, onCancel: () => void)` — exported: **yes** — Implements logic starting with: { super(); this.mode = mode; this.authStorage = authStorage; this.onSelectCallback = onSelect; this.onCancelCallback = onCancel; // Load all OAuth… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/oauth-selector.ts:97` — `OAuthSelectorComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); // Up arrow (wrap) if (kb.matches(keyData, "selectUp")) { this.selectedIndex = this.selectedIndex === 0 ? this… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts:29` — `class ProviderManagerComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts:31` — `ProviderManagerComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts:34` — `ProviderManagerComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts:51` — `ProviderManagerComponent.constructor(tui: TUI, authStorage: AuthStorage, modelRegistry: ModelRegistry, onDone: () => void, onDiscover: (provider: string) => void, onSetupAuth?: (provider: string) => void)` — exported: **yes** — Implements logic starting with: { super(); this.tui = tui; this.authStorage = authStorage; this.modelRegistry = modelRegistry; this.modelsJsonWriter = new ModelsJsonWriter(this.mo… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts:176` — `ProviderManagerComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); if (kb.matches(keyData, "selectUp")) { if (this.providers.length === 0) return; this.selectedIndex = this.sele… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts:107` — `ScopedModelsSelectorComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts:110` — `ScopedModelsSelectorComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.searchInput.focused = value; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts:120` — `ScopedModelsSelectorComponent.constructor(config: ModelsConfig, callbacks: ModelsCallbacks)` — exported: **yes** — Implements logic starting with: { super(); this.callbacks = callbacks; for (const model of config.allModels) { const fullId = `${model.provider}/${model.id}`; this.modelsById.set(… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts:227` — `ScopedModelsSelectorComponent.handleInput(data: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts:339` — `ScopedModelsSelectorComponent.getSearchInput()` — exported: **yes** — Implements logic starting with: { return this.searchInput; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/session-selector-search.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector-search.ts:30` — `hasSessionName(session: SessionInfo)` — exported: **yes** — Implements logic starting with: { return Boolean(session.name?.trim()); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector-search.ts:156` — `filterAndSortSessions(sessions: SessionInfo[], query: string, sortMode: SortMode, nameFilter: NameFilter = "all")` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:656` — `SessionSelectorComponent.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if (this.mode === "rename") { const kb = getEditorKeybindings(); if (kb.matches(data, "selectCancel") || matchesKey(data, "ctrl+c")) { this.exitR… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:694` — `SessionSelectorComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:697` — `SessionSelectorComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; this.sessionList.focused = value; this.renameInput.focused = value; if (value && this.mode === "rename") { this.renameInpu… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:720` — `SessionSelectorComponent.constructor(currentSessionsLoader: SessionsLoader, allSessionsLoader: SessionsLoader, onSelect: (sessionPath: string) => void, onCancel: () => void, onExit: () => void, requestRender: () => void, options?: { renameSession?: (sessionPath: string, currentName: string | undefined) => Promise<void>; showRenameHint?: boolean; keybindings?: KeybindingsManager; }, currentSessionFilePath?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:1004` — `SessionSelectorComponent.getSessionList()` — exported: **yes** — Implements logic starting with: { return this.sessionList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts:86` — `SelectSubmenu.constructor(title: string, description: string, options: SelectItem[], currentValue: string, onSelect: (value: string) => void, onCancel: () => void, onSelectionChange?: (value: string) => void)` — exported: **yes** — Implements logic starting with: { super(); // Title this.addChild(new Text(theme.bold(theme.fg("accent", title)), 0, 0)); // Description if (description) { this.addChild(new Space… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts:137` — `SelectSubmenu.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { this.selectList.handleInput(data); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts:148` — `SettingsSelectorComponent.constructor(config: SettingsConfig, callbacks: SettingsCallbacks)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts:463` — `SettingsSelectorComponent.getSettingsList()` — exported: **yes** — Implements logic starting with: { return this.settingsList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/show-images-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/show-images-selector.ts:11` — `ShowImagesSelectorComponent.constructor(currentValue: boolean, onSelect: (show: boolean) => void, onCancel: () => void)` — exported: **yes** — Implements logic starting with: { super(); const items: SelectItem[] = [ { value: "yes", label: "Yes", description: "Show images inline in terminal" }, { value: "no", label: "No",… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/show-images-selector.ts:42` — `ShowImagesSelectorComponent.getSelectList()` — exported: **yes** — Implements logic starting with: { return this.selectList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts:18` — `SkillInvocationMessageComponent.constructor(skillBlock: ParsedSkillBlock, markdownTheme: MarkdownTheme = getMarkdownTheme())` — exported: **yes** — Implements logic starting with: { super(); this.skillBlock = skillBlock; this.markdownTheme = markdownTheme; this.rebuild(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts:25` — `SkillInvocationMessageComponent.setExpanded(expanded: boolean)` — exported: **yes** — Implements logic starting with: { if (this.expanded !== expanded) { this.expanded = expanded; this.rebuild(); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts:32` — `SkillInvocationMessageComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); this.rebuild(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts:57` — `SkillInvocationMessageComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { const frameWidth = Math.max(20, width); const contentWidth = Math.max(1, frameWidth - 4); const lines = super.render(contentWidth); const framed… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/theme-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/theme-selector.ts:12` — `ThemeSelectorComponent.constructor(currentTheme: string, onSelect: (themeName: string) => void, onCancel: () => void, onPreview: (themeName: string) => void)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/theme-selector.ts:59` — `ThemeSelectorComponent.getSelectList()` — exported: **yes** — Implements logic starting with: { return this.selectList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/thinking-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/thinking-selector.ts:21` — `ThinkingSelectorComponent.constructor(currentLevel: ThinkingLevel, availableLevels: ThinkingLevel[], onSelect: (level: ThinkingLevel) => void, onCancel: () => void)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/thinking-selector.ts:61` — `ThinkingSelectorComponent.getSelectList()` — exported: **yes** — Implements logic starting with: { return this.selectList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:343` — `ToolExecutionComponent.constructor(toolName: string, args: any, options: ToolExecutionOptions = {}, toolDefinition: ToolDefinition | undefined, ui: TUI, cwd: string = process.cwd())` — exported: **yes** — Implements logic starting with: { super(); this.toolName = toolName; this.args = args; this.showImages = options.showImages ?? true; this.toolDefinition = toolDefinition; this.ui… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:388` — `ToolExecutionComponent.dispose()` — exported: **yes** — Implements logic starting with: { this.convertedImages.clear(); this.imageComponents = []; this.imageSpacers = []; this.editDiffPreview = undefined; this.writeHighlightCache = und… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:397` — `ToolExecutionComponent.updateArgs(args: any)` — exported: **yes** — Implements logic starting with: { this.args = args; if (this.normalizedToolName === "write" && this.isPartial) { this.updateWriteHighlightCacheIncremental(); } this.updateDisplay(… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:545` — `ToolExecutionComponent.updateResult(result: { content: Array<{ type: string; text?: string; data?: string; mimeType?: string }>; details?: any; isError: boolean; }, isPartial = false)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:643` — `ToolExecutionComponent.setExpanded(expanded: boolean)` — exported: **yes** — Implements logic starting with: { this.expanded = expanded; this.updateDisplay(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:648` — `ToolExecutionComponent.setShowImages(show: boolean)` — exported: **yes** — Implements logic starting with: { this.showImages = show; this.updateDisplay(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:653` — `ToolExecutionComponent.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); this.updateDisplay(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:658` — `ToolExecutionComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.hideComponent) { return []; } const frameWidth = Math.max(20, width); const contentWidth = Math.max(1, frameWidth - 4); const frameTone:… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:715` — `ToolExecutionComponent.getRollupPhase()` — exported: **yes** — Implements logic starting with: { if (!this.shouldRenderCompactSuccess()) return null; const label = this.getPhaseLabel(); const endedAt = this.endedAt ?? Date.now(); const target… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:1432` — `class ToolPhaseSummaryComponent` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:1433` — `ToolPhaseSummaryComponent.constructor(private readonly phases: ToolExecutionPhase[])` — exported: **yes** — Implements logic starting with: { super(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:1437` — `ToolPhaseSummaryComponent.getPhases()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:1441` — `ToolPhaseSummaryComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { const frameWidth = Math.max(20, width); const rows = this.phases.flatMap((phase) => { const left = summarizePhaseLabel(phase); const right = `suc… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:10` — `chatMessageWidth(width: number)` — exported: **yes** — Implements logic starting with: { return Math.max(24, Math.min(width, Math.floor(width * 0.72))); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:33` — `rightAlign(left: string, right: string, width: number)` — exported: **yes** — Implements logic starting with: { return alignRight(left, right, width); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:37` — `renderAssistantRail(lines: string[], width: number, opts: { label: string; meta?: string; railColor?: ThemeColor } = { label: "GSD" })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:60` — `renderUserRail(lines: string[], width: number, opts: { label: string; meta?: string })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:90` — `renderTranscriptCard(lines: string[], width: number, opts: { title: string; right?: string; tone: StatusTone; footerLeft?: string; footerRight?: string; })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:124` — `renderToolLineCard(title: string, target: string | undefined, width: number, opts: { status: string; tone: StatusTone; hidden?: boolean; titlePrefix?: string; bg?: ThemeBg })` — exported: **yes** — Implements logic starting with: { const outerWidth = Math.max(20, width); const innerWidth = Math.max(1, outerWidth - 2); const paddingX = 2; const contentWidth = Math.max(1, inne… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:152` — `renderCommandCard(command: string, width: number, opts: { status: string; tone: StatusTone; progress?: string })` — exported: **yes** — Implements logic starting with: { const outerWidth = Math.max(20, width); const innerWidth = Math.max(1, outerWidth - 2); const paddingX = 2; const contentWidth = Math.max(1, inne… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:176` — `renderProgressBar(done: number, total: number, width: number, tone: StatusTone = "success")` — exported: **yes** — Implements logic starting with: { const clampedWidth = Math.max(0, width); const pct = total > 0 ? Math.max(0, Math.min(1, done / total)) : 0; const filled = Math.round(pct * clam… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/transcript-design.ts:186` — `renderFooterStrip(leftSegments: string[], right: string, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts:1084` — `TreeSelectorComponent.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts:1087` — `TreeSelectorComponent.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; // Propagate to labelInput when it's active if (this.labelInput) { this.labelInput.focused = value; } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts:1095` — `TreeSelectorComponent.constructor(tree: SessionTreeNode[], currentLeafId: string | null, terminalHeight: number, onSelect: (entryId: string) => void, onCancel: () => void, onLabelChange?: (entryId: string, label: string | undefined) => void, initialSelectedId?: string, initialFilterMode?: FilterMode)` — exported: **yes** — Implements logic starting with: { super(); this.onLabelChangeCallback = onLabelChange; const maxVisibleLines = Math.max(5, Math.floor(terminalHeight / 2)); this.treeList = new Tre… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts:1168` — `TreeSelectorComponent.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { if (this.labelInput) { this.labelInput.handleInput(keyData); } else { this.treeList.handleInput(keyData); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts:1176` — `TreeSelectorComponent.getTreeList()` — exported: **yes** — Implements logic starting with: { return this.treeList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:11` — `breakpoint(width: number)` — exported: **yes** — Implements logic starting with: { if (width < 72) return "compact"; if (width < 112) return "regular"; return "wide"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:17` — `padRight(text: string, width: number)` — exported: **yes** — Implements logic starting with: { const clipped = truncateToWidth(text, Math.max(0, width), ""); return clipped + " ".repeat(Math.max(0, width - visibleWidth(clipped))); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:22` — `alignRight(left: string, right: string, width: number)` — exported: **yes** — Implements logic starting with: { if (width <= 0) return ""; if (!right) return truncateToWidth(left, width, ""); const gap = Math.max(1, width - visibleWidth(left) - visibleWidth… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:29` — `toneColor(tone: TuiTone)` — exported: **yes** — Implements logic starting with: { switch (tone) { case "accent": return "borderAccent"; case "success": return "success"; case "warning": return "warning"; case "error": return "e… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:41` — `badge(text: string, tone: TuiTone = "default")` — exported: **yes** — Implements logic starting with: { return theme.fg(toneColor(tone), text); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:45` — `keyValue(label: string, value: string, valueColor: ThemeColor = "text", labelWidth = 10)` — exported: **yes** — Implements logic starting with: { return `${theme.fg("dim", label.padEnd(labelWidth))}${theme.fg(valueColor, value)}`; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/tui-style-kit.ts:49` — `roundedPanel(lines: string[], width: number, opts: { tone?: TuiTone; title?: string; rightTitle?: string; paddingX?: number; } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/user-message-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/user-message-selector.ts:112` — `UserMessageSelectorComponent.constructor(messages: UserMessageItem[], onSelect: (entryId: string) => void, onCancel: () => void)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/user-message-selector.ts:141` — `UserMessageSelectorComponent.getMessageList()` — exported: **yes** — Implements logic starting with: { return this.messageList; } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/components/user-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/user-message.ts:19` — `UserMessageComponent.constructor(text: string, markdownTheme: MarkdownTheme = getMarkdownTheme(), timestamp?: number, timestampFormat: TimestampFormat = "date-time-iso")` — exported: **yes** — Implements logic starting with: { super(); this.timestamp = timestamp; this.timestampFormat = timestampFormat; this.addChild(new Markdown(text, 0, 0, markdownTheme)); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/components/user-message.ts:26` — `UserMessageComponent.render(width: number)` — exported: **yes** — Implements logic starting with: { const frameWidth = Math.max(20, width); const messageWidth = chatMessageWidth(frameWidth); const contentWidth = Math.max(1, messageWidth - 2); co… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts`
- `packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts:55` — `findLatestPinnableCandidates(contentBlocks: Array<any>)` — exported: **yes** — Implements logic starting with: { let lastToolIdx = -1; for (let i = contentBlocks.length - 1; i >= 0; i--) { const c = contentBlocks[i]; if (c?.type === "toolCall" || c?.type ===… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts:76` — `findLatestPinnableText(contentBlocks: Array<any>)` — exported: **yes** — Implements logic starting with: { return findLatestPinnableCandidates(contentBlocks)[0]?.text ?? ""; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts:203` — `handleAgentEvent(host: InteractiveModeStateHost & { init: () => Promise<void>; getMarkdownThemeWithSettings: () => any; addMessageToChat: (message: any, options?: any) => void; formatWebSearchResult: (content: unknown) => string; getRegisteredToolDefinition: (toolName: string) => any; checkShutdownRequested: () => Promise<void>; rebuildChatFromMessages: () => void; flushCompactionQueue: (options?: { willRetry?: boolean }) => Promise<void>; showStatus: (message: string) => void; showError: (message: string) => void; updatePendingMessagesDisplay: () => void; updateTerminalTitle: () => void; updateEditorBorderColor: () => void; pendingMessagesContainer: { clear: () => void }; }, event: InteractiveModeEvent)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/controllers/extension-ui-controller.ts`
- `packages/pi-coding-agent/src/modes/interactive/controllers/extension-ui-controller.ts:6` — `createExtensionUIContext(host: any)` — exported: **yes** — Implements logic starting with: { return { select: (title, options, opts) => host.showExtensionSelector(title, options, opts), confirm: (title, message, opts) => host.showExtensio… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.ts`
- `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.ts:17` — `setupEditorSubmitHandler(host: InteractiveModeStateHost & { getSlashCommandContext: () => any; handleBashCommand: (command: string, excludeFromContext?: boolean) => Promise<void>; showWarning: (message: string) => void; showError: (message: string) => void; showTip: (message: string) => void; updateEditorBorderColor: () => void; isExtensionCommand: (text: string) => boolean; isKnownSlashCommand: (text: string) => boolean; queueCompactionMessage: (text: string, mode: "steer" | "followUp") => void; updatePendingMessagesDisplay: () => void; flushPendingBashComponents: () => void; contextualTips: ContextualTips; getContextPercent: () => number | undefined; options?: { submitPromptsDirectly?: boolean }; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/controllers/model-controller.ts`
- `packages/pi-coding-agent/src/modes/interactive/controllers/model-controller.ts:3` — `handleModelCommand(host: any, searchTerm?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/controllers/model-controller.ts:26` — `findExactModelMatch(host: any, searchTerm: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/controllers/model-controller.ts:53` — `getModelCandidates(host: any)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/controllers/model-controller.ts:71` — `updateAvailableProviderCount(host: any)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts`
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:179` — `shouldRenderExtensionNotifyInChat(type: ExtensionNotifyType)` — exported: **yes** — Implements logic starting with: { return type !== "warning"; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:189` — `renderExtensionNotifyInChat(chatContainer: Container, message: string, type?: ExtensionNotifyType)` — exported: **yes** — Implements logic starting with: { if (!shouldRenderExtensionNotifyInChat(type)) { return { rendered: false }; } const spacer = new Spacer(1); chatContainer.addChild(spacer); if (t… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:218` — `renderBlockingErrorBanner(container: Container, message: string | undefined)` — exported: **yes** — Implements logic starting with: { container.clear(); if (message === undefined) return; container.addChild(new Spacer(1)); container.addChild(new Text(theme.fg("error", `Error: ${… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:252` — `class InteractiveMode` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:374` — `InteractiveMode.constructor(session: AgentSession, private options: InteractiveModeOptions = {})` — exported: **yes** — Implements logic starting with: { this.session = session; this.version = VERSION; this.ui = new TUI(options.terminal ?? new ProcessTerminal(), this.settingsManager.getShowHardware… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:517` — `InteractiveMode.init()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:1656` — `InteractiveMode.getExtensionUIContext()` — exported: **yes** — Implements logic starting with: { return this.createExtensionUIContext(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2531` — `InteractiveMode.renderInitialMessages()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2549` — `InteractiveMode.getUserInput()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2911` — `InteractiveMode.clearEditor()` — exported: **yes** — Implements logic starting with: { this.editor.setText(""); this.ui.requestRender(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2916` — `InteractiveMode.showError(errorMessage: string)` — exported: **yes** — Implements logic starting with: { this.lastBlockingError = errorMessage; renderBlockingErrorBanner(this.blockingErrorContainer, this.lastBlockingError); this.chatContainer.addChil… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2924` — `InteractiveMode.clearBlockingError()` — exported: **yes** — Implements logic starting with: { this.lastBlockingError = undefined; renderBlockingErrorBanner(this.blockingErrorContainer, undefined); this.ui.requestRender(); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2930` — `InteractiveMode.showWarning(warningMessage: string)` — exported: **yes** — Implements logic starting with: { this.chatContainer.addChild(new Spacer(1)); this.chatContainer.addChild(new Text(theme.fg("warning", `Warning: ${warningMessage}`), 1, 0)); this.… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2936` — `InteractiveMode.showSuccess(successMessage: string)` — exported: **yes** — Implements logic starting with: { this.chatContainer.addChild(new Spacer(1)); this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("success", text))); this.chatContain… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2947` — `InteractiveMode.showTip(message: string)` — exported: **yes** — Implements logic starting with: { this.chatContainer.addChild(new Spacer(1)); this.chatContainer.addChild(new Text(theme.fg("dim", `💡 ${message}`), 1, 0)); this.ui.requestRender(… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2953` — `InteractiveMode.getContextPercent()` — exported: **yes** — Implements logic starting with: { return this.session.getContextUsage()?.percent ?? undefined; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:2957` — `InteractiveMode.showNewVersionNotification(newVersion: string)` — exported: **yes** — Implements logic starting with: { const action = theme.fg("accent", getUpdateInstruction("@gsd/pi-coding-agent")); const updateInstruction = theme.fg("muted", `New version ${newVe… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:4262` — `InteractiveMode.requestRender(force = false)` — exported: **yes** — Implements logic starting with: { if (!this.isInitialized) return; this.ui.requestRender(force); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:4267` — `InteractiveMode.stop()` — exported: **yes** — Implements logic starting with: { if (this.loadingAnimation) { this.loadingAnimation.stop(); this.loadingAnimation = undefined; } this.clearExtensionTerminalInputListeners(); // C… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/slash-command-handlers.ts`
- `packages/pi-coding-agent/src/modes/interactive/slash-command-handlers.ts:456` — `capitalizeKey(key: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/slash-command-handlers.ts:468` — `getAppKeyDisplay(keybindings: KeybindingsManager, action: AppAction)` — exported: **yes** — Implements logic starting with: { return capitalizeKey(appKey(keybindings, action)); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts`
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:282` — `class Theme` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:289` — `Theme.constructor(fgColors: Record<ThemeColor, string | number>, bgColors: Record<ThemeBg, string | number>, mode: ColorMode, options: { name?: string; sourcePath?: string } = {})` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:308` — `Theme.fg(color: ThemeColor, text: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:314` — `Theme.bg(color: ThemeBg, text: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:320` — `Theme.bold(text: string)` — exported: **yes** — Implements logic starting with: { return chalk.bold(text); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:324` — `Theme.italic(text: string)` — exported: **yes** — Implements logic starting with: { return chalk.italic(text); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:328` — `Theme.underline(text: string)` — exported: **yes** — Implements logic starting with: { return chalk.underline(text); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:332` — `Theme.inverse(text: string)` — exported: **yes** — Implements logic starting with: { return chalk.inverse(text); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:336` — `Theme.strikethrough(text: string)` — exported: **yes** — Implements logic starting with: { return chalk.strikethrough(text); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:340` — `Theme.getFgAnsi(color: ThemeColor)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:346` — `Theme.getBgAnsi(color: ThemeBg)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:352` — `Theme.getColorMode()` — exported: **yes** — Implements logic starting with: { return this.mode; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:356` — `Theme.getThinkingBorderColor(level: "off" | "minimal" | "low" | "medium" | "high" | "xhigh")` — exported: **yes** — Implements logic starting with: { // Map thinking levels to dedicated theme colors switch (level) { case "off": return (str: string) => this.fg("thinkingOff", str); case "minimal"… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:376` — `Theme.getBashModeBorderColor()` — exported: **yes** — Implements logic starting with: { return (str: string) => this.fg("bashMode", str); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:407` — `getAvailableThemes()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:429` — `getAvailableThemesWithPaths()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:550` — `loadThemeFromPath(themePath: string, mode?: ColorMode)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:565` — `getThemeByName(name: string)` — exported: **yes** — Implements logic starting with: { try { return loadTheme(name); } catch { return undefined; } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:618` — `setRegisteredThemes(themes: Theme[])` — exported: **yes** — Implements logic starting with: { registeredThemes.clear(); for (const theme of themes) { if (theme.name) { registeredThemes.set(theme.name, theme); } } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:627` — `initTheme(themeName?: string, enableWatcher: boolean = false)` — exported: **yes** — Implements logic starting with: { const name = themeName ?? getDefaultTheme(); currentThemeName = name; try { setGlobalTheme(loadTheme(name)); if (enableWatcher) { startThemeWatch… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:643` — `setTheme(name: string, enableWatcher: boolean = false)` — exported: **yes** — Implements logic starting with: { currentThemeName = name; try { setGlobalTheme(loadTheme(name)); if (enableWatcher) { startThemeWatcher(); } onThemeChangeCallbacks.forEach(cb =>… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:664` — `setThemeInstance(themeInstance: Theme)` — exported: **yes** — Implements logic starting with: { setGlobalTheme(themeInstance); currentThemeName = "<in-memory>"; stopThemeWatcher(); // Can't watch a direct instance onThemeChangeCallbacks.forE… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:671` — `onThemeChange(callback: () => void)` — exported: **yes** — Implements logic starting with: { onThemeChangeCallbacks.add(callback); return () => { onThemeChangeCallbacks.delete(callback); }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:730` — `stopThemeWatcher()` — exported: **yes** — Implements logic starting with: { if (themeWatcher) { themeWatcher.close(); themeWatcher = undefined; } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:1074` — `getMarkdownTheme()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:1105` — `getSelectListTheme()` — exported: **yes** — Implements logic starting with: { return { selectedPrefix: (text: string) => theme.fg("accent", text), selectedText: (text: string) => theme.fg("accent", text), description: (text… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:1115` — `getEditorTheme()` — exported: **yes** — Implements logic starting with: { return { borderColor: (text: string) => theme.fg("borderMuted", text), selectList: getSelectListTheme(), }; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:1122` — `getSettingsListTheme()` — exported: **yes** — Implements logic starting with: { return { label: (text: string, selected: boolean) => (selected ? theme.fg("accent", text) : text), value: (text: string, selected: boolean) => (s… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/interactive/tui-mode.ts`
- `packages/pi-coding-agent/src/modes/interactive/tui-mode.ts:14` — `resolveTuiMode(context: TuiModeContext)` — exported: **yes** — Implements logic starting with: { if (context.override && context.override !== "auto") return context.override; if (context.terminalWidth < 72) return "compact"; if (context.hasBl… (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts`
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:20` — `RemoteTerminal.constructor(private readonly options: RemoteTerminalOptions)` — exported: **yes** — Implements logic starting with: { this._columns = Math.max(1, options.initialColumns ?? 120); this._rows = Math.max(1, options.initialRows ?? 30); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:25` — `RemoteTerminal.start(onInput: (data: string) => void, onResize: () => void)` — exported: **yes** — Implements logic starting with: { this.inputHandler = onInput; this.resizeHandler = onResize; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:30` — `RemoteTerminal.stop()` — exported: **yes** — Implements logic starting with: { this.inputHandler = undefined; this.resizeHandler = undefined; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:35` — `RemoteTerminal.drainInput()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:39` — `RemoteTerminal.write(data: string)` — exported: **yes** — Implements logic starting with: { if (!data) return; this.options.onWrite(data); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:44` — `RemoteTerminal.get columns()` — exported: **yes** — Implements logic starting with: { return this._columns; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:48` — `RemoteTerminal.get rows()` — exported: **yes** — Implements logic starting with: { return this._rows; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:52` — `RemoteTerminal.get isTTY()` — exported: **yes** — Implements logic starting with: { // RemoteTerminal renders to a browser-based terminal emulator via // the RPC bridge — it behaves like a real TTY for rendering purposes. return… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:58` — `RemoteTerminal.get kittyProtocolActive()` — exported: **yes** — Implements logic starting with: { return false; } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:62` — `RemoteTerminal.pushInput(data: string)` — exported: **yes** — Implements logic starting with: { if (!data) return; this.inputHandler?.(data); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:67` — `RemoteTerminal.resize(columns: number, rows: number)` — exported: **yes** — Implements logic starting with: { const nextColumns = Math.max(1, Math.floor(columns)); const nextRows = Math.max(1, Math.floor(rows)); const changed = nextColumns !== this._colum… (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:78` — `RemoteTerminal.moveBy(lines: number)` — exported: **yes** — Implements logic starting with: { if (lines > 0) { this.write(`\x1b[${lines}B`); } else if (lines < 0) { this.write(`\x1b[${-lines}A`); } } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:86` — `RemoteTerminal.hideCursor()` — exported: **yes** — Implements logic starting with: { this.write("\x1b[?25l"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:90` — `RemoteTerminal.showCursor()` — exported: **yes** — Implements logic starting with: { this.write("\x1b[?25h"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:94` — `RemoteTerminal.clearLine()` — exported: **yes** — Implements logic starting with: { this.write("\x1b[K"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:98` — `RemoteTerminal.clearFromCursor()` — exported: **yes** — Implements logic starting with: { this.write("\x1b[J"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:102` — `RemoteTerminal.clearScreen()` — exported: **yes** — Implements logic starting with: { this.write("\x1b[2J\x1b[H"); } (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:106` — `RemoteTerminal.setTitle(title: string)` — exported: **yes** — Implements logic starting with: { this.write(`\x1b]0;${title}\x07`); } (missing: jsdoc)

### `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts`
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:54` — `class RpcClient` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:64` — `RpcClient.constructor(private options: RpcClientOptions = {})` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)

### `packages/pi-coding-agent/src/resources/extensions/memory/index.ts`
- `packages/pi-coding-agent/src/resources/extensions/memory/index.ts:47` — `memoryExtension(api: ExtensionAPI)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts`
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:47` — `class MemoryStorage` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:57` — `MemoryStorage.create(dbPath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:471` — `MemoryStorage.close()` — exported: **yes** — Implements logic starting with: { if (this.persistTimer) { clearTimeout(this.persistTimer); this.persistTimer = null; } this.persist(); this.db.close(); } (missing: jsdoc)

### `packages/pi-coding-agent/src/utils/clipboard-image.ts`
- `packages/pi-coding-agent/src/utils/clipboard-image.ts:25` — `extensionForImageMimeType(mimeType: string)` — exported: **yes** — Implements logic starting with: { switch (baseMimeType(mimeType)) { case "image/png": return "png"; case "image/jpeg": return "jpg"; case "image/webp": return "webp"; case "image/… (missing: jsdoc)
- `packages/pi-coding-agent/src/utils/clipboard-image.ts:183` — `readClipboardImage(options?: { env?: NodeJS.ProcessEnv; platform?: NodeJS.Platform; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/utils/clipboard.ts`
- `packages/pi-coding-agent/src/utils/clipboard.ts:3` — `copyToClipboard(text: string)` — exported: **yes** — Implements logic starting with: { // Always emit OSC 52 - works over SSH/mosh, harmless locally const encoded = Buffer.from(text).toString("base64"); process.stdout.write(`\x1b]52… (missing: jsdoc)

### `packages/pi-coding-agent/src/utils/frontmatter.ts`
- `packages/pi-coding-agent/src/utils/frontmatter.ts:28` — `parseFrontmatter(content: string)` — exported: **yes** — Implements logic starting with: { const { yamlString, body } = extractFrontmatter(content); if (!yamlString) { return { frontmatter: {} as T, body }; } const parsed = parse(yamlSt… (missing: jsdoc)
- `packages/pi-coding-agent/src/utils/frontmatter.ts:39` — `stripFrontmatter(content: string)` — exported: **yes** — Implements logic starting with: parseFrontmatter(content).body (missing: jsdoc)

### `packages/pi-coding-agent/src/utils/mime.ts`
- `packages/pi-coding-agent/src/utils/mime.ts:8` — `detectSupportedImageMimeTypeFromFile(filePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-coding-agent/src/utils/shell.ts`
- `packages/pi-coding-agent/src/utils/shell.ts:132` — `getShellEnv()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `packages/pi-coding-agent/src/utils/tools-manager.ts`
- `packages/pi-coding-agent/src/utils/tools-manager.ts:243` — `ensureTool(tool: "fd" | "rg", silent: boolean = false)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `packages/pi-tui/src/autocomplete.ts`
- `packages/pi-tui/src/autocomplete.ts:130` — `class CombinedAutocompleteProvider` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:136` — `CombinedAutocompleteProvider.constructor(commands: (SlashCommand | AutocompleteItem)[] = [], basePath: string = process.cwd(), options?: { respectGitignore?: boolean; excludeDirs?: string[] })` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:147` — `CombinedAutocompleteProvider.setRespectGitignore(value: boolean)` — exported: **yes** — Implements logic starting with: { this.respectGitignore = value; } (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:151` — `CombinedAutocompleteProvider.setExcludeDirs(dirs: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:155` — `CombinedAutocompleteProvider.getSuggestions(lines: string[], cursorLine: number, cursorCol: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:255` — `CombinedAutocompleteProvider.applyCompletion(lines: string[], cursorLine: number, cursorCol: number, item: AutocompleteItem, prefix: string)` — exported: **yes** — Implements logic starting with: { const currentLine = lines[cursorLine] || ""; const beforePrefix = currentLine.slice(0, cursorCol - prefix.length); const afterCursor = currentLin… (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:635` — `CombinedAutocompleteProvider.getForceFileSuggestions(lines: string[], cursorLine: number, cursorCol: number)` — exported: **yes** — Implements logic starting with: { const currentLine = lines[cursorLine] || ""; const textBeforeCursor = currentLine.slice(0, cursorCol); // Don't trigger if we're typing a slash c… (missing: jsdoc)
- `packages/pi-tui/src/autocomplete.ts:664` — `CombinedAutocompleteProvider.shouldTriggerFileCompletion(lines: string[], cursorLine: number, cursorCol: number)` — exported: **yes** — Implements logic starting with: { const currentLine = lines[cursorLine] || ""; const textBeforeCursor = currentLine.slice(0, cursorCol); // Don't trigger if we're typing a slash c… (missing: jsdoc)

### `packages/pi-tui/src/components/box.ts`
- `packages/pi-tui/src/components/box.ts:23` — `Box.constructor(paddingX = 1, paddingY = 1, bgFn?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { this.paddingX = paddingX; this.paddingY = paddingY; this.bgFn = bgFn; } (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:29` — `Box.addChild(component: Component)` — exported: **yes** — Implements logic starting with: { this.children.push(component); this.invalidateCache(); } (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:34` — `Box.insertChildBefore(component: Component, before: Component)` — exported: **yes** — Implements logic starting with: { const index = this.children.indexOf(before); if (index !== -1) { this.children.splice(index, 0, component); } else { this.children.push(component… (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:44` — `Box.removeChild(component: Component)` — exported: **yes** — Implements logic starting with: { const index = this.children.indexOf(component); if (index !== -1) { this.children.splice(index, 1); this.invalidateCache(); } } (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:52` — `Box.clear()` — exported: **yes** — Implements logic starting with: { this.children = []; this.invalidateCache(); } (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:57` — `Box.setBgFn(bgFn?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { this.bgFn = bgFn; // Don't invalidate here - we'll detect bgFn changes by sampling output } (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:77` — `Box.invalidate()` — exported: **yes** — Implements logic starting with: { this.invalidateCache(); for (const child of this.children) { child.invalidate?.(); } } (missing: jsdoc)
- `packages/pi-tui/src/components/box.ts:84` — `Box.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.children.length === 0) { return []; } const contentWidth = Math.max(1, width - this.paddingX * 2); const leftPad = " ".repeat(this.paddi… (missing: jsdoc)

### `packages/pi-tui/src/components/cancellable-loader.ts`
- `packages/pi-tui/src/components/cancellable-loader.ts:29` — `CancellableLoader.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); if (kb.matches(data, "selectCancel")) { this.abortController.abort(); this.onAbort?.(); } } (missing: jsdoc)
- `packages/pi-tui/src/components/cancellable-loader.ts:37` — `CancellableLoader.dispose()` — exported: **yes** — Implements logic starting with: { this.abortController.abort(); this.onAbort = undefined; this.stop(); } (missing: jsdoc)

### `packages/pi-tui/src/components/editor.ts`
- `packages/pi-tui/src/components/editor.ts:123` — `class Editor` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:132` — `Editor.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:135` — `Editor.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; if (!value) { this.isInPaste = false; this.pasteBuffer = ""; } } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:203` — `Editor.constructor(tui: TUI, theme: EditorTheme, options: EditorOptions = {})` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.borderColor = theme.borderColor; const paddingX = options.paddingX ?? 0; this.paddingX = Number.isFinite… (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:213` — `Editor.getPaddingX()` — exported: **yes** — Implements logic starting with: { return this.paddingX; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:217` — `Editor.setPaddingX(padding: number)` — exported: **yes** — Implements logic starting with: { const newPadding = Number.isFinite(padding) ? Math.max(0, Math.floor(padding)) : 0; if (this.paddingX !== newPadding) { this.paddingX = newPaddin… (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:225` — `Editor.getAutocompleteMaxVisible()` — exported: **yes** — Implements logic starting with: { return this.autocompleteMaxVisible; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:229` — `Editor.setAutocompleteMaxVisible(maxVisible: number)` — exported: **yes** — Implements logic starting with: { const newMaxVisible = Number.isFinite(maxVisible) ? Math.max(3, Math.min(20, Math.floor(maxVisible))) : 5; if (this.autocompleteMaxVisible !== ne… (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:237` — `Editor.setAutocompleteProvider(provider: AutocompleteProvider)` — exported: **yes** — Implements logic starting with: { this.autocompleteProvider = provider; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:333` — `Editor.invalidate()` — exported: **yes** — Implements logic starting with: { this.clearLayoutCaches(); } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:337` — `Editor.render(width: number)` — exported: **yes** — Implements logic starting with: { const maxPadding = Math.max(0, Math.floor((width - 1) / 2)); const paddingX = Math.min(this.paddingX, maxPadding); const contentWidth = Math.max(… (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:459` — `Editor.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); // Handle character jump mode (awaiting next character to jump to) if (this.jumpMode !== null) { // Cancel if… (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:852` — `Editor.getText()` — exported: **yes** — Implements logic starting with: { if (this.cachedText === null) { this.cachedText = this.state.lines.join("\n"); } return this.cachedText; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:872` — `Editor.getLines()` — exported: **yes** — Implements logic starting with: { return [...this.state.lines]; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:876` — `Editor.getCursor()` — exported: **yes** — Implements logic starting with: { return { line: this.state.cursorLine, col: this.state.cursorCol }; } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:880` — `Editor.setText(text: string)` — exported: **yes** — Implements logic starting with: { this.lastAction = null; this.historyIndex = -1; // Exit history browsing mode // Push undo snapshot if content differs (makes programmatic change… (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:2110` — `Editor.dispose()` — exported: **yes** — Implements logic starting with: { this.clearAutocompleteDebounce(); } (missing: jsdoc)
- `packages/pi-tui/src/components/editor.ts:2114` — `Editor.isShowingAutocomplete()` — exported: **yes** — Implements logic starting with: { return this.autocompleteState !== null; } (missing: jsdoc)

### `packages/pi-tui/src/components/image.ts`
- `packages/pi-tui/src/components/image.ts:22` — `class Image` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/components/image.ts:35` — `Image.constructor(base64Data: string, mimeType: string, theme: ImageTheme, options: ImageOptions = {}, dimensions?: ImageDimensions)` — exported: **yes** — Implements logic starting with: { this.base64Data = base64Data; this.mimeType = mimeType; this.theme = theme; this.options = options; this.dimensions = dimensions || { widthPx: 80… (missing: jsdoc)
- `packages/pi-tui/src/components/image.ts:80` — `Image.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedLines = undefined; this.cachedWidth = undefined; } (missing: jsdoc)
- `packages/pi-tui/src/components/image.ts:85` — `Image.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.cachedLines && this.cachedWidth === width) { return this.cachedLines; } const maxWidth = Math.min(width - 2, this.options.maxWidthCells… (missing: jsdoc)

### `packages/pi-tui/src/components/input.ts`
- `packages/pi-tui/src/components/input.ts:29` — `Input.get focused()` — exported: **yes** — Implements logic starting with: { return this._focused; } (missing: jsdoc)
- `packages/pi-tui/src/components/input.ts:32` — `Input.set focused(value: boolean)` — exported: **yes** — Implements logic starting with: { this._focused = value; if (!value) { this.isInPaste = false; this.pasteBuffer = ""; } } (missing: jsdoc)
- `packages/pi-tui/src/components/input.ts:51` — `Input.getValue()` — exported: **yes** — Implements logic starting with: { return this.value; } (missing: jsdoc)
- `packages/pi-tui/src/components/input.ts:55` — `Input.setValue(value: string)` — exported: **yes** — Implements logic starting with: { this.value = value; this.cursor = Math.min(this.cursor, value.length); } (missing: jsdoc)
- `packages/pi-tui/src/components/input.ts:60` — `Input.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { // Handle bracketed paste mode // Start of paste: \x1b[200~ // End of paste: \x1b[201~ // Check if we're starting a bracketed paste if (data.incl… (missing: jsdoc)
- `packages/pi-tui/src/components/input.ts:443` — `Input.invalidate()` — exported: **yes** — Implements logic starting with: { // No cached state to invalidate currently } (missing: jsdoc)
- `packages/pi-tui/src/components/input.ts:447` — `Input.render(width: number)` — exported: **yes** — Implements logic starting with: { // Calculate visible window const prompt = "> "; const availableWidth = width - prompt.length; const renderValue = this.secure ? "*".repeat(this.… (missing: jsdoc)

### `packages/pi-tui/src/components/loader.ts`
- `packages/pi-tui/src/components/loader.ts:16` — `Loader.constructor(ui: TUI, private spinnerColorFn: (str: string) => string, private messageColorFn: (str: string) => string, private message: string = "Loading...")` — exported: **yes** — Implements logic starting with: { super("", 1, 0); this.ui = ui; this.start(); } (missing: jsdoc)
- `packages/pi-tui/src/components/loader.ts:27` — `Loader.render(width: number)` — exported: **yes** — Implements logic starting with: { // Only update Text content when message actually changes — // frame rotation is prepended below without touching the cache if (this.message !==… (missing: jsdoc)
- `packages/pi-tui/src/components/loader.ts:45` — `Loader.start()` — exported: **yes** — Implements logic starting with: { if (this.intervalId) { clearInterval(this.intervalId); } this.currentFrame = 0; this.intervalId = setInterval(() => { this.currentFrame = (this.c… (missing: jsdoc)
- `packages/pi-tui/src/components/loader.ts:62` — `Loader.stop()` — exported: **yes** — Implements logic starting with: { if (this.intervalId) { clearInterval(this.intervalId); this.intervalId = null; } } (missing: jsdoc)
- `packages/pi-tui/src/components/loader.ts:69` — `Loader.dispose()` — exported: **yes** — Implements logic starting with: { this.stop(); this.ui = null; } (missing: jsdoc)
- `packages/pi-tui/src/components/loader.ts:74` — `Loader.setMessage(message: string)` — exported: **yes** — Implements logic starting with: { this.message = message; if (this.ui) { this.ui.requestRender(); } } (missing: jsdoc)

### `packages/pi-tui/src/components/markdown.ts`
- `packages/pi-tui/src/components/markdown.ts:54` — `class Markdown` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/components/markdown.ts:70` — `Markdown.constructor(text: string, paddingX: number, paddingY: number, theme: MarkdownTheme, defaultTextStyle?: DefaultTextStyle)` — exported: **yes** — Implements logic starting with: { this.text = text; this.paddingX = paddingX; this.paddingY = paddingY; this.theme = theme; this.defaultTextStyle = defaultTextStyle; } (missing: jsdoc)
- `packages/pi-tui/src/components/markdown.ts:84` — `Markdown.setText(text: string)` — exported: **yes** — Implements logic starting with: { this.text = text; this.invalidate(); } (missing: jsdoc)
- `packages/pi-tui/src/components/markdown.ts:89` — `Markdown.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedText = undefined; this.cachedWidth = undefined; this.cachedMaxLines = undefined; this.cachedLines = undefined; } (missing: jsdoc)
- `packages/pi-tui/src/components/markdown.ts:96` — `Markdown.render(width: number)` — exported: **yes** — Implements logic starting with: { // Check cache if (this.cachedLines && this.cachedText === this.text && this.cachedWidth === width && this.cachedMaxLines === this.maxLines) { re… (missing: jsdoc)

### `packages/pi-tui/src/components/select-list.ts`
- `packages/pi-tui/src/components/select-list.ts:21` — `class SelectList` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:32` — `SelectList.constructor(items: SelectItem[], maxVisible: number, theme: SelectListTheme)` — exported: **yes** — Implements logic starting with: { this.items = items; this.filteredItems = items; this.maxVisible = maxVisible; this.theme = theme; } (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:39` — `SelectList.setFilter(filter: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:45` — `SelectList.setSelectedIndex(index: number)` — exported: **yes** — Implements logic starting with: { this.selectedIndex = Math.max(0, Math.min(index, this.filteredItems.length - 1)); } (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:49` — `SelectList.invalidate()` — exported: **yes** — Implements logic starting with: { // No cached state to invalidate currently } (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:53` — `SelectList.render(width: number)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; // If no items match filter, show message if (this.filteredItems.length === 0) { lines.push(this.theme.noMatch(" No m… (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:150` — `SelectList.handleInput(keyData: string)` — exported: **yes** — Implements logic starting with: { const kb = getEditorKeybindings(); // Up arrow - wrap to bottom when at top if (kb.matches(keyData, "selectUp")) { this.selectedIndex = this.sele… (missing: jsdoc)
- `packages/pi-tui/src/components/select-list.ts:184` — `SelectList.getSelectedItem()` — exported: **yes** — Implements logic starting with: { const item = this.filteredItems[this.selectedIndex]; return item || null; } (missing: jsdoc)

### `packages/pi-tui/src/components/settings-list.ts`
- `packages/pi-tui/src/components/settings-list.ts:34` — `class SettingsList` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/components/settings-list.ts:49` — `SettingsList.constructor(items: SettingItem[], maxVisible: number, theme: SettingsListTheme, onChange: (id: string, newValue: string) => void, onCancel: () => void, options: SettingsListOptions = {})` — exported: **yes** — Implements logic starting with: { this.items = items; this.filteredItems = items; this.maxVisible = maxVisible; this.theme = theme; this.onChange = onChange; this.onCancel = onCan… (missing: jsdoc)
- `packages/pi-tui/src/components/settings-list.ts:77` — `SettingsList.invalidate()` — exported: **yes** — Implements logic starting with: { this.submenuComponent?.invalidate?.(); } (missing: jsdoc)
- `packages/pi-tui/src/components/settings-list.ts:81` — `SettingsList.render(width: number)` — exported: **yes** — Implements logic starting with: { // If submenu is active, render it instead if (this.submenuComponent) { return this.submenuComponent.render(width); } return this.renderMainList(… (missing: jsdoc)
- `packages/pi-tui/src/components/settings-list.ts:169` — `SettingsList.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { // If submenu is active, delegate all input to it // The submenu's onCancel (triggered by escape) will call done() which closes it if (this.subme… (missing: jsdoc)

### `packages/pi-tui/src/components/spacer.ts`
- `packages/pi-tui/src/components/spacer.ts:9` — `Spacer.constructor(lines: number = 1)` — exported: **yes** — Implements logic starting with: { this.lines = lines; } (missing: jsdoc)
- `packages/pi-tui/src/components/spacer.ts:13` — `Spacer.setLines(lines: number)` — exported: **yes** — Implements logic starting with: { this.lines = lines; } (missing: jsdoc)
- `packages/pi-tui/src/components/spacer.ts:17` — `Spacer.invalidate()` — exported: **yes** — Implements logic starting with: { // No cached state to invalidate currently } (missing: jsdoc)
- `packages/pi-tui/src/components/spacer.ts:21` — `Spacer.render(_width: number)` — exported: **yes** — Implements logic starting with: { const result: string[] = []; for (let i = 0; i < this.lines; i++) { result.push(""); } return result; } (missing: jsdoc)

### `packages/pi-tui/src/components/text.ts`
- `packages/pi-tui/src/components/text.ts:18` — `Text.constructor(text: string = "", paddingX: number = 1, paddingY: number = 1, customBgFn?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { this.text = text; this.paddingX = paddingX; this.paddingY = paddingY; this.customBgFn = customBgFn; } (missing: jsdoc)
- `packages/pi-tui/src/components/text.ts:25` — `Text.setText(text: string)` — exported: **yes** — Implements logic starting with: { if (this.text === text) return; this.text = text; this.cachedText = undefined; this.cachedWidth = undefined; this.cachedLines = undefined; } (missing: jsdoc)
- `packages/pi-tui/src/components/text.ts:33` — `Text.setCustomBgFn(customBgFn?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { this.customBgFn = customBgFn; this.cachedText = undefined; this.cachedWidth = undefined; this.cachedLines = undefined; } (missing: jsdoc)
- `packages/pi-tui/src/components/text.ts:40` — `Text.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedText = undefined; this.cachedWidth = undefined; this.cachedLines = undefined; } (missing: jsdoc)
- `packages/pi-tui/src/components/text.ts:46` — `Text.render(width: number)` — exported: **yes** — Implements logic starting with: { // Check cache if (this.cachedLines && this.cachedText === this.text && this.cachedWidth === width) { return this.cachedLines; } // Don't render… (missing: jsdoc)

### `packages/pi-tui/src/components/truncated-text.ts`
- `packages/pi-tui/src/components/truncated-text.ts:12` — `TruncatedText.constructor(text: string, paddingX: number = 0, paddingY: number = 0)` — exported: **yes** — Implements logic starting with: { this.text = text; this.paddingX = paddingX; this.paddingY = paddingY; } (missing: jsdoc)
- `packages/pi-tui/src/components/truncated-text.ts:18` — `TruncatedText.invalidate()` — exported: **yes** — Implements logic starting with: { // No cached state to invalidate currently } (missing: jsdoc)
- `packages/pi-tui/src/components/truncated-text.ts:22` — `TruncatedText.render(width: number)` — exported: **yes** — Implements logic starting with: { const result: string[] = []; // Empty line padded to width const emptyLine = " ".repeat(width); // Add vertical padding above for (let i = 0; i <… (missing: jsdoc)

### `packages/pi-tui/src/fuzzy.ts`
- `packages/pi-tui/src/fuzzy.ts:12` — `fuzzyMatch(query: string, text: string)` — exported: **yes** — Implements logic starting with: { const queryLower = query.toLowerCase(); const textLower = text.toLowerCase(); const matchQuery = (normalizedQuery: string): FuzzyMatch => { if (n… (missing: jsdoc)

### `packages/pi-tui/src/keybindings.ts`
- `packages/pi-tui/src/keybindings.ts:128` — `EditorKeybindingsManager.constructor(config: EditorKeybindingsConfig = {})` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `packages/pi-tui/src/keybindings.ts:180` — `getEditorKeybindings()` — exported: **yes** — Implements logic starting with: { if (!globalEditorKeybindings) { globalEditorKeybindings = new EditorKeybindingsManager(); } return globalEditorKeybindings; } (missing: jsdoc)
- `packages/pi-tui/src/keybindings.ts:187` — `setEditorKeybindings(manager: EditorKeybindingsManager)` — exported: **yes** — Implements logic starting with: { globalEditorKeybindings = manager; } (missing: jsdoc)

### `packages/pi-tui/src/keys.ts`
- `packages/pi-tui/src/keys.ts:470` — `isKeyRelease(data: string)` — exported: **yes** — Implements logic starting with: { return hasKittyEventType(data, 3); } (missing: jsdoc)
- `packages/pi-tui/src/keys.ts:1069` — `parseKey(data: string)` — exported: **yes** — Implements logic starting with: { const kitty = parseKittySequence(data); if (kitty) { return formatParsedKey(kitty.codepoint, kitty.modifier, kitty.baseLayoutKey); } const modify… (missing: jsdoc)

### `packages/pi-tui/src/kill-ring.ts`
- `packages/pi-tui/src/kill-ring.ts:43` — `KillRing.get length()` — exported: **yes** — Implements logic starting with: { return this.ring.length; } (missing: jsdoc)

### `packages/pi-tui/src/overlay-layout.ts`
- `packages/pi-tui/src/overlay-layout.ts:29` — `resolveAnchorRow(anchor: OverlayAnchor, height: number, availHeight: number, marginTop: number)` — exported: **yes** — Implements logic starting with: { switch (anchor) { case "top-left": case "top-center": case "top-right": return marginTop; case "bottom-left": case "bottom-center": case "bottom-… (missing: jsdoc)
- `packages/pi-tui/src/overlay-layout.ts:51` — `resolveAnchorCol(anchor: OverlayAnchor, width: number, availWidth: number, marginLeft: number)` — exported: **yes** — Implements logic starting with: { switch (anchor) { case "top-left": case "left-center": case "bottom-left": return marginLeft; case "top-right": case "right-center": case "bottom… (missing: jsdoc)

### `packages/pi-tui/src/stdin-buffer.ts`
- `packages/pi-tui/src/stdin-buffer.ts:257` — `StdinBuffer.constructor(options: StdinBufferOptions = {})` — exported: **yes** — Implements logic starting with: { super(); this.timeoutMs = options.timeout ?? 10; this.staleTimeoutMs = options.staleTimeout ?? 1000; } (missing: jsdoc)
- `packages/pi-tui/src/stdin-buffer.ts:263` — `StdinBuffer.process(data: string | Buffer)` — exported: **yes** — Implements logic starting with: { // Clear any pending timeout if (this.timeout) { clearTimeout(this.timeout); this.timeout = null; } if (this.staleTimeout) { clearTimeout(this.st… (missing: jsdoc)
- `packages/pi-tui/src/stdin-buffer.ts:366` — `StdinBuffer.flush()` — exported: **yes** — Implements logic starting with: { if (this.timeout) { clearTimeout(this.timeout); this.timeout = null; } if (this.buffer.length === 0) { return []; } // Keep incomplete escape pre… (missing: jsdoc)
- `packages/pi-tui/src/stdin-buffer.ts:398` — `StdinBuffer.clear()` — exported: **yes** — Implements logic starting with: { if (this.timeout) { clearTimeout(this.timeout); this.timeout = null; } if (this.staleTimeout) { clearTimeout(this.staleTimeout); this.staleTimeou… (missing: jsdoc)
- `packages/pi-tui/src/stdin-buffer.ts:412` — `StdinBuffer.getBuffer()` — exported: **yes** — Implements logic starting with: { return this.buffer; } (missing: jsdoc)
- `packages/pi-tui/src/stdin-buffer.ts:416` — `StdinBuffer.destroy()` — exported: **yes** — Implements logic starting with: { this.clear(); } (missing: jsdoc)

### `packages/pi-tui/src/style.ts`
- `packages/pi-tui/src/style.ts:80` — `class TerminalStyle` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/pi-tui/src/style.ts:83` — `TerminalStyle.constructor(spec: TerminalStyleSpec = {})` — exported: **yes** — Implements logic starting with: { this.spec = { ...spec }; } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:87` — `TerminalStyle.width(width: number)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, width }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:91` — `TerminalStyle.padding(x: number, y = x)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, paddingX: x, paddingY: y }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:95` — `TerminalStyle.paddingX(paddingX: number)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, paddingX }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:99` — `TerminalStyle.paddingY(paddingY: number)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, paddingY }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:103` — `TerminalStyle.border(border: TerminalBorderStyle)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, border }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:107` — `TerminalStyle.density(density: TerminalDensity)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, density }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:111` — `TerminalStyle.tone(tone: TerminalTone, toneColor?: (tone: TerminalTone, text: string) => string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, tone, toneColor }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:115` — `TerminalStyle.borderColor(borderColor: (text: string) => string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, borderColor }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:119` — `TerminalStyle.foreground(foreground: (text: string) => string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, foreground }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:123` — `TerminalStyle.toneColor(toneColor: (tone: TerminalTone, text: string) => string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, toneColor }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:127` — `TerminalStyle.title(title: string, titleColor?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, title, titleColor }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:131` — `TerminalStyle.titleRight(titleRight: string, titleRightColor?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, titleRight, titleRightColor }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:135` — `TerminalStyle.rightTitle(titleRight: string, titleRightColor?: (text: string) => string)` — exported: **yes** — Implements logic starting with: { return this.titleRight(titleRight, titleRightColor); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:139` — `TerminalStyle.bodyGutter(bodyGutter: string)` — exported: **yes** — Implements logic starting with: { return new TerminalStyle({ ...this.spec, bodyGutter }); } (missing: jsdoc)
- `packages/pi-tui/src/style.ts:143` — `TerminalStyle.render(contentLines: string[], width?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `packages/pi-tui/src/style.ts:223` — `style(spec: TerminalStyleSpec = {})` — exported: **yes** — Implements logic starting with: { return new TerminalStyle(spec); } (missing: jsdoc)

### `packages/pi-tui/src/terminal-image.ts`
- `packages/pi-tui/src/terminal-image.ts:32` — `getCellDimensions()` — exported: **yes** — Implements logic starting with: { return cellDimensions; } (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:36` — `setCellDimensions(dims: CellDimensions)` — exported: **yes** — Implements logic starting with: { cellDimensions = dims; } (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:40` — `detectCapabilities()` — exported: **yes** — Implements logic starting with: { const termProgram = process.env.TERM_PROGRAM?.toLowerCase() || ""; const term = process.env.TERM?.toLowerCase() || ""; const colorTerm = process.… (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:78` — `getCapabilities()` — exported: **yes** — Implements logic starting with: { if (!cachedCapabilities) { cachedCapabilities = detectCapabilities(); } return cachedCapabilities; } (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:85` — `resetCapabilitiesCache()` — exported: **yes** — Implements logic starting with: { cachedCapabilities = null; } (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:92` — `isImageLine(line: string)` — exported: **yes** — Implements logic starting with: { // Fast path: sequence at line start (single-row images) if (line.startsWith(KITTY_PREFIX) || line.startsWith(ITERM2_PREFIX)) { return true; } //… (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:111` — `encodeKitty(base64Data: string, options: { columns?: number; rows?: number; imageId?: number; } = {})` — exported: **yes** — Implements logic starting with: { const CHUNK_SIZE = 4096; const params: string[] = ["a=T", "f=100", "q=2"]; if (options.columns) params.push(`c=${options.columns}`); if (options.… (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:170` — `encodeITerm2(base64Data: string, options: { width?: number | string; height?: number | string; name?: string; preserveAspectRatio?: boolean; inline?: boolean; } = {})` — exported: **yes** — Implements logic starting with: { const params: string[] = [`inline=${options.inline !== false ? 1 : 0}`]; if (options.width !== undefined) params.push(`width=${options.width}`);… (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:195` — `calculateImageRows(imageDimensions: ImageDimensions, targetWidthCells: number, cellDimensions: CellDimensions = { widthPx: 9, heightPx: 18 })` — exported: **yes** — Implements logic starting with: { const targetWidthPx = targetWidthCells * cellDimensions.widthPx; const scale = targetWidthPx / imageDimensions.widthPx; const scaledHeightPx = im… (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:222` — `renderImage(base64Data: string, imageDimensions: ImageDimensions, options: ImageRenderOptions = {})` — exported: **yes** — Implements logic starting with: { const caps = getCapabilities(); if (!caps.images) { return null; } const maxWidth = options.maxWidthCells ?? 80; const rows = calculateImageRows(… (missing: jsdoc)
- `packages/pi-tui/src/terminal-image.ts:254` — `imageFallback(mimeType: string, dimensions?: ImageDimensions, filename?: string)` — exported: **yes** — Implements logic starting with: { const parts: string[] = []; if (filename) parts.push(filename); parts.push(`[${mimeType}]`); if (dimensions) parts.push(`${dimensions.widthPx}x${… (missing: jsdoc)

### `packages/pi-tui/src/terminal.ts`
- `packages/pi-tui/src/terminal.ts:69` — `ProcessTerminal.get isTTY()` — exported: **yes** — Implements logic starting with: { return !!process.stdout.isTTY; } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:73` — `ProcessTerminal.get kittyProtocolActive()` — exported: **yes** — Implements logic starting with: { return this._kittyProtocolActive; } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:77` — `ProcessTerminal.start(onInput: (data: string) => void, onResize: () => void)` — exported: **yes** — Implements logic starting with: { // Non-TTY stdout (pipe) — skip TUI initialization entirely. // RPC bridge processes communicate via JSON, not terminal escape codes. // Without… (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:231` — `ProcessTerminal.drainInput(maxMs = 1000, idleMs = 50)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:269` — `ProcessTerminal.stop()` — exported: **yes** — Implements logic starting with: { // Disable bracketed paste mode process.stdout.write("\x1b[?2004l"); // Disable Kitty keyboard protocol if not already done by drainInput() if (t… (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:312` — `ProcessTerminal.write(data: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:323` — `ProcessTerminal.get columns()` — exported: **yes** — Implements logic starting with: { return process.stdout.columns || 80; } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:327` — `ProcessTerminal.get rows()` — exported: **yes** — Implements logic starting with: { return process.stdout.rows || 24; } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:331` — `ProcessTerminal.moveBy(lines: number)` — exported: **yes** — Implements logic starting with: { if (lines > 0) { // Move down process.stdout.write(`\x1b[${lines}B`); } else if (lines < 0) { // Move up process.stdout.write(`\x1b[${-lines}A`);… (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:342` — `ProcessTerminal.hideCursor()` — exported: **yes** — Implements logic starting with: { process.stdout.write("\x1b[?25l"); } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:346` — `ProcessTerminal.showCursor()` — exported: **yes** — Implements logic starting with: { process.stdout.write("\x1b[?25h"); } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:350` — `ProcessTerminal.clearLine()` — exported: **yes** — Implements logic starting with: { process.stdout.write("\x1b[K"); } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:354` — `ProcessTerminal.clearFromCursor()` — exported: **yes** — Implements logic starting with: { process.stdout.write("\x1b[J"); } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:358` — `ProcessTerminal.clearScreen()` — exported: **yes** — Implements logic starting with: { process.stdout.write("\x1b[2J\x1b[H"); // Clear screen and move to home (1,1) } (missing: jsdoc)
- `packages/pi-tui/src/terminal.ts:362` — `ProcessTerminal.setTitle(title: string)` — exported: **yes** — Implements logic starting with: { // OSC 0;title BEL - set terminal window title process.stdout.write(`\x1b]0;${title}\x07`); } (missing: jsdoc)

### `packages/pi-tui/src/tui.ts`
- `packages/pi-tui/src/tui.ts:174` — `Container.addChild(component: Component)` — exported: **yes** — Implements logic starting with: { this.children.push(component); this._prevRender = null; } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:179` — `Container.removeChild(component: Component)` — exported: **yes** — Implements logic starting with: { const index = this.children.indexOf(component); if (index !== -1) { const child = this.children[index]; this.children.splice(index, 1); if ('disp… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:191` — `Container.clear()` — exported: **yes** — Implements logic starting with: { for (const child of this.children) { if ('dispose' in child && typeof (child as any).dispose === 'function') { (child as any).dispose(); } } this… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:212` — `Container.invalidate()` — exported: **yes** — Implements logic starting with: { for (const child of this.children) { child.invalidate?.(); } } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:218` — `Container.render(width: number)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; for (const child of this.children) { const rendered = child.render(width); for (let i = 0; i < rendered.length; i++)… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:276` — `TUI.constructor(terminal: Terminal, showHardwareCursor?: boolean)` — exported: **yes** — Implements logic starting with: { super(); this.terminal = terminal; if (showHardwareCursor !== undefined) { this.showHardwareCursor = showHardwareCursor; } } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:284` — `TUI.get fullRedraws()` — exported: **yes** — Implements logic starting with: { return this.fullRedrawCount; } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:288` — `TUI.getShowHardwareCursor()` — exported: **yes** — Implements logic starting with: { return this.showHardwareCursor; } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:292` — `TUI.setShowHardwareCursor(enabled: boolean)` — exported: **yes** — Implements logic starting with: { if (this.showHardwareCursor === enabled) return; this.showHardwareCursor = enabled; if (!enabled) { this.terminal.hideCursor(); } this.requestRen… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:301` — `TUI.getClearOnShrink()` — exported: **yes** — Implements logic starting with: { return this.clearOnShrink; } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:314` — `TUI.setFocus(component: Component | null)` — exported: **yes** — Implements logic starting with: { // Clear focused flag on old component if (isFocusable(this.focusedComponent)) { this.focusedComponent.focused = false; } this.focusedComponent =… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:435` — `TUI.invalidate()` — exported: **yes** — Implements logic starting with: { super.invalidate(); for (const overlay of this.overlayStack) overlay.component.invalidate?.(); } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:440` — `TUI.start()` — exported: **yes** — Implements logic starting with: { this.stopped = false; // Non-TTY stdout (pipe) — skip TUI entirely to avoid burning CPU. // RPC bridge processes have piped stdio; rendering ANSI… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:457` — `TUI.addInputListener(listener: InputListener)` — exported: **yes** — Implements logic starting with: { this.inputListeners.add(listener); return () => { this.inputListeners.delete(listener); }; } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:464` — `TUI.removeInputListener(listener: InputListener)` — exported: **yes** — Implements logic starting with: { this.inputListeners.delete(listener); } (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:479` — `TUI.stop()` — exported: **yes** — Implements logic starting with: { this.stopped = true; // Dispose all overlays to stop any running timers for (const entry of this.overlayStack) { if ("dispose" in entry.component… (missing: jsdoc)
- `packages/pi-tui/src/tui.ts:506` — `TUI.requestRender(force = false)` — exported: **yes** — Implements logic starting with: { // Skip rendering on non-TTY stdout to prevent CPU burn (issue #3095) if (!this.terminal.isTTY) return; if (force) { this.previousLines = []; thi… (missing: jsdoc)

### `packages/pi-tui/src/undo-stack.ts`
- `packages/pi-tui/src/undo-stack.ts:25` — `UndoStack.get length()` — exported: **yes** — Implements logic starting with: { return this.stack.length; } (missing: jsdoc)

### `packages/rpc-client/src/rpc-client.ts`
- `packages/rpc-client/src/rpc-client.ts:59` — `class RpcClient` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `packages/rpc-client/src/rpc-client.ts:70` — `RpcClient.constructor(private options: RpcClientOptions = {})` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)

### `scripts/__fixtures__/coderabbit-themes/bad-once-after-trigger.ts`
- `scripts/__fixtures__/coderabbit-themes/bad-once-after-trigger.ts:5` — `bad1()` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/bad-once-after-trigger.ts:11` — `bad2()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/bad-once-after-trigger.ts:17` — `bad3()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `scripts/__fixtures__/coderabbit-themes/bad-sqlite-null-guard.ts`
- `scripts/__fixtures__/coderabbit-themes/bad-sqlite-null-guard.ts:7` — `bad1(k: string)` — exported: **yes** — Implements logic starting with: { const row = stmt.get(k); if (row !== null) { return (row as { v: number }).v; } return 0; } (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/bad-sqlite-null-guard.ts:15` — `bad2(k: string)` — exported: **yes** — Implements logic starting with: { const row = stmt.get(k); if (row === null) return null; return row; } (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/bad-sqlite-null-guard.ts:21` — `bad3(k: string)` — exported: **yes** — Implements logic starting with: { const r = stmt.pluck().get(k); return r !== null ? r : "default"; } (missing: jsdoc)

### `scripts/__fixtures__/coderabbit-themes/good-once-after-trigger.ts`
- `scripts/__fixtures__/coderabbit-themes/good-once-after-trigger.ts:5` — `good1()` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/good-once-after-trigger.ts:12` — `good2()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/good-once-after-trigger.ts:20` — `good3()` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `scripts/__fixtures__/coderabbit-themes/good-sqlite-null-guard.ts`
- `scripts/__fixtures__/coderabbit-themes/good-sqlite-null-guard.ts:7` — `good1(k: string)` — exported: **yes** — Implements logic starting with: { const row = stmt.get(k); if (row != null) { return (row as { v: number }).v; } return 0; } (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/good-sqlite-null-guard.ts:15` — `good2(k: string)` — exported: **yes** — Implements logic starting with: { const row = stmt.get(k); if (row === undefined) return null; return row; } (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/good-sqlite-null-guard.ts:21` — `good3(k: string)` — exported: **yes** — Implements logic starting with: { const r = stmt.pluck().get(k); return r ?? "default"; } (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/good-sqlite-null-guard.ts:26` — `good4(k: string)` — exported: **yes** — Implements logic starting with: { const row = stmt.get(k); if (row) { return (row as { v: number }).v; } } (missing: jsdoc)
- `scripts/__fixtures__/coderabbit-themes/good-sqlite-null-guard.ts:34` — `unrelated(obj: { v: number } | null)` — exported: **yes** — Implements logic starting with: { if (obj !== null) return obj.v; return 0; } (missing: jsdoc)

### `scripts/check-coderabbit-themes.mjs`
- `scripts/check-coderabbit-themes.mjs:124` — `checkSqliteNullGuard(filePath, source)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `scripts/check-coderabbit-themes.mjs:183` — `checkOnceAfterTrigger(filePath, source)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `scripts/check-coderabbit-themes.mjs:250` — `checkMjsTsImport(filePath, source, invocationsOverride)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `scripts/check-coderabbit-themes.mjs:275` — `hasStripTypesFlag(cmd)` — exported: **yes** — Implements logic starting with: { return ( cmd.includes("--experimental-strip-types") || cmd.includes("--import tsx") || cmd.includes("--loader tsx") || cmd.includes("--loader ts-… (missing: jsdoc)
- `scripts/check-coderabbit-themes.mjs:395` — `runChecks(files)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `scripts/compile-tests.mjs`
- `scripts/compile-tests.mjs:105` — `buildCompileFingerprint(entries)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `scripts/compile-tests.mjs:122` — `isCompileCacheFresh(cache, fingerprint, distTestExists)` — exported: **yes** — Implements logic starting with: { return Boolean( distTestExists && cache && cache.schemaVersion === CACHE_SCHEMA_VERSION && cache.hash === fingerprint.hash, ); } (missing: jsdoc)

### `scripts/dev-cli-helpers.mjs`
- `scripts/dev-cli-helpers.mjs:3` — `buildDevCliSpawnArgs({ resolveTsPath, srcLoaderPath, argv, })` — exported: **yes** — Implements logic starting with: { return ['--import', resolveTsPath, '--experimental-strip-types', srcLoaderPath, ...argv] } (missing: jsdoc)
- `scripts/dev-cli-helpers.mjs:11` — `buildDevCliChildEnv(baseEnv, devCliPath)` — exported: **yes** — Implements logic starting with: { return { ...baseEnv, // Child GSD processes (subagents, parallel workers, workflow MCP) // must re-enter through this wrapper so source-mode TS i… (missing: jsdoc)

### `scripts/dist-test-resolve.mjs`
- `scripts/dist-test-resolve.mjs:32` — `resolve(specifier, context, nextResolve)` — exported: **yes** — Implements logic starting with: { // 1. @gsd/* bare imports → compiled dist-test counterpart if (specifier in GSD_ALIASES) { return nextResolve(GSD_ALIASES[specifier], context); }… (missing: jsdoc)

### `scripts/legacy-cleanup-evidence.mjs`
- `scripts/legacy-cleanup-evidence.mjs:17` — `parseArgs(argv = process.argv.slice(2), env = process.env)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/legacy-cleanup-evidence.mjs:60` — `parseCommandSpec(value)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/legacy-cleanup-evidence.mjs:73` — `ensureTelemetryReport(file)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `scripts/legacy-cleanup-evidence.mjs:89` — `runEvidenceCommand(command, env)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/legacy-cleanup-evidence.mjs:103` — `collectLegacyCleanupEvidence(opts)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `scripts/legacy-cleanup-gate.mjs`
- `scripts/legacy-cleanup-gate.mjs:15` — `parseArgs(argv = process.argv.slice(2), env = process.env)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/legacy-cleanup-gate.mjs:47` — `readTelemetryReport(file)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/legacy-cleanup-gate.mjs:60` — `evaluateLegacyCleanupGate(report)` — exported: **yes** — Implements logic starting with: { const counters = {}; const nonZero = []; const missing = []; for (const counter of LEGACY_COUNTERS) { const value = report.counters[counter]; if… (missing: jsdoc)
- `scripts/legacy-cleanup-gate.mjs:85` — `renderLegacyCleanupGateSummary(result)` — exported: **yes** — Implements logic starting with: { const lines = [ "GSD-2 Legacy Cleanup Gate", `Snapshot: ${result.ts || "unknown"}`, `Status: ${result.ok ? "PASS" : "BLOCK"}`, "", "Counters:", ]… (missing: jsdoc)

### `scripts/refactor-baseline.mjs`
- `scripts/refactor-baseline.mjs:96` — `parseArgs(argv = process.argv.slice(2))` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:135` — `parseCommandSpec(spec)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:146` — `collectBaseline(root, commandSpecs = [])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:206` — `collectTestCompileMetrics(root)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:242` — `collectPromptMetrics(root)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:268` — `collectContextMetrics(root)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:286` — `collectWorkspaceMetrics(root)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:305` — `collectContractsMetrics(root)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:343` — `collectProcessMetrics(root)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:394` — `collectDirectoryMetrics(dir)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:414` — `collectFiles(dir, predicate)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:435` — `fileTextMetric(root, file, content)` — exported: **yes** — Implements logic starting with: { return { path: normalizePath(relative(root, file)), chars: content.length, bytes: Buffer.byteLength(content, "utf8"), lines: content.length === 0… (missing: jsdoc)
- `scripts/refactor-baseline.mjs:445` — `sum(entries, key)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:449` — `normalizePath(path)` — exported: **yes** — Implements logic starting with: { return path.split("\\").join("/"); } (missing: jsdoc)
- `scripts/refactor-baseline.mjs:453` — `buildMetricIndex(report)` — exported: **yes** — Implements logic starting with: { const metrics = { "prompt.fileCount": report.prompt.fileCount, "prompt.totalChars": report.prompt.totalChars, "prompt.totalBytes": report.prompt.… (missing: jsdoc)
- `scripts/refactor-baseline.mjs:521` — `numberOrZero(value)` — exported: **yes** — Implements logic starting with: { return typeof value === "number" && Number.isFinite(value) ? value : 0; } (missing: jsdoc)
- `scripts/refactor-baseline.mjs:525` — `compareReports(previous, current)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:557` — `loadBaselineReport(path)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:569` — `writeJsonFile(path, value)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:574` — `numberOrNull(value)` — exported: **yes** — Implements logic starting with: { return typeof value === "number" && Number.isFinite(value) ? value : null; } (missing: jsdoc)
- `scripts/refactor-baseline.mjs:578` — `countMatches(value, pattern)` — exported: **yes** — Implements logic starting with: { return Array.from(value.matchAll(pattern)).length; } (missing: jsdoc)
- `scripts/refactor-baseline.mjs:582` — `countLegacyContractImports(value)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:598` — `hasProcessDocConflict(content)` — exported: **yes** — Implements logic starting with: { return /markdown\s+(?:files?\s+)?(?:are|is)\s+(?:the\s+)?authoritative/i.test(content) || /filesystem[-\s]+authoritative/i.test(content) || /\.gs… (missing: jsdoc)
- `scripts/refactor-baseline.mjs:604` — `metricSafeLabel(label)` — exported: **yes** — Implements logic starting with: { return label .trim() .replace(/[^A-Za-z0-9_.-]+/g, "-") .replace(/^-+|-+$/g, "") || "command"; } (missing: jsdoc)
- `scripts/refactor-baseline.mjs:611` — `timeCommand(root, spec)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `scripts/refactor-baseline.mjs:644` — `renderSummary(report)` — exported: **yes** — Implements logic starting with: { const lines = [ "GSD-2 Refactor Baseline", `Generated: ${report.generatedAt}`, `Root: ${report.root}`, `Schema version: ${report.schemaVersion}`,… (missing: jsdoc)
- `scripts/refactor-baseline.mjs:720` — `formatDelta(delta)` — exported: **yes** — Implements logic starting with: { if (delta === null) return "n/a"; return delta >= 0 ? `+${delta}` : String(delta); } (missing: jsdoc)
- `scripts/refactor-baseline.mjs:725` — `formatDeltaPercent(deltaPercent)` — exported: **yes** — Implements logic starting with: { if (deltaPercent === null) return "n/a"; return deltaPercent >= 0 ? `+${deltaPercent}%` : `${deltaPercent}%`; } (missing: jsdoc)

### `scripts/test-reporter-compact.mjs`
- `scripts/test-reporter-compact.mjs:7` — `class CompactReporter` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `scripts/test-reporter-compact.mjs:13` — `CompactReporter.constructor()` — exported: **yes** — Implements logic starting with: { super({ objectMode: true }); } (missing: jsdoc)
- `scripts/test-reporter-compact.mjs:17` — `CompactReporter._transform(event, _enc, cb)` — exported: **yes** — Implements logic starting with: { switch (event.type) { case 'test:pass': if (!event.data.skip) this.#pass++; else this.#skip++; break; case 'test:fail': { this.#fail++; const { n… (missing: jsdoc)
- `scripts/test-reporter-compact.mjs:36` — `CompactReporter._flush(cb)` — exported: **yes** — Implements logic starting with: { if (this.#failures.length) { this.push(`\n✖ failing tests:\n${this.#failures.join('\n\n')}\n`); } const status = this.#fail === 0 ? '✔' : '✖'; th… (missing: jsdoc)

### `scripts/verify-changed-src-tests.mjs`
- `scripts/verify-changed-src-tests.mjs:16` — `normalizeRepoPath(path)` — exported: **yes** — Implements logic starting with: { return path.replaceAll('\\', '/').replace(/^\.\//, ''); } (missing: jsdoc)
- `scripts/verify-changed-src-tests.mjs:20` — `isSourceCandidate(path)` — exported: **yes** — Implements logic starting with: { const normalized = normalizeRepoPath(path); return ( normalized.startsWith('src/') && SOURCE_RE.test(normalized) && !normalized.endsWith('.d.ts')… (missing: jsdoc)
- `scripts/verify-changed-src-tests.mjs:30` — `isTestFile(path)` — exported: **yes** — Implements logic starting with: { return TEST_FILE_RE.test(normalizeRepoPath(path)); } (missing: jsdoc)
- `scripts/verify-changed-src-tests.mjs:34` — `candidateTestsForSource(path)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `scripts/verify-changed-src-tests.mjs:60` — `selectChangedSrcTests(changedFiles, exists = existsSync)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `scripts/verify-changed-src-tests.mjs:78` — `buildNodeTestArgs(testFiles)` — exported: **yes** — Implements logic starting with: { return [ '--import', './src/resources/extensions/gsd/tests/resolve-ts.mjs', '--experimental-strip-types', '--test', ...testFiles, ]; } (missing: jsdoc)
- `scripts/verify-changed-src-tests.mjs:88` — `parseArgs(argv)` — exported: **yes** — Implements logic starting with: { const options = { files: [], list: false, base: 'HEAD', }; for (let i = 0; i < argv.length; i++) { const arg = argv[i]; if (arg === '--list') { o… (missing: jsdoc)

### `src/bundled-extension-paths.ts`
- `src/bundled-extension-paths.ts:3` — `serializeBundledExtensionPaths(paths: readonly string[], pathDelimiter = delimiter)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/bundled-extension-paths.ts:10` — `parseBundledExtensionPaths(value: string | undefined, pathDelimiter = delimiter)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/bundled-resource-path.ts`
- `src/bundled-resource-path.ts:7` — `resolvePackageRoot(importUrl: string)` — exported: **yes** — Implements logic starting with: { const moduleDir = dirname(fileURLToPath(importUrl)); return resolve(moduleDir, ".."); } (missing: jsdoc)
- `src/bundled-resource-path.ts:12` — `hasCompleteBundledResources(resourcesDir: string, fileExists: FileExists = existsSync)` — exported: **yes** — Implements logic starting with: { return fileExists(join(resourcesDir, "agents")) && fileExists(join(resourcesDir, "extensions")); } (missing: jsdoc)
- `src/bundled-resource-path.ts:20` — `resolveBundledResourcesDirFromPackageRoot(packageRoot: string, fileExists: FileExists = existsSync)` — exported: **yes** — Implements logic starting with: { const distResources = join(packageRoot, "dist", "resources"); const srcResources = join(packageRoot, "src", "resources"); return hasCompleteBundl… (missing: jsdoc)
- `src/bundled-resource-path.ts:31` — `resolveBundledResourcesDir(importUrl: string, fileExists: FileExists = existsSync)` — exported: **yes** — Implements logic starting with: { return resolveBundledResourcesDirFromPackageRoot(resolvePackageRoot(importUrl), fileExists); } (missing: jsdoc)
- `src/bundled-resource-path.ts:38` — `resolveBundledResource(importUrl: string, ...segments: string[])` — exported: **yes** — Implements logic starting with: { return join(resolveBundledResourcesDir(importUrl), ...segments); } (missing: jsdoc)
- `src/bundled-resource-path.ts:45` — `resolveBundledGsdExtensionModule(importUrl: string, moduleFile: string, fileExists: FileExists = existsSync)` — exported: **yes** — Implements logic starting with: { const packageRoot = resolvePackageRoot(importUrl); const distResources = join(packageRoot, "dist", "resources"); const jsFile = moduleFile.replac… (missing: jsdoc)

### `src/cli-auto-routing.ts`
- `src/cli-auto-routing.ts:1` — `shouldRedirectAutoToHeadless(subcommand: string | undefined, stdinIsTTY: boolean | undefined, stdoutIsTTY: boolean | undefined)` — exported: **yes** — Implements logic starting with: { if (subcommand !== 'auto') return false return stdinIsTTY !== true || stdoutIsTTY !== true } (missing: jsdoc)

### `src/cli-web-branch.ts`
- `src/cli-web-branch.ts:45` — `parseCliArgs(argv: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/cli-web-branch.ts:100` — `buildHeadlessAutoArgs(flags: Pick<CliFlags, 'messages' | 'model'>)` — exported: **yes** — Implements logic starting with: { return flags.model ? ['--model', flags.model, ...flags.messages] : [...flags.messages] } (missing: jsdoc)
- `src/cli-web-branch.ts:106` — `migrateLegacyFlatSessions(baseSessionsDir: string, projectSessionsDir: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/cli-web-branch.ts:206` — `runWebCliBranch(flags: CliFlags, deps: RunWebCliBranchDeps = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/extension-registry.ts`
- `src/extension-registry.ts:71` — `getRegistryPath()` — exported: **yes** — Implements logic starting with: { return join(appRoot, "extensions", "registry.json"); } (missing: jsdoc)
- `src/extension-registry.ts:81` — `loadRegistry()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/extension-registry.ts:93` — `saveRegistry(registry: ExtensionRegistry)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/extension-registry.ts:116` — `enableExtension(registry: ExtensionRegistry, id: string)` — exported: **yes** — Implements logic starting with: { const entry = registry.entries[id]; if (entry) { entry.enabled = true; delete entry.disabledAt; delete entry.disabledReason; } else { registry.en… (missing: jsdoc)

### `src/headless-answers.ts`
- `src/headless-answers.ts:40` — `loadAndValidateAnswerFile(path: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/headless-answers.ts:104` — `class AnswerInjector` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/headless-answers.ts:116` — `AnswerInjector.constructor(answerFile: AnswerFile)` — exported: **yes** — Implements logic starting with: { this.answerFile = answerFile } (missing: jsdoc)

### `src/headless-context.ts`
- `src/headless-context.ts:24` — `readStdin()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/headless-context.ts:36` — `loadContext(options: ContextOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/headless-events.ts`
- `src/headless-events.ts:75` — `isTerminalNotification(event: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { if (event.type !== 'extension_ui_request' || event.method !== 'notify') return false const message = String(event.message ?? '').toLowerCase() re… (missing: jsdoc)
- `src/headless-events.ts:81` — `isBlockedNotification(event: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { if (event.type !== 'extension_ui_request' || event.method !== 'notify') return false const message = String(event.message ?? '').toLowerCase() //… (missing: jsdoc)
- `src/headless-events.ts:88` — `isMilestoneReadyNotification(event: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { if (event.type !== 'extension_ui_request' || event.method !== 'notify') return false return /milestone\s+m\d+.*ready/i.test(String(event.message… (missing: jsdoc)
- `src/headless-events.ts:93` — `isInteractiveHeadlessTool(toolName: string | undefined)` — exported: **yes** — Implements logic starting with: { return INTERACTIVE_HEADLESS_TOOLS.has(String(toolName ?? '')) } (missing: jsdoc)
- `src/headless-events.ts:97` — `shouldArmHeadlessIdleTimeout(toolCallCount: number, interactiveToolCount: number)` — exported: **yes** — Implements logic starting with: { return toolCallCount > 0 && interactiveToolCount === 0 } (missing: jsdoc)
- `src/headless-events.ts:116` — `isQuickCommand(command: string, commandArgs: readonly string[] = [])` — exported: **yes** — Implements logic starting with: { if (QUICK_COMMANDS.has(command)) return true return command === 'workflow' && QUICK_WORKFLOW_SUBCOMMANDS.has(commandArgs[0] ?? '') } (missing: jsdoc)

### `src/headless-query.ts`
- `src/headless-query.ts:122` — `runHeadlessQuery(basePath: string, modules: HeadlessQueryModules, writeOutput: (text: string) => void = (text) => process.stdout.write(text))` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/headless-query.ts:181` — `handleQuery(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/headless-recover.ts`
- `src/headless-recover.ts:68` — `handleRecover(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/headless-ui.ts`
- `src/headless-ui.ts:209` — `handleExtensionUIRequest(event: ExtensionUIRequest, client: RpcClient)` — exported: **yes** — Implements logic starting with: { const { id, method } = event switch (method) { case 'select': { // Lock-guard prompts list "View status" first, but headless needs "Force start"… (missing: jsdoc)
- `src/headless-ui.ts:255` — `formatProgress(event: Record<string, unknown>, ctx: ProgressContext)` — exported: **yes** — Implements logic starting with: { const type = String(event.type ?? '') // Emit accumulated thinking preview before tool calls if (ctx.thinkingPreview) { // thinkingPreview is han… (missing: jsdoc)
- `src/headless-ui.ts:419` — `startSupervisedStdinReader(client: RpcClient, onResponse: (id: string) => void)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)

### `src/headless.ts`
- `src/headless.ts:147` — `parseHeadlessArgs(argv: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/headless.ts:238` — `runHeadless(options: HeadlessOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/help-text.ts`
- `src/help-text.ts:176` — `printHelp(version: string)` — exported: **yes** — Implements logic starting with: { process.stdout.write(`GSD v${version} — Get Shit Done\n\n`) process.stdout.write('Usage: gsd [options] [message...]\n\n') process.stdout.write('O… (missing: jsdoc)
- `src/help-text.ts:205` — `printSubcommandHelp(subcommand: string, version: string)` — exported: **yes** — Implements logic starting with: { const help = SUBCOMMAND_HELP[subcommand] if (!help) return false process.stdout.write(`GSD v${version} — Get Shit Done\n\n`) process.stdout.write… (missing: jsdoc)

### `src/loader-entrypoint.ts`
- `src/loader-entrypoint.ts:13` — `resolveLoaderCliEntrypoint({ gsdRoot, invokedBinPath, env = process.env, existsSync = defaultExistsSync, }: LoaderEntrypointOptions)` — exported: **yes** — Implements logic starting with: { const sourceLoaderPath = join(gsdRoot, "src", "loader.ts") const devCliPath = env.GSD_DEV_CLI_PATH?.trim() || join(gsdRoot, "scripts", "dev-cli.j… (missing: jsdoc)
- `src/loader-entrypoint.ts:27` — `applyLoaderCliEntrypointEnv(env: NodeJS.ProcessEnv, options: LoaderEntrypointOptions)` — exported: **yes** — Implements logic starting with: { const resolvedGsdBinPath = resolveLoaderCliEntrypoint({ ...options, env }) if (resolvedGsdBinPath) { env.GSD_BIN_PATH = resolvedGsdBinPath if (!e… (missing: jsdoc)

### `src/mcp-server.ts`
- `src/mcp-server.ts:57` — `mcpSdkSpecifier(subpath: 'server/index' | 'server/stdio' | 'types')` — exported: **yes** — Implements logic starting with: { return `${MCP_PKG}/${subpath}.js` } (missing: jsdoc)

### `src/onboarding.ts`
- `src/onboarding.ts:203` — `detectNativeProviderFromBaseUrl(baseUrl: string)` — exported: **yes** — Implements logic starting with: { try { const hostname = new URL(baseUrl).hostname.toLowerCase() if (hostname === 'api.minimax.io' || hostname.endsWith('.minimax.io')) { return 'm… (missing: jsdoc)
- `src/onboarding.ts:385` — `runLlmStep(p: ClackModule, pc: PicoModule, authStorage: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/onboarding.ts:754` — `runWebSearchStep(p: ClackModule, pc: PicoModule, authStorage: AuthStorage, isAnthropicAuth: boolean)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/onboarding.ts:835` — `runToolKeysStep(p: ClackModule, pc: PicoModule, authStorage: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/onboarding.ts:878` — `runRemoteQuestionsStep(p: ClackModule, pc: PicoModule, authStorage: AuthStorage)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: jsdoc)

### `src/pi-migration.ts`
- `src/pi-migration.ts:62` — `getPiDefaultModelAndProvider()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/project-sessions.ts`
- `src/project-sessions.ts:5` — `getProjectSessionsDir(cwd: string, baseSessionsDir = defaultSessionsDir)` — exported: **yes** — Implements logic starting with: { const safePath = `--${cwd.replace(/^[/\\]/, "").replace(/[/\\:]/g, "-")}--` return join(baseSessionsDir, safePath) } (missing: jsdoc)

### `src/provider-migrations.ts`
- `src/provider-migrations.ts:29` — `hasDirectAnthropicApiKey(authStorage: Pick<AuthStorage, "getCredentialsForProvider">, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { if ((env.ANTHROPIC_API_KEY ?? "").trim()) { return true } return authStorage.getCredentialsForProvider("anthropic").some((credential: { type?: st… (missing: jsdoc)
- `src/provider-migrations.ts:42` — `shouldMigrateAnthropicToClaudeCode({ authStorage, isClaudeCodeReady, defaultProvider, env = process.env, }: AnthropicMigrationDeps)` — exported: **yes** — Implements logic starting with: { if (defaultProvider !== "anthropic") { return false } if (hasDirectAnthropicApiKey(authStorage, env)) { return false } return typeof isClaudeCode… (missing: jsdoc)
- `src/provider-migrations.ts:59` — `migrateAnthropicDefaultToClaudeCode({ authStorage, isClaudeCodeReady, settingsManager, modelRegistry, env = process.env, }: AnthropicDefaultMigrationDeps)` — exported: **yes** — Implements logic starting with: { const defaultProvider = settingsManager.getDefaultProvider() if (!shouldMigrateAnthropicToClaudeCode({ authStorage, isClaudeCodeReady, defaultPro… (missing: jsdoc)

### `src/remote-questions-config.ts`
- `src/remote-questions-config.ts:21` — `saveRemoteQuestionsConfig(channel: "slack" | "discord" | "telegram", channelId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resource-loader.ts`
- `src/resource-loader.ts:56` — `getExtensionKey(entryPath: string, extensionsDir: string)` — exported: **yes** — Implements logic starting with: { const relPath = relative(extensionsDir, entryPath) return relPath.split(/[\\/]/)[0].replace(/\.(?:ts|js)$/, '') } (missing: jsdoc)
- `src/resource-loader.ts:119` — `readManagedResourceVersion(agentDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resource-loader.ts:198` — `getNewerManagedResourceVersion(agentDir: string, currentVersion: string)` — exported: **yes** — Implements logic starting with: { const managedVersion = readManagedResourceVersion(agentDir) if (!managedVersion) { return null } // Managed resources stamped from the same relea… (missing: jsdoc)
- `src/resource-loader.ts:727` — `hasStaleCompiledExtensionSiblings(extensionsDir: string, sourceDir: string = bundledExtensionsDir)` — exported: **yes** — Implements logic starting with: { if (!existsSync(extensionsDir)) return false const sourceFiles = collectRelativeFiles(sourceDir) const installedFiles = collectRelativeFiles(exte… (missing: jsdoc)
- `src/resource-loader.ts:786` — `buildResourceLoader(agentDir: string, options: BuildResourceLoaderOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/ask-user-questions.ts`
- `src/resources/extensions/ask-user-questions.ts:98` — `questionSignature(questions: Question[])` — exported: **yes** — Serializes a value to JSON. (missing: description, @param questions, @returns)
- `src/resources/extensions/ask-user-questions.ts:209` — `AskUserQuestions(pi: ExtensionAPI)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/async-jobs/async-bash-tool.ts`
- `src/resources/extensions/async-jobs/async-bash-tool.ts:63` — `createAsyncBashTool(getManager: () => AsyncJobManager, getCwd: () => string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/async-jobs/await-tool.ts`
- `src/resources/extensions/async-jobs/await-tool.ts:29` — `createAwaitTool(getManager: () => AsyncJobManager)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/async-jobs/cancel-job-tool.ts`
- `src/resources/extensions/async-jobs/cancel-job-tool.ts:13` — `createCancelJobTool(getManager: () => AsyncJobManager)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/async-jobs/index.ts`
- `src/resources/extensions/async-jobs/index.ts:23` — `AsyncJobs(pi: ExtensionAPI)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/async-jobs/job-manager.ts`
- `src/resources/extensions/async-jobs/job-manager.ts:44` — `class AsyncJobManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/async-jobs/job-manager.ts:53` — `AsyncJobManager.constructor(options: JobManagerOptions = {})` — exported: **yes** — Implements logic starting with: { this.maxRunning = options.maxRunning ?? 15; this.maxTotal = options.maxTotal ?? 100; this.evictionMs = options.evictionMs ?? 5 * 60 * 1000; this.… (missing: jsdoc)
- `src/resources/extensions/async-jobs/job-manager.ts:141` — `AsyncJobManager.getJob(id: string)` — exported: **yes** — Implements logic starting with: { return this.jobs.get(id); } (missing: jsdoc)
- `src/resources/extensions/async-jobs/job-manager.ts:145` — `AsyncJobManager.getRunningJobs()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/async-jobs/job-manager.ts:149` — `AsyncJobManager.getRecentJobs(limit = 10)` — exported: **yes** — Implements logic starting with: { return [...this.jobs.values()] .sort((a, b) => b.startTime - a.startTime) .slice(0, limit); } (missing: jsdoc)
- `src/resources/extensions/async-jobs/job-manager.ts:155` — `AsyncJobManager.getAllJobs()` — exported: **yes** — Implements logic starting with: { return [...this.jobs.values()]; } (missing: jsdoc)

### `src/resources/extensions/bg-shell/bg-shell-command.ts`
- `src/resources/extensions/bg-shell/bg-shell-command.ts:25` — `registerBgShellCommand(pi: ExtensionAPI, state: BgShellSharedState)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/bg-shell/bg-shell-lifecycle.ts`
- `src/resources/extensions/bg-shell/bg-shell-lifecycle.ts:32` — `registerBgShellLifecycle(pi: ExtensionAPI, state: BgShellSharedState)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/bg-shell/bg-shell-tool.ts`
- `src/resources/extensions/bg-shell/bg-shell-tool.ts:33` — `registerBgShellTool(pi: ExtensionAPI, state: BgShellSharedState)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/bg-shell/interaction.ts`
- `src/resources/extensions/bg-shell/interaction.ts:11` — `queryShellEnv(bg: BgProcess, timeout: number, signal?: AbortSignal)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/bg-shell/interaction.ts:78` — `sendAndWait(bg: BgProcess, input: string, waitPattern: string, timeout: number, signal?: AbortSignal)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/interaction.ts:117` — `runOnSession(bg: BgProcess, command: string, timeout: number, signal?: AbortSignal)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/bg-shell/output-formatter.ts`
- `src/resources/extensions/bg-shell/output-formatter.ts:32` — `analyzeLine(bg: BgProcess, line: string, stream: "stdout" | "stderr")` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/bg-shell/output-formatter.ts:112` — `generateDigest(bg: BgProcess, mutate: boolean = false)` — exported: **yes** — Implements logic starting with: { // Change summary: what's different since last read const newErrors = bg.recentErrors.length - bg.lastErrorCount; const newWarnings = bg.recentWa… (missing: jsdoc)
- `src/resources/extensions/bg-shell/output-formatter.ts:152` — `getHighlights(bg: BgProcess, maxLines: number = 15)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; // Collect significant lines const significant: { line: string; score: number; idx: number }[] = []; for (let i = 0;… (missing: jsdoc)
- `src/resources/extensions/bg-shell/output-formatter.ts:190` — `getOutput(bg: BgProcess, opts: GetOutputOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/output-formatter.ts:236` — `formatDigestText(bg: BgProcess, digest: OutputDigest)` — exported: **yes** — Implements logic starting with: { let text = `Process ${bg.id} (${bg.label}):\n`; text += ` status: ${digest.status}\n`; text += ` type: ${bg.processType}\n`; text += ` uptime: ${… (missing: jsdoc)

### `src/resources/extensions/bg-shell/overlay.ts`
- `src/resources/extensions/bg-shell/overlay.ts:17` — `class BgManagerOverlay` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/bg-shell/overlay.ts:29` — `BgManagerOverlay.constructor(tui: { requestRender: () => void }, theme: Theme, onClose: () => void)` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.onClose = onClose; this.refreshTimer = setInterval(() => { this.invalidate(); this.tui.requestRender();… (missing: jsdoc)
- `src/resources/extensions/bg-shell/overlay.ts:47` — `BgManagerOverlay.selectAndView(index: number)` — exported: **yes** — Implements logic starting with: { const procs = this.getProcessList(); if (index >= 0 && index < procs.length) { this.selected = index; this.viewingProcess = procs[index]; this.mo… (missing: jsdoc)
- `src/resources/extensions/bg-shell/overlay.ts:57` — `BgManagerOverlay.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if (this.mode === "output") { this.handleOutputInput(data); return; } if (this.mode === "events") { this.handleEventsInput(data); return; } this.… (missing: jsdoc)
- `src/resources/extensions/bg-shell/overlay.ts:255` — `BgManagerOverlay.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.cachedLines && this.cachedWidth === width) { return this.cachedLines; } let lines: string[]; if (this.mode === "events") { lines = this.… (missing: jsdoc)
- `src/resources/extensions/bg-shell/overlay.ts:433` — `BgManagerOverlay.dispose()` — exported: **yes** — Implements logic starting with: { clearInterval(this.refreshTimer); } (missing: jsdoc)
- `src/resources/extensions/bg-shell/overlay.ts:437` — `BgManagerOverlay.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedWidth = undefined; this.cachedLines = undefined; } (missing: jsdoc)

### `src/resources/extensions/bg-shell/process-manager.ts`
- `src/resources/extensions/bg-shell/process-manager.ts:43` — `addOutputLine(bg: BgProcess, stream: "stdout" | "stderr", line: string)` — exported: **yes** — Implements logic starting with: { bg.output.push({ stream, line, ts: Date.now() }); if (stream === "stdout") bg.stdoutLineCount++; else bg.stderrLineCount++; if (bg.output.length… (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:55` — `addEvent(bg: BgProcess, event: Omit<ProcessEvent, "timestamp">)` — exported: **yes** — Implements logic starting with: { const ev: ProcessEvent = { ...event, timestamp: Date.now() }; bg.events.push(ev); if (bg.events.length > MAX_EVENTS) { bg.events.splice(0, bg.eve… (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:63` — `pushAlert(bg: BgProcess | null, message: string)` — exported: **yes** — Implements logic starting with: { const prefix = bg ? `[bg:${bg.id} ${bg.label}] ` : ""; pendingAlerts.push(`${prefix}${message}`); if (pendingAlerts.length > MAX_PENDING_ALERTS)… (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:71` — `getInfo(p: BgProcess)` — exported: **yes** — Implements logic starting with: { return { id: p.id, label: p.label, command: p.command, cwd: p.cwd, ownerSessionFile: p.ownerSessionFile, persistAcrossSessions: p.persistAcrossSe… (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:101` — `detectProcessType(command: string)` — exported: **yes** — Implements logic starting with: { const cmd = command.toLowerCase(); // Server patterns if ( /\b(serve|server|dev|start)\b/.test(cmd) && /\b(npm|yarn|pnpm|bun|node|next|vite|nuxt|… (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:129` — `startProcess(opts: StartOptions)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:262` — `killProcess(id: string, sig: NodeJS.Signals = "SIGTERM")` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:302` — `restartProcess(id: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:339` — `getGroupProcesses(group: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:343` — `getGroupStatus(group: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:364` — `pruneDeadProcesses()` — exported: **yes** — Implements logic starting with: { const now = Date.now(); for (const [id, bg] of processes) { if (!bg.alive) { const ttl = bg.processType === "shell" ? DEAD_PROCESS_TTL * 6 : DEAD… (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:376` — `cleanupAll()` — exported: **yes** — Implements logic starting with: { for (const [id, bg] of processes) { if (bg.alive) killProcess(id, "SIGKILL"); } processes.clear(); } (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:409` — `cleanupSessionProcesses(sessionFile: string, options?: { graceMs?: number })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:433` — `getManifestPath(cwd: string)` — exported: **yes** — Implements logic starting with: { const dir = join(cwd, ".bg-shell"); if (!existsSync(dir)) mkdirSync(dir, { recursive: true }); return join(dir, "manifest.json"); } (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:439` — `persistManifest(cwd: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/bg-shell/process-manager.ts:461` — `loadManifest(cwd: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/bg-shell/readiness-detector.ts`
- `src/resources/extensions/bg-shell/readiness-detector.ts:16` — `transitionToReady(bg: BgProcess, detail: string)` — exported: **yes** — Implements logic starting with: { bg.status = "ready"; bg.wasReady = true; addEvent(bg, { type: "ready", detail }); } (missing: jsdoc)
- `src/resources/extensions/bg-shell/readiness-detector.ts:24` — `probePort(port: number, host: string = "127.0.0.1")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/bg-shell/readiness-detector.ts:43` — `startPortProbing(bg: BgProcess, port: number, customTimeout?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/readiness-detector.ts:82` — `waitForReady(bg: BgProcess, timeout: number, signal?: AbortSignal)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/bg-shell/utilities.ts`
- `src/resources/extensions/bg-shell/utilities.ts:13` — `restoreWindowsVTInput()` — exported: **yes** — Implements logic starting with: { if (process.platform !== "win32") return; try { if (!_vtHandles) { const cjsRequire = createRequire(import.meta.url); const koffi = cjsRequire("k… (missing: jsdoc)
- `src/resources/extensions/bg-shell/utilities.ts:42` — `formatTimeAgo(timestamp: number)` — exported: **yes** — Implements logic starting with: { return formatDuration(Date.now() - timestamp) + " ago"; } (missing: jsdoc)
- `src/resources/extensions/bg-shell/utilities.ts:52` — `getBgShellLiveCwd(cachedCwd?: string, pathExists: (path: string) => boolean = existsSync, getCwd: () => string = () => process.cwd(), chdir: (path: string) => void = (path) => process.chdir(path))` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/bg-shell/utilities.ts:81` — `resolveBgShellPersistenceCwd(cachedCwd: string, liveCwd: string | undefined = undefined, pathExists: (path: string) => boolean = existsSync)` — exported: **yes** — Implements logic starting with: { const resolvedLiveCwd = liveCwd ?? getBgShellLiveCwd(cachedCwd, pathExists); const cachedIsAutoWorktree = /(?:^|[\\/])\.gsd[\\/]worktrees[\\/]/.t… (missing: jsdoc)

### `src/resources/extensions/browser-tools/capture.ts`
- `src/resources/extensions/browser-tools/capture.ts:82` — `captureCompactPageState(p: Page, options: { selectors?: string[]; includeBodyText?: boolean; target?: Page | Frame } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/core.ts`
- `src/resources/extensions/browser-tools/core.ts:171` — `createActionTimeline(limit = 60)` — exported: **yes** — Implements logic starting with: { return { limit, nextId: 1, entries: [], }; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:179` — `beginAction(timeline: ActionTimeline, partial: ActionPartial)` — exported: **yes** — Implements logic starting with: { const entry: ActionEntry = { id: timeline.nextId++, tool: partial.tool, paramsSummary: partial.paramsSummary ?? "", startedAt: partial.startedAt… (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:202` — `finishAction(timeline: ActionTimeline, actionId: number, updates: ActionUpdates = {})` — exported: **yes** — Implements logic starting with: { const entry = timeline.entries.find((item) => item.id === actionId); if (!entry) return null; Object.assign(entry, updates, { finishedAt: updates… (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:218` — `findAction(timeline: ActionTimeline, actionId: number)` — exported: **yes** — Implements logic starting with: { return timeline.entries.find((item) => item.id === actionId) ?? null; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:222` — `toActionParamsSummary(params: unknown)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:258` — `diffCompactStates(before: CompactStateForDiff | null | undefined, after: CompactStateForDiff | null | undefined)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:348` — `includesNeedle(haystack: string, needle: string)` — exported: **yes** — Implements logic starting with: { return normalizeString(haystack).toLowerCase().includes(normalizeString(needle).toLowerCase()); } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:427` — `evaluateAssertionChecks({ checks, state }: { checks: AssertionCheckInput[]; state: AssertionState })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:707` — `createPageRegistry()` — exported: **yes** — Implements logic starting with: { return { pages: [], activePageId: null, nextId: 1 }; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:711` — `registryAddPage(registry: PageRegistry, { page, title = "", url = "", opener = null }: { page: unknown; title?: string; url?: string; opener?: number | null })` — exported: **yes** — Implements logic starting with: { const entry: PageEntry = { id: registry.nextId++, page, title, url, opener }; registry.pages.push(entry); return entry; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:720` — `registryRemovePage(registry: PageRegistry, pageId: number)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:754` — `registrySetActive(registry: PageRegistry, pageId: number)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:767` — `registryGetActive(registry: PageRegistry)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:786` — `registryGetPage(registry: PageRegistry, pageId: number)` — exported: **yes** — Implements logic starting with: { return registry.pages.find((p) => p.id === pageId) ?? null; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:790` — `registryListPages(registry: PageRegistry)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:804` — `createBoundedLogPusher(maxSize: number)` — exported: **yes** — Implements logic starting with: { return function push(array: unknown[], entry: unknown): void { array.push(entry); if (array.length > maxSize) { array.splice(0, array.length - ma… (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:813` — `runBatchSteps({ steps, executeStep, stopOnFailure = true }: { steps: unknown[]; executeStep: (step: unknown, index: number) => Promise<{ ok: boolean; [key: string]: unknown }>; stopOnFailure?: boolean; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:901` — `getSnapshotModeConfig(mode: string)` — exported: **yes** — Implements logic starting with: { return SNAPSHOT_MODES[mode] ?? null; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:909` — `computeContentHash(text: string)` — exported: **yes** — Implements logic starting with: { if (!text) return "0"; let h = 5381; for (let i = 0; i < text.length; i++) { h = ((h << 5) - h + text.charCodeAt(i)) | 0; } return (h >>> 0).toSt… (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:918` — `computeStructuralSignature(tag: string, role: string, childTags: string[])` — exported: **yes** — Implements logic starting with: { const input = `${tag}|${role}|${childTags.join(",")}`; let h = 5381; for (let i = 0; i < input.length; i++) { h = ((h << 5) - h + input.charCodeA… (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:927` — `matchFingerprint(stored: { contentHash?: string; structuralSignature?: string }, candidate: { contentHash?: string; structuralSignature?: string })` — exported: **yes** — Implements logic starting with: { if (!stored || !candidate) return false; if (!stored.contentHash || !stored.structuralSignature) return false; if (!candidate.contentHash || !can… (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:963` — `formatTimelineEntries(entries: ActionEntry[] = [], options: Record<string, unknown> = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:1022` — `buildFailureHypothesis(session: Record<string, any> = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/core.ts:1099` — `summarizeBrowserSession(session: Record<string, any> = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/lifecycle.ts`
- `src/resources/extensions/browser-tools/lifecycle.ts:162` — `ensureBrowser()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/lifecycle.ts:264` — `closeBrowser()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/browser-tools/refs.ts`
- `src/resources/extensions/browser-tools/refs.ts:20` — `buildRefSnapshot(target: Page | Frame, options: { selector?: string; interactiveOnly: boolean; limit: number; mode?: string })` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/browser-tools/refs.ts:181` — `resolveRefTarget(target: Page | Frame, node: RefNode)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/settle.ts`
- `src/resources/extensions/browser-tools/settle.ts:17` — `ensureMutationCounter(p: Page)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/settle.ts:38` — `readMutationCounter(p: Page)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/settle.ts:54` — `readFocusedDescriptor(target: Page | Frame)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/settle.ts:109` — `settleAfterActionAdaptive(p: Page, opts: AdaptiveSettleOptions = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/browser-tools/state.ts`
- `src/resources/extensions/browser-tools/state.ts:191` — `getBrowser()` — exported: **yes** — Implements logic starting with: { return _browser; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:192` — `setBrowser(b: Browser | null)` — exported: **yes** — Implements logic starting with: { _browser = b; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:196` — `getContext()` — exported: **yes** — Implements logic starting with: { return _context; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:197` — `setContext(c: BrowserContext | null)` — exported: **yes** — Implements logic starting with: { _context = c; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:201` — `getPageRegistry()` — exported: **yes** — Implements logic starting with: { return pageRegistry; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:205` — `getActiveFrame()` — exported: **yes** — Implements logic starting with: { return _activeFrame; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:206` — `setActiveFrame(f: Frame | null)` — exported: **yes** — Implements logic starting with: { _activeFrame = f; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:213` — `getConsoleLogs()` — exported: **yes** — Implements logic starting with: { return _consoleLogs; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:214` — `setConsoleLogs(logs: ConsoleEntry[])` — exported: **yes** — Implements logic starting with: { _consoleLogs = logs; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:218` — `getNetworkLogs()` — exported: **yes** — Implements logic starting with: { return _networkLogs; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:219` — `setNetworkLogs(logs: NetworkEntry[])` — exported: **yes** — Implements logic starting with: { _networkLogs = logs; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:223` — `getDialogLogs()` — exported: **yes** — Implements logic starting with: { return _dialogLogs; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:224` — `setDialogLogs(logs: DialogEntry[])` — exported: **yes** — Implements logic starting with: { _dialogLogs = logs; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:228` — `getPendingCriticalRequestsByPage()` — exported: **yes** — Implements logic starting with: { return _pendingCriticalRequestsByPage; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:229` — `resetPendingCriticalRequestsByPage()` — exported: **yes** — Implements logic starting with: { _pendingCriticalRequestsByPage = new WeakMap(); } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:233` — `getCurrentRefMap()` — exported: **yes** — Implements logic starting with: { return _currentRefMap; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:234` — `setCurrentRefMap(m: Record<string, RefNode>)` — exported: **yes** — Implements logic starting with: { _currentRefMap = m; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:238` — `getRefVersion()` — exported: **yes** — Implements logic starting with: { return _refVersion; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:239` — `setRefVersion(v: number)` — exported: **yes** — Implements logic starting with: { _refVersion = v; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:243` — `getRefMetadata()` — exported: **yes** — Implements logic starting with: { return _refMetadata; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:244` — `setRefMetadata(m: RefMetadata | null)` — exported: **yes** — Implements logic starting with: { _refMetadata = m; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:248` — `getActionTimeline()` — exported: **yes** — Implements logic starting with: { return actionTimeline; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:252` — `getLastActionBeforeState()` — exported: **yes** — Implements logic starting with: { return _lastActionBeforeState; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:253` — `setLastActionBeforeState(s: CompactPageState | null)` — exported: **yes** — Implements logic starting with: { _lastActionBeforeState = s; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:257` — `getLastActionAfterState()` — exported: **yes** — Implements logic starting with: { return _lastActionAfterState; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:258` — `setLastActionAfterState(s: CompactPageState | null)` — exported: **yes** — Implements logic starting with: { _lastActionAfterState = s; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:262` — `getSessionStartedAt()` — exported: **yes** — Implements logic starting with: { return _sessionStartedAt; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:263` — `setSessionStartedAt(t: number | null)` — exported: **yes** — Implements logic starting with: { _sessionStartedAt = t; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:267` — `getSessionArtifactDir()` — exported: **yes** — Implements logic starting with: { return _sessionArtifactDir; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:268` — `setSessionArtifactDir(d: string | null)` — exported: **yes** — Implements logic starting with: { _sessionArtifactDir = d; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:272` — `getActiveTraceSession()` — exported: **yes** — Implements logic starting with: { return _activeTraceSession; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:273` — `setActiveTraceSession(t: TraceSessionState | null)` — exported: **yes** — Implements logic starting with: { _activeTraceSession = t; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:285` — `getHarState()` — exported: **yes** — Implements logic starting with: { return _harState; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:286` — `setHarState(h: HarState)` — exported: **yes** — Implements logic starting with: { _harState = h; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/state.ts:292` — `resetAllState()` — exported: **yes** — Implements logic starting with: { _browser = null; _context = null; pageRegistry.pages = []; pageRegistry.activePageId = null; pageRegistry.nextId = 1; _activeFrame = null; _conso… (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/action-cache.ts`
- `src/resources/extensions/browser-tools/tools/action-cache.ts:22` — `registerActionCacheTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/assertions.ts`
- `src/resources/extensions/browser-tools/tools/assertions.ts:25` — `registerAssertionTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/forms.ts`
- `src/resources/extensions/browser-tools/tools/forms.ts:327` — `registerFormTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/injection-detect.ts`
- `src/resources/extensions/browser-tools/tools/injection-detect.ts:43` — `registerInjectionDetectionTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/inspection.ts`
- `src/resources/extensions/browser-tools/tools/inspection.ts:14` — `registerInspectionTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/intent.ts`
- `src/resources/extensions/browser-tools/tools/intent.ts:347` — `registerIntentTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/interaction.ts`
- `src/resources/extensions/browser-tools/tools/interaction.ts:14` — `registerInteractionTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/navigation.ts`
- `src/resources/extensions/browser-tools/tools/navigation.ts:12` — `registerNavigationTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/network-mock.ts`
- `src/resources/extensions/browser-tools/tools/network-mock.ts:22` — `registerNetworkMockTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/pages.ts`
- `src/resources/extensions/browser-tools/tools/pages.ts:15` — `registerPageTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/pdf.ts`
- `src/resources/extensions/browser-tools/tools/pdf.ts:5` — `registerPdfTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/refs.ts`
- `src/resources/extensions/browser-tools/tools/refs.ts:18` — `registerRefTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/screenshot.ts`
- `src/resources/extensions/browser-tools/tools/screenshot.ts:6` — `registerScreenshotTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/session.ts`
- `src/resources/extensions/browser-tools/tools/session.ts:32` — `registerSessionTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/state-persistence.ts`
- `src/resources/extensions/browser-tools/tools/state-persistence.ts:11` — `registerStatePersistenceTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/verify.ts`
- `src/resources/extensions/browser-tools/tools/verify.ts:5` — `registerVerifyTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/visual-diff.ts`
- `src/resources/extensions/browser-tools/tools/visual-diff.ts:11` — `registerVisualDiffTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/browser-tools/tools/wait.ts`
- `src/resources/extensions/browser-tools/tools/wait.ts:15` — `registerWaitTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/browser-tools/utils.ts`
- `src/resources/extensions/browser-tools/utils.ts:59` — `truncateText(text: string)` — exported: **yes** — Implements logic starting with: { const result = truncateHead(text, { maxLines: DEFAULT_MAX_LINES, maxBytes: DEFAULT_MAX_BYTES, }); if (result.truncated) { return ( result.content… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:77` — `formatArtifactTimestamp(timestamp: number)` — exported: **yes** — Implements logic starting with: { return new Date(timestamp).toISOString().replace(/[:.]/g, "-"); } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:81` — `ensureDir(dirPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:86` — `writeArtifactFile(filePath: string, content: string | Uint8Array)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:96` — `copyArtifactFile(sourcePath: string, destinationPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:106` — `ensureSessionStartedAt()` — exported: **yes** — Implements logic starting with: { let t = getSessionStartedAt(); if (!t) { t = Date.now(); setSessionStartedAt(t); } return t; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:115` — `ensureSessionArtifactDir()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:128` — `buildSessionArtifactPath(filename: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:136` — `getActivePageMetadata()` — exported: **yes** — Implements logic starting with: { const registry = pageRegistry; const activeEntry = registry.activePageId !== null ? registry.pages.find((entry: any) => entry.id === registry.act… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:149` — `getActiveFrameMetadata()` — exported: **yes** — Implements logic starting with: { const frame = getActiveFrame(); if (!frame) { return { name: null, url: null }; } return { name: frame.name() || null, url: frame.url() || null,… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:160` — `getSessionArtifactMetadata()` — exported: **yes** — Implements logic starting with: { return { artifactRoot: ARTIFACT_ROOT, sessionStartedAt: getSessionStartedAt(), sessionArtifactDir: getSessionArtifactDir(), activeTraceSession: g… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:172` — `sanitizeArtifactName(value: string, fallback: string)` — exported: **yes** — Implements logic starting with: { const sanitized = value .trim() .replace(/[^a-zA-Z0-9._-]+/g, "-") .replace(/^-+|-+$/g, ""); return sanitized || fallback; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:205` — `resolveAccessibilityScope(selector?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:249` — `isCriticalResourceType(resourceType: string)` — exported: **yes** — Implements logic starting with: { return resourceType === "document" || resourceType === "fetch" || resourceType === "xhr"; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:253` — `updatePendingCriticalRequests(p: Page, delta: number)` — exported: **yes** — Implements logic starting with: { const map = getPendingCriticalRequestsByPage(); const current = map.get(p) ?? 0; map.set(p, Math.max(0, current + delta)); } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:259` — `getPendingCriticalRequests(p: Page)` — exported: **yes** — Implements logic starting with: { return getPendingCriticalRequestsByPage().get(p) ?? 0; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:267` — `verificationFromChecks(checks: BrowserVerificationCheck[], retryHint?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:285` — `verificationLine(verification: BrowserVerificationResult)` — exported: **yes** — Implements logic starting with: { return `Verification: ${verification.verificationSummary}`; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:293` — `collectAssertionState(p: Page, checks: BrowserAssertionCheckInput[], captureCompactPageState: ( p: Page, options?: { selectors?: string[]; includeBodyText?: boolean; target?: Page | Frame }, ) => Promise<CompactPageState>, target?: Page | Frame)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:340` — `formatAssertionText(result: ReturnType<typeof import("./core.js").evaluateAssertionChecks>)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:353` — `formatDiffText(diff: ReturnType<typeof import("./core.js").diffCompactStates>)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:369` — `getUrlHash(url: string)` — exported: **yes** — Implements logic starting with: { try { return new URL(url).hash || ""; } catch { return ""; } } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:377` — `countOpenDialogs(target: Page | Frame)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:392` — `captureClickTargetState(target: Page | Frame, selector: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:430` — `readInputLikeValue(target: Page | Frame, selector?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:469` — `firstErrorLine(err: unknown)` — exported: **yes** — Implements logic starting with: { const message = typeof err === "object" && err && "message" in err ? String((err as { message?: unknown }).message ?? "") : String(err ?? "unknow… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:481` — `beginTrackedAction(tool: string, params: unknown, beforeUrl: string)` — exported: **yes** — Implements logic starting with: { return beginAction(actionTimeline, { tool, paramsSummary: toActionParamsSummary(params), beforeUrl, }); } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:493` — `finishTrackedAction(actionId: number, updates: { status: "success" | "error"; afterUrl?: string; verificationSummary?: string; warningSummary?: string; diffSummary?: string; changed?: boolean; error?: string; beforeState?: CompactPageState; afterState?: CompactPageState; })` — exported: **yes** — Implements logic starting with: { return finishAction(actionTimeline, actionId, updates); } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:510` — `getSinceTimestamp(sinceActionId?: number)` — exported: **yes** — Implements logic starting with: { if (!sinceActionId) return 0; const action = findAction(actionTimeline, sinceActionId); if (!action) return 0; return action.startedAt ?? 0; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:517` — `getConsoleEntriesSince(sinceActionId?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:522` — `getNetworkEntriesSince(sinceActionId?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:531` — `getRecentErrors(pageUrl: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:611` — `parseRef(input: string)` — exported: **yes** — Implements logic starting with: { const trimmed = input.trim().toLowerCase(); const token = trimmed.startsWith("@") ? trimmed.slice(1) : trimmed; const versioned = token.match(/^v… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:623` — `formatVersionedRef(version: number, key: string)` — exported: **yes** — Implements logic starting with: { return `@v${version}:${key}`; } (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:627` — `staleRefGuidance(refDisplay: string, reason: string)` — exported: **yes** — Implements logic starting with: { return `Ref ${refDisplay} could not be resolved (${reason}). The ref is likely stale after DOM/navigation changes. Call browser_snapshot_refs aga… (missing: jsdoc)
- `src/resources/extensions/browser-tools/utils.ts:635` — `formatCompactStateSummary(state: CompactPageState)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/claude-code-cli/index.ts`
- `src/resources/extensions/claude-code-cli/index.ts:19` — `claudeCodeCli(pi: ExtensionAPI)` — exported: **yes** — Implements logic starting with: { pi.registerProvider("claude-code", { authMode: "externalCli", api: "anthropic-messages", baseUrl: "local://claude-code", isReady: isClaudeCodeRea… (missing: jsdoc)

### `src/resources/extensions/claude-code-cli/partial-builder.ts`
- `src/resources/extensions/claude-code-cli/partial-builder.ts:111` — `mapStopReason(reason: string | null)` — exported: **yes** — Implements logic starting with: { switch (reason) { case "end_turn": case "stop_sequence": return "stop"; case "max_tokens": return "length"; case "tool_use": return "toolUse"; de… (missing: jsdoc)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:181` — `PartialMessageBuilder.constructor(model: string)` — exported: **yes** — Implements logic starting with: { this.partial = { role: "assistant", content: [], api: "anthropic-messages", provider: "claude-code", model, usage: { ...ZERO_USAGE }, stopReason:… (missing: jsdoc)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:194` — `PartialMessageBuilder.get message()` — exported: **yes** — Implements logic starting with: { return this.partial; } (missing: jsdoc)

### `src/resources/extensions/claude-code-cli/stream-adapter.ts`
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:427` — `isClaudeCodeAbortErrorMessage(message: string | undefined | null)` — exported: **yes** — Implements logic starting with: { if (!message) return false; return /\b(?:claude code process aborted by user|request aborted by user|process aborted by user)\b/i.test(message); } (missing: jsdoc)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:440` — `resolveClaudeCodeAbortedMessageText(errorMsg: string, lastTextContent: string)` — exported: **yes** — Implements logic starting with: { const trimmedError = errorMsg.trim(); if (trimmedError && !isBareClaudeCodeAbortErrorMessage(trimmedError)) { return trimmedError; } return lastT… (missing: jsdoc)

### `src/resources/extensions/cmux/index.ts`
- `src/resources/extensions/cmux/index.ts:40` — `detectCmuxEnvironment(env: NodeJS.ProcessEnv = process.env, socketExists: (path: string) => boolean = existsSync, cliAvailable: () => boolean = isCmuxCliAvailable)` — exported: **yes** — Implements logic starting with: { const socketPath = env.CMUX_SOCKET_PATH ?? DEFAULT_SOCKET_PATH; const workspaceId = env.CMUX_WORKSPACE_ID?.trim() || undefined; const surfaceId =… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:58` — `resolveCmuxConfig(preferences: CmuxPreferences | undefined, env: NodeJS.ProcessEnv = process.env, socketExists: (path: string) => boolean = existsSync, cliAvailable: () => boolean = isCmuxCliAvailable)` — exported: **yes** — Implements logic starting with: { const detected = detectCmuxEnvironment(env, socketExists, cliAvailable); const cmux = preferences?.cmux ?? {}; const enabled = detected.available… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:77` — `shouldPromptToEnableCmux(preferences: CmuxPreferences | undefined, env: NodeJS.ProcessEnv = process.env, socketExists: (path: string) => boolean = existsSync, cliAvailable: () => boolean = isCmuxCliAvailable)` — exported: **yes** — Implements logic starting with: { if (cmuxPromptedThisSession) return false; const detected = detectCmuxEnvironment(env, socketExists, cliAvailable); if (!detected.available) retu… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:89` — `markCmuxPromptShown()` — exported: **yes** — Implements logic starting with: { cmuxPromptedThisSession = true; } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:93` — `resetCmuxPromptState()` — exported: **yes** — Implements logic starting with: { cmuxPromptedThisSession = false; } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:97` — `isCmuxCliAvailable()` — exported: **yes** — Implements logic starting with: { if (cachedCliAvailability !== null) return cachedCliAvailability; try { execFileSync("cmux", ["--help"], { stdio: "ignore", timeout: 1000 }); cac… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:108` — `supportsOsc777Notifications(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { const termProgram = env.TERM_PROGRAM?.toLowerCase() ?? ""; return termProgram === "ghostty" || termProgram === "wezterm" || termProgram === "iter… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:113` — `emitOsc777Notification(title: string, body: string)` — exported: **yes** — Implements logic starting with: { if (!supportsOsc777Notifications()) return; const safeTitle = normalizeNotificationText(title).replace(/;/g, ","); const safeBody = normalizeNoti… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:120` — `buildCmuxStatusLabel(state: CmuxState)` — exported: **yes** — Implements logic starting with: { const parts: string[] = []; if (state.activeMilestone) parts.push(state.activeMilestone.id); if (state.activeSlice) parts.push(state.activeSlice.… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:132` — `buildCmuxProgress(state: CmuxState)` — exported: **yes** — Implements logic starting with: { const progress = state.progress; if (!progress) return null; const choose = (done: number, total: number, label: string): CmuxSidebarProgress | n… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:171` — `class CmuxClient` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:174` — `CmuxClient.constructor(config: ResolvedCmuxConfig)` — exported: **yes** — Implements logic starting with: { this.config = config; } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:178` — `CmuxClient.fromPreferences(preferences: CmuxPreferences | undefined)` — exported: **yes** — Implements logic starting with: { return new CmuxClient(resolveCmuxConfig(preferences)); } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:182` — `CmuxClient.getConfig()` — exported: **yes** — Implements logic starting with: { return this.config; } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:234` — `CmuxClient.getCapabilities()` — exported: **yes** — Implements logic starting with: { const stdout = this.runSync(["capabilities", "--json"]); return stdout ? parseJson(stdout) : null; } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:239` — `CmuxClient.identify()` — exported: **yes** — Implements logic starting with: { const stdout = this.runSync(["identify", "--json"]); return stdout ? parseJson(stdout) : null; } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:244` — `CmuxClient.setStatus(label: string, phase: Phase)` — exported: **yes** — Implements logic starting with: { if (!this.config.sidebar) return; const visuals = phaseVisuals(phase); this.runSync(this.appendWorkspace([ "set-status", STATUS_KEY, label, "--ic… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:258` — `CmuxClient.clearStatus()` — exported: **yes** — Implements logic starting with: { if (!this.config.sidebar) return; this.runSync(this.appendWorkspace(["clear-status", STATUS_KEY])); } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:263` — `CmuxClient.setProgress(progress: CmuxSidebarProgress | null)` — exported: **yes** — Implements logic starting with: { if (!this.config.sidebar) return; if (!progress) { this.runSync(this.appendWorkspace(["clear-progress"])); return; } this.runSync(this.appendWork… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:277` — `CmuxClient.log(message: string, level: CmuxLogLevel = "info", source = "gsd")` — exported: **yes** — Implements logic starting with: { if (!this.config.sidebar) return; this.runSync(this.appendWorkspace([ "log", "--level", level, "--source", source, "--", message, ])); } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:290` — `CmuxClient.notify(title: string, body: string, subtitle?: string)` — exported: **yes** — Implements logic starting with: { if (!this.config.notifications) return false; const args = ["notify", "--title", title, "--body", body]; if (subtitle) args.push("--subtitle", su… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:297` — `CmuxClient.listSurfaceIds()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:303` — `CmuxClient.createSplit(direction: "right" | "down" | "left" | "up")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:307` — `CmuxClient.createSplitFrom(sourceSurfaceId: string | undefined, direction: "right" | "down" | "left" | "up")` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:372` — `CmuxClient.sendSurface(surfaceId: string, text: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:379` — `CmuxClient.sendInterrupt(surfaceId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:385` — `syncCmuxSidebar(preferences: CmuxPreferences | undefined, state: CmuxState)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:401` — `clearCmuxSidebar(preferences: CmuxPreferences | undefined)` — exported: **yes** — Implements logic starting with: { const config = resolveCmuxConfig(preferences); if (!config.available || !config.cliAvailable) return; const client = new CmuxClient({ ...config,… (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:411` — `logCmuxEvent(preferences: CmuxPreferences | undefined, message: string, level: CmuxLogLevel = "info")` — exported: **yes** — Implements logic starting with: { CmuxClient.fromPreferences(preferences).log(message, level); } (missing: jsdoc)
- `src/resources/extensions/cmux/index.ts:419` — `shellEscape(value: string)` — exported: **yes** — Implements logic starting with: { return `'${value.replace(/'/g, `'\\''`)}'`; } (missing: jsdoc)

### `src/resources/extensions/get-secrets-from-user.ts`
- `src/resources/extensions/get-secrets-from-user.ts:489` — `secureEnv(pi: ExtensionAPI)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/github-sync/cli.ts`
- `src/resources/extensions/github-sync/cli.ts:31` — `ghIsAvailable()` — exported: **yes** — Implements logic starting with: { if (_ghAvailable !== null) return _ghAvailable; try { execFileSync("gh", ["--version"], { encoding: "utf-8", stdio: ["ignore", "pipe", "ignore"],… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:57` — `ghHasRateLimit(cwd: string)` — exported: **yes** — Implements logic starting with: { const now = Date.now(); if (now - _rateLimitCheckedAt < RATE_LIMIT_CHECK_INTERVAL_MS) return _rateLimitOk; _rateLimitCheckedAt = now; try { const… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:114` — `ghDetectRepo(cwd: string)` — exported: **yes** — Implements logic starting with: { const result = runGh( ["repo", "view", "--json", "nameWithOwner", "--jq", ".nameWithOwner"], cwd, ); if (!result.ok) return fail(result.error!);… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:136` — `ghCreateIssue(cwd: string, opts: CreateIssueOpts)` — exported: **yes** — Implements logic starting with: { const args = [ "issue", "create", "--repo", opts.repo, "--title", opts.title, "--body", truncateBody(opts.body), ]; if (opts.labels?.length) { ar… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:166` — `ghCloseIssue(cwd: string, repo: string, issueNumber: number, comment?: string)` — exported: **yes** — Implements logic starting with: { if (comment) { ghAddComment(cwd, repo, issueNumber, comment); } const result = runGh( ["issue", "close", String(issueNumber), "--repo", repo], cw… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:178` — `ghAddComment(cwd: string, repo: string, issueNumber: number, body: string)` — exported: **yes** — Implements logic starting with: { const result = runGh( ["issue", "comment", String(issueNumber), "--repo", repo, "--body", truncateBody(body)], cwd, ); if (!result.ok) return fai… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:210` — `ghCreateMilestone(cwd: string, repo: string, title: string, description: string)` — exported: **yes** — Implements logic starting with: { const result = runGhJson<{ number: number }>( [ "api", `repos/${repo}/milestones`, "-X", "POST", "-f", `title=${title}`, "-f", `description=${tru… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:226` — `ghCloseMilestone(cwd: string, repo: string, milestoneNumber: number)` — exported: **yes** — Implements logic starting with: { const result = runGh( [ "api", `repos/${repo}/milestones/${milestoneNumber}`, "-X", "PATCH", "-f", "state=closed", ], cwd, ); if (!result.ok) ret… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:250` — `ghCreatePR(cwd: string, opts: CreatePROpts)` — exported: **yes** — Implements logic starting with: { const args = [ "pr", "create", "--repo", opts.repo, "--base", opts.base, "--head", opts.head, "--title", opts.title, "--body", truncateBody(opts.… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:269` — `ghMarkPRReady(cwd: string, repo: string, prNumber: number)` — exported: **yes** — Implements logic starting with: { const result = runGh( ["pr", "ready", String(prNumber), "--repo", repo], cwd, ); if (!result.ok) return fail(result.error!); return ok(undefined)… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:278` — `ghMergePR(cwd: string, repo: string, prNumber: number, strategy: "squash" | "merge" = "squash")` — exported: **yes** — Implements logic starting with: { const args = [ "pr", "merge", String(prNumber), "--repo", repo, strategy === "squash" ? "--squash" : "--merge", "--delete-branch", ]; const resul… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:292` — `ghAddToProject(cwd: string, repo: string, projectNumber: number, issueNumber: number)` — exported: **yes** — Implements logic starting with: { // Get the issue's node ID first const issueResult = runGhJson<{ id: string }>( ["api", `repos/${repo}/issues/${issueNumber}`, "--jq", "{id: .nod… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:335` — `ghPushBranch(cwd: string, branch: string, setUpstream = true)` — exported: **yes** — Implements logic starting with: { const args = ["git", "push"]; if (setUpstream) args.push("-u", "origin", branch); else args.push("origin", branch); try { execFileSync(args[0], a… (missing: jsdoc)
- `src/resources/extensions/github-sync/cli.ts:354` — `ghCreateBranch(cwd: string, branch: string, from: string)` — exported: **yes** — Implements logic starting with: { try { execFileSync("git", ["branch", branch, from], { cwd, encoding: "utf-8", stdio: ["ignore", "pipe", "pipe"], timeout: 10_000, env: GIT_NO_PRO… (missing: jsdoc)

### `src/resources/extensions/github-sync/mapping.ts`
- `src/resources/extensions/github-sync/mapping.ts:22` — `loadSyncMapping(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:35` — `saveSyncMapping(basePath: string, mapping: SyncMapping)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:40` — `createEmptyMapping(repo: string)` — exported: **yes** — Implements logic starting with: { return { version: 1, repo, milestones: {}, slices: {}, tasks: {}, }; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:52` — `getMilestoneRecord(mapping: SyncMapping, mid: string)` — exported: **yes** — Implements logic starting with: { return mapping.milestones[mid] ?? null; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:56` — `getSliceRecord(mapping: SyncMapping, mid: string, sid: string)` — exported: **yes** — Implements logic starting with: { return mapping.slices[`${mid}/${sid}`] ?? null; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:60` — `getTaskRecord(mapping: SyncMapping, mid: string, sid: string, tid: string)` — exported: **yes** — Implements logic starting with: { return mapping.tasks[`${mid}/${sid}/${tid}`] ?? null; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:64` — `getTaskIssueNumber(mapping: SyncMapping, mid: string, sid: string, tid: string)` — exported: **yes** — Implements logic starting with: { const record = getTaskRecord(mapping, mid, sid, tid); return record?.issueNumber ?? null; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:71` — `setMilestoneRecord(mapping: SyncMapping, mid: string, record: MilestoneSyncRecord)` — exported: **yes** — Implements logic starting with: { mapping.milestones[mid] = record; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:75` — `setSliceRecord(mapping: SyncMapping, mid: string, sid: string, record: SliceSyncRecord)` — exported: **yes** — Implements logic starting with: { mapping.slices[`${mid}/${sid}`] = record; } (missing: jsdoc)
- `src/resources/extensions/github-sync/mapping.ts:79` — `setTaskRecord(mapping: SyncMapping, mid: string, sid: string, tid: string, record: SyncEntityRecord)` — exported: **yes** — Implements logic starting with: { mapping.tasks[`${mid}/${sid}/${tid}`] = record; } (missing: jsdoc)

### `src/resources/extensions/github-sync/sync.ts`
- `src/resources/extensions/github-sync/sync.ts:125` — `shouldCreateSlicePrForSyncEvent(unitType: string, config: Pick<GitHubSyncConfig, "slice_prs">)` — exported: **yes** — Implements logic starting with: { return unitType === "complete-slice" && config.slice_prs !== false; } (missing: jsdoc)

### `src/resources/extensions/github-sync/templates.ts`
- `src/resources/extensions/github-sync/templates.ts:54` — `formatMilestoneIssueBody(data: MilestoneData)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`# ${data.id}: ${data.title}`); lines.push(""); if (data.vision) { lines.push("## Vision"); lines.push(dat… (missing: jsdoc)
- `src/resources/extensions/github-sync/templates.ts:102` — `formatSlicePRBody(data: SliceData)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/github-sync/templates.ts:143` — `formatTaskIssueBody(data: TaskData)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`## ${data.id}: ${data.title}`); lines.push(""); if (data.description) { lines.push(data.description); lin… (missing: jsdoc)
- `src/resources/extensions/github-sync/templates.ts:181` — `formatSummaryComment(data: SummaryData)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/github-sync/templates.ts:255` — `formatSwarmLanePRBody(data: SwarmLanePRData)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/github-sync/templates.ts:285` — `formatSwarmReleaseChecklistBody(data: SwarmReleaseChecklistData)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`# UOK Swarm Release Checklist`); lines.push(""); lines.push(`**Integration branch:** ${inlineCode(data.in… (missing: jsdoc)

### `src/resources/extensions/gsd/activity-log.ts`
- `src/resources/extensions/gsd/activity-log.ts:104` — `saveActivityLog(ctx: ExtensionContext, basePath: string, unitType: string, unitId: string)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/activity-log.ts:164` — `pruneActivityLogs(activityDir: string, retentionDays: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/atomic-write.ts`
- `src/resources/extensions/gsd/atomic-write.ts:92` — `atomicWriteAsyncWithOps(filePath: string, content: string, encoding: RetryableEncoding = "utf-8", ops: AtomicWriteAsyncOps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: description, @param filePath, @param content, @param encoding, @param ops)
- `src/resources/extensions/gsd/atomic-write.ts:123` — `atomicWriteSyncWithOps(filePath: string, content: string, encoding: RetryableEncoding = "utf-8", ops: AtomicWriteSyncOps)` — exported: **yes** — Implements logic starting with: { ops.mkdir(dirname(filePath), { recursive: true }); const tmpPath = ops.createTempPath?.(filePath) ?? defaultTempPath(filePath); ops.writeFile(tmp… (missing: description, @param filePath, @param content, @param encoding, @param ops)

### `src/resources/extensions/gsd/auto-artifact-paths.ts`
- `src/resources/extensions/gsd/auto-artifact-paths.ts:131` — `diagnoseExpectedArtifact(unitType: string, unitId: string, base: string)` — exported: **yes** — Implements logic starting with: { const { milestone: mid, slice: sid, task: tid } = parseUnitId(unitId); switch (unitType) { case "workflow-preferences": return ".gsd/PREFERENCES.… (missing: jsdoc)

### `src/resources/extensions/gsd/auto-budget.ts`
- `src/resources/extensions/gsd/auto-budget.ts:10` — `getBudgetAlertLevel(budgetPct: number)` — exported: **yes** — Implements logic starting with: { if (budgetPct >= 1.0) return 100; if (budgetPct >= 0.90) return 90; if (budgetPct >= 0.80) return 80; if (budgetPct >= 0.75) return 75; return 0;… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-budget.ts:18` — `getNewBudgetAlertLevel(previousLevel: BudgetAlertLevel, budgetPct: number)` — exported: **yes** — Implements logic starting with: { const currentLevel = getBudgetAlertLevel(budgetPct); if (currentLevel === 0 || currentLevel <= previousLevel) return null; return currentLevel; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-budget.ts:24` — `getBudgetEnforcementAction(enforcement: BudgetEnforcementMode, budgetPct: number)` — exported: **yes** — Implements logic starting with: { if (budgetPct < 1.0) return "none"; if (enforcement === "halt") return "halt"; if (enforcement === "pause") return "pause"; return "warn"; } (missing: jsdoc)

### `src/resources/extensions/gsd/auto-dashboard.ts`
- `src/resources/extensions/gsd/auto-dashboard.ts:125` — `unitVerb(unitType: string)` — exported: **yes** — Implements logic starting with: { if (unitType.startsWith("hook/")) return `hook: ${unitType.slice(5)}`; switch (unitType) { case "discuss-milestone": case "discuss-slice": return… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:146` — `unitPhaseLabel(unitType: string)` — exported: **yes** — Implements logic starting with: { if (unitType.startsWith("hook/")) return "HOOK"; switch (unitType) { case "discuss-milestone": case "discuss-slice": return "DISCUSS"; case "rese… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:251` — `formatRuntimeHealthSignal(record: AutoUnitRuntimeRecord | null, now = Date.now())` — exported: **yes** — Implements logic starting with: { if (!record) return null; const idleMs = Math.max(0, now - record.lastProgressAt); const idleMinutes = Math.floor(idleMs / 60_000); if ((record.r… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:275` — `shouldRenderRoadmapProgress(progress: { total: number; activeSliceTasks?: { total: number } | null } | null)` — exported: **yes** — Implements logic starting with: { return !!progress && progress.total > 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:344` — `updateSliceProgressCache(base: string, mid: string, activeSid?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:388` — `getRoadmapSlicesSync()` — exported: **yes** — Implements logic starting with: { return cachedSliceProgress; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:392` — `clearSliceProgressCache()` — exported: **yes** — Implements logic starting with: { cachedSliceProgress = null; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:448` — `_resetLastCommitCacheForTests()` — exported: **yes** — Implements logic starting with: { cachedLastCommit = null; lastCommitFetchedAt = 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:453` — `_refreshLastCommitForTests(basePath: string)` — exported: **yes** — Implements logic starting with: { refreshLastCommit(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:457` — `_getLastCommitForTests(basePath: string)` — exported: **yes** — Implements logic starting with: { return getLastCommit(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:461` — `_getLastCommitFetchedAtForTests()` — exported: **yes** — Implements logic starting with: { return lastCommitFetchedAt; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:475` — `hideFooter(_tui: unknown, theme: Theme, footerData: ReadonlyFooterDataProvider)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:625` — `updateProgressWidget(ctx: ExtensionContext, unitType: string, unitId: string, state: GSDState, accessors: WidgetStateAccessors, tierBadge?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:961` — `setCompletionProgressWidget(ctx: ExtensionContext, snapshot: CompletionDashboardSnapshot)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dashboard.ts:1084` — `setAutoOutcomeWidget(ctx: ExtensionContext, snapshot: AutoOutcomeSurfaceSnapshot)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-direct-dispatch.ts`
- `src/resources/extensions/gsd/auto-direct-dispatch.ts:39` — `dispatchDirectPhase(ctx: ExtensionCommandContext, pi: ExtensionAPI, phase: string, base: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-dispatch.ts`
- `src/resources/extensions/gsd/auto-dispatch.ts:134` — `setReassessmentCheckerForTest(checker: ReassessmentChecker)` — exported: **yes** — Implements logic starting with: { const previous = reassessmentChecker; reassessmentChecker = checker; return () => { reassessmentChecker = previous; }; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:142` — `setResearchProjectPromptBuilderForTest(builder: ResearchProjectPromptBuilder)` — exported: **yes** — Implements logic starting with: { const previous = researchProjectPromptBuilder; researchProjectPromptBuilder = builder; return () => { researchProjectPromptBuilder = previous; };… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:212` — `hasPendingDeepStage(prefs: GSDPreferences | undefined, basePath: string)` — exported: **yes** — Implements logic starting with: { const gate = getDeepStageGate(prefs, basePath); return gate.status === "pending" || gate.status === "blocked"; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:217` — `shouldRunDeepProjectSetup(state: Pick<GSDState, "phase">, prefs: GSDPreferences | undefined, basePath: string, options: { hasSurvivorBranch?: boolean } = {})` — exported: **yes** — Implements logic starting with: { if (options.hasSurvivorBranch === true) return false; if ( state.phase !== "pre-planning" && state.phase !== "needs-discussion" && state.phase !=… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:275` — `getRewriteCount(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:284` — `setRewriteCount(basePath: string, count: number)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:299` — `getUatCount(basePath: string, mid: string, sid: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-dispatch.ts:308` — `incrementUatCount(basePath: string, mid: string, sid: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-model-selection.ts`
- `src/resources/extensions/gsd/auto-model-selection.ts:36` — `ModelPolicyDispatchBlockedError.constructor(unitType: string, unitId: string, reasons: ReadonlyArray<{ provider: string; modelId: string; reason: string }>)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-model-selection.ts:121` — `resolvePreferredModelConfig(unitType: string, autoModeStartModel: { provider: string; id: string; flatRateCtx?: FlatRateContext } | null, isAutoMode = true)` — exported: **yes** — Implements logic starting with: { const explicitConfig = resolveModelWithFallbacksForUnit(unitType); if (explicitConfig) { return { ...explicitConfig, source: "explicit", }; } //… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-model-selection.ts:715` — `isFlatRateProvider(provider: string, opts?: FlatRateContext)` — exported: **yes** — Implements logic starting with: { const p = provider.toLowerCase(); if (BUILTIN_FLAT_RATE.has(p)) return true; if (opts?.userFlatRate?.some(id => id.toLowerCase() === p)) return t… (missing: jsdoc)

### `src/resources/extensions/gsd/auto-post-unit.ts`
- `src/resources/extensions/gsd/auto-post-unit.ts:205` — `_shouldDispatchTriageForTest(state: Pick<AutoSession, "stepMode" | "currentUnit">)` — exported: **yes** — Implements logic starting with: { return !state.stepMode && !!state.currentUnit && !state.currentUnit.type.startsWith("hook/") && state.currentUnit.type !== "triage-captures" && s… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-post-unit.ts:215` — `_shouldDispatchQuickTaskForTest(state: Pick<AutoSession, "stepMode" | "currentUnit" | "pendingQuickTasks">)` — exported: **yes** — Implements logic starting with: { return !state.stepMode && state.pendingQuickTasks.length > 0 && !!state.currentUnit && state.currentUnit.type !== "quick-task"; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-post-unit.ts:224` — `shouldDeferCloseoutGitAction(unitType: string)` — exported: **yes** — Implements logic starting with: { return unitType === "execute-task"; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-post-unit.ts:273` — `detectRogueFileWrites(unitType: string, unitId: string, basePath: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const { milestone: mid, slice: sid, task: tid } = parseUnitId(unitId); const rogues: RogueFileWrite[] = []; if (… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-post-unit.ts:375` — `buildStepCompleteMessage(nextState: import("./types.js").GSDState)` — exported: **yes** — Implements logic starting with: { if (nextState.phase === "complete") { return "Step complete — milestone finished. Run /gsd status to review, or start the next milestone."; } con… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-post-unit.ts:441` — `autoCommitUnit(basePath: string, unitType: string, unitId: string, ctx?: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-prompts.ts`
- `src/resources/extensions/gsd/auto-prompts.ts:643` — `buildTaskSummaryExcerpt(absPath: string | null, relPath: string, tid: string, options?: { blocker?: boolean })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1189` — `buildSkillActivationBlock(params: { base: string; milestoneId: string; milestoneTitle?: string; sliceId?: string; sliceTitle?: string; taskId?: string; taskTitle?: string; extraContext?: string[]; taskPlanContent?: string | null; preferences?: GSDPreferences; /** * Unit type dispatching this prompt. When provided, skills are filtered * through the per-unit-type manifest (see `skill-manifest.ts`). Unknown * or omitted values retain the pre-manifest behavior (all skills eligible). */ unitType?: string; })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1343` — `extractMarkdownSection(content: string, heading: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1354` — `escapeRegExp(value: string)` — exported: **yes** — Implements logic starting with: { return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1364` — `buildResumeSection(continueContent: string | null, legacyContinueContent: string | null, continueRelPath: string, legacyContinueRelPath: string | null)` — exported: **yes** — Implements logic starting with: { const resolvedContent = continueContent ?? legacyContinueContent; const resolvedRelPath = continueContent ? continueRelPath : legacyContinueRelPa… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1395` — `buildCarryForwardSection(priorSummaryPaths: string[], base: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1426` — `extractSliceExecutionExcerpt(content: string | null, relPath: string)` — exported: **yes** — Implements logic starting with: { if (!content) { return [ "## Slice Plan Excerpt", `Slice plan not found at dispatch time. Read \`${relPath}\` before running slice-level verifica… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1456` — `getPriorTaskSummaryPaths(mid: string, sid: string, currentTid: string, base: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1772` — `buildResearchMilestonePrompt(mid: string, midTitle: string, base: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1848` — `buildPlanMilestonePrompt(mid: string, midTitle: string, base: string, level?: InlineLevel)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:1933` — `buildResearchSlicePrompt(mid: string, _midTitle: string, sid: string, sTitle: string, base: string, options?: { contextModeRenderMode?: ContextModeRenderMode })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2130` — `buildPlanSlicePrompt(mid: string, _midTitle: string, sid: string, sTitle: string, base: string, level?: InlineLevel, options?: { softScopeHint?: string; sessionContextWindow?: number; modelRegistry?: MinimalModelRegistry; sessionProvider?: string; /** Failure context from a prior pre-exec gate run (#4551). When present, a * "Fix these specific issues" section is appended so the LLM addresses the * exact problems instead of producing an identical plan that fails again. */ priorPreExecFailure?: { blockingFindings: string[]; verdictExcerpt: string; }; })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2250` — `buildExecuteTaskPrompt(mid: string, sid: string, sTitle: string, tid: string, tTitle: string, base: string, level?: InlineLevel | ExecuteTaskPromptOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2424` — `buildCompleteSlicePrompt(mid: string, midTitle: string, sid: string, sTitle: string, base: string, level?: InlineLevel)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2555` — `buildCompleteMilestonePrompt(mid: string, midTitle: string, base: string, level?: InlineLevel)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2666` — `buildValidateMilestonePrompt(mid: string, midTitle: string, base: string, level?: InlineLevel)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2811` — `buildReplanSlicePrompt(mid: string, midTitle: string, sid: string, sTitle: string, base: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2898` — `buildRunUatPrompt(mid: string, sliceId: string, uatPath: string, uatContent: string, base: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:2958` — `buildReassessRoadmapPrompt(mid: string, midTitle: string, completedSliceId: string, base: string, level?: InlineLevel)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:3055` — `buildReactiveExecutePrompt(mid: string, midTitle: string, sid: string, sTitle: string, readyTaskIds: string[], base: string, subagentModel?: string, opts?: { sessionContextWindow?: number; modelRegistry?: MinimalModelRegistry; sessionProvider?: string })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:3185` — `buildParallelResearchSlicesPrompt(mid: string, midTitle: string, slices: Array<{ id: string; title: string }>, basePath: string, subagentModel?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:3218` — `buildGateEvaluatePrompt(mid: string, midTitle: string, sid: string, sTitle: string, base: string, subagentModel?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-prompts.ts:3303` — `buildRewriteDocsPrompt(mid: string, midTitle: string, activeSlice: { id: string; title: string } | null, base: string, overrides: Override[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-recovery.ts`
- `src/resources/extensions/gsd/auto-recovery.ts:63` — `refreshRecoveryDbForArtifact(unitType: string, unitId: string)` — exported: **yes** — Implements logic starting with: { if (unitType !== "plan-slice" && unitType !== "execute-task") return { ok: true }; if (!isDbAvailable()) return { ok: true }; if (!refreshOpenDat… (missing: jsdoc)

### `src/resources/extensions/gsd/auto-runtime-state.ts`
- `src/resources/extensions/gsd/auto-runtime-state.ts:24` — `getAutoRuntimeSnapshot()` — exported: **yes** — Implements logic starting with: { const orchestrationStatus = autoSession.orchestration?.getStatus(); return { active: autoSession.active, paused: autoSession.paused, currentUnit:… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-runtime-state.ts:37` — `isAutoActive()` — exported: **yes** — Implements logic starting with: { return autoSession.active; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-runtime-state.ts:41` — `isAutoPaused()` — exported: **yes** — Implements logic starting with: { return autoSession.paused; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-runtime-state.ts:45` — `markToolStart(toolCallId: string, toolName?: string)` — exported: **yes** — Implements logic starting with: { markTrackedToolStart(toolCallId, autoSession.active, toolName); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-runtime-state.ts:49` — `markToolEnd(toolCallId: string)` — exported: **yes** — Implements logic starting with: { markTrackedToolEnd(toolCallId); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-runtime-state.ts:53` — `recordToolInvocationError(toolName: string, errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!autoSession.active) return; if (isToolInvocationError(errorMsg) || isQueuedUserMessageSkip(errorMsg) || isDeterministicPolicyError(errorMsg)… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-runtime-state.ts:60` — `clearToolInvocationError()` — exported: **yes** — Implements logic starting with: { if (!autoSession.active) return; autoSession.lastToolInvocationError = null; } (missing: jsdoc)

### `src/resources/extensions/gsd/auto-start.ts`
- `src/resources/extensions/gsd/auto-start.ts:108` — `resolveIsolationNoneBranchCheckout(currentBranch: string, integrationBranch: string, isolationMode: string, isRepo: boolean)` — exported: **yes** — Implements logic starting with: { if (!isRepo || isolationMode !== "none") return null; return currentBranch.startsWith("milestone/") ? integrationBranch : null; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-start.ts:131` — `hasGitIndexLockForTest(basePath: string)` — exported: **yes** — Implements logic starting with: { return existsSync(join(basePath, ".git", "index.lock")); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-start.ts:135` — `_shouldAbortBootstrapForUnavailableDbForTest(gsdDbPath: string, dbAvailable: boolean, pathExists: (path: string) => boolean = existsSync)` — exported: **yes** — Implements logic starting with: { return pathExists(gsdDbPath) && !dbAvailable; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-start.ts:143` — `openProjectDbIfPresent(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-start.ts:192` — `decideSurvivorAction(hasSurvivorBranch: boolean, phase: string | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!hasSurvivorBranch) return "none"; if (phase === "needs-discussion") return "discuss"; if (phase === "complete") return "finalize"; return "n… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-start.ts:202` — `auditOrphanedMilestoneBranches(basePath: string, isolationMode: "worktree" | "branch" | "none")` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/auto-start.ts:523` — `bootstrapAutoSession(s: AutoSession, ctx: ExtensionCommandContext, pi: ExtensionAPI, base: string, verboseMode: boolean, requestedStepMode: boolean, deps: BootstrapDeps, interrupted: InterruptedSessionAssessment)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-timeout-recovery.ts`
- `src/resources/extensions/gsd/auto-timeout-recovery.ts:31` — `recoverTimedOutUnit(ctx: ExtensionContext, pi: ExtensionAPI, unitType: string, unitId: string, reason: "idle" | "hard", rctx: RecoveryContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-timers.ts`
- `src/resources/extensions/gsd/auto-timers.ts:46` — `resolveUnitSupervisionTimeouts(unitType: string, supervisor: AutoSupervisorConfig, timeoutScale: number)` — exported: **yes** — Implements logic starting with: { const softMinutes = supervisor.soft_timeout_minutes ?? 0; const idleMinutes = supervisor.idle_timeout_minutes ?? 0; const hardMinutes = superviso… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-timers.ts:107` — `startUnitSupervision(sctx: SupervisionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto-unit-closeout.ts`
- `src/resources/extensions/gsd/auto-unit-closeout.ts:38` — `snapshotUnitActivity(ctx: ExtensionContext, startedAt: number, now = Date.now())` — exported: **yes** — Implements logic starting with: { let toolCalls = 0; let assistantMessages = 0; const entries = ctx.sessionManager.getEntries() ?? []; for (const entry of entries) { if (entry.typ… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-unit-closeout.ts:65` — `isSuspiciousGhostCompletion(ctx: ExtensionContext, startedAt: number, maxElapsedMs = GHOST_COMPLETION_MAX_ELAPSED_MS)` — exported: **yes** — Implements logic starting with: { const activity = snapshotUnitActivity(ctx, startedAt); return ( activity.elapsedMs < maxElapsedMs && activity.toolCalls === 0 && activity.assista… (missing: jsdoc)

### `src/resources/extensions/gsd/auto-worktree.ts`
- `src/resources/extensions/gsd/auto-worktree.ts:196` — `_isSamePath(a: string, b: string)` — exported: **yes** — Implements logic starting with: { return isSamePath(a, b); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-worktree.ts:200` — `_resolveAutoWorktreeStartPoint(integrationBranch: string | null | undefined, gitMainBranch: string | null | undefined, branchExists: (branch: string) => boolean)` — exported: **yes** — Implements logic starting with: { if (integrationBranch) return integrationBranch; return gitMainBranch && typeof gitMainBranch === "string" && gitMainBranch.length > 0 && branchE… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-worktree.ts:214` — `_shouldReconcileWorktreeDb(worktreeDbPath: string, mainDbPath: string, pathExists: (path: string) => boolean = existsSync, samePath: (a: string, b: string) => boolean = isSamePath)` — exported: **yes** — Implements logic starting with: { return pathExists(worktreeDbPath) && !samePath(worktreeDbPath, mainDbPath); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-worktree.ts:223` — `_isExpectedWorktreeUnlinkError(code: string | undefined)` — exported: **yes** — Implements logic starting with: { return code === "ENOENT" || code === "EISDIR"; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto-worktree.ts:366` — `isSafeToAutoResolve(filePath: string)` — exported: **yes** — Implements logic starting with: filePath.startsWith(".gsd/") || SAFE_AUTO_RESOLVE_PATTERNS.some((re) => re.test(filePath)) (missing: jsdoc)
- `src/resources/extensions/gsd/auto-worktree.ts:1093` — `createAutoWorktree(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { basePath = resolveWorktreeProjectRoot(basePath); // Check if repo has commits — git worktree requires a valid HEAD try { execFileSync("git", ["re… (missing: jsdoc)
- `src/resources/extensions/gsd/auto-worktree.ts:1434` — `getActiveAutoWorktreeContext()` — exported: **yes** — Implements logic starting with: { const ws = getActiveWorkspace(); if (!ws) return null; const originalBase = ws.projectRoot; const cwd = process.cwd(); if (!isGsdWorktreePath(cwd… (missing: jsdoc)

### `src/resources/extensions/gsd/auto.ts`
- `src/resources/extensions/gsd/auto.ts:368` — `_captureProjectRootEnvForTest(projectRoot: string)` — exported: **yes** — Implements logic starting with: { captureProjectRootEnv(projectRoot); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:372` — `_restoreProjectRootEnvForTest()` — exported: **yes** — Implements logic starting with: { restoreProjectRootEnv(); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:455` — `_synthesizePausedSessionRecoveryForTest(basePath: string, unitType: string, unitId: string, sessionFile: string)` — exported: **yes** — Implements logic starting with: { return synthesizePausedSessionRecovery(basePath, unitType, unitId, sessionFile); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:480` — `startAutoDetached(ctx: ExtensionCommandContext, pi: ExtensionAPI, base: string, verboseMode: boolean, options?: { step?: boolean; interrupted?: InterruptedSessionAssessment; milestoneLock?: string | null; })` — exported: **yes** — Implements logic starting with: { void withDetachedAutoKeepalive(startAuto(ctx, pi, base, verboseMode, options)).catch((err) => { const message = getErrorMessage(err); ctx.ui.noti… (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:629` — `getAutoDashboardData()` — exported: **yes** — Implements logic starting with: { const ledger = getLedger(); const totals = ledger ? getProjectTotals(ledger.units) : null; const sessionId = s.cmdCtx?.sessionManager?.getSession… (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:667` — `isAutoActive()` — exported: **yes** — Implements logic starting with: { return s.active; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:671` — `isAutoCompletionStopInProgress()` — exported: **yes** — Implements logic starting with: { return s.completionStopInProgress; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:700` — `isAutoPaused()` — exported: **yes** — Implements logic starting with: { return s.paused; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:710` — `refreshResumeResourcesAndDb(basePath: string, deps: ResumeResourceRefreshDeps = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:728` — `setActiveEngineId(id: string | null)` — exported: **yes** — Implements logic starting with: { s.activeEngineId = id; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:732` — `getActiveEngineId()` — exported: **yes** — Implements logic starting with: { return s.activeEngineId; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:736` — `setActiveRunDir(runDir: string | null)` — exported: **yes** — Implements logic starting with: { s.activeRunDir = runDir; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:740` — `getActiveRunDir()` — exported: **yes** — Implements logic starting with: { return s.activeRunDir; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:766` — `markToolStart(toolCallId: string, toolName?: string)` — exported: **yes** — Implements logic starting with: { _markToolStart(toolCallId, s.active, toolName); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:770` — `markToolEnd(toolCallId: string)` — exported: **yes** — Implements logic starting with: { _markToolEnd(toolCallId); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:789` — `getOldestInFlightToolAgeMs()` — exported: **yes** — Implements logic starting with: { return _getOldestInFlightToolAgeMs(); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:875` — `isStepMode()` — exported: **yes** — Implements logic starting with: { return s.stepMode; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:1023` — `cleanupAfterLoopExit(ctx: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:1080` — `_cleanupAfterLoopExitForTest(ctx: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:1138` — `_resolveAutoWorktreeExitActionForTest(currentMilestoneId: string | null | undefined, milestoneMergedInPhases: boolean, milestoneComplete: boolean)` — exported: **yes** — Implements logic starting with: { const action = _selectStopAutoWorktreeExit({ currentMilestoneId: currentMilestoneId ?? null, milestoneComplete, milestoneMergedInPhases, }); retu… (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:1151` — `stopAuto(ctx?: ExtensionContext, pi?: ExtensionAPI, reason?: string, options: StopAutoOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:1608` — `_selectStopAutoWorktreeExit(args: { currentMilestoneId: string | null; milestoneComplete: boolean; milestoneMergedInPhases: boolean; })` — exported: **yes** — Implements logic starting with: { if (!args.currentMilestoneId || args.milestoneMergedInPhases) return "none"; return args.milestoneComplete ? "merge" : "preserve"; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto.ts:2743` — `dispatchHookUnit(ctx: ExtensionContext, pi: ExtensionAPI, hookName: string, triggerUnitType: string, triggerUnitId: string, hookPrompt: string, hookModel: string | undefined, targetBasePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/custom-verify-retry-store.ts`
- `src/resources/extensions/gsd/auto/custom-verify-retry-store.ts:16` — `customVerifyRetryStateDir(s: Pick<AutoSession, "activeRunDir" | "basePath">)` — exported: **yes** — Implements logic starting with: { return s.activeRunDir ? join(s.activeRunDir, "runtime") : join(gsdRoot(s.basePath), "runtime"); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/custom-verify-retry-store.ts:20` — `customVerifyRetryStatePath(s: Pick<AutoSession, "activeRunDir" | "basePath">)` — exported: **yes** — Implements logic starting with: { return join(customVerifyRetryStateDir(s), "custom-verify-retries.json"); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/custom-verify-retry-store.ts:24` — `hydrateCustomVerifyRetryCounts(s: RetrySession, deps: RetryStoreLogDeps)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/custom-verify-retry-store.ts:49` — `saveCustomVerifyRetryCounts(s: RetrySession, deps: RetryStoreLogDeps)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/loop.ts`
- `src/resources/extensions/gsd/auto/loop.ts:1149` — `runUokKernelLoop(ctx: ExtensionContext, pi: ExtensionAPI, s: AutoSession, deps: LoopDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/loop.ts:1158` — `runLegacyAutoLoop(ctx: ExtensionContext, pi: ExtensionAPI, s: AutoSession, deps: LoopDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/orchestrator.ts`
- `src/resources/extensions/gsd/auto/orchestrator.ts:20` — `class AutoOrchestrator` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:29` — `AutoOrchestrator.constructor(deps: AutoOrchestratorDeps)` — exported: **yes** — Implements logic starting with: { this.deps = deps; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:33` — `AutoOrchestrator.start(_sessionContext: AutoSessionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:43` — `AutoOrchestrator.advance()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:284` — `AutoOrchestrator.resume()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:294` — `AutoOrchestrator.stop(reason: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:309` — `AutoOrchestrator.getStatus()` — exported: **yes** — Implements logic starting with: { return { ...this.status, activeUnit: this.status.activeUnit ? { ...this.status.activeUnit } : undefined }; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/orchestrator.ts:319` — `createAutoOrchestrator(deps: AutoOrchestratorDeps)` — exported: **yes** — Implements logic starting with: { return new AutoOrchestrator(deps); } (missing: jsdoc)

### `src/resources/extensions/gsd/auto/phases.ts`
- `src/resources/extensions/gsd/auto/phases.ts:137` — `shouldDegradeEmptyWorktreeToProjectRoot(worktreeClassification: ReturnType<typeof classifyProject>, projectRootClassification: ReturnType<typeof classifyProject>)` — exported: **yes** — Implements logic starting with: { return ( worktreeClassification.kind === "greenfield" && projectRootClassification.kind !== "greenfield" && projectRootClassification.kind !== "i… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/phases.ts:249` — `resetSessionTimeoutState()` — exported: **yes** — Implements logic starting with: { consecutiveSessionTimeouts = 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/phases.ts:282` — `shouldRunPlanV2Gate(phase: Phase)` — exported: **yes** — Implements logic starting with: { return PLAN_V2_GATE_PHASES.has(phase); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/phases.ts:286` — `_shouldProceedWithInvalidRepoClassificationForTest(reason: string | undefined, hasGit: boolean)` — exported: **yes** — Implements logic starting with: { return reason === "missing .git" && hasGit; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/phases.ts:293` — `_resolveCurrentUnitStartedAtForTest(currentUnit: { startedAt: number } | null | undefined)` — exported: **yes** — Implements logic starting with: { return currentUnit?.startedAt; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/phases.ts:505` — `_runMilestoneMergeOnceWithStashRestore(ic: IterationContext, milestoneId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/phases.ts:543` — `_buildCancelledUnitStopReason(unitType: string, unitId: string, errorContext?: { message: string; category: string })` — exported: **yes** — Implements logic starting with: { const cancellationMessage = errorContext?.message ?? "unknown"; const isSessionCreationFailure = errorContext?.category === "session-failed"; if… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/resolve.ts`
- `src/resources/extensions/gsd/auto/resolve.ts:32` — `_setCurrentResolve(fn: ((result: UnitResult) => void) | null)` — exported: **yes** — Implements logic starting with: { _currentResolve = fn; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:36` — `_setSessionSwitchInFlight(v: boolean)` — exported: **yes** — Implements logic starting with: { _sessionSwitchInFlight = v; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:40` — `_markSessionSwitchAbortGraceWindow(durationMs = DEFAULT_SESSION_SWITCH_ABORT_GRACE_MS)` — exported: **yes** — Implements logic starting with: { _sessionSwitchAbortGraceUntil = Math.max( _sessionSwitchAbortGraceUntil, Date.now() + durationMs, ); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:47` — `_clearSessionSwitchAbortGraceWindow()` — exported: **yes** — Implements logic starting with: { _sessionSwitchAbortGraceUntil = 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:51` — `isSessionSwitchAbortGraceActive(now = Date.now())` — exported: **yes** — Implements logic starting with: { return now < _sessionSwitchAbortGraceUntil; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:55` — `_clearCurrentResolve()` — exported: **yes** — Implements logic starting with: { _currentResolve = null; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:59` — `_consumePendingSwitchCancellation()` — exported: **yes** — Implements logic starting with: { const pending = _pendingSwitchCancellation; _pendingSwitchCancellation = null; return pending; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:93` — `isSessionSwitchInFlight()` — exported: **yes** — Implements logic starting with: { return _sessionSwitchInFlight; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/resolve.ts:166` — `_hasPendingResolveForTest()` — exported: **yes** — Implements logic starting with: { return _currentResolve !== null; } (missing: jsdoc)

### `src/resources/extensions/gsd/auto/run-unit.ts`
- `src/resources/extensions/gsd/auto/run-unit.ts:34` — `shouldDeferUnitFailsafeTimeout(runtime: AutoUnitRuntimeRecord | null, opts: { nowMs: number; currentUnitStartedAt?: number; freshProgressMs: number; })` — exported: **yes** — Implements logic starting with: { if (!runtime) return false; if (opts.currentUnitStartedAt === undefined) return false; if (runtime.startedAt !== opts.currentUnitStartedAt) retur… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/session.ts`
- `src/resources/extensions/gsd/auto/session.ts:87` — `class AutoSession` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/session.ts:251` — `AutoSession.clearTimers()` — exported: **yes** — Implements logic starting with: { if (this.unitTimeoutHandle) { clearTimeout(this.unitTimeoutHandle); this.unitTimeoutHandle = null; } if (this.wrapupWarningHandle) { clearTimeout… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/session.ts:258` — `AutoSession.resetDispatchCounters()` — exported: **yes** — Implements logic starting with: { this.unitDispatchCount.clear(); this.unitLifetimeDispatches.clear(); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/session.ts:263` — `AutoSession.get lockBasePath()` — exported: **yes** — Implements logic starting with: { return resolveWorktreeProjectRoot(this.basePath, this.originalBasePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/session.ts:285` — `AutoSession.reset()` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/session.ts:379` — `AutoSession.resetAfterStop(options: { preserveCompletionSurface?: boolean } = {})` — exported: **yes** — Implements logic starting with: { const completionStopInProgress = options.preserveCompletionSurface ? this.completionStopInProgress : false; this.reset(); this.completionStopInPr… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/session.ts:385` — `AutoSession.toJSON()` — exported: **yes** — Implements logic starting with: { const orchestrationStatus = this.orchestration?.getStatus(); return { active: this.active, paused: this.paused, stepMode: this.stepMode, basePath… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/unit-runner-events.ts`
- `src/resources/extensions/gsd/auto/unit-runner-events.ts:5` — `isInternalSessionTransitionAbortEvent(event: Pick<AgentEndEvent, "abortOrigin">)` — exported: **yes** — Implements logic starting with: { return event.abortOrigin === "session-transition"; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/unit-runner-events.ts:11` — `shouldIgnoreAgentEndForActiveUnit(event: Pick<AgentEndEvent, "abortOrigin">)` — exported: **yes** — Implements logic starting with: { return isInternalSessionTransitionAbortEvent(event); } (missing: jsdoc)

### `src/resources/extensions/gsd/auto/verification-retry-policy.ts`
- `src/resources/extensions/gsd/auto/verification-retry-policy.ts:27` — `verificationRetryKey(unitType: string, unitId: string)` — exported: **yes** — Implements logic starting with: { return `${unitType}:${unitId}`; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/verification-retry-policy.ts:31` — `hashVerificationFailureContext(failureContext: string)` — exported: **yes** — Implements logic starting with: { const normalized = failureContext.replace(/\r\n/g, "\n").trim(); return createHash("sha256").update(normalized).digest("hex"); } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/verification-retry-policy.ts:36` — `verificationRetryDelayMs(attempt: number, random: () => number = Math.random)` — exported: **yes** — Implements logic starting with: { const safeAttempt = Math.max(1, Math.floor(attempt)); const baseDelayMs = Math.min( VERIFICATION_RETRY_MAX_DELAY_MS, VERIFICATION_RETRY_BASE_DELA… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/verification-retry-policy.ts:54` — `decideVerificationRetry(input: { unitType: string | undefined; retryInfo: PendingVerificationRetry | null | undefined; previousFailureHash: string | undefined; random?: () => number; })` — exported: **yes** — Implements logic starting with: { const { retryInfo, unitType } = input; if (!retryInfo || !unitType) { return { action: "pause", reason: "missing-retry-context" }; } const key =… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-custom-engine-dispatch-outcome.ts`
- `src/resources/extensions/gsd/auto/workflow-custom-engine-dispatch-outcome.ts:15` — `handleCustomEngineDispatchOutcome(input: { decision: EngineDispatchDecision; deps: HandleCustomEngineDispatchOutcomeDeps; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-custom-engine-iteration.ts`
- `src/resources/extensions/gsd/auto/workflow-custom-engine-iteration.ts:28` — `buildCustomEngineIterationData(input: BuildCustomEngineIterationDataInput)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-custom-engine-reconcile-outcome.ts`
- `src/resources/extensions/gsd/auto/workflow-custom-engine-reconcile-outcome.ts:22` — `handleCustomEngineReconcileOutcome(input: { outcome: CustomEngineReconcileOutcome; unitType: string; unitId: string; deps: HandleCustomEngineReconcileOutcomeDeps; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-custom-engine-reconcile.ts`
- `src/resources/extensions/gsd/auto/workflow-custom-engine-reconcile.ts:38` — `handleCustomEngineReconcile(input: { session: CustomEngineReconcileSession; engineState: EngineState; iterData: IterationData; iteration: number; deps: HandleCustomEngineReconcileDeps; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-custom-engine-retry.ts`
- `src/resources/extensions/gsd/auto/workflow-custom-engine-retry.ts:34` — `handleCustomEngineVerifyRetry(input: { session: RetrySession; unitType: string; unitId: string; basePath: string; iteration: number; maxRetries: number; deps: HandleCustomEngineVerifyRetryDeps; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-custom-engine-verify-outcome.ts`
- `src/resources/extensions/gsd/auto/workflow-custom-engine-verify-outcome.ts:19` — `handleCustomEngineVerifyPause(input: { unitType: string; unitId: string; deps: HandleCustomEngineVerifyOutcomeDeps; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-custom-engine-verify-outcome.ts:33` — `handleCustomEngineVerifyRetryOutcome(input: { outcome: CustomEngineVerifyRetryOutcome; deps: HandleCustomEngineVerifyOutcomeDeps; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-dispatch-claim.ts`
- `src/resources/extensions/gsd/auto/workflow-dispatch-claim.ts:72` — `ensureDispatchLease(s: AutoSession, milestoneId: string | undefined, deps: EnsureDispatchLeaseDeps, opts: { forceReclaim?: boolean } = {})` — exported: **yes** — Implements logic starting with: { if (!s.workerId) return { kind: "degraded", reason: "missing-worker" }; if (!milestoneId) return { kind: "degraded", reason: "missing-milestone"… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-dispatch-claim.ts:109` — `openDispatchClaim(s: AutoSession, flowId: string, turnId: string, iterData: IterationData, deps: OpenDispatchClaimDeps)` — exported: **yes** — Implements logic starting with: { if (!s.workerId || typeof s.milestoneLeaseToken !== "number") return { kind: "degraded" }; const mid = iterData.mid; if (!mid) return { kind: "de… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-dispatch-ledger.ts`
- `src/resources/extensions/gsd/auto/workflow-dispatch-ledger.ts:16` — `settleDispatchFailed(dispatchId: number | null, errorSummary: string, deps: DispatchLedgerFailDeps)` — exported: **yes** — Implements logic starting with: { if (dispatchId === null) return false; try { deps.markFailed(dispatchId, { errorSummary }); return true; } catch (err) { deps.logWriteFailure(err… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-dispatch-ledger.ts:32` — `settleDispatchCompleted(dispatchId: number | null, deps: DispatchLedgerCompleteDeps)` — exported: **yes** — Implements logic starting with: { if (dispatchId === null) return false; try { deps.markCompleted(dispatchId); return true; } catch (err) { deps.logWriteFailure(err); return false… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-iteration-completion.ts`
- `src/resources/extensions/gsd/auto/workflow-iteration-completion.ts:16` — `completeWorkflowIteration(state: WorkflowIterationRecoveryState, deps: CompleteWorkflowIterationDeps)` — exported: **yes** — Implements logic starting with: { state.consecutiveErrors = 0; state.consecutiveCooldowns = 0; state.recentErrorMessages.length = 0; deps.emitIterationEnd(); deps.saveStuckState()… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-journal-reporter.ts`
- `src/resources/extensions/gsd/auto/workflow-journal-reporter.ts:17` — `createWorkflowJournalReporter(input: WorkflowJournalReporterInput)` — exported: **yes** — Implements logic starting with: { const now = input.now ?? (() => new Date().toISOString()); return { emit(eventType: JournalEventType, data?: Record<string, unknown>): void { inp… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-kernel.ts`
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:204` — `decideWorkflowLoop(input: WorkflowLoopInput)` — exported: **yes** — Implements logic starting with: { if (!input.active) { return { action: "stop", reason: "inactive", message: "Auto-mode is not active.", }; } if (input.iteration > input.maxIterat… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:242` — `decideDispatchClaim(input: DispatchClaimInput)` — exported: **yes** — Implements logic starting with: { if (input.kind === "skip") { return { action: "skip", reason: input.reason, }; } if (input.kind === "opened") { return { action: "run", dispatchI… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:263` — `decideEngineDispatch(input: EngineDispatchInput)` — exported: **yes** — Implements logic starting with: { if (input.action === "stop") { return { action: "stop", reason: input.reason ?? "Engine stopped", }; } if (input.action === "skip") { return { ac… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:278` — `decideFinalizeResult(input: FinalizeInput)` — exported: **yes** — Implements logic starting with: { if (input.action === "break") { const reason = input.reason ?? "unknown"; return { action: "stop", failureClass: reason === "git-closeout-failure… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:299` — `decideEngineReconcile(input: EngineReconcileInput)` — exported: **yes** — Implements logic starting with: { if (input.outcome === "milestone-complete") { return { action: "complete-workflow", stopReason: "Workflow complete", }; } if (input.outcome === "… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:321` — `decideMemoryPressure(input: MemoryPressureInput)` — exported: **yes** — Implements logic starting with: { if (!input.pressured) { return { action: "continue" }; } const pct = Math.round(input.pct * 100); return { action: "stop", warningMessage: `Memor… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:339` — `decideMinRequestInterval(input: MinRequestIntervalInput)` — exported: **yes** — Implements logic starting with: { if (input.minIntervalMs <= 0 || input.lastRequestTimestamp <= 0) { return { action: "continue" }; } const elapsedMs = input.nowMs - input.lastReq… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:355` — `decideCooldownRecovery(input: CooldownRecoveryInput)` — exported: **yes** — Implements logic starting with: { if (input.consecutiveCooldowns > input.maxCooldownRetries) { return { action: "stop", notifyMessage: `Auto-mode stopped: ${input.consecutiveCoold… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:379` — `decideIterationErrorRecovery(input: IterationErrorRecoveryInput)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:412` — `decideCustomEngineVerifyRetry(input: CustomEngineVerifyRetryInput)` — exported: **yes** — Implements logic starting with: { return input.attempts > input.maxRetries ? { action: "recover" } : { action: "retry" }; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:420` — `shouldUseCustomEnginePath(input: CustomEnginePathInput)` — exported: **yes** — Implements logic starting with: { return input.activeEngineId != null && input.activeEngineId !== "dev" && !input.hasSidecarItem && !input.engineBypass; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:427` — `resolveUnitRequestTimestamp(input: UnitRequestTimestampInput)` — exported: **yes** — Implements logic starting with: { const requestTimestamp = input.requestDispatchedAt ?? input.unitStartedAt; return typeof requestTimestamp === "number" ? requestTimestamp : undef… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:432` — `decideCustomEngineRecovery(input: CustomEngineRecoveryInput)` — exported: **yes** — Implements logic starting with: { const exhaustedTurnError = "custom-engine-verify-retry-exhausted"; if (input.outcome === "pause") { return { action: "pause", turnError: input.re… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:463` — `decideInfrastructureError(input: InfrastructureErrorInput)` — exported: **yes** — Implements logic starting with: { return { notifyMessage: `Auto-mode stopped: infrastructure error ${input.code} — ${input.errorMessage}`, stopMessage: `Infrastructure error (${in… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:472` — `decideModelPolicyBlocked(input: ModelPolicyBlockedInput)` — exported: **yes** — Implements logic starting with: { return { notifyMessage: `Auto-mode paused: model-policy denied dispatch for ${input.unitType}/${input.unitId}. ${input.errorMessage}`, journalDat… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:488` — `decideDispatchNodeKind(unitType: string, sidecarKind?: DispatchSidecarKind)` — exported: **yes** — Implements logic starting with: { if (sidecarKind === "hook") return "hook"; if (sidecarKind === "triage") return "verification"; if (sidecarKind === "quick-task") return "team-wo… (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:512` — `formatDispatchExceptionSummary(input: DispatchLedgerErrorInput)` — exported: **yes** — Implements logic starting with: { const message = input.error instanceof Error ? input.error.message : String(input.error); return `exception:${message}`; } (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-kernel.ts:517` — `formatUnhandledDispatchErrorSummary(input: DispatchLedgerErrorInput)` — exported: **yes** — Implements logic starting with: { const message = input.error instanceof Error ? input.error.message : String(input.error); return `unhandled-error:${message.slice(0, 200)}`; } (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-memory-pressure.ts`
- `src/resources/extensions/gsd/auto/workflow-memory-pressure.ts:33` — `measureMemoryPressure(options?: { threshold?: number; fallbackLimitMB?: number; deps?: Partial<MeasureMemoryPressureDeps>; })` — exported: **yes** — Implements logic starting with: { const threshold = options?.threshold ?? DEFAULT_MEMORY_PRESSURE_THRESHOLD; const fallbackLimitMB = options?.fallbackLimitMB ?? DEFAULT_HEAP_LIMIT… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-phase-reporter.ts`
- `src/resources/extensions/gsd/auto/workflow-phase-reporter.ts:14` — `createWorkflowPhaseReporter(input: WorkflowPhaseReporterInput)` — exported: **yes** — Implements logic starting with: { return { report(phase: TurnPhase, action: string, data?: Record<string, unknown>): void { input.observer?.onPhaseResult(phase, action, data); },… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-session-lock.ts`
- `src/resources/extensions/gsd/auto/workflow-session-lock.ts:29` — `validateWorkflowSessionLock(input: { active: boolean; iteration: number; maxIterations: number; deps: WorkflowSessionLockDeps; })` — exported: **yes** — Implements logic starting with: { const sessionLockBase = input.deps.lockBase(); if (!sessionLockBase) return { action: "continue" }; const lockStatus = input.deps.validateSession… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-sidecar-iteration.ts`
- `src/resources/extensions/gsd/auto/workflow-sidecar-iteration.ts:22` — `buildSidecarIterationData(input: BuildSidecarIterationDataInput)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-sidecar-queue.ts`
- `src/resources/extensions/gsd/auto/workflow-sidecar-queue.ts:21` — `dequeueSidecarItem(input: DequeueSidecarItemInput)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-turn-reporter.ts`
- `src/resources/extensions/gsd/auto/workflow-turn-reporter.ts:34` — `createWorkflowTurnReporter(input: WorkflowTurnReporterInput)` — exported: **yes** — Implements logic starting with: { let finished = false; const now = input.now ?? (() => new Date().toISOString()); return { start(): void { input.observer?.onTurnStart({ traceId:… (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-unit-dispatch.ts`
- `src/resources/extensions/gsd/auto/workflow-unit-dispatch.ts:41` — `runUnitPhaseViaContract(dispatchContract: DispatchContract, ic: IterationContext, iterData: IterationData, loopState: LoopState, sidecarItem: SidecarItem | undefined, deps: RunUnitPhaseViaContractDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/auto/workflow-unit-dispatch.ts:84` — `createExecutionGraphUnitDispatchDeps()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/auto/workflow-worker-heartbeat.ts`
- `src/resources/extensions/gsd/auto/workflow-worker-heartbeat.ts:25` — `maintainWorkerHeartbeat(session: WorkerHeartbeatSession, deps: MaintainWorkerHeartbeatDeps)` — exported: **yes** — Implements logic starting with: { if (!session.workerId) return; try { deps.heartbeatAutoWorker(session.workerId); if (session.currentMilestoneId && session.milestoneLeaseToken) {… (missing: jsdoc)

### `src/resources/extensions/gsd/blocked-models.ts`
- `src/resources/extensions/gsd/blocked-models.ts:53` — `loadBlockedModels(basePath: string)` — exported: **yes** — Implements logic starting with: { return readFileSafe(blockedModelsPath(basePath)).blocked; } (missing: jsdoc)
- `src/resources/extensions/gsd/blocked-models.ts:57` — `isModelBlocked(basePath: string, provider: string | undefined, id: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!provider || !id) return false; const target = modelKey(provider, id); return loadBlockedModels(basePath).some( (e) => modelKey(e.provider, e… (missing: jsdoc)
- `src/resources/extensions/gsd/blocked-models.ts:69` — `blockModel(basePath: string, provider: string, id: string, reason: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts`
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:79` — `_buildAbortedPauseContext(lastMsg: { errorMessage?: unknown })` — exported: **yes** — Implements logic starting with: { const hasErrorMessage = Object.prototype.hasOwnProperty.call(lastMsg, "errorMessage") && !!lastMsg.errorMessage; return { message: hasErrorMessag… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:92` — `isUserInitiatedAbortMessage(message: string | undefined | null)` — exported: **yes** — Implements logic starting with: { if (!message) return false; return /\b(?:claude code process aborted by user|request aborted by user|process aborted by user)\b/i.test(message); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:118` — `isClaudeCodeSessionSwitchAbortMessage(lastMsg: unknown)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:133` — `isBareClaudeCodeStreamAbortPlaceholder(lastMsg: unknown)` — exported: **yes** — Implements logic starting with: { if (!lastMsg || typeof lastMsg !== "object") return false; const m = lastMsg as { stopReason?: unknown; errorMessage?: unknown; content?: unknown… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:187` — `resolveAgentEndErrorDisplay(rawErrorMsg: string, content: unknown)` — exported: **yes** — Implements logic starting with: { const isUseless = !rawErrorMsg || /^(success|ok|true|error|unknown)$/i.test(rawErrorMsg.trim()); if (isUseless && Array.isArray(content)) { const… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:233` — `handleAgentEnd(pi: ExtensionAPI, event: AgentEndEvent, ctx: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/db-tools.ts`
- `src/resources/extensions/gsd/bootstrap/db-tools.ts:65` — `registerDbTools(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts`
- `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts:14` — `safeWorkspaceCwd()` — exported: **yes** — Implements logic starting with: { try { return process.cwd(); } catch { const projectRoot = process.env.GSD_PROJECT_ROOT; if (projectRoot && existsSync(projectRoot)) return projec… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts:24` — `resolveCtxCwd(ctx?: unknown)` — exported: **yes** — Implements logic starting with: { if (ctx && typeof ctx === "object" && typeof (ctx as { cwd?: unknown }).cwd === "string") { const cwd = (ctx as { cwd: string }).cwd; if (existsS… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts:42` — `ensureDbOpen(basePath: string = safeWorkspaceCwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts:73` — `registerDynamicTools(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/exec-tools.ts`
- `src/resources/extensions/gsd/bootstrap/exec-tools.ts:27` — `registerExecTools(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/journal-tools.ts`
- `src/resources/extensions/gsd/bootstrap/journal-tools.ts:11` — `registerJournalTools(pi: ExtensionAPI)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/memory-tools.ts`
- `src/resources/extensions/gsd/bootstrap/memory-tools.ts:20` — `registerMemoryTools(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/provider-error-resume.ts`
- `src/resources/extensions/gsd/bootstrap/provider-error-resume.ts:30` — `resumeAutoAfterProviderDelay(pi: ExtensionAPI, ctx: ExtensionContext, deps: ProviderErrorResumeDeps = defaultDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/query-tools.ts`
- `src/resources/extensions/gsd/bootstrap/query-tools.ts:10` — `registerQueryTools(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/register-extension.ts`
- `src/resources/extensions/gsd/bootstrap/register-extension.ts:28` — `handleRecoverableExtensionProcessError(err: Error)` — exported: **yes** — Implements logic starting with: { if ((err as NodeJS.ErrnoException).code === "EPIPE") { process.exit(0); } if ((err as NodeJS.ErrnoException).code === "ENOENT") { const syscall =… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-extension.ts:46` — `installEpipeGuard()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-extension.ts:70` — `registerGsdExtension(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/register-hooks.ts`
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:170` — `buildMinimalGsdToolSet(activeToolNames: readonly string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:177` — `buildMinimalAutoGsdToolSet(activeToolNames: readonly string[], unitType: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:189` — `buildMinimalGsdWorkflowToolSet(activeToolNames: readonly string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:197` — `buildRequestScopedGsdToolSet(activeToolNames: readonly string[], requestCustomMessages: readonly { customType?: string }[] | undefined)` — exported: **yes** — Implements logic starting with: { for (let index = (requestCustomMessages?.length ?? 0) - 1; index >= 0; index--) { const currentCustomType = requestCustomMessages?.[index]?.custo… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:215` — `isFullGsdToolSurfaceRequested()` — exported: **yes** — Implements logic starting with: { return process.env.PI_GSD_FULL_TOOLS === "1"; } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:242` — `scopeGsdWorkflowToolsForDispatch(pi: GsdWorkflowScopeApi, unitType?: string)` — exported: **yes** — Implements logic starting with: { if (isFullGsdToolSurfaceRequested()) return null; const current = pi.getActiveTools(); const scoped = unitType ? buildMinimalAutoGsdToolSet(curre… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:271` — `restoreGsdWorkflowTools(pi: Pick<ExtensionAPI, "setActiveTools"> & Partial<Pick<ExtensionAPI, "setVisibleSkills">>, savedState: ScopedGsdWorkflowState | null)` — exported: **yes** — Implements logic starting with: { if (!savedState) return; if (savedState.tools) pi.setActiveTools(savedState.tools); if (savedState.restoreVisibleSkills && pi.setVisibleSkills) {… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:380` — `resolveNotificationStoreBasePath(basePath: string)` — exported: **yes** — Implements logic starting with: { return resolveWorktreeProjectRoot(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:422` — `registerHooks(pi: ExtensionAPI, ecosystemHandlers: GSDEcosystemBeforeAgentStartHandler[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/register-shortcuts.ts`
- `src/resources/extensions/gsd/bootstrap/register-shortcuts.ts:18` — `registerShortcuts(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/subagent-input.ts`
- `src/resources/extensions/gsd/bootstrap/subagent-input.ts:1` — `extractSubagentAgentClasses(input: unknown)` — exported: **yes** — Implements logic starting with: { if (!input || typeof input !== "object") return []; const agentClasses: string[] = []; const visited = new WeakSet<object>(); const addAgentClass… (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/system-context.ts`
- `src/resources/extensions/gsd/bootstrap/system-context.ts:105` — `buildBeforeAgentStartResult(event: { prompt: string; systemPrompt: string }, ctx: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/system-context.ts:408` — `loadKnowledgeBlock(gsdHomeDir: string, cwd: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/system-context.ts:533` — `isLowEntropyResumePrompt(prompt: string)` — exported: **yes** — Implements logic starting with: { const trimmed = prompt.trim().toLowerCase().replace(/[.!?,]+$/g, ""); return RESUME_INTENT_PATTERNS.test(trimmed); } (missing: jsdoc)

### `src/resources/extensions/gsd/bootstrap/write-gate.ts`
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:123` — `canonicalToolName(toolName: string)` — exported: **yes** — Implements logic starting with: { if (!toolName.startsWith("mcp__")) return toolName; const toolSeparator = toolName.indexOf("__", "mcp__".length); return toolSeparator >= 0 ? too… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:230` — `loadWriteGateSnapshot(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:246` — `isDepthVerified(basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return getWriteGateState(basePath).verifiedDepthMilestones.size > 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:261` — `isMilestoneDepthVerifiedInSnapshot(snapshot: WriteGateSnapshot, milestoneId: string | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!milestoneId) return false; return snapshot.verifiedDepthMilestones.includes(milestoneId); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:269` — `isQueuePhaseActive(basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return getWriteGateState(basePath).activeQueuePhase; } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:273` — `setQueuePhaseActive(active: boolean, basePath: string)` — exported: **yes** — Implements logic starting with: { getWriteGateState(basePath).activeQueuePhase = active; persistWriteGateSnapshot(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:278` — `resetWriteGateState(basePath: string)` — exported: **yes** — Implements logic starting with: { const state = getWriteGateState(basePath); state.verifiedDepthMilestones.clear(); state.verifiedApprovalGates.clear(); state.pendingGateId = null… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:286` — `clearDiscussionFlowState(basePath: string)` — exported: **yes** — Implements logic starting with: { writeGateStatesByBasePath.delete(writeGateStateKey(basePath)); clearPersistedWriteGateSnapshot(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:291` — `markDepthVerified(milestoneId?: string | null, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { if (!milestoneId) return; getWriteGateState(basePath).verifiedDepthMilestones.add(milestoneId); persistWriteGateSnapshot(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:297` — `markApprovalGateVerified(gateId?: string | null, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { if (!gateId) return; getWriteGateState(basePath).verifiedApprovalGates.add(gateId); persistWriteGateSnapshot(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:303` — `isApprovalGateVerifiedInSnapshot(snapshot: WriteGateSnapshot, gateId?: string | null)` — exported: **yes** — Implements logic starting with: { if (!gateId) return false; return (snapshot.verifiedApprovalGates ?? []).includes(gateId); } (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:378` — `shouldBlockPendingGateInSnapshot(snapshot: WriteGateSnapshot, toolName: string, _milestoneId: string | null, _queuePhaseActive?: boolean)` — exported: **yes** — Implements logic starting with: { if (!snapshot.pendingGateId) return { block: false }; if (GATE_SAFE_TOOLS.has(canonicalToolName(toolName))) return { block: false }; return { blo… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:414` — `shouldBlockPendingGateBashInSnapshot(snapshot: WriteGateSnapshot, command: string, _milestoneId: string | null, _queuePhaseActive?: boolean)` — exported: **yes** — Implements logic starting with: { if (!snapshot.pendingGateId) return { block: false }; return { block: true, reason: [ `HARD BLOCK: Discussion gate "${snapshot.pendingGateId}" ha… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:463` — `shouldBlockContextWrite(toolName: string, inputPath: string, milestoneId: string | null, _queuePhaseActive?: boolean, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { if (toolName !== "write") return { block: false }; if (!MILESTONE_CONTEXT_RE.test(inputPath)) return { block: false }; const targetMilestoneId =… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:513` — `shouldBlockContextArtifactSaveInSnapshot(snapshot: WriteGateSnapshot, artifactType: string, milestoneId: string | null, sliceId?: string | null)` — exported: **yes** — Implements logic starting with: { if (artifactType !== "CONTEXT") return { block: false }; if (sliceId) return { block: false }; if (!milestoneId) { return { block: true, reason:… (missing: jsdoc)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:614` — `shouldBlockQueueExecutionInSnapshot(snapshot: WriteGateSnapshot, toolName: string, input: string, queuePhaseActive: boolean = snapshot.activeQueuePhase)` — exported: **yes** — Implements logic starting with: { if (!queuePhaseActive) return { block: false }; // Always-safe tools (read-only, discussion, planning) if (QUEUE_SAFE_TOOLS.has(toolName)) return… (missing: jsdoc)

### `src/resources/extensions/gsd/changelog.ts`
- `src/resources/extensions/gsd/changelog.ts:108` — `handleChangelog(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: jsdoc)

### `src/resources/extensions/gsd/claude-import.ts`
- `src/resources/extensions/gsd/claude-import.ts:50` — `getClaudeSearchRoots(cwd: string)` — exported: **yes** — Implements logic starting with: { const home = homedir(); const parent = resolve(cwd, ".."); const grandparent = resolve(cwd, "..", ".."); // Claude Code user-scope skills live un… (missing: jsdoc)
- `src/resources/extensions/gsd/claude-import.ts:172` — `discoverClaudeSkills(cwd: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/claude-import.ts:197` — `discoverClaudePlugins(cwd: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/claude-import.ts:507` — `runClaudeImportFlow(ctx: ExtensionCommandContext, scope: "global" | "project", readPrefs: () => Record<string, unknown>, writePrefs: (prefs: Record<string, unknown>) => Promise<void>)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/codebase-generator.ts`
- `src/resources/extensions/gsd/codebase-generator.ts:155` — `parseCodebaseMapMetadata(content: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `src/resources/extensions/gsd/codebase-generator.ts:465` — `ensureCodebaseMapFresh(basePath: string, options?: CodebaseMapOptions, ensureOptions?: EnsureCodebaseMapOptions)` — exported: **yes** — Implements logic starting with: { const resolved = resolveGeneratorOptions(options); const cacheKey = `${basePath}::${resolved.optionSignature}`; const ttlMs = ensureOptions?.ttlM… (missing: jsdoc)

### `src/resources/extensions/gsd/commands-add-tests.ts`
- `src/resources/extensions/gsd/commands-add-tests.ts:89` — `handleAddTests(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-backlog.ts`
- `src/resources/extensions/gsd/commands-backlog.ts:159` — `handleBacklog(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-bootstrap.ts`
- `src/resources/extensions/gsd/commands-bootstrap.ts:271` — `registerLazyGSDCommand(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-cmux.ts`
- `src/resources/extensions/gsd/commands-cmux.ts:118` — `handleCmux(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-codebase.ts`
- `src/resources/extensions/gsd/commands-codebase.ts:35` — `handleCodebase(args: string, ctx: ExtensionCommandContext, _pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-config.ts`
- `src/resources/extensions/gsd/commands-config.ts:26` — `getStoredToolKey(auth: AuthStorage, providerId: string)` — exported: **yes** — Implements logic starting with: { const creds = auth.getCredentialsForProvider(providerId); const cred = creds.find((c) => c.type === "api_key" && c.key); return cred?.type === "a… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-config.ts:53` — `getConfigAuthStorage()` — exported: **yes** — Implements logic starting with: { const authPath = join(gsdHome(), "agent", "auth.json"); mkdirSync(dirname(authPath), { recursive: true }); return AuthStorage.create(authPath); } (missing: jsdoc)
- `src/resources/extensions/gsd/commands-config.ts:61` — `handleConfig(ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-debug.ts`
- `src/resources/extensions/gsd/commands-debug.ts:56` — `parseDebugCommand(args: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-debug.ts:95` — `handleDebug(args: string, ctx: ExtensionCommandContext, pi?: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-do.ts`
- `src/resources/extensions/gsd/commands-do.ts:73` — `handleDo(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-eval-review.ts`
- `src/resources/extensions/gsd/commands-eval-review.ts:139` — `EvalReviewArgError.constructor(reason: string)` — exported: **yes** — Implements logic starting with: { super(reason); this.name = "EvalReviewArgError"; } (missing: jsdoc)
- `src/resources/extensions/gsd/commands-eval-review.ts:575` — `planEvalReviewAction(args: EvalReviewArgs, detected: EvalReviewState, existingPath: string | null)` — exported: **yes** — Implements logic starting with: { if (detected.kind === "no-slice-dir") return { kind: "no-slice-dir" }; // --show is read-only and tolerates missing SUMMARY.md. if (args.show) re… (missing: jsdoc)

### `src/resources/extensions/gsd/commands-extensions.ts`
- `src/resources/extensions/gsd/commands-extensions.ts:183` — `validateExtensionPackage(packageDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-extensions.ts:796` — `handleExtensions(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-handlers.ts`
- `src/resources/extensions/gsd/commands-handlers.ts:80` — `dispatchDoctorHeal(pi: ExtensionAPI, scope: string | undefined, reportText: string, structuredIssues: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:118` — `isDoctorHealActionable(issue: { fixable: boolean; severity: string })` — exported: **yes** — Implements logic starting with: { return issue.fixable && issue.severity !== "info"; } (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:122` — `handleDoctor(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:165` — `handleSkillHealth(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:204` — `handleCapture(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:232` — `handleTriage(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:291` — `handleSteer(change: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:346` — `handleKnowledge(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:392` — `handleRunHook(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-handlers.ts:472` — `handleUpdate(ctx: ExtensionCommandContext)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-inspect.ts`
- `src/resources/extensions/gsd/commands-inspect.ts:21` — `formatInspectOutput(data: InspectData)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push("=== GSD Database Inspect ==="); lines.push(`Schema version: ${data.schemaVersion ?? "unknown"}`); lines.p… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-inspect.ts:49` — `handleInspect(ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-logs.ts`
- `src/resources/extensions/gsd/commands-logs.ts:246` — `handleLogs(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-maintenance.ts`
- `src/resources/extensions/gsd/commands-maintenance.ts:12` — `handleCleanupBranches(ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-maintenance.ts:125` — `handleCleanupSnapshots(ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-maintenance.ts:164` — `handleCleanupWorktrees(ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-maintenance.ts:241` — `handleSkip(unitArg: string, ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-maintenance.ts:287` — `handleDryRun(ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-maintenance.ts:363` — `handleCleanupProjects(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-mcp-status.ts`
- `src/resources/extensions/gsd/commands-mcp-status.ts:35` — `formatMcpInitResult(status: "created" | "updated" | "unchanged", configPath: string, targetPath: string)` — exported: **yes** — Implements logic starting with: { const summary = status === "created" ? "Created project MCP config." : status === "updated" ? "Updated project MCP config." : "Project MCP config… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-mcp-status.ts:118` — `formatMcpStatusReport(servers: McpServerStatus[])` — exported: **yes** — Implements logic starting with: { if (servers.length === 0) { return [ "No MCP servers configured.", "", "Add servers to .mcp.json, .gsd/mcp.json, or $GSD_HOME/mcp.json (default:… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-mcp-status.ts:148` — `formatMcpServerDetail(server: McpServerDetail)` — exported: **yes** — Implements logic starting with: { const lines: string[] = [`MCP Server: ${server.name}\n`]; lines.push(` Transport: ${server.transport}`); if (server.error) { lines.push(` Status:… (missing: jsdoc)

### `src/resources/extensions/gsd/commands-memory.ts`
- `src/resources/extensions/gsd/commands-memory.ts:100` — `handleMemory(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-pr-branch.ts`
- `src/resources/extensions/gsd/commands-pr-branch.ts:125` — `handlePrBranch(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-prefs-wizard.ts`
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:216` — `handlePrefs(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:274` — `handleImportClaude(ctx: ExtensionCommandContext, scope: "global" | "project")` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:301` — `handlePrefsMode(ctx: ExtensionCommandContext, scope: "global" | "project")` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:556` — `formatConfiguredModel(config: unknown)` — exported: **yes** — Implements logic starting with: { if (typeof config === "string") return config; if (!config || typeof config !== "object") return "(invalid)"; const maybeConfig = config as { mod… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:567` — `toPersistedModelId(provider: string, modelId: string)` — exported: **yes** — Implements logic starting with: { if (!provider.trim()) return modelId; const normalizedProvider = provider.trim(); const normalizedModelId = modelId.trim(); return normalizedMode… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1480` — `configureMode(ctx: ExtensionCommandContext, prefs: Record<string, unknown>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1551` — `handlePrefsWizard(ctx: ExtensionCommandContext, scope: "global" | "project", prefill?: Record<string, unknown>, opts?: { pathOverride?: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1670` — `serializePreferencesToFrontmatter(prefs: Record<string, unknown>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1765` — `ensurePreferencesFile(path: string, ctx: ExtensionCommandContext, scope: "global" | "project")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-rate.ts`
- `src/resources/extensions/gsd/commands-rate.ts:13` — `handleRate(args: string, ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-scan.ts`
- `src/resources/extensions/gsd/commands-scan.ts:85` — `handleScan(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-session-report.ts`
- `src/resources/extensions/gsd/commands-session-report.ts:58` — `handleSessionReport(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-ship.ts`
- `src/resources/extensions/gsd/commands-ship.ts:194` — `handleShip(args: string, ctx: ExtensionCommandContext, _pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-workflow-templates.ts`
- `src/resources/extensions/gsd/commands-workflow-templates.ts:170` — `handleStart(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:526` — `handleTemplates(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands-worktree.ts`
- `src/resources/extensions/gsd/commands-worktree.ts:86` — `formatWorktreeList(statuses: WorktreeStatus[])` — exported: **yes** — Implements logic starting with: { if (statuses.length === 0) { return "No worktrees.\n\nCreate one from the CLI: gsd -w <name>"; } const lines: string[] = [`Worktrees — ${statuses… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-worktree.ts:115` — `formatCleanKeepReason(status: WorktreeStatus)` — exported: **yes** — Implements logic starting with: { if (!status.exists) { return "directory missing — run 'git worktree prune' to unregister"; } if (status.filesChanged > 0) { return `${status.file… (missing: jsdoc)
- `src/resources/extensions/gsd/commands-worktree.ts:345` — `handleWorktree(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands.ts`
- `src/resources/extensions/gsd/commands.ts:3` — `handleGSDCommand(...args: Parameters<typeof import("./commands/dispatcher.js").handleGSDCommand>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands.ts:10` — `fireStatusViaCommand(...args: Parameters<typeof import("./commands/handlers/core.js").fireStatusViaCommand>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/catalog.ts`
- `src/resources/extensions/gsd/commands/catalog.ts:430` — `getGsdArgumentCompletions(prefix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/context.ts`
- `src/resources/extensions/gsd/commands/context.ts:21` — `GSDNoProjectError.constructor(reason: string)` — exported: **yes** — Implements logic starting with: { super(reason); this.name = "GSDNoProjectError"; } (missing: jsdoc)
- `src/resources/extensions/gsd/commands/context.ts:29` — `withCommandCwd(cwd: string | undefined, fn: () => Promise<T>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/context.ts:39` — `projectRoot()` — exported: **yes** — Implements logic starting with: { let cwd: string; if (commandCwdOverride) { cwd = commandCwdOverride; } else { try { cwd = process.cwd(); } catch { // cwd directory was deleted (… (missing: jsdoc)
- `src/resources/extensions/gsd/commands/context.ts:60` — `currentDirectoryRoot()` — exported: **yes** — Implements logic starting with: { let cwd: string; if (commandCwdOverride) { cwd = commandCwdOverride; } else { try { cwd = process.cwd(); } catch { cwd = homedir(); } } const res… (missing: jsdoc)
- `src/resources/extensions/gsd/commands/context.ts:78` — `guardRemoteSession(ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/dispatcher.ts`
- `src/resources/extensions/gsd/commands/dispatcher.ts:10` — `handleGSDCommand(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/auto.ts`
- `src/resources/extensions/gsd/commands/handlers/auto.ts:45` — `handleAutoCommand(trimmed: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/core.ts`
- `src/resources/extensions/gsd/commands/handlers/core.ts:17` — `showHelp(ctx: ExtensionCommandContext, args = "")` — exported: **yes** — Implements logic starting with: { const summaryLines = [ "GSD — Get Shit Done\n", "QUICK START", " /gsd start <tpl> Start a workflow template", " /gsd Run next unit (same as /gsd… (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:150` — `handleStatus(ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:181` — `fireStatusViaCommand(ctx: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:185` — `handleVisualize(ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:210` — `handleBrief(args: string, ctx: ExtensionCommandContext, pi?: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:226` — `handleSetup(args: string, ctx: ExtensionCommandContext, pi?: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:435` — `handleCoreCommand(trimmed: string, ctx: ExtensionCommandContext, pi?: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/core.ts:522` — `formatTextStatus(state: GSDState)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/escalate.ts`
- `src/resources/extensions/gsd/commands/handlers/escalate.ts:51` — `handleEscalateCommand(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/notifications-handler.ts`
- `src/resources/extensions/gsd/commands/handlers/notifications-handler.ts:37` — `handleNotificationsCommand(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/onboarding.ts`
- `src/resources/extensions/gsd/commands/handlers/onboarding.ts:166` — `handleOnboarding(rawArgs: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/ops.ts`
- `src/resources/extensions/gsd/commands/handlers/ops.ts:22` — `handleOpsCommand(trimmed: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/parallel.ts`
- `src/resources/extensions/gsd/commands/handlers/parallel.ts:23` — `handleParallelCommand(trimmed: string, _ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/commands/handlers/workflow.ts`
- `src/resources/extensions/gsd/commands/handlers/workflow.ts:136` — `parseWorkflowRunArgs(args: string)` — exported: **yes** — Implements logic starting with: { const parts = splitWorkflowRunArgs(args); const defName = parts[0] ?? ""; const overrides: Record<string, string> = {}; for (let i = 1; i < parts… (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/workflow.ts:493` — `handleWorkflowCommand(trimmed: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/commands/handlers/workflow.ts:626` — `getNextMilestoneId(basePath: string)` — exported: **yes** — Implements logic starting with: { const milestoneIds = findMilestoneIds(basePath); const uniqueIds = !!loadEffectiveGSDPreferences()?.preferences?.unique_milestone_ids; return nex… (missing: jsdoc)

### `src/resources/extensions/gsd/commands/index.ts`
- `src/resources/extensions/gsd/commands/index.ts:5` — `registerGSDCommand(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/compaction-snapshot.ts`
- `src/resources/extensions/gsd/compaction-snapshot.ts:113` — `writeCompactionSnapshot(baseDir: string, opts: WriteSnapshotOptions = {})` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/compaction-snapshot.ts:141` — `readCompactionSnapshot(baseDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/config-overlay.ts`
- `src/resources/extensions/gsd/config-overlay.ts:203` — `formatConfigText()` — exported: **yes** — Implements logic starting with: { const sections = collectConfigSections(); const lines: string[] = ["GSD Configuration\n"]; let maxLabel = 0; for (const section of sections) { fo… (missing: jsdoc)
- `src/resources/extensions/gsd/config-overlay.ts:228` — `class GSDConfigOverlay` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/config-overlay.ts:237` — `GSDConfigOverlay.constructor(tui: { requestRender: () => void }, theme: Theme, onClose: () => void)` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.onClose = onClose; this.sections = collectConfigSections(); } (missing: jsdoc)
- `src/resources/extensions/gsd/config-overlay.ts:248` — `GSDConfigOverlay.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedLines = undefined; } (missing: jsdoc)
- `src/resources/extensions/gsd/config-overlay.ts:252` — `GSDConfigOverlay.dispose()` — exported: **yes** — Implements logic starting with: { this.disposed = true; } (missing: jsdoc)
- `src/resources/extensions/gsd/config-overlay.ts:256` — `GSDConfigOverlay.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if (matchesKey(data, Key.escape) || data === "q") { this.dispose(); this.onClose(); return; } if (matchesKey(data, Key.down) || data === "j") { t… (missing: jsdoc)
- `src/resources/extensions/gsd/config-overlay.ts:288` — `GSDConfigOverlay.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.cachedLines) return this.cachedLines; const t = this.theme; const w = Math.max(width, 50); const allLines: string[] = []; // Header allL… (missing: jsdoc)

### `src/resources/extensions/gsd/context-masker.ts`
- `src/resources/extensions/gsd/context-masker.ts:60` — `createObservationMask(keepRecentTurns: number = 8)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/crash-recovery.ts`
- `src/resources/extensions/gsd/crash-recovery.ts:297` — `emitOpenUnitEndForUnit(basePath: string, unitType: string, unitId: string, status: string, errorContext?: { message: string; category: string; stopReason?: string; isTransient?: boolean; retryAfterMs?: number })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/custom-execution-policy.ts`
- `src/resources/extensions/gsd/custom-execution-policy.ts:19` — `class CustomExecutionPolicy` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/custom-execution-policy.ts:22` — `CustomExecutionPolicy.constructor(runDir: string)` — exported: **yes** — Implements logic starting with: { this.runDir = runDir; } (missing: jsdoc)

### `src/resources/extensions/gsd/custom-workflow-engine.ts`
- `src/resources/extensions/gsd/custom-workflow-engine.ts:62` — `class CustomWorkflowEngine` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/custom-workflow-engine.ts:66` — `CustomWorkflowEngine.constructor(runDir: string)` — exported: **yes** — Implements logic starting with: { this.runDir = runDir; } (missing: jsdoc)

### `src/resources/extensions/gsd/dashboard-overlay.ts`
- `src/resources/extensions/gsd/dashboard-overlay.ts:32` — `unitLabel(type: string)` — exported: **yes** — Implements logic starting with: { switch (type) { case "discuss-milestone": case "discuss-slice": return "Discuss"; case "research-milestone": return "Research"; case "plan-milest… (missing: jsdoc)
- `src/resources/extensions/gsd/dashboard-overlay.ts:52` — `class GSDDashboardOverlay` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/dashboard-overlay.ts:68` — `GSDDashboardOverlay.constructor(tui: { requestRender: () => void }, theme: Theme, onClose: () => void)` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.onClose = onClose; this.dashData = getAutoDashboardData(); // Invalidate cache on terminal resize this.r… (missing: jsdoc)
- `src/resources/extensions/gsd/dashboard-overlay.ts:207` — `GSDDashboardOverlay.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if ( matchesKey(data, Key.escape) || matchesKey(data, Key.ctrl("c")) || matchesKey(data, Key.ctrlAlt("g")) || matchesKey(data, Key.ctrlShift("g")… (missing: jsdoc)
- `src/resources/extensions/gsd/dashboard-overlay.ts:248` — `GSDDashboardOverlay.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.cachedLines && this.cachedWidth === width) { return this.cachedLines; } const content = this.buildContentLines(width); const viewportHei… (missing: jsdoc)
- `src/resources/extensions/gsd/dashboard-overlay.ts:623` — `GSDDashboardOverlay.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedWidth = undefined; this.cachedLines = undefined; } (missing: jsdoc)
- `src/resources/extensions/gsd/dashboard-overlay.ts:628` — `GSDDashboardOverlay.dispose()` — exported: **yes** — Implements logic starting with: { this.disposed = true; clearInterval(this.refreshTimer); if (this.resizeHandler) { process.stdout.removeListener("resize", this.resizeHandler); th… (missing: jsdoc)

### `src/resources/extensions/gsd/db-adapter.ts`
- `src/resources/extensions/gsd/db-adapter.ts:16` — `normalizeDbRow(row: unknown)` — exported: **yes** — Implements logic starting with: { if (row == null) return undefined; if (Object.getPrototypeOf(row) === null) { return { ...(row as Record<string, unknown>) }; } return row as Rec… (missing: jsdoc)
- `src/resources/extensions/gsd/db-adapter.ts:24` — `normalizeDbRows(rows: unknown[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/db-adapter.ts:28` — `createDbAdapter(rawDb: unknown)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/gsd/db-base-schema.ts`
- `src/resources/extensions/gsd/db-base-schema.ts:11` — `createBaseSchemaObjects(db: DbAdapter, hooks: BaseSchemaHooks)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/gsd/db-connection-cache.ts`
- `src/resources/extensions/gsd/db-connection-cache.ts:11` — `class DbConnectionCache` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:14` — `DbConnectionCache.get(key: string)` — exported: **yes** — Implements logic starting with: { return this.entries.get(key); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:18` — `DbConnectionCache.set(key: string, entry: DbConnectionCacheEntry)` — exported: **yes** — Implements logic starting with: { this.entries.set(key, entry); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:22` — `DbConnectionCache.has(key: string)` — exported: **yes** — Implements logic starting with: { return this.entries.has(key); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:26` — `DbConnectionCache.delete(key: string)` — exported: **yes** — Implements logic starting with: { return this.entries.delete(key); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:30` — `DbConnectionCache.asReadonlyMap()` — exported: **yes** — Implements logic starting with: { return this.entries; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:34` — `DbConnectionCache.closeNonActive(activeDb: DbAdapter | null, closeEntry: (entry: DbConnectionCacheEntry) => void)` — exported: **yes** — Implements logic starting with: { for (const [key, entry] of this.entries) { if (entry.db === activeDb) continue; this.entries.delete(key); closeEntry(entry); } } (missing: jsdoc)
- `src/resources/extensions/gsd/db-connection-cache.ts:43` — `createDbConnectionCache()` — exported: **yes** — Implements logic starting with: { return new DbConnectionCache(); } (missing: jsdoc)

### `src/resources/extensions/gsd/db-decision-requirement-rows.ts`
- `src/resources/extensions/gsd/db-decision-requirement-rows.ts:8` — `rowToDecision(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { seq: row["seq"] as number, id: row["id"] as string, when_context: row["when_context"] as string, scope: row["scope"] as string, decision… (missing: jsdoc)
- `src/resources/extensions/gsd/db-decision-requirement-rows.ts:24` — `rowToActiveDecision(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { ...rowToDecision(row), superseded_by: null, }; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-decision-requirement-rows.ts:31` — `rowToRequirement(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { id: row["id"] as string, class: row["class"] as string, status: row["status"] as string, description: row["description"] as string, why:… (missing: jsdoc)
- `src/resources/extensions/gsd/db-decision-requirement-rows.ts:48` — `rowToActiveRequirement(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { ...rowToRequirement(row), superseded_by: null, }; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-decision-requirement-rows.ts:55` — `rowsToRequirementCounts(rows: DbRow[])` — exported: **yes** — Implements logic starting with: { const counts: RequirementCounts = { active: 0, validated: 0, deferred: 0, outOfScope: 0, blocked: 0, total: 0, }; for (const row of rows) { const… (missing: jsdoc)

### `src/resources/extensions/gsd/db-gate-rows.ts`
- `src/resources/extensions/gsd/db-gate-rows.ts:6` — `rowToGate(row: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { return { milestone_id: row["milestone_id"] as string, slice_id: row["slice_id"] as string, gate_id: row["gate_id"] as GateId, scope: row["scope"]… (missing: jsdoc)

### `src/resources/extensions/gsd/db-lightweight-query-rows.ts`
- `src/resources/extensions/gsd/db-lightweight-query-rows.ts:21` — `rowToIdStatusSummary(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { id: row["id"] as string, status: row["status"] as string, }; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-lightweight-query-rows.ts:28` — `rowToActiveTaskSummary(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { ...rowToIdStatusSummary(row), title: row["title"] as string, }; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-lightweight-query-rows.ts:35` — `rowToTaskStatusCounts(row: DbRow | undefined)` — exported: **yes** — Implements logic starting with: { if (!row) return emptyTaskStatusCounts(); return { total: (row["total"] as number) ?? 0, done: (row["done"] as number) ?? 0, pending: (row["pendi… (missing: jsdoc)
- `src/resources/extensions/gsd/db-lightweight-query-rows.ts:44` — `emptyTaskStatusCounts()` — exported: **yes** — Implements logic starting with: { return { total: 0, done: 0, pending: 0 }; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-lightweight-query-rows.ts:48` — `rowsToStringColumn(rows: DbRow[], column: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/db-memory-fts-schema.ts`
- `src/resources/extensions/gsd/db-memory-fts-schema.ts:57` — `isMemoriesFtsAvailableSchema(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { try { const row = db .prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='memories_fts'") .get(); return !!row; } catch { return… (missing: jsdoc)

### `src/resources/extensions/gsd/db-migration-backup.ts`
- `src/resources/extensions/gsd/db-migration-backup.ts:12` — `backupDatabaseBeforeMigration(db: DbAdapter, dbPath: string | null, currentVersion: number, deps: MigrationBackupDeps)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/gsd/db-migration-steps.ts`
- `src/resources/extensions/gsd/db-migration-steps.ts:7` — `applyMigrationV2Artifacts(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:21` — `applyMigrationV3Memories(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:49` — `applyMigrationV4DecisionMadeBy(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:55` — `applyMigrationV5HierarchyTables(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:116` — `applyMigrationV6SliceSummaries(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "slices", "full_summary_md", "ALTER TABLE slices ADD COLUMN full_summary_md TEXT NOT NULL DEFAULT ''"); ensureColumn(db, "slices… (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:121` — `applyMigrationV7Dependencies(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "slices", "depends", "ALTER TABLE slices ADD COLUMN depends TEXT NOT NULL DEFAULT '[]'"); ensureColumn(db, "slices", "demo", "AL… (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:127` — `applyMigrationV8PlanningFields(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:183` — `applyMigrationV9Ordering(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "slices", "sequence", "ALTER TABLE slices ADD COLUMN sequence INTEGER DEFAULT 0"); ensureColumn(db, "tasks", "sequence", "ALTER… (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:188` — `applyMigrationV10ReplanTrigger(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "slices", "replan_triggered_at", "ALTER TABLE slices ADD COLUMN replan_triggered_at TEXT DEFAULT NULL"); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:192` — `applyMigrationV11TaskPlanning(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:201` — `applyMigrationV12QualityGates(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:220` — `applyMigrationV13HotPathIndexes(db: DbAdapter, ensureVerificationEvidenceDedupIndex: (db: DbAdapter) => void)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:232` — `applyMigrationV14SliceDependencies(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:246` — `applyMigrationV15AuditTables(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:314` — `applyMigrationV16EscalationSource(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "slices", "is_sketch", "ALTER TABLE slices ADD COLUMN is_sketch INTEGER NOT NULL DEFAULT 0"); ensureColumn(db, "slices", "sketch… (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:320` — `applyMigrationV17TaskEscalation(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:329` — `applyMigrationV18MemorySources(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:359` — `applyMigrationV19MemoryFts(db: DbAdapter, hooks: MigrationV19Hooks)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:379` — `applyMigrationV20MemoryRelations(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:394` — `applyMigrationV21StructuredMemories(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "memories", "structured_fields", "ALTER TABLE memories ADD COLUMN structured_fields TEXT DEFAULT NULL"); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:398` — `applyMigrationV23MilestoneQueue(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "milestones", "sequence", "ALTER TABLE milestones ADD COLUMN sequence INTEGER DEFAULT 0"); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:402` — `applyMigrationV26MilestoneCommitAttributions(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:419` — `applyMigrationV27ArtifactHash(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "artifacts", "content_hash", "ALTER TABLE artifacts ADD COLUMN content_hash TEXT DEFAULT NULL"); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:423` — `applyMigrationV28MemoryLastHitAt(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { ensureColumn(db, "memories", "last_hit_at", "ALTER TABLE memories ADD COLUMN last_hit_at TEXT DEFAULT NULL"); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-migration-steps.ts:431` — `applyMigrationV22QualityGateRepair(db: DbAdapter, hooks: MigrationV22Hooks)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/gsd/db-milestone-artifact-rows.ts`
- `src/resources/extensions/gsd/db-milestone-artifact-rows.ts:37` — `rowToMilestone(row: DbRow)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `src/resources/extensions/gsd/db-milestone-artifact-rows.ts:60` — `rowToArtifact(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { path: row["path"] as string, artifact_type: row["artifact_type"] as string, milestone_id: (row["milestone_id"] as string) ?? null, slice… (missing: jsdoc)

### `src/resources/extensions/gsd/db-open-state.ts`
- `src/resources/extensions/gsd/db-open-state.ts:12` — `class DbOpenState` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/db-open-state.ts:17` — `DbOpenState.markAttempted()` — exported: **yes** — Implements logic starting with: { this.attempted = true; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-open-state.ts:21` — `DbOpenState.clearError()` — exported: **yes** — Implements logic starting with: { this.lastError = null; this.lastPhase = null; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-open-state.ts:26` — `DbOpenState.recordError(phase: DbOpenPhase, error: unknown)` — exported: **yes** — Implements logic starting with: { this.lastPhase = phase; this.lastError = error instanceof Error ? error : new Error(String(error)); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-open-state.ts:31` — `DbOpenState.reset()` — exported: **yes** — Implements logic starting with: { this.attempted = false; this.clearError(); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-open-state.ts:36` — `DbOpenState.snapshot()` — exported: **yes** — Implements logic starting with: { return { attempted: this.attempted, lastError: this.lastError, lastPhase: this.lastPhase, }; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-open-state.ts:45` — `createDbOpenState()` — exported: **yes** — Implements logic starting with: { return new DbOpenState(); } (missing: jsdoc)

### `src/resources/extensions/gsd/db-provider.ts`
- `src/resources/extensions/gsd/db-provider.ts:29` — `suppressSqliteWarning()` — exported: **yes** — Implements logic starting with: { const origEmit = process.emit; // Override via loose cast: Node's overloaded emit signature is not directly assignable. (process as any).emit = f… (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:49` — `class SqliteProviderLoader` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:55` — `SqliteProviderLoader.constructor(deps: SqliteProviderDeps)` — exported: **yes** — Implements logic starting with: { this.deps = deps; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:59` — `SqliteProviderLoader.load()` — exported: **yes** — Implements logic starting with: { if (this.loadAttempted) return; this.loadAttempted = true; try { this.deps.suppressSqliteWarning(); const mod = this.deps.requireModule("node:sql… (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:91` — `SqliteProviderLoader.getProviderName()` — exported: **yes** — Implements logic starting with: { return this.providerName; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:95` — `SqliteProviderLoader.openRaw(path: string)` — exported: **yes** — Implements logic starting with: { this.load(); if (!this.providerModule || !this.providerName) return null; if (this.providerName === "node:sqlite") { const { DatabaseSync } = thi… (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:110` — `SqliteProviderLoader.tryOpenBetterSqliteFallback(path: string)` — exported: **yes** — Implements logic starting with: { if (this.providerName !== "node:sqlite") return null; const Database = this.loadBetterSqliteModule(); if (!Database) return null; return { provid… (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:123` — `SqliteProviderLoader.commitFallback(fallback: SqliteFallbackOpen)` — exported: **yes** — Implements logic starting with: { this.providerName = fallback.providerName; this.providerModule = fallback.providerModule; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:128` — `SqliteProviderLoader.reset()` — exported: **yes** — Implements logic starting with: { this.loadAttempted = false; this.providerModule = null; this.providerName = null; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-provider.ts:146` — `createSqliteProviderLoader(deps: SqliteProviderDeps)` — exported: **yes** — Implements logic starting with: { return new SqliteProviderLoader(deps); } (missing: jsdoc)

### `src/resources/extensions/gsd/db-schema-metadata.ts`
- `src/resources/extensions/gsd/db-schema-metadata.ts:6` — `indexExists(db: DbAdapter, name: string)` — exported: **yes** — Implements logic starting with: { return !!db.prepare( "SELECT 1 as present FROM sqlite_master WHERE type = 'index' AND name = ?", ).get(name); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-schema-metadata.ts:12` — `columnExists(db: DbAdapter, table: string, column: string)` — exported: **yes** — Implements logic starting with: { const rows = db.prepare(`PRAGMA table_info(${table})`).all(); return rows.some((row) => row["name"] === column); } (missing: jsdoc)
- `src/resources/extensions/gsd/db-schema-metadata.ts:17` — `ensureColumn(db: DbAdapter, table: string, column: string, ddl: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-schema-metadata.ts:21` — `getCurrentSchemaVersion(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { const row = db.prepare("SELECT MAX(version) as v FROM schema_version").get(); return row ? (row["v"] as number) : 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-schema-metadata.ts:26` — `recordSchemaVersion(db: DbAdapter, version: number)` — exported: **yes** — Implements logic starting with: { db.prepare( "INSERT INTO schema_version (version, applied_at) VALUES (:version, :applied_at)", ).run({ ":version": version, ":applied_at": new Da… (missing: jsdoc)

### `src/resources/extensions/gsd/db-task-slice-rows.ts`
- `src/resources/extensions/gsd/db-task-slice-rows.ts:62` — `parseTaskArrayColumn(raw: unknown)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `src/resources/extensions/gsd/db-task-slice-rows.ts:88` — `rowToSlice(row: DbRow)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `src/resources/extensions/gsd/db-task-slice-rows.ts:113` — `rowToTask(row: DbRow)` — exported: **yes** — Implements logic starting with: { return { milestone_id: row["milestone_id"] as string, slice_id: row["slice_id"] as string, id: row["id"] as string, title: row["title"] as string… (missing: jsdoc)

### `src/resources/extensions/gsd/db-transaction.ts`
- `src/resources/extensions/gsd/db-transaction.ts:11` — `class DbTransactionRunner` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/db-transaction.ts:14` — `DbTransactionRunner.isInTransaction()` — exported: **yes** — Implements logic starting with: { return this.depth > 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/db-transaction.ts:18` — `DbTransactionRunner.transaction(controls: DbTransactionControls, fn: () => T)` — exported: **yes** — Implements logic starting with: { if (this.depth > 0) { return this.runNested(fn); } controls.begin(); this.depth++; try { const result = fn(); controls.commit(); return result; }… (missing: jsdoc)
- `src/resources/extensions/gsd/db-transaction.ts:37` — `DbTransactionRunner.readTransaction(controls: DbTransactionControls, fn: () => T, logRollbackError: (error: Error) => void)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/db-transaction.ts:74` — `createDbTransactionRunner()` — exported: **yes** — Implements logic starting with: { return new DbTransactionRunner(); } (missing: jsdoc)

### `src/resources/extensions/gsd/db-verification-evidence-schema.ts`
- `src/resources/extensions/gsd/db-verification-evidence-schema.ts:7` — `dedupeVerificationEvidenceRows(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/db-verification-evidence-schema.ts:18` — `ensureVerificationEvidenceDedupIndex(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/resources/extensions/gsd/db/unit-dispatches.ts`
- `src/resources/extensions/gsd/db/unit-dispatches.ts:520` — `getRecentUnitKeysForProjectRoot(projectRootRealpath: string, limit = 20)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/debug-session-store.ts`
- `src/resources/extensions/gsd/debug-session-store.ts:99` — `debugSessionsDir(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(debugRoot(basePath), "sessions"); } (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:103` — `debugSessionArtifactPath(basePath: string, slug: string)` — exported: **yes** — Implements logic starting with: { assertValidDebugSessionSlug(slug); return join(debugSessionsDir(basePath), `${slug}${SESSION_FILE_SUFFIX}`); } (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:108` — `debugSessionLogPath(basePath: string, slug: string)` — exported: **yes** — Implements logic starting with: { assertValidDebugSessionSlug(slug); return join(debugRoot(basePath), `${slug}.log`); } (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:119` — `slugifyDebugSessionIssue(issue: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:135` — `assertValidDebugSessionSlug(slug: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:244` — `createDebugSession(basePath: string, input: CreateDebugSessionInput, deps: DebugSessionStoreDeps = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:279` — `loadDebugSession(basePath: string, slug: string, deps: DebugSessionStoreDeps = {})` — exported: **yes** — Implements logic starting with: { assertValidDebugSessionSlug(slug); const d = defaultDeps(deps); const artifactPath = debugSessionArtifactPath(basePath, slug); if (!d.exists(arti… (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:295` — `listDebugSessions(basePath: string, deps: DebugSessionStoreDeps = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/debug-session-store.ts:337` — `updateDebugSession(basePath: string, slug: string, update: UpdateDebugSessionInput, deps: DebugSessionStoreDeps = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/gsd/deep-project-setup-policy.ts`
- `src/resources/extensions/gsd/deep-project-setup-policy.ts:44` — `researchDecisionPath(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(runtimeDir(basePath), "research-decision.json"); } (missing: jsdoc)
- `src/resources/extensions/gsd/deep-project-setup-policy.ts:48` — `isWorkflowPrefsCaptured(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/deep-project-setup-policy.ts:62` — `writeDefaultResearchSkipDecision(basePath: string, reason = "deterministic-default", previousSource?: string)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/deep-project-setup-policy.ts:128` — `resolveDeepProjectSetupState(prefs: GSDPreferences | undefined, basePath: string)` — exported: **yes** — Implements logic starting with: { if (prefs?.planning_depth !== "deep") { return { status: "not-applicable", stage: null, reason: "Deep planning mode is not enabled.", }; } const… (missing: jsdoc)

### `src/resources/extensions/gsd/delegation-policy.ts`
- `src/resources/extensions/gsd/delegation-policy.ts:140` — `getDelegationVerdict(toolName: string)` — exported: **yes** — Implements logic starting with: { return POLICY[resolveCanonical(toolName)] ?? null; } (missing: jsdoc)
- `src/resources/extensions/gsd/delegation-policy.ts:144` — `isBackgroundable(toolName: string)` — exported: **yes** — Implements logic starting with: { const entry = getDelegationVerdict(toolName); return entry?.verdict === "good"; } (missing: jsdoc)
- `src/resources/extensions/gsd/delegation-policy.ts:149` — `listBackgroundableTools()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/delegation-policy.ts:156` — `getVerdictByUnitType(unitType: string)` — exported: **yes** — Implements logic starting with: { for (const entry of Object.values(POLICY)) { if (entry.unitType === unitType) return entry; } return null; } (missing: jsdoc)

### `src/resources/extensions/gsd/detection.ts`
- `src/resources/extensions/gsd/detection.ts:1308` — `scanProjectFiles(basePath: string)` — exported: **yes** — Implements logic starting with: { const files: string[] = []; const queue: Array<{ path: string; depth: number }> = [{ path: basePath, depth: 0 }]; while (queue.length > 0 && file… (missing: jsdoc)

### `src/resources/extensions/gsd/dev-execution-policy.ts`
- `src/resources/extensions/gsd/dev-execution-policy.ts:12` — `class DevExecutionPolicy` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-execution-policy.ts:13` — `DevExecutionPolicy.prepareWorkspace(_basePath: string, _milestoneId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-execution-policy.ts:20` — `DevExecutionPolicy.selectModel(_unitType: string, _unitId: string, _context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-execution-policy.ts:28` — `DevExecutionPolicy.verify(_unitType: string, _unitId: string, _context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-execution-policy.ts:36` — `DevExecutionPolicy.recover(_unitType: string, _unitId: string, _context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-execution-policy.ts:44` — `DevExecutionPolicy.closeout(_unitType: string, _unitId: string, _context: { basePath: string; startedAt: number })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/dev-workflow-engine.ts`
- `src/resources/extensions/gsd/dev-workflow-engine.ts:56` — `class DevWorkflowEngine` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-workflow-engine.ts:59` — `DevWorkflowEngine.deriveState(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-workflow-engine.ts:71` — `DevWorkflowEngine.resolveDispatch(state: EngineState, context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-workflow-engine.ts:93` — `DevWorkflowEngine.reconcile(state: EngineState, _completedStep: CompletedStep)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/dev-workflow-engine.ts:102` — `DevWorkflowEngine.getDisplayMetadata(state: EngineState)` — exported: **yes** — Implements logic starting with: { return { engineLabel: "GSD Dev", currentPhase: state.phase, progressSummary: `${state.currentMilestoneId ?? "no milestone"} / ${state.activeSlice… (missing: jsdoc)

### `src/resources/extensions/gsd/dispatch-guard.ts`
- `src/resources/extensions/gsd/dispatch-guard.ts:20` — `getPriorSliceCompletionBlocker(base: string, _mainBranch: string, unitType: string, unitId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/doctor-engine-checks.ts`
- `src/resources/extensions/gsd/doctor-engine-checks.ts:11` — `checkEngineHealth(basePath: string, issues: DoctorIssue[], fixesApplied: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/doctor-format.ts`
- `src/resources/extensions/gsd/doctor-format.ts:9` — `summarizeDoctorIssues(issues: DoctorIssue[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/doctor-format.ts:24` — `filterDoctorIssues(issues: DoctorIssue[], options?: { scope?: string; includeWarnings?: boolean; includeHistorical?: boolean })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/doctor-format.ts:31` — `formatDoctorReport(report: DoctorReport, options?: { scope?: string; includeWarnings?: boolean; maxIssues?: number; title?: string })` — exported: **yes** — Implements logic starting with: { const scopedIssues = filterDoctorIssues(report.issues, { scope: options?.scope, includeWarnings: options?.includeWarnings ?? true, }); const summ… (missing: jsdoc)
- `src/resources/extensions/gsd/doctor-format.ts:73` — `formatDoctorIssuesForPrompt(issues: DoctorIssue[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/doctor-git-checks.ts`
- `src/resources/extensions/gsd/doctor-git-checks.ts:93` — `checkGitHealth(basePath: string, issues: DoctorIssue[], fixesApplied: string[], shouldFix: (code: DoctorIssueCode) => boolean, isolationMode: "none" | "worktree" | "branch" = "none")` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/doctor-runtime-checks.ts`
- `src/resources/extensions/gsd/doctor-runtime-checks.ts:31` — `checkRuntimeHealth(basePath: string, issues: DoctorIssue[], fixesApplied: string[], shouldFix: (code: DoctorIssueCode) => boolean)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/doctor.ts`
- `src/resources/extensions/gsd/doctor.ts:208` — `selectDoctorScope(basePath: string, requestedScope?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/doctor.ts:332` — `runGSDDoctor(basePath: string, options?: { fix?: boolean; dryRun?: boolean; scope?: string; fixLevel?: "task" | "all"; isolationMode?: "none" | "worktree" | "branch"; includeBuild?: boolean; includeTests?: boolean })` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts`
- `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts:121` — `getSnapshotPhase()` — exported: **yes** — Implements logic starting with: { return _snapshot.phase; } (missing: jsdoc)
- `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts:125` — `getSnapshotActiveUnit()` — exported: **yes** — Implements logic starting with: { return _snapshot.activeUnit; } (missing: jsdoc)

### `src/resources/extensions/gsd/error-classifier.ts`
- `src/resources/extensions/gsd/error-classifier.ts:34` — `createRetryState()` — exported: **yes** — Implements logic starting with: { return { networkRetryCount: 0, consecutiveTransientCount: 0, currentRetryModelId: undefined }; } (missing: jsdoc)
- `src/resources/extensions/gsd/error-classifier.ts:38` — `resetRetryState(state: RetryState)` — exported: **yes** — Implements logic starting with: { state.networkRetryCount = 0; state.consecutiveTransientCount = 0; state.currentRetryModelId = undefined; } (missing: jsdoc)

### `src/resources/extensions/gsd/errors.ts`
- `src/resources/extensions/gsd/errors.ts:21` — `class GSDError` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/errors.ts:24` — `GSDError.constructor(code: string, message: string, options?: ErrorOptions)` — exported: **yes** — Implements logic starting with: { super(message, options); this.name = "GSDError"; this.code = code; } (missing: jsdoc)

### `src/resources/extensions/gsd/exec-history.ts`
- `src/resources/extensions/gsd/exec-history.ts:85` — `listExecHistory(baseDir: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/exec-history.ts:135` — `searchExecHistory(baseDir: string, opts: ExecSearchOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/exit-command.ts`
- `src/resources/extensions/gsd/exit-command.ts:5` — `registerExitCommand(pi: ExtensionAPI, deps: { stopAuto?: StopAutoFn } = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/export-html.ts`
- `src/resources/extensions/gsd/export-html.ts:42` — `generateHtmlReport(data: VisualizerData, opts: HtmlReportOptions)` — exported: **yes** — Implements logic starting with: { const generated = new Date().toISOString(); const sections = [ buildSummarySection(data, opts, generated), buildBlockersSection(data), buildProgr… (missing: jsdoc)

### `src/resources/extensions/gsd/file-lock.ts`
- `src/resources/extensions/gsd/file-lock.ts:76` — `withFileLockSync(filePath: string, fn: () => T, opts: FileLockOptions = {})` — exported: **yes** — Implements logic starting with: { const lockfile = _require("proper-lockfile"); if (!lockfile) return fn(); if (!existsSync(filePath)) return fn(); const retries = opts.retries ??… (missing: jsdoc)
- `src/resources/extensions/gsd/file-lock.ts:105` — `withFileLock(filePath: string, fn: () => Promise<T> | T, opts: FileLockOptions = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/files.ts`
- `src/resources/extensions/gsd/files.ts:172` — `parseSecretsManifest(content: string)` — exported: **yes** — Implements logic starting with: { const milestone = extractBoldField(content, 'Milestone') || ''; const generatedAt = extractBoldField(content, 'Generated') || ''; const h3Section… (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:207` — `formatSecretsManifest(manifest: SecretsManifest)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push('# Secrets Manifest'); lines.push(''); lines.push(`**Milestone:** ${manifest.milestone}`); lines.push(`**G… (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:268` — `parseTaskPlanFile(content: string)` — exported: **yes** — Implements logic starting with: { const [fmLines] = splitFrontmatter(content); const fm = fmLines ? parseFrontmatterMap(fmLines) : {}; return { frontmatter: normalizeTaskPlanFront… (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:278` — `parseSummary(content: string)` — exported: **yes** — Implements logic starting with: { return cachedParse(content, 'summary', _parseSummaryImpl); } (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:381` — `parseContinue(content: string)` — exported: **yes** — Implements logic starting with: { return cachedParse(content, 'continue', _parseContinueImpl); } (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:446` — `formatContinue(cont: Continue)` — exported: **yes** — Implements logic starting with: { const fm = cont.frontmatter; const fmData: Record<string, unknown> = { milestone: fm.milestone, slice: fm.slice, task: fm.task, step: fm.step, to… (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:502` — `parseRequirementCounts(content: string | null)` — exported: **yes** — Implements logic starting with: { const counts: RequirementCounts = { active: 0, validated: 0, deferred: 0, outOfScope: 0, blocked: 0, total: 0, }; if (!content) return counts; co… (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:798` — `appendOverride(basePath: string, change: string, appliedAt: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:828` — `appendKnowledge(basePath: string, type: "rule" | "pattern" | "lesson", entry: string, scope: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:950` — `loadActiveOverrides(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:957` — `parseOverrides(content: string)` — exported: **yes** — Implements logic starting with: { const overrides: Override[] = []; const blocks = content.split(/^## Override: /m).slice(1); for (const block of blocks) { const lines = block.spl… (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:985` — `formatOverridesSection(overrides: Override[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/files.ts:1003` — `resolveAllOverrides(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/forensics.ts`
- `src/resources/extensions/gsd/forensics.ts:187` — `handleForensics(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/forensics.ts:273` — `buildForensicReport(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/git-service.ts`
- `src/resources/extensions/gsd/git-service.ts:284` — `MergeConflictError.constructor(conflictedFiles: string[], strategy: "squash" | "merge", branch: string, mainBranch: string)` — exported: **yes** — Implements logic starting with: { super( GSD_MERGE_CONFLICT, `${strategy === "merge" ? "Merge" : "Squash-merge"} of "${branch}" into "${mainBranch}" ` + `failed with conflicts in… (missing: jsdoc)
- `src/resources/extensions/gsd/git-service.ts:380` — `writeIntegrationBranch(basePath: string, milestoneId: string, branch: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/git-service.ts:663` — `class GitServiceImpl` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/git-service.ts:670` — `GitServiceImpl.constructor(basePath: string, prefs: GitPreferences = {})` — exported: **yes** — Implements logic starting with: { this.basePath = basePath; this.prefs = prefs; } (missing: jsdoc)
- `src/resources/extensions/gsd/git-service.ts:1152` — `handleTurnGitActionError(action: TurnGitActionMode, err: unknown)` — exported: **yes** — Implements logic starting with: { if (isInfrastructureError(err)) { throw err; } return { action, status: "failed", error: getErrorMessage(err), }; } (missing: jsdoc)
- `src/resources/extensions/gsd/git-service.ts:1163` — `runTurnGitAction(args: { basePath: string; action: TurnGitActionMode; unitType: string; unitId: string; taskContext?: TaskCommitContext; })` — exported: **yes** — Implements logic starting with: { try { // Force fresh working-tree status per turn; nativeHasChanges caches briefly. _resetHasChangesCache(); if (args.action === "status-only") {… (missing: jsdoc)

### `src/resources/extensions/gsd/gsd-db.ts`
- `src/resources/extensions/gsd/gsd-db.ts:171` — `isMemoriesFtsAvailable(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { return isMemoriesFtsAvailableSchema(db); } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:544` — `getDbProvider()` — exported: **yes** — Implements logic starting with: { providerLoader.load(); return providerLoader.getProviderName(); } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:549` — `isDbAvailable()` — exported: **yes** — Implements logic starting with: { return currentDb !== null; } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:563` — `getDbStatus()` — exported: **yes** — Implements logic starting with: { providerLoader.load(); const openState = _dbOpenState.snapshot(); return { available: currentDb !== null, provider: providerLoader.getProviderNam… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:581` — `openDatabase(path: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:644` — `closeDatabase()` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:738` — `transaction(fn: () => T)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); return _transactionRunner.transaction(createTransactionControls(… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:763` — `insertDecision(d: Omit<Decision, "seq">)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT INTO decisions (id, when_context, sco… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:782` — `getDecisionById(id: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare("SELECT * FROM decisions WHERE id = ?").get(id); if (!row) return null; return rowToDe… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:789` — `getActiveDecisions()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:795` — `insertRequirement(r: Requirement)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT INTO requirements (id, class, status,… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:816` — `getRequirementById(id: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare("SELECT * FROM requirements WHERE id = ?").get(id); if (!row) return null; return rowT… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:823` — `getActiveRequirements()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:829` — `getRequirementCounts()` — exported: **yes** — Implements logic starting with: { if (!currentDb) { return { active: 0, validated: 0, deferred: 0, outOfScope: 0, blocked: 0, total: 0 }; } const rows = currentDb .prepare("SELECT… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:846` — `getDbOwnerPid()` — exported: **yes** — Implements logic starting with: { return currentPid; } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:850` — `getDbPath()` — exported: **yes** — Implements logic starting with: { return currentPath; } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:854` — `_getAdapter()` — exported: **yes** — Implements logic starting with: { return currentDb; } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:858` — `_resetProvider()` — exported: **yes** — Implements logic starting with: { providerLoader.reset(); } (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:862` — `upsertDecision(d: Omit<Decision, "seq">)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); // Use ON CONFLICT DO UPDATE instead of INSERT OR REPLACE to pre… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:894` — `upsertRequirement(r: Requirement)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR REPLACE INTO requirements (id, cla… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:915` — `clearArtifacts()` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:920` — `insertArtifact(a: { path: string; artifact_type: string; milestone_id: string | null; slice_id: string | null; task_id: string | null; full_content: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); const contentHash = createHash("sha256").update(a.full_content).… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:979` — `insertMilestone(m: { id: string; title?: string; status?: string; depends_on?: string[]; planning?: Partial<MilestonePlanningRecord>; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1021` — `upsertMilestonePlanning(milestoneId: string, planning: Partial<MilestonePlanningRecord> & { title?: string; status?: string })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1057` — `insertSlice(s: { id: string; milestoneId: string; title?: string; status?: string; risk?: string; depends?: string[]; demo?: string; sequence?: number; isSketch?: boolean; sketchScope?: string; planning?: Partial<SlicePlanningRecord>; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1131` — `setSliceSketchFlag(milestoneId: string, sliceId: string, isSketch: boolean)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE slices SET is_sketch = :is_sketch WHE… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1152` — `upsertSlicePlanning(milestoneId: string, sliceId: string, planning: Partial<SlicePlanningRecord>)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE slices SET goal = COALESCE(:goal, goa… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1173` — `insertTask(t: { id: string; sliceId: string; milestoneId: string; title?: string; status?: string; oneLiner?: string; narrative?: string; verificationResult?: string; duration?: string; blockerDiscovered?: boolean; deviations?: string; knownIssues?: string; keyFiles?: string[]; keyDecisions?: string[]; fullSummaryMd?: string; sequence?: number; planning?: Partial<TaskPlanningRecord>; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1255` — `updateTaskStatus(milestoneId: string, sliceId: string, taskId: string, status: string, completedAt?: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE tasks SET status = :status, completed… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1269` — `setTaskBlockerDiscovered(milestoneId: string, sliceId: string, taskId: string, discovered: boolean)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return; currentDb.prepare( `UPDATE tasks SET blocker_discovered = :discovered WHERE milestone_id = :mid AND slice_id = :sid AND i… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1276` — `upsertTaskPlanning(milestoneId: string, sliceId: string, taskId: string, planning: Partial<TaskPlanningRecord>)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1306` — `getSlice(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare("SELECT * FROM slices WHERE milestone_id = :mid AND id = :sid").get({ ":mid": mileston… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1313` — `updateSliceStatus(milestoneId: string, sliceId: string, status: string, completedAt?: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE slices SET status = :status, complete… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1326` — `setTaskSummaryMd(milestoneId: string, sliceId: string, taskId: string, md: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE tasks SET full_summary_md = :md WHERE… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1333` — `setSliceSummaryMd(milestoneId: string, sliceId: string, summaryMd: string, uatMd: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE slices SET full_summary_md = :summary… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1340` — `getTask(milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( "SELECT * FROM tasks WHERE milestone_id = :mid AND slice_id = :sid AND id = :tid", ).… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1349` — `getSliceTasks(milestoneId: string, sliceId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1357` — `getCompletedMilestoneTaskFileHints(milestoneId: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1515` — `insertVerificationEvidence(e: { taskId: string; sliceId: string; milestoneId: string; command: string; exitCode: number; verdict: string; durationMs: number; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR IGNORE INTO verification_evidence… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1552` — `getVerificationEvidence(milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return []; const rows = currentDb.prepare( "SELECT * FROM verification_evidence WHERE milestone_id = :mid AND slice_id = :sid AND… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1560` — `getAllMilestones()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1568` — `getMilestone(id: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare("SELECT * FROM milestones WHERE id = :id").get({ ":id": id }); if (!row) return null;… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1575` — `setMilestoneQueueOrder(order: string[])` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1603` — `getActiveMilestoneFromDb()` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( "SELECT * FROM milestones WHERE status NOT IN ('complete', 'parked') ORDER BY id LIMI… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1612` — `getActiveSliceFromDb(milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; // Single query: find the first non-complete slice whose dependencies are all satisfied. // Uses json_each() to expa… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1634` — `getActiveTaskFromDb(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( "SELECT * FROM tasks WHERE milestone_id = :mid AND slice_id = :sid AND status NOT IN… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1643` — `getMilestoneSlices(milestoneId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1649` — `getArtifact(path: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare("SELECT * FROM artifacts WHERE path = :path").get({ ":path": path }); if (!row) return… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1725` — `copyWorktreeDb(srcDbPath: string, destDbPath: string)` — exported: **yes** — Implements logic starting with: { try { if (!existsSync(srcDbPath)) return false; const destDir = dirname(destDbPath); mkdirSync(destDir, { recursive: true }); copyFileSync(srcDbP… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:1750` — `reconcileWorktreeDb(mainDbPath: string, worktreeDbPath: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2010` — `insertReplanHistory(entry: { milestoneId: string; sliceId?: string | null; taskId?: string | null; summary: string; previousArtifactPath?: string | null; replacementArtifactPath?: string | null; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); // INSERT OR REPLACE: idempotent on (milestone_id, slice_id, tas… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2035` — `insertAssessment(entry: { path: string; milestoneId: string; sliceId?: string | null; taskId?: string | null; status: string; scope: string; fullContent: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); // Idempotent: PRIMARY KEY is `path`, which is deterministic giv… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2063` — `deleteAssessmentByScope(milestoneId: string, scope: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `DELETE FROM assessments WHERE milestone_id =… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2070` — `deleteVerificationEvidence(milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `DELETE FROM verification_evidence WHERE mile… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2077` — `deleteTask(milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); transaction(() => { // Must delete verification_evidence first (… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2090` — `deleteSlice(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); transaction(() => { // Cascade-style manual deletion: evidence →… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2112` — `deleteMilestone(milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); transaction(() => { currentDb!.prepare( `DELETE FROM verificatio… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2154` — `updateSliceFields(milestoneId: string, sliceId: string, fields: { title?: string; risk?: string; depends?: string[]; demo?: string; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2178` — `getReplanHistory(milestoneId: string, sliceId?: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return []; if (sliceId) { return currentDb.prepare( `SELECT * FROM replan_history WHERE milestone_id = :mid AND slice_id = :sid O… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2190` — `getAssessment(path: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( `SELECT * FROM assessments WHERE path = :path`, ).get({ ":path": path }); return row… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2198` — `getLatestAssessmentByScope(milestoneId: string, scope: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( `SELECT * FROM assessments WHERE milestone_id = :mid AND scope = :scope ORDER BY crea… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2214` — `insertGateRow(g: { milestoneId: string; sliceId: string; gateId: GateId; scope: GateScope; taskId?: string | null; status?: GateStatus; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR IGNORE INTO quality_gates (milesto… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2236` — `saveGateResult(g: { milestoneId: string; sliceId: string; gateId: string; taskId?: string | null; verdict: GateVerdict; rationale: string; findings: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE quality_gates SET status = 'complete'… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2288` — `getPendingGates(milestoneId: string, sliceId: string, scope?: GateScope)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2298` — `getGateResults(milestoneId: string, sliceId: string, scope?: GateScope)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2308` — `markAllGatesOmitted(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return; currentDb.prepare( `UPDATE quality_gates SET status = 'complete', verdict = 'omitted', evaluated_at = :now WHERE mileston… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2320` — `getPendingSliceGateCount(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return 0; const row = currentDb.prepare( `SELECT COUNT(*) as cnt FROM quality_gates WHERE milestone_id = :mid AND slice_id = :sid… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2379` — `insertGateRun(entry: { traceId: string; turnId: string; gateId: string; gateType: string; unitType?: string; unitId?: string; milestoneId?: string; sliceId?: string; taskId?: string; outcome: "pass" | "fail" | "retry" | "manual-attention"; failureClass: "none" | "policy" | "input" | "execution" | "artifact" | "verification" | "closeout" | "git" | "timeout" | "manual-attention" | "unknown"; rationale?: string; findings?: string; attempt: number; maxAttempts: number; retryable: boolean; evaluatedAt: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) return; currentDb.prepare( `INSERT INTO gate_runs ( trace_id, turn_id, gate_id, gate_type, unit_type, unit_id, milestone_id, slic… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2428` — `upsertTurnGitTransaction(entry: { traceId: string; turnId: string; unitType?: string; unitId?: string; stage: string; action: "commit" | "snapshot" | "status-only"; push: boolean; status: "ok" | "failed"; error?: string; metadata?: Record<string, unknown>; updatedAt: string; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2463` — `getMilestoneCommitAttributionShas(milestoneId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2476` — `recordMilestoneCommitAttribution(entry: { commitSha: string; milestoneId: string; sliceId?: string; taskId?: string; source: "recorded" | "backfill"; confidence: number; files: string[]; createdAt: string; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2532` — `insertAuditEvent(entry: { eventId: string; traceId: string; turnId?: string; causedBy?: string; category: string; type: string; ts: string; payload: Record<string, unknown>; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2922` — `insertMemoryRow(args: { id: string; category: string; content: string; confidence: number; sourceUnitType: string | null; sourceUnitId: string | null; createdAt: string; updatedAt: string; scope?: string; tags?: string[]; /** * ADR-013 Step 2: optional structured payload preserved alongside the flat * `content` field. Used to retain gsd_save_decision-style fields (scope, * decision, choice, rationale, made_by, revisable) on architecture-category * memories so the cutover in Step 6 is lossless. Schema is intentionally * open inside the JSON; documented per category in ADR-013. */ structuredFields?: Record<string, unknown> | null; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2961` — `insertMemorySourceRow(args: { id: string; kind: string; uri: string | null; title: string | null; content: string; contentHash: string; importedAt: string; scope?: string; tags?: string[]; })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2989` — `deleteMemorySourceRow(id: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); const res = currentDb .prepare("DELETE FROM memory_sources WHERE… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:2997` — `upsertMemoryEmbedding(args: { memoryId: string; model: string; dim: number; vector: Uint8Array; updatedAt: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT INTO memory_embeddings (memory_id, mo… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3022` — `deleteMemoryEmbedding(memoryId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); const res = currentDb .prepare("DELETE FROM memory_embeddings WH… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3030` — `insertMemoryRelationRow(args: { fromId: string; toId: string; rel: string; confidence: number; createdAt: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR REPLACE INTO memory_relations (fro… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3050` — `deleteMemoryRelationsFor(memoryId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb .prepare("DELETE FROM memory_relations WHERE from_id =… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3057` — `rewriteMemoryId(placeholderId: string, realId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare("UPDATE memories SET id = :real_id WHERE id =… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3065` — `updateMemoryContentRow(id: string, content: string, confidence: number | undefined, updatedAt: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); if (confidence != null) { currentDb.prepare( "UPDATE memories SE… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3083` — `incrementMemoryHitCount(id: string, updatedAt: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( "UPDATE memories SET hit_count = hit_count +… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3090` — `supersedeMemoryRow(oldId: string, newId: string, updatedAt: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( "UPDATE memories SET superseded_by = :new_id,… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3097` — `markMemoryUnitProcessed(unitKey: string, activityFile: string, processedAt: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR IGNORE INTO memory_processed_units… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3109` — `decayMemoriesBefore(cutoffTs: string, now: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE memories SET confidence = MAX(0.1, co… (missing: jsdoc)
- `src/resources/extensions/gsd/gsd-db.ts:3118` — `supersedeLowestRankedMemories(limit: number, now: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE memories SET superseded_by = 'CAP_EXC… (missing: jsdoc)

### `src/resources/extensions/gsd/guided-flow-queue.ts`
- `src/resources/extensions/gsd/guided-flow-queue.ts:109` — `handleQueueReorder(ctx: ExtensionCommandContext, basePath: string, state: Awaited<ReturnType<typeof deriveState>>)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow-queue.ts:162` — `showQueueAdd(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, state: Awaited<ReturnType<typeof deriveState>>)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/guided-flow.ts`
- `src/resources/extensions/gsd/guided-flow.ts:75` — `shouldSkipGitBootstrapAfterInit(result: { gitEnabled?: boolean })` — exported: **yes** — Implements logic starting with: { return result.gitEnabled === false; } (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:215` — `_roadmapHasParseableSlicesForTest(roadmapContent: string | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!roadmapContent) return false; return parseRoadmapSlices(roadmapContent).length > 0; } (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:370` — `clearPendingDeepProjectSetup(basePath?: string)` — exported: **yes** — Implements logic starting with: { if (basePath) { pendingDeepProjectSetupMap.delete(basePath); } else { pendingDeepProjectSetupMap.clear(); } } (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:425` — `getPendingDeepProjectSetupUnitForContext(ctx: ExtensionContext | undefined, basePath?: string)` — exported: **yes** — Implements logic starting with: { const entry = _getPendingDeepProjectSetupForContext(ctx, basePath); if (!entry?.currentUnitType || !entry.currentUnitId) return null; return { un… (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:437` — `startDeepProjectSetupForeground(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, step?: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:455` — `checkDeepProjectSetupAfterTurn(_event: { messages: any[] }, ctx?: ExtensionContext, basePath?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:935` — `maybeHandleEmptyIntentTurn(event: { messages: any[] }, isAuto: boolean)` — exported: **yes** — Implements logic starting with: { // Gate: only fire when there is system-driven work in flight. Interactive // /gsd discuss (user-driven) produces legitimate text-only turns. if… (missing: jsdoc)
- `src/resources/extensions/gsd/guided-flow.ts:1986` — `showSmartEntry(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, options?: { step?: boolean })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/health-widget-core.ts`
- `src/resources/extensions/gsd/health-widget-core.ts:28` — `detectHealthWidgetProjectState(basePath: string)` — exported: **yes** — Implements logic starting with: { if (!existsSync(gsdRoot(basePath))) return "none"; const { state } = detectProjectState(basePath); return state === "v2-gsd" ? "active" : "initia… (missing: jsdoc)

### `src/resources/extensions/gsd/hook-emitter.ts`
- `src/resources/extensions/gsd/hook-emitter.ts:22` — `setHookEmitter(pi: ExtensionAPI)` — exported: **yes** — Implements logic starting with: { _pi = pi; } (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:26` — `clearHookEmitter()` — exported: **yes** — Implements logic starting with: { _pi = undefined; } (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:32` — `emitNotification(kind: "blocked" | "input_needed" | "milestone_ready" | "idle" | "error", message: string, details?: Record<string, unknown>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:43` — `emitBeforeCommit(args: { message: string; files: string[]; cwd: string; author?: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:56` — `emitCommit(args: { sha: string; message: string; files: string[]; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:66` — `emitBeforePush(args: { remote: string; branch: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:78` — `emitPush(args: { remote: string; branch: string; cwd: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:83` — `emitBeforePr(args: { branch: string; targetBranch: string; title: string; body: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:97` — `emitPrOpened(args: { url: string; branch: string; targetBranch: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:109` — `emitBeforeVerify(args: { unitType?: string; unitId?: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:121` — `emitVerifyResult(args: { passed: boolean; failures: VerifyFailure[]; unitType?: string; unitId?: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:134` — `emitBudgetThreshold(args: { fraction: number; spent: number; limit: number; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:151` — `emitMilestoneStart(args: { milestoneId: string; title?: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:160` — `emitMilestoneEnd(args: { milestoneId: string; status: "completed" | "failed" | "cancelled"; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:169` — `emitUnitStart(args: { unitType: string; unitId: string; milestoneId?: string; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/hook-emitter.ts:179` — `emitUnitEnd(args: { unitType: string; unitId: string; milestoneId?: string; status: "completed" | "failed" | "cancelled" | "blocked"; cwd: string; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/index.ts`
- `src/resources/extensions/gsd/index.ts:18` — `registerExtension(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/init-wizard.ts`
- `src/resources/extensions/gsd/init-wizard.ts:33` — `shouldWriteGitFiles(gitEnabled: boolean)` — exported: **yes** — Implements logic starting with: { return gitEnabled; } (missing: jsdoc)
- `src/resources/extensions/gsd/init-wizard.ts:676` — `detectMainBranch(basePath: string)` — exported: **yes** — Implements logic starting with: { try { // Match runtime branch resolution: origin/HEAD -> main -> master -> current. // Reading .git/HEAD first records whichever feature branch h… (missing: jsdoc)

### `src/resources/extensions/gsd/interrupted-session.ts`
- `src/resources/extensions/gsd/interrupted-session.ts:96` — `readPausedSessionMetadata(basePath: string)` — exported: **yes** — Implements logic starting with: { // basePath is unused now (the DB is workspace-scoped via the connection // openDatabase opened on it) but kept in the signature for callers. voi… (missing: jsdoc)
- `src/resources/extensions/gsd/interrupted-session.ts:111` — `isBootstrapCrashLock(lock: LockData | null)` — exported: **yes** — Implements logic starting with: { return !!( lock && lock.unitType === "starting" && lock.unitId === "bootstrap" ); } (missing: jsdoc)
- `src/resources/extensions/gsd/interrupted-session.ts:119` — `hasResumableDerivedState(state: GSDState | null)` — exported: **yes** — Implements logic starting with: { return !!(state?.activeMilestone && state.phase !== "complete"); } (missing: jsdoc)
- `src/resources/extensions/gsd/interrupted-session.ts:123` — `assessInterruptedSession(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/interrupted-session.ts:249` — `formatInterruptedSessionSummary(assessment: InterruptedSessionAssessment)` — exported: **yes** — Implements logic starting with: { if (assessment.lock) return [formatCrashInfo(assessment.lock)]; if (assessment.pausedSession?.milestoneId) { return [ `Paused auto-mode session d… (missing: jsdoc)
- `src/resources/extensions/gsd/interrupted-session.ts:263` — `formatInterruptedSessionRunningMessage(assessment: InterruptedSessionAssessment)` — exported: **yes** — Implements logic starting with: { const pid = assessment.lock?.pid; return pid ? `Another auto-mode session (PID ${pid}) appears to be running.\nStop it with \`kill ${pid}\` befor… (missing: jsdoc)

### `src/resources/extensions/gsd/legacy-telemetry.ts`
- `src/resources/extensions/gsd/legacy-telemetry.ts:50` — `incrementLegacyTelemetry(counter: LegacyTelemetryCounter, amount = 1)` — exported: **yes** — Implements logic starting with: { if (!Number.isFinite(amount) || amount <= 0) return; values[counter] += amount; emitLegacyDiagnostic(counter); persistLegacyTelemetry(); } (missing: jsdoc)
- `src/resources/extensions/gsd/legacy-telemetry.ts:57` — `getLegacyTelemetry()` — exported: **yes** — Implements logic starting with: { return { ...values }; } (missing: jsdoc)
- `src/resources/extensions/gsd/legacy-telemetry.ts:61` — `resetLegacyTelemetry()` — exported: **yes** — Implements logic starting with: { for (const counter of COUNTERS) { values[counter] = 0; } warnedCounters.clear(); } (missing: jsdoc)
- `src/resources/extensions/gsd/legacy-telemetry.ts:68` — `listLegacyTelemetryCounters()` — exported: **yes** — Implements logic starting with: { return [...COUNTERS]; } (missing: jsdoc)
- `src/resources/extensions/gsd/legacy-telemetry.ts:72` — `getLegacyTelemetryReport()` — exported: **yes** — Implements logic starting with: { return { ts: new Date().toISOString(), counters: getLegacyTelemetry(), }; } (missing: jsdoc)
- `src/resources/extensions/gsd/legacy-telemetry.ts:79` — `persistLegacyTelemetrySnapshot()` — exported: **yes** — Implements logic starting with: { persistLegacyTelemetry(); } (missing: jsdoc)

### `src/resources/extensions/gsd/markdown-renderer.ts`
- `src/resources/extensions/gsd/markdown-renderer.ts:362` — `renderPlanFromDb(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/markdown-renderer.ts:400` — `renderTaskPlanFromDb(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/markdown-renderer.ts:429` — `renderRoadmapFromDb(basePath: string, milestoneId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/markdown-renderer.ts:947` — `renderReplanFromDb(basePath: string, milestoneId: string, sliceId: string, replanData: ReplanData)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/markdown-renderer.ts:986` — `renderAssessmentFromDb(basePath: string, milestoneId: string, sliceId: string, assessmentData: AssessmentData)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/mcp-project-config.ts`
- `src/resources/extensions/gsd/mcp-project-config.ts:30` — `resolveBundledGsdCliPath(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { const explicit = env.GSD_CLI_PATH?.trim() || env.GSD_BIN_PATH?.trim(); if (explicit) return explicit; const candidates = [ resolve(fileURLToPath(… (missing: jsdoc)
- `src/resources/extensions/gsd/mcp-project-config.ts:47` — `buildProjectWorkflowMcpServerConfig(projectRoot: string, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/mcp-project-config.ts:86` — `ensureProjectWorkflowMcpConfig(projectRoot: string, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/memory-embeddings.ts`
- `src/resources/extensions/gsd/memory-embeddings.ts:73` — `packFloat32(vec: Float32Array)` — exported: **yes** — Implements logic starting with: { return new Uint8Array(vec.buffer, vec.byteOffset, vec.byteLength); } (missing: jsdoc)
- `src/resources/extensions/gsd/memory-embeddings.ts:77` — `unpackFloat32(blob: unknown)` — exported: **yes** — Implements logic starting with: { if (!blob) return null; try { if (blob instanceof Float32Array) return blob; let view: Uint8Array; if (blob instanceof Uint8Array) { view = blob;… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-embeddings.ts:106` — `cosineSimilarity(a: Float32Array, b: Float32Array)` — exported: **yes** — Implements logic starting with: { if (a.length === 0 || a.length !== b.length) return 0; let dot = 0; let normA = 0; let normB = 0; for (let i = 0; i < a.length; i++) { const x =… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-embeddings.ts:124` — `getEmbeddingForMemory(memoryId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const adapter = _getAdapter(); if (!adapter) return null; try { const row = adapter .prepare( "SELECT memory_i… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-embeddings.ts:148` — `loadAllEmbeddings()` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const adapter = _getAdapter(); if (!adapter) return []; try { const rows = adapter .prepare( `SELECT e.memory_id… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-embeddings.ts:180` — `saveEmbedding(memoryId: string, vector: Float32Array, model: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { upsertMemoryEmbedding({ memoryId, model, dim: vector.length, vector: packFloat32(vector), updatedAt: ne… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-embeddings.ts:200` — `removeEmbedding(memoryId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { return deleteMemoryEmbedding(memoryId); } catch { return false; } } (missing: jsdoc)

### `src/resources/extensions/gsd/memory-ingest.ts`
- `src/resources/extensions/gsd/memory-ingest.ts:90` — `ingestNote(note: string, ctx: ExtensionContext | null, opts: IngestOptions = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/memory-ingest.ts:127` — `ingestFile(path: string, ctx: ExtensionContext | null, opts: IngestOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/memory-ingest.ts:172` — `ingestUrl(url: string, ctx: ExtensionContext | null, opts: IngestOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/memory-ingest.ts:276` — `summarizeIngest(result: IngestResult)` — exported: **yes** — Implements logic starting with: { if (!result.sourceId) return "Ingest failed: could not persist source."; const status = result.duplicate ? "duplicate (content_hash match)" : "ne… (missing: jsdoc)

### `src/resources/extensions/gsd/memory-relations.ts`
- `src/resources/extensions/gsd/memory-relations.ts:54` — `isValidRelation(value: unknown)` — exported: **yes** — Implements logic starting with: { return typeof value === "string" && (VALID_RELATIONS as readonly string[]).includes(value); } (missing: jsdoc)
- `src/resources/extensions/gsd/memory-relations.ts:67` — `createMemoryRelation(from: string, to: string, rel: RelationType, confidence?: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; if (!from || !to || from === to || !isValidRelation(rel)) return false; const adapter = _getAdapter(); if (!a… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-relations.ts:98` — `removeMemoryRelationsFor(memoryId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable() || !memoryId) return; try { deleteMemoryRelationsFor(memoryId); } catch { // non-fatal } } (missing: jsdoc)
- `src/resources/extensions/gsd/memory-relations.ts:109` — `listRelationsFor(memoryId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/memory-relations.ts:125` — `traverseGraph(startId: string, depth: number)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/memory-source-store.ts`
- `src/resources/extensions/gsd/memory-source-store.ts:47` — `hashContent(content: string)` — exported: **yes** — Implements logic starting with: { return createHash("sha256").update(content).digest("hex"); } (missing: jsdoc)
- `src/resources/extensions/gsd/memory-source-store.ts:51` — `newSourceId()` — exported: **yes** — Implements logic starting with: { return `SRC-${randomUUID().slice(0, 8)}`; } (missing: jsdoc)
- `src/resources/extensions/gsd/memory-source-store.ts:105` — `listMemorySources(limit = 50)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/memory-source-store.ts:119` — `getMemorySource(id: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const adapter = _getAdapter(); if (!adapter) return null; try { const row = adapter.prepare("SELECT * FROM mem… (missing: jsdoc)
- `src/resources/extensions/gsd/memory-source-store.ts:131` — `deleteMemorySource(id: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { return deleteMemorySourceRow(id); } catch { return false; } } (missing: jsdoc)

### `src/resources/extensions/gsd/memory-store.ts`
- `src/resources/extensions/gsd/memory-store.ts:241` — `queryMemoriesRanked(opts: QueryMemoriesOptions)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/metrics.ts`
- `src/resources/extensions/gsd/metrics.ts:87` — `classifyUnitPhase(unitType: string)` — exported: **yes** — Implements logic starting with: { switch (unitType) { case "research-milestone": case "research-slice": return "research"; case "discuss-milestone": case "discuss-slice": return "… (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:518` — `aggregateByPhase(units: UnitMetrics[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:537` — `aggregateBySlice(units: UnitMetrics[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:555` — `aggregateByModel(units: UnitMetrics[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:573` — `getProjectTotals(units: UnitMetrics[])` — exported: **yes** — Implements logic starting with: { const totals: ProjectTotals = { units: units.length, tokens: emptyTokens(), cost: 0, duration: 0, toolCalls: 0, assistantMessages: 0, userMessage… (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:610` — `aggregateByTier(units: UnitMetrics[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:661` — `formatCost(cost: number)` — exported: **yes** — Implements logic starting with: { const n = Number(cost) || 0; if (n < 0.01) return `$${n.toFixed(4)}`; if (n < 1) return `$${n.toFixed(3)}`; return `$${n.toFixed(2)}`; } (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:854` — `getLockSleepyRetries()` — exported: **yes** — Implements logic starting with: { return _lockSleepyRetries; } (missing: jsdoc)
- `src/resources/extensions/gsd/metrics.ts:855` — `resetLockSleepyRetries()` — exported: **yes** — Implements logic starting with: { _lockSleepyRetries = 0; } (missing: jsdoc)

### `src/resources/extensions/gsd/migrate/command.ts`
- `src/resources/extensions/gsd/migrate/command.ts:57` — `importWrittenMigrationToDb(basePath: string, preview?: MigrationPreview)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/migrate/command.ts:128` — `handleMigrate(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/migrate/transformer.ts`
- `src/resources/extensions/gsd/migrate/transformer.ts:348` — `transformToGSD(parsed: PlanningProject)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/migration-auto-check.ts`
- `src/resources/extensions/gsd/migration-auto-check.ts:43` — `countMarkdownHierarchy(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/migration-auto-check.ts:69` — `countDbHierarchy()` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return zeroCounts(); const counts = zeroCounts(); const milestones = getAllMilestones(); counts.milestones = milestones.len… (missing: jsdoc)
- `src/resources/extensions/gsd/migration-auto-check.ts:86` — `autoImportMarkdownHierarchyIfDbMismatch(basePath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/gsd/milestone-id-utils.ts`
- `src/resources/extensions/gsd/milestone-id-utils.ts:19` — `findMilestoneIds(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/milestone-summary-classifier.ts`
- `src/resources/extensions/gsd/milestone-summary-classifier.ts:15` — `classifyMilestoneSummaryContent(content: string)` — exported: **yes** — Implements logic starting with: { const [fmLines] = splitFrontmatter(content); const fm = fmLines ? parseFrontmatterMap(fmLines) : null; const rawStatus = typeof fm?.status === "s… (missing: jsdoc)

### `src/resources/extensions/gsd/model-router.ts`
- `src/resources/extensions/gsd/model-router.ts:564` — `canonicalModelForTier(tier: ComplexityTier)` — exported: **yes** — Implements logic starting with: { return CANONICAL_TIER_MODELS[tier]; } (missing: jsdoc)
- `src/resources/extensions/gsd/model-router.ts:623` — `resolveModelForTier(tier: ComplexityTier, availableModelIds: string[], routingConfig?: DynamicRoutingConfig, crossProvider?: boolean)` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/model-router.ts:629` — `resolveModelForTier(tier: ComplexityTier, availableModelIds: string[], routingConfigOrCrossProvider: DynamicRoutingConfig | boolean = defaultRoutingConfig(), crossProvider?: boolean)` — exported: **yes** — Implements logic starting with: { const routingConfig = typeof routingConfigOrCrossProvider === "boolean" ? defaultRoutingConfig() : { ...defaultRoutingConfig(), ...routingConfigO… (missing: jsdoc)

### `src/resources/extensions/gsd/native-git-bridge.ts`
- `src/resources/extensions/gsd/native-git-bridge.ts:708` — `nativeIsIgnored(basePath: string, path: string)` — exported: **yes** — Implements logic starting with: { try { execFileSync("git", ["check-ignore", "-q", "--", path], { cwd: basePath, stdio: "pipe", env: GIT_NO_PROMPT_ENV, }); return true; } catch {… (missing: jsdoc)
- `src/resources/extensions/gsd/native-git-bridge.ts:1206` — `assertWorktreeMaterialized(wtPath: string)` — exported: **yes** — Implements logic starting with: { if (existsSync(join(wtPath, ".git"))) return; throw new GSDError( GSD_GIT_ERROR, `git worktree add did not materialize a valid worktree at ${wtPa… (missing: jsdoc)

### `src/resources/extensions/gsd/notification-overlay.ts`
- `src/resources/extensions/gsd/notification-overlay.ts:31` — `notificationOverlayOptions()` — exported: **yes** — Implements logic starting with: { return { width: OVERLAY_WIDTH, minWidth: OVERLAY_MIN_WIDTH, maxHeight: `${OVERLAY_MAX_HEIGHT_PERCENT}%`, anchor: "top-center", row: "24%", margin… (missing: jsdoc)
- `src/resources/extensions/gsd/notification-overlay.ts:73` — `class GSDNotificationOverlay` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/notification-overlay.ts:88` — `GSDNotificationOverlay.constructor(tui: { requestRender: () => void }, theme: Theme, onClose: () => void)` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.onClose = onClose; // Mark all as read on open markAllRead(); this.entries = readNotifications(); this.e… (missing: jsdoc)
- `src/resources/extensions/gsd/notification-overlay.ts:132` — `GSDNotificationOverlay.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if ( matchesKey(data, Key.escape) || matchesKey(data, Key.ctrl("c")) || matchesKey(data, Key.ctrlAlt("n")) || matchesKey(data, Key.ctrlShift("n")… (missing: jsdoc)
- `src/resources/extensions/gsd/notification-overlay.ts:191` — `GSDNotificationOverlay.render(width: number)` — exported: **yes** — Implements logic starting with: { if (this.cachedLines && this.cachedWidth === width) { return this.cachedLines; } const content = this.buildContentLines(width); const terminalRow… (missing: jsdoc)
- `src/resources/extensions/gsd/notification-overlay.ts:216` — `GSDNotificationOverlay.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedLines = undefined; this.cachedWidth = undefined; } (missing: jsdoc)
- `src/resources/extensions/gsd/notification-overlay.ts:221` — `GSDNotificationOverlay.dispose()` — exported: **yes** — Implements logic starting with: { this.disposed = true; clearInterval(this.refreshTimer); if (this.unsubscribeStore) { this.unsubscribeStore(); this.unsubscribeStore = null; } if… (missing: jsdoc)

### `src/resources/extensions/gsd/notification-store.ts`
- `src/resources/extensions/gsd/notification-store.ts:195` — `unsuppressPersistence()` — exported: **yes** — Implements logic starting with: { _suppressCount = Math.max(0, _suppressCount - 1); } (missing: jsdoc)

### `src/resources/extensions/gsd/notification-widget.ts`
- `src/resources/extensions/gsd/notification-widget.ts:16` — `buildNotificationChip()` — exported: **yes** — Implements logic starting with: { const unread = getUnreadCount(); if (unread === 0) return ""; return `🔔 ${unread} unread (${formattedShortcutPair("notifications")})`; } (missing: jsdoc)
- `src/resources/extensions/gsd/notification-widget.ts:24` — `buildNotificationWidgetLines()` — exported: **yes** — Implements logic starting with: { const chip = buildNotificationChip(); return chip ? [` ${chip}`] : []; } (missing: jsdoc)
- `src/resources/extensions/gsd/notification-widget.ts:32` — `initNotificationWidget(ctx: ExtensionContext)` — exported: **yes** — Implements logic starting with: { notificationWidgetCleanup?.(); notificationWidgetCleanup = undefined; if (!ctx.hasUI) return () => {}; const push = () => { const chip = buildNot… (missing: jsdoc)
- `src/resources/extensions/gsd/notification-widget.ts:60` — `_resetNotificationWidgetForTests()` — exported: **yes** — Implements logic starting with: { notificationWidgetCleanup?.(); notificationWidgetCleanup = undefined; } (missing: jsdoc)

### `src/resources/extensions/gsd/notifications.ts`
- `src/resources/extensions/gsd/notifications.ts:70` — `shouldSendDesktopNotification(kind: NotificationKind, preferences: NotificationPreferences | undefined = loadEffectiveGSDPreferences()?.preferences.notifications)` — exported: **yes** — Implements logic starting with: { if (preferences?.enabled === false) return false; switch (kind) { case "error": return preferences?.on_error ?? true; case "budget": return prefe… (missing: jsdoc)
- `src/resources/extensions/gsd/notifications.ts:101` — `buildDesktopNotificationCommand(platform: NodeJS.Platform, title: string, message: string, level: NotifyLevel = "info")` — exported: **yes** — Implements logic starting with: { const normalizedTitle = normalizeNotificationText(title); const normalizedMessage = normalizeNotificationText(message); if (platform === "darwin"… (missing: jsdoc)

### `src/resources/extensions/gsd/observability-validator.ts`
- `src/resources/extensions/gsd/observability-validator.ts:97` — `validateSlicePlanContent(file: string, content: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:172` — `validateTaskPlanContent(file: string, content: string)` — exported: **yes** — Implements logic starting with: { const issues: ValidationIssue[] = []; // ── Plan quality rules (always run, not gated by runtime relevance) ── // Rule: empty or missing Steps se… (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:294` — `validateTaskSummaryContent(file: string, content: string)` — exported: **yes** — Implements logic starting with: { const issues: ValidationIssue[] = []; if (!hasFrontmatterKey(content, "observability_surfaces")) { issues.push({ severity: "warning", scope: "tas… (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:352` — `validateSliceSummaryContent(file: string, content: string)` — exported: **yes** — Implements logic starting with: { const issues: ValidationIssue[] = []; if (!hasFrontmatterKey(content, "observability_surfaces")) { issues.push({ severity: "warning", scope: "sli… (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:389` — `validatePlanBoundary(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:410` — `validateExecuteBoundary(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:427` — `validateCompleteBoundary(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/observability-validator.ts:448` — `formatValidationIssues(issues: ValidationIssue[], limit: number = 4)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/onboarding-state.ts`
- `src/resources/extensions/gsd/onboarding-state.ts:46` — `readOnboardingRecord()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/onboarding-state.ts:77` — `writeOnboardingRecord(patch: Partial<OnboardingRecord>)` — exported: **yes** — Implements logic starting with: { const current = readOnboardingRecord() const next: OnboardingRecord = { ...current, ...patch, version: RECORD_VERSION, // flowVersion is sticky o… (missing: jsdoc)
- `src/resources/extensions/gsd/onboarding-state.ts:109` — `markStepCompleted(stepId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/onboarding-state.ts:122` — `markStepSkipped(stepId: string)` — exported: **yes** — Implements logic starting with: { const r = readOnboardingRecord() if (r.skippedSteps.includes(stepId) || r.completedSteps.includes(stepId)) return writeOnboardingRecord({ skipped… (missing: jsdoc)
- `src/resources/extensions/gsd/onboarding-state.ts:131` — `markOnboardingComplete(completedSteps: string[])` — exported: **yes** — Implements logic starting with: { writeOnboardingRecord({ completedAt: new Date().toISOString(), flowVersion: FLOW_VERSION, completedSteps, }) } (missing: jsdoc)
- `src/resources/extensions/gsd/onboarding-state.ts:139` — `resetOnboarding()` — exported: **yes** — Implements logic starting with: { writeOnboardingRecord({ completedAt: null, completedSteps: [], skippedSteps: [], lastResumePoint: null, }) } (missing: jsdoc)

### `src/resources/extensions/gsd/parallel-monitor-overlay.ts`
- `src/resources/extensions/gsd/parallel-monitor-overlay.ts:282` — `class ParallelMonitorOverlay` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/parallel-monitor-overlay.ts:296` — `ParallelMonitorOverlay.constructor(tui: { requestRender: () => void }, theme: Theme, onClose: () => void, basePath?: string)` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.onClose = onClose; this.basePath = basePath || process.cwd(); this.resizeHandler = () => { if (this.disp… (missing: jsdoc)
- `src/resources/extensions/gsd/parallel-monitor-overlay.ts:336` — `ParallelMonitorOverlay.dispose()` — exported: **yes** — Implements logic starting with: { this.disposed = true; clearInterval(this.refreshTimer); if (this.resizeHandler) { process.stdout.removeListener("resize", this.resizeHandler); th… (missing: jsdoc)
- `src/resources/extensions/gsd/parallel-monitor-overlay.ts:345` — `ParallelMonitorOverlay.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if ( matchesKey(data, Key.escape) || matchesKey(data, Key.ctrlAlt("p")) || matchesKey(data, Key.ctrlShift("p")) || data === "q" ) { this.dispose(… (missing: jsdoc)
- `src/resources/extensions/gsd/parallel-monitor-overlay.ts:370` — `ParallelMonitorOverlay.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedLines = undefined; this.cachedWidth = undefined; } (missing: jsdoc)
- `src/resources/extensions/gsd/parallel-monitor-overlay.ts:375` — `ParallelMonitorOverlay.render(width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/parallel-orchestrator.ts`
- `src/resources/extensions/gsd/parallel-orchestrator.ts:922` — `shutdownParallel(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/parsers-legacy.ts`
- `src/resources/extensions/gsd/parsers-legacy.ts:59` — `parseRoadmap(content: string)` — exported: **yes** — Implements logic starting with: { return cachedParse(content, 'roadmap', _parseRoadmapImpl); } (missing: jsdoc)
- `src/resources/extensions/gsd/parsers-legacy.ts:148` — `parsePlan(content: string)` — exported: **yes** — Implements logic starting with: { return cachedParse(content, 'plan', _parsePlanImpl); } (missing: jsdoc)

### `src/resources/extensions/gsd/paths.ts`
- `src/resources/extensions/gsd/paths.ts:325` — `resolveGsdPathContract(workRoot: string, originalProjectRoot?: string | null)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/gsd/paths.ts:553` — `milestonesDir(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(gsdRoot(basePath), "milestones"); } (missing: jsdoc)
- `src/resources/extensions/gsd/paths.ts:557` — `resolveRuntimeFile(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(gsdRoot(basePath), "RUNTIME.md"); } (missing: jsdoc)
- `src/resources/extensions/gsd/paths.ts:561` — `resolveGsdRootFile(basePath: string, key: GSDRootFileKey)` — exported: **yes** — Implements logic starting with: { const root = gsdRoot(basePath); const canonical = join(root, GSD_ROOT_FILES[key]); if (existsSync(canonical)) return canonical; const legacy = jo… (missing: jsdoc)
- `src/resources/extensions/gsd/paths.ts:570` — `relGsdRootFile(key: GSDRootFileKey)` — exported: **yes** — Implements logic starting with: { return `.gsd/${GSD_ROOT_FILES[key]}`; } (missing: jsdoc)

### `src/resources/extensions/gsd/phase-anchor.ts`
- `src/resources/extensions/gsd/phase-anchor.ts:29` — `writePhaseAnchor(basePath: string, milestoneId: string, anchor: PhaseAnchor)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/phase-anchor.ts:37` — `readPhaseAnchor(basePath: string, milestoneId: string, phase: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/phase-anchor.ts:47` — `formatAnchorForPrompt(anchor: PhaseAnchor)` — exported: **yes** — Implements logic starting with: { const lines: string[] = [ `## Handoff from ${anchor.phase}`, "", `**Intent:** ${anchor.intent}`, ]; if (anchor.decisions.length > 0) { lines.push… (missing: jsdoc)

### `src/resources/extensions/gsd/planning-depth.ts`
- `src/resources/extensions/gsd/planning-depth.ts:44` — `ensureWorkflowPreferencesCaptured(basePath: string)` — exported: **yes** — Implements logic starting with: { const path = getProjectGSDPreferencesPath(basePath); const { frontmatter, body } = readProjectPreferencesParts(path); frontmatter.planning_depth… (missing: jsdoc)

### `src/resources/extensions/gsd/post-unit-hooks.ts`
- `src/resources/extensions/gsd/post-unit-hooks.ts:20` — `checkPostUnitHooks(completedUnitType: string, completedUnitId: string, basePath: string)` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().evaluatePostUnit(completedUnitType, completedUnitId, basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:28` — `getActiveHook()` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().getActiveHook(); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:32` — `isRetryPending()` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().isRetryPending(); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:36` — `consumeRetryTrigger()` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().consumeRetryTrigger(); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:40` — `resetHookState()` — exported: **yes** — Implements logic starting with: { getOrCreateRegistry().resetState(); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:46` — `runPreDispatchHooks(unitType: string, unitId: string, prompt: string, basePath: string)` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().evaluatePreDispatch(unitType, unitId, prompt, basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:57` — `persistHookState(basePath: string)` — exported: **yes** — Implements logic starting with: { getOrCreateRegistry().persistState(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:61` — `restoreHookState(basePath: string)` — exported: **yes** — Implements logic starting with: { getOrCreateRegistry().restoreState(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:65` — `clearPersistedHookState(basePath: string)` — exported: **yes** — Implements logic starting with: { getOrCreateRegistry().clearPersistedState(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:71` — `getHookStatus()` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().getHookStatus(); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:75` — `triggerHookManually(hookName: string, unitType: string, unitId: string, basePath: string)` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().triggerHookManually(hookName, unitType, unitId, basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/post-unit-hooks.ts:84` — `formatHookStatus()` — exported: **yes** — Implements logic starting with: { return getOrCreateRegistry().formatHookStatus(); } (missing: jsdoc)

### `src/resources/extensions/gsd/pr-evidence.ts`
- `src/resources/extensions/gsd/pr-evidence.ts:100` — `buildPrEvidence(input: PrEvidenceInput)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/preferences-models.ts`
- `src/resources/extensions/gsd/preferences-models.ts:363` — `resolveAutoSupervisorConfig()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(); const configured = prefs?.preferences.auto_supervisor ?? {}; return { soft_timeout_minutes: configur… (missing: jsdoc)

### `src/resources/extensions/gsd/preferences-validation.ts`
- `src/resources/extensions/gsd/preferences-validation.ts:32` — `validatePreferences(preferences: GSDPreferences)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/gsd/preferences.ts`
- `src/resources/extensions/gsd/preferences.ts:73` — `resolveSkillDiscoveryMode(basePath?: string)` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(basePath); return prefs?.preferences.skill_discovery ?? "suggest"; } (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:78` — `resolveSkillStalenessDays(basePath?: string)` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(basePath); return prefs?.preferences.skill_staleness_days ?? 60; } (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:124` — `getGlobalGSDPreferencesPath()` — exported: **yes** — Implements logic starting with: { return globalPreferencesPath(); } (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:128` — `getLegacyGlobalGSDPreferencesPath()` — exported: **yes** — Implements logic starting with: { return legacyGlobalPreferencesPath(); } (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:132` — `getProjectGSDPreferencesPath(basePath?: string)` — exported: **yes** — Implements logic starting with: { return projectPreferencesPath(basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:138` — `loadGlobalGSDPreferences()` — exported: **yes** — Implements logic starting with: { return loadPreferencesFile(globalPreferencesPath(), "global") ?? loadPreferencesFile(legacyGlobalPreferencesPathLowercase(), "global") ?? loadPre… (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:144` — `loadProjectGSDPreferences(basePath?: string)` — exported: **yes** — Implements logic starting with: { return loadPreferencesFile(projectPreferencesPath(basePath), "project") ?? loadPreferencesFile(legacyProjectPreferencesPathLowercase(basePath), "… (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:149` — `loadEffectiveGSDPreferences(basePath?: string, opts?: { availableModelIds?: string[] })` — exported: **yes** — Implements logic starting with: { const globalPreferences = loadGlobalGSDPreferences(); const projectPreferences = loadProjectGSDPreferences(basePath); const projectHasPlanningDep… (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:244` — `_resetParseWarningFlag()` — exported: **yes** — Implements logic starting with: { _warnedUnrecognizedFormat = false; _warnedFrontmatterParse = false; _warnedSectionParse = false; } (missing: description)
- `src/resources/extensions/gsd/preferences.ts:251` — `parsePreferencesMarkdown(content: string)` — exported: **yes** — Logs diagnostic information. (missing: description, @param content, @returns)
- `src/resources/extensions/gsd/preferences.ts:533` — `renderPreferencesForSystemPrompt(preferences: GSDPreferences, resolutions?: Map<string, SkillResolution>)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/preferences.ts:648` — `resolveParallelConfig(prefs: GSDPreferences | undefined)` — exported: **yes** — Implements logic starting with: { return { enabled: prefs?.parallel?.enabled ?? false, max_workers: Math.max(1, Math.min(4, prefs?.parallel?.max_workers ?? 2)), budget_ceiling: pr… (missing: jsdoc)

### `src/resources/extensions/gsd/process-task-path.ts`
- `src/resources/extensions/gsd/process-task-path.ts:63` — `recommendProcessPath(taskSize: ProcessTaskSize)` — exported: **yes** — Implements logic starting with: { return TASK_PATHS[taskSize]; } (missing: jsdoc)
- `src/resources/extensions/gsd/process-task-path.ts:67` — `listRecommendedProcessPaths()` — exported: **yes** — Implements logic starting with: { return [ TASK_PATHS.hotfix, TASK_PATHS.bugfix, TASK_PATHS["small-feature"], TASK_PATHS["large-feature"], TASK_PATHS["architecture-change"], ]; } (missing: jsdoc)
- `src/resources/extensions/gsd/process-task-path.ts:77` — `formatRecommendedProcessPaths()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/project-research-policy.ts`
- `src/resources/extensions/gsd/project-research-policy.ts:97` — `classifyProjectResearchScope(projectContent: string, requirementsContent: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/project-research-policy.ts:139` — `getProjectResearchStatus(basePath: string)` — exported: **yes** — Implements logic starting with: { const dir = researchDir(basePath); const globalBlocker = existsSync(join(dir, PROJECT_RESEARCH_BLOCKER)); const completedDimensions: ProjectResea… (missing: jsdoc)
- `src/resources/extensions/gsd/project-research-policy.ts:174` — `writeProjectResearchAutoSkipDecision(basePath: string, classification: ProjectResearchClassification)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/project-research-policy.ts:193` — `clearProjectResearchInflightMarker(basePath: string)` — exported: **yes** — Implements logic starting with: { const marker = join(runtimeDir(basePath), PROJECT_RESEARCH_INFLIGHT_MARKER); if (existsSync(marker)) unlinkSync(marker); } (missing: jsdoc)
- `src/resources/extensions/gsd/project-research-policy.ts:198` — `finalizeProjectResearchTimeout(basePath: string, reason: string)` — exported: **yes** — Implements logic starting with: { const dir = researchDir(basePath); mkdirSync(dir, { recursive: true }); clearProjectResearchInflightMarker(basePath); const before = getProjectRe… (missing: jsdoc)

### `src/resources/extensions/gsd/prompt-loader.ts`
- `src/resources/extensions/gsd/prompt-loader.ts:35` — `resolveExtensionDirFromCandidates(moduleDir: string, agentGsdDir: string, exists: ExistsFn = existsSync)` — exported: **yes** — Implements logic starting with: { const moduleUsable = hasRequiredExtensionAssets(moduleDir, exists); const agentUsable = hasRequiredExtensionAssets(agentGsdDir, exists); // Prefe… (missing: jsdoc)

### `src/resources/extensions/gsd/quick.ts`
- `src/resources/extensions/gsd/quick.ts:93` — `buildQuickCommitInstruction(basePath: string, root: string)` — exported: **yes** — Implements logic starting with: { const externalState = isExternalGsdRoot(basePath, root); if (externalState) { return [ "Commit repo changes atomically, but do not stage or commi… (missing: jsdoc)
- `src/resources/extensions/gsd/quick.ts:161` — `cleanupQuickBranch(basePath = process.cwd())` — exported: **yes** — Implements logic starting with: { const state = readPendingReturn(basePath); if (!state) return false; const repoPath = state.basePath; const gitPrefs = loadEffectiveGSDPreference… (missing: jsdoc)
- `src/resources/extensions/gsd/quick.ts:194` — `handleQuick(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/recovery-classification.ts`
- `src/resources/extensions/gsd/recovery-classification.ts:36` — `classifyFailure(input: RecoveryClassificationInput)` — exported: **yes** — Implements logic starting with: { const message = errorMessage(input.error); // ADR-017: ReconciliationFailedError is a typed throw from the State // Reconciliation Module. Recogn… (missing: jsdoc)

### `src/resources/extensions/gsd/repo-identity.ts`
- `src/resources/extensions/gsd/repo-identity.ts:343` — `cleanNumberedGsdVariants(projectPath: string)` — exported: **yes** — Implements logic starting with: { const removed: string[] = []; try { const entries = readdirSync(projectPath); for (const entry of entries) { if (GSD_NUMBERED_VARIANT_RE.test(ent… (missing: jsdoc)

### `src/resources/extensions/gsd/reports.ts`
- `src/resources/extensions/gsd/reports.ts:60` — `reportsDir(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(gsdRoot(basePath), 'reports'); } (missing: jsdoc)
- `src/resources/extensions/gsd/reports.ts:74` — `loadReportsIndex(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/reports.ts:172` — `regenerateHtmlIndex(basePath: string, index: ReportsIndex)` — exported: **yes** — Implements logic starting with: { const html = buildIndexHtml(index); atomicWriteSync(reportsHtmlIndexPath(basePath), html, 'utf-8'); } (missing: jsdoc)

### `src/resources/extensions/gsd/rethink.ts`
- `src/resources/extensions/gsd/rethink.ts:27` — `handleRethink(_args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/roadmap-slices.ts`
- `src/resources/extensions/gsd/roadmap-slices.ts:155` — `parseRoadmapSlices(content: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/rule-registry.ts`
- `src/resources/extensions/gsd/rule-registry.ts:28` — `resolveHookArtifactPath(basePath: string, unitId: string, artifactName: string)` — exported: **yes** — Implements logic starting with: { const { milestone, slice, task } = parseUnitId(unitId); if (task !== undefined && slice !== undefined) { return join(basePath, ".gsd", "milestone… (missing: jsdoc)
- `src/resources/extensions/gsd/rule-registry.ts:60` — `class RuleRegistry` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/rule-registry.ts:76` — `RuleRegistry.constructor(dispatchRules: UnifiedRule[])` — exported: **yes** — Implements logic starting with: { this.dispatchRules = dispatchRules; } (missing: jsdoc)
- `src/resources/extensions/gsd/rule-registry.ts:346` — `RuleRegistry.getActiveHook()` — exported: **yes** — Implements logic starting with: { return this.activeHook; } (missing: jsdoc)
- `src/resources/extensions/gsd/rule-registry.ts:350` — `RuleRegistry.isRetryPending()` — exported: **yes** — Implements logic starting with: { return this.retryPending; } (missing: jsdoc)

### `src/resources/extensions/gsd/schemas/parsers.ts`
- `src/resources/extensions/gsd/schemas/parsers.ts:107` — `parseProject(content: string)` — exported: **yes** — Implements logic starting with: { const { sections, order } = splitH2Sections(content); const tokens = detectTemplateTokens(sections); const milestones: ParsedProject["milestones"… (missing: jsdoc)
- `src/resources/extensions/gsd/schemas/parsers.ts:175` — `parseRequirements(content: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/schemas/parsers.ts:289` — `parseRoadmap(content: string)` — exported: **yes** — Implements logic starting with: { const { sections, order } = splitH2Sections(content); const tokens = detectTemplateTokens(sections); const slices: ParsedRoadmapSlice[] = []; con… (missing: jsdoc)

### `src/resources/extensions/gsd/schemas/validate.ts`
- `src/resources/extensions/gsd/schemas/validate.ts:418` — `validateArtifact(filePath: string, kind: ArtifactKind, opts: ValidateOptions = {})` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/service-tier.ts`
- `src/resources/extensions/gsd/service-tier.ts:88` — `formatServiceTierFooterStatus(tier: ServiceTierSetting, modelId: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!tier || !modelId || !supportsServiceTier(modelId)) return undefined; return tier === "priority" ? "fast: ⚡ priority" : "fast: 💰 flex"; } (missing: jsdoc)

### `src/resources/extensions/gsd/session-lock.ts`
- `src/resources/extensions/gsd/session-lock.ts:512` — `validateSessionLock(basePath: string)` — exported: **yes** — Implements logic starting with: { return getSessionLockStatus(basePath).valid; } (missing: jsdoc)
- `src/resources/extensions/gsd/session-lock.ts:687` — `readExistingLockDataWithRetry(lp: string, options?: RetryOptions)` — exported: **yes** — Implements logic starting with: { const maxAttempts = options?.maxAttempts ?? 3; const delayMs = options?.delayMs ?? 200; for (let attempt = 1; attempt <= maxAttempts; attempt++)… (missing: jsdoc)

### `src/resources/extensions/gsd/session-model-override.ts`
- `src/resources/extensions/gsd/session-model-override.ts:12` — `setSessionModelOverride(sessionId: string, override: SessionModelOverride)` — exported: **yes** — Implements logic starting with: { const key = normalizeSessionId(sessionId); if (!key) return; sessionOverrides.set(key, { provider: override.provider, id: override.id, }); } (missing: jsdoc)
- `src/resources/extensions/gsd/session-model-override.ts:24` — `getSessionModelOverride(sessionId: string)` — exported: **yes** — Implements logic starting with: { const key = normalizeSessionId(sessionId); if (!key) return undefined; return sessionOverrides.get(key); } (missing: jsdoc)
- `src/resources/extensions/gsd/session-model-override.ts:32` — `clearSessionModelOverride(sessionId: string)` — exported: **yes** — Implements logic starting with: { const key = normalizeSessionId(sessionId); if (!key) return; sessionOverrides.delete(key); } (missing: jsdoc)

### `src/resources/extensions/gsd/setup-catalog.ts`
- `src/resources/extensions/gsd/setup-catalog.ts:54` — `getStep(id: string)` — exported: **yes** — Implements logic starting with: { const idx = STEP_INDEX.get(id as OnboardingStepId) return idx === undefined ? undefined : ONBOARDING_STEPS[idx] } (missing: jsdoc)
- `src/resources/extensions/gsd/setup-catalog.ts:59` — `isValidStepId(id: string)` — exported: **yes** — Implements logic starting with: { return STEP_INDEX.has(id as OnboardingStepId) } (missing: jsdoc)
- `src/resources/extensions/gsd/setup-catalog.ts:86` — `getLlmProviders()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/setup-catalog.ts:90` — `getToolProviders()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/setup-catalog.ts:94` — `getSearchProviders()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/setup-catalog.ts:98` — `getRemoteProviders()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/shortcut-defs.ts`
- `src/resources/extensions/gsd/shortcut-defs.ts:40` — `primaryShortcutCombo(id: GSDShortcutId)` — exported: **yes** — Implements logic starting with: { return combo("Ctrl+Alt+", GSD_SHORTCUTS[id].key); } (missing: jsdoc)
- `src/resources/extensions/gsd/shortcut-defs.ts:44` — `fallbackShortcutCombo(id: GSDShortcutId)` — exported: **yes** — Implements logic starting with: { return combo("Ctrl+Shift+", GSD_SHORTCUTS[id].key); } (missing: jsdoc)
- `src/resources/extensions/gsd/shortcut-defs.ts:48` — `shortcutPair(id: GSDShortcutId, formatter: (combo: string) => string = (combo) => combo)` — exported: **yes** — Implements logic starting with: { const primary = formatter(primaryShortcutCombo(id)); if (!GSD_SHORTCUTS[id].hasFallback) return primary; return `${primary} / ${formatter(fallbac… (missing: jsdoc)
- `src/resources/extensions/gsd/shortcut-defs.ts:54` — `formattedShortcutPair(id: GSDShortcutId)` — exported: **yes** — Implements logic starting with: { return shortcutPair(id, formatShortcut); } (missing: jsdoc)

### `src/resources/extensions/gsd/skill-manifest.ts`
- `src/resources/extensions/gsd/skill-manifest.ts:158` — `warnIfManifestHasMissingSkills(unitType: string | undefined, installedNames: Set<string>)` — exported: **yes** — Implements logic starting with: { // Strict mode is intentionally opt-in via exactly "1"; values like "0" or // "false" must preserve the normal silent manifest behavior. if (proc… (missing: jsdoc)

### `src/resources/extensions/gsd/slice-cadence.ts`
- `src/resources/extensions/gsd/slice-cadence.ts:405` — `getMilestoneResquash(prefs: { git?: { milestone_resquash?: boolean } } | undefined | null)` — exported: **yes** — Implements logic starting with: { // Default true when cadence is slice — resquash preserves the milestone- // level history shape users expect. return prefs?.git?.milestone_resqu… (missing: jsdoc)

### `src/resources/extensions/gsd/slice-parallel-orchestrator.ts`
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:87` — `_resolveSliceParallelMaxWorkersForTest(maxWorkers?: number)` — exported: **yes** — Implements logic starting with: { return maxWorkers ?? 2; } (missing: jsdoc)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:91` — `_buildSliceWorkerEnvForTest(basePath: string, milestoneId: string, sliceId: string, workerToken: string, sourceEnv: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return { ...sourceEnv, GSD_SLICE_LOCK: sliceId, GSD_MILESTONE_LOCK: milestoneId, GSD_PROJECT_ROOT: basePath, GSD_PARALLEL_WORKER: "1", GSD_SLICE_… (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/completion.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/completion.ts:43` — `detectMissingCompletionTimestampDrift(state: GSDState, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const mid = state.activeMilestone?.id; if (!mid) return []; const milestone = getMilestone(mid); if (!milestone)… (missing: jsdoc)
- `src/resources/extensions/gsd/state-reconciliation/drift/completion.ts:112` — `repairMissingCompletionTimestamp(record: CompletionTimestampDrift, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { const composite = record.ids[0]; if (!composite) return; const parts = composite.split("/"); if (record.entity === "milestone") { const [mid] = p… (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts:306` — `detectMergeStateDrift(_state: GSDState, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { if (hasMergeStateLeftovers(ctx.basePath)) { return [{ kind: "unmerged-merge-state", basePath: ctx.basePath }]; } return []; } (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/project-md.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/project-md.ts:33` — `detectUnregisteredMilestoneDrift(_state: GSDState, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const drifts: UnregisteredMilestoneDrift[] = []; for (const milestoneId of findMilestoneIds(ctx.basePath)) { if… (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/roadmap.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/roadmap.ts:61` — `detectRoadmapDivergenceDrift(_state: GSDState, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const drifts: RoadmapDivergenceDrift[] = []; for (const milestoneId of findMilestoneIds(ctx.basePath)) { // Skip… (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/sketch-flag.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/sketch-flag.ts:24` — `detectStaleSketchFlags(state: GSDState, ctx: DriftContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/state-reconciliation/drift/sketch-flag.ts:41` — `repairStaleSketchFlag(record: SketchFlagDrift)` — exported: **yes** — Implements logic starting with: { setSliceSketchFlag(record.mid, record.sid, false); } (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/stale-render.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/stale-render.ts:132` — `detectStaleRenderDrift(_state: GSDState, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { return detectStaleRenderDriftFromBasePath(ctx.basePath); } (missing: jsdoc)
- `src/resources/extensions/gsd/state-reconciliation/drift/stale-render.ts:139` — `repairStaleRender(record: StaleRenderDrift, ctx: DriftContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/drift/stale-worker.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/stale-worker.ts:18` — `detectStaleWorkerDrift(_state: GSDState, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { const data = readSessionLockData(ctx.basePath); if (!data) return []; if (typeof data.pid !== "number") return []; if (isSessionLockProcessAlive(… (missing: jsdoc)
- `src/resources/extensions/gsd/state-reconciliation/drift/stale-worker.ts:36` — `repairStaleWorker(_record: StaleWorkerDrift, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { // removeStaleSessionLock is idempotent: it re-reads lock state and is a // no-op when the lock is held by an alive process. Safe under cap=2 ret… (missing: jsdoc)

### `src/resources/extensions/gsd/state-reconciliation/errors.ts`
- `src/resources/extensions/gsd/state-reconciliation/errors.ts:41` — `ReconciliationFailedError.constructor(opts: ReconciliationFailedErrorOptions)` — exported: **yes** — Implements logic starting with: { super(formatMessage(opts)); this.name = "ReconciliationFailedError"; this.failures = opts.failures ?? []; this.detectionFailures = opts.detection… (missing: jsdoc)

### `src/resources/extensions/gsd/state-transition-matrix.ts`
- `src/resources/extensions/gsd/state-transition-matrix.ts:125` — `findTransition(from: Phase, event: string)` — exported: **yes** — Implements logic starting with: { return STATE_TRANSITION_MATRIX.find((entry) => (entry.from === from || entry.from === "*") && entry.event === event, ); } (missing: jsdoc)
- `src/resources/extensions/gsd/state-transition-matrix.ts:134` — `validateTransitionMatrix(requiredEvents: readonly string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/state.ts`
- `src/resources/extensions/gsd/state.ts:208` — `getDeriveTelemetry()` — exported: **yes** — Implements logic starting with: { return { ..._telemetry }; } (missing: jsdoc)
- `src/resources/extensions/gsd/state.ts:209` — `resetDeriveTelemetry()` — exported: **yes** — Implements logic starting with: { _telemetry = { dbDeriveCount: 0, markdownDeriveCount: 0 }; } (missing: jsdoc)
- `src/resources/extensions/gsd/state.ts:654` — `deriveStateFromDb(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/state.ts:870` — `_deriveStateImpl(basePath: string, opts?: DeriveStateOptions)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/tests/dist-redirect.mjs`
- `src/resources/extensions/gsd/tests/dist-redirect.mjs:9` — `resolve(specifier, context, nextResolve)` — exported: **yes** — Implements logic starting with: { // 1. Redirect all workspace package bare imports to source. // CI portability runs don't build any packages/ dist artifacts, so every // @gsd/*… (missing: jsdoc)
- `src/resources/extensions/gsd/tests/dist-redirect.mjs:67` — `load(url, context, nextLoad)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/tests/marketplace-test-fixtures.ts`
- `src/resources/extensions/gsd/tests/marketplace-test-fixtures.ts:35` — `getMarketplaceFixtures(testFileDir: string)` — exported: **yes** — Implements logic starting with: { const gsd2Root = resolve(testFileDir, '../../../../..'); const localClaudeSkillsPath = resolve(gsd2Root, '../claude_skills'); const localClaudePl… (missing: jsdoc)

### `src/resources/extensions/gsd/tests/resolve-ts-hooks.mjs`
- `src/resources/extensions/gsd/tests/resolve-ts-hooks.mjs:6` — `resolve(specifier, context, nextResolve)` — exported: **yes** — Implements logic starting with: { let tsSpecifier = specifier; if (specifier.includes('@gsd/')) { tsSpecifier = specifier.replace('@gsd/', PACKAGES_ROOT).replace('/dist/', '/src/'… (missing: jsdoc)

### `src/resources/extensions/gsd/tests/test-helpers.ts`
- `src/resources/extensions/gsd/tests/test-helpers.ts:106` — `extractSourceRegion(src: string, startAnchor: string, endAnchor?: string | { fromIdx: number })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: description, @returns)

### `src/resources/extensions/gsd/token-counter.ts`
- `src/resources/extensions/gsd/token-counter.ts:67` — `initTokenCounter()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/token-counter.ts:72` — `isAccurateCountingAvailable()` — exported: **yes** — Implements logic starting with: { return encoder !== null; } (missing: jsdoc)
- `src/resources/extensions/gsd/token-counter.ts:76` — `getCharsPerToken(provider: TokenProvider)` — exported: **yes** — Implements logic starting with: { return CHARS_PER_TOKEN_BY_PROVIDER[provider] ?? CHARS_PER_TOKEN_BY_PROVIDER.unknown; } (missing: jsdoc)
- `src/resources/extensions/gsd/token-counter.ts:80` — `estimateTokensForProvider(text: string, provider: TokenProvider)` — exported: **yes** — Implements logic starting with: { const ratio = getCharsPerToken(provider); return Math.ceil(text.length / ratio); } (missing: jsdoc)

### `src/resources/extensions/gsd/tool-contract.ts`
- `src/resources/extensions/gsd/tool-contract.ts:31` — `compileUnitToolContract(unitType: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/complete-milestone.ts`
- `src/resources/extensions/gsd/tools/complete-milestone.ts:127` — `handleCompleteMilestone(params: CompleteMilestoneParams, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/context-mode-tool-result.ts`
- `src/resources/extensions/gsd/tools/context-mode-tool-result.ts:12` — `contextModeDisabledResult(operation: ContextModeToolName)` — exported: **yes** — Implements logic starting with: { return { content: [ { type: "text", text: `${operation} is disabled by \`context_mode.enabled: false\` in preferences. ` + "Remove that override… (missing: jsdoc)

### `src/resources/extensions/gsd/tools/exec-search-tool.ts`
- `src/resources/extensions/gsd/tools/exec-search-tool.ts:17` — `executeExecSearch(params: ExecSearchToolParams, opts: { baseDir: string; preferences?: { context_mode?: ContextModeConfig } | null })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/exec-tool.ts`
- `src/resources/extensions/gsd/tools/exec-tool.ts:34` — `buildExecOptions(baseDir: string, cfg: ContextModeConfig | undefined, extras?: Pick<ExecSandboxOptions, "env" | "now" | "generateId">)` — exported: **yes** — Implements logic starting with: { const allowlist = Array.isArray(cfg?.exec_env_allowlist) ? cfg!.exec_env_allowlist! : EXEC_DEFAULTS.envAllowlist; const stdoutCap = clampNumber(… (missing: jsdoc)
- `src/resources/extensions/gsd/tools/exec-tool.ts:84` — `executeGsdExec(params: ExecToolParams, deps: ExecToolDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/memory-tools.ts`
- `src/resources/extensions/gsd/tools/memory-tools.ts:70` — `executeMemoryCapture(params: MemoryCaptureParams)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return dbUnavailable("memory_capture"); const category = (params.category ?? "").trim().toLowerCase(); const content = (par… (missing: jsdoc)
- `src/resources/extensions/gsd/tools/memory-tools.ts:183` — `executeMemoryQuery(params: MemoryQueryParams)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/memory-tools.ts:342` — `executeGsdGraph(params: GsdGraphParams)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/plan-milestone.ts`
- `src/resources/extensions/gsd/tools/plan-milestone.ts:212` — `handlePlanMilestone(rawParams: PlanMilestoneParams, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/plan-slice.ts`
- `src/resources/extensions/gsd/tools/plan-slice.ts:134` — `handlePlanSlice(rawParams: PlanSliceParams, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/plan-task.ts`
- `src/resources/extensions/gsd/tools/plan-task.ts:59` — `handlePlanTask(rawParams: PlanTaskParams, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/reassess-roadmap.ts`
- `src/resources/extensions/gsd/tools/reassess-roadmap.ts:97` — `handleReassessRoadmap(rawParams: ReassessRoadmapParams, basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/reopen-milestone.ts`
- `src/resources/extensions/gsd/tools/reopen-milestone.ts:47` — `handleReopenMilestone(params: ReopenMilestoneParams, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/reopen-slice.ts`
- `src/resources/extensions/gsd/tools/reopen-slice.ts:48` — `handleReopenSlice(params: ReopenSliceParams, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/reopen-task.ts`
- `src/resources/extensions/gsd/tools/reopen-task.ts:47` — `handleReopenTask(params: ReopenTaskParams, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/replan-slice.ts`
- `src/resources/extensions/gsd/tools/replan-slice.ts:80` — `handleReplanSlice(rawParams: ReplanSliceParams, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/resume-tool.ts`
- `src/resources/extensions/gsd/tools/resume-tool.ts:14` — `executeResume(_params: ResumeToolParams, opts: { baseDir: string; preferences?: { context_mode?: ContextModeConfig } | null })` — exported: **yes** — Implements logic starting with: { if (!isContextModeEnabled(opts.preferences)) { return contextModeDisabledResult("gsd_resume"); } const snapshot = readCompactionSnapshot(opts.bas… (missing: jsdoc)

### `src/resources/extensions/gsd/tools/validate-milestone.ts`
- `src/resources/extensions/gsd/tools/validate-milestone.ts:91` — `handleValidateMilestone(params: ValidateMilestoneParams, basePath: string, opts?: ValidateMilestoneOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/tools/workflow-tool-executors.ts`
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:58` — `isSupportedSummaryArtifactType(artifactType: string)` — exported: **yes** — Implements logic starting with: { return (SUPPORTED_SUMMARY_ARTIFACT_TYPES as readonly string[]).includes(artifactType); } (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:99` — `executeSummarySave(params: SummarySaveParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:339` — `executeTaskComplete(params: TaskCompleteParams, basePath: string = process.cwd())` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:386` — `executeTaskReopen(params: ReopenTaskExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:427` — `executeSliceReopen(params: ReopenSliceExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:468` — `executeMilestoneReopen(params: ReopenMilestoneExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:509` — `executeSliceComplete(params: SliceCompleteExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:593` — `executeCompleteMilestone(params: CompleteMilestoneExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:639` — `executeValidateMilestone(params: ValidateMilestoneExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:680` — `executeReassessRoadmap(params: ReassessRoadmapExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:722` — `executeSaveGateResult(params: SaveGateResultParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:781` — `executePlanMilestone(params: PlanMilestoneExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:821` — `executePlanSlice(params: PlanSliceExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:863` — `executeReplanSlice(params: ReplanSliceExecutorParams, basePath: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/tools/workflow-tool-executors.ts:909` — `executeMilestoneStatus(params: MilestoneStatusParams, basePath: string = process.cwd())` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/gsd/tui/render-kit.ts`
- `src/resources/extensions/gsd/tui/render-kit.ts:11` — `safeLine(text: string, width: number, ellipsis = "…")` — exported: **yes** — Implements logic starting with: { if (width <= 0) return ""; return truncateToWidth(text, width, ellipsis); } (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:16` — `padRightVisible(text: string, width: number)` — exported: **yes** — Implements logic starting with: { if (width <= 0) return ""; const truncated = safeLine(text, width); const pad = Math.max(0, width - visibleWidth(truncated)); return truncated +… (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:23` — `rightAlign(left: string, right: string, width: number)` — exported: **yes** — Implements logic starting with: { if (width <= 0) return ""; if (!right) return safeLine(left, width); if (!left) return safeLine(" ".repeat(Math.max(0, width - visibleWidth(right… (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:31` — `wrapVisibleText(text: string, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:38` — `renderBar(theme: ThemeLike, width: number, color = "muted")` — exported: **yes** — Implements logic starting with: { return safeLine(theme.fg(color, "─".repeat(Math.max(0, width))), width, ""); } (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:42` — `renderKeyHints(theme: ThemeLike, hints: string[], width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:46` — `renderProgressBar(theme: ThemeLike, done: number, total: number, width: number, options: { filledColor?: string; emptyColor?: string; filledChar?: string; emptyChar?: string; } = {})` — exported: **yes** — Implements logic starting with: { const barWidth = Math.max(0, width); const pct = total > 0 ? Math.max(0, Math.min(1, done / total)) : 0; const filled = Math.round(pct * barWidth… (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:69` — `statusGlyph(theme: ThemeLike, level: "active" | "idle" | "warning" | "error" | "success")` — exported: **yes** — Implements logic starting with: { switch (level) { case "active": return theme.fg("success", "●"); case "success": return theme.fg("success", "✓"); case "warning": return theme.fg… (missing: jsdoc)
- `src/resources/extensions/gsd/tui/render-kit.ts:83` — `renderFrame(theme: ThemeLike, inner: string[], width: number, options: { borderColor?: string; paddingX?: number } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/undo.ts`
- `src/resources/extensions/gsd/undo.ts:382` — `uncheckTaskInPlan(basePath: string, mid: string, sid: string, tid: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/undo.ts:414` — `findCommitsForUnit(activityDir: string, unitType: string, unitId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/undo.ts:454` — `extractCommitShas(content: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/unit-context-manifest.ts`
- `src/resources/extensions/gsd/unit-context-manifest.ts:748` — `compileSubagentPermissionContract(policy: ToolsPolicy | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!policy) { return { allowed: false, allowedSubagents: [], toolsMode: "unknown" }; } if (policy.mode === "all") { return { allowed: true, allo… (missing: jsdoc)
- `src/resources/extensions/gsd/unit-context-manifest.ts:767` — `resolveSubagentPermissionContract(unitType: string)` — exported: **yes** — Implements logic starting with: { return compileSubagentPermissionContract(resolveManifest(unitType)?.tools); } (missing: jsdoc)

### `src/resources/extensions/gsd/unit-ownership.ts`
- `src/resources/extensions/gsd/unit-ownership.ts:158` — `taskUnitKey(milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { return `${milestoneId}/${sliceId}/${taskId}`; } (missing: jsdoc)
- `src/resources/extensions/gsd/unit-ownership.ts:162` — `sliceUnitKey(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { return `${milestoneId}/${sliceId}`; } (missing: jsdoc)

### `src/resources/extensions/gsd/unit-runtime.ts`
- `src/resources/extensions/gsd/unit-runtime.ts:87` — `isInFlightRuntimePhase(phase: UnitRuntimePhase)` — exported: **yes** — Implements logic starting with: { return IN_FLIGHT_RUNTIME_PHASES.has(phase); } (missing: jsdoc)
- `src/resources/extensions/gsd/unit-runtime.ts:130` — `writeUnitRuntimeRecord(basePath: string, unitType: string, unitId: string, startedAt: number, updates: Partial<AutoUnitRuntimeRecord> = {})` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/gsd/unit-runtime.ts:162` — `readUnitRuntimeRecord(basePath: string, unitType: string, unitId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/unit-runtime.ts:172` — `clearUnitRuntimeRecord(basePath: string, unitType: string, unitId: string)` — exported: **yes** — Implements logic starting with: { const path = runtimePath(basePath, unitType, unitId); if (existsSync(path)) unlinkSync(path); } (missing: jsdoc)
- `src/resources/extensions/gsd/unit-runtime.ts:198` — `inspectExecuteTaskDurability(basePath: string, unitId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/unit-runtime.ts:257` — `formatExecuteTaskRecoveryStatus(status: ExecuteTaskRecoveryStatus)` — exported: **yes** — Implements logic starting with: { if (status.dbComplete) return "DB task status is closed"; const missing = [] as string[]; if (!status.summaryExists) missing.push(`summary missin… (missing: jsdoc)

### `src/resources/extensions/gsd/uok/audit-toggle.ts`
- `src/resources/extensions/gsd/uok/audit-toggle.ts:3` — `setUnifiedAuditEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { process.env[AUDIT_ENV_KEY] = enabled ? "1" : "0"; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/audit-toggle.ts:7` — `isUnifiedAuditEnabled()` — exported: **yes** — Implements logic starting with: { return process.env[AUDIT_ENV_KEY] === "1"; } (missing: jsdoc)

### `src/resources/extensions/gsd/uok/audit.ts`
- `src/resources/extensions/gsd/uok/audit.ts:21` — `buildAuditEnvelope(args: { traceId: string; turnId?: string; causedBy?: string; category: AuditEventEnvelope["category"]; type: string; payload?: Record<string, unknown>; })` — exported: **yes** — Implements logic starting with: { return { version: CURRENT_UOK_CONTRACT_VERSION, eventId: randomUUID(), traceId: args.traceId, turnId: args.turnId, causedBy: args.causedBy, categ… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/audit.ts:42` — `emitUokAuditEvent(basePath: string, event: AuditEventEnvelope)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/contracts.ts`
- `src/resources/extensions/gsd/uok/contracts.ts:251` — `normalizeTurnResult(value: TurnResult)` — exported: **yes** — Implements logic starting with: { return { ...value, version: normalizeVersion(value.version) }; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/contracts.ts:255` — `normalizeDispatchEnvelope(value: UokDispatchEnvelope)` — exported: **yes** — Implements logic starting with: { return { ...value, version: normalizeVersion(value.version) }; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/contracts.ts:259` — `normalizeAuditEvent(value: AuditEventEnvelope)` — exported: **yes** — Implements logic starting with: { return { ...value, version: normalizeVersion(value.version) }; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/contracts.ts:263` — `validateTurnResult(value: TurnResult)` — exported: **yes** — Implements logic starting with: { const normalized = normalizeTurnResult(value); const record = normalized as unknown as Record<string, unknown>; const issues: ContractValidationI… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/contracts.ts:282` — `validateDispatchEnvelope(value: UokDispatchEnvelope)` — exported: **yes** — Implements logic starting with: { const normalized = normalizeDispatchEnvelope(value); const record = normalized as unknown as Record<string, unknown>; const issues: ContractValid… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/contracts.ts:295` — `validateAuditEvent(value: AuditEventEnvelope)` — exported: **yes** — Implements logic starting with: { const normalized = normalizeAuditEvent(value); const record = normalized as unknown as Record<string, unknown>; const issues: ContractValidationI… (missing: jsdoc)

### `src/resources/extensions/gsd/uok/dispatch-envelope.ts`
- `src/resources/extensions/gsd/uok/dispatch-envelope.ts:26` — `buildDispatchEnvelope(input: BuildDispatchEnvelopeInput)` — exported: **yes** — Implements logic starting with: { return { version: CURRENT_UOK_CONTRACT_VERSION, action: input.action, nodeKind: input.node?.kind, unitType: input.unitType, unitId: input.unitId,… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/dispatch-envelope.ts:52` — `explainDispatch(envelope: UokDispatchEnvelope)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/execution-graph.ts`
- `src/resources/extensions/gsd/uok/execution-graph.ts:43` — `selectConflictFreeBatch({ orderedIds, maxParallel, hasConflict, }: ConflictFreeBatchInput)` — exported: **yes** — Implements logic starting with: { if (maxParallel <= 0 || orderedIds.length === 0) return []; const selected: string[] = []; for (const candidate of orderedIds) { if (selected.len… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:70` — `selectReactiveDispatchBatch(input: ReactiveDispatchSelectionInput)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:115` — `buildSidecarQueueNodes(queue: SidecarItem[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:124` — `buildExecutionGraphSnapshot(nodes: UokGraphNode[], phase: ExecutionGraphSnapshot["phase"])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:138` — `scheduleSidecarQueue(queue: SidecarItem[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:156` — `class ExecutionGraphScheduler` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:159` — `ExecutionGraphScheduler.registerHandler(kind: UokGraphNode["kind"], handler: ExecutionNodeHandler)` — exported: **yes** — Implements logic starting with: { this.handlers.set(kind, handler); } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/execution-graph.ts:163` — `ExecutionGraphScheduler.run(nodes: UokGraphNode[], options?: ExecutionGraphRunOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/flags.ts`
- `src/resources/extensions/gsd/uok/flags.ts:24` — `resolveUokFlags(prefs: GSDPreferences | undefined)` — exported: **yes** — Implements logic starting with: { const uok = prefs?.uok; const legacyFallback = uok?.legacy_fallback?.enabled === true || envForcesLegacyFallback(); const enabledByPreference = u… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/flags.ts:42` — `loadUokFlags()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences()?.preferences; return resolveUokFlags(prefs); } (missing: jsdoc)

### `src/resources/extensions/gsd/uok/gate-runner.ts`
- `src/resources/extensions/gsd/uok/gate-runner.ts:41` — `class UokGateRunner` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/gate-runner.ts:44` — `UokGateRunner.register(gate: GateExecutionInput)` — exported: **yes** — Implements logic starting with: { this.registry.set(gate.id, gate); } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/gate-runner.ts:48` — `UokGateRunner.list()` — exported: **yes** — Implements logic starting with: { return Array.from(this.registry.values()); } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/gate-runner.ts:52` — `UokGateRunner.run(id: string, ctx: GateRunnerContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/gitops.ts`
- `src/resources/extensions/gsd/uok/gitops.ts:21` — `writeTurnGitTransaction(args: GitTxArgs)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; upsertTurnGitTransaction({ traceId: args.traceId, turnId: args.turnId, unitType: args.unitType, unitId: args.unitId… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/gitops.ts:57` — `writeTurnCloseoutGitRecord(basePath: string, record: TurnCloseoutRecord, metadata?: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { writeTurnGitTransaction({ basePath, traceId: record.traceId, turnId: record.turnId, unitType: record.unitType, unitId: record.unitId, stage: "rec… (missing: jsdoc)

### `src/resources/extensions/gsd/uok/kernel.ts`
- `src/resources/extensions/gsd/uok/kernel.ts:55` — `runAutoLoopWithUok(args: RunAutoLoopWithUokArgs)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/loop-adapter.ts`
- `src/resources/extensions/gsd/uok/loop-adapter.ts:22` — `createTurnObserver(options: CreateTurnObserverOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/model-policy.ts`
- `src/resources/extensions/gsd/uok/model-policy.ts:31` — `buildRequirementVector(unitType?: string, taskMetadata?: TaskMetadata)` — exported: **yes** — Implements logic starting with: { if (!unitType) return {}; return computeTaskRequirements(unitType, taskMetadata) as unknown as Partial<Record<string, number>>; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/model-policy.ts:36` — `applyModelPolicyFilter(candidates: T[], options: ModelPolicyOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/parity-report.ts`
- `src/resources/extensions/gsd/uok/parity-report.ts:38` — `parseParityEvents(raw: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/parity-report.ts:51` — `buildParityReport(events: readonly UokParityEvent[], sourcePath: string)` — exported: **yes** — Implements logic starting with: { const paths: Record<string, number> = {}; const statuses: Record<string, number> = {}; const criticalMismatches: string[] = []; let fallbackInvoc… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/parity-report.ts:77` — `writeParityReport(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/gsd/uok/plan-v2.ts`
- `src/resources/extensions/gsd/uok/plan-v2.ts:21` — `isExecutionEntryPhase(phase: Phase)` — exported: **yes** — Implements logic starting with: { return EXECUTION_ENTRY_PHASES.has(phase); } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/plan-v2.ts:74` — `hasFinalizedMilestoneContext(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { return hasMilestoneFileContent(basePath, milestoneId, "CONTEXT"); } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/plan-v2.ts:78` — `isMissingFinalizedContextResult(result: PlanV2CompileResult)` — exported: **yes** — Implements logic starting with: { return !result.ok && result.finalizedContextIncluded === false; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/plan-v2.ts:82` — `isEmptyPlanV2GraphResult(result: PlanV2CompileResult)` — exported: **yes** — Implements logic starting with: { return !result.ok && result.emptyGraph === true; } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/plan-v2.ts:96` — `compileUnitGraphFromState(basePath: string, state: GSDState)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/plan-v2.ts:191` — `ensurePlanV2Graph(basePath: string, state: GSDState)` — exported: **yes** — Implements logic starting with: { const compiled = compileUnitGraphFromState(basePath, state); if (!compiled.ok) return compiled; if ((compiled.nodeCount ?? 0) <= 0) { if ( (state… (missing: jsdoc)

### `src/resources/extensions/gsd/uok/timeline.ts`
- `src/resources/extensions/gsd/uok/timeline.ts:144` — `buildTurnTimeline(basePath: string, filter: TurnTimelineFilter = {})` — exported: **yes** — Implements logic starting with: { if (isDbAvailable()) { return { authoritative: "db", degraded: false, entries: readDbTimeline(filter), }; } return { authoritative: "degraded-fal… (missing: jsdoc)

### `src/resources/extensions/gsd/uok/writer.ts`
- `src/resources/extensions/gsd/uok/writer.ts:44` — `acquireWriterToken(args: { basePath: string; traceId: string; turnId: string; owner?: WriterToken["owner"]; })` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/writer.ts:67` — `releaseWriterToken(basePath: string, token: WriterToken)` — exported: **yes** — Implements logic starting with: { const key = tokenKey(basePath, token.turnId); const current = activeTokens.get(key); if (current?.tokenId === token.tokenId) { activeTokens.delet… (missing: jsdoc)
- `src/resources/extensions/gsd/uok/writer.ts:75` — `hasActiveWriterToken(basePath: string, turnId: string)` — exported: **yes** — Implements logic starting with: { return activeTokens.has(tokenKey(basePath, turnId)); } (missing: jsdoc)
- `src/resources/extensions/gsd/uok/writer.ts:79` — `nextWriteRecord(args: { basePath: string; token: WriterToken; category: WriteRecord["category"]; operation: WriteRecord["operation"]; path?: string; metadata?: Record<string, unknown>; })` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/gsd/uok/writer.ts:111` — `resetWriterTokensForTests()` — exported: **yes** — Implements logic starting with: { activeTokens.clear(); } (missing: jsdoc)

### `src/resources/extensions/gsd/user-input-boundary.ts`
- `src/resources/extensions/gsd/user-input-boundary.ts:95` — `approvalGateIdForUnit(unitType: string | undefined, unitId?: string | null)` — exported: **yes** — Implements logic starting with: { if (!unitType) return null; if (unitType === "discuss-project") return "depth_verification_project_confirm"; if (unitType === "discuss-requiremen… (missing: jsdoc)
- `src/resources/extensions/gsd/user-input-boundary.ts:121` — `isExplicitApprovalResponse(input: string | undefined, pendingGateId?: string | null)` — exported: **yes** — Implements logic starting with: { const text = input?.trim() ?? ""; if (!text) return false; if (pendingGateId?.includes("research_decision")) { return RESEARCH_DECISION_RESPONSE_… (missing: jsdoc)
- `src/resources/extensions/gsd/user-input-boundary.ts:134` — `isAwaitingUserInput(messages: unknown[] | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/user-input-boundary.ts:145` — `isAwaitingApprovalBoundary(messages: unknown[] | undefined)` — exported: **yes** — Implements logic starting with: { if (anyMessageMatches(messages, /ask_user_questions was cancelled before receiving a response/i)) return true; if (anyMessageMatches(messages, RE… (missing: jsdoc)
- `src/resources/extensions/gsd/user-input-boundary.ts:154` — `shouldPauseForUserApprovalQuestion(unitType: string | undefined, messages: unknown[] | undefined)` — exported: **yes** — Implements logic starting with: { if (!unitType || !USER_APPROVAL_UNIT_TYPES.has(unitType)) return false; if (anyMessageMatches(messages, /ask_user_questions was cancelled before… (missing: jsdoc)

### `src/resources/extensions/gsd/verification-verdict.ts`
- `src/resources/extensions/gsd/verification-verdict.ts:18` — `decideVerificationVerdict(unitType: string, result: VerificationGateResult)` — exported: **yes** — Implements logic starting with: { if (unitType === "execute-task" && result.discoverySource === "none" && result.checks.length === 0) { return { passed: false, reason: "no-host-ch… (missing: jsdoc)

### `src/resources/extensions/gsd/visualizer-data.ts`
- `src/resources/extensions/gsd/visualizer-data.ts:233` — `computeCriticalPath(milestones: VisualizerMilestone[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-data.ts:785` — `loadVisualizerData(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/visualizer-overlay.ts`
- `src/resources/extensions/gsd/visualizer-overlay.ts:55` — `class GSDVisualizerOverlay` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-overlay.ts:85` — `GSDVisualizerOverlay.constructor(tui: { requestRender: () => void }, theme: Theme, onClose: () => void)` — exported: **yes** — Implements logic starting with: { this.tui = tui; this.theme = theme; this.onClose = onClose; this.basePath = process.cwd(); // Enable SGR mouse tracking process.stdout.write("\x1… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-overlay.ts:136` — `GSDVisualizerOverlay.handleInput(data: string)` — exported: **yes** — Implements logic starting with: { if (matchesKey(data, Key.escape) || matchesKey(data, Key.ctrl("c"))) { this.dispose(); this.onClose(); return; } // Filter mode input routing if… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-overlay.ts:447` — `GSDVisualizerOverlay.render(width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-overlay.ts:555` — `GSDVisualizerOverlay.invalidate()` — exported: **yes** — Implements logic starting with: { this.cachedWidth = undefined; this.cachedLines = undefined; } (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-overlay.ts:560` — `GSDVisualizerOverlay.dispose()` — exported: **yes** — Implements logic starting with: { this.disposed = true; clearInterval(this.refreshTimer); if (this.resizeHandler) { process.stdout.removeListener("resize", this.resizeHandler); th… (missing: jsdoc)

### `src/resources/extensions/gsd/visualizer-views.ts`
- `src/resources/extensions/gsd/visualizer-views.ts:107` — `renderProgressView(data: VisualizerData, th: Theme, width: number, filter?: ProgressFilter, collapsed?: Set<string>)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; // Risk Heatmap lines.push(...renderRiskHeatmap(data, th, width)); if (data.milestones.length > 0) lines.push(""); //… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:284` — `renderDepsView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:426` — `renderMetricsView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:595` — `renderTimelineView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; if (data.units.length === 0) { lines.push(th.fg("dim", "No execution history.")); return lines; } // Gantt mode for w… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:740` — `renderAgentView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:842` — `renderChangelogView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; const changelog = data.changelog; if (changelog.entries.length === 0) { lines.push(th.fg("dim", "No completed slices… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:907` — `renderExportView(_data: VisualizerData, th: Theme, _width: number, lastExportPath?: string)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(th.fg("accent", th.bold("Export Options"))); lines.push(""); lines.push(` ${th.fg("accent", "[m]")} Markdo… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:931` — `renderKnowledgeView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; const knowledge = data.knowledge; if (!knowledge.exists) { lines.push(th.fg("dim", "No KNOWLEDGE.md found")); return… (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:993` — `renderCapturesView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/visualizer-views.ts:1052` — `renderHealthView(data: VisualizerData, th: Theme, width: number)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/workflow-events.ts`
- `src/resources/extensions/gsd/workflow-events.ts:16` — `getSessionId()` — exported: **yes** — Implements logic starting with: { return ENGINE_SESSION_ID; } (missing: jsdoc)

### `src/resources/extensions/gsd/workflow-install.ts`
- `src/resources/extensions/gsd/workflow-install.ts:75` — `globalInstallDir()` — exported: **yes** — Implements logic starting with: { return join(gsdHome(), "workflows"); } (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-install.ts:79` — `projectInstallDir(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(basePath, ".gsd", "workflows"); } (missing: jsdoc)

### `src/resources/extensions/gsd/workflow-mcp-auto-prep.ts`
- `src/resources/extensions/gsd/workflow-mcp-auto-prep.ts:32` — `shouldAutoPrepareWorkflowMcp(ctx: WorkflowMcpAutoPrepContext)` — exported: **yes** — Implements logic starting with: { const provider = ctx.model?.provider; const baseUrl = ctx.model?.baseUrl; const authMode = getAuthModeSafe(ctx, provider); if (provider !== "clau… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp-auto-prep.ts:41` — `prepareWorkflowMcpForProject(ctx: WorkflowMcpAutoPrepContext, projectRoot: string)` — exported: **yes** — Implements logic starting with: { if (!shouldAutoPrepareWorkflowMcp(ctx)) return null; try { const result = ensureProjectWorkflowMcpConfig(projectRoot); if (result.status !== "unc… (missing: jsdoc)

### `src/resources/extensions/gsd/workflow-mcp.ts`
- `src/resources/extensions/gsd/workflow-mcp.ts:219` — `detectWorkflowMcpLaunchConfig(projectRoot = process.cwd(), env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp.ts:286` — `buildWorkflowMcpServers(projectRoot = process.cwd(), env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { const launch = detectWorkflowMcpLaunchConfig(projectRoot, env); if (!launch) return undefined; return { [launch.name]: { command: launch.command,… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp.ts:303` — `getRequiredWorkflowToolsForGuidedUnit(unitType: string)` — exported: **yes** — Implements logic starting with: { switch (unitType) { case "discuss-project": return ["ask_user_questions", "gsd_summary_save"]; case "discuss-requirements": return ["ask_user_que… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp.ts:331` — `getRequiredWorkflowToolsForAutoUnit(unitType: string)` — exported: **yes** — Implements logic starting with: { switch (unitType) { case "discuss-project": return ["ask_user_questions", "gsd_summary_save"]; case "discuss-requirements": return ["ask_user_que… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp.ts:370` — `usesWorkflowMcpTransport(authMode: WorkflowCapabilityOptions["authMode"], baseUrl: string | undefined)` — exported: **yes** — Implements logic starting with: { return authMode === "externalCli" && typeof baseUrl === "string" && baseUrl.startsWith("local://"); } (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp.ts:391` — `supportsStructuredQuestions(activeTools: string[], options: Pick<WorkflowCapabilityOptions, "authMode" | "baseUrl" | "env"> = {})` — exported: **yes** — Implements logic starting with: { if (!hasAskUserQuestionsTool(activeTools)) return false; if (usesWorkflowMcpTransport(options.authMode, options.baseUrl)) { // Claude Code local… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-mcp.ts:407` — `getWorkflowTransportSupportError(provider: string | undefined, requiredTools: string[], options: WorkflowCapabilityOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/workflow-protocol.ts`
- `src/resources/extensions/gsd/workflow-protocol.ts:11` — `buildWorkflowProtocolExcerpt(workflow: string, workflowPath: string, opts: { maxChars?: number } = {})` — exported: **yes** — Implements logic starting with: { const limit = opts.maxChars ?? getWorkflowProtocolExcerptLimit(); const trimmed = workflow.trim(); const excerpt = buildPrioritizedWorkflowExcerp… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-protocol.ts:36` — `buildWorkflowDispatchContent(opts: { workflow: string; workflowPath: string; task: string; maxProtocolChars?: number; })` — exported: **yes** — Implements logic starting with: { return [ "Read the following GSD workflow protocol excerpt and execute exactly. Use the source path for a full protocol read only if the excerpt… (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-protocol.ts:53` — `buildDoctorHealSummary(reportText: string, opts: { maxChars?: number } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/workflow-protocol.ts:67` — `buildDoctorHealIssuePayload(structuredIssues: string, opts: { maxIssues?: number; maxIssueChars?: number; maxChars?: number } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/gsd/workflow-templates.ts`
- `src/resources/extensions/gsd/workflow-templates.ts:37` — `isLegacyWorkflowMode(mode: WorkflowMode | undefined)` — exported: **yes** — Implements logic starting with: { return (mode ?? "markdown-phase") !== "auto-milestone"; } (missing: jsdoc)

### `src/resources/extensions/gsd/working-output-messages.ts`
- `src/resources/extensions/gsd/working-output-messages.ts:47` — `formatAutoUnitWorkingMessage(unitType: string, unitId: string)` — exported: **yes** — Implements logic starting with: { switch (unitType) { case "research-milestone": case "research-slice": return `Researching ${unitId}: waiting for provider response`; case "plan-m… (missing: jsdoc)
- `src/resources/extensions/gsd/working-output-messages.ts:65` — `evaluateWorkingOutputMessage(item: WorkingOutputMessage)` — exported: **yes** — Implements logic starting with: { const findings: WorkingOutputFinding[] = []; const message = item.message.trim(); const health = item.context?.health; if (!message) { findings.p… (missing: jsdoc)
- `src/resources/extensions/gsd/working-output-messages.ts:116` — `evaluateWorkingOutputMessages(items: WorkingOutputMessage[])` — exported: **yes** — Implements logic starting with: { return items.flatMap(evaluateWorkingOutputMessage); } (missing: jsdoc)

### `src/resources/extensions/gsd/workspace-index.ts`
- `src/resources/extensions/gsd/workspace-index.ts:148` — `indexWorkspace(basePath: string, opts: IndexWorkspaceOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/workspace-index.ts:248` — `listDoctorScopeSuggestions(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/gsd/workspace-index.ts:264` — `getSuggestedNextCommands(basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-command-bootstrap.ts`
- `src/resources/extensions/gsd/worktree-command-bootstrap.ts:43` — `registerLazyWorktreeCommands(pi: ExtensionAPI)` — exported: **yes** — Implements logic starting with: { registerLazyWorktreeAlias(pi, "worktree", "Git worktrees (also /wt): /worktree <name> | list | merge | remove"); registerLazyWorktreeAlias(pi, "w… (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-command.ts`
- `src/resources/extensions/gsd/worktree-command.ts:223` — `handleWorktreeCommand(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI, alias: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-command.ts:232` — `registerWorktreeCommand(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-lifecycle.ts`
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1312` — `WorktreeLifecycle.constructor(s: AutoSession, deps: WorktreeLifecycleDeps)` — exported: **yes** — Implements logic starting with: { this.s = s; this.deps = deps; } (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-manager.ts`
- `src/resources/extensions/gsd/worktree-manager.ts:136` — `worktreesDir(basePath: string)` — exported: **yes** — Implements logic starting with: { return join(resolveWorktreeProjectRoot(basePath), ".gsd", "worktrees"); } (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-manager.ts:140` — `worktreePath(basePath: string, name: string)` — exported: **yes** — Implements logic starting with: { return join(worktreesDir(basePath), name); } (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-manager.ts:144` — `worktreeBranchName(name: string)` — exported: **yes** — Implements logic starting with: { return `worktree/${name}`; } (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-root.ts`
- `src/resources/extensions/gsd/worktree-root.ts:10` — `normalizeWorktreePathForCompare(path: string)` — exported: **yes** — Implements logic starting with: { let normalized: string; try { normalized = realpathSync(path); } catch { normalized = resolve(path); } const slashed = normalized.replaceAll("\\"… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-root.ts:45` — `isGsdWorktreePath(path: string)` — exported: **yes** — Implements logic starting with: { return findWorktreeSegment(path.replaceAll("\\", "/")) !== null; } (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-safety.ts`
- `src/resources/extensions/gsd/worktree-safety.ts:117` — `createWorktreeSafetyModule(deps: WorktreeSafetyDeps = defaultDeps)` — exported: **yes** — Implements logic starting with: { return { validateUnitRoot(input) { if (input.writeScope === "planning-only") { return { ok: true, kind: "not-required", reason: "planning-only Un… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-safety.ts:280` — `createFsOnlyWorktreeSafetyModule()` — exported: **yes** — Implements logic starting with: { return createWorktreeSafetyModule(fsOnlyDeps); } (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-session-state.ts`
- `src/resources/extensions/gsd/worktree-session-state.ts:4` — `getWorktreeOriginalCwd()` — exported: **yes** — Implements logic starting with: { return originalCwd; } (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-session-state.ts:8` — `setWorktreeOriginalCwd(cwd: string)` — exported: **yes** — Implements logic starting with: { originalCwd = cwd; } (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-session-state.ts:12` — `clearWorktreeOriginalCwd()` — exported: **yes** — Implements logic starting with: { originalCwd = null; } (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-session-state.ts:16` — `ensureWorktreeOriginalCwdFromPath(cwd: string = process.cwd())` — exported: **yes** — Implements logic starting with: { if (originalCwd) return originalCwd; const marker = `${/\\/.test(cwd) ? "\\" : "/"}.gsd${/\\/.test(cwd) ? "\\" : "/"}worktrees${/\\/.test(cwd) ?… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-session-state.ts:26` — `getActiveWorktreeName()` — exported: **yes** — Implements logic starting with: { if (!originalCwd) return null; const cwd = process.cwd(); const wtDir = `${originalCwd.replace(/[\\/]+$/, "")}/.gsd/worktrees`.replaceAll("\\", "… (missing: jsdoc)

### `src/resources/extensions/gsd/worktree-telemetry.ts`
- `src/resources/extensions/gsd/worktree-telemetry.ts:59` — `emitWorktreeCreated(projectRoot: string, milestoneId: string, meta: { flowId?: string; reason?: WorktreeCreatedReason } = {})` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("worktree-created", { milestoneId, startedAt: now(), flowId: meta.flowId, reason: meta.reason ?? "enter-m… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:72` — `emitWorktreeMerged(projectRoot: string, milestoneId: string, meta: { flowId?: string; reason?: "milestone-complete" | "all-complete" | "stop-fallback" | "transition" | "other"; startedAt?: string; durationMs?: number; sliceCount?: number; taskCount?: number; conflict?: boolean; conflictedFiles?: number; } = {})` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("worktree-merged", { milestoneId, endedAt: now(), flowId: meta.flowId, reason: meta.reason ?? "other", st… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:100` — `emitWorktreeOrphaned(projectRoot: string, milestoneId: string, meta: { flowId?: string; reason: "in-progress-unmerged" | "complete-unmerged" | "stale-branch"; commitsAhead?: number; worktreeDirExists?: boolean; })` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("worktree-orphaned", { milestoneId, flowId: meta.flowId, reason: meta.reason, commitsAhead: meta.commitsA… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:120` — `emitAutoExit(projectRoot: string, meta: { flowId?: string; /** Must come from the closed AutoExitReason set. Callers with free-form * reasons (e.g. stopAuto's `reason?: string` parameter) should map to * the closed set before emitting. */ reason: AutoExitReason; milestoneId?: string; milestoneMerged: boolean; isolationMode?: WorktreeIsolationMode; worktreeActive?: boolean; })` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("auto-exit", { reason: meta.reason, flowId: meta.flowId, milestoneId: meta.milestoneId, milestoneMerged:… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:145` — `emitWorktreeSync(projectRoot: string, milestoneId: string, meta: { flowId?: string; filesCopied?: number; bytesCopied?: number; commitsAhead?: number; worktreeAgeMs?: number; })` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("worktree-sync", { milestoneId, flowId: meta.flowId, filesCopied: meta.filesCopied, bytesCopied: meta.byt… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:166` — `emitCanonicalRootRedirect(projectRoot: string, milestoneId: string, redirectedTo: string, meta: { flowId?: string } = {})` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("canonical-root-redirect", { milestoneId, redirectedTo, flowId: meta.flowId, })); } (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:181` — `emitSliceMerged(projectRoot: string, milestoneId: string, sliceId: string, meta: { durationMs?: number; conflict?: boolean; commitSha?: string; flowId?: string } = {})` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("slice-merged", { milestoneId, sliceId, mergedAt: now(), durationMs: meta.durationMs, conflict: meta.conf… (missing: jsdoc)
- `src/resources/extensions/gsd/worktree-telemetry.ts:198` — `emitMilestoneResquash(projectRoot: string, milestoneId: string, meta: { sliceCount: number; startSha?: string; endSha?: string; flowId?: string } = { sliceCount: 0 })` — exported: **yes** — Implements logic starting with: { emitJournalEvent(projectRoot, baseEntry("milestone-resquash", { milestoneId, sliceCount: meta.sliceCount, startSha: meta.startSha, endSha: meta.e… (missing: jsdoc)

### `src/resources/extensions/gsd/worktree.ts`
- `src/resources/extensions/gsd/worktree.ts:176` — `getCurrentBranch(basePath: string)` — exported: **yes** — Implements logic starting with: { return getService(basePath).getCurrentBranch(); } (missing: jsdoc)

### `src/resources/extensions/mcp-client/index.ts`
- `src/resources/extensions/mcp-client/index.ts:161` — `_buildMcpChildEnvForTest(configEnv: Record<string, string> | undefined)` — exported: **yes** — Implements logic starting with: { const childEnv: Record<string, string> = {}; for (const key of CHILD_ENV_ALLOWLIST) { const value = process.env[key]; if (typeof value === "strin… (missing: jsdoc)
- `src/resources/extensions/mcp-client/index.ts:173` — `_buildMcpTrustConfirmOptionsForTest(signal?: AbortSignal)` — exported: **yes** — Implements logic starting with: { return signal ? { timeout: 120_000, signal } : { timeout: 120_000 }; } (missing: jsdoc)
- `src/resources/extensions/mcp-client/index.ts:211` — `getServerConfig(name: string)` — exported: **yes** — Implements logic starting with: { const trimmed = name.trim(); return readConfigs().find((s) => s.name === trimmed || s.name.toLowerCase() === trimmed.toLowerCase(), ); } (missing: jsdoc)

### `src/resources/extensions/ollama/index.ts`
- `src/resources/extensions/ollama/index.ts:102` — `ollama(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/ollama/ollama-chat-provider.ts`
- `src/resources/extensions/ollama/ollama-chat-provider.ts:54` — `streamOllamaChat(model: Model<Api>, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/ollama/ollama-commands.ts`
- `src/resources/extensions/ollama/ollama-commands.ts:20` — `registerOllamaCommands(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/ollama/ollama-tool.ts`
- `src/resources/extensions/ollama/ollama-tool.ts:22` — `registerOllamaTool(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/ollama/thinking-parser.ts`
- `src/resources/extensions/ollama/thinking-parser.ts:20` — `class ThinkingTagParser` — exported: **yes** — Defines an API surface. (missing: jsdoc)

### `src/resources/extensions/remote-questions/config.ts`
- `src/resources/extensions/remote-questions/config.ts:77` — `resolveRemoteConfig()` — exported: **yes** — Implements logic starting with: { hydrateRemoteTokensFromAuth(); const prefs = loadEffectiveGSDPreferences(); const rq: RemoteQuestionsConfig | undefined = prefs?.preferences.remo… (missing: jsdoc)
- `src/resources/extensions/remote-questions/config.ts:102` — `getRemoteConfigStatus()` — exported: **yes** — Implements logic starting with: { hydrateRemoteTokensFromAuth(); const prefs = loadEffectiveGSDPreferences(); const rq: RemoteQuestionsConfig | undefined = prefs?.preferences.remo… (missing: jsdoc)
- `src/resources/extensions/remote-questions/config.ts:118` — `isValidChannelId(channel: RemoteChannel, id: string)` — exported: **yes** — Implements logic starting with: { return CHANNEL_ID_PATTERNS[channel].test(id); } (missing: jsdoc)

### `src/resources/extensions/remote-questions/discord-adapter.ts`
- `src/resources/extensions/remote-questions/discord-adapter.ts:11` — `class DiscordAdapter` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/remote-questions/discord-adapter.ts:18` — `DiscordAdapter.constructor(token: string, channelId: string)` — exported: **yes** — Implements logic starting with: { this.token = token; this.channelId = channelId; } (missing: jsdoc)
- `src/resources/extensions/remote-questions/discord-adapter.ts:23` — `DiscordAdapter.validate()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/remote-questions/discord-adapter.ts:40` — `DiscordAdapter.sendPrompt(prompt: RemotePrompt)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/remote-questions/discord-adapter.ts:76` — `DiscordAdapter.pollAnswer(prompt: RemotePrompt, ref: RemotePromptRef)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/remote-questions/format.ts`
- `src/resources/extensions/remote-questions/format.ts:25` — `formatForSlack(prompt: RemotePrompt)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/format.ts:92` — `formatForDiscord(prompt: RemotePrompt)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/format.ts:126` — `parseSlackReply(text: string, questions: RemoteQuestion[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/format.ts:146` — `parseDiscordResponse(reactions: Array<{ emoji: string; count: number }>, replyText: string | null, questions: RemoteQuestion[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/format.ts:174` — `parseSlackReactionResponse(reactionNames: string[], questions: RemoteQuestion[])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/format.ts:218` — `formatForTelegram(prompt: RemotePrompt)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/format.ts:262` — `parseTelegramResponse(callbackData: string | null, replyText: string | null, questions: RemoteQuestion[], promptId: string)` — exported: **yes** — Implements logic starting with: { // Handle callback_data from inline keyboard button press if (callbackData) { const match = callbackData.match(new RegExp(`^${promptId.replace(/[… (missing: jsdoc)

### `src/resources/extensions/remote-questions/manager.ts`
- `src/resources/extensions/remote-questions/manager.ts:101` — `tryRemoteQuestions(questions: QuestionInput[], signal?: AbortSignal, basePath?: string)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/resources/extensions/remote-questions/remote-command.ts`
- `src/resources/extensions/remote-questions/remote-command.ts:16` — `handleRemote(subcommand: string, ctx: ExtensionCommandContext, _pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/remote-questions/remote-command.ts:319` — `saveRemoteQuestionsConfig(channel: "slack" | "discord" | "telegram", channelId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/remote-questions/slack-adapter.ts`
- `src/resources/extensions/remote-questions/slack-adapter.ts:12` — `class SlackAdapter` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/remote-questions/slack-adapter.ts:18` — `SlackAdapter.constructor(token: string, channelId: string)` — exported: **yes** — Implements logic starting with: { this.token = token; this.channelId = channelId; } (missing: jsdoc)
- `src/resources/extensions/remote-questions/slack-adapter.ts:23` — `SlackAdapter.validate()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/remote-questions/slack-adapter.ts:29` — `SlackAdapter.sendPrompt(prompt: RemotePrompt)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/remote-questions/slack-adapter.ts:63` — `SlackAdapter.pollAnswer(prompt: RemotePrompt, ref: RemotePromptRef)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/remote-questions/slack-adapter.ts:86` — `SlackAdapter.acknowledgeAnswer(ref: RemotePromptRef)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/remote-questions/status.ts`
- `src/resources/extensions/remote-questions/status.ts:16` — `getLatestPromptSummary()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/remote-questions/store.ts`
- `src/resources/extensions/remote-questions/store.ts:18` — `createPromptRecord(prompt: RemotePrompt)` — exported: **yes** — Implements logic starting with: { return { version: 1, id: prompt.id, createdAt: prompt.createdAt, updatedAt: Date.now(), status: "pending", channel: prompt.channel, timeoutAt: pr… (missing: jsdoc)
- `src/resources/extensions/remote-questions/store.ts:33` — `writePromptRecord(record: RemotePromptRecord)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/remote-questions/store.ts:38` — `readPromptRecord(id: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/remote-questions/store.ts:48` — `updatePromptRecord(id: string, updates: Partial<RemotePromptRecord>)` — exported: **yes** — Implements logic starting with: { const current = readPromptRecord(id); if (!current) return null; const merged = { ...current, ...updates, updatedAt: Date.now(), }; // After spre… (missing: jsdoc)
- `src/resources/extensions/remote-questions/store.ts:67` — `markPromptDispatched(id: string, ref: RemotePromptRef)` — exported: **yes** — Implements logic starting with: { return updatePromptRecord(id, { ref, status: "pending" }); } (missing: jsdoc)
- `src/resources/extensions/remote-questions/store.ts:71` — `markPromptAnswered(id: string, response: RemoteAnswer)` — exported: **yes** — Implements logic starting with: { return updatePromptRecord(id, { response, status: "answered", lastPollAt: Date.now() }); } (missing: jsdoc)
- `src/resources/extensions/remote-questions/store.ts:75` — `markPromptStatus(id: string, status: RemotePromptStatus, lastError?: string)` — exported: **yes** — Implements logic starting with: { return updatePromptRecord(id, { status, lastPollAt: Date.now(), ...(lastError ? { lastError } : {}), }); } (missing: jsdoc)

### `src/resources/extensions/remote-questions/telegram-adapter.ts`
- `src/resources/extensions/remote-questions/telegram-adapter.ts:12` — `class TelegramAdapter` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/remote-questions/telegram-adapter.ts:21` — `TelegramAdapter.constructor(token: string, chatId: string, basePath: string)` — exported: **yes** — Implements logic starting with: { this.token = token; this.chatId = chatId; this.basePath = basePath; } (missing: jsdoc)
- `src/resources/extensions/remote-questions/telegram-adapter.ts:27` — `TelegramAdapter.validate()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/remote-questions/telegram-adapter.ts:33` — `TelegramAdapter.sendPrompt(prompt: RemotePrompt)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/remote-questions/telegram-adapter.ts:65` — `TelegramAdapter.pollAnswer(prompt: RemotePrompt, ref: RemotePromptRef)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/search-the-web/cache.ts`
- `src/resources/extensions/search-the-web/cache.ts:16` — `LRUTTLCache.constructor(options: { max: number; ttlMs: number })` — exported: **yes** — Implements logic starting with: { this.max = options.max; this.ttlMs = options.ttlMs; } (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:21` — `LRUTTLCache.get(key: string)` — exported: **yes** — Implements logic starting with: { const entry = this.store.get(key); if (!entry) return undefined; if (Date.now() > entry.expiresAt) { this.store.delete(key); return undefined; }… (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:34` — `LRUTTLCache.set(key: string, value: V)` — exported: **yes** — Implements logic starting with: { if (this.store.has(key)) { this.store.delete(key); } else if (this.store.size >= this.max) { const oldest = this.store.keys().next().value; if (o… (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:44` — `LRUTTLCache.has(key: string)` — exported: **yes** — Implements logic starting with: { return this.get(key) !== undefined; } (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:48` — `LRUTTLCache.purgeStale()` — exported: **yes** — Implements logic starting with: { const now = Date.now(); for (const [key, entry] of this.store) { if (now > entry.expiresAt) this.store.delete(key); } } (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:55` — `LRUTTLCache.startPurgeInterval(intervalMs: number)` — exported: **yes** — Implements logic starting with: { if (this.purgeTimer !== null) return; this.purgeTimer = setInterval(() => this.purgeStale(), intervalMs); // Don't keep the process alive just fo… (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:64` — `LRUTTLCache.stopPurgeInterval()` — exported: **yes** — Implements logic starting with: { if (this.purgeTimer !== null) { clearInterval(this.purgeTimer); this.purgeTimer = null; } } (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:71` — `LRUTTLCache.clear()` — exported: **yes** — Implements logic starting with: { this.store.clear(); } (missing: jsdoc)
- `src/resources/extensions/search-the-web/cache.ts:75` — `LRUTTLCache.get size()` — exported: **yes** — Implements logic starting with: { return this.store.size; } (missing: jsdoc)

### `src/resources/extensions/search-the-web/command-search-provider.ts`
- `src/resources/extensions/search-the-web/command-search-provider.ts:48` — `registerSearchProviderCommand(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/search-the-web/http.ts`
- `src/resources/extensions/search-the-web/http.ts:14` — `HttpError.constructor(message: string, statusCode: number, response?: Response)` — exported: **yes** — Implements logic starting with: { super(message); this.name = "HttpError"; this.statusCode = statusCode; this.response = response; Object.setPrototypeOf(this, HttpError.prototype)… (missing: jsdoc)
- `src/resources/extensions/search-the-web/http.ts:33` — `classifyError(err: unknown)` — exported: **yes** — Implements logic starting with: { if (err instanceof HttpError) { const code = err.statusCode; if (code === 401 || code === 403) { return { kind: "auth_error", message: `HTTP ${co… (missing: jsdoc)

### `src/resources/extensions/search-the-web/tool-fetch-page.ts`
- `src/resources/extensions/search-the-web/tool-fetch-page.ts:360` — `registerFetchPageTool(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/search-the-web/tool-llm-context.ts`
- `src/resources/extensions/search-the-web/tool-llm-context.ts:276` — `registerLLMContextTool(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/search-the-web/tool-search.ts`
- `src/resources/extensions/search-the-web/tool-search.ts:309` — `registerSearchTool(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/search-the-web/url-utils.ts`
- `src/resources/extensions/search-the-web/url-utils.ts:42` — `isBlockedUrl(url: string)` — exported: **yes** — Implements logic starting with: { try { const parsed = new URL(url); if (parsed.protocol !== "https:" && parsed.protocol !== "http:") return true; const hostname = parsed.hostname… (missing: jsdoc)

### `src/resources/extensions/shared/gsd-phase-state.ts`
- `src/resources/extensions/shared/gsd-phase-state.ts:44` — `configureGSDPhaseAudit(context: GSDPhaseAuditContext | null)` — exported: **yes** — Implements logic starting with: { _auditContext = context; } (missing: jsdoc)

### `src/resources/extensions/shared/interview-ui.ts`
- `src/resources/extensions/shared/interview-ui.ts:131` — `showWrapUpScreen(opts: WrapUpOptions, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/resources/extensions/shared/interview-ui.ts:198` — `showInterviewRound(questions: Question[], opts: InterviewRoundOptions, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/shared/rtk-session-stats.ts`
- `src/resources/extensions/shared/rtk-session-stats.ts:106` — `readCurrentRtkGainSummary(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/shared/rtk-session-stats.ts:166` — `ensureRtkSessionBaseline(basePath: string, sessionId: string, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { if (!sessionId) return null; const current = readCurrentRtkGainSummary(env); if (!current) return null; const store = loadBaselineStore(basePath)… (missing: jsdoc)
- `src/resources/extensions/shared/rtk-session-stats.ts:190` — `getRtkSessionSavings(basePath: string, sessionId: string | null | undefined, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { if (!sessionId) return null; const current = readCurrentRtkGainSummary(env); if (!current) return null; const store = loadBaselineStore(basePath)… (missing: jsdoc)
- `src/resources/extensions/shared/rtk-session-stats.ts:233` — `clearRtkSessionBaseline(basePath: string, sessionId: string)` — exported: **yes** — Implements logic starting with: { if (!sessionId) return; const store = loadBaselineStore(basePath); if (!(sessionId in store.sessions)) return; delete store.sessions[sessionId];… (missing: jsdoc)
- `src/resources/extensions/shared/rtk-session-stats.ts:241` — `formatRtkSavingsLabel(savings: RtkSessionSavings | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!savings) return null; if (savings.commands <= 0) return "rtk: waiting for shell usage"; if (savings.inputTokens <= 0 && savings.outputTokens… (missing: jsdoc)

### `src/resources/extensions/shared/rtk-shared.ts`
- `src/resources/extensions/shared/rtk-shared.ts:9` — `isTruthy(value: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!value) return false; const normalized = value.trim().toLowerCase(); return normalized === "1" || normalized === "true" || normalized === "ye… (missing: jsdoc)
- `src/resources/extensions/shared/rtk-shared.ts:15` — `isRtkEnabled(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return !isTruthy(env[GSD_RTK_DISABLED_ENV]); } (missing: jsdoc)
- `src/resources/extensions/shared/rtk-shared.ts:19` — `getManagedRtkDir(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return join(env.GSD_HOME || join(osHomedir(), ".gsd"), "agent", "bin"); } (missing: jsdoc)
- `src/resources/extensions/shared/rtk-shared.ts:23` — `getRtkBinaryName(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { return platform === "win32" ? "rtk.exe" : "rtk"; } (missing: jsdoc)
- `src/resources/extensions/shared/rtk-shared.ts:27` — `getPathValue(env: NodeJS.ProcessEnv)` — exported: **yes** — Implements logic starting with: { const pathKey = Object.keys(env).find((key) => key.toLowerCase() === "path"); return pathKey ? env[pathKey] : env.PATH; } (missing: jsdoc)
- `src/resources/extensions/shared/rtk-shared.ts:32` — `resolvePathCandidates(pathValue: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/shared/rtk-shared.ts:40` — `resolveSystemRtkPath(pathValue: string | undefined, platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { const candidates = platform === "win32" ? ["rtk.exe", "rtk.cmd", "rtk.bat", "rtk"] : ["rtk"]; for (const dir of resolvePathCandidates(pathValue))… (missing: jsdoc)

### `src/resources/extensions/shared/rtk.ts`
- `src/resources/extensions/shared/rtk.ts:28` — `buildRtkEnv(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return { ...env, [RTK_TELEMETRY_DISABLED_ENV]: "1", }; } (missing: jsdoc)
- `src/resources/extensions/shared/rtk.ts:42` — `resolveRtkBinaryPath(options: ResolveRtkBinaryPathOptions = {})` — exported: **yes** — Implements logic starting with: { const env = options.env ?? process.env; const platform = options.platform ?? process.platform; const explicitPath = options.binaryPath ?? env[GSD… (missing: jsdoc)
- `src/resources/extensions/shared/rtk.ts:74` — `rewriteCommandWithRtk(command: string, options: RewriteCommandOptions = {})` — exported: **yes** — Implements logic starting with: { const env = options.env ?? process.env; if (!command.trim()) return command; if (!isRtkEnabled(env)) return command; const binaryPath = options.b… (missing: jsdoc)

### `src/resources/extensions/shared/sanitize.ts`
- `src/resources/extensions/shared/sanitize.ts:16` — `sanitizeError(msg: string)` — exported: **yes** — Implements logic starting with: { let sanitized = msg; for (const pattern of TOKEN_PATTERNS) { sanitized = sanitized.replace(pattern, "[REDACTED]"); } return sanitized; } (missing: jsdoc)

### `src/resources/extensions/shared/terminal.ts`
- `src/resources/extensions/shared/terminal.ts:10` — `isCmuxTerminal(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return Boolean(env.CMUX_WORKSPACE_ID && env.CMUX_SURFACE_ID); } (missing: jsdoc)
- `src/resources/extensions/shared/terminal.ts:14` — `supportsCtrlAltShortcuts()` — exported: **yes** — Implements logic starting with: { const term = (process.env.TERM_PROGRAM || "").toLowerCase(); const jetbrains = (process.env.TERMINAL_EMULATOR || "").toLowerCase().includes("jetb… (missing: jsdoc)

### `src/resources/extensions/slash-commands/audit.ts`
- `src/resources/extensions/slash-commands/audit.ts:4` — `auditCommand(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/slash-commands/clear.ts`
- `src/resources/extensions/slash-commands/clear.ts:3` — `clearCommand(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/slash-commands/create-extension.ts`
- `src/resources/extensions/slash-commands/create-extension.ts:4` — `createExtension(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/slash-commands/create-slash-command.ts`
- `src/resources/extensions/slash-commands/create-slash-command.ts:4` — `createSlashCommand(pi: ExtensionAPI)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/resources/extensions/slash-commands/index.ts`
- `src/resources/extensions/slash-commands/index.ts:7` — `slashCommands(pi: ExtensionAPI)` — exported: **yes** — Implements logic starting with: { createSlashCommand(pi); createExtension(pi); auditCommand(pi); clearCommand(pi); } (missing: jsdoc)

### `src/resources/extensions/subagent/agents.ts`
- `src/resources/extensions/subagent/agents.ts:37` — `parseConflictsWith(value: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/subagent/agents.ts:137` — `discoverAgents(cwd: string, scope: AgentScope)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)
- `src/resources/extensions/subagent/agents.ts:158` — `formatAgentList(agents: AgentConfig[], maxItems: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/subagent/isolation.ts`
- `src/resources/extensions/subagent/isolation.ts:57` — `encodeCwd(cwd: string)` — exported: **yes** — Implements logic starting with: { // Encode the entire cwd so Windows drive letters, separators, and UNC // prefixes cannot leak into the isolation path. return Buffer.from(cwd, "… (missing: jsdoc)
- `src/resources/extensions/subagent/isolation.ts:239` — `createWorktreeIsolation(repoRoot: string, taskId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/subagent/isolation.ts:309` — `createFuseOverlayIsolation(repoRoot: string, taskId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/subagent/isolation.ts:417` — `createIsolation(repoRoot: string, taskId: string, mode: IsolationMode)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/subagent/isolation.ts:436` — `mergeDeltaPatches(repoRoot: string, patches: DeltaPatch[])` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/subagent/isolation.ts:492` — `readIsolationMode()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/resources/extensions/subagent/launch.ts`
- `src/resources/extensions/subagent/launch.ts:42` — `isSubagentChildProcess(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return env[SUBAGENT_CHILD_ENV_VAR] === SUBAGENT_CHILD_ENV_VALUE; } (missing: jsdoc)
- `src/resources/extensions/subagent/launch.ts:46` — `buildSubagentProcessEnv(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return { ...env, [SUBAGENT_CHILD_ENV_VAR]: SUBAGENT_CHILD_ENV_VALUE, }; } (missing: jsdoc)
- `src/resources/extensions/subagent/launch.ts:53` — `buildShellEnvAssignments(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `src/resources/extensions/subagent/launch.ts:58` — `buildSubagentProcessArgs(agent: AgentConfig, task: string, tmpPromptPath: string | null, modelOverride?: string, session: SubagentSessionArgs = { mode: "fresh" })` — exported: **yes** — Implements logic starting with: { const args: string[] = ["--mode", "json", "-p"]; if (session.mode === "fork") { args.push("--session", session.sessionFile); if (session.sessionD… (missing: jsdoc)
- `src/resources/extensions/subagent/launch.ts:80` — `resolveSubagentSessionArgs(contextMode: SubagentContextMode = "fresh", parentSessionManager?: SubagentParentSessionManager)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/subagent/launch.ts:117` — `createSubagentLaunchPlan(input: SubagentLaunchInput)` — exported: **yes** — Implements logic starting with: { const session = input.session ?? resolveSubagentSessionArgs(input.contextMode ?? "fresh", input.parentSessionManager); return { args: buildSubage… (missing: jsdoc)

### `src/resources/extensions/subagent/run-store.ts`
- `src/resources/extensions/subagent/run-store.ts:61` — `defaultSubagentRunStoreDir()` — exported: **yes** — Implements logic starting with: { return path.join(getAgentDir(), "subagent-runs"); } (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:99` — `createSubagentTrackingName(existingNames: ReadonlySet<string> = new Set())` — exported: **yes** — Implements logic starting with: { const maxCombinations = TRACKING_ADJECTIVES.length * TRACKING_NOUNS.length; for (let attempt = 0; attempt < maxCombinations * 2; attempt++) { con… (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:113` — `class SubagentRunStore` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:116` — `SubagentRunStore.constructor(baseDir: string = defaultSubagentRunStoreDir())` — exported: **yes** — Implements logic starting with: { this.baseDir = baseDir; } (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:120` — `SubagentRunStore.getBaseDir()` — exported: **yes** — Implements logic starting with: { return this.baseDir; } (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:124` — `SubagentRunStore.create(record: SubagentRunRecord)` — exported: **yes** — Implements logic starting with: { this.write(record); return record; } (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:129` — `SubagentRunStore.get(runId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:149` — `SubagentRunStore.update(runId: string, updater: (record: SubagentRunRecord) => SubagentRunRecord)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:161` — `SubagentRunStore.list()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:184` — `createInitialRunRecord(input: { runId: string; mode: SubagentRunMode; contextMode: "fresh" | "fork"; cwd: string; children: Array<{ agent: string; trackingName?: string; task: string; cwd?: string }>; now?: string; })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/subagent/run-store.ts:213` — `deriveRunStatus(children: readonly SubagentChildArtifact[])` — exported: **yes** — Implements logic starting with: { if (children.some((child) => child.status === "interrupted")) return "interrupted"; if (children.some((child) => child.status === "failed")) retu… (missing: jsdoc)

### `src/resources/extensions/ttsr/ttsr-manager.ts`
- `src/resources/extensions/ttsr/ttsr-manager.ts:120` — `class TtsrManager` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:131` — `TtsrManager.constructor(settings?: TtsrSettings)` — exported: **yes** — Implements logic starting with: { this.#settings = { ...DEFAULT_SETTINGS, ...settings }; } (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:135` — `TtsrManager.#canTrigger(ruleName: string)` — exported: **yes** — Implements logic starting with: { const record = this.#injectionRecords.get(ruleName); if (!record) return true; if (this.#settings.repeatMode === "once") return false; const gap… (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:143` — `TtsrManager.#compileConditions(rule: Rule)` — exported: **yes** — Logs diagnostic information. (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:155` — `TtsrManager.#compileGlobalPathMatchers(globs: Rule["globs"])` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:164` — `TtsrManager.#parseToolScopeToken(token: string)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:183` — `TtsrManager.#buildScope(rule: Rule)` — exported: **yes** — Implements logic starting with: { if (!rule.scope || rule.scope.length === 0) { return { allowText: DEFAULT_SCOPE.allowText, allowThinking: DEFAULT_SCOPE.allowThinking, allowAnyTo… (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:232` — `TtsrManager.#hasReachableScope(scope: TtsrScope)` — exported: **yes** — Implements logic starting with: { return scope.allowText || scope.allowThinking || scope.allowAnyTool || scope.toolScopes.length > 0; } (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:236` — `TtsrManager.#bufferKey(context: TtsrMatchContext)` — exported: **yes** — Implements logic starting with: { if (context.streamKey && context.streamKey.trim().length > 0) return context.streamKey; if (context.source !== "tool") return context.source; con… (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:243` — `TtsrManager.#normalizePath(pathValue: string)` — exported: **yes** — Implements logic starting with: { return pathValue.replaceAll("\\", "/"); } (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:247` — `TtsrManager.#matchesGlob(matcher: PicomatchMatcher, filePaths: string[] | undefined)` — exported: **yes** — Implements logic starting with: { if (!filePaths || filePaths.length === 0) return false; for (const filePath of filePaths) { const normalized = this.#normalizePath(filePath); if… (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:259` — `TtsrManager.#matchesGlobalPaths(entry: TtsrEntry, context: TtsrMatchContext)` — exported: **yes** — Implements logic starting with: { if (!entry.globalPathMatchers || entry.globalPathMatchers.length === 0) return true; for (const matcher of entry.globalPathMatchers) { if (this.#… (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:267` — `TtsrManager.#matchesScope(entry: TtsrEntry, context: TtsrMatchContext)` — exported: **yes** — Implements logic starting with: { if (context.source === "text") return entry.scope.allowText; if (context.source === "thinking") return entry.scope.allowThinking; if (entry.scope… (missing: jsdoc)
- `src/resources/extensions/ttsr/ttsr-manager.ts:281` — `TtsrManager.#matchesCondition(entry: TtsrEntry, streamBuffer: string)` — exported: **yes** — Implements logic starting with: { for (const condition of entry.conditions) { condition.lastIndex = 0; if (condition.test(streamBuffer)) return true; } return false; } (missing: jsdoc)

### `src/resources/extensions/universal-config/index.ts`
- `src/resources/extensions/universal-config/index.ts:26` — `universalConfig(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/visual-brief/artifact-policy.ts`
- `src/resources/extensions/visual-brief/artifact-policy.ts:14` — `getVisualBriefOutputDir(agentDir: string = getAgentDir())` — exported: **yes** — Implements logic starting with: { return join(agentDir, "diagrams"); } (missing: jsdoc)
- `src/resources/extensions/visual-brief/artifact-policy.ts:18` — `createVisualBriefArtifactPolicy(outputDir: string)` — exported: **yes** — Implements logic starting with: { return { outputDir, filenameStyle: "Use a descriptive kebab-case filename ending in .html.", fileShape: "Produce one self-contained responsive HT… (missing: jsdoc)
- `src/resources/extensions/visual-brief/artifact-policy.ts:32` — `formatArtifactPolicy(policy: VisualBriefArtifactPolicy)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/visual-brief/index.ts`
- `src/resources/extensions/visual-brief/index.ts:5` — `visualBrief(_pi: ExtensionAPI)` — exported: **yes** — Implements logic starting with: { // Visual Brief is invoked through /gsd brief. This module keeps the bundled // extension discoverable without adding a second top-level slash co… (missing: jsdoc)

### `src/resources/extensions/visual-brief/page-contract.ts`
- `src/resources/extensions/visual-brief/page-contract.ts:23` — `getVisualBriefModeProfile(mode: VisualBriefMode, slides: boolean)` — exported: **yes** — Implements logic starting with: { if (slides || mode === "slides") { return { goal: "Turn the subject into a concise, visually paced deck that someone can present or skim quickly.… (missing: jsdoc)
- `src/resources/extensions/visual-brief/page-contract.ts:132` — `formatPageRules(rules: readonly string[] = VISUAL_BRIEF_PAGE_RULES)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/resources/extensions/visual-brief/prompts.ts`
- `src/resources/extensions/visual-brief/prompts.ts:66` — `parseVisualBriefArgs(args: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/visual-brief/prompts.ts:91` — `buildVisualBriefPrompt(request: VisualBriefRequest, options: { outputDir: string })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/resources/extensions/visual-brief/prompts.ts:130` — `getVisualBriefCompletions(prefix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/rtk-shared.js`
- `src/rtk-shared.js:9` — `isTruthy(value)` — exported: **yes** — Implements logic starting with: { if (!value) return false; const normalized = value.trim().toLowerCase(); return normalized === "1" || normalized === "true" || normalized === "ye… (missing: jsdoc)
- `src/rtk-shared.js:15` — `isRtkEnabled(env = process.env)` — exported: **yes** — Implements logic starting with: { return !isTruthy(env[GSD_RTK_DISABLED_ENV]); } (missing: jsdoc)
- `src/rtk-shared.js:19` — `getManagedRtkDir(env = process.env)` — exported: **yes** — Implements logic starting with: { return join(env.GSD_HOME || join(osHomedir(), ".gsd"), "agent", "bin"); } (missing: jsdoc)
- `src/rtk-shared.js:23` — `getRtkBinaryName(platform = process.platform)` — exported: **yes** — Implements logic starting with: { return platform === "win32" ? "rtk.exe" : "rtk"; } (missing: jsdoc)
- `src/rtk-shared.js:27` — `getPathValue(env)` — exported: **yes** — Implements logic starting with: { const pathKey = Object.keys(env).find((key) => key.toLowerCase() === "path"); return pathKey ? env[pathKey] : env.PATH; } (missing: jsdoc)
- `src/rtk-shared.js:32` — `resolvePathCandidates(pathValue)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/rtk-shared.js:40` — `resolveSystemRtkPath(pathValue, platform = process.platform)` — exported: **yes** — Implements logic starting with: { const candidates = platform === "win32" ? ["rtk.exe", "rtk.cmd", "rtk.bat", "rtk"] : ["rtk"]; for (const dir of resolvePathCandidates(pathValue))… (missing: jsdoc)
- `src/rtk-shared.js:57` — `prependPathEntry(env, entry)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/rtk-shared.js:67` — `applyRtkProcessEnv(env = process.env)` — exported: **yes** — Implements logic starting with: { prependPathEntry(env, getManagedRtkDir(env)); env[RTK_TELEMETRY_DISABLED_ENV] = "1"; return env; } (missing: jsdoc)
- `src/rtk-shared.js:73` — `buildRtkEnv(env = process.env)` — exported: **yes** — Implements logic starting with: { return applyRtkProcessEnv({ ...env }); } (missing: jsdoc)

### `src/rtk-shared.ts`
- `src/rtk-shared.ts:9` — `isTruthy(value: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!value) return false; const normalized = value.trim().toLowerCase(); return normalized === "1" || normalized === "true" || normalized === "ye… (missing: jsdoc)
- `src/rtk-shared.ts:15` — `isRtkEnabled(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return !isTruthy(env[GSD_RTK_DISABLED_ENV]); } (missing: jsdoc)
- `src/rtk-shared.ts:19` — `getManagedRtkDir(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return join(env.GSD_HOME || join(osHomedir(), ".gsd"), "agent", "bin"); } (missing: jsdoc)
- `src/rtk-shared.ts:23` — `getRtkBinaryName(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { return platform === "win32" ? "rtk.exe" : "rtk"; } (missing: jsdoc)
- `src/rtk-shared.ts:27` — `getPathValue(env: NodeJS.ProcessEnv)` — exported: **yes** — Implements logic starting with: { const pathKey = Object.keys(env).find((key) => key.toLowerCase() === "path"); return pathKey ? env[pathKey] : env.PATH; } (missing: jsdoc)
- `src/rtk-shared.ts:32` — `resolvePathCandidates(pathValue: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/rtk-shared.ts:40` — `resolveSystemRtkPath(pathValue: string | undefined, platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { const candidates = platform === "win32" ? ["rtk.exe", "rtk.cmd", "rtk.bat", "rtk"] : ["rtk"]; for (const dir of resolvePathCandidates(pathValue))… (missing: jsdoc)
- `src/rtk-shared.ts:60` — `prependPathEntry(env: NodeJS.ProcessEnv, entry: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/rtk-shared.ts:70` — `applyRtkProcessEnv(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { prependPathEntry(env, getManagedRtkDir(env)); env[RTK_TELEMETRY_DISABLED_ENV] = "1"; return env; } (missing: jsdoc)
- `src/rtk-shared.ts:76` — `buildRtkEnv(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { return applyRtkProcessEnv({ ...env }); } (missing: jsdoc)

### `src/rtk.ts`
- `src/rtk.ts:60` — `getManagedRtkPath(platform: NodeJS.Platform = process.platform, targetDir: string = getManagedRtkDir())` — exported: **yes** — Implements logic starting with: { return join(targetDir, getRtkBinaryName(platform)); } (missing: jsdoc)
- `src/rtk.ts:67` — `resolveRtkAssetName(platform: NodeJS.Platform, arch: string, version: string = RTK_VERSION)` — exported: **yes** — Implements logic starting with: { void version; if (platform === "darwin" && arch === "arm64") return "rtk-aarch64-apple-darwin.tar.gz"; if (platform === "darwin" && arch === "x64… (missing: jsdoc)
- `src/rtk.ts:179` — `resolveRtkBinaryPath(options: ResolveRtkBinaryPathOptions = {})` — exported: **yes** — Implements logic starting with: { const env = options.env ?? process.env; const platform = options.platform ?? process.platform; if (options.binaryPath) return options.binaryPath;… (missing: jsdoc)
- `src/rtk.ts:213` — `rewriteCommandWithRtk(command: string, options: RewriteCommandOptions = {})` — exported: **yes** — Implements logic starting with: { if (!command.trim()) return command; if (!isRtkEnabled(options.env ?? process.env)) return command; const env = options.env ?? process.env; const… (missing: jsdoc)
- `src/rtk.ts:247` — `validateRtkBinary(binaryPath: string, options: ValidateRtkBinaryOptions = {})` — exported: **yes** — Implements logic starting with: { const run = options.spawnSyncImpl ?? spawnSync; const result = run(binaryPath, ["rewrite", "git status"], { encoding: "utf-8", env: buildRtkEnv(o… (missing: jsdoc)
- `src/rtk.ts:261` — `ensureRtkAvailable(options: EnsureRtkOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/rtk.ts:360` — `bootstrapRtk(options: EnsureRtkOptions = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/security-overrides.ts`
- `src/security-overrides.ts:14` — `applySecurityOverrides(settingsManager: SettingsManager)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/startup-timings.ts`
- `src/startup-timings.ts:7` — `markStartup(label: string)` — exported: **yes** — Implements logic starting with: { if (!ENABLED) return; const now = Date.now(); timings.push({ label, ms: now - lastTime }); lastTime = now; } (missing: jsdoc)
- `src/startup-timings.ts:14` — `printStartupTimings()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/tests/fetch-test-helpers.ts`
- `src/tests/fetch-test-helpers.ts:17` — `parseJsonBody(body: BodyInit | null | undefined)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)

### `src/tests/integration/web-mode-runtime-fixtures.ts`
- `src/tests/integration/web-mode-runtime-fixtures.ts:128` — `makeRuntimeWorkspaceFixture()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-fixtures.ts:157` — `makeInterruptedRunRuntimeFixture()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-fixtures.ts:215` — `seedCurrentProjectSession(options: { projectCwd: string baseSessionsDir: string sessionId: string name: string baseTimestamp: string })` — exported: **yes** — Implements logic starting with: { const targetSessionDirs = resolveSeedTargetSessionDirs(options.projectCwd, options.baseSessionsDir) let session: SeededRuntimeSession | null = nu… (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-fixtures.ts:250` — `seedInterruptedRunRecoverySessions(options: { projectCwd: string baseSessionsDir: string })` — exported: **yes** — Implements logic starting with: { const targetSessionDirs = resolveSeedTargetSessionDirs(options.projectCwd, options.baseSessionsDir) let alternateSession: SeededRuntimeSession |… (missing: jsdoc)

### `src/tests/integration/web-mode-runtime-harness.ts`
- `src/tests/integration/web-mode-runtime-harness.ts:110` — `writePreseededAuthFile(tempHome: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:143` — `ensureRuntimeArtifacts()` — exported: **yes** — Implements logic starting with: { if (runtimeArtifactsReady) return if (!existsSync(builtAgentEntryPath)) { runNpmScript(["run", "build:pi"], "npm run build:pi") } if (!existsSync… (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:157` — `parseStartedUrl(stderr: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:170` — `launchPackagedWebHost(options: { launchCwd: string tempHome: string browserLogPath?: string env?: NodeJS.ProcessEnv timeoutMs?: number })` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:268` — `waitForHttpOk(url: string, timeoutMs = 60_000, headers?: Record<string, string>)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:298` — `killProcessOnPort(port: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:333` — `assertBrowserOpenAttempt(browserLogPath: string, expectedUrl: string, timeoutMs = 5_000)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:354` — `fetchBootInPage(page: Page)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:371` — `readFirstSseEventInPage(page: Page, timeoutMs = 15_000)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)
- `src/tests/integration/web-mode-runtime-harness.ts:502` — `waitForLaunchedHostReady(page: Page, options: { label: string expectedProjectCwd: string expectedSessionsDir?: string | string[] launchStderr?: string navigation?: () => Promise<unknown> timeoutMs?: number })` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `src/tests/rtk-test-utils.ts`
- `src/tests/rtk-test-utils.ts:11` — `createFakeRtk(mapping: Record<string, FakeRtkResponse>)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `src/tool-bootstrap.ts`
- `src/tool-bootstrap.ts:80` — `resolveToolFromPath(tool: ManagedTool, pathValue: string | undefined = process.env.PATH)` — exported: **yes** — Implements logic starting with: { const spec = TOOL_SPECS[tool]; for (const dir of splitPath(pathValue)) { for (const candidate of spec.candidates) { for (const name of getCandida… (missing: jsdoc)
- `src/tool-bootstrap.ts:121` — `ensureManagedTools(targetDir: string, pathValue: string | undefined = process.env.PATH)` — exported: **yes** — Implements logic starting with: { const provisioned: string[] = []; for (const tool of Object.keys(TOOL_SPECS) as ManagedTool[]) { const targetPath = join(targetDir, TOOL_SPECS[to… (missing: jsdoc)

### `src/update-check.ts`
- `src/update-check.ts:34` — `readUpdateCache(cachePath: string = CACHE_FILE)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/update-check.ts:43` — `writeUpdateCache(cache: UpdateCheckCache, cachePath: string = CACHE_FILE)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/update-check.ts:58` — `fetchLatestVersionFromRegistry(registryUrl: string = DEFAULT_REGISTRY_URL, fetchTimeoutMs: number = FETCH_TIMEOUT_MS)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: jsdoc)
- `src/update-check.ts:99` — `resolveInstallCommand(pkg: string)` — exported: **yes** — Implements logic starting with: { if (isBunInstall()) return `bun add -g ${pkg}` return `npm install -g ${pkg}` } (missing: jsdoc)

### `src/update-cmd.ts`
- `src/update-cmd.ts:6` — `runUpdate()` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/web-mode.ts`
- `src/web-mode.ts:130` — `readInstanceRegistry(registryPath = WEB_INSTANCES_PATH)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web-mode.ts:138` — `writeInstanceRegistry(registry: WebInstanceRegistry, registryPath = WEB_INSTANCES_PATH)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web-mode.ts:142` — `registerInstance(cwd: string, entry: Omit<WebInstanceEntry, 'cwd' | 'startedAt'>, registryPath = WEB_INSTANCES_PATH)` — exported: **yes** — Implements logic starting with: { const registry = readInstanceRegistry(registryPath) registry[resolve(cwd)] = { ...entry, cwd: resolve(cwd), startedAt: new Date().toISOString(),… (missing: jsdoc)
- `src/web-mode.ts:152` — `unregisterInstance(cwd: string, registryPath = WEB_INSTANCES_PATH)` — exported: **yes** — Implements logic starting with: { const registry = readInstanceRegistry(registryPath) delete registry[resolve(cwd)] writeInstanceRegistry(registry, registryPath) } (missing: jsdoc)
- `src/web-mode.ts:169` — `writePidFile(filePath: string, pid: number)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web-mode.ts:173` — `readPidFile(filePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web-mode.ts:183` — `deletePidFile(filePath: string)` — exported: **yes** — Implements logic starting with: { try { unlinkSync(filePath) } catch { // Non-fatal — file may already be gone } } (missing: jsdoc)
- `src/web-mode.ts:198` — `stopWebMode(deps: Pick<WebModeDeps, 'pidFilePath' | 'readPidFile' | 'deletePidFile' | 'stderr'> = {}, options: WebModeStopOptions = {})` — exported: **yes** — Implements logic starting with: { const stderr = deps.stderr ?? process.stderr // ── Stop all instances ────────────────────────────────────────────── if (options.all) { const reg… (missing: jsdoc)
- `src/web-mode.ts:293` — `resolveWebHostBootstrap(options: { packageRoot?: string existsSync?: (path: string) => boolean } = {})` — exported: **yes** — Implements logic starting with: { const packageRoot = options.packageRoot ?? DEFAULT_PACKAGE_ROOT const checkExists = options.existsSync ?? existsSync const packagedStandaloneServ… (missing: jsdoc)
- `src/web-mode.ts:330` — `reserveWebPort(host = DEFAULT_HOST)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web-mode.ts:547` — `launchWebMode(options: WebModeLaunchOptions, deps: WebModeDeps = {})` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `src/web/auto-dashboard-service.ts`
- `src/web/auto-dashboard-service.ts:41` — `collectTestOnlyFallbackAutoDashboardData()` — exported: **yes** — Implements logic starting with: { return fallbackAutoDashboardData(); } (missing: jsdoc)
- `src/web/auto-dashboard-service.ts:103` — `collectAuthoritativeAutoDashboardData(packageRoot: string, options: AutoDashboardServiceOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/web/bridge-service.ts`
- `src/web/bridge-service.ts:63` — `resetDefaultPackageRootForTests()` — exported: **yes** — Implements logic starting with: { _defaultPackageRoot = undefined; } (missing: description)
- `src/web/bridge-service.ts:518` — `detectProjectKind(projectCwd: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/web/bridge-service.ts:1082` — `resolveBridgeRuntimeConfig(env: NodeJS.ProcessEnv = getBridgeDeps().env ?? process.env, projectCwdOverride?: string)` — exported: **yes** — Implements logic starting with: { const projectCwd = projectCwdOverride || env.GSD_WEB_PROJECT_CWD || process.cwd(); const projectSessionsDir = env.GSD_WEB_PROJECT_SESSIONS_DIR ||… (missing: jsdoc)
- `src/web/bridge-service.ts:1332` — `class BridgeService` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/web/bridge-service.ts:1347` — `BridgeService.constructor(config: BridgeRuntimeConfig, deps: BridgeServiceDeps)` — exported: **yes** — Implements logic starting with: { this.config = config; this.deps = deps; this.snapshot = { phase: "idle", projectCwd: config.projectCwd, projectSessionsDir: config.projectSession… (missing: jsdoc)
- `src/web/bridge-service.ts:1366` — `BridgeService.getSnapshot()` — exported: **yes** — Implements logic starting with: { return structuredClone(this.snapshot); } (missing: jsdoc)
- `src/web/bridge-service.ts:1370` — `BridgeService.publishLiveStateInvalidation(descriptor: BridgeLiveStateInvalidationDescriptor)` — exported: **yes** — Implements logic starting with: { const event = buildLiveStateInvalidationEvent(descriptor); if (event.workspaceIndexCacheInvalidated) { invalidateWorkspaceIndexCache(this.config.… (missing: jsdoc)
- `src/web/bridge-service.ts:1381` — `BridgeService.ensureStarted()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:1393` — `BridgeService.sendInput(input: BridgeInput)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/bridge-service.ts:1430` — `BridgeService.refreshAuth()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:1480` — `BridgeService.subscribe(listener: (event: BridgeEvent) => void)` — exported: **yes** — Implements logic starting with: { this.subscribers.add(listener); this.snapshot.connectionCount = this.subscribers.size; this.snapshot.updatedAt = nowIso(); this.broadcastStatus()… (missing: jsdoc)
- `src/web/bridge-service.ts:1496` — `BridgeService.subscribeTerminal(listener: (data: string) => void)` — exported: **yes** — Implements logic starting with: { this.terminalSubscribers.add(listener); return () => { this.terminalSubscribers.delete(listener); }; } (missing: jsdoc)
- `src/web/bridge-service.ts:1503` — `BridgeService.sendTerminalInput(data: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:1507` — `BridgeService.resizeTerminal(cols: number, rows: number)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:1511` — `BridgeService.redrawTerminal()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:1525` — `BridgeService.dispose()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:1791` — `getProjectBridgeServiceForCwd(projectCwd: string)` — exported: **yes** — Implements logic starting with: { const resolvedPath = resolve(projectCwd); const existing = projectBridgeRegistry.get(resolvedPath); if (existing) return existing; const config =… (missing: jsdoc)
- `src/web/bridge-service.ts:1830` — `class NoProjectError` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/web/bridge-service.ts:1831` — `NoProjectError.constructor()` — exported: **yes** — Implements logic starting with: { super("No project selected"); this.name = "NoProjectError"; } (missing: jsdoc)
- `src/web/bridge-service.ts:1837` — `getProjectBridgeService()` — exported: **yes** — Implements logic starting with: { const config = resolveBridgeRuntimeConfig(); return getProjectBridgeServiceForCwd(config.projectCwd); } (missing: jsdoc)
- `src/web/bridge-service.ts:1982` — `collectSessionBrowserPayload(query: SessionBrowserQuery = {}, projectCwd?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/web/bridge-service.ts:2015` — `renameSessionInCurrentProject(request: RenameSessionRequest, projectCwd?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:2122` — `collectCurrentProjectOnboardingState(projectCwd?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:2137` — `collectSelectiveLiveStatePayload(domains: BridgeSelectiveLiveStateDomain[] = ["auto", "workspace", "resumable_sessions"], projectCwd?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/web/bridge-service.ts:2178` — `collectBootPayload(projectCwd?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/web/bridge-service.ts:2269` — `buildBridgeFailureResponse(commandType: string, error: unknown)` — exported: **yes** — Implements logic starting with: { return { type: "response", command: commandType, success: false, error: sanitizeErrorMessage(error), }; } (missing: jsdoc)
- `src/web/bridge-service.ts:2278` — `refreshProjectBridgeAuth(projectCwd?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:2287` — `emitProjectLiveStateInvalidation(descriptor: BridgeLiveStateInvalidationDescriptor, projectCwd?: string)` — exported: **yes** — Implements logic starting with: { const bridge = projectCwd ? getProjectBridgeServiceForCwd(projectCwd) : getProjectBridgeService(); return bridge.publishLiveStateInvalidation(des… (missing: jsdoc)
- `src/web/bridge-service.ts:2295` — `sendBridgeInput(input: BridgeInput, projectCwd?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/bridge-service.ts:2307` — `configureBridgeServiceForTests(overrides: Partial<BridgeServiceDeps> | null)` — exported: **yes** — Implements logic starting with: { bridgeServiceOverrides = overrides; invalidateWorkspaceIndexCache(); } (missing: jsdoc)
- `src/web/bridge-service.ts:2312` — `resetBridgeServiceForTests()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `src/web/cli-entry.ts`
- `src/web/cli-entry.ts:33` — `resolveGsdCliEntry(options: ResolveGsdCliEntryOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/web/git-summary-service.ts`
- `src/web/git-summary-service.ts:151` — `collectCurrentProjectGitSummary(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/web/notifications-service.ts`
- `src/web/notifications-service.ts:32` — `collectNotificationsData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/notifications-service.ts:95` — `clearNotificationsData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `src/web/onboarding-service.ts`
- `src/web/onboarding-service.ts:541` — `class OnboardingService` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/web/onboarding-service.ts:548` — `OnboardingService.constructor(deps: OnboardingServiceDeps)` — exported: **yes** — Implements logic starting with: { this.deps = deps; } (missing: jsdoc)
- `src/web/onboarding-service.ts:552` — `OnboardingService.getState()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/onboarding-service.ts:556` — `OnboardingService.validateAndSaveApiKey(providerId: string, apiKey: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/onboarding-service.ts:606` — `OnboardingService.startProviderFlow(providerId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/onboarding-service.ts:640` — `OnboardingService.submitProviderFlowInput(flowId: string, input: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/onboarding-service.ts:659` — `OnboardingService.cancelProviderFlow(flowId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/onboarding-service.ts:669` — `OnboardingService.logoutProvider(providerId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/onboarding-service.ts:958` — `getOnboardingService()` — exported: **yes** — Implements logic starting with: { if (!onboardingServiceSingleton) { onboardingServiceSingleton = new OnboardingService(getOnboardingDeps()); } return onboardingServiceSingleton; } (missing: jsdoc)
- `src/web/onboarding-service.ts:965` — `collectOnboardingState()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/web/onboarding-service.ts:969` — `registerOnboardingBridgeAuthRefresher(refresher: BridgeAuthRefresher | null)` — exported: **yes** — Implements logic starting with: { onboardingBridgeAuthRefresher = refresher; onboardingServiceSingleton = null; } (missing: jsdoc)
- `src/web/onboarding-service.ts:974` — `configureOnboardingServiceForTests(overrides: Partial<OnboardingServiceDeps> | null)` — exported: **yes** — Implements logic starting with: { onboardingServiceOverrides = overrides; onboardingServiceSingleton = null; } (missing: jsdoc)
- `src/web/onboarding-service.ts:979` — `resetOnboardingServiceForTests()` — exported: **yes** — Implements logic starting with: { onboardingServiceOverrides = null; onboardingServiceSingleton = null; } (missing: jsdoc)

### `src/web/recovery-diagnostics-service.ts`
- `src/web/recovery-diagnostics-service.ts:516` — `collectCurrentProjectRecoveryDiagnostics(options: RecoveryDiagnosticsServiceOptions = {}, projectCwdOverride?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/web/update-service.ts`
- `src/web/update-service.ts:17` — `checkForUpdate()` — exported: **yes** — Performs an HTTP request and returns a result. (missing: jsdoc)
- `src/web/update-service.ts:57` — `getUpdateStatus()` — exported: **yes** — Implements logic starting with: { return { ...updateState } } (missing: jsdoc)

### `src/web/web-auth-storage.ts`
- `src/web/web-auth-storage.ts:60` — `class FileOnboardingAuthStorage` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `src/web/web-auth-storage.ts:64` — `FileOnboardingAuthStorage.constructor(authPath: string)` — exported: **yes** — Implements logic starting with: { this.authPath = authPath; this.reload(); } (missing: jsdoc)
- `src/web/web-auth-storage.ts:69` — `FileOnboardingAuthStorage.reload()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web/web-auth-storage.ts:74` — `FileOnboardingAuthStorage.getCredentialsForProvider(provider: string)` — exported: **yes** — Implements logic starting with: { const entry = this.data[provider]; if (!entry) return []; return Array.isArray(entry) ? entry : [entry]; } (missing: jsdoc)
- `src/web/web-auth-storage.ts:80` — `FileOnboardingAuthStorage.set(provider: string, credential: StoredCredential)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web/web-auth-storage.ts:92` — `FileOnboardingAuthStorage.hasAuth(provider: string)` — exported: **yes** — Implements logic starting with: { if (this.getCredentialsForProvider(provider).length > 0) { return true; } return Boolean(getEnvApiKey(provider)); } (missing: jsdoc)
- `src/web/web-auth-storage.ts:99` — `FileOnboardingAuthStorage.getOAuthProviders()` — exported: **yes** — Implements logic starting with: { return getOAuthProviders(); } (missing: jsdoc)
- `src/web/web-auth-storage.ts:103` — `FileOnboardingAuthStorage.login(providerId: string, callbacks: OAuthLoginCallbacks)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `src/web/web-auth-storage.ts:113` — `FileOnboardingAuthStorage.logout(providerId: string)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `src/web/web-auth-storage.ts:133` — `createOnboardingAuthStorage(authPath: string)` — exported: **yes** — Implements logic starting with: { return new FileOnboardingAuthStorage(authPath); } (missing: jsdoc)

### `src/welcome-screen.ts`
- `src/welcome-screen.ts:114` — `buildWelcomeScreenLines(opts: WelcomeScreenOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/welcome-screen.ts:187` — `printWelcomeScreen(opts: WelcomeScreenOptions)` — exported: **yes** — Implements logic starting with: { if (!process.stderr.isTTY) return process.stderr.write(buildWelcomeScreenLines(opts).join('\n') + '\n') } (missing: jsdoc)

### `src/worktree-cli.ts`
- `src/worktree-cli.ts:144` — `getWorktreeStatus(ext: ExtensionModules, basePath: string, name: string, wtPath: string, branch: string)` — exported: **yes** — Implements logic starting with: { const diff = ext.diffWorktreeAll(basePath, name, branch) const numstat = ext.diffWorktreeNumstat(basePath, name, branch) const filesChanged = dif… (missing: jsdoc)
- `src/worktree-cli.ts:203` — `handleList(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/worktree-cli.ts:222` — `handleMerge(basePath: string, args: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/worktree-cli.ts:288` — `handleClean(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/worktree-cli.ts:318` — `handleRemove(basePath: string, args: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `src/worktree-cli.ts:349` — `handleStatusBanner(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `src/worktree-cli.ts:379` — `handleWorktreeFlag(worktreeFlag: boolean | string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `src/worktree-name-gen.ts`
- `src/worktree-name-gen.ts:47` — `generateWorktreeName()` — exported: **yes** — Implements logic starting with: { return `${pick(ADJECTIVES)}-${pick(VERBS)}-${pick(NOUNS)}` } (missing: jsdoc)

### `src/worktree-status-banner.ts`
- `src/worktree-status-banner.ts:128` — `showWorktreeStatusBanner(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `studio/src/renderer/src/App.tsx`
- `studio/src/renderer/src/App.tsx:10` — `App()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `vscode-extension/src/activity-feed.ts`
- `vscode-extension/src/activity-feed.ts:77` — `GsdActivityFeedProvider.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.maxItems = vscode.workspace.getConfiguration("gsd").get<number>("activityFeedMaxItems", 100); this.disposables.push( this._onDidChangeTreeDa… (missing: jsdoc)
- `vscode-extension/src/activity-feed.ts:97` — `GsdActivityFeedProvider.getTreeItem(element: ActivityItem)` — exported: **yes** — Implements logic starting with: { const item = new vscode.TreeItem(element.label, vscode.TreeItemCollapsibleState.None); item.iconPath = element.icon; item.description = element.d… (missing: jsdoc)
- `vscode-extension/src/activity-feed.ts:118` — `GsdActivityFeedProvider.getChildren()` — exported: **yes** — Implements logic starting with: { // Show newest first return [...this.items].reverse(); } (missing: jsdoc)
- `vscode-extension/src/activity-feed.ts:123` — `GsdActivityFeedProvider.clear()` — exported: **yes** — Implements logic starting with: { this.items = []; this.runningTools.clear(); this._onDidChangeTreeData.fire(); } (missing: jsdoc)
- `vscode-extension/src/activity-feed.ts:129` — `GsdActivityFeedProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/approval-mode.ts`
- `vscode-extension/src/approval-mode.ts:18` — `nextApprovalMode(current: ApprovalMode)` — exported: **yes** — Implements logic starting with: { const currentIdx = APPROVAL_MODES.indexOf(current); return APPROVAL_MODES[(currentIdx + 1) % APPROVAL_MODES.length]; } (missing: jsdoc)
- `vscode-extension/src/approval-mode.ts:23` — `describeApprovalEvent(evt: { type?: unknown; toolName?: unknown; toolInput?: unknown; })` — exported: **yes** — Implements logic starting with: { if (evt.type !== "tool_execution_start") return null; const toolName = String(evt.toolName ?? ""); if (toolName !== "Write" && toolName !== "Edit… (missing: jsdoc)

### `vscode-extension/src/bash-terminal.ts`
- `vscode-extension/src/bash-terminal.ts:13` — `GsdBashTerminal.constructor(client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.disposables.push( client.onEvent((evt: AgentEvent) => this.handleEvent(evt)), client.onConnectionChange((connected) => { if (!connected) { t… (missing: jsdoc)
- `vscode-extension/src/bash-terminal.ts:71` — `GsdBashTerminal.close()` — exported: **yes** — Implements logic starting with: { this.terminal?.dispose(); this.terminal = undefined; this.writeEmitter?.dispose(); this.writeEmitter = undefined; } (missing: jsdoc)
- `vscode-extension/src/bash-terminal.ts:78` — `GsdBashTerminal.dispose()` — exported: **yes** — Implements logic starting with: { this.close(); for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/change-tracker-core.ts`
- `vscode-extension/src/change-tracker-core.ts:16` — `getToolInput(evt: ChangeTrackerAgentEvent)` — exported: **yes** — Implements logic starting with: { const input = evt.args ?? evt.toolInput ?? evt.input ?? {}; return input && typeof input === "object" ? input as Record<string, unknown> : {}; } (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:21` — `getToolUseId(evt: ChangeTrackerAgentEvent)` — exported: **yes** — Implements logic starting with: { return String(evt.toolCallId ?? evt.toolUseId ?? ""); } (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:25` — `normalizeToolName(toolName: unknown)` — exported: **yes** — Implements logic starting with: { return String(toolName ?? "").toLowerCase(); } (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:29` — `isFileMutationTool(toolName: string)` — exported: **yes** — Implements logic starting with: { return toolName === "write" || toolName === "write_file" || toolName === "edit"; } (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:33` — `resolveToolPath(workspaceRoot: string, input: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { const rawPath = String(input.file_path ?? input.path ?? ""); if (!rawPath) return ""; return path.isAbsolute(rawPath) ? rawPath : path.resolve(wo… (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:39` — `captureOriginalContent(filePath: string, fsImpl: ChangeTrackerFileSystem)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:47` — `captureCurrentSnapshots(filePaths: Iterable<string>, fsImpl: ChangeTrackerFileSystem)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `vscode-extension/src/change-tracker-core.ts:62` — `describeAction(toolName: string, input: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { switch (toolName.toLowerCase()) { case "read": { const p = String(input.file_path ?? input.path ?? ""); return `Read ${p.split(/[\\/]/).pop() ??… (missing: jsdoc)

### `vscode-extension/src/change-tracker.ts`
- `vscode-extension/src/change-tracker.ts:57` — `GsdChangeTracker.constructor(private readonly client: GsdClient, private readonly workspaceRoot = vscode.workspace.workspaceFolders?.[0]?.uri.fsPath ?? process.cwd())` — exported: **yes** — Implements logic starting with: { this.disposables.push(this._onDidChange, this._onCheckpointChange); this.disposables.push( client.onEvent((evt) => this.handleEvent(evt)), client… (missing: jsdoc)
- `vscode-extension/src/change-tracker.ts:199` — `GsdChangeTracker.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/checkpoints.ts`
- `vscode-extension/src/checkpoints.ts:16` — `GsdCheckpointProvider.constructor(private readonly tracker: GsdChangeTracker)` — exported: **yes** — Implements logic starting with: { this.disposables.push( this._onDidChangeTreeData, tracker.onCheckpointChange(() => this._onDidChangeTreeData.fire()), ); } (missing: jsdoc)
- `vscode-extension/src/checkpoints.ts:23` — `GsdCheckpointProvider.getTreeItem(checkpoint: Checkpoint)` — exported: **yes** — Implements logic starting with: { const fileCount = checkpoint.snapshots.size; const time = new Date(checkpoint.timestamp); const timeStr = time.toLocaleTimeString([], { hour: "2-… (missing: jsdoc)
- `vscode-extension/src/checkpoints.ts:45` — `GsdCheckpointProvider.getChildren()` — exported: **yes** — Implements logic starting with: { // Show newest first return [...this.tracker.checkpoints].reverse(); } (missing: jsdoc)
- `vscode-extension/src/checkpoints.ts:50` — `GsdCheckpointProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/code-lens.ts`
- `vscode-extension/src/code-lens.ts:59` — `GsdCodeLensProvider.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.disposables.push( this._onDidChangeCodeLenses, client.onConnectionChange(() => this._onDidChangeCodeLenses.fire()), vscode.workspace.onDidCh… (missing: jsdoc)
- `vscode-extension/src/code-lens.ts:71` — `GsdCodeLensProvider.provideCodeLenses(document: vscode.TextDocument, _token: vscode.CancellationToken)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)
- `vscode-extension/src/code-lens.ts:134` — `GsdCodeLensProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/conversation-history.ts`
- `vscode-extension/src/conversation-history.ts:30` — `GsdConversationHistoryPanel.createOrShow(extensionUri: vscode.Uri, client: GsdClient)` — exported: **yes** — Implements logic starting with: { const column = vscode.window.activeTextEditor?.viewColumn ?? vscode.ViewColumn.One; if (GsdConversationHistoryPanel.currentPanel) { GsdConversati… (missing: jsdoc)
- `vscode-extension/src/conversation-history.ts:92` — `GsdConversationHistoryPanel.refresh()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `vscode-extension/src/conversation-history.ts:107` — `GsdConversationHistoryPanel.dispose()` — exported: **yes** — Implements logic starting with: { GsdConversationHistoryPanel.currentPanel = undefined; this.panel.dispose(); for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/diagnostics.ts`
- `vscode-extension/src/diagnostics.ts:13` — `GsdDiagnosticBridge.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.collection = vscode.languages.createDiagnosticCollection("gsd"); this.disposables.push(this.collection); } (missing: jsdoc)
- `vscode-extension/src/diagnostics.ts:115` — `GsdDiagnosticBridge.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/extension.ts`
- `vscode-extension/src/extension.ts:51` — `resolveTrustedGsdStartupConfig()` — exported: **yes** — Implements logic starting with: { return { binaryPath: getTrustedConfigurationValue("gsd", "binaryPath", "gsd"), autoStart: getTrustedConfigurationValue("gsd", "autoStart", false)… (missing: jsdoc)
- `vscode-extension/src/extension.ts:71` — `activate(context: vscode.ExtensionContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `vscode-extension/src/extension.ts:1009` — `deactivate()` — exported: **yes** — Implements logic starting with: { client?.dispose(); sidebarProvider?.dispose(); fileDecorations?.dispose(); sessionTreeProvider?.dispose(); activityFeedProvider?.dispose(); check… (missing: jsdoc)

### `vscode-extension/src/file-decorations.ts`
- `vscode-extension/src/file-decorations.ts:15` — `GsdFileDecorationProvider.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.disposables.push( this._onDidChangeFileDecorations, client.onEvent((evt: AgentEvent) => this.handleEvent(evt)), client.onConnectionChange((c… (missing: jsdoc)
- `vscode-extension/src/file-decorations.ts:47` — `GsdFileDecorationProvider.provideFileDecoration(uri: vscode.Uri)` — exported: **yes** — Implements logic starting with: { if (this.modifiedUris.has(uri.toString())) { return { badge: "G", tooltip: "Modified by GSD", color: new vscode.ThemeColor("gitDecoration.modifie… (missing: jsdoc)
- `vscode-extension/src/file-decorations.ts:58` — `GsdFileDecorationProvider.clear()` — exported: **yes** — Implements logic starting with: { this.modifiedUris.clear(); this._onDidChangeFileDecorations.fire(undefined); } (missing: jsdoc)
- `vscode-extension/src/file-decorations.ts:63` — `GsdFileDecorationProvider.dispose()` — exported: **yes** — Implements logic starting with: { this.clear(); for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/git-args.ts`
- `vscode-extension/src/git-args.ts:4` — `buildAgentGitAddArgs(files: string[])` — exported: **yes** — Implements logic starting with: { return ["add", ...files]; } (missing: jsdoc)
- `vscode-extension/src/git-args.ts:8` — `buildAgentGitDiffArgs(files: string[])` — exported: **yes** — Implements logic starting with: { return ["diff", "--", ...files]; } (missing: jsdoc)
- `vscode-extension/src/git-args.ts:12` — `buildAgentGitStatusArgs(files: string[])` — exported: **yes** — Implements logic starting with: { return ["status", "--short", "--", ...files]; } (missing: jsdoc)

### `vscode-extension/src/git-integration.ts`
- `vscode-extension/src/git-integration.ts:12` — `GsdGitIntegration.constructor(private readonly tracker: GsdChangeTracker, private readonly cwd: string)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `vscode-extension/src/git-integration.ts:106` — `GsdGitIntegration.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/gsd-client-spawn.ts`
- `vscode-extension/src/gsd-client-spawn.ts:12` — `buildGsdClientSpawnPlan(binaryPath: string, cwd: string, env: NodeJS.ProcessEnv = process.env, platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { return { command: binaryPath, args: ["--mode", "rpc"], options: { cwd, stdio: ["pipe", "pipe", "pipe"], env: { ...env }, shell: platform === "win… (missing: jsdoc)

### `vscode-extension/src/gsd-client.ts`
- `vscode-extension/src/gsd-client.ts:67` — `GsdClient.constructor(private readonly binaryPath: string, private readonly cwd: string)` — exported: **yes** — Implements logic starting with: { this.disposables.push(this._onEvent, this._onConnectionChange, this._onError); } (missing: jsdoc)
- `vscode-extension/src/gsd-client.ts:74` — `GsdClient.get isConnected()` — exported: **yes** — Implements logic starting with: { return this.process !== null && this.process.exitCode === null; } (missing: jsdoc)
- `vscode-extension/src/gsd-client.ts:78` — `GsdClient.get autoRetryEnabled()` — exported: **yes** — Implements logic starting with: { return this._autoRetryEnabled; } (missing: jsdoc)
- `vscode-extension/src/gsd-client.ts:498` — `GsdClient.dispose()` — exported: **yes** — Implements logic starting with: { this.stop(); for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/line-decorations.ts`
- `vscode-extension/src/line-decorations.ts:14` — `GsdLineDecorationManager.constructor(private readonly tracker: GsdChangeTracker)` — exported: **yes** — Implements logic starting with: { this.addedDecoration = vscode.window.createTextEditorDecorationType({ isWholeLine: true, backgroundColor: "rgba(78, 201, 176, 0.07)", overviewRul… (missing: jsdoc)
- `vscode-extension/src/line-decorations.ts:98` — `GsdLineDecorationManager.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/permissions.ts`
- `vscode-extension/src/permissions.ts:25` — `GsdPermissionManager.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { // Load saved mode from configuration this._mode = vscode.workspace.getConfiguration(GSD_APPROVAL_CONFIG_SECTION).get<ApprovalMode>(GSD_APPROVAL_… (missing: jsdoc)
- `vscode-extension/src/permissions.ts:47` — `GsdPermissionManager.get mode()` — exported: **yes** — Implements logic starting with: { return this._mode; } (missing: jsdoc)
- `vscode-extension/src/permissions.ts:89` — `GsdPermissionManager.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/plan-viewer.ts`
- `vscode-extension/src/plan-viewer.ts:29` — `GsdPlanViewerProvider.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.disposables.push( this._onDidChangeTreeData, client.onEvent((evt) => this.handleEvent(evt)), client.onConnectionChange((connected) => { if (… (missing: jsdoc)
- `vscode-extension/src/plan-viewer.ts:43` — `GsdPlanViewerProvider.getTreeItem(step: PlanStep)` — exported: **yes** — Implements logic starting with: { const icon = stepIcon(step.status); const item = new vscode.TreeItem(step.description, vscode.TreeItemCollapsibleState.None); item.iconPath = ico… (missing: jsdoc)
- `vscode-extension/src/plan-viewer.ts:55` — `GsdPlanViewerProvider.getChildren()` — exported: **yes** — Implements logic starting with: { return this.steps; } (missing: jsdoc)
- `vscode-extension/src/plan-viewer.ts:59` — `GsdPlanViewerProvider.clear()` — exported: **yes** — Implements logic starting with: { this.steps = []; this.runningTools.clear(); this._onDidChangeTreeData.fire(); } (missing: jsdoc)
- `vscode-extension/src/plan-viewer.ts:65` — `GsdPlanViewerProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/rpc-display.ts`
- `vscode-extension/src/rpc-display.ts:11` — `getSessionInputTokens(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return stats?.tokens.input ?? 0; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:15` — `getSessionOutputTokens(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return stats?.tokens.output ?? 0; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:19` — `getSessionCacheReadTokens(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return stats?.tokens.cacheRead ?? 0; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:23` — `getSessionCacheWriteTokens(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return stats?.tokens.cacheWrite ?? 0; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:27` — `getSessionTotalTokens(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return stats?.tokens.total ?? (getSessionInputTokens(stats) + getSessionOutputTokens(stats)); } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:31` — `getSessionCost(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return stats?.cost ?? 0; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:35` — `hasSessionTokenStats(stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return getSessionInputTokens(stats) > 0 || getSessionOutputTokens(stats) > 0; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:39` — `getContextUsageDisplay(_stats: SessionStats | null | undefined)` — exported: **yes** — Implements logic starting with: { return { percent: null, text: "Context unknown", }; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:46` — `formatSessionStatsLines(stats: SessionStats)` — exported: **yes** — Implements logic starting with: { const lines = [ `Input tokens: ${getSessionInputTokens(stats).toLocaleString()}`, `Output tokens: ${getSessionOutputTokens(stats).toLocaleString(… (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:65` — `getBashOutput(result: BashResult)` — exported: **yes** — Implements logic starting with: { return result.output; } (missing: jsdoc)
- `vscode-extension/src/rpc-display.ts:69` — `getBashExitCode(result: BashResult)` — exported: **yes** — Implements logic starting with: { return result.exitCode; } (missing: jsdoc)

### `vscode-extension/src/scm-provider.ts`
- `vscode-extension/src/scm-provider.ts:18` — `GsdScmProvider.constructor(private readonly tracker: GsdChangeTracker, private readonly workspaceRoot: string)` — exported: **yes** — Implements logic starting with: { // Register content provider for original file contents this.contentProvider = new GsdOriginalContentProvider(tracker); this.disposables.push( vs… (missing: jsdoc)
- `vscode-extension/src/scm-provider.ts:97` — `GsdScmProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/session-tree.ts`
- `vscode-extension/src/session-tree.ts:28` — `GsdSessionTreeProvider.constructor(private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.disposables.push( this._onDidChangeTreeData, client.onConnectionChange(() => this.refresh()), ); } (missing: jsdoc)
- `vscode-extension/src/session-tree.ts:35` — `GsdSessionTreeProvider.refresh()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `vscode-extension/src/session-tree.ts:98` — `GsdSessionTreeProvider.getTreeItem(element: SessionItem)` — exported: **yes** — Implements logic starting with: { const item = new vscode.TreeItem(element.label, vscode.TreeItemCollapsibleState.None); item.description = element.sessionId.slice(0, 8); item.too… (missing: jsdoc)
- `vscode-extension/src/session-tree.ts:119` — `GsdSessionTreeProvider.getChildren()` — exported: **yes** — Implements logic starting with: { return this.sessions; } (missing: jsdoc)
- `vscode-extension/src/session-tree.ts:123` — `GsdSessionTreeProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/sidebar.ts`
- `vscode-extension/src/sidebar.ts:37` — `GsdSidebarProvider.constructor(private readonly extensionUri: vscode.Uri, private readonly client: GsdClient)` — exported: **yes** — Implements logic starting with: { this.disposables.push( client.onConnectionChange(() => this.refresh()), client.onEvent((evt) => { switch (evt.type) { case "agent_start": case "a… (missing: jsdoc)
- `vscode-extension/src/sidebar.ts:60` — `GsdSidebarProvider.resolveWebviewView(webviewView: vscode.WebviewView, _context: vscode.WebviewViewResolveContext, _token: vscode.CancellationToken)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `vscode-extension/src/sidebar.ts:193` — `GsdSidebarProvider.refresh()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `vscode-extension/src/sidebar.ts:263` — `GsdSidebarProvider.dispose()` — exported: **yes** — Implements logic starting with: { if (this.refreshTimer) { clearInterval(this.refreshTimer); } for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/slash-completion.ts`
- `vscode-extension/src/slash-completion.ts:18` — `GsdSlashCompletionProvider.constructor(private readonly client: GsdClient)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `vscode-extension/src/slash-completion.ts:31` — `GsdSlashCompletionProvider.provideCompletionItems(document: vscode.TextDocument, position: vscode.Position, _token: vscode.CancellationToken)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `vscode-extension/src/slash-completion.ts:72` — `GsdSlashCompletionProvider.dispose()` — exported: **yes** — Implements logic starting with: { for (const d of this.disposables) { d.dispose(); } } (missing: jsdoc)

### `vscode-extension/src/trusted-config.ts`
- `vscode-extension/src/trusted-config.ts:24` — `pickTrustedConfigurationValue(inspected: InspectedConfigurationValue<T> | undefined, fallback: T)` — exported: **yes** — Implements logic starting with: { return inspected?.globalValue ?? inspected?.defaultValue ?? fallback; } (missing: jsdoc)

### `web/app/api/boot/route.ts`
- `web/app/api/boot/route.ts:8` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/bridge-terminal/input/route.ts`
- `web/app/api/bridge-terminal/input/route.ts:6` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/bridge-terminal/resize/route.ts`
- `web/app/api/bridge-terminal/resize/route.ts:6` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/bridge-terminal/stream/route.ts`
- `web/app/api/bridge-terminal/stream/route.ts:17` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/captures/route.ts`
- `web/app/api/captures/route.ts:16` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/captures/route.ts:39` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/cleanup/route.ts`
- `web/app/api/cleanup/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/cleanup/route.ts:30` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/dev-mode/route.ts`
- `web/app/api/dev-mode/route.ts:7` — `GET()` — exported: **yes** — Implements logic starting with: { const hostKind = process.env.GSD_WEB_HOST_KIND ?? "unknown"; const packageRoot = process.env.GSD_WEB_PACKAGE_ROOT ?? ""; const isSourceDev = host… (missing: jsdoc)

### `web/app/api/doctor/route.ts`
- `web/app/api/doctor/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/doctor/route.ts:32` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/experimental/route.ts`
- `web/app/api/experimental/route.ts:55` — `GET()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/experimental/route.ts:72` — `PATCH(request: Request)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `web/app/api/export-data/route.ts`
- `web/app/api/export-data/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/files/route.ts`
- `web/app/api/files/route.ts:81` — `GET(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `web/app/api/files/route.ts:144` — `POST(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `web/app/api/forensics/route.ts`
- `web/app/api/forensics/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/git/route.ts`
- `web/app/api/git/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/history/route.ts`
- `web/app/api/history/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/hooks/route.ts`
- `web/app/api/hooks/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/inspect/route.ts`
- `web/app/api/inspect/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/knowledge/route.ts`
- `web/app/api/knowledge/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/live-state/route.ts`
- `web/app/api/live-state/route.ts:24` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/notifications/route.ts`
- `web/app/api/notifications/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/notifications/route.ts:34` — `DELETE(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/onboarding/route.ts`
- `web/app/api/onboarding/route.ts:42` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/onboarding/route.ts:54` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/preferences/route.ts`
- `web/app/api/preferences/route.ts:16` — `GET()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `web/app/api/preferences/route.ts:32` — `PUT(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `web/app/api/projects/route.ts`
- `web/app/api/projects/route.ts:18` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/projects/route.ts:41` — `POST(request: Request)` — exported: **yes** — Executes a subprocess command. (missing: jsdoc)

### `web/app/api/recovery/route.ts`
- `web/app/api/recovery/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/remote-questions/route.ts`
- `web/app/api/remote-questions/route.ts:148` — `GET()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `web/app/api/remote-questions/route.ts:229` — `POST(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `web/app/api/remote-questions/route.ts:311` — `DELETE()` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)
- `web/app/api/remote-questions/route.ts:350` — `PATCH(request: Request)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/app/api/session/browser/route.ts`
- `web/app/api/session/browser/route.ts:22` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/session/command/route.ts`
- `web/app/api/session/command/route.ts:20` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/session/events/route.ts`
- `web/app/api/session/events/route.ts:18` — `GET(request: Request)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `web/app/api/session/manage/route.ts`
- `web/app/api/session/manage/route.ts:50` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/settings-data/route.ts`
- `web/app/api/settings-data/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/shutdown/route.ts`
- `web/app/api/shutdown/route.ts:7` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/skill-health/route.ts`
- `web/app/api/skill-health/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/steer/route.ts`
- `web/app/api/steer/route.ts:10` — `GET(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: jsdoc)

### `web/app/api/terminal/input/route.ts`
- `web/app/api/terminal/input/route.ts:13` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/terminal/resize/route.ts`
- `web/app/api/terminal/resize/route.ts:13` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/terminal/sessions/route.ts`
- `web/app/api/terminal/sessions/route.ts:27` — `GET()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/terminal/sessions/route.ts:31` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/terminal/sessions/route.ts:53` — `DELETE(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/terminal/stream/route.ts`
- `web/app/api/terminal/stream/route.ts:21` — `GET(request: Request)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `web/app/api/terminal/upload/route.ts`
- `web/app/api/terminal/upload/route.ts:44` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/undo/route.ts`
- `web/app/api/undo/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/undo/route.ts:30` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/update/route.ts`
- `web/app/api/update/route.ts:11` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/app/api/update/route.ts:44` — `POST(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/api/visualizer/route.ts`
- `web/app/api/visualizer/route.ts:7` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/app/layout.tsx`
- `web/app/layout.tsx:46` — `RootLayout({ children, }: Readonly<{ children: React.ReactNode }>)` — exported: **yes** — Implements logic starting with: { return ( <html lang="en" suppressHydrationWarning> <body className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}> <ThemeP… (missing: jsdoc)

### `web/app/page.tsx`
- `web/app/page.tsx:17` — `Page()` — exported: **yes** — Implements logic starting with: { return <GSDAppShell /> } (missing: jsdoc)

### `web/components/gsd/activity-view.tsx`
- `web/components/gsd/activity-view.tsx:25` — `ActivityView()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/app-shell.tsx`
- `web/components/gsd/app-shell.tsx:578` — `GSDAppShell()` — exported: **yes** — Implements logic starting with: { // Extract the auth token from the URL fragment on first render. // Must happen before any API calls fire. getAuthToken() return ( <ProjectStoreM… (missing: jsdoc)

### `web/components/gsd/code-editor.tsx`
- `web/components/gsd/code-editor.tsx:166` — `CodeEditor({ value, onChange, language, fontSize, className, }: CodeEditorProps)` — exported: **yes** — Implements logic starting with: { const { resolvedTheme } = useTheme() const theme = resolvedTheme !== "light" ? darkTheme : lightTheme // Resolve and cache language extension con… (missing: jsdoc)

### `web/components/gsd/command-surface.tsx`
- `web/components/gsd/command-surface.tsx:320` — `CommandSurface()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/dashboard.tsx`
- `web/components/gsd/dashboard.tsx:117` — `Dashboard({ onSwitchView, onExpandTerminal }: DashboardProps = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/diagnostics-panels.tsx`
- `web/components/gsd/diagnostics-panels.tsx:136` — `ForensicsPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/diagnostics-panels.tsx:270` — `DoctorPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/diagnostics-panels.tsx:394` — `SkillHealthPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/dual-terminal.tsx`
- `web/components/gsd/dual-terminal.tsx:11` — `DualTerminal()` — exported: **yes** — Implements logic starting with: { const [splitPosition, setSplitPosition] = useState(50) const containerRef = useRef<HTMLDivElement>(null) const rootRef = useRef<HTMLDivElement>(n… (missing: jsdoc)

### `web/components/gsd/file-content-viewer.tsx`
- `web/components/gsd/file-content-viewer.tsx:546` — `FileContentViewer({ content, filepath, className, root, path, onSave, diff, onDismissDiff, agentOpened, }: FileContentViewerProps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/components/gsd/files-view.tsx`
- `web/components/gsd/files-view.tsx:474` — `FilesView()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/components/gsd/focused-panel.tsx`
- `web/components/gsd/focused-panel.tsx:254` — `FocusedPanel()` — exported: **yes** — Implements logic starting with: { const workspace = useGSDWorkspaceState() const { respondToUiRequest, dismissUiRequest } = useGSDWorkspaceActions() const pending = workspace.pend… (missing: jsdoc)

### `web/components/gsd/knowledge-captures-panel.tsx`
- `web/components/gsd/knowledge-captures-panel.tsx:375` — `KnowledgeCapturesPanel({ initialTab }: KnowledgeCapturesPanelProps)` — exported: **yes** — Implements logic starting with: { const [activeTab, setActiveTab] = useState<"knowledge" | "captures">(initialTab) const workspace = useGSDWorkspaceState() const { loadKnowledgeDa… (missing: jsdoc)

### `web/components/gsd/loading-skeletons.tsx`
- `web/components/gsd/loading-skeletons.tsx:38` — `CurrentSliceCardSkeleton()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/loading-skeletons.tsx:56` — `SessionCardSkeleton()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/loading-skeletons.tsx:79` — `RecoveryCardSkeleton()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/loading-skeletons.tsx:102` — `ActivityCardSkeleton()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/loading-skeletons.tsx:130` — `DashboardMetricsSkeleton({ icons }: DashboardSkeletonProps)` — exported: **yes** — Implements logic starting with: { return ( <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4 2xl:grid-cols-5"> <CurrentUnitCardSkeleton icon={icons.Activity} /> <MetricCard… (missing: jsdoc)
- `web/components/gsd/loading-skeletons.tsx:196` — `StatusBarValueSkeleton({ width = "w-16" }: { width?: string })` — exported: **yes** — Implements logic starting with: { return <Skeleton className={cn("h-3 inline-block", width)} /> } (missing: jsdoc)

### `web/components/gsd/main-session-terminal.tsx`
- `web/components/gsd/main-session-terminal.tsx:85` — `MainSessionTerminal({ className, fontSize, projectCwd }: MainSessionTerminalProps)` — exported: **yes** — Parses JSON input into a structured value. (missing: jsdoc)

### `web/components/gsd/onboarding-gate.tsx`
- `web/components/gsd/onboarding-gate.tsx:84` — `OnboardingGate()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/components/gsd/onboarding/step-authenticate.tsx`
- `web/components/gsd/onboarding/step-authenticate.tsx:93` — `StepAuthenticate({ provider, activeFlow, lastValidation, requestState, requestProviderId, onSaveApiKey, onStartFlow, onSubmitFlowInput, onCancelFlow, onBack, onNext, bridgeRefreshPhase, bridgeRefreshError, }: StepAuthenticateProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/onboarding/step-dev-root.tsx`
- `web/components/gsd/onboarding/step-dev-root.tsx:158` — `StepDevRoot({ onNext, onBack }: StepDevRootProps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/components/gsd/onboarding/step-mode.tsx`
- `web/components/gsd/onboarding/step-mode.tsx:42` — `StepMode({ selected, onSelect, onNext, onBack }: StepModeProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/onboarding/step-optional.tsx`
- `web/components/gsd/onboarding/step-optional.tsx:18` — `StepOptional({ sections, onBack, onNext }: StepOptionalProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/onboarding/step-project.tsx`
- `web/components/gsd/onboarding/step-project.tsx:103` — `StepProject({ onFinish, onBack, onBeforeSwitch }: StepProjectProps)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/components/gsd/onboarding/step-provider.tsx`
- `web/components/gsd/onboarding/step-provider.tsx:56` — `StepProvider({ providers, selectedId, onSelect, onNext, onBack }: StepProviderProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/onboarding/step-ready.tsx`
- `web/components/gsd/onboarding/step-ready.tsx:14` — `StepReady({ providerLabel, onFinish }: StepReadyProps)` — exported: **yes** — Implements logic starting with: { return ( <div className="flex flex-col items-center text-center"> {/* Success icon with staggered entrance */} <motion.div initial={{ opacity: 0,… (missing: jsdoc)

### `web/components/gsd/onboarding/step-remote.tsx`
- `web/components/gsd/onboarding/step-remote.tsx:70` — `StepRemote({ onBack, onNext }: StepRemoteProps)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `web/components/gsd/onboarding/step-welcome.tsx`
- `web/components/gsd/onboarding/step-welcome.tsx:13` — `StepWelcome({ onNext }: StepWelcomeProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/onboarding/wizard-stepper.tsx`
- `web/components/gsd/onboarding/wizard-stepper.tsx:19` — `WizardStepper({ steps, currentIndex, onStepClick, className }: WizardStepperProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/project-welcome.tsx`
- `web/components/gsd/project-welcome.tsx:130` — `ProjectWelcome({ detection, onCommand, onSwitchView, disabled = false, }: ProjectWelcomeProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/projects-view.tsx`
- `web/components/gsd/projects-view.tsx:261` — `ProjectsPanel({ open, onOpenChange, }: { open: boolean onOpenChange: (open: boolean) => void })` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `web/components/gsd/projects-view.tsx:924` — `DevRootSettingsSection()` — exported: **yes** — Implements logic starting with: { const [devRoot, setDevRoot] = useState<string | null>(null) const [loading, setLoading] = useState(true) useEffect(() => { authFetch("/api/prefer… (missing: jsdoc)
- `web/components/gsd/projects-view.tsx:968` — `ProjectSelectionGate()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/components/gsd/remaining-command-panels.tsx`
- `web/components/gsd/remaining-command-panels.tsx:150` — `QuickPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:203` — `HistoryPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:375` — `UndoPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:521` — `SteerPanel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:609` — `HooksPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:701` — `InspectPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:809` — `ExportPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:909` — `CleanupPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:1074` — `QueuePanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/remaining-command-panels.tsx:1177` — `StatusPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/roadmap.tsx`
- `web/components/gsd/roadmap.tsx:44` — `Roadmap()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/settings-panels.tsx`
- `web/components/gsd/settings-panels.tsx:201` — `PrefsPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/settings-panels.tsx:382` — `ModelRoutingPanel()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/settings-panels.tsx:487` — `BudgetPanel()` — exported: **yes** — Implements logic starting with: { const { state, data, busy, refresh } = useSettingsData() const prefs = data?.preferences ?? null const budget = data?.budgetAllocation ?? null co… (missing: jsdoc)
- `web/components/gsd/settings-panels.tsx:615` — `RemoteQuestionsPanel()` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)
- `web/components/gsd/settings-panels.tsx:1224` — `GeneralPanel()` — exported: **yes** — Implements logic starting with: { const [terminalFontSize, setTerminalFontSize] = useTerminalFontSize() const [editorFontSize, setEditorFontSize] = useEditorFontSize() return ( <d… (missing: jsdoc)
- `web/components/gsd/settings-panels.tsx:1284` — `ExperimentalPanel()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `web/components/gsd/settings-panels.tsx:1424` — `EditorSizePanel()` — exported: **yes** — Implements logic starting with: null (missing: jsdoc)

### `web/components/gsd/shell-terminal.tsx`
- `web/components/gsd/shell-terminal.tsx:492` — `ShellTerminal({ className, command, commandArgs, sessionPrefix, hideSidebar = false, fontSize, hideInitialGsdHeader = false, projectCwd, }: ShellTerminalProps)` — exported: **yes** — Serializes a value to JSON. (missing: jsdoc)

### `web/components/gsd/sidebar.tsx`
- `web/components/gsd/sidebar.tsx:79` — `NavRail({ activeView, onViewChange, isConnecting = false }: NavRailProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/sidebar.tsx:319` — `MilestoneExplorer({ isConnecting = false, width, onCollapse }: { isConnecting?: boolean; width?: number; onCollapse?: () => void })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/gsd/sidebar.tsx:629` — `CollapsedMilestoneSidebar({ onExpand }: { onExpand: () => void })` — exported: **yes** — Implements logic starting with: { const workspace = useGSDWorkspaceState() const { sendCommand } = useGSDWorkspaceActions() const liveWorkspace = getLiveWorkspaceIndex(workspace)… (missing: jsdoc)
- `web/components/gsd/sidebar.tsx:707` — `Sidebar({ activeView, onViewChange, isConnecting = false, mobile = false }: SidebarProps)` — exported: **yes** — Implements logic starting with: { if (mobile) { return <MobileNavPanel activeView={activeView} onViewChange={onViewChange} isConnecting={isConnecting} /> } return ( <div className… (missing: jsdoc)

### `web/components/gsd/status-bar.tsx`
- `web/components/gsd/status-bar.tsx:40` — `StatusBar()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/components/gsd/terminal.tsx`
- `web/components/gsd/terminal.tsx:128` — `Terminal({ className }: TerminalProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/gsd/update-banner.tsx`
- `web/components/gsd/update-banner.tsx:18` — `UpdateBanner()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/components/gsd/visualizer-view.tsx`
- `web/components/gsd/visualizer-view.tsx:1942` — `VisualizerView()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/components/theme-provider.tsx`
- `web/components/theme-provider.tsx:9` — `ThemeProvider({ children, ...props }: ThemeProviderProps)` — exported: **yes** — Implements logic starting with: { return <NextThemesProvider {...props}>{children}</NextThemesProvider> } (missing: jsdoc)

### `web/components/ui/accordion.tsx`
- `web/components/ui/accordion.tsx:9` — `Accordion({ ...props }: React.ComponentProps<typeof AccordionPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <AccordionPrimitive.Root data-slot="accordion" {...props} /> } (missing: jsdoc)
- `web/components/ui/accordion.tsx:15` — `AccordionItem({ className, ...props }: React.ComponentProps<typeof AccordionPrimitive.Item>)` — exported: **yes** — Implements logic starting with: { return ( <AccordionPrimitive.Item data-slot="accordion-item" className={cn('border-b last:border-b-0', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/accordion.tsx:28` — `AccordionTrigger({ className, children, ...props }: React.ComponentProps<typeof AccordionPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <AccordionPrimitive.Header className="flex"> <AccordionPrimitive.Trigger data-slot="accordion-trigger" className={cn( 'focus-visible:bor… (missing: jsdoc)
- `web/components/ui/accordion.tsx:50` — `AccordionContent({ className, children, ...props }: React.ComponentProps<typeof AccordionPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <AccordionPrimitive.Content data-slot="accordion-content" className="data-[state=closed]:animate-accordion-up data-[state=open]:animate-… (missing: jsdoc)

### `web/components/ui/alert-dialog.tsx`
- `web/components/ui/alert-dialog.tsx:9` — `AlertDialog({ ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <AlertDialogPrimitive.Root data-slot="alert-dialog" {...props} /> } (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:15` — `AlertDialogTrigger({ ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Trigger data-slot="alert-dialog-trigger" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:23` — `AlertDialogPortal({ ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Portal>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Portal data-slot="alert-dialog-portal" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:31` — `AlertDialogOverlay({ className, ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Overlay>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Overlay data-slot="alert-dialog-overlay" className={cn( 'data-[state=open]:animate-in data-[state=closed]:animate-… (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:47` — `AlertDialogContent({ className, ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPortal> <AlertDialogOverlay /> <AlertDialogPrimitive.Content data-slot="alert-dialog-content" className={cn( 'bg-background… (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:66` — `AlertDialogHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="alert-dialog-header" className={cn('flex flex-col gap-2 text-center sm:text-left', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:79` — `AlertDialogFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="alert-dialog-footer" className={cn( 'flex flex-col-reverse gap-2 sm:flex-row sm:justify-end', className, )} {...props} /… (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:95` — `AlertDialogTitle({ className, ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Title>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Title data-slot="alert-dialog-title" className={cn('text-lg font-semibold', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:108` — `AlertDialogDescription({ className, ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Description>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Description data-slot="alert-dialog-description" className={cn('text-muted-foreground text-sm', className)} {...pr… (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:121` — `AlertDialogAction({ className, ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Action>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Action className={cn(buttonVariants(), className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/alert-dialog.tsx:133` — `AlertDialogCancel({ className, ...props }: React.ComponentProps<typeof AlertDialogPrimitive.Cancel>)` — exported: **yes** — Implements logic starting with: { return ( <AlertDialogPrimitive.Cancel className={cn(buttonVariants({ variant: 'outline' }), className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/alert.tsx`
- `web/components/ui/alert.tsx:22` — `Alert({ className, variant, ...props }: React.ComponentProps<'div'> & VariantProps<typeof alertVariants>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="alert" role="alert" className={cn(alertVariants({ variant }), className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/alert.tsx:37` — `AlertTitle({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="alert-title" className={cn( 'col-start-2 line-clamp-1 min-h-4 font-medium tracking-tight', className, )} {...props} /> )… (missing: jsdoc)
- `web/components/ui/alert.tsx:50` — `AlertDescription({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="alert-description" className={cn( 'text-muted-foreground col-start-2 grid justify-items-start gap-1 text-sm [&_p]:leadin… (missing: jsdoc)

### `web/components/ui/aspect-ratio.tsx`
- `web/components/ui/aspect-ratio.tsx:5` — `AspectRatio({ ...props }: React.ComponentProps<typeof AspectRatioPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <AspectRatioPrimitive.Root data-slot="aspect-ratio" {...props} /> } (missing: jsdoc)

### `web/components/ui/avatar.tsx`
- `web/components/ui/avatar.tsx:8` — `Avatar({ className, ...props }: React.ComponentProps<typeof AvatarPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <AvatarPrimitive.Root data-slot="avatar" className={cn( 'relative flex size-8 shrink-0 overflow-hidden rounded-full', className, )} {...… (missing: jsdoc)
- `web/components/ui/avatar.tsx:24` — `AvatarImage({ className, ...props }: React.ComponentProps<typeof AvatarPrimitive.Image>)` — exported: **yes** — Implements logic starting with: { return ( <AvatarPrimitive.Image data-slot="avatar-image" className={cn('aspect-square size-full', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/avatar.tsx:37` — `AvatarFallback({ className, ...props }: React.ComponentProps<typeof AvatarPrimitive.Fallback>)` — exported: **yes** — Implements logic starting with: { return ( <AvatarPrimitive.Fallback data-slot="avatar-fallback" className={cn( 'bg-muted flex size-full items-center justify-center rounded-full',… (missing: jsdoc)

### `web/components/ui/badge.tsx`
- `web/components/ui/badge.tsx:28` — `Badge({ className, variant, asChild = false, ...props }: React.ComponentProps<'span'> & VariantProps<typeof badgeVariants> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'span' return ( <Comp data-slot="badge" className={cn(badgeVariants({ variant }), className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/breadcrumb.tsx`
- `web/components/ui/breadcrumb.tsx:7` — `Breadcrumb({ ...props }: React.ComponentProps<'nav'>)` — exported: **yes** — Implements logic starting with: { return <nav aria-label="breadcrumb" data-slot="breadcrumb" {...props} /> } (missing: jsdoc)
- `web/components/ui/breadcrumb.tsx:11` — `BreadcrumbList({ className, ...props }: React.ComponentProps<'ol'>)` — exported: **yes** — Implements logic starting with: { return ( <ol data-slot="breadcrumb-list" className={cn( 'text-muted-foreground flex flex-wrap items-center gap-1.5 text-sm break-words sm:gap-2.5… (missing: jsdoc)
- `web/components/ui/breadcrumb.tsx:24` — `BreadcrumbItem({ className, ...props }: React.ComponentProps<'li'>)` — exported: **yes** — Implements logic starting with: { return ( <li data-slot="breadcrumb-item" className={cn('inline-flex items-center gap-1.5', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/breadcrumb.tsx:34` — `BreadcrumbLink({ asChild, className, ...props }: React.ComponentProps<'a'> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'a' return ( <Comp data-slot="breadcrumb-link" className={cn('hover:text-foreground transition-colors', className)}… (missing: jsdoc)
- `web/components/ui/breadcrumb.tsx:52` — `BreadcrumbPage({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span data-slot="breadcrumb-page" role="link" aria-disabled="true" aria-current="page" className={cn('text-foreground font-normal', clas… (missing: jsdoc)
- `web/components/ui/breadcrumb.tsx:65` — `BreadcrumbSeparator({ children, className, ...props }: React.ComponentProps<'li'>)` — exported: **yes** — Implements logic starting with: { return ( <li data-slot="breadcrumb-separator" role="presentation" aria-hidden="true" className={cn('[&>svg]:size-3.5', className)} {...props} > {… (missing: jsdoc)
- `web/components/ui/breadcrumb.tsx:83` — `BreadcrumbEllipsis({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span data-slot="breadcrumb-ellipsis" role="presentation" aria-hidden="true" className={cn('flex size-9 items-center justify-center', cl… (missing: jsdoc)

### `web/components/ui/button-group.tsx`
- `web/components/ui/button-group.tsx:24` — `ButtonGroup({ className, orientation, ...props }: React.ComponentProps<'div'> & VariantProps<typeof buttonGroupVariants>)` — exported: **yes** — Implements logic starting with: { return ( <div role="group" data-slot="button-group" data-orientation={orientation} className={cn(buttonGroupVariants({ orientation }), className)… (missing: jsdoc)
- `web/components/ui/button-group.tsx:40` — `ButtonGroupText({ className, asChild = false, ...props }: React.ComponentProps<'div'> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'div' return ( <Comp className={cn( "bg-muted flex items-center gap-2 rounded-md border px-4 text-sm font-medium sh… (missing: jsdoc)
- `web/components/ui/button-group.tsx:60` — `ButtonGroupSeparator({ className, orientation = 'vertical', ...props }: React.ComponentProps<typeof Separator>)` — exported: **yes** — Implements logic starting with: { return ( <Separator data-slot="button-group-separator" orientation={orientation} className={cn( 'bg-input relative !m-0 self-stretch data-[orient… (missing: jsdoc)

### `web/components/ui/button.tsx`
- `web/components/ui/button.tsx:39` — `Button({ className, variant, size, asChild = false, ...props }: React.ComponentProps<'button'> & VariantProps<typeof buttonVariants> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'button' return ( <Comp data-slot="button" className={cn(buttonVariants({ variant, size, className }))} {...props}… (missing: jsdoc)

### `web/components/ui/calendar.tsx`
- `web/components/ui/calendar.tsx:14` — `Calendar({ className, classNames, showOutsideDays = true, captionLayout = 'label', buttonVariant = 'ghost', formatters, components, ...props }: React.ComponentProps<typeof DayPicker> & { buttonVariant?: React.ComponentProps<typeof Button>['variant'] })` — exported: **yes** — Implements logic starting with: { const defaultClassNames = getDefaultClassNames() return ( <DayPicker showOutsideDays={showOutsideDays} className={cn( 'bg-background group/calend… (missing: jsdoc)
- `web/components/ui/calendar.tsx:175` — `CalendarDayButton({ className, day, modifiers, ...props }: React.ComponentProps<typeof DayButton>)` — exported: **yes** — Implements logic starting with: { const defaultClassNames = getDefaultClassNames() const ref = React.useRef<HTMLButtonElement>(null) React.useEffect(() => { if (modifiers.focused)… (missing: jsdoc)

### `web/components/ui/card.tsx`
- `web/components/ui/card.tsx:5` — `Card({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card" className={cn( 'bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm', className, )}… (missing: jsdoc)
- `web/components/ui/card.tsx:18` — `CardHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card-header" className={cn( '@container/card-header grid auto-rows-min grid-rows-[auto_auto] items-start gap-2 px-6 has-… (missing: jsdoc)
- `web/components/ui/card.tsx:31` — `CardTitle({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card-title" className={cn('leading-none font-semibold', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/card.tsx:41` — `CardDescription({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card-description" className={cn('text-muted-foreground text-sm', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/card.tsx:51` — `CardAction({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card-action" className={cn( 'col-start-2 row-span-2 row-start-1 self-start justify-self-end', className, )} {...props} /… (missing: jsdoc)
- `web/components/ui/card.tsx:64` — `CardContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card-content" className={cn('px-6', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/card.tsx:74` — `CardFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="card-footer" className={cn('flex items-center px-6 [.border-t]:pt-6', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/carousel.tsx`
- `web/components/ui/carousel.tsx:45` — `Carousel({ orientation = 'horizontal', opts, setApi, plugins, className, children, ...props }: React.ComponentProps<'div'> & CarouselProps)` — exported: **yes** — Implements logic starting with: { const [carouselRef, api] = useEmblaCarousel( { ...opts, axis: orientation === 'horizontal' ? 'x' : 'y', }, plugins, ) const [canScrollPrev, setCa… (missing: jsdoc)
- `web/components/ui/carousel.tsx:135` — `CarouselContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { const { carouselRef, orientation } = useCarousel() return ( <div ref={carouselRef} className="overflow-hidden" data-slot="carousel-content" > <di… (missing: jsdoc)
- `web/components/ui/carousel.tsx:156` — `CarouselItem({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { const { orientation } = useCarousel() return ( <div role="group" aria-roledescription="slide" data-slot="carousel-item" className={cn( 'min-w-0 s… (missing: jsdoc)
- `web/components/ui/carousel.tsx:174` — `CarouselPrevious({ className, variant = 'outline', size = 'icon', ...props }: React.ComponentProps<typeof Button>)` — exported: **yes** — Implements logic starting with: { const { orientation, scrollPrev, canScrollPrev } = useCarousel() return ( <Button data-slot="carousel-previous" variant={variant} size={size} cla… (missing: jsdoc)
- `web/components/ui/carousel.tsx:204` — `CarouselNext({ className, variant = 'outline', size = 'icon', ...props }: React.ComponentProps<typeof Button>)` — exported: **yes** — Implements logic starting with: { const { orientation, scrollNext, canScrollNext } = useCarousel() return ( <Button data-slot="carousel-next" variant={variant} size={size} classNa… (missing: jsdoc)

### `web/components/ui/chart.tsx`
- `web/components/ui/chart.tsx:37` — `ChartContainer({ id, className, children, config, ...props }: React.ComponentProps<'div'> & { config: ChartConfig children: React.ComponentProps< typeof RechartsPrimitive.ResponsiveContainer >['children'] })` — exported: **yes** — Implements logic starting with: { const uniqueId = React.useId() const chartId = `chart-${id || uniqueId.replace(/:/g, '')}` return ( <ChartContext.Provider value={{ config }}> <d… (missing: jsdoc)
- `web/components/ui/chart.tsx:72` — `ChartStyle({ id, config }: { id: string; config: ChartConfig })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/ui/chart.tsx:107` — `ChartTooltipContent({ active, payload, className, indicator = 'dot', hideLabel = false, hideIndicator = false, label, labelFormatter, labelClassName, formatter, color, nameKey, labelKey, }: React.ComponentProps<typeof RechartsPrimitive.Tooltip> & React.ComponentProps<'div'> & { hideLabel?: boolean hideIndicator?: boolean indicator?: 'line' | 'dot' | 'dashed' nameKey?: string labelKey?: string })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/ui/chart.tsx:253` — `ChartLegendContent({ className, hideIcon = false, payload, verticalAlign = 'bottom', nameKey, }: React.ComponentProps<'div'> & Pick<RechartsPrimitive.LegendProps, 'payload' | 'verticalAlign'> & { hideIcon?: boolean nameKey?: string })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/ui/checkbox.tsx`
- `web/components/ui/checkbox.tsx:9` — `Checkbox({ className, ...props }: React.ComponentProps<typeof CheckboxPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <CheckboxPrimitive.Root data-slot="checkbox" className={cn( 'peer border-input dark:bg-input/30 data-[state=checked]:bg-primary data-[st… (missing: jsdoc)

### `web/components/ui/collapsible.tsx`
- `web/components/ui/collapsible.tsx:5` — `Collapsible({ ...props }: React.ComponentProps<typeof CollapsiblePrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <CollapsiblePrimitive.Root data-slot="collapsible" {...props} /> } (missing: jsdoc)
- `web/components/ui/collapsible.tsx:11` — `CollapsibleTrigger({ ...props }: React.ComponentProps<typeof CollapsiblePrimitive.CollapsibleTrigger>)` — exported: **yes** — Implements logic starting with: { return ( <CollapsiblePrimitive.CollapsibleTrigger data-slot="collapsible-trigger" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/collapsible.tsx:22` — `CollapsibleContent({ ...props }: React.ComponentProps<typeof CollapsiblePrimitive.CollapsibleContent>)` — exported: **yes** — Implements logic starting with: { return ( <CollapsiblePrimitive.CollapsibleContent data-slot="collapsible-content" {...props} /> ) } (missing: jsdoc)

### `web/components/ui/command.tsx`
- `web/components/ui/command.tsx:16` — `Command({ className, ...props }: React.ComponentProps<typeof CommandPrimitive>)` — exported: **yes** — Implements logic starting with: { return ( <CommandPrimitive data-slot="command" className={cn( 'bg-popover text-popover-foreground flex h-full w-full flex-col overflow-hidden rou… (missing: jsdoc)
- `web/components/ui/command.tsx:32` — `CommandDialog({ title = 'Command Palette', description = 'Search for a command to run...', children, className, showCloseButton = true, ...props }: React.ComponentProps<typeof Dialog> & { title?: string description?: string className?: string showCloseButton?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <Dialog {...props}> <DialogHeader className="sr-only"> <DialogTitle>{title}</DialogTitle> <DialogDescription>{description}</DialogDescri… (missing: jsdoc)
- `web/components/ui/command.tsx:63` — `CommandInput({ className, ...props }: React.ComponentProps<typeof CommandPrimitive.Input>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="command-input-wrapper" className="flex h-9 items-center gap-2 border-b px-3" > <SearchIcon className="size-4 shrink-0 op… (missing: jsdoc)
- `web/components/ui/command.tsx:85` — `CommandList({ className, ...props }: React.ComponentProps<typeof CommandPrimitive.List>)` — exported: **yes** — Implements logic starting with: { return ( <CommandPrimitive.List data-slot="command-list" className={cn( 'max-h-[300px] scroll-py-1 overflow-x-hidden overflow-y-auto', className,… (missing: jsdoc)
- `web/components/ui/command.tsx:101` — `CommandEmpty({ ...props }: React.ComponentProps<typeof CommandPrimitive.Empty>)` — exported: **yes** — Implements logic starting with: { return ( <CommandPrimitive.Empty data-slot="command-empty" className="py-6 text-center text-sm" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/command.tsx:113` — `CommandGroup({ className, ...props }: React.ComponentProps<typeof CommandPrimitive.Group>)` — exported: **yes** — Implements logic starting with: { return ( <CommandPrimitive.Group data-slot="command-group" className={cn( 'text-foreground [&_[cmdk-group-heading]]:text-muted-foreground overflo… (missing: jsdoc)
- `web/components/ui/command.tsx:129` — `CommandSeparator({ className, ...props }: React.ComponentProps<typeof CommandPrimitive.Separator>)` — exported: **yes** — Implements logic starting with: { return ( <CommandPrimitive.Separator data-slot="command-separator" className={cn('bg-border -mx-1 h-px', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/command.tsx:142` — `CommandItem({ className, ...props }: React.ComponentProps<typeof CommandPrimitive.Item>)` — exported: **yes** — Implements logic starting with: { return ( <CommandPrimitive.Item data-slot="command-item" className={cn( "data-[selected=true]:bg-accent data-[selected=true]:text-accent-foregrou… (missing: jsdoc)
- `web/components/ui/command.tsx:158` — `CommandShortcut({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span data-slot="command-shortcut" className={cn( 'text-muted-foreground ml-auto text-xs tracking-widest', className, )} {...props} /> )… (missing: jsdoc)

### `web/components/ui/context-menu.tsx`
- `web/components/ui/context-menu.tsx:9` — `ContextMenu({ ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <ContextMenuPrimitive.Root data-slot="context-menu" {...props} /> } (missing: jsdoc)
- `web/components/ui/context-menu.tsx:15` — `ContextMenuTrigger({ ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Trigger data-slot="context-menu-trigger" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/context-menu.tsx:23` — `ContextMenuGroup({ ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Group>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Group data-slot="context-menu-group" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/context-menu.tsx:31` — `ContextMenuPortal({ ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Portal>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Portal data-slot="context-menu-portal" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/context-menu.tsx:39` — `ContextMenuSub({ ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Sub>)` — exported: **yes** — Implements logic starting with: { return <ContextMenuPrimitive.Sub data-slot="context-menu-sub" {...props} /> } (missing: jsdoc)
- `web/components/ui/context-menu.tsx:45` — `ContextMenuRadioGroup({ ...props }: React.ComponentProps<typeof ContextMenuPrimitive.RadioGroup>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.RadioGroup data-slot="context-menu-radio-group" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/context-menu.tsx:56` — `ContextMenuSubTrigger({ className, inset, children, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.SubTrigger> & { inset?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.SubTrigger data-slot="context-menu-sub-trigger" data-inset={inset} className={cn( "focus:bg-accent focus:text-acce… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:80` — `ContextMenuSubContent({ className, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.SubContent>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.SubContent data-slot="context-menu-sub-content" className={cn( 'bg-popover text-popover-foreground data-[state=ope… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:96` — `ContextMenuContent({ className, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Portal> <ContextMenuPrimitive.Content data-slot="context-menu-content" className={cn( 'bg-popover text-popover-for… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:114` — `ContextMenuItem({ className, inset, variant = 'default', ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Item> & { inset?: boolean variant?: 'default' | 'destructive' })` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Item data-slot="context-menu-item" data-inset={inset} data-variant={variant} className={cn( "focus:bg-accent focus… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:137` — `ContextMenuCheckboxItem({ className, children, checked, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.CheckboxItem>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.CheckboxItem data-slot="context-menu-checkbox-item" className={cn( "focus:bg-accent focus:text-accent-foreground r… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:163` — `ContextMenuRadioItem({ className, children, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.RadioItem>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.RadioItem data-slot="context-menu-radio-item" className={cn( "focus:bg-accent focus:text-accent-foreground relativ… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:187` — `ContextMenuLabel({ className, inset, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Label> & { inset?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Label data-slot="context-menu-label" data-inset={inset} className={cn( 'text-foreground px-2 py-1.5 text-sm font-m… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:207` — `ContextMenuSeparator({ className, ...props }: React.ComponentProps<typeof ContextMenuPrimitive.Separator>)` — exported: **yes** — Implements logic starting with: { return ( <ContextMenuPrimitive.Separator data-slot="context-menu-separator" className={cn('bg-border -mx-1 my-1 h-px', className)} {...props} />… (missing: jsdoc)
- `web/components/ui/context-menu.tsx:220` — `ContextMenuShortcut({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span data-slot="context-menu-shortcut" className={cn( 'text-muted-foreground ml-auto text-xs tracking-widest', className, )} {...props}… (missing: jsdoc)

### `web/components/ui/dialog.tsx`
- `web/components/ui/dialog.tsx:9` — `Dialog({ ...props }: React.ComponentProps<typeof DialogPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <DialogPrimitive.Root data-slot="dialog" {...props} /> } (missing: jsdoc)
- `web/components/ui/dialog.tsx:15` — `DialogTrigger({ ...props }: React.ComponentProps<typeof DialogPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return <DialogPrimitive.Trigger data-slot="dialog-trigger" {...props} /> } (missing: jsdoc)
- `web/components/ui/dialog.tsx:21` — `DialogPortal({ ...props }: React.ComponentProps<typeof DialogPrimitive.Portal>)` — exported: **yes** — Implements logic starting with: { return <DialogPrimitive.Portal data-slot="dialog-portal" {...props} /> } (missing: jsdoc)
- `web/components/ui/dialog.tsx:27` — `DialogClose({ ...props }: React.ComponentProps<typeof DialogPrimitive.Close>)` — exported: **yes** — Implements logic starting with: { return <DialogPrimitive.Close data-slot="dialog-close" {...props} /> } (missing: jsdoc)
- `web/components/ui/dialog.tsx:33` — `DialogOverlay({ className, ...props }: React.ComponentProps<typeof DialogPrimitive.Overlay>)` — exported: **yes** — Implements logic starting with: { return ( <DialogPrimitive.Overlay data-slot="dialog-overlay" className={cn( 'data-[state=open]:animate-in data-[state=closed]:animate-out data-[s… (missing: jsdoc)
- `web/components/ui/dialog.tsx:49` — `DialogContent({ className, children, showCloseButton = true, ...props }: React.ComponentProps<typeof DialogPrimitive.Content> & { showCloseButton?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <DialogPortal data-slot="dialog-portal"> <DialogOverlay /> <DialogPrimitive.Content data-slot="dialog-content" className={cn( 'bg-backgr… (missing: jsdoc)
- `web/components/ui/dialog.tsx:83` — `DialogHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="dialog-header" className={cn('flex flex-col gap-2 text-center sm:text-left', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dialog.tsx:93` — `DialogFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="dialog-footer" className={cn( 'flex flex-col-reverse gap-2 sm:flex-row sm:justify-end', className, )} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dialog.tsx:106` — `DialogTitle({ className, ...props }: React.ComponentProps<typeof DialogPrimitive.Title>)` — exported: **yes** — Implements logic starting with: { return ( <DialogPrimitive.Title data-slot="dialog-title" className={cn('text-lg leading-none font-semibold', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dialog.tsx:119` — `DialogDescription({ className, ...props }: React.ComponentProps<typeof DialogPrimitive.Description>)` — exported: **yes** — Implements logic starting with: { return ( <DialogPrimitive.Description data-slot="dialog-description" className={cn('text-muted-foreground text-sm', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/drawer.tsx`
- `web/components/ui/drawer.tsx:8` — `Drawer({ ...props }: React.ComponentProps<typeof DrawerPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <DrawerPrimitive.Root data-slot="drawer" {...props} /> } (missing: jsdoc)
- `web/components/ui/drawer.tsx:14` — `DrawerTrigger({ ...props }: React.ComponentProps<typeof DrawerPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return <DrawerPrimitive.Trigger data-slot="drawer-trigger" {...props} /> } (missing: jsdoc)
- `web/components/ui/drawer.tsx:20` — `DrawerPortal({ ...props }: React.ComponentProps<typeof DrawerPrimitive.Portal>)` — exported: **yes** — Implements logic starting with: { return <DrawerPrimitive.Portal data-slot="drawer-portal" {...props} /> } (missing: jsdoc)
- `web/components/ui/drawer.tsx:26` — `DrawerClose({ ...props }: React.ComponentProps<typeof DrawerPrimitive.Close>)` — exported: **yes** — Implements logic starting with: { return <DrawerPrimitive.Close data-slot="drawer-close" {...props} /> } (missing: jsdoc)
- `web/components/ui/drawer.tsx:32` — `DrawerOverlay({ className, ...props }: React.ComponentProps<typeof DrawerPrimitive.Overlay>)` — exported: **yes** — Implements logic starting with: { return ( <DrawerPrimitive.Overlay data-slot="drawer-overlay" className={cn( 'data-[state=open]:animate-in data-[state=closed]:animate-out data-[s… (missing: jsdoc)
- `web/components/ui/drawer.tsx:48` — `DrawerContent({ className, children, ...props }: React.ComponentProps<typeof DrawerPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <DrawerPortal data-slot="drawer-portal"> <DrawerOverlay /> <DrawerPrimitive.Content data-slot="drawer-content" className={cn( 'group/dra… (missing: jsdoc)
- `web/components/ui/drawer.tsx:75` — `DrawerHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="drawer-header" className={cn( 'flex flex-col gap-0.5 p-4 group-data-[vaul-drawer-direction=bottom]/drawer-content:text-c… (missing: jsdoc)
- `web/components/ui/drawer.tsx:88` — `DrawerFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="drawer-footer" className={cn('mt-auto flex flex-col gap-2 p-4', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/drawer.tsx:98` — `DrawerTitle({ className, ...props }: React.ComponentProps<typeof DrawerPrimitive.Title>)` — exported: **yes** — Implements logic starting with: { return ( <DrawerPrimitive.Title data-slot="drawer-title" className={cn('text-foreground font-semibold', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/drawer.tsx:111` — `DrawerDescription({ className, ...props }: React.ComponentProps<typeof DrawerPrimitive.Description>)` — exported: **yes** — Implements logic starting with: { return ( <DrawerPrimitive.Description data-slot="drawer-description" className={cn('text-muted-foreground text-sm', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/dropdown-menu.tsx`
- `web/components/ui/dropdown-menu.tsx:9` — `DropdownMenu({ ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <DropdownMenuPrimitive.Root data-slot="dropdown-menu" {...props} /> } (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:15` — `DropdownMenuPortal({ ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Portal>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Portal data-slot="dropdown-menu-portal" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:23` — `DropdownMenuTrigger({ ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Trigger data-slot="dropdown-menu-trigger" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:34` — `DropdownMenuContent({ className, sideOffset = 4, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Portal> <DropdownMenuPrimitive.Content data-slot="dropdown-menu-content" sideOffset={sideOffset} className={cn( '… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:54` — `DropdownMenuGroup({ ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Group>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Group data-slot="dropdown-menu-group" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:62` — `DropdownMenuItem({ className, inset, variant = 'default', ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Item> & { inset?: boolean variant?: 'default' | 'destructive' })` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Item data-slot="dropdown-menu-item" data-inset={inset} data-variant={variant} className={cn( "focus:bg-accent foc… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:85` — `DropdownMenuCheckboxItem({ className, children, checked, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.CheckboxItem>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.CheckboxItem data-slot="dropdown-menu-checkbox-item" className={cn( "focus:bg-accent focus:text-accent-foreground… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:111` — `DropdownMenuRadioGroup({ ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.RadioGroup>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.RadioGroup data-slot="dropdown-menu-radio-group" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:122` — `DropdownMenuRadioItem({ className, children, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.RadioItem>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.RadioItem data-slot="dropdown-menu-radio-item" className={cn( "focus:bg-accent focus:text-accent-foreground relat… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:146` — `DropdownMenuLabel({ className, inset, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Label> & { inset?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Label data-slot="dropdown-menu-label" data-inset={inset} className={cn( 'px-2 py-1.5 text-sm font-medium data-[in… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:166` — `DropdownMenuSeparator({ className, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Separator>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.Separator data-slot="dropdown-menu-separator" className={cn('bg-border -mx-1 my-1 h-px', className)} {...props} /… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:179` — `DropdownMenuShortcut({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span data-slot="dropdown-menu-shortcut" className={cn( 'text-muted-foreground ml-auto text-xs tracking-widest', className, )} {...props… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:195` — `DropdownMenuSub({ ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.Sub>)` — exported: **yes** — Implements logic starting with: { return <DropdownMenuPrimitive.Sub data-slot="dropdown-menu-sub" {...props} /> } (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:201` — `DropdownMenuSubTrigger({ className, inset, children, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.SubTrigger> & { inset?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.SubTrigger data-slot="dropdown-menu-sub-trigger" data-inset={inset} className={cn( "focus:bg-accent focus:text-ac… (missing: jsdoc)
- `web/components/ui/dropdown-menu.tsx:225` — `DropdownMenuSubContent({ className, ...props }: React.ComponentProps<typeof DropdownMenuPrimitive.SubContent>)` — exported: **yes** — Implements logic starting with: { return ( <DropdownMenuPrimitive.SubContent data-slot="dropdown-menu-sub-content" className={cn( 'bg-popover text-popover-foreground data-[state=o… (missing: jsdoc)

### `web/components/ui/empty.tsx`
- `web/components/ui/empty.tsx:5` — `Empty({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="empty" className={cn( 'flex min-w-0 flex-1 flex-col items-center justify-center gap-6 rounded-lg border-dashed p-6 text-… (missing: jsdoc)
- `web/components/ui/empty.tsx:18` — `EmptyHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="empty-header" className={cn( 'flex max-w-sm flex-col items-center gap-2 text-center', className, )} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/empty.tsx:46` — `EmptyMedia({ className, variant = 'default', ...props }: React.ComponentProps<'div'> & VariantProps<typeof emptyMediaVariants>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="empty-icon" data-variant={variant} className={cn(emptyMediaVariants({ variant, className }))} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/empty.tsx:61` — `EmptyTitle({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="empty-title" className={cn('text-lg font-medium tracking-tight', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/empty.tsx:71` — `EmptyDescription({ className, ...props }: React.ComponentProps<'p'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="empty-description" className={cn( 'text-muted-foreground [&>a:hover]:text-primary text-sm/relaxed [&>a]:underline [&>a]:… (missing: jsdoc)
- `web/components/ui/empty.tsx:84` — `EmptyContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="empty-content" className={cn( 'flex w-full max-w-sm min-w-0 flex-col items-center gap-4 text-sm text-balance', className… (missing: jsdoc)

### `web/components/ui/field.tsx`
- `web/components/ui/field.tsx:10` — `FieldSet({ className, ...props }: React.ComponentProps<'fieldset'>)` — exported: **yes** — Implements logic starting with: { return ( <fieldset data-slot="field-set" className={cn( 'flex flex-col gap-6', 'has-[>[data-slot=checkbox-group]]:gap-3 has-[>[data-slot=radio-gr… (missing: jsdoc)
- `web/components/ui/field.tsx:24` — `FieldLegend({ className, variant = 'legend', ...props }: React.ComponentProps<'legend'> & { variant?: 'legend' | 'label' })` — exported: **yes** — Implements logic starting with: { return ( <legend data-slot="field-legend" data-variant={variant} className={cn( 'mb-3 font-medium', 'data-[variant=legend]:text-base', 'data-[var… (missing: jsdoc)
- `web/components/ui/field.tsx:44` — `FieldGroup({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="field-group" className={cn( 'group/field-group @container/field-group flex w-full flex-col gap-7 data-[slot=checkbox-gro… (missing: jsdoc)
- `web/components/ui/field.tsx:81` — `Field({ className, orientation = 'vertical', ...props }: React.ComponentProps<'div'> & VariantProps<typeof fieldVariants>)` — exported: **yes** — Implements logic starting with: { return ( <div role="group" data-slot="field" data-orientation={orientation} className={cn(fieldVariants({ orientation }), className)} {...props}… (missing: jsdoc)
- `web/components/ui/field.tsx:97` — `FieldContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="field-content" className={cn( 'group/field-content flex flex-1 flex-col gap-1.5 leading-snug', className, )} {...props}… (missing: jsdoc)
- `web/components/ui/field.tsx:110` — `FieldLabel({ className, ...props }: React.ComponentProps<typeof Label>)` — exported: **yes** — Implements logic starting with: { return ( <Label data-slot="field-label" className={cn( 'group/field-label peer/field-label flex w-fit gap-2 leading-snug group-data-[disabled=tru… (missing: jsdoc)
- `web/components/ui/field.tsx:128` — `FieldTitle({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="field-label" className={cn( 'flex w-fit items-center gap-2 text-sm leading-snug font-medium group-data-[disabled=true]/f… (missing: jsdoc)
- `web/components/ui/field.tsx:141` — `FieldDescription({ className, ...props }: React.ComponentProps<'p'>)` — exported: **yes** — Implements logic starting with: { return ( <p data-slot="field-description" className={cn( 'text-muted-foreground text-sm leading-normal font-normal group-has-[[data-orientation=h… (missing: jsdoc)
- `web/components/ui/field.tsx:156` — `FieldSeparator({ children, className, ...props }: React.ComponentProps<'div'> & { children?: React.ReactNode })` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="field-separator" data-content={!!children} className={cn( 'relative -my-2 h-5 text-sm group-data-[variant=outline]/field… (missing: jsdoc)
- `web/components/ui/field.tsx:186` — `FieldError({ className, children, errors, ...props }: React.ComponentProps<'div'> & { errors?: Array<{ message?: string } | undefined> })` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/ui/form.tsx`
- `web/components/ui/form.tsx:32` — `FormField({ ...props }: ControllerProps<TFieldValues, TName>)` — exported: **yes** — Implements logic starting with: { return ( <FormFieldContext.Provider value={{ name: props.name }}> <Controller {...props} /> </FormFieldContext.Provider> ) } (missing: jsdoc)
- `web/components/ui/form.tsx:45` — `useFormField()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `web/components/ui/form.tsx:76` — `FormItem({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { const id = React.useId() return ( <FormItemContext.Provider value={{ id }}> <div data-slot="form-item" className={cn('grid gap-2', className)} {.… (missing: jsdoc)
- `web/components/ui/form.tsx:90` — `FormLabel({ className, ...props }: React.ComponentProps<typeof LabelPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { const { error, formItemId } = useFormField() return ( <Label data-slot="form-label" data-error={!!error} className={cn('data-[error=true]:text-de… (missing: jsdoc)
- `web/components/ui/form.tsx:107` — `FormControl({ ...props }: React.ComponentProps<typeof Slot>)` — exported: **yes** — Implements logic starting with: { const { error, formItemId, formDescriptionId, formMessageId } = useFormField() return ( <Slot data-slot="form-control" id={formItemId} aria-descr… (missing: jsdoc)
- `web/components/ui/form.tsx:125` — `FormDescription({ className, ...props }: React.ComponentProps<'p'>)` — exported: **yes** — Implements logic starting with: { const { formDescriptionId } = useFormField() return ( <p data-slot="form-description" id={formDescriptionId} className={cn('text-muted-foreground… (missing: jsdoc)
- `web/components/ui/form.tsx:138` — `FormMessage({ className, ...props }: React.ComponentProps<'p'>)` — exported: **yes** — Implements logic starting with: { const { error, formMessageId } = useFormField() const body = error ? String(error?.message ?? '') : props.children if (!body) { return null } ret… (missing: jsdoc)

### `web/components/ui/hover-card.tsx`
- `web/components/ui/hover-card.tsx:8` — `HoverCard({ ...props }: React.ComponentProps<typeof HoverCardPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <HoverCardPrimitive.Root data-slot="hover-card" {...props} /> } (missing: jsdoc)
- `web/components/ui/hover-card.tsx:14` — `HoverCardTrigger({ ...props }: React.ComponentProps<typeof HoverCardPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <HoverCardPrimitive.Trigger data-slot="hover-card-trigger" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/hover-card.tsx:22` — `HoverCardContent({ className, align = 'center', sideOffset = 4, ...props }: React.ComponentProps<typeof HoverCardPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <HoverCardPrimitive.Portal data-slot="hover-card-portal"> <HoverCardPrimitive.Content data-slot="hover-card-content" align={align} sideO… (missing: jsdoc)

### `web/components/ui/input-group.tsx`
- `web/components/ui/input-group.tsx:10` — `InputGroup({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="input-group" role="group" className={cn( 'group/input-group border-input dark:bg-input/30 relative flex w-full items-cen… (missing: jsdoc)
- `web/components/ui/input-group.tsx:59` — `InputGroupAddon({ className, align = 'inline-start', ...props }: React.ComponentProps<'div'> & VariantProps<typeof inputGroupAddonVariants>)` — exported: **yes** — Implements logic starting with: { return ( <div role="group" data-slot="input-group-addon" data-align={align} className={cn(inputGroupAddonVariants({ align }), className)} onClick… (missing: jsdoc)
- `web/components/ui/input-group.tsx:99` — `InputGroupButton({ className, type = 'button', variant = 'ghost', size = 'xs', ...props }: Omit<React.ComponentProps<typeof Button>, 'size'> & VariantProps<typeof inputGroupButtonVariants>)` — exported: **yes** — Implements logic starting with: { return ( <Button type={type} data-size={size} variant={variant} className={cn(inputGroupButtonVariants({ size }), className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/input-group.tsx:118` — `InputGroupText({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span className={cn( "text-muted-foreground flex items-center gap-2 text-sm [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:si… (missing: jsdoc)
- `web/components/ui/input-group.tsx:130` — `InputGroupInput({ className, ...props }: React.ComponentProps<'input'>)` — exported: **yes** — Implements logic starting with: { return ( <Input data-slot="input-group-control" className={cn( 'flex-1 rounded-none border-0 bg-transparent shadow-none focus-visible:ring-0 dark… (missing: jsdoc)
- `web/components/ui/input-group.tsx:146` — `InputGroupTextarea({ className, ...props }: React.ComponentProps<'textarea'>)` — exported: **yes** — Implements logic starting with: { return ( <Textarea data-slot="input-group-control" className={cn( 'flex-1 resize-none rounded-none border-0 bg-transparent py-3 shadow-none focus… (missing: jsdoc)

### `web/components/ui/input-otp.tsx`
- `web/components/ui/input-otp.tsx:9` — `InputOTP({ className, containerClassName, ...props }: React.ComponentProps<typeof OTPInput> & { containerClassName?: string })` — exported: **yes** — Implements logic starting with: { return ( <OTPInput data-slot="input-otp" containerClassName={cn( 'flex items-center gap-2 has-disabled:opacity-50', containerClassName, )} classN… (missing: jsdoc)
- `web/components/ui/input-otp.tsx:29` — `InputOTPGroup({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="input-otp-group" className={cn('flex items-center', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/input-otp.tsx:39` — `InputOTPSlot({ index, className, ...props }: React.ComponentProps<'div'> & { index: number })` — exported: **yes** — Implements logic starting with: { const inputOTPContext = React.useContext(OTPInputContext) const { char, hasFakeCaret, isActive } = inputOTPContext?.slots[index] ?? {} return ( <… (missing: jsdoc)
- `web/components/ui/input-otp.tsx:69` — `InputOTPSeparator({ ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="input-otp-separator" role="separator" {...props}> <MinusIcon /> </div> ) } (missing: jsdoc)

### `web/components/ui/input.tsx`
- `web/components/ui/input.tsx:5` — `Input({ className, type, ...props }: React.ComponentProps<'input'>)` — exported: **yes** — Implements logic starting with: { return ( <input type={type} data-slot="input" className={cn( 'file:text-foreground placeholder:text-muted-foreground selection:bg-primary selecti… (missing: jsdoc)

### `web/components/ui/item.tsx`
- `web/components/ui/item.tsx:8` — `ItemGroup({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div role="list" data-slot="item-group" className={cn('group/item-group flex flex-col', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/item.tsx:19` — `ItemSeparator({ className, ...props }: React.ComponentProps<typeof Separator>)` — exported: **yes** — Implements logic starting with: { return ( <Separator data-slot="item-separator" orientation="horizontal" className={cn('my-0', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/item.tsx:54` — `Item({ className, variant = 'default', size = 'default', asChild = false, ...props }: React.ComponentProps<'div'> & VariantProps<typeof itemVariants> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'div' return ( <Comp data-slot="item" data-variant={variant} data-size={size} className={cn(itemVariants({ variant,… (missing: jsdoc)
- `web/components/ui/item.tsx:91` — `ItemMedia({ className, variant = 'default', ...props }: React.ComponentProps<'div'> & VariantProps<typeof itemMediaVariants>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="item-media" data-variant={variant} className={cn(itemMediaVariants({ variant, className }))} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/item.tsx:106` — `ItemContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="item-content" className={cn( 'flex flex-1 flex-col gap-1 [&+[data-slot=item-content]]:flex-none', className, )} {...prop… (missing: jsdoc)
- `web/components/ui/item.tsx:119` — `ItemTitle({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="item-title" className={cn( 'flex w-fit items-center gap-2 text-sm leading-snug font-medium', className, )} {...props} />… (missing: jsdoc)
- `web/components/ui/item.tsx:132` — `ItemDescription({ className, ...props }: React.ComponentProps<'p'>)` — exported: **yes** — Implements logic starting with: { return ( <p data-slot="item-description" className={cn( 'text-muted-foreground line-clamp-2 text-sm leading-normal font-normal text-balance', '[&… (missing: jsdoc)
- `web/components/ui/item.tsx:146` — `ItemActions({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="item-actions" className={cn('flex items-center gap-2', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/item.tsx:156` — `ItemHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="item-header" className={cn( 'flex basis-full items-center justify-between gap-2', className, )} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/item.tsx:169` — `ItemFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="item-footer" className={cn( 'flex basis-full items-center justify-between gap-2', className, )} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/kbd.tsx`
- `web/components/ui/kbd.tsx:3` — `Kbd({ className, ...props }: React.ComponentProps<'kbd'>)` — exported: **yes** — Implements logic starting with: { return ( <kbd data-slot="kbd" className={cn( 'bg-muted w-fit text-muted-foreground pointer-events-none inline-flex h-5 min-w-5 items-center justi… (missing: jsdoc)
- `web/components/ui/kbd.tsx:18` — `KbdGroup({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <kbd data-slot="kbd-group" className={cn('inline-flex items-center gap-1', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/label.tsx`
- `web/components/ui/label.tsx:8` — `Label({ className, ...props }: React.ComponentProps<typeof LabelPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <LabelPrimitive.Root data-slot="label" className={cn( 'flex items-center gap-2 text-sm leading-none font-medium select-none group-data-[… (missing: jsdoc)

### `web/components/ui/menubar.tsx`
- `web/components/ui/menubar.tsx:9` — `Menubar({ className, ...props }: React.ComponentProps<typeof MenubarPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.Root data-slot="menubar" className={cn( 'bg-background flex h-9 items-center gap-1 rounded-md border p-1 shadow-xs', c… (missing: jsdoc)
- `web/components/ui/menubar.tsx:25` — `MenubarMenu({ ...props }: React.ComponentProps<typeof MenubarPrimitive.Menu>)` — exported: **yes** — Implements logic starting with: { return <MenubarPrimitive.Menu data-slot="menubar-menu" {...props} /> } (missing: jsdoc)
- `web/components/ui/menubar.tsx:31` — `MenubarGroup({ ...props }: React.ComponentProps<typeof MenubarPrimitive.Group>)` — exported: **yes** — Implements logic starting with: { return <MenubarPrimitive.Group data-slot="menubar-group" {...props} /> } (missing: jsdoc)
- `web/components/ui/menubar.tsx:37` — `MenubarPortal({ ...props }: React.ComponentProps<typeof MenubarPrimitive.Portal>)` — exported: **yes** — Implements logic starting with: { return <MenubarPrimitive.Portal data-slot="menubar-portal" {...props} /> } (missing: jsdoc)
- `web/components/ui/menubar.tsx:43` — `MenubarRadioGroup({ ...props }: React.ComponentProps<typeof MenubarPrimitive.RadioGroup>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.RadioGroup data-slot="menubar-radio-group" {...props} /> ) } (missing: jsdoc)
- `web/components/ui/menubar.tsx:51` — `MenubarTrigger({ className, ...props }: React.ComponentProps<typeof MenubarPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.Trigger data-slot="menubar-trigger" className={cn( 'focus:bg-accent focus:text-accent-foreground data-[state=open]:bg-… (missing: jsdoc)
- `web/components/ui/menubar.tsx:67` — `MenubarContent({ className, align = 'start', alignOffset = -4, sideOffset = 8, ...props }: React.ComponentProps<typeof MenubarPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPortal> <MenubarPrimitive.Content data-slot="menubar-content" align={align} alignOffset={alignOffset} sideOffset={sideOffset} cl… (missing: jsdoc)
- `web/components/ui/menubar.tsx:91` — `MenubarItem({ className, inset, variant = 'default', ...props }: React.ComponentProps<typeof MenubarPrimitive.Item> & { inset?: boolean variant?: 'default' | 'destructive' })` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.Item data-slot="menubar-item" data-inset={inset} data-variant={variant} className={cn( "focus:bg-accent focus:text-acc… (missing: jsdoc)
- `web/components/ui/menubar.tsx:114` — `MenubarCheckboxItem({ className, children, checked, ...props }: React.ComponentProps<typeof MenubarPrimitive.CheckboxItem>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.CheckboxItem data-slot="menubar-checkbox-item" className={cn( "focus:bg-accent focus:text-accent-foreground relative f… (missing: jsdoc)
- `web/components/ui/menubar.tsx:140` — `MenubarRadioItem({ className, children, ...props }: React.ComponentProps<typeof MenubarPrimitive.RadioItem>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.RadioItem data-slot="menubar-radio-item" className={cn( "focus:bg-accent focus:text-accent-foreground relative flex cu… (missing: jsdoc)
- `web/components/ui/menubar.tsx:164` — `MenubarLabel({ className, inset, ...props }: React.ComponentProps<typeof MenubarPrimitive.Label> & { inset?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.Label data-slot="menubar-label" data-inset={inset} className={cn( 'px-2 py-1.5 text-sm font-medium data-[inset]:pl-8',… (missing: jsdoc)
- `web/components/ui/menubar.tsx:184` — `MenubarSeparator({ className, ...props }: React.ComponentProps<typeof MenubarPrimitive.Separator>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.Separator data-slot="menubar-separator" className={cn('bg-border -mx-1 my-1 h-px', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/menubar.tsx:197` — `MenubarShortcut({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span data-slot="menubar-shortcut" className={cn( 'text-muted-foreground ml-auto text-xs tracking-widest', className, )} {...props} /> )… (missing: jsdoc)
- `web/components/ui/menubar.tsx:213` — `MenubarSub({ ...props }: React.ComponentProps<typeof MenubarPrimitive.Sub>)` — exported: **yes** — Implements logic starting with: { return <MenubarPrimitive.Sub data-slot="menubar-sub" {...props} /> } (missing: jsdoc)
- `web/components/ui/menubar.tsx:219` — `MenubarSubTrigger({ className, inset, children, ...props }: React.ComponentProps<typeof MenubarPrimitive.SubTrigger> & { inset?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.SubTrigger data-slot="menubar-sub-trigger" data-inset={inset} className={cn( 'focus:bg-accent focus:text-accent-foregr… (missing: jsdoc)
- `web/components/ui/menubar.tsx:243` — `MenubarSubContent({ className, ...props }: React.ComponentProps<typeof MenubarPrimitive.SubContent>)` — exported: **yes** — Implements logic starting with: { return ( <MenubarPrimitive.SubContent data-slot="menubar-sub-content" className={cn( 'bg-popover text-popover-foreground data-[state=open]:animat… (missing: jsdoc)

### `web/components/ui/navigation-menu.tsx`
- `web/components/ui/navigation-menu.tsx:8` — `NavigationMenu({ className, children, viewport = true, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Root> & { viewport?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.Root data-slot="navigation-menu" data-viewport={viewport} className={cn( 'group/navigation-menu relative flex m… (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:32` — `NavigationMenuList({ className, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.List>)` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.List data-slot="navigation-menu-list" className={cn( 'group flex flex-1 list-none items-center justify-center g… (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:48` — `NavigationMenuItem({ className, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Item>)` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.Item data-slot="navigation-menu-item" className={cn('relative', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:65` — `NavigationMenuTrigger({ className, children, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.Trigger data-slot="navigation-menu-trigger" className={cn(navigationMenuTriggerStyle(), 'group', className)} {.… (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:85` — `NavigationMenuContent({ className, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.Content data-slot="navigation-menu-content" className={cn( 'data-[motion^=from-]:animate-in data-[motion^=to-]:… (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:102` — `NavigationMenuViewport({ className, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Viewport>)` — exported: **yes** — Implements logic starting with: { return ( <div className="absolute top-full left-0 isolate z-50 flex justify-center" > <NavigationMenuPrimitive.Viewport data-slot="navigation-men… (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:122` — `NavigationMenuLink({ className, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Link>)` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.Link data-slot="navigation-menu-link" className={cn( "data-[active=true]:focus:bg-accent data-[active=true]:hov… (missing: jsdoc)
- `web/components/ui/navigation-menu.tsx:138` — `NavigationMenuIndicator({ className, ...props }: React.ComponentProps<typeof NavigationMenuPrimitive.Indicator>)` — exported: **yes** — Implements logic starting with: { return ( <NavigationMenuPrimitive.Indicator data-slot="navigation-menu-indicator" className={cn( 'data-[state=visible]:animate-in data-[state=hid… (missing: jsdoc)

### `web/components/ui/pagination.tsx`
- `web/components/ui/pagination.tsx:11` — `Pagination({ className, ...props }: React.ComponentProps<'nav'>)` — exported: **yes** — Implements logic starting with: { return ( <nav role="navigation" aria-label="pagination" data-slot="pagination" className={cn('mx-auto flex w-full justify-center', className)} {.… (missing: jsdoc)
- `web/components/ui/pagination.tsx:23` — `PaginationContent({ className, ...props }: React.ComponentProps<'ul'>)` — exported: **yes** — Implements logic starting with: { return ( <ul data-slot="pagination-content" className={cn('flex flex-row items-center gap-1', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/pagination.tsx:36` — `PaginationItem({ ...props }: React.ComponentProps<'li'>)` — exported: **yes** — Implements logic starting with: { return <li data-slot="pagination-item" {...props} /> } (missing: jsdoc)
- `web/components/ui/pagination.tsx:45` — `PaginationLink({ className, isActive, size = 'icon', ...props }: PaginationLinkProps)` — exported: **yes** — Implements logic starting with: { return ( <a aria-current={isActive ? 'page' : undefined} data-slot="pagination-link" data-active={isActive} className={cn( buttonVariants({ varia… (missing: jsdoc)
- `web/components/ui/pagination.tsx:68` — `PaginationPrevious({ className, ...props }: React.ComponentProps<typeof PaginationLink>)` — exported: **yes** — Implements logic starting with: { return ( <PaginationLink aria-label="Go to previous page" size="default" className={cn('gap-1 px-2.5 sm:pl-2.5', className)} {...props} > <Chevro… (missing: jsdoc)
- `web/components/ui/pagination.tsx:85` — `PaginationNext({ className, ...props }: React.ComponentProps<typeof PaginationLink>)` — exported: **yes** — Implements logic starting with: { return ( <PaginationLink aria-label="Go to next page" size="default" className={cn('gap-1 px-2.5 sm:pr-2.5', className)} {...props} > <span class… (missing: jsdoc)
- `web/components/ui/pagination.tsx:102` — `PaginationEllipsis({ className, ...props }: React.ComponentProps<'span'>)` — exported: **yes** — Implements logic starting with: { return ( <span aria-hidden data-slot="pagination-ellipsis" className={cn('flex size-9 items-center justify-center', className)} {...props} > <Mor… (missing: jsdoc)

### `web/components/ui/popover.tsx`
- `web/components/ui/popover.tsx:8` — `Popover({ ...props }: React.ComponentProps<typeof PopoverPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <PopoverPrimitive.Root data-slot="popover" {...props} /> } (missing: jsdoc)
- `web/components/ui/popover.tsx:14` — `PopoverTrigger({ ...props }: React.ComponentProps<typeof PopoverPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return <PopoverPrimitive.Trigger data-slot="popover-trigger" {...props} /> } (missing: jsdoc)
- `web/components/ui/popover.tsx:20` — `PopoverContent({ className, align = 'center', sideOffset = 4, ...props }: React.ComponentProps<typeof PopoverPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <PopoverPrimitive.Portal> <PopoverPrimitive.Content data-slot="popover-content" align={align} sideOffset={sideOffset} className={cn( 'bg… (missing: jsdoc)
- `web/components/ui/popover.tsx:42` — `PopoverAnchor({ ...props }: React.ComponentProps<typeof PopoverPrimitive.Anchor>)` — exported: **yes** — Implements logic starting with: { return <PopoverPrimitive.Anchor data-slot="popover-anchor" {...props} /> } (missing: jsdoc)

### `web/components/ui/progress.tsx`
- `web/components/ui/progress.tsx:8` — `Progress({ className, value, ...props }: React.ComponentProps<typeof ProgressPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <ProgressPrimitive.Root data-slot="progress" className={cn( 'bg-primary/20 relative h-2 w-full overflow-hidden rounded-full', className,… (missing: jsdoc)

### `web/components/ui/radio-group.tsx`
- `web/components/ui/radio-group.tsx:9` — `RadioGroup({ className, ...props }: React.ComponentProps<typeof RadioGroupPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <RadioGroupPrimitive.Root data-slot="radio-group" className={cn('grid gap-3', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/radio-group.tsx:22` — `RadioGroupItem({ className, ...props }: React.ComponentProps<typeof RadioGroupPrimitive.Item>)` — exported: **yes** — Implements logic starting with: { return ( <RadioGroupPrimitive.Item data-slot="radio-group-item" className={cn( 'border-input text-primary focus-visible:border-ring focus-visible… (missing: jsdoc)

### `web/components/ui/resizable.tsx`
- `web/components/ui/resizable.tsx:9` — `ResizablePanelGroup({ className, ...props }: React.ComponentProps<typeof ResizablePrimitive.PanelGroup>)` — exported: **yes** — Implements logic starting with: { return ( <ResizablePrimitive.PanelGroup data-slot="resizable-panel-group" className={cn( 'flex h-full w-full data-[panel-group-direction=vertical… (missing: jsdoc)
- `web/components/ui/resizable.tsx:25` — `ResizablePanel({ ...props }: React.ComponentProps<typeof ResizablePrimitive.Panel>)` — exported: **yes** — Implements logic starting with: { return <ResizablePrimitive.Panel data-slot="resizable-panel" {...props} /> } (missing: jsdoc)
- `web/components/ui/resizable.tsx:31` — `ResizableHandle({ withHandle, className, ...props }: React.ComponentProps<typeof ResizablePrimitive.PanelResizeHandle> & { withHandle?: boolean })` — exported: **yes** — Implements logic starting with: { return ( <ResizablePrimitive.PanelResizeHandle data-slot="resizable-handle" className={cn( 'bg-border focus-visible:ring-ring relative flex w-px… (missing: jsdoc)

### `web/components/ui/scroll-area.tsx`
- `web/components/ui/scroll-area.tsx:12` — `ScrollArea({ className, children, viewportRef, ...props }: ScrollAreaProps)` — exported: **yes** — Implements logic starting with: { return ( <ScrollAreaPrimitive.Root data-slot="scroll-area" className={cn('relative', className)} {...props} > <ScrollAreaPrimitive.Viewport ref={… (missing: jsdoc)
- `web/components/ui/scroll-area.tsx:37` — `ScrollBar({ className, orientation = 'vertical', ...props }: React.ComponentProps<typeof ScrollAreaPrimitive.ScrollAreaScrollbar>)` — exported: **yes** — Implements logic starting with: { return ( <ScrollAreaPrimitive.ScrollAreaScrollbar data-slot="scroll-area-scrollbar" orientation={orientation} className={cn( 'flex touch-none p-p… (missing: jsdoc)

### `web/components/ui/select.tsx`
- `web/components/ui/select.tsx:9` — `Select({ ...props }: React.ComponentProps<typeof SelectPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <SelectPrimitive.Root data-slot="select" {...props} /> } (missing: jsdoc)
- `web/components/ui/select.tsx:15` — `SelectGroup({ ...props }: React.ComponentProps<typeof SelectPrimitive.Group>)` — exported: **yes** — Implements logic starting with: { return <SelectPrimitive.Group data-slot="select-group" {...props} /> } (missing: jsdoc)
- `web/components/ui/select.tsx:21` — `SelectValue({ ...props }: React.ComponentProps<typeof SelectPrimitive.Value>)` — exported: **yes** — Implements logic starting with: { return <SelectPrimitive.Value data-slot="select-value" {...props} /> } (missing: jsdoc)
- `web/components/ui/select.tsx:27` — `SelectTrigger({ className, size = 'default', children, ...props }: React.ComponentProps<typeof SelectPrimitive.Trigger> & { size?: 'sm' | 'default' })` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.Trigger data-slot="select-trigger" data-size={size} className={cn( "border-input data-[placeholder]:text-muted-foregrou… (missing: jsdoc)
- `web/components/ui/select.tsx:53` — `SelectContent({ className, children, position = 'popper', ...props }: React.ComponentProps<typeof SelectPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.Portal> <SelectPrimitive.Content data-slot="select-content" className={cn( 'bg-popover text-popover-foreground data-[st… (missing: jsdoc)
- `web/components/ui/select.tsx:88` — `SelectLabel({ className, ...props }: React.ComponentProps<typeof SelectPrimitive.Label>)` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.Label data-slot="select-label" className={cn('text-muted-foreground px-2 py-1.5 text-xs', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/select.tsx:101` — `SelectItem({ className, children, ...props }: React.ComponentProps<typeof SelectPrimitive.Item>)` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.Item data-slot="select-item" className={cn( "focus:bg-accent focus:text-accent-foreground [&_svg:not([class*='text-'])]… (missing: jsdoc)
- `web/components/ui/select.tsx:125` — `SelectSeparator({ className, ...props }: React.ComponentProps<typeof SelectPrimitive.Separator>)` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.Separator data-slot="select-separator" className={cn('bg-border pointer-events-none -mx-1 my-1 h-px', className)} {...p… (missing: jsdoc)
- `web/components/ui/select.tsx:138` — `SelectScrollUpButton({ className, ...props }: React.ComponentProps<typeof SelectPrimitive.ScrollUpButton>)` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.ScrollUpButton data-slot="select-scroll-up-button" className={cn( 'flex cursor-default items-center justify-center py-1… (missing: jsdoc)
- `web/components/ui/select.tsx:156` — `SelectScrollDownButton({ className, ...props }: React.ComponentProps<typeof SelectPrimitive.ScrollDownButton>)` — exported: **yes** — Implements logic starting with: { return ( <SelectPrimitive.ScrollDownButton data-slot="select-scroll-down-button" className={cn( 'flex cursor-default items-center justify-center… (missing: jsdoc)

### `web/components/ui/separator.tsx`
- `web/components/ui/separator.tsx:8` — `Separator({ className, orientation = 'horizontal', decorative = true, ...props }: React.ComponentProps<typeof SeparatorPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <SeparatorPrimitive.Root data-slot="separator" decorative={decorative} orientation={orientation} className={cn( 'bg-border shrink-0 data… (missing: jsdoc)

### `web/components/ui/sheet.tsx`
- `web/components/ui/sheet.tsx:8` — `Sheet({ ...props }: React.ComponentProps<typeof SheetPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return <SheetPrimitive.Root data-slot="sheet" {...props} /> } (missing: jsdoc)
- `web/components/ui/sheet.tsx:12` — `SheetTrigger({ ...props }: React.ComponentProps<typeof SheetPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return <SheetPrimitive.Trigger data-slot="sheet-trigger" {...props} /> } (missing: jsdoc)
- `web/components/ui/sheet.tsx:18` — `SheetClose({ ...props }: React.ComponentProps<typeof SheetPrimitive.Close>)` — exported: **yes** — Implements logic starting with: { return <SheetPrimitive.Close data-slot="sheet-close" {...props} /> } (missing: jsdoc)
- `web/components/ui/sheet.tsx:46` — `SheetContent({ className, children, side = 'right', ...props }: React.ComponentProps<typeof SheetPrimitive.Content> & { side?: 'top' | 'right' | 'bottom' | 'left' })` — exported: **yes** — Implements logic starting with: { return ( <SheetPortal> <SheetOverlay /> <SheetPrimitive.Content data-slot="sheet-content" className={cn( 'bg-background data-[state=open]:animate… (missing: jsdoc)
- `web/components/ui/sheet.tsx:79` — `SheetHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sheet-header" className={cn('flex flex-col gap-1.5 p-4', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sheet.tsx:89` — `SheetFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sheet-footer" className={cn('mt-auto flex flex-col gap-2 p-4', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sheet.tsx:99` — `SheetTitle({ className, ...props }: React.ComponentProps<typeof SheetPrimitive.Title>)` — exported: **yes** — Implements logic starting with: { return ( <SheetPrimitive.Title data-slot="sheet-title" className={cn('text-foreground font-semibold', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sheet.tsx:112` — `SheetDescription({ className, ...props }: React.ComponentProps<typeof SheetPrimitive.Description>)` — exported: **yes** — Implements logic starting with: { return ( <SheetPrimitive.Description data-slot="sheet-description" className={cn('text-muted-foreground text-sm', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/sidebar.tsx`
- `web/components/ui/sidebar.tsx:47` — `useSidebar()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `web/components/ui/sidebar.tsx:56` — `SidebarProvider({ defaultOpen = true, open: openProp, onOpenChange: setOpenProp, className, style, children, ...props }: React.ComponentProps<'div'> & { defaultOpen?: boolean open?: boolean onOpenChange?: (open: boolean) => void })` — exported: **yes** — Implements logic starting with: { const isMobile = useIsMobile() const [openMobile, setOpenMobile] = React.useState(false) // This is the internal state of the sidebar. // We use… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:154` — `Sidebar({ side = 'left', variant = 'sidebar', collapsible = 'offcanvas', className, children, ...props }: React.ComponentProps<'div'> & { side?: 'left' | 'right' variant?: 'sidebar' | 'floating' | 'inset' collapsible?: 'offcanvas' | 'icon' | 'none' })` — exported: **yes** — Implements logic starting with: { const { isMobile, state, openMobile, setOpenMobile } = useSidebar() if (collapsible === 'none') { return ( <div data-slot="sidebar" className={cn… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:256` — `SidebarTrigger({ className, onClick, ...props }: React.ComponentProps<typeof Button>)` — exported: **yes** — Implements logic starting with: { const { toggleSidebar } = useSidebar() return ( <Button data-sidebar="trigger" data-slot="sidebar-trigger" variant="ghost" size="icon" className=… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:282` — `SidebarRail({ className, ...props }: React.ComponentProps<'button'>)` — exported: **yes** — Implements logic starting with: { const { toggleSidebar } = useSidebar() return ( <button data-sidebar="rail" data-slot="sidebar-rail" aria-label="Toggle Sidebar" tabIndex={-1} on… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:307` — `SidebarInset({ className, ...props }: React.ComponentProps<'main'>)` — exported: **yes** — Implements logic starting with: { return ( <main data-slot="sidebar-inset" className={cn( 'bg-background relative flex w-full flex-1 flex-col', 'md:peer-data-[variant=inset]:m-2 m… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:321` — `SidebarInput({ className, ...props }: React.ComponentProps<typeof Input>)` — exported: **yes** — Implements logic starting with: { return ( <Input data-slot="sidebar-input" data-sidebar="input" className={cn('bg-background h-8 w-full shadow-none', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sidebar.tsx:335` — `SidebarHeader({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sidebar-header" data-sidebar="header" className={cn('flex flex-col gap-2 p-2', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sidebar.tsx:346` — `SidebarFooter({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sidebar-footer" data-sidebar="footer" className={cn('flex flex-col gap-2 p-2', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sidebar.tsx:357` — `SidebarSeparator({ className, ...props }: React.ComponentProps<typeof Separator>)` — exported: **yes** — Implements logic starting with: { return ( <Separator data-slot="sidebar-separator" data-sidebar="separator" className={cn('bg-sidebar-border mx-2 w-auto', className)} {...props}… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:371` — `SidebarContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sidebar-content" data-sidebar="content" className={cn( 'flex min-h-0 flex-1 flex-col gap-2 overflow-auto group-data-[col… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:385` — `SidebarGroup({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sidebar-group" data-sidebar="group" className={cn('relative flex w-full min-w-0 flex-col p-2', className)} {...props} />… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:396` — `SidebarGroupLabel({ className, asChild = false, ...props }: React.ComponentProps<'div'> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'div' return ( <Comp data-slot="sidebar-group-label" data-sidebar="group-label" className={cn( 'text-sidebar-foregr… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:417` — `SidebarGroupAction({ className, asChild = false, ...props }: React.ComponentProps<'button'> & { asChild?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'button' return ( <Comp data-slot="sidebar-group-action" data-sidebar="group-action" className={cn( 'text-sidebar-f… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:440` — `SidebarGroupContent({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sidebar-group-content" data-sidebar="group-content" className={cn('w-full text-sm', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sidebar.tsx:454` — `SidebarMenu({ className, ...props }: React.ComponentProps<'ul'>)` — exported: **yes** — Implements logic starting with: { return ( <ul data-slot="sidebar-menu" data-sidebar="menu" className={cn('flex w-full min-w-0 flex-col gap-1', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sidebar.tsx:465` — `SidebarMenuItem({ className, ...props }: React.ComponentProps<'li'>)` — exported: **yes** — Implements logic starting with: { return ( <li data-slot="sidebar-menu-item" data-sidebar="menu-item" className={cn('group/menu-item relative', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/sidebar.tsx:498` — `SidebarMenuButton({ asChild = false, isActive = false, variant = 'default', size = 'default', tooltip, className, ...props }: React.ComponentProps<'button'> & { asChild?: boolean isActive?: boolean tooltip?: string | React.ComponentProps<typeof TooltipContent> } & VariantProps<typeof sidebarMenuButtonVariants>)` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'button' const { isMobile, state } = useSidebar() const button = ( <Comp data-slot="sidebar-menu-button" data-sideb… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:548` — `SidebarMenuAction({ className, asChild = false, showOnHover = false, ...props }: React.ComponentProps<'button'> & { asChild?: boolean showOnHover?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'button' return ( <Comp data-slot="sidebar-menu-action" data-sidebar="menu-action" className={cn( 'text-sidebar-for… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:580` — `SidebarMenuBadge({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="sidebar-menu-badge" data-sidebar="menu-badge" className={cn( 'text-sidebar-foreground pointer-events-none absolute right… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:602` — `SidebarMenuSkeleton({ className, showIcon = false, ...props }: React.ComponentProps<'div'> & { showIcon?: boolean })` — exported: **yes** — Implements logic starting with: { const skeletonId = React.useId() const width = React.useMemo(() => { let hash = 0 for (const char of skeletonId) { hash = (hash * 31 + char.charC… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:644` — `SidebarMenuSub({ className, ...props }: React.ComponentProps<'ul'>)` — exported: **yes** — Implements logic starting with: { return ( <ul data-slot="sidebar-menu-sub" data-sidebar="menu-sub" className={cn( 'border-sidebar-border mx-3.5 flex min-w-0 translate-x-px flex-c… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:659` — `SidebarMenuSubItem({ className, ...props }: React.ComponentProps<'li'>)` — exported: **yes** — Implements logic starting with: { return ( <li data-slot="sidebar-menu-sub-item" data-sidebar="menu-sub-item" className={cn('group/menu-sub-item relative', className)} {...props}… (missing: jsdoc)
- `web/components/ui/sidebar.tsx:673` — `SidebarMenuSubButton({ asChild = false, size = 'md', isActive = false, className, ...props }: React.ComponentProps<'a'> & { asChild?: boolean size?: 'sm' | 'md' isActive?: boolean })` — exported: **yes** — Implements logic starting with: { const Comp = asChild ? Slot : 'a' return ( <Comp data-slot="sidebar-menu-sub-button" data-sidebar="menu-sub-button" data-size={size} data-active=… (missing: jsdoc)

### `web/components/ui/skeleton.tsx`
- `web/components/ui/skeleton.tsx:3` — `Skeleton({ className, ...props }: React.ComponentProps<'div'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="skeleton" className={cn('bg-accent animate-pulse rounded-md', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/slider.tsx`
- `web/components/ui/slider.tsx:8` — `Slider({ className, defaultValue, value, min = 0, max = 100, ...props }: React.ComponentProps<typeof SliderPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { const _values = React.useMemo( () => Array.isArray(value) ? value : Array.isArray(defaultValue) ? defaultValue : [min, max], [value, defaultValue… (missing: jsdoc)

### `web/components/ui/sonner.tsx`
- `web/components/ui/sonner.tsx:6` — `Toaster({ ...props }: ToasterProps)` — exported: **yes** — Implements logic starting with: { const { theme = 'system' } = useTheme() return ( <Sonner theme={theme as ToasterProps['theme']} className="toaster group" style={ { '--normal-bg'… (missing: jsdoc)

### `web/components/ui/spinner.tsx`
- `web/components/ui/spinner.tsx:5` — `Spinner({ className, ...props }: React.ComponentProps<'svg'>)` — exported: **yes** — Implements logic starting with: { return ( <Loader2Icon role="status" aria-label="Loading" className={cn('size-4 animate-spin', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/switch.tsx`
- `web/components/ui/switch.tsx:8` — `Switch({ className, ...props }: React.ComponentProps<typeof SwitchPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <SwitchPrimitive.Root data-slot="switch" className={cn( 'peer data-[state=checked]:bg-primary data-[state=unchecked]:bg-input focus-visi… (missing: jsdoc)

### `web/components/ui/table.tsx`
- `web/components/ui/table.tsx:7` — `Table({ className, ...props }: React.ComponentProps<'table'>)` — exported: **yes** — Implements logic starting with: { return ( <div data-slot="table-container" className="relative w-full overflow-x-auto" > <table data-slot="table" className={cn('w-full caption-bo… (missing: jsdoc)
- `web/components/ui/table.tsx:22` — `TableHeader({ className, ...props }: React.ComponentProps<'thead'>)` — exported: **yes** — Implements logic starting with: { return ( <thead data-slot="table-header" className={cn('[&_tr]:border-b', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/table.tsx:32` — `TableBody({ className, ...props }: React.ComponentProps<'tbody'>)` — exported: **yes** — Implements logic starting with: { return ( <tbody data-slot="table-body" className={cn('[&_tr:last-child]:border-0', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/table.tsx:42` — `TableFooter({ className, ...props }: React.ComponentProps<'tfoot'>)` — exported: **yes** — Implements logic starting with: { return ( <tfoot data-slot="table-footer" className={cn( 'bg-muted/50 border-t font-medium [&>tr]:last:border-b-0', className, )} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/table.tsx:55` — `TableRow({ className, ...props }: React.ComponentProps<'tr'>)` — exported: **yes** — Implements logic starting with: { return ( <tr data-slot="table-row" className={cn( 'hover:bg-muted/50 data-[state=selected]:bg-muted border-b transition-colors', className, )} {.… (missing: jsdoc)
- `web/components/ui/table.tsx:68` — `TableHead({ className, ...props }: React.ComponentProps<'th'>)` — exported: **yes** — Implements logic starting with: { return ( <th data-slot="table-head" className={cn( 'text-foreground h-10 px-2 text-left align-middle font-medium whitespace-nowrap [&:has([role=c… (missing: jsdoc)
- `web/components/ui/table.tsx:81` — `TableCell({ className, ...props }: React.ComponentProps<'td'>)` — exported: **yes** — Implements logic starting with: { return ( <td data-slot="table-cell" className={cn( 'p-2 align-middle whitespace-nowrap [&:has([role=checkbox])]:pr-0 [&>[role=checkbox]]:translat… (missing: jsdoc)
- `web/components/ui/table.tsx:94` — `TableCaption({ className, ...props }: React.ComponentProps<'caption'>)` — exported: **yes** — Implements logic starting with: { return ( <caption data-slot="table-caption" className={cn('text-muted-foreground mt-4 text-sm', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/tabs.tsx`
- `web/components/ui/tabs.tsx:8` — `Tabs({ className, ...props }: React.ComponentProps<typeof TabsPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <TabsPrimitive.Root data-slot="tabs" className={cn('flex flex-col gap-2', className)} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/tabs.tsx:21` — `TabsList({ className, ...props }: React.ComponentProps<typeof TabsPrimitive.List>)` — exported: **yes** — Implements logic starting with: { return ( <TabsPrimitive.List data-slot="tabs-list" className={cn( 'bg-muted text-muted-foreground inline-flex h-9 w-fit items-center justify-cent… (missing: jsdoc)
- `web/components/ui/tabs.tsx:37` — `TabsTrigger({ className, ...props }: React.ComponentProps<typeof TabsPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return ( <TabsPrimitive.Trigger data-slot="tabs-trigger" className={cn( "data-[state=active]:bg-background dark:data-[state=active]:text-foregrou… (missing: jsdoc)
- `web/components/ui/tabs.tsx:53` — `TabsContent({ className, ...props }: React.ComponentProps<typeof TabsPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <TabsPrimitive.Content data-slot="tabs-content" className={cn('flex-1 outline-none', className)} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/textarea.tsx`
- `web/components/ui/textarea.tsx:5` — `Textarea({ className, ...props }: React.ComponentProps<'textarea'>)` — exported: **yes** — Implements logic starting with: { return ( <textarea data-slot="textarea" className={cn( 'border-input placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ri… (missing: jsdoc)

### `web/components/ui/toaster.tsx`
- `web/components/ui/toaster.tsx:13` — `Toaster()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/components/ui/toggle-group.tsx`
- `web/components/ui/toggle-group.tsx:17` — `ToggleGroup({ className, variant, size, children, ...props }: React.ComponentProps<typeof ToggleGroupPrimitive.Root> & VariantProps<typeof toggleVariants>)` — exported: **yes** — Implements logic starting with: { return ( <ToggleGroupPrimitive.Root data-slot="toggle-group" data-variant={variant} data-size={size} className={cn( 'group/toggle-group flex w-fi… (missing: jsdoc)
- `web/components/ui/toggle-group.tsx:43` — `ToggleGroupItem({ className, children, variant, size, ...props }: React.ComponentProps<typeof ToggleGroupPrimitive.Item> & VariantProps<typeof toggleVariants>)` — exported: **yes** — Implements logic starting with: { const context = React.useContext(ToggleGroupContext) return ( <ToggleGroupPrimitive.Item data-slot="toggle-group-item" data-variant={context.vari… (missing: jsdoc)

### `web/components/ui/toggle.tsx`
- `web/components/ui/toggle.tsx:31` — `Toggle({ className, variant, size, ...props }: React.ComponentProps<typeof TogglePrimitive.Root> & VariantProps<typeof toggleVariants>)` — exported: **yes** — Implements logic starting with: { return ( <TogglePrimitive.Root data-slot="toggle" className={cn(toggleVariants({ variant, size, className }))} {...props} /> ) } (missing: jsdoc)

### `web/components/ui/tooltip.tsx`
- `web/components/ui/tooltip.tsx:8` — `TooltipProvider({ delayDuration = 0, ...props }: React.ComponentProps<typeof TooltipPrimitive.Provider>)` — exported: **yes** — Implements logic starting with: { return ( <TooltipPrimitive.Provider data-slot="tooltip-provider" delayDuration={delayDuration} {...props} /> ) } (missing: jsdoc)
- `web/components/ui/tooltip.tsx:21` — `Tooltip({ ...props }: React.ComponentProps<typeof TooltipPrimitive.Root>)` — exported: **yes** — Implements logic starting with: { return ( <TooltipProvider> <TooltipPrimitive.Root data-slot="tooltip" {...props} /> </TooltipProvider> ) } (missing: jsdoc)
- `web/components/ui/tooltip.tsx:31` — `TooltipTrigger({ ...props }: React.ComponentProps<typeof TooltipPrimitive.Trigger>)` — exported: **yes** — Implements logic starting with: { return <TooltipPrimitive.Trigger data-slot="tooltip-trigger" {...props} /> } (missing: jsdoc)
- `web/components/ui/tooltip.tsx:37` — `TooltipContent({ className, sideOffset = 0, children, ...props }: React.ComponentProps<typeof TooltipPrimitive.Content>)` — exported: **yes** — Implements logic starting with: { return ( <TooltipPrimitive.Portal> <TooltipPrimitive.Content data-slot="tooltip-content" sideOffset={sideOffset} className={cn( 'bg-foreground te… (missing: jsdoc)

### `web/components/ui/use-mobile.tsx`
- `web/components/ui/use-mobile.tsx:5` — `useIsMobile()` — exported: **yes** — Implements logic starting with: { const [isMobile, setIsMobile] = React.useState<boolean | undefined>(undefined) React.useEffect(() => { const mql = window.matchMedia(`(max-width:… (missing: jsdoc)

### `web/components/ui/use-toast.ts`
- `web/components/ui/use-toast.ts:65` — `reducer(state: State, action: Action)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/components/ui/use-toast.ts:131` — `toast({ ...props }: Toast)` — exported: **yes** — Implements logic starting with: { const id = genId() const update = (props: ToasterToast) => dispatch({ type: 'UPDATE_TOAST', toast: { ...props, id }, }) const dismiss = () => dis… (missing: jsdoc)
- `web/components/ui/use-toast.ts:160` — `useToast()` — exported: **yes** — Implements logic starting with: { const [state, setState] = React.useState<State>(memoryState) React.useEffect(() => { listeners.push(setState) return () => { const index = listen… (missing: jsdoc)

### `web/hooks/use-mobile.ts`
- `web/hooks/use-mobile.ts:5` — `useIsMobile()` — exported: **yes** — Implements logic starting with: { const [isMobile, setIsMobile] = React.useState<boolean | undefined>(undefined) React.useEffect(() => { const mql = window.matchMedia(`(max-width:… (missing: jsdoc)

### `web/hooks/use-toast.ts`
- `web/hooks/use-toast.ts:65` — `reducer(state: State, action: Action)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/hooks/use-toast.ts:131` — `toast({ ...props }: Toast)` — exported: **yes** — Implements logic starting with: { const id = genId() const update = (props: ToasterToast) => dispatch({ type: 'UPDATE_TOAST', toast: { ...props, id }, }) const dismiss = () => dis… (missing: jsdoc)
- `web/hooks/use-toast.ts:160` — `useToast()` — exported: **yes** — Implements logic starting with: { const [state, setState] = React.useState<State>(memoryState) React.useEffect(() => { listeners.push(setState) return () => { const index = listen… (missing: jsdoc)

### `web/lib/browser-slash-command-dispatch.ts`
- `web/lib/browser-slash-command-dispatch.ts:277` — `isAuthoritativeBuiltinSlashCommand(commandName: string)` — exported: **yes** — Implements logic starting with: { return BUILTIN_COMMAND_NAMES.has(commandName) } (missing: jsdoc)
- `web/lib/browser-slash-command-dispatch.ts:281` — `dispatchBrowserSlashCommand(input: string, options: BrowserSlashCommandDispatchOptions = {})` — exported: **yes** — Implements logic starting with: { const trimmed = input.trim() const parsed = parseSlashCommand(trimmed) if (trimmed === "/clear") { return { kind: "local", input: trimmed, comman… (missing: jsdoc)
- `web/lib/browser-slash-command-dispatch.ts:377` — `getBrowserSlashCommandTerminalNotice(outcome: BrowserSlashCommandDispatchResult)` — exported: **yes** — Implements logic starting with: { switch (outcome.kind) { case "surface": return { type: "system", message: `/${outcome.commandName} is reserved for browser-native handling and wa… (missing: jsdoc)

### `web/lib/command-surface-contract.ts`
- `web/lib/command-surface-contract.ts:375` — `createInitialDiagnosticsPhaseState()` — exported: **yes** — Implements logic starting with: { return { phase: "idle", data: null, error: null, lastLoadedAt: null } } (missing: jsdoc)
- `web/lib/command-surface-contract.ts:379` — `createInitialDoctorState()` — exported: **yes** — Implements logic starting with: { return { phase: "idle", data: null, error: null, lastLoadedAt: null, fixPending: false, lastFixResult: null, lastFixError: null } } (missing: jsdoc)
- `web/lib/command-surface-contract.ts:383` — `createInitialDiagnosticsState()` — exported: **yes** — Implements logic starting with: { return { forensics: createInitialDiagnosticsPhaseState<ForensicReport>(), doctor: createInitialDoctorState(), skillHealth: createInitialDiagnosti… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:405` — `createInitialKnowledgeCapturesState()` — exported: **yes** — Implements logic starting with: { return { knowledge: createInitialDiagnosticsPhaseState<KnowledgeData>(), captures: createInitialDiagnosticsPhaseState<CapturesData>(), resolveReq… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:417` — `createInitialSettingsState()` — exported: **yes** — Implements logic starting with: { return createInitialDiagnosticsPhaseState<SettingsData>() } (missing: jsdoc)
- `web/lib/command-surface-contract.ts:433` — `createInitialRemainingState()` — exported: **yes** — Implements logic starting with: { return { history: createInitialDiagnosticsPhaseState<HistoryData>(), inspect: createInitialDiagnosticsPhaseState<InspectData>(), hooks: createIni… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:593` — `createInitialCommandSurfaceRecoveryState()` — exported: **yes** — Implements logic starting with: { return { phase: "idle", pending: false, loaded: false, stale: false, diagnostics: null, error: null, lastLoadedAt: null, lastInvalidatedAt: null,… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:618` — `isCommandSurfaceThinkingLevel(value: string | null | undefined)` — exported: **yes** — Implements logic starting with: { return COMMAND_SURFACE_THINKING_LEVELS.includes((value ?? "") as CommandSurfaceThinkingLevel) } (missing: jsdoc)
- `web/lib/command-surface-contract.ts:622` — `createInitialCommandSurfaceState()` — exported: **yes** — Implements logic starting with: { return { open: false, activeSurface: null, source: null, section: null, args: "", pendingAction: null, selectedTarget: null, lastError: null, las… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:650` — `commandSurfaceSectionForRequest(request: CommandSurfaceOpenRequest)` — exported: **yes** — Implements logic starting with: { switch (request.surface) { case "model": return "model" case "thinking": return "thinking" case "settings": return request.onboardingLocked ? "au… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:777` — `buildCommandSurfaceTarget(request: CommandSurfaceOpenRequest)` — exported: **yes** — Implements logic starting with: { if (request.selectedTarget !== undefined) { return request.selectedTarget } const section = commandSurfaceSectionForRequest(request) if (!section… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:830` — `openCommandSurfaceState(current: WorkspaceCommandSurfaceState, request: CommandSurfaceOpenRequest)` — exported: **yes** — Implements logic starting with: { const section = commandSurfaceSectionForRequest(request) return { ...current, open: true, activeSurface: request.surface, source: request.source,… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:862` — `closeCommandSurfaceState(current: WorkspaceCommandSurfaceState)` — exported: **yes** — Implements logic starting with: { return { ...current, open: false, pendingAction: null, } } (missing: jsdoc)
- `web/lib/command-surface-contract.ts:870` — `setCommandSurfaceSection(current: WorkspaceCommandSurfaceState, section: CommandSurfaceSection, context: CommandSurfaceOpenContext = {})` — exported: **yes** — Implements logic starting with: { const request: CommandSurfaceOpenRequest = { surface: current.activeSurface ?? "settings", source: current.source ?? "surface", args: current.arg… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:930` — `selectCommandSurfaceStateTarget(current: WorkspaceCommandSurfaceState, target: CommandSurfaceTarget)` — exported: **yes** — Implements logic starting with: { const nextSection = target.kind === "settings" ? target.section : target.kind === "model" ? "model" : target.kind === "thinking" ? "thinking" : t… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:962` — `setCommandSurfacePending(current: WorkspaceCommandSurfaceState, action: CommandSurfacePendingAction, selectedTarget: CommandSurfaceTarget | null = current.selectedTarget)` — exported: **yes** — Implements logic starting with: { const nextResumeRequest = action === "switch_session" ? { pending: true, sessionPath: selectedTarget?.kind === "resume" ? selectedTarget.sessionP… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:1035` — `applyCommandSurfaceActionResult(current: WorkspaceCommandSurfaceState, result: CommandSurfaceActionResult)` — exported: **yes** — Implements logic starting with: { const nextSelectedTarget = result.selectedTarget === undefined ? current.selectedTarget : result.selectedTarget const resumeSessionPath = (nextSe… (missing: jsdoc)
- `web/lib/command-surface-contract.ts:1098` — `surfaceOutcomeToOpenRequest(outcome: Extract<BrowserSlashCommandDispatchResult, { kind: "surface" }>, context: CommandSurfaceOpenContext = {})` — exported: **yes** — Implements logic starting with: { return { surface: outcome.surface, source: "slash", args: outcome.args, ...context, } } (missing: jsdoc)

### `web/lib/dashboard-metrics.ts`
- `web/lib/dashboard-metrics.ts:23` — `deriveDashboardRtkMetric(auto: DashboardAutoMetrics | null | undefined, isConnecting: boolean, formatTokenCount: (tokens: number) => string)` — exported: **yes** — Implements logic starting with: { const savings = auto?.rtkSavings ?? null const enabled = auto?.rtkEnabled === true return { enabled, label: 'RTK Saved', value: isConnecting ? nu… (missing: jsdoc)

### `web/lib/dev-overrides.tsx`
- `web/lib/dev-overrides.tsx:78` — `DevOverridesProvider({ children }: { children: ReactNode })` — exported: **yes** — Implements logic starting with: { const [enabled, setEnabled] = useState(false) const [overrides, setOverrides] = useState<DevOverrideMap>(DEFAULT_OVERRIDES) const [isDevMode, set… (missing: jsdoc)
- `web/lib/dev-overrides.tsx:149` — `useDevOverrides()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/lib/git-summary-contract.ts`
- `web/lib/git-summary-contract.ts:50` — `isGitSummaryResponse(value: unknown)` — exported: **yes** — Implements logic starting with: { if (!value || typeof value !== "object") return false const response = value as Partial<GitSummaryResponse> if (response.kind !== "repo" && respo… (missing: jsdoc)

### `web/lib/gsd-workspace-store.tsx`
- `web/lib/gsd-workspace-store.tsx:454` — `isWorkspaceEvent(value: unknown)` — exported: **yes** — Implements logic starting with: { return value !== null && typeof value === "object" && typeof (value as Record<string, unknown>).type === "string" } (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1040` — `shortenPath(path: string | undefined, segmentCount = 3)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1050` — `getProjectDisplayName(path: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1056` — `formatDuration(ms: number)` — exported: **yes** — Implements logic starting with: { if (!ms || ms < 1000) return "0m" const totalMinutes = Math.floor(ms / 60_000) const hours = Math.floor(totalMinutes / 60) const minutes = totalM… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1065` — `formatTokens(tokens: number)` — exported: **yes** — Implements logic starting with: { if (!Number.isFinite(tokens) || tokens <= 0) return "0" if (tokens >= 1_000_000) return `${(tokens / 1_000_000).toFixed(1)}M` if (tokens >= 1_000… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1072` — `formatCost(cost: number)` — exported: **yes** — Implements logic starting with: { if (!Number.isFinite(cost) || cost <= 0) return "$0.00" return `$${cost.toFixed(2)}` } (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1077` — `getCurrentScopeLabel(workspace: WorkspaceIndex | null | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1085` — `getCurrentBranch(workspace: WorkspaceIndex | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!workspace?.active.milestoneId || !workspace.active.sliceId) { return null } const milestone = workspace.milestones.find((entry) => entry.id… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1095` — `getCurrentSlice(workspace: WorkspaceIndex | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!workspace?.active.milestoneId || !workspace.active.sliceId) return null const milestone = workspace.milestones.find((entry) => entry.id ===… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1101` — `getSessionLabelFromBridge(bridge: BridgeRuntimeSnapshot | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!bridge?.sessionState && !bridge?.activeSessionId) return null const sessionName = bridge.sessionState?.sessionName?.trim() if (sessionName)… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1109` — `getModelLabel(bridge: BridgeRuntimeSnapshot | null | undefined)` — exported: **yes** — Implements logic starting with: { const model = bridge?.sessionState?.model if (!model) return "model pending" return model.id || model.providerId || model.provider || "model pend… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1434` — `getOnboardingPresentation(state: Pick<WorkspaceStoreState, "bootStatus" | "boot" | "onboardingRequestState">)` — exported: **yes** — Implements logic starting with: { if (state.bootStatus === "loading" || !state.boot) { return { phase: "loading", label: "Loading setup state", detail: "Resolving the current proj… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1530` — `getVisibleWorkspaceError(state: Pick<WorkspaceStoreState, "boot" | "lastBridgeError" | "lastClientError">)` — exported: **yes** — Implements logic starting with: { const onboarding = state.boot?.onboarding if (onboarding?.bridgeAuthRefresh.phase === "failed" && onboarding.bridgeAuthRefresh.error) { return on… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1543` — `getStatusPresentation(state: Pick<WorkspaceStoreState, "bootStatus" | "connectionState" | "boot" | "onboardingRequestState">)` — exported: **yes** — Implements logic starting with: { if (state.bootStatus === "loading") { return { label: "Loading workspace", tone: "info" } } if (state.bootStatus === "error") { return { label: "… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1688` — `getLiveWorkspaceIndex(state: Pick<WorkspaceStoreState, "boot" | "live">)` — exported: **yes** — Implements logic starting with: { return state.live.workspace ?? state.boot?.workspace ?? null } (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1694` — `getLiveAutoDashboard(state: Pick<WorkspaceStoreState, "boot" | "live">)` — exported: **yes** — Implements logic starting with: { return state.live.auto ?? state.boot?.auto ?? null } (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1700` — `getLiveResumableSessions(state: Pick<WorkspaceStoreState, "boot" | "live">)` — exported: **yes** — Implements logic starting with: { return state.live.resumableSessions.length > 0 ? state.live.resumableSessions : state.boot?.resumableSessions ?? [] } (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1706` — `createWorkspaceRecoverySummary(state: Pick<WorkspaceStoreState, "boot" | "live">)` — exported: **yes** — Implements logic starting with: { const bridge = state.boot?.bridge ?? null const workspace = getLiveWorkspaceIndex(state) const auto = getLiveAutoDashboard(state) const validatio… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1839` — `buildProjectUrl(path: string, projectCwd?: string)` — exported: **yes** — Implements logic starting with: { if (!projectCwd) return path const url = new URL(path, "http://localhost") url.searchParams.set("project", projectCwd) return url.pathname + url.… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1846` — `class GSDWorkspaceStore` — exported: **yes** — Defines an API surface. (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:1847` — `GSDWorkspaceStore.constructor(private readonly projectCwd?: string)` — exported: **yes** — Implements logic starting with: {} (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:5234` — `GSDWorkspaceProvider({ children, store: externalStore }: { children: ReactNode; store?: GSDWorkspaceStore })` — exported: **yes** — Implements logic starting with: { const [internalStore] = useState(() => new GSDWorkspaceStore()) const store = externalStore ?? internalStore useEffect(() => { // Only start/disp… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:5257` — `useGSDWorkspaceState()` — exported: **yes** — Implements logic starting with: { const store = useWorkspaceStore() return useSyncExternalStore(store.subscribe, store.getSnapshot, store.getSnapshot) } (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:5262` — `useGSDWorkspaceActions()` — exported: **yes** — Implements logic starting with: { const store = useWorkspaceStore() return { sendCommand: store.sendCommand, submitInput: store.submitInput, clearTerminalLines: store.clearTermina… (missing: jsdoc)
- `web/lib/gsd-workspace-store.tsx:5392` — `buildPromptCommand(input: string, bridge: BridgeRuntimeSnapshot | null | undefined)` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/lib/image-utils.ts`
- `web/lib/image-utils.ts:49` — `validateImageFile(file: File)` — exported: **yes** — Logs diagnostic information. (missing: jsdoc)
- `web/lib/image-utils.ts:68` — `readFileAsBase64(file: File)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: jsdoc)

### `web/lib/project-store-manager.tsx`
- `web/lib/project-store-manager.tsx:30` — `ProjectStoreManager.getActiveStore()` — exported: **yes** — Implements logic starting with: { if (!this.activeProjectCwd) return null return this.stores.get(this.activeProjectCwd) ?? null } (missing: jsdoc)
- `web/lib/project-store-manager.tsx:35` — `ProjectStoreManager.getActiveProjectCwd()` — exported: **yes** — Implements logic starting with: { return this.activeProjectCwd } (missing: jsdoc)
- `web/lib/project-store-manager.tsx:119` — `ProjectStoreManagerProvider({ children }: { children: ReactNode })` — exported: **yes** — Implements logic starting with: { const [manager] = useState(() => new ProjectStoreManager()) useEffect(() => { return () => manager.disposeAll() }, [manager]) return ( <ProjectSt… (missing: jsdoc)
- `web/lib/project-store-manager.tsx:133` — `useProjectStoreManager()` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)

### `web/lib/project-url.ts`
- `web/lib/project-url.ts:1` — `buildProjectPath(path: string, projectCwd?: string)` — exported: **yes** — Implements logic starting with: { if (!projectCwd) return path const url = new URL(path, "http://localhost") url.searchParams.set("project", projectCwd) return url.pathname + url.… (missing: jsdoc)
- `web/lib/project-url.ts:8` — `buildProjectAbsoluteUrl(path: string, origin: string, projectCwd?: string)` — exported: **yes** — Implements logic starting with: { return new URL(buildProjectPath(path, projectCwd), origin) } (missing: jsdoc)

### `web/lib/proxy-auth.ts`
- `web/lib/proxy-auth.ts:17` — `evaluateWebProxyAuth(request: WebProxyAuthRequest, env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Builds an in-memory lookup structure. (missing: jsdoc)

### `web/lib/pty-chat-parser.ts`
- `web/lib/pty-chat-parser.ts:316` — `PtyChatParser.constructor(source = "default")` — exported: **yes** — Implements logic starting with: { this._source = source } (missing: jsdoc)

### `web/lib/pty-manager.ts`
- `web/lib/pty-manager.ts:130` — `isAllowedTerminalCommand(command?: string)` — exported: **yes** — Implements logic starting with: { if (!command) return true; return ALLOWED_TERMINAL_COMMANDS.has(command); } (missing: jsdoc)
- `web/lib/pty-manager.ts:249` — `getOrCreateSession(sessionId: string, projectCwd?: string, command?: string, commandArgs: string[] = [])` — exported: **yes** — Validates inputs and throws on failure. (missing: jsdoc)
- `web/lib/pty-manager.ts:362` — `writeToSession(sessionId: string, data: string)` — exported: **yes** — Implements logic starting with: { const session = getSessions().get(sessionId); if (!session?.alive) return false; session.pty.write(data); return true; } (missing: jsdoc)
- `web/lib/pty-manager.ts:369` — `resizeSession(sessionId: string, cols: number, rows: number)` — exported: **yes** — Implements logic starting with: { const session = getSessions().get(sessionId); if (!session?.alive) return false; try { session.pty.resize(cols, rows); return true; } catch { ret… (missing: jsdoc)
- `web/lib/pty-manager.ts:384` — `destroySession(sessionId: string)` — exported: **yes** — Implements logic starting with: { const map = getSessions(); const session = map.get(sessionId); if (!session) return false; session.alive = false; try { session.pty.kill(); } cat… (missing: jsdoc)
- `web/lib/pty-manager.ts:399` — `addListener(sessionId: string, listener: (data: string) => void)` — exported: **yes** — Implements logic starting with: { const session = getSessions().get(sessionId); if (!session) return null; const snapshot = session.buffer.slice(); session.listeners.add(listener)… (missing: jsdoc)
- `web/lib/pty-manager.ts:423` — `isSessionAlive(sessionId: string)` — exported: **yes** — Implements logic starting with: { const session = getSessions().get(sessionId); return session?.alive ?? false; } (missing: jsdoc)
- `web/lib/pty-manager.ts:434` — `listSessions()` — exported: **yes** — Transforms a collection into a derived result. (missing: jsdoc)

### `web/lib/session-browser-contract.ts`
- `web/lib/session-browser-contract.ts:88` — `isSessionBrowserSortMode(value: string | null | undefined)` — exported: **yes** — Implements logic starting with: { return SESSION_BROWSER_SORT_MODES.includes((value ?? "") as SessionBrowserSortMode) } (missing: jsdoc)
- `web/lib/session-browser-contract.ts:92` — `isSessionBrowserNameFilter(value: string | null | undefined)` — exported: **yes** — Implements logic starting with: { return SESSION_BROWSER_NAME_FILTERS.includes((value ?? "") as SessionBrowserNameFilter) } (missing: jsdoc)
- `web/lib/session-browser-contract.ts:96` — `isSessionManageAction(value: string | null | undefined)` — exported: **yes** — Implements logic starting with: { return SESSION_MANAGE_ACTIONS.includes((value ?? "") as SessionManageAction) } (missing: jsdoc)
- `web/lib/session-browser-contract.ts:100` — `normalizeSessionBrowserQuery(query?: SessionBrowserQuery)` — exported: **yes** — Implements logic starting with: { return { query: query?.query?.trim() ?? "", sortMode: query?.sortMode ?? "threaded", nameFilter: query?.nameFilter ?? "all", } } (missing: jsdoc)

### `web/lib/shutdown-gate.ts`
- `web/lib/shutdown-gate.ts:60` — `drainStreams()` — exported: **yes** — Implements logic starting with: { for (const unsubscribe of gate.activeStreams) { try { unsubscribe(); } catch { // best-effort; never let a bad unsubscriber prevent shutdown } }… (missing: jsdoc)

### `web/lib/use-user-mode.ts`
- `web/lib/use-user-mode.ts:59` — `useUserMode()` — exported: **yes** — Implements logic starting with: { const mode = useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) const set = useCallback((m: UserMode) => setUserMode(m), []) return… (missing: jsdoc)

### `web/lib/utils.ts`
- `web/lib/utils.ts:4` — `cn(...inputs: ClassValue[])` — exported: **yes** — Implements logic starting with: { return twMerge(clsx(inputs)) } (missing: jsdoc)

### `web/lib/workflow-action-execution.ts`
- `web/lib/workflow-action-execution.ts:6` — `navigateToGSDView(view: GSDViewName)` — exported: **yes** — Implements logic starting with: { if (typeof window === "undefined") return window.dispatchEvent(new CustomEvent("gsd:navigate-view", { detail: { view } })) } (missing: jsdoc)
- `web/lib/workflow-action-execution.ts:32` — `derivePendingWorkflowCommandLabel({ commandInFlight, terminalLines, }: { commandInFlight: string | null terminalLines: WorkspaceTerminalLine[] })` — exported: **yes** — Implements logic starting with: { if (!commandInFlight) return null for (let index = terminalLines.length - 1; index >= 0; index -= 1) { const line = terminalLines[index] if (line… (missing: jsdoc)

### `web/lib/workflow-actions.ts`
- `web/lib/workflow-actions.ts:33` — `deriveWorkflowAction(input: WorkflowActionInput)` — exported: **yes** — Implements logic starting with: { const { phase, autoActive, autoPaused, onboardingLocked, commandInFlight, bootStatus, hasMilestones, projectDetectionKind } = input // When the p… (missing: jsdoc)

### `web/lib/workspace-status.ts`
- `web/lib/workspace-status.ts:9` — `getMilestoneStatus(milestone: WorkspaceMilestoneTarget, active: { milestoneId?: string })` — exported: **yes** — Implements logic starting with: { // Prefer authoritative milestone status from GSD state registry (#2807) if (milestone.status) { switch (milestone.status) { case "complete": ret… (missing: jsdoc)
- `web/lib/workspace-status.ts:37` — `getSliceStatus(milestoneId: string, slice: WorkspaceSliceTarget, active: { milestoneId?: string; sliceId?: string })` — exported: **yes** — Implements logic starting with: { if (slice.done) return "done" if (active.milestoneId === milestoneId && active.sliceId === slice.id) return "in-progress" return "pending" } (missing: jsdoc)
- `web/lib/workspace-status.ts:47` — `getTaskStatus(milestoneId: string, sliceId: string, task: WorkspaceTaskTarget, active: { milestoneId?: string; sliceId?: string; taskId?: string })` — exported: **yes** — Implements logic starting with: { if (task.done) return "done" if (active.milestoneId === milestoneId && active.sliceId === sliceId && active.taskId === task.id) return "in-progre… (missing: jsdoc)

### `web/lib/xterm-theme.ts`
- `web/lib/xterm-theme.ts:104` — `getXtermTheme(isDark: boolean, palette: XtermPaletteMode = "classic")` — exported: **yes** — Implements logic starting with: { if (palette === "vivid") { return isDark ? XTERM_VIVID_DARK_THEME : XTERM_VIVID_LIGHT_THEME; } return isDark ? XTERM_CLASSIC_DARK_THEME : XTERM_C… (missing: jsdoc)
- `web/lib/xterm-theme.ts:111` — `getXtermOptions(isDark: boolean, fontSize?: number, palette: XtermPaletteMode = "classic")` — exported: **yes** — Implements logic starting with: { return { cursorBlink: true, cursorStyle: "bar" as const, fontSize: fontSize ?? 13, fontFamily: "'SF Mono', 'Cascadia Code', 'Fira Code', Menlo, M… (missing: jsdoc)


## Partial gaps
Entries with some JSDoc but missing description, @param, or @returns.

### `extensions/google-search/index.ts`
- `extensions/google-search/index.ts:75` — `searchWithOAuth(query: string, accessToken: string, projectId: string, signal?: AbortSignal)` — exported: **no** — Validates inputs and throws on failure. (missing: @param query, @param accessToken, @param projectId, @param signal, @returns)

### `packages/daemon/src/channel-manager.ts`
- `packages/daemon/src/channel-manager.ts:44` — `sanitizeChannelName(projectDir: string)` — exported: **yes** — Implements logic starting with: { // Extract basename — handle both forward and back slashes const parts = projectDir.replace(/\\/g, '/').split('/'); let basename = parts[parts.le… (missing: @param projectDir, @returns)
- `packages/daemon/src/channel-manager.ts:114` — `ChannelManager.resolveCategory()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/daemon/src/channel-manager.ts:141` — `ChannelManager.createProjectChannel(projectDir: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param projectDir, @returns)
- `packages/daemon/src/channel-manager.ts:165` — `ChannelManager.archiveChannel(channelId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param channelId)

### `packages/daemon/src/commands.ts`
- `packages/daemon/src/commands.ts:24` — `buildCommands()` — exported: **yes** — Implements logic starting with: { return [ new SlashCommandBuilder() .setName('gsd-status') .setDescription('Show status of all active GSD sessions') .toJSON(), new SlashCommandBu… (missing: @returns)
- `packages/daemon/src/commands.ts:64` — `registerGuildCommands(rest: REST, clientId: string, guildId: string, commands: RESTPostAPIChatInputApplicationCommandsJSONBody[], logger?: Logger)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param rest, @param clientId, @param guildId, @param commands, @param logger, @returns)
- `packages/daemon/src/commands.ts:97` — `formatSessionStatus(sessions: ManagedSession[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param sessions, @returns)

### `packages/daemon/src/config.ts`
- `packages/daemon/src/config.ts:10` — `expandTilde(p: string)` — exported: **no** — Implements logic starting with: { if (p.startsWith('~/') || p === '~') { return resolve(homedir(), p.slice(2) || '.'); } return p; } (missing: @param p, @returns)
- `packages/daemon/src/config.ts:18` — `defaults()` — exported: **no** — Implements logic starting with: { return { discord: undefined, projects: { scan_roots: [] }, log: { file: resolve(homedir(), '.gsd', 'daemon.log'), level: 'info', max_size_mb: 50,… (missing: @returns)
- `packages/daemon/src/config.ts:34` — `resolveConfigPath(cliPath?: string)` — exported: **yes** — Implements logic starting with: { if (cliPath) return expandTilde(cliPath); const envPath = process.env['GSD_DAEMON_CONFIG']; if (envPath) return expandTilde(envPath); return reso… (missing: @param cliPath, @returns)
- `packages/daemon/src/config.ts:45` — `validateConfig(raw: unknown)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param raw, @returns)
- `packages/daemon/src/config.ts:121` — `loadConfig(configPath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param configPath, @returns)

### `packages/daemon/src/daemon.ts`
- `packages/daemon/src/daemon.ts:13` — `class Daemon` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/daemon/src/daemon.ts:128` — `Daemon.scanProjects()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/daemon/src/daemon.ts:133` — `Daemon.getSessionManager()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `packages/daemon/src/daemon.ts:141` — `Daemon.getEventBridge()` — exported: **yes** — Implements logic starting with: { return this.eventBridge; } (missing: @returns)
- `packages/daemon/src/daemon.ts:146` — `Daemon.getOrchestrator()` — exported: **yes** — Implements logic starting with: { return this.orchestrator; } (missing: @returns)

### `packages/daemon/src/discord-bot.ts`
- `packages/daemon/src/discord-bot.ts:35` — `isAuthorized(userId: string, ownerId: string)` — exported: **yes** — Implements logic starting with: { if (!ownerId || !userId) return false; return userId === ownerId; } (missing: @param userId, @param ownerId, @returns)
- `packages/daemon/src/discord-bot.ts:44` — `validateDiscordConfig(config: DaemonConfig['discord'])` — exported: **yes** — Validates inputs and throws on failure. (missing: @param config)
- `packages/daemon/src/discord-bot.ts:236` — `DiscordBot.getChannelManager()` — exported: **yes** — Implements logic starting with: { if (this.channelManager) return this.channelManager; if (!this.client?.isReady()) return null; const guild = this.client.guilds.cache.get(this.co… (missing: @returns)
- `packages/daemon/src/discord-bot.ts:254` — `DiscordBot.getClient()` — exported: **yes** — Implements logic starting with: { return this.client; } (missing: @returns)
- `packages/daemon/src/discord-bot.ts:262` — `DiscordBot.setEventBridge(bridge: EventBridge)` — exported: **yes** — Implements logic starting with: { this.eventBridge = bridge; } (missing: @param bridge)

### `packages/daemon/src/event-bridge.ts`
- `packages/daemon/src/event-bridge.ts:167` — `EventBridge.getVerbosityManager()` — exported: **yes** — Implements logic starting with: { return this.verbosity; } (missing: @returns)

### `packages/daemon/src/event-formatter.ts`
- `packages/daemon/src/event-formatter.ts:34` — `truncate(s: string, maxLen: number)` — exported: **no** — Implements logic starting with: { if (s.length <= maxLen) return s; return s.slice(0, maxLen - 1) + '…'; } (missing: @param s, @param maxLen, @returns)
- `packages/daemon/src/event-formatter.ts:40` — `str(value: unknown, fallback = '')` — exported: **no** — Implements logic starting with: { if (typeof value === 'string') return value; if (value == null) return fallback; return String(value); } (missing: @param value, @param fallback, @returns)
- `packages/daemon/src/event-formatter.ts:47` — `num(value: unknown, fallback = 0)` — exported: **no** — Implements logic starting with: { if (typeof value === 'number' && !Number.isNaN(value)) return value; return fallback; } (missing: @param value, @param fallback, @returns)
- `packages/daemon/src/event-formatter.ts:53` — `formatCost(cost: number)` — exported: **no** — Implements logic starting with: { if (cost < 0.01) return `$${cost.toFixed(4)}`; return `$${cost.toFixed(2)}`; } (missing: @param cost, @returns)
- `packages/daemon/src/event-formatter.ts:164` — `formatBlocker(blocker: PendingBlocker, ownerId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param blocker, @param ownerId, @returns)
- `packages/daemon/src/event-formatter.ts:380` — `formatEvent(event: SdkAgentEvent, ownerId?: string)` — exported: **yes** — Implements logic starting with: { const type = str(event.type); switch (type) { case 'tool_execution_start': return formatToolStart(event); case 'tool_execution_end': return forma… (missing: @param event, @param ownerId, @returns)

### `packages/daemon/src/launchd.ts`
- `packages/daemon/src/launchd.ts:43` — `escapeXml(str: string)` — exported: **yes** — Implements logic starting with: { return str .replace(/&/g, '&amp;') .replace(/</g, '&lt;') .replace(/>/g, '&gt;') .replace(/"/g, '&quot;') .replace(/'/g, '&apos;'); } (missing: @param str, @returns)
- `packages/daemon/src/launchd.ts:53` — `getPlistPath()` — exported: **yes** — Implements logic starting with: { return resolve(homedir(), 'Library', 'LaunchAgents', PLIST_FILENAME); } (missing: @returns)
- `packages/daemon/src/launchd.ts:62` — `buildEnvPath(nodePath: string)` — exported: **no** — Implements logic starting with: { const nodeBinDir = dirname(nodePath); // Keep system essentials and prepend the node binary's directory return `${nodeBinDir}:/usr/local/bin:/usr… (missing: @param nodePath, @returns)
- `packages/daemon/src/launchd.ts:71` — `generatePlist(opts: PlistOptions)` — exported: **yes** — Implements logic starting with: { const home = homedir(); const workDir = opts.workingDirectory ?? home; const stdoutPath = opts.stdoutPath ?? resolve(home, '.gsd', 'daemon-stdout… (missing: @param opts, @returns)
- `packages/daemon/src/launchd.ts:133` — `defaultRunCommand(cmd: string)` — exported: **no** — Executes a subprocess command. (missing: @param cmd, @returns)
- `packages/daemon/src/launchd.ts:141` — `install(opts: PlistOptions, runCommand: RunCommandFn = defaultRunCommand)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param opts, @param runCommand)
- `packages/daemon/src/launchd.ts:176` — `uninstall(runCommand: RunCommandFn = defaultRunCommand)` — exported: **yes** — Implements logic starting with: { const plistPath = getPlistPath(); if (existsSync(plistPath)) { try { runCommand(`launchctl unload ${plistPath}`); } catch { // already unloaded —… (missing: @param runCommand)
- `packages/daemon/src/launchd.ts:198` — `status(runCommand: RunCommandFn = defaultRunCommand)` — exported: **yes** — Implements logic starting with: { try { const output = runCommand(`launchctl list ${LABEL}`); // --- Try tabular format first --- const lines = output.trim().split('\n'); for (con… (missing: @param runCommand, @returns)

### `packages/daemon/src/logger.ts`
- `packages/daemon/src/logger.ts:23` — `class Logger` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/daemon/src/message-batcher.test.ts`
- `packages/daemon/src/message-batcher.test.ts:12` — `fakeEvent(content: string, hasEmbed = false)` — exported: **no** — Implements logic starting with: { const fe: FormattedEvent = { content }; if (hasEmbed) { // Minimal mock embed — just needs to be truthy and pass through fe.embed = { data: { tit… (missing: @param content, @param hasEmbed, @returns)
- `packages/daemon/src/message-batcher.test.ts:22` — `createSend()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/daemon/src/message-batcher.test.ts:31` — `createLogger()` — exported: **no** — Implements logic starting with: { const errors: string[] = []; const warns: string[] = []; const debugs: string[] = []; const logger: BatcherLogger = { error(msg: string) { errors… (missing: @returns)

### `packages/daemon/src/message-batcher.ts`
- `packages/daemon/src/message-batcher.ts:115` — `MessageBatcher.enqueue(formatted: FormattedEvent)` — exported: **yes** — Implements logic starting with: { if (this.destroyed) return; this.buffer.push(formatted); if (this.buffer.length >= this.maxBatchSize) { void this.flush(); } } (missing: @param formatted)
- `packages/daemon/src/message-batcher.ts:127` — `MessageBatcher.enqueueImmediate(formatted: FormattedEvent)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param formatted)
- `packages/daemon/src/message-batcher.ts:136` — `MessageBatcher.get pending()` — exported: **yes** — Implements logic starting with: { return this.buffer.length; } (missing: @returns)

### `packages/daemon/src/orchestrator.test.ts`
- `packages/daemon/src/orchestrator.test.ts:69` — `MockAnthropicClient.defaultHandler()` — exported: **no** — Implements logic starting with: { return { stop_reason: 'end_turn', content: [{ type: 'text', text: 'Mock LLM response' }], }; } (missing: @returns)
- `packages/daemon/src/orchestrator.test.ts:77` — `MockAnthropicClient.toolThenTextHandler(toolName: string, toolInput: unknown, finalText: string)` — exported: **no** — Implements logic starting with: { let callCount = 0; return () => { callCount++; if (callCount === 1) { return { stop_reason: 'tool_use', content: [ { type: 'tool_use', id: `toolu… (missing: @param toolName, @param toolInput, @param finalText, @returns)
- `packages/daemon/src/orchestrator.test.ts:102` — `MockAnthropicClient.errorHandler(message: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param message, @returns)

### `packages/daemon/src/orchestrator.ts`
- `packages/daemon/src/orchestrator.ts:198` — `Orchestrator.handleMessage(message: DiscordMessageLike)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message)
- `packages/daemon/src/orchestrator.ts:440` — `Orchestrator.getHistory()` — exported: **yes** — Implements logic starting with: { return [...this.history]; } (missing: @returns)

### `packages/daemon/src/project-scanner.test.ts`
- `packages/daemon/src/project-scanner.test.ts:28` — `createProject(root: string, name: string, markers: string[])` — exported: **no** — Performs filesystem I/O. (missing: @param root, @param name, @param markers, @returns)

### `packages/daemon/src/project-scanner.ts`
- `packages/daemon/src/project-scanner.ts:38` — `scanForProjects(scanRoots: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param scanRoots, @returns)

### `packages/daemon/src/session-manager.test.ts`
- `packages/daemon/src/session-manager.test.ts:83` — `MockRpcClient.emitEvent(event: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { for (const listener of this.eventListeners) { listener(event); } } (missing: @param event)

### `packages/daemon/src/session-manager.ts`
- `packages/daemon/src/session-manager.ts:76` — `SessionManager.startSession(options: StartSessionOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param options, @returns)
- `packages/daemon/src/session-manager.ts:169` — `SessionManager.getSession(sessionId: string)` — exported: **yes** — Implements logic starting with: { for (const session of this.sessions.values()) { if (session.sessionId === sessionId) return session; } return undefined; } (missing: @param sessionId, @returns)
- `packages/daemon/src/session-manager.ts:179` — `SessionManager.getSessionByDir(projectDir: string)` — exported: **yes** — Implements logic starting with: { return this.sessions.get(resolve(projectDir)); } (missing: @param projectDir, @returns)
- `packages/daemon/src/session-manager.ts:186` — `SessionManager.getAllSessions()` — exported: **yes** — Implements logic starting with: { return Array.from(this.sessions.values()); } (missing: @returns)
- `packages/daemon/src/session-manager.ts:193` — `SessionManager.resolveBlocker(sessionId: string, response: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sessionId, @param response)
- `packages/daemon/src/session-manager.ts:216` — `SessionManager.cancelSession(sessionId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sessionId)
- `packages/daemon/src/session-manager.ts:237` — `SessionManager.getResult(sessionId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sessionId, @returns)
- `packages/daemon/src/session-manager.ts:283` — `SessionManager.resolveCLIPath()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)

### `packages/daemon/src/verbosity.ts`
- `packages/daemon/src/verbosity.ts:54` — `VerbosityManager.getLevel(channelId: string)` — exported: **yes** — Implements logic starting with: { return this.levels.get(channelId) ?? 'default'; } (missing: @param channelId, @returns)
- `packages/daemon/src/verbosity.ts:59` — `VerbosityManager.setLevel(channelId: string, level: VerbosityLevel)` — exported: **yes** — Implements logic starting with: { this.levels.set(channelId, level); } (missing: @param channelId, @param level)
- `packages/daemon/src/verbosity.ts:67` — `VerbosityManager.shouldShow(channelId: string, eventType: string)` — exported: **yes** — Implements logic starting with: { const level = this.getLevel(channelId); return shouldShowAtLevel(level, eventType); } (missing: @param channelId, @param eventType, @returns)
- `packages/daemon/src/verbosity.ts:80` — `shouldShowAtLevel(level: VerbosityLevel, eventType: string)` — exported: **yes** — Implements logic starting with: { // Always-shown events pass through regardless of level if (ALWAYS_SHOWN.has(eventType)) return true; switch (level) { case 'quiet': // Quiet onl… (missing: @param level, @param eventType, @returns)

### `packages/mcp-server/src/alias-telemetry.ts`
- `packages/mcp-server/src/alias-telemetry.ts:18` — `logAliasUsage(alias: string, canonical: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param alias, @param canonical)

### `packages/mcp-server/src/env-writer.ts`
- `packages/mcp-server/src/env-writer.ts:19` — `checkExistingEnvKeys(keys: string[], envFilePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param keys, @param envFilePath, @returns)
- `packages/mcp-server/src/env-writer.ts:46` — `detectDestination(basePath: string)` — exported: **yes** — Implements logic starting with: { if (existsSync(resolve(basePath, "vercel.json"))) { return "vercel"; } const convexPath = resolve(basePath, "convex"); try { if (existsSync(conve… (missing: @param basePath, @returns)
- `packages/mcp-server/src/env-writer.ts:69` — `writeEnvKey(filePath: string, key: string, value: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param filePath, @param key, @param value)
- `packages/mcp-server/src/env-writer.ts:221` — `applySecrets(provided: Array<{ key: string; value: string }>, destination: "dotenv" | "vercel" | "convex", opts: { envFilePath: string; environment?: string; execFn?: (cmd: string, args: string[], opts?: ExecOptions) => Promise<{ code: number; stderr: string }>; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param provided, @param destination, @param opts, @returns)

### `packages/mcp-server/src/mcp-server.test.ts`
- `packages/mcp-server/src/mcp-server.test.ts:93` — `MockRpcClient.emitEvent(event: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { for (const listener of this.eventListeners) { listener(event); } } (missing: @param event)
- `packages/mcp-server/src/mcp-server.test.ts:197` — `TestableSessionManager._putSession(key: string, session: ManagedSession)` — exported: **no** — Implements logic starting with: { // Access the private sessions map via any cast (this as any).sessions.set(key, session); } (missing: @param key, @param session)
- `packages/mcp-server/src/mcp-server.test.ts:203` — `TestableSessionManager._handleEvent(session: ManagedSession, event: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { (this as any).handleEvent(session, event); } (missing: @param session, @param event)

### `packages/mcp-server/src/readers/graph.ts`
- `packages/mcp-server/src/readers/graph.ts:125` — `parseStateFile(gsdRoot: string, nodes: GraphNode[], _edges: GraphEdge[])` — exported: **no** — Performs filesystem I/O. (missing: @param gsdRoot, @param nodes, @param _edges)
- `packages/mcp-server/src/readers/graph.ts:170` — `parseKnowledgeFile(gsdRoot: string, nodes: GraphNode[], _edges: GraphEdge[])` — exported: **no** — Performs filesystem I/O. (missing: @param gsdRoot, @param nodes, @param _edges)
- `packages/mcp-server/src/readers/graph.ts:248` — `parseMilestoneFiles(gsdRoot: string, nodes: GraphNode[], edges: GraphEdge[])` — exported: **no** — Implements logic starting with: { const milestoneIds = findMilestoneIds(gsdRoot); for (const milestoneId of milestoneIds) { try { parseSingleMilestone(gsdRoot, milestoneId, nodes,… (missing: @param gsdRoot, @param nodes, @param edges)
- `packages/mcp-server/src/readers/graph.ts:408` — `parseLearningsFiles(gsdRoot: string, nodes: GraphNode[], edges: GraphEdge[])` — exported: **no** — Implements logic starting with: { const milestoneIds = findMilestoneIds(gsdRoot); for (const milestoneId of milestoneIds) { try { parseSingleLearningsFile(gsdRoot, milestoneId, no… (missing: @param gsdRoot, @param nodes, @param edges)
- `packages/mcp-server/src/readers/graph.ts:553` — `buildGraph(projectDir: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param projectDir, @returns)
- `packages/mcp-server/src/readers/graph.ts:606` — `writeGraph(gsdRoot: string, graph: KnowledgeGraph)` — exported: **yes** — Performs filesystem I/O. (missing: @param gsdRoot, @param graph)
- `packages/mcp-server/src/readers/graph.ts:625` — `writeSnapshot(gsdRoot: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param gsdRoot)
- `packages/mcp-server/src/readers/graph.ts:652` — `graphStatus(projectDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectDir, @returns)
- `packages/mcp-server/src/readers/graph.ts:693` — `applyBudget(graph: KnowledgeGraph, seedIds: Set<string>, budget: number)` — exported: **no** — Transforms a collection into a derived result. (missing: @param graph, @param seedIds, @param budget, @returns)
- `packages/mcp-server/src/readers/graph.ts:751` — `graphQuery(projectDir: string, term: string, budget = 4000)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectDir, @param term, @param budget, @returns)
- `packages/mcp-server/src/readers/graph.ts:807` — `graphDiff(projectDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectDir, @returns)

### `packages/mcp-server/src/readers/paths.ts`
- `packages/mcp-server/src/readers/paths.ts:62` — `readWithMtimeCache(cache: Map<string, MtimeEntry<V>>, cacheKey: string, dir: string, compute: () => V, cloneForStore: (value: V) => V = (value) => value, cloneForReturn: (value: V) => V = (value) => value)` — exported: **no** — Implements logic starting with: { let mtimeMs: number; try { mtimeMs = statSync(dir).mtimeMs; } catch { cache.delete(cacheKey); return compute(); } const hit = cache.get(cacheKey)… (missing: @param cache, @param cacheKey, @param dir, @param compute, @param cloneForStore, @param cloneForReturn, @returns)
- `packages/mcp-server/src/readers/paths.ts:122` — `resolveGsdRoot(projectDir: string)` — exported: **yes** — Implements logic starting with: { const resolved = resolve(projectDir); const cached = cachedGsdRoot(resolved); if (cached) return cached; // Fast path: .gsd/ in the given directo… (missing: @param projectDir, @returns)
- `packages/mcp-server/src/readers/paths.ts:167` — `resolveRootFile(gsdRoot: string, name: string)` — exported: **yes** — Implements logic starting with: { return join(gsdRoot, name); } (missing: @param gsdRoot, @param name, @returns)
- `packages/mcp-server/src/readers/paths.ts:172` — `milestonesDir(gsdRoot: string)` — exported: **yes** — Implements logic starting with: { return join(gsdRoot, 'milestones'); } (missing: @param gsdRoot, @returns)
- `packages/mcp-server/src/readers/paths.ts:180` — `findMilestoneIds(gsdRoot: string)` — exported: **yes** — Implements logic starting with: { const dir = milestonesDir(gsdRoot); if (!existsSync(dir)) return []; return readWithMtimeCache(milestoneIdsCache, dir, dir, () => { const entries… (missing: @param gsdRoot, @returns)
- `packages/mcp-server/src/readers/paths.ts:200` — `resolveMilestoneDir(gsdRoot: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const dir = milestonesDir(gsdRoot); if (!existsSync(dir)) return null; return readWithMtimeCache(milestoneDirCache, `${dir} ${milestoneId}`, dir,… (missing: @param gsdRoot, @param milestoneId, @returns)
- `packages/mcp-server/src/readers/paths.ts:225` — `resolveMilestoneFile(gsdRoot: string, milestoneId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const mDir = resolveMilestoneDir(gsdRoot, milestoneId); if (!mDir) return null; const dirName = basename(mDir); // Try: M001-ROADMAP.md, then DIR… (missing: @param gsdRoot, @param milestoneId, @param suffix, @returns)
- `packages/mcp-server/src/readers/paths.ts:245` — `findSliceIds(gsdRoot: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const mDir = resolveMilestoneDir(gsdRoot, milestoneId); if (!mDir) return []; const slicesDir = join(mDir, 'slices'); if (!existsSync(slicesDir))… (missing: @param gsdRoot, @param milestoneId, @returns)
- `packages/mcp-server/src/readers/paths.ts:265` — `resolveSliceDir(gsdRoot: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const mDir = resolveMilestoneDir(gsdRoot, milestoneId); if (!mDir) return null; const slicesDir = join(mDir, 'slices'); if (!existsSync(slicesDir… (missing: @param gsdRoot, @param milestoneId, @param sliceId, @returns)
- `packages/mcp-server/src/readers/paths.ts:287` — `resolveSliceFile(gsdRoot: string, milestoneId: string, sliceId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const sDir = resolveSliceDir(gsdRoot, milestoneId, sliceId); if (!sDir) return null; const dirName = basename(sDir); const candidates = [ join(sD… (missing: @param gsdRoot, @param milestoneId, @param sliceId, @param suffix, @returns)
- `packages/mcp-server/src/readers/paths.ts:307` — `findTaskFiles(gsdRoot: string, milestoneId: string, sliceId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param gsdRoot, @param milestoneId, @param sliceId, @returns)

### `packages/mcp-server/src/remote-questions.ts`
- `packages/mcp-server/src/remote-questions.ts:125` — `parseSimpleFrontmatter(content: string)` — exported: **no** — Implements logic starting with: { const match = content.match(/^---\r?\n([\s\S]*?)\r?\n---/m); if (!match) return {}; const yaml = match[1]; const result: Record<string, unknown>… (missing: @param content, @returns)
- `packages/mcp-server/src/remote-questions.ts:212` — `isRemoteConfigured()` — exported: **yes** — Implements logic starting with: { return resolveRemoteConfig() !== null; } (missing: @returns)
- `packages/mcp-server/src/remote-questions.ts:849` — `toRoundResultResponse(answer: RemoteAnswer, questions: RemoteQuestion[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param answer, @param questions, @returns)
- `packages/mcp-server/src/remote-questions.ts:876` — `tryRemoteQuestions(questions: RemoteQuestion[], signal?: AbortSignal)` — exported: **yes** — Serializes a value to JSON. (missing: @param questions, @param signal, @returns)

### `packages/mcp-server/src/secure-env-collect.test.ts`
- `packages/mcp-server/src/secure-env-collect.test.ts:44` — `fakeElicit(response: ElicitResponse)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param response, @returns)

### `packages/mcp-server/src/server.ts`
- `packages/mcp-server/src/server.ts:65` — `defaultExecFn(cmd: string, args: string[], opts?: { stdin?: string })` — exported: **no** — Executes a subprocess command. (missing: @param cmd, @param args, @param opts, @returns)
- `packages/mcp-server/src/server.ts:113` — `withElicitTimeout(promise: Promise<T>, label: string, timeoutMs = ELICIT_TIMEOUT_MS, signal?: AbortSignal)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param promise, @param label, @param signal, @returns)
- `packages/mcp-server/src/server.ts:155` — `jsonContent(data: unknown)` — exported: **no** — Serializes a value to JSON. (missing: @param data, @returns)
- `packages/mcp-server/src/server.ts:160` — `errorContent(message: string)` — exported: **no** — Implements logic starting with: { return { isError: true, content: [{ type: 'text' as const, text: message }] }; } (missing: @param message, @returns)
- `packages/mcp-server/src/server.ts:165` — `textContent(text: string)` — exported: **no** — Implements logic starting with: { return { content: [{ type: 'text' as const, text }] }; } (missing: @param text, @returns)
- `packages/mcp-server/src/server.ts:481` — `buildAskUserQuestionsRoundResult(questions: AskUserQuestion[], result: AskUserQuestionsElicitResult)` — exported: **yes** — Implements logic starting with: { const answers: Record<string, AskUserQuestionsRoundResultAnswer> = {}; const content = result.content ?? {}; for (const question of questions) {… (missing: @param questions, @param result, @returns)
- `packages/mcp-server/src/server.ts:623` — `isRoundResultLike(value: unknown)` — exported: **no** — Implements logic starting with: { if (!value || typeof value !== 'object') return false; const answers = (value as Record<string, unknown>)['answers']; return !!answers && typeof… (missing: @param value, @returns)
- `packages/mcp-server/src/server.ts:871` — `createMcpServer(sessionManager: SessionManager)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param sessionManager, @returns)

### `packages/mcp-server/src/session-manager.ts`
- `packages/mcp-server/src/session-manager.ts:87` — `SessionManager.startSession(projectDir: string, options: ExecuteOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectDir, @param options, @returns)
- `packages/mcp-server/src/session-manager.ts:182` — `SessionManager.getSession(sessionId: string)` — exported: **yes** — Implements logic starting with: { if (!sessionId) return undefined; for (const session of this.sessions.values()) { if (session.sessionId === sessionId) return session; } return u… (missing: @param sessionId, @returns)
- `packages/mcp-server/src/session-manager.ts:193` — `SessionManager.getSessionByDir(projectDir: string)` — exported: **yes** — Implements logic starting with: { return this.sessions.get(resolve(projectDir)); } (missing: @param projectDir, @returns)
- `packages/mcp-server/src/session-manager.ts:200` — `SessionManager.resolveBlocker(sessionId: string, response: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sessionId, @param response)
- `packages/mcp-server/src/session-manager.ts:216` — `SessionManager.cancelSession(sessionId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sessionId)
- `packages/mcp-server/src/session-manager.ts:230` — `SessionManager.cancelSessionByDir(projectDir: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectDir)
- `packages/mcp-server/src/session-manager.ts:276` — `SessionManager.getResult(sessionId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sessionId, @returns)
- `packages/mcp-server/src/session-manager.ts:321` — `SessionManager.resolveCLIPath()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)

### `packages/mcp-server/src/workflow-tools.ts`
- `packages/mcp-server/src/workflow-tools.ts:320` — `resolveExternalStateRoot(allowedRoot: string)` — exported: **no** — Implements logic starting with: { try { return realpathSync(join(allowedRoot, ".gsd")); } catch { return null; } } (missing: @param allowedRoot, @returns)
- `packages/mcp-server/src/workflow-tools.ts:383` — `extractMilestoneId(parsed: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { const candidates = [parsed.milestoneId, parsed.milestone_id, parsed.mid]; for (const c of candidates) { if (typeof c === "string" && c.trim() !==… (missing: @param parsed, @returns)
- `packages/mcp-server/src/workflow-tools.ts:403` — `resolveActiveWorktreeBasePath(projectRoot: string, milestoneId: string | null)` — exported: **no** — Implements logic starting with: { if (!milestoneId) return null; const wtPath = join(projectRoot, ".gsd", "worktrees", milestoneId); if (!existsSync(wtPath)) return null; // Sanit… (missing: @param projectRoot, @param milestoneId, @returns)
- `packages/mcp-server/src/workflow-tools.ts:421` — `resolveSoleActiveWorktree(projectRoot: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param projectRoot, @returns)
- `packages/mcp-server/src/workflow-tools.ts:581` — `warnCustomWorkflowModule(varName: string, value: string)` — exported: **no** — Implements logic starting with: { if (warnedCustomWorkflowModuleVars.has(varName)) return; warnedCustomWorkflowModuleVars.add(varName); process.stderr.write( `[gsd-mcp-server] WAR… (missing: @param varName, @param value)
- `packages/mcp-server/src/workflow-tools.ts:745` — `adaptExecutorResult(result: unknown)` — exported: **no** — Implements logic starting with: { if (!result || typeof result !== "object") return result; const r = result as Record<string, unknown>; if (!("details" in r)) return result; cons… (missing: @param result, @returns)
- `packages/mcp-server/src/workflow-tools.ts:763` — `isPlainObject(value: unknown)` — exported: **no** — Implements logic starting with: { if (value === null || typeof value !== "object") return false; if (Array.isArray(value)) return false; const proto = Object.getPrototypeOf(value)… (missing: @param value, @returns)
- `packages/mcp-server/src/workflow-tools.ts:1010` — `generateOrReuseMilestoneId(projectDir: string)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param projectDir, @returns)
- `packages/mcp-server/src/workflow-tools.ts:1541` — `wrapServerWithErrorHandler(realServer: McpToolServer)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param realServer, @returns)

### `packages/native/src/clipboard/index.ts`
- `packages/native/src/clipboard/index.ts:19` — `copyToClipboard(text: string)` — exported: **yes** — Implements logic starting with: { native.copyToClipboard(text); } (missing: @param text)
- `packages/native/src/clipboard/index.ts:28` — `readTextFromClipboard()` — exported: **yes** — Implements logic starting with: { return native.readTextFromClipboard() as string | null; } (missing: @returns)
- `packages/native/src/clipboard/index.ts:38` — `readImageFromClipboard()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)

### `packages/native/src/diff/index.ts`
- `packages/native/src/diff/index.ts:20` — `normalizeForFuzzyMatch(text: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).normalizeForFuzzyMatch( text, ) as string; } (missing: @param text, @returns)
- `packages/native/src/diff/index.ts:32` — `fuzzyFindText(content: string, oldText: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).fuzzyFindText( content, oldText, ) as FuzzyMatchResult; } (missing: @param content, @param oldText, @returns)
- `packages/native/src/diff/index.ts:51` — `generateDiff(oldContent: string, newContent: string, contextLines?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).generateDiff( oldContent, newContent, contextLines, ) as DiffResult; } (missing: @returns)

### `packages/native/src/fd/index.ts`
- `packages/native/src/fd/index.ts:34` — `fuzzyFind(options: FuzzyFindOptions)` — exported: **yes** — Implements logic starting with: { return native.fuzzyFind(options) as FuzzyFindResult; } (missing: @param options, @returns)

### `packages/native/src/grep/index.ts`
- `packages/native/src/grep/index.ts:34` — `searchContent(content: Buffer | Uint8Array, options: SearchOptions)` — exported: **yes** — Implements logic starting with: { return native.search(content, options) as SearchResult; } (missing: @param content, @param options, @returns)
- `packages/native/src/grep/index.ts:47` — `grep(options: GrepOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)

### `packages/native/src/gsd-parser/index.ts`
- `packages/native/src/gsd-parser/index.ts:33` — `parseFrontmatter(content: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).parseFrontmatter( content, ) as FrontmatterResult; } (missing: @param content, @returns)
- `packages/native/src/gsd-parser/index.ts:46` — `extractSection(content: string, heading: string, level?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).extractSection( content, heading, level, ) as SectionResult; } (missing: @returns)
- `packages/native/src/gsd-parser/index.ts:64` — `extractAllSections(content: string, level?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).extractAllSections( content, level, ) as string; } (missing: @param content, @param level, @returns)
- `packages/native/src/gsd-parser/index.ts:80` — `batchParseGsdFiles(directory: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).batchParseGsdFiles( directory, ) as BatchParseResult; } (missing: @param directory, @returns)
- `packages/native/src/gsd-parser/index.ts:94` — `parseRoadmapFile(content: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).parseRoadmapFile( content, ) as NativeRoadmap; } (missing: @param content, @returns)

### `packages/native/src/highlight/index.ts`
- `packages/native/src/highlight/index.ts:35` — `supportsLanguage(lang: string)` — exported: **yes** — Implements logic starting with: { return native.supportsLanguage(lang) as boolean; } (missing: @param lang, @returns)
- `packages/native/src/highlight/index.ts:42` — `getSupportedLanguages()` — exported: **yes** — Implements logic starting with: { return native.getSupportedLanguages() as string[]; } (missing: @returns)

### `packages/native/src/html/index.ts`
- `packages/native/src/html/index.ts:19` — `htmlToMarkdown(html: string, options?: HtmlToMarkdownOptions)` — exported: **yes** — Implements logic starting with: { return native.htmlToMarkdown(html, options ?? {}) as string; } (missing: @param html, @param options, @returns)

### `packages/native/src/image/index.ts`
- `packages/native/src/image/index.ts:26` — `parseImage(bytes: Uint8Array)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param bytes, @returns)

### `packages/native/src/json-parse/index.ts`
- `packages/native/src/json-parse/index.ts:18` — `jsFallbackStreamingJson(text: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param text, @returns)
- `packages/native/src/json-parse/index.ts:46` — `parseJson(text: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param text, @returns)
- `packages/native/src/json-parse/index.ts:57` — `parsePartialJson(text: string)` — exported: **yes** — Implements logic starting with: { if (hasNativeJson) { return native.parsePartialJson(text) as T; } return jsFallbackStreamingJson<T>(text); } (missing: @param text, @returns)
- `packages/native/src/json-parse/index.ts:68` — `parseStreamingJson(text: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!text || text.trim() === "") { return {} as T; } if (hasNativeJson) { return native.parseStreamingJson(text) as T; } return jsFallbackStreami… (missing: @param text, @returns)

### `packages/native/src/stream-process/index.ts`
- `packages/native/src/stream-process/index.ts:29` — `processStreamChunk(chunk: Buffer, state?: StreamState)` — exported: **yes** — Implements logic starting with: { const nativeProcessStreamChunk = (native as Record<string, unknown>) .processStreamChunk; if (typeof nativeProcessStreamChunk !== "function") { r… (missing: @param chunk, @param state, @returns)
- `packages/native/src/stream-process/index.ts:284` — `stripAnsiNative(text: string)` — exported: **yes** — Implements logic starting with: { const nativeStripAnsi = (native as Record<string, unknown>).stripAnsiNative; if (typeof nativeStripAnsi !== "function") { return stripAnsiFallbac… (missing: @param text, @returns)
- `packages/native/src/stream-process/index.ts:302` — `sanitizeBinaryOutputNative(text: string)` — exported: **yes** — Implements logic starting with: { const nativeSanitizeBinaryOutput = (native as Record<string, unknown>) .sanitizeBinaryOutputNative; if (typeof nativeSanitizeBinaryOutput !== "fu… (missing: @param text, @returns)

### `packages/native/src/text/index.ts`
- `packages/native/src/text/index.ts:22` — `wrapTextWithAnsi(text: string, width: number, tabWidth?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).wrapTextWithAnsi( text, width, tabWidth, ) as string[]; } (missing: @param text, @param width, @param tabWidth, @returns)
- `packages/native/src/text/index.ts:43` — `truncateToWidth(text: string, maxWidth: number, ellipsisKind: number, pad: boolean, tabWidth?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).truncateToWidth( text, maxWidth, ellipsisKind, pad, tabWidth, ) as string; } (missing: @returns)
- `packages/native/src/text/index.ts:65` — `sliceWithWidth(line: string, startCol: number, length: number, strict: boolean, tabWidth?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).sliceWithWidth( line, startCol, length, strict, tabWidth, ) as SliceResult; } (missing: @param line, @param startCol, @param length, @param strict, @param tabWidth, @returns)
- `packages/native/src/text/index.ts:87` — `extractSegments(line: string, beforeEnd: number, afterStart: number, afterLen: number, strictAfter: boolean, tabWidth?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).extractSegments( line, beforeEnd, afterStart, afterLen, strictAfter, tabWidth, ) as ExtractSegmentsRe… (missing: @param line, @param beforeEnd, @param afterStart, @param afterLen, @param strictAfter, @param tabWidth, @returns)
- `packages/native/src/text/index.ts:111` — `sanitizeText(text: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).sanitizeText(text) as string; } (missing: @param text, @returns)
- `packages/native/src/text/index.ts:120` — `visibleWidth(text: string, tabWidth?: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).visibleWidth( text, tabWidth, ) as number; } (missing: @param text, @param tabWidth, @returns)

### `packages/native/src/truncate/index.ts`
- `packages/native/src/truncate/index.ts:26` — `truncateTail(text: string, maxBytes: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).truncateTail(text, maxBytes) as TruncateResult; } (missing: @param text, @param maxBytes, @returns)
- `packages/native/src/truncate/index.ts:33` — `truncateHead(text: string, maxBytes: number)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).truncateHead(text, maxBytes) as TruncateResult; } (missing: @param text, @param maxBytes, @returns)
- `packages/native/src/truncate/index.ts:40` — `truncateOutput(text: string, maxBytes: number, mode?: string)` — exported: **yes** — Implements logic starting with: { return (native as Record<string, Function>).truncateOutput( text, maxBytes, mode, ) as TruncateOutputResult; } (missing: @param text, @param maxBytes, @param mode, @returns)

### `packages/native/src/ttsr/index.ts`
- `packages/native/src/ttsr/index.ts:18` — `ttsrCompileRules(rules: TtsrRuleInput[])` — exported: **yes** — Implements logic starting with: { return native.ttsrCompileRules(rules) as TtsrHandle; } (missing: @param rules, @returns)
- `packages/native/src/ttsr/index.ts:28` — `ttsrCheckBuffer(handle: TtsrHandle, buffer: string)` — exported: **yes** — Implements logic starting with: { return native.ttsrCheckBuffer(handle, buffer) as string[]; } (missing: @param handle, @param buffer, @returns)
- `packages/native/src/ttsr/index.ts:37` — `ttsrFreeRules(handle: TtsrHandle)` — exported: **yes** — Implements logic starting with: { native.ttsrFreeRules(handle); } (missing: @param handle)

### `packages/native/src/xxhash/index.ts`
- `packages/native/src/xxhash/index.ts:24` — `accumulate(v: number, lane: number)` — exported: **no** — Implements logic starting with: { return Math.imul(rotl32((v + Math.imul(lane, PRIME32_2)) >>> 0, 13), PRIME32_1) >>> 0; } (missing: @param v, @param lane, @returns)
- `packages/native/src/xxhash/index.ts:75` — `xxHash32Fallback(input: string, seed: number)` — exported: **yes** — Implements logic starting with: { return xxHash32JS(input, seed); } (missing: @param input, @param seed, @returns)

### `packages/pi-agent-core/src/agent-loop.test.ts`
- `packages/pi-agent-core/src/agent-loop.test.ts:476` — `createMockStreamFn(responses: AssistantMessage[])` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param responses, @returns)

### `packages/pi-agent-core/src/agent-loop.ts`
- `packages/pi-agent-core/src/agent-loop.ts:48` — `createErrorMessage(error: unknown, config: AgentLoopConfig)` — exported: **no** — Implements logic starting with: { const msg = error instanceof Error ? error.message : String(error); return { role: "assistant", content: [{ type: "text", text: msg }], api: conf… (missing: @param error, @param config, @returns)
- `packages/pi-agent-core/src/agent-loop.ts:66` — `emitMessagePair(stream: EventStream<AgentEvent, AgentMessage[]>, message: AgentMessage)` — exported: **no** — Implements logic starting with: { stream.push({ type: "message_start", message }); stream.push({ type: "message_end", message }); } (missing: @param stream, @param message)
- `packages/pi-agent-core/src/agent-loop.ts:75` — `emitErrorSequence(stream: EventStream<AgentEvent, AgentMessage[]>, errMsg: AssistantMessage, newMessages: AgentMessage[])` — exported: **no** — Implements logic starting with: { emitMessagePair(stream, errMsg); stream.push({ type: "turn_end", message: errMsg, toolResults: [] }); stream.push({ type: "agent_end", messages:… (missing: @param stream, @param errMsg, @param newMessages)
- `packages/pi-agent-core/src/agent-loop.ts:90` — `agentLoop(prompts: AgentMessage[], context: AgentContext, config: AgentLoopConfig, signal?: AbortSignal, streamFn?: StreamFn)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param prompts, @param context, @param config, @param signal, @param streamFn, @returns)
- `packages/pi-agent-core/src/agent-loop.ts:130` — `agentLoopContinue(context: AgentContext, config: AgentLoopConfig, signal?: AbortSignal, streamFn?: StreamFn)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param context, @param config, @param signal, @param streamFn, @returns)
- `packages/pi-agent-core/src/agent-loop.ts:176` — `runLoop(currentContext: AgentContext, newMessages: AgentMessage[], config: AgentLoopConfig, signal: AbortSignal | undefined, stream: EventStream<AgentEvent, AgentMessage[]>, streamFn?: StreamFn)` — exported: **no** — Transforms a collection into a derived result. (missing: @param currentContext, @param newMessages, @param config, @param signal, @param stream, @param streamFn)
- `packages/pi-agent-core/src/agent-loop.ts:405` — `streamAssistantResponse(context: AgentContext, config: AgentLoopConfig, signal: AbortSignal | undefined, stream: EventStream<AgentEvent, AgentMessage[]>, streamFn?: StreamFn)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param context, @param config, @param signal, @param stream, @param streamFn, @returns)
- `packages/pi-agent-core/src/agent-loop.ts:512` — `executeToolCalls(currentContext: AgentContext, assistantMessage: AssistantMessage, config: AgentLoopConfig, signal: AbortSignal | undefined, stream: EventStream<AgentEvent, AgentMessage[]>)` — exported: **no** — Transforms a collection into a derived result. (missing: @param currentContext, @param assistantMessage, @param config, @param signal, @param stream, @returns)

### `packages/pi-agent-core/src/agent.ts`
- `packages/pi-agent-core/src/agent.ts:38` — `defaultConvertToLlm(messages: AgentMessage[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param messages, @returns)
- `packages/pi-agent-core/src/agent.ts:195` — `Agent.get sessionId()` — exported: **yes** — Implements logic starting with: { return this._sessionId; } (missing: @returns)
- `packages/pi-agent-core/src/agent.ts:203` — `Agent.set sessionId(value: string | undefined)` — exported: **yes** — Implements logic starting with: { this._sessionId = value; } (missing: @param value)
- `packages/pi-agent-core/src/agent.ts:210` — `Agent.get thinkingBudgets()` — exported: **yes** — Implements logic starting with: { return this._thinkingBudgets; } (missing: @returns)
- `packages/pi-agent-core/src/agent.ts:217` — `Agent.set thinkingBudgets(value: ThinkingBudgets | undefined)` — exported: **yes** — Implements logic starting with: { this._thinkingBudgets = value; } (missing: @param value)
- `packages/pi-agent-core/src/agent.ts:224` — `Agent.get transport()` — exported: **yes** — Implements logic starting with: { return this._transport; } (missing: @returns)
- `packages/pi-agent-core/src/agent.ts:231` — `Agent.setTransport(value: Transport)` — exported: **yes** — Implements logic starting with: { this._transport = value; } (missing: @param value)
- `packages/pi-agent-core/src/agent.ts:238` — `Agent.get maxRetryDelayMs()` — exported: **yes** — Implements logic starting with: { return this._maxRetryDelayMs; } (missing: @returns)
- `packages/pi-agent-core/src/agent.ts:246` — `Agent.set maxRetryDelayMs(value: number | undefined)` — exported: **yes** — Implements logic starting with: { this._maxRetryDelayMs = value; } (missing: @param value)
- `packages/pi-agent-core/src/agent.ts:254` — `Agent.setBeforeToolCall(fn: AgentLoopConfig["beforeToolCall"])` — exported: **yes** — Implements logic starting with: { this._beforeToolCall = fn; } (missing: @param fn)
- `packages/pi-agent-core/src/agent.ts:262` — `Agent.setAfterToolCall(fn: AgentLoopConfig["afterToolCall"])` — exported: **yes** — Implements logic starting with: { this._afterToolCall = fn; } (missing: @param fn)
- `packages/pi-agent-core/src/agent.ts:320` — `Agent.steer(m: AgentMessage, origin: "user" | "system" = "system")` — exported: **yes** — Implements logic starting with: { this.steeringQueue.push({ message: m, origin }); } (missing: @param m, @param origin)
- `packages/pi-agent-core/src/agent.ts:328` — `Agent.followUp(m: AgentMessage, origin: "user" | "system" = "system")` — exported: **yes** — Implements logic starting with: { this.followUpQueue.push({ message: m, origin }); } (missing: @param m, @param origin)
- `packages/pi-agent-core/src/agent.ts:349` — `Agent.drainUserMessages()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-agent-core/src/agent.ts:415` — `Agent.prompt(message: AgentMessage | AgentMessage[])` — exported: **yes** — Defines an API surface. (missing: @param message)

### `packages/pi-agent-core/src/proxy.ts`
- `packages/pi-agent-core/src/proxy.ts:86` — `streamProxy(model: Model<any>, context: Context, options: ProxyStreamOptions)` — exported: **no** — Validates inputs and throws on failure. (missing: @param model, @param context, @param options, @returns)
- `packages/pi-agent-core/src/proxy.ts:205` — `processProxyEvent(proxyEvent: ProxyAssistantMessageEvent, partial: AssistantMessage)` — exported: **no** — Validates inputs and throws on failure. (missing: @param proxyEvent, @param partial, @returns)

### `packages/pi-ai/src/env-api-keys.ts`
- `packages/pi-ai/src/env-api-keys.ts:63` — `getEnvApiKey(provider: KnownProvider)` — exported: **yes** — Defines an API surface. (missing: @param provider, @returns)

### `packages/pi-ai/src/models.test.ts`
- `packages/pi-ai/src/models.test.ts:266` — `syntheticModel(overrides: Partial<Model<Api>>)` — exported: **no** — Implements logic starting with: { return { id: "test-model", name: "Test Model", api: "openai-completions" as Api, provider: "test-provider", baseUrl: "https://example.com", reaso… (missing: @param overrides, @returns)

### `packages/pi-ai/src/models/capability-patches.ts`
- `packages/pi-ai/src/models/capability-patches.ts:39` — `applyCapabilityPatches(models: Model<Api>[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param models, @returns)

### `packages/pi-ai/src/models/index.ts`
- `packages/pi-ai/src/models/index.ts:99` — `supportsXhigh(model: Model<TApi>)` — exported: **yes** — Implements logic starting with: { return model.capabilities?.supportsXhigh ?? false; } (missing: @param model, @returns)
- `packages/pi-ai/src/models/index.ts:107` — `modelsAreEqual(a: Model<TApi> | null | undefined, b: Model<TApi> | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!a || !b) return false; return a.id === b.id && a.provider === b.provider; } (missing: @param a, @param b, @returns)

### `packages/pi-ai/src/providers/amazon-bedrock.test.ts`
- `packages/pi-ai/src/providers/amazon-bedrock.test.ts:29` — `makeModel(id: string)` — exported: **no** — Implements logic starting with: { return { id, name: id, api: "bedrock-converse-stream", provider: "amazon-bedrock" as any, baseUrl: "", reasoning: true, input: ["text"], cost: {… (missing: @param id, @returns)

### `packages/pi-ai/src/providers/amazon-bedrock.ts`
- `packages/pi-ai/src/providers/amazon-bedrock.ts:268` — `handleContentBlockStart(event: ContentBlockStartEvent, blocks: Block[], output: AssistantMessage, stream: AssistantMessageEventStream)` — exported: **no** — Implements logic starting with: { const index = event.contentBlockIndex!; const start = event.start; if (start?.toolUse) { const block: Block = { type: "toolCall", id: start.toolU… (missing: @param event, @param blocks, @param output, @param stream)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:292` — `handleContentBlockDelta(event: ContentBlockDeltaEvent, blocks: Block[], output: AssistantMessage, stream: AssistantMessageEventStream)` — exported: **no** — Implements logic starting with: { const contentBlockIndex = event.contentBlockIndex!; const delta = event.delta; let index = blocks.findIndex((b) => b.index === contentBlockIndex)… (missing: @param event, @param blocks, @param output, @param stream)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:351` — `handleMetadata(event: ConverseStreamMetadataEvent, model: Model<"bedrock-converse-stream">, output: AssistantMessage)` — exported: **no** — Implements logic starting with: { if (event.usage) { output.usage.input = event.usage.inputTokens || 0; output.usage.output = event.usage.outputTokens || 0; output.usage.cacheRead… (missing: @param event, @param model, @param output)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:367` — `handleContentBlockStop(event: ContentBlockStopEvent, blocks: Block[], output: AssistantMessage, stream: AssistantMessageEventStream)` — exported: **no** — Implements logic starting with: { const index = blocks.findIndex((b) => b.index === event.contentBlockIndex); const block = blocks[index]; if (!block) return; delete (block as Blo… (missing: @param event, @param blocks, @param output, @param stream)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:397` — `supportsAdaptiveThinking(modelId: string)` — exported: **yes** — Implements logic starting with: { return ( modelId.includes("opus-4-6") || modelId.includes("opus-4.6") || modelId.includes("opus-4-7") || modelId.includes("opus-4.7") || modelId.… (missing: @param modelId, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:417` — `mapThinkingLevelToEffort(level: SimpleStreamOptions["reasoning"], modelId: string)` — exported: **yes** — Implements logic starting with: { switch (level) { case "minimal": case "low": return "low"; case "medium": return "medium"; case "high": return "high"; case "xhigh": if (modelId.… (missing: @param level, @param modelId, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:442` — `resolveCacheRetention(cacheRetention?: CacheRetention)` — exported: **no** — Implements logic starting with: { if (cacheRetention) { return cacheRetention; } if (typeof process !== "undefined" && process.env.PI_CACHE_RETENTION === "long") { return "long";… (missing: @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:456` — `supportsPromptCaching(model: Model<"bedrock-converse-stream">)` — exported: **no** — Implements logic starting with: { if (model.cost.cacheRead || model.cost.cacheWrite) { return true; } const id = model.id.toLowerCase(); // Claude 4.x models (opus-4, sonnet-4, ha… (missing: @param model, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:477` — `supportsThinkingSignature(model: Model<"bedrock-converse-stream">)` — exported: **no** — Implements logic starting with: { const id = model.id.toLowerCase(); return id.includes("anthropic.claude") || id.includes("anthropic/claude"); } (missing: @param model, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:483` — `buildSystemPrompt(systemPrompt: string | undefined, model: Model<"bedrock-converse-stream">, cacheRetention: CacheRetention)` — exported: **no** — Implements logic starting with: { if (!systemPrompt) return undefined; const blocks: SystemContentBlock[] = [{ text: sanitizeSurrogates(systemPrompt) }]; // Add cache point for su… (missing: @param systemPrompt, @param model, @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:503` — `normalizeToolCallId(id: string)` — exported: **no** — Implements logic starting with: { const sanitized = id.replace(/[^a-zA-Z0-9_-]/g, "_"); return sanitized.length > 64 ? sanitized.slice(0, 64) : sanitized; } (missing: @param id, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:509` — `convertMessages(context: Context, model: Model<"bedrock-converse-stream">, cacheRetention: CacheRetention)` — exported: **no** — Validates inputs and throws on failure. (missing: @param context, @param model, @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:659` — `convertToolConfig(tools: Tool[] | undefined, toolChoice: BedrockOptions["toolChoice"], model: Model<"bedrock-converse-stream">, cacheRetention: CacheRetention)` — exported: **no** — Transforms a collection into a derived result. (missing: @param tools, @param toolChoice, @param model, @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:703` — `mapStopReason(reason: string | undefined)` — exported: **no** — Implements logic starting with: { switch (reason) { case BedrockStopReason.END_TURN: case BedrockStopReason.STOP_SEQUENCE: return "stop"; case BedrockStopReason.MAX_TOKENS: case B… (missing: @param reason, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:724` — `buildAdditionalModelRequestFields(model: Model<"bedrock-converse-stream">, options: BedrockOptions)` — exported: **yes** — Implements logic starting with: { if (!options.reasoning || !model.reasoning) { return undefined; } if (model.id.includes("anthropic.claude") || model.id.includes("anthropic/claud… (missing: @param model, @param options, @returns)
- `packages/pi-ai/src/providers/amazon-bedrock.ts:770` — `createImageBlock(mimeType: string, data: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param mimeType, @param data, @returns)

### `packages/pi-ai/src/providers/anthropic-shared.cache-breakpoint.test.ts`
- `packages/pi-ai/src/providers/anthropic-shared.cache-breakpoint.test.ts:32` — `userMsgArray(text: string, opts: { cacheBreakpoint?: boolean } = {})` — exported: **no** — Implements logic starting with: { return { role: "user", content: [{ type: "text", text }], timestamp: 0, ...(opts.cacheBreakpoint ? { cacheBreakpoint: true } : {}), } as Message;… (missing: @param text, @param opts, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.cache-breakpoint.test.ts:58` — `hasCacheControl(params: ReturnType<typeof convertMessages>, index: number)` — exported: **no** — Implements logic starting with: { const param = params[index]; if (!param || param.role !== "user") return false; if (!Array.isArray(param.content)) return false; const lastBlock… (missing: @param params, @param index, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.cache-breakpoint.test.ts:200` — `countBreakpoints(params: { system?: any; tools?: any[]; messages: any[] })` — exported: **no** — Implements logic starting with: { let n = 0; if (Array.isArray(params.system)) { for (const block of params.system) if (block.cache_control) n++; } if (Array.isArray(params.tools)… (missing: @param params, @returns)

### `packages/pi-ai/src/providers/anthropic-shared.ts`
- `packages/pi-ai/src/providers/anthropic-shared.ts:92` — `resolveCacheRetention(cacheRetention?: CacheRetention)` — exported: **no** — Implements logic starting with: { if (cacheRetention) { return cacheRetention; } if (typeof process !== "undefined" && process.env.PI_CACHE_RETENTION === "long") { return "long";… (missing: @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:103` — `getCacheControl(baseUrl: string, cacheRetention?: CacheRetention)` — exported: **yes** — Implements logic starting with: { const retention = resolveCacheRetention(cacheRetention); if (retention === "none") { return { retention }; } const ttl = retention === "long" &&… (missing: @param baseUrl, @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:119` — `convertContentBlocks(content: (TextContent | ImageContent)[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:166` — `supportsAdaptiveThinking(modelId: string)` — exported: **yes** — Implements logic starting with: { return ( modelId.includes("opus-4-6") || modelId.includes("opus-4.6") || modelId.includes("opus-4-7") || modelId.includes("opus-4.7") || modelId.… (missing: @param modelId, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:182` — `mapThinkingLevelToEffort(level: string | undefined, modelId: string)` — exported: **yes** — Implements logic starting with: { switch (level) { case "minimal": return "low"; case "low": return "low"; case "medium": return "medium"; case "high": return "high"; case "xhigh"… (missing: @param level, @param modelId, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:202` — `isTransientNetworkError(error: unknown)` — exported: **yes** — Implements logic starting with: { if (!(error instanceof Error)) return false; const msg = error.message.toLowerCase(); const code = (error as NodeJS.ErrnoException).code; return… (missing: @param error, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:221` — `extractRetryAfterMs(headers: Headers | { get(name: string): string | null }, errorText = "")` — exported: **yes** — Implements logic starting with: { const normalizeDelay = (ms: number): number | undefined => (ms > 0 ? Math.ceil(ms + 1000) : undefined); const retryAfter = headers.get("retry-aft… (missing: @param headers, @param errorText, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:253` — `normalizeToolCallId(id: string)` — exported: **yes** — Implements logic starting with: { return id.replace(/[^a-zA-Z0-9_-]/g, "_").slice(0, 64); } (missing: @param id, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:258` — `convertMessages(messages: Message[], model: Model<AnthropicApi>, isOAuthToken: boolean, cacheControl?: { type: "ephemeral"; ttl?: "1h" })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param messages, @param model, @param isOAuthToken, @param cacheControl, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:431` — `applyCacheControlToParam(params: MessageParam[], index: number, cacheControl: { type: "ephemeral"; ttl?: "1h" })` — exported: **no** — Implements logic starting with: { const param = params[index]; if (!param || param.role !== "user") return; if (Array.isArray(param.content)) { const lastBlock = param.content[par… (missing: @param params, @param index, @param cacheControl)
- `packages/pi-ai/src/providers/anthropic-shared.ts:458` — `convertTools(tools: Tool[], isOAuthToken: boolean, cacheControl?: { type: "ephemeral"; ttl?: "1h" })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tools, @param isOAuthToken, @param cacheControl, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:488` — `buildParams(model: Model<AnthropicApi>, context: Context, isOAuthToken: boolean, options?: AnthropicOptions)` — exported: **yes** — Implements logic starting with: { const { cacheControl } = getCacheControl(model.baseUrl, options?.cacheRetention); const apiModelId = model.id.replace(/\[.*\]$/, ""); const param… (missing: @param model, @param context, @param isOAuthToken, @param options, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:577` — `mapStopReason(reason: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param reason, @returns)
- `packages/pi-ai/src/providers/anthropic-shared.ts:609` — `processAnthropicStream(stream: AssistantMessageEventStream, args: StreamAnthropicArgs)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param stream, @param args)

### `packages/pi-ai/src/providers/anthropic.ts`
- `packages/pi-ai/src/providers/anthropic.ts:38` — `resolveAnthropicBaseUrl(model: Model<"anthropic-messages">)` — exported: **yes** — Implements logic starting with: { const envBaseUrl = process.env.ANTHROPIC_BASE_URL?.trim(); if (envBaseUrl) { return envBaseUrl; } return model.baseUrl; } (missing: @param model, @returns)

### `packages/pi-ai/src/providers/api-family.ts`
- `packages/pi-ai/src/providers/api-family.ts:22` — `isAnthropicApi(model: HasApi)` — exported: **yes** — Implements logic starting with: { const api = model?.api; return api === "anthropic-messages" || api === "anthropic-vertex"; } (missing: @param model, @returns)
- `packages/pi-ai/src/providers/api-family.ts:31` — `isOpenAIApi(model: HasApi)` — exported: **yes** — Implements logic starting with: { const api = model?.api; return ( api === "openai-completions" || api === "openai-responses" || api === "azure-openai-responses" || api === "opena… (missing: @param model, @returns)
- `packages/pi-ai/src/providers/api-family.ts:45` — `isGeminiApi(model: HasApi)` — exported: **yes** — Implements logic starting with: { const api = model?.api; return ( api === "google-generative-ai" || api === "google-gemini-cli" || api === "google-vertex" ); } (missing: @param model, @returns)
- `packages/pi-ai/src/providers/api-family.ts:55` — `isBedrockApi(model: HasApi)` — exported: **yes** — Implements logic starting with: { return model?.api === "bedrock-converse-stream"; } (missing: @param model, @returns)

### `packages/pi-ai/src/providers/fake.ts`
- `packages/pi-ai/src/providers/fake.ts:203` — `createFakeProvider(opts: { transcriptPath: string })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param opts, @returns)

### `packages/pi-ai/src/providers/google-gemini-cli.ts`
- `packages/pi-ai/src/providers/google-gemini-cli.ts:115` — `extractRetryDelay(errorText: string, response?: Response | Headers)` — exported: **no** — Implements logic starting with: { const normalizeDelay = (ms: number): number | undefined => (ms > 0 ? Math.ceil(ms + 1000) : undefined); const headers = response instanceof Heade… (missing: @param errorText, @param response, @returns)
- `packages/pi-ai/src/providers/google-gemini-cli.ts:225` — `isRetryableError(status: number, errorText: string)` — exported: **no** — Implements logic starting with: { if (status === 429 || status === 500 || status === 502 || status === 503 || status === 504) { return true; } return /resource.?exhausted|rate.?li… (missing: @param status, @param errorText, @returns)
- `packages/pi-ai/src/providers/google-gemini-cli.ts:236` — `extractErrorMessage(errorText: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param errorText, @returns)
- `packages/pi-ai/src/providers/google-gemini-cli.ts:251` — `sleep(ms: number, signal?: AbortSignal)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ms, @param signal)

### `packages/pi-ai/src/providers/google-shared.ts`
- `packages/pi-ai/src/providers/google-shared.ts:27` — `isThinkingPart(part: Pick<Part, "thought" | "thoughtSignature">)` — exported: **yes** — Implements logic starting with: { return part.thought === true; } (missing: @param part, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:40` — `retainThoughtSignature(existing: string | undefined, incoming: string | undefined)` — exported: **yes** — Implements logic starting with: { if (typeof incoming === "string" && incoming.length > 0) return incoming; return existing; } (missing: @param existing, @param incoming, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:62` — `resolveThoughtSignature(isSameProviderAndModel: boolean, signature: string | undefined)` — exported: **no** — Implements logic starting with: { return isSameProviderAndModel && isValidThoughtSignature(signature) ? signature : undefined; } (missing: @param isSameProviderAndModel, @param signature, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:69` — `requiresToolCallId(modelId: string)` — exported: **no** — Implements logic starting with: { return modelId.startsWith("claude-") || modelId.startsWith("gpt-oss-"); } (missing: @param modelId, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:76` — `convertMessages(model: Model<T>, context: Context)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param model, @param context, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:240` — `sanitizeSchemaForGoogle(schema: unknown)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param schema, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:286` — `convertTools(tools: Tool[], useParameters = false)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tools, @param useParameters, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:307` — `mapToolChoice(choice: string)` — exported: **yes** — Implements logic starting with: { switch (choice) { case "auto": return FunctionCallingConfigMode.AUTO; case "none": return FunctionCallingConfigMode.NONE; case "any": return Func… (missing: @param choice, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:323` — `mapStopReason(reason: FinishReason)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param reason, @returns)
- `packages/pi-ai/src/providers/google-shared.ts:355` — `mapStopReasonString(reason: string)` — exported: **yes** — Implements logic starting with: { switch (reason) { case "STOP": return "stop"; case "MAX_TOKENS": return "length"; default: return "error"; } } (missing: @param reason, @returns)

### `packages/pi-ai/src/providers/openai-completions.ts`
- `packages/pi-ai/src/providers/openai-completions.ts:50` — `hasToolHistory(messages: Message[])` — exported: **no** — Implements logic starting with: { for (const msg of messages) { if (msg.role === "toolResult") { return true; } if (msg.role === "assistant") { if (msg.content.some((block) => blo… (missing: @param messages, @returns)
- `packages/pi-ai/src/providers/openai-completions.ts:750` — `detectCompat(model: Model<"openai-completions">)` — exported: **no** — Implements logic starting with: { const provider = model.provider; const baseUrl = model.baseUrl; const isZai = provider === "zai" || baseUrl.includes("api.z.ai"); const isNonStan… (missing: @param model, @returns)
- `packages/pi-ai/src/providers/openai-completions.ts:803` — `getCompat(model: Model<"openai-completions">)` — exported: **no** — Implements logic starting with: { const detected = detectCompat(model); if (!model.compat) return detected; return { supportsStore: model.compat.supportsStore ?? detected.supports… (missing: @param model, @returns)

### `packages/pi-ai/src/providers/openai-responses.ts`
- `packages/pi-ai/src/providers/openai-responses.ts:33` — `resolveCacheRetention(cacheRetention?: CacheRetention)` — exported: **no** — Implements logic starting with: { if (cacheRetention) { return cacheRetention; } if (typeof process !== "undefined" && process.env.PI_CACHE_RETENTION === "long") { return "long";… (missing: @param cacheRetention, @returns)
- `packages/pi-ai/src/providers/openai-responses.ts:47` — `getPromptCacheRetention(baseUrl: string, cacheRetention: CacheRetention)` — exported: **no** — Implements logic starting with: { if (cacheRetention !== "long") { return undefined; } if (baseUrl.includes("api.openai.com")) { return "24h"; } return undefined; } (missing: @param baseUrl, @param cacheRetention, @returns)

### `packages/pi-ai/src/providers/openai-shared.ts`
- `packages/pi-ai/src/providers/openai-shared.ts:31` — `getOpenAIClass()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-ai/src/providers/openai-shared.ts:59` — `createOpenAIClient(model: Model<TApi>, context: Context, apiKey: string | undefined, options?: CreateClientOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param model, @param context, @param apiKey, @param options, @returns)
- `packages/pi-ai/src/providers/openai-shared.ts:107` — `buildInitialOutput(model: Model<TApi>)` — exported: **yes** — Implements logic starting with: { return { role: "assistant", content: [], api: model.api as Api, provider: model.provider, model: model.id, usage: { input: 0, output: 0, cacheRea… (missing: @param model, @returns)
- `packages/pi-ai/src/providers/openai-shared.ts:137` — `assertStreamSuccess(output: AssistantMessage, signal?: AbortSignal)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param output, @param signal)
- `packages/pi-ai/src/providers/openai-shared.ts:149` — `finalizeStream(stream: AssistantMessageEventStream, output: AssistantMessage)` — exported: **yes** — Implements logic starting with: { stream.push({ type: "done", reason: output.stopReason as Extract<StopReason, "stop" | "length" | "toolUse">, message: output }); stream.end(); } (missing: @param stream, @param output)
- `packages/pi-ai/src/providers/openai-shared.ts:163` — `handleStreamError(stream: AssistantMessageEventStream, output: AssistantMessage, error: unknown, signal?: AbortSignal, extraMessage?: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param stream, @param output, @param error, @param signal, @param extraMessage)
- `packages/pi-ai/src/providers/openai-shared.ts:189` — `clampReasoningForModel(modelName: string, effort: string)` — exported: **yes** — Implements logic starting with: { const name = modelName.includes("/") ? modelName.split("/").pop()! : modelName; if (name.startsWith("gpt-5") && effort === "minimal") return "low… (missing: @param modelName, @param effort, @returns)

### `packages/pi-ai/src/providers/provider-capabilities.ts`
- `packages/pi-ai/src/providers/provider-capabilities.ts:175` — `getProviderCapabilities(api: string)` — exported: **yes** — Implements logic starting with: { return PROVIDER_CAPABILITIES[api] ?? DEFAULT_CAPABILITIES; } (missing: @param api, @returns)
- `packages/pi-ai/src/providers/provider-capabilities.ts:183` — `getUnsupportedFeatures(api: string, requiredFeatures: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param api, @param requiredFeatures, @returns)
- `packages/pi-ai/src/providers/provider-capabilities.ts:192` — `mergeCapabilityOverrides(api: string, overrides: Partial<Omit<ProviderCapabilities, "toolCallIdFormat">> & { toolCallIdFormat?: Partial<ProviderCapabilities["toolCallIdFormat"]>; })` — exported: **yes** — Implements logic starting with: { const base = getProviderCapabilities(api); return { ...base, ...overrides, toolCallIdFormat: overrides.toolCallIdFormat ? { ...base.toolCallIdFor… (missing: @param api, @param overrides, @returns)
- `packages/pi-ai/src/providers/provider-capabilities.ts:213` — `getRegisteredApis()` — exported: **yes** — Implements logic starting with: { return Object.keys(PROVIDER_CAPABILITIES); } (missing: @returns)

### `packages/pi-ai/src/providers/simple-options.ts`
- `packages/pi-ai/src/providers/simple-options.ts:12` — `defaultMaxTokens(model: Model<Api>)` — exported: **yes** — Implements logic starting with: { if (model.api === "anthropic-messages") { return Math.min(model.maxTokens, 32000); } return model.maxTokens; } (missing: @param model, @returns)

### `packages/pi-ai/src/providers/think-tag-parser.ts`
- `packages/pi-ai/src/providers/think-tag-parser.ts:29` — `class ThinkTagParser` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-ai/src/providers/transform-messages.ts`
- `packages/pi-ai/src/providers/transform-messages.ts:37` — `setProviderSwitchObserver(observer: ProviderSwitchObserver | undefined)` — exported: **yes** — Implements logic starting with: { providerSwitchObserver = observer; } (missing: @param observer)
- `packages/pi-ai/src/providers/transform-messages.ts:44` — `createEmptyReport(fromApi: string, toApi: string)` — exported: **yes** — Implements logic starting with: { return { fromApi, toApi, thinkingBlocksDropped: 0, thinkingBlocksDowngraded: 0, toolCallIdsRemapped: 0, syntheticToolResultsInserted: 0, thoughtS… (missing: @param fromApi, @param toApi, @returns)
- `packages/pi-ai/src/providers/transform-messages.ts:59` — `hasTransformations(report: ProviderSwitchReport)` — exported: **yes** — Implements logic starting with: { return ( report.thinkingBlocksDropped > 0 || report.thinkingBlocksDowngraded > 0 || report.toolCallIdsRemapped > 0 || report.syntheticToolResults… (missing: @param report, @returns)
- `packages/pi-ai/src/providers/transform-messages.ts:73` — `transformMessagesWithReport(messages: Message[], model: Model<TApi>, normalizeToolCallId?: (id: string, model: Model<TApi>, source: AssistantMessage) => string, sourceApi?: string)` — exported: **yes** — Implements logic starting with: { const report = createEmptyReport(sourceApi ?? "unknown", model.api); const result = transformMessages(messages, model, normalizeToolCallId, repor… (missing: @param messages, @param model, @param normalizeToolCallId, @param sourceApi, @returns)
- `packages/pi-ai/src/providers/transform-messages.ts:95` — `logProviderSwitchReport(report: ProviderSwitchReport)` — exported: **no** — Implements logic starting with: { const parts: string[] = [`Provider switch ${report.fromApi} → ${report.toApi}:`]; if (report.thinkingBlocksDropped > 0) parts.push(`${report.thin… (missing: @param report)
- `packages/pi-ai/src/providers/transform-messages.ts:114` — `transformMessages(messages: Message[], model: Model<TApi>, normalizeToolCallId?: (id: string, model: Model<TApi>, source: AssistantMessage) => string, report?: ProviderSwitchReport)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param messages, @param model, @param normalizeToolCallId, @param report, @returns)

### `packages/pi-ai/src/utils/event-stream.ts`
- `packages/pi-ai/src/utils/event-stream.ts:85` — `createAssistantMessageEventStream()` — exported: **yes** — Implements logic starting with: { return new AssistantMessageEventStream(); } (missing: @returns)

### `packages/pi-ai/src/utils/hash.ts`
- `packages/pi-ai/src/utils/hash.ts:2` — `shortHash(str: string)` — exported: **yes** — Implements logic starting with: { let h1 = 0xdeadbeef; let h2 = 0x41c6ce57; for (let i = 0; i < str.length; i++) { const ch = str.charCodeAt(i); h1 = Math.imul(h1 ^ ch, 2654435761… (missing: @param str, @returns)

### `packages/pi-ai/src/utils/oauth/github-copilot.ts`
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:83` — `getBaseUrlFromToken(token: string)` — exported: **no** — Implements logic starting with: { const match = token.match(/proxy-ep=([^;]+)/); if (!match) return null; const proxyHost = match[1]; // Convert proxy.xxx to api.xxx const apiHost… (missing: @param token, @returns)
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:162` — `abortableSleep(ms: number, signal?: AbortSignal)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ms, @param signal)
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:241` — `refreshGitHubCopilotToken(refreshToken: string, enterpriseDomain?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param refreshToken, @param enterpriseDomain, @returns)
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:279` — `enableGitHubCopilotModel(token: string, modelId: string, enterpriseDomain?: string)` — exported: **no** — Performs an HTTP request and returns a result. (missing: @param token, @param modelId, @param enterpriseDomain, @returns)
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:306` — `enableAllGitHubCopilotModels(token: string, enterpriseDomain?: string, onProgress?: (model: string, success: boolean) => void)` — exported: **no** — Transforms a collection into a derived result. (missing: @param token, @param enterpriseDomain, @param onProgress)
- `packages/pi-ai/src/utils/oauth/github-copilot.ts:369` — `loginGitHubCopilot(options: { onAuth: (url: string, instructions?: string) => void; onPrompt: (prompt: { message: string; placeholder?: string; allowEmpty?: boolean }) => Promise<string>; onProgress?: (message: string) => void; signal?: AbortSignal; })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param options, @returns)

### `packages/pi-ai/src/utils/oauth/google-antigravity.ts`
- `packages/pi-ai/src/utils/oauth/google-antigravity.ts:69` — `discoverProject(accessToken: string, onProgress?: (message: string) => void)` — exported: **no** — Performs an HTTP request and returns a result. (missing: @param accessToken, @param onProgress, @returns)
- `packages/pi-ai/src/utils/oauth/google-antigravity.ts:130` — `refreshAntigravityToken(refreshToken: string, projectId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param refreshToken, @param projectId, @returns)
- `packages/pi-ai/src/utils/oauth/google-antigravity.ts:142` — `loginAntigravity(onAuth: (info: { url: string; instructions?: string }) => void, onProgress?: (message: string) => void, onManualCodeInput?: () => Promise<string>)` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)

### `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts`
- `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts:84` — `wait(ms: number)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ms)
- `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts:91` — `getDefaultTier(allowedTiers?: Array<{ id?: string; isDefault?: boolean }>)` — exported: **no** — Implements logic starting with: { if (!allowedTiers || allowedTiers.length === 0) return { id: TIER_LEGACY }; const defaultTier = allowedTiers.find((t) => t.isDefault); return def… (missing: @param allowedTiers, @returns)
- `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts:108` — `pollOperation(operationName: string, headers: Record<string, string>, onProgress?: (message: string) => void)` — exported: **no** — Validates inputs and throws on failure. (missing: @param operationName, @param headers, @param onProgress, @returns)
- `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts:142` — `discoverProject(accessToken: string, onProgress?: (message: string) => void)` — exported: **no** — Validates inputs and throws on failure. (missing: @param accessToken, @param onProgress, @returns)
- `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts:275` — `refreshGoogleCloudToken(refreshToken: string, projectId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param refreshToken, @param projectId, @returns)
- `packages/pi-ai/src/utils/oauth/google-gemini-cli.ts:287` — `loginGeminiCli(onAuth: (info: { url: string; instructions?: string }) => void, onProgress?: (message: string) => void, onManualCodeInput?: () => Promise<string>)` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)

### `packages/pi-ai/src/utils/oauth/google-oauth-utils.ts`
- `packages/pi-ai/src/utils/oauth/google-oauth-utils.ts:30` — `getNodeCreateServer(providerName: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param providerName, @returns)
- `packages/pi-ai/src/utils/oauth/google-oauth-utils.ts:48` — `startCallbackServer(port: number, callbackPath: string, providerName: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-ai/src/utils/oauth/google-oauth-utils.ts:118` — `parseRedirectUrl(input: string)` — exported: **yes** — Implements logic starting with: { const value = input.trim(); if (!value) return {}; try { const url = new URL(value); return { code: url.searchParams.get("code") ?? undefined, st… (missing: @param input, @returns)
- `packages/pi-ai/src/utils/oauth/google-oauth-utils.ts:137` — `getGoogleUserEmail(accessToken: string)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: @param accessToken, @returns)
- `packages/pi-ai/src/utils/oauth/google-oauth-utils.ts:165` — `refreshGoogleOAuthToken(refreshToken: string, clientId: string, clientSecret: string, providerName: string, extraFields?: Record<string, unknown>)` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)

### `packages/pi-ai/src/utils/oauth/index.ts`
- `packages/pi-ai/src/utils/oauth/index.ts:55` — `getOAuthProvider(id: OAuthProviderId)` — exported: **yes** — Implements logic starting with: { return oauthProviderRegistry.get(id); } (missing: @param id, @returns)
- `packages/pi-ai/src/utils/oauth/index.ts:62` — `registerOAuthProvider(provider: OAuthProviderInterface)` — exported: **yes** — Implements logic starting with: { oauthProviderRegistry.set(provider.id, provider); } (missing: @param provider)
- `packages/pi-ai/src/utils/oauth/index.ts:72` — `unregisterOAuthProvider(id: string)` — exported: **yes** — Implements logic starting with: { const builtInProvider = BUILT_IN_OAUTH_PROVIDERS.find((provider) => provider.id === id); if (builtInProvider) { oauthProviderRegistry.set(id, bui… (missing: @param id)
- `packages/pi-ai/src/utils/oauth/index.ts:94` — `getOAuthProviders()` — exported: **yes** — Implements logic starting with: { return Array.from(oauthProviderRegistry.values()); } (missing: @returns)
- `packages/pi-ai/src/utils/oauth/index.ts:109` — `getOAuthApiKey(providerId: OAuthProviderId, credentials: Record<string, OAuthCredentials>)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param providerId, @param credentials)

### `packages/pi-ai/src/utils/oauth/openai-codex.ts`
- `packages/pi-ai/src/utils/oauth/openai-codex.ts:315` — `loginOpenAICodex(options: { onAuth: (info: { url: string; instructions?: string }) => void; onPrompt: (prompt: OAuthPrompt) => Promise<string>; onProgress?: (message: string) => void; onManualCodeInput?: () => Promise<string>; originator?: string; })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param options, @returns)
- `packages/pi-ai/src/utils/oauth/openai-codex.ts:425` — `refreshOpenAICodexToken(refreshToken: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param refreshToken, @returns)

### `packages/pi-ai/src/utils/oauth/pkce.ts`
- `packages/pi-ai/src/utils/oauth/pkce.ts:9` — `base64urlEncode(bytes: Uint8Array)` — exported: **no** — Implements logic starting with: { let binary = ""; for (const byte of bytes) { binary += String.fromCharCode(byte); } return btoa(binary).replace(/\+/g, "-").replace(/\//g, "_").r… (missing: @param bytes, @returns)
- `packages/pi-ai/src/utils/oauth/pkce.ts:21` — `generatePKCE()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)

### `packages/pi-ai/src/utils/repair-tool-json.ts`
- `packages/pi-ai/src/utils/repair-tool-json.ts:24` — `hasYamlBulletLists(json: string)` — exported: **yes** — Implements logic starting with: { // Match: "key": followed by whitespace then a dash-space pattern (YAML bullet) // The negative lookahead excludes negative numbers (e.g. "key":… (missing: @param json, @returns)
- `packages/pi-ai/src/utils/repair-tool-json.ts:39` — `hasXmlParameterTags(json: string)` — exported: **yes** — Implements logic starting with: { return /<\/?parameter[\s>]/.test(json); } (missing: @param json, @returns)
- `packages/pi-ai/src/utils/repair-tool-json.ts:52` — `hasTruncatedNumbers(json: string)` — exported: **yes** — Implements logic starting with: { // Match: colon, optional whitespace, then a comma or } without a value // Or: colon, optional whitespace, bare minus sign followed by comma/} re… (missing: @param json, @returns)
- `packages/pi-ai/src/utils/repair-tool-json.ts:123` — `stripXmlParameterTags(json: string)` — exported: **no** — Implements logic starting with: { // Remove opening tags: <parameter name="X"> let cleaned = json.replace(/<parameter\s+name="[^"]*"\s*>/g, ""); // Remove closing tags: </paramete… (missing: @param json, @returns)
- `packages/pi-ai/src/utils/repair-tool-json.ts:164` — `repairTruncatedNumbers(json: string)` — exported: **no** — Implements logic starting with: { // Bare comma after colon (missing value entirely) let repaired = json.replace(/:\s*,/g, ": 0,"); // Bare minus sign followed by comma or closing… (missing: @param json, @returns)
- `packages/pi-ai/src/utils/repair-tool-json.ts:184` — `repairToolJson(json: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param json, @returns)

### `packages/pi-ai/src/utils/typebox-helpers.ts`
- `packages/pi-ai/src/utils/typebox-helpers.ts:14` — `StringEnum(values: T, options?: { description?: string; default?: T[number] })` — exported: **yes** — Implements logic starting with: { return Type.Unsafe<T[number]>({ type: "string", enum: values as any, ...(options?.description && { description: options.description }), ...(optio… (missing: @param values, @param options, @returns)

### `packages/pi-ai/src/web-runtime-env-api-keys.ts`
- `packages/pi-ai/src/web-runtime-env-api-keys.ts:30` — `getEnvApiKey(provider: KnownProvider)` — exported: **yes** — Defines an API surface. (missing: @param provider, @returns)

### `packages/pi-coding-agent/scripts/copy-assets.cjs`
- `packages/pi-coding-agent/scripts/copy-assets.cjs:9` — `safeCpSync(src, dest, options)` — exported: **no** — Implements logic starting with: { try { cpSync(src, dest, options); } catch { if (options && options.recursive) { copyDirRecursive(src, dest, options && options.filter); } else {… (missing: @param src, @param dest, @param options)

### `packages/pi-coding-agent/src/cli/config-selector.ts`
- `packages/pi-coding-agent/src/cli/config-selector.ts:19` — `selectConfig(options: ConfigSelectorOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)

### `packages/pi-coding-agent/src/cli/file-processor.ts`
- `packages/pi-coding-agent/src/cli/file-processor.ts:24` — `processFileArguments(fileArgs: string[], options?: ProcessFileOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param fileArgs, @param options, @returns)

### `packages/pi-coding-agent/src/cli/list-models.ts`
- `packages/pi-coding-agent/src/cli/list-models.ts:19` — `formatTokenCount(count: number)` — exported: **no** — Implements logic starting with: { if (count >= 1_000_000) { const millions = count / 1_000_000; return millions % 1 === 0 ? `${millions}M` : `${millions.toFixed(1)}M`; } if (count… (missing: @param count, @returns)
- `packages/pi-coding-agent/src/cli/list-models.ts:34` — `discoverAndPrintModels(modelRegistry: ModelRegistry, provider?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param modelRegistry, @param provider)
- `packages/pi-coding-agent/src/cli/list-models.ts:56` — `listModels(modelRegistry: ModelRegistry, optionsOrSearch?: string | ListModelsOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param modelRegistry, @param optionsOrSearch)

### `packages/pi-coding-agent/src/cli/session-picker.ts`
- `packages/pi-coding-agent/src/cli/session-picker.ts:13` — `selectSession(currentSessionsLoader: SessionsLoader, allSessionsLoader: SessionsLoader)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param currentSessionsLoader, @param allSessionsLoader, @returns)

### `packages/pi-coding-agent/src/config.ts`
- `packages/pi-coding-agent/src/config.ts:115` — `getThemesDir()` — exported: **yes** — Implements logic starting with: { if (isBunBinary) { return join(dirname(process.execPath), "theme"); } // Theme is in modes/interactive/theme/ relative to src/ or dist/ const pac… (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:131` — `getExportTemplateDir()` — exported: **yes** — Implements logic starting with: { if (isBunBinary) { return join(dirname(process.execPath), "export-html"); } const packageDir = getPackageDir(); const srcOrDist = existsSync(join… (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:141` — `getPackageJsonPath()` — exported: **no** — Implements logic starting with: { return join(getPackageDir(), "package.json"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:146` — `getReadmePath()` — exported: **yes** — Implements logic starting with: { return resolve(join(getPackageDir(), "README.md")); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:151` — `getDocsPath()` — exported: **yes** — Implements logic starting with: { return resolve(join(getPackageDir(), "docs")); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:156` — `getExamplesPath()` — exported: **yes** — Implements logic starting with: { return resolve(join(getPackageDir(), "examples")); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:161` — `getChangelogPath()` — exported: **yes** — Implements logic starting with: { return resolve(join(getPackageDir(), "CHANGELOG.md")); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:181` — `getShareViewerUrl(gistId: string)` — exported: **yes** — Implements logic starting with: { const baseUrl = process.env.PI_SHARE_VIEWER_URL || DEFAULT_SHARE_VIEWER_URL; return `${baseUrl}#${gistId}`; } (missing: @param gistId, @returns)
- `packages/pi-coding-agent/src/config.ts:191` — `getAgentDir()` — exported: **yes** — Implements logic starting with: { const envDir = process.env[ENV_AGENT_DIR]; if (envDir) { // Expand tilde to home directory if (envDir === "~") return homedir(); if (envDir.start… (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:203` — `getCustomThemesDir()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "themes"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:208` — `getModelsPath()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "models.json"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:213` — `getAuthPath()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "auth.json"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:218` — `getSettingsPath()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "settings.json"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:223` — `getBinDir()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "bin"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:228` — `getPromptsDir()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "prompts"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:233` — `getSessionsDir()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "sessions"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:238` — `getBlobsDir()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), "blobs"); } (missing: @returns)
- `packages/pi-coding-agent/src/config.ts:243` — `getDebugLogPath()` — exported: **yes** — Implements logic starting with: { return join(getAgentDir(), `${APP_NAME}-debug.log`); } (missing: @returns)

### `packages/pi-coding-agent/src/core/agent-session-abort-order.test.ts`
- `packages/pi-coding-agent/src/core/agent-session-abort-order.test.ts:61` — `recordCallOrder(target: O, methods: Array<keyof O>)` — exported: **no** — Validates inputs and throws on failure. (missing: @param target, @param methods, @returns)

### `packages/pi-coding-agent/src/core/agent-session.ts`
- `packages/pi-coding-agent/src/core/agent-session.ts:103` — `parseSkillBlock(text: string)` — exported: **yes** — Implements logic starting with: { const match = text.match(/^<skill name="([^"]+)" location="([^"]+)">\n([\s\S]*?)\n<\/skill>(?:\n\n([\s\S]+))?$/); if (!match) return null; return… (missing: @param text, @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:381` — `AgentSession.get modelRegistry()` — exported: **yes** — Implements logic starting with: { return this._modelRegistry; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:386` — `AgentSession.get fallbackResolver()` — exported: **yes** — Implements logic starting with: { return this._fallbackResolver; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:785` — `AgentSession.subscribe(listener: AgentSessionEventListener)` — exported: **yes** — Implements logic starting with: { this._eventListeners.push(listener); // Return unsubscribe function for this specific listener return () => { const index = this._eventListeners.… (missing: @param listener, @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:834` — `AgentSession.get state()` — exported: **yes** — Implements logic starting with: { return this.agent.state; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:839` — `AgentSession.get model()` — exported: **yes** — Implements logic starting with: { return this.agent.state.model; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:844` — `AgentSession.get thinkingLevel()` — exported: **yes** — Implements logic starting with: { return this.agent.state.thinkingLevel; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:849` — `AgentSession.get isStreaming()` — exported: **yes** — Implements logic starting with: { return this.agent.state.isStreaming; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:854` — `AgentSession.get systemPrompt()` — exported: **yes** — Implements logic starting with: { return this.agent.state.systemPrompt; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:859` — `AgentSession.get retryAttempt()` — exported: **yes** — Implements logic starting with: { return this._retryHandler.retryAttempt; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:867` — `AgentSession.getActiveToolNames()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:874` — `AgentSession.getAllTools()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:888` — `AgentSession.setActiveToolsByName(toolNames: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param toolNames)
- `packages/pi-coding-agent/src/core/agent-session.ts:913` — `AgentSession.setVisibleSkillsByName(skillNames: string[] | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param skillNames)
- `packages/pi-coding-agent/src/core/agent-session.ts:922` — `AgentSession.getVisibleSkillNames()` — exported: **yes** — Implements logic starting with: { return this._visibleSkillNames ? [...this._visibleSkillNames] : undefined; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:927` — `AgentSession.get isCompacting()` — exported: **yes** — Implements logic starting with: { return this._compactionOrchestrator.isCompacting; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:935` — `AgentSession.setEditMode(mode: "standard" | "hashline")` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param mode)
- `packages/pi-coding-agent/src/core/agent-session.ts:963` — `AgentSession.get editMode()` — exported: **yes** — Implements logic starting with: { return this.settingsManager.getEditMode(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:968` — `AgentSession.get messages()` — exported: **yes** — Implements logic starting with: { return this.agent.state.messages; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:973` — `AgentSession.get steeringMode()` — exported: **yes** — Implements logic starting with: { return this.agent.getSteeringMode(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:978` — `AgentSession.get followUpMode()` — exported: **yes** — Implements logic starting with: { return this.agent.getFollowUpMode(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:983` — `AgentSession.get sessionFile()` — exported: **yes** — Implements logic starting with: { return this.sessionManager.getSessionFile(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:988` — `AgentSession.get sessionId()` — exported: **yes** — Implements logic starting with: { return this.sessionManager.getSessionId(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:993` — `AgentSession.get sessionName()` — exported: **yes** — Implements logic starting with: { return this.sessionManager.getSessionName(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:998` — `AgentSession.get scopedModels()` — exported: **yes** — Implements logic starting with: { return this._scopedModels; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1003` — `AgentSession.setScopedModels(scopedModels: Array<{ model: Model<any>; thinkingLevel?: ThinkingLevel }>)` — exported: **yes** — Implements logic starting with: { this._scopedModels = scopedModels; } (missing: @param scopedModels)
- `packages/pi-coding-agent/src/core/agent-session.ts:1008` — `AgentSession.get promptTemplates()` — exported: **yes** — Implements logic starting with: { return this._resourceLoader.getPrompts().prompts; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1129` — `AgentSession.prompt(text: string, options?: PromptOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param text, @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1405` — `AgentSession.steer(text: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param text)
- `packages/pi-coding-agent/src/core/agent-session.ts:1425` — `AgentSession.followUp(text: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param text)
- `packages/pi-coding-agent/src/core/agent-session.ts:1505` — `AgentSession.sendCustomMessage(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details">, options?: { triggerTurn?: boolean; deliverAs?: "steer" | "followUp" | "nextTurn" })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1547` — `AgentSession.sendUserMessage(content: string | (TextContent | ImageContent)[], options?: { deliverAs?: "steer" | "followUp" })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1613` — `AgentSession.get pendingMessageCount()` — exported: **yes** — Implements logic starting with: { return this._steeringMessages.length + this._followUpMessages.length; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1618` — `AgentSession.getSteeringMessages()` — exported: **yes** — Implements logic starting with: { return this._steeringMessages; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1623` — `AgentSession.getFollowUpMessages()` — exported: **yes** — Implements logic starting with: { return this._followUpMessages; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1634` — `AgentSession.abort(options?: { origin?: AgentAbortOrigin })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1711` — `AgentSession.newSession(options?: { parentSession?: string; setup?: (sessionManager: SessionManager) => Promise<void>; /** Explicit workspace root for the new session/tool runtime. */ workspaceRoot?: string; /** See ExtensionCommandContext.newSession for docs (#3731). */ abortSignal?: AbortSignal; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1862` — `AgentSession.setModel(model: Model<any>, options?: { persist?: boolean })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param model, @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1877` — `AgentSession.cycleModel(direction: "forward" | "backward" = "forward", options?: { persist?: boolean })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:1937` — `AgentSession.setThinkingLevel(level: ThinkingLevel)` — exported: **yes** — Implements logic starting with: { const availableLevels = this.getAvailableThinkingLevels(); const effectiveLevel = availableLevels.includes(level) ? level : this._clampThinkingLe… (missing: @param level)
- `packages/pi-coding-agent/src/core/agent-session.ts:1975` — `AgentSession.getAvailableThinkingLevels()` — exported: **yes** — Implements logic starting with: { if (!this.supportsThinking()) return ["off"]; return this.supportsXhighThinking() ? THINKING_LEVELS_WITH_XHIGH : THINKING_LEVELS; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1983` — `AgentSession.supportsXhighThinking()` — exported: **yes** — Implements logic starting with: { return this.model ? supportsXhigh(this.model) : false; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:1990` — `AgentSession.supportsThinking()` — exported: **yes** — Implements logic starting with: { return !!this.model?.reasoning; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2030` — `AgentSession.setSteeringMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Implements logic starting with: { this.agent.setSteeringMode(mode); this.settingsManager.setSteeringMode(mode); this._emitSessionStateChanged("set_steering_mode"); } (missing: @param mode)
- `packages/pi-coding-agent/src/core/agent-session.ts:2040` — `AgentSession.setFollowUpMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Implements logic starting with: { this.agent.setFollowUpMode(mode); this.settingsManager.setFollowUpMode(mode); this._emitSessionStateChanged("set_follow_up_mode"); } (missing: @param mode)
- `packages/pi-coding-agent/src/core/agent-session.ts:2055` — `AgentSession.compact(customInstructions?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2070` — `AgentSession.setAutoCompactionEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this._compactionOrchestrator.setAutoCompactionEnabled(enabled); this._emitSessionStateChanged("set_auto_compaction"); } (missing: @param enabled)
- `packages/pi-coding-agent/src/core/agent-session.ts:2076` — `AgentSession.get autoCompactionEnabled()` — exported: **yes** — Implements logic starting with: { return this._compactionOrchestrator.autoCompactionEnabled; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2464` — `AgentSession.get isRetrying()` — exported: **yes** — Implements logic starting with: { return this._retryHandler.isRetrying; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2469` — `AgentSession.get autoRetryEnabled()` — exported: **yes** — Implements logic starting with: { return this._retryHandler.autoRetryEnabled; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2474` — `AgentSession.setAutoRetryEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this._retryHandler.setAutoRetryEnabled(enabled); this._emitSessionStateChanged("set_auto_retry"); } (missing: @param enabled)
- `packages/pi-coding-agent/src/core/agent-session.ts:2491` — `AgentSession.executeBash(command: string, onChunk?: (chunk: string) => void, options?: { excludeFromContext?: boolean; operations?: BashOperations; loginShell?: boolean })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2525` — `AgentSession.recordBashResult(command: string, result: BashResult, options?: { excludeFromContext?: boolean })` — exported: **yes** — Implements logic starting with: { const bashMessage: BashExecutionMessage = { role: "bashExecution", command, output: result.output, exitCode: result.exitCode, cancelled: result.c… (missing: @param command, @param result, @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:2559` — `AgentSession.get isBashRunning()` — exported: **yes** — Implements logic starting with: { return this._bashAbortController !== undefined; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2564` — `AgentSession.get hasPendingBashMessages()` — exported: **yes** — Implements logic starting with: { return this._pendingBashMessages.length > 0; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2596` — `AgentSession.switchSession(sessionPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param sessionPath)
- `packages/pi-coding-agent/src/core/agent-session.ts:2680` — `AgentSession.setSessionName(name: string)` — exported: **yes** — Implements logic starting with: { this.sessionManager.appendSessionInfo(name); this._emitSessionStateChanged("set_session_name"); } (missing: @param name)
- `packages/pi-coding-agent/src/core/agent-session.ts:2765` — `AgentSession.navigateTree(targetId: string, options: { summarize?: boolean; customInstructions?: string; replaceInstructions?: boolean; label?: string } = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param options)
- `packages/pi-coding-agent/src/core/agent-session.ts:2950` — `AgentSession.getUserMessagesForForking()` — exported: **yes** — Implements logic starting with: { const entries = this.sessionManager.getEntries(); const result: Array<{ entryId: string; text: string }> = []; for (const entry of entries) { if… (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:2981` — `AgentSession.getSessionStats()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:3029` — `AgentSession.getLastTurnCost()` — exported: **yes** — Implements logic starting with: { return this._lastTurnCost; } (missing: @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:3140` — `AgentSession.hasExtensionHandlers(eventType: string)` — exported: **yes** — Implements logic starting with: { return this._extensionRunner?.hasHandlers(eventType) ?? false; } (missing: @param eventType, @returns)
- `packages/pi-coding-agent/src/core/agent-session.ts:3147` — `AgentSession.get extensionRunner()` — exported: **yes** — Implements logic starting with: { return this._extensionRunner; } (missing: @returns)

### `packages/pi-coding-agent/src/core/artifact-manager.ts`
- `packages/pi-coding-agent/src/core/artifact-manager.ts:16` — `class ArtifactManager` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:34` — `ArtifactManager.get dir()` — exported: **yes** — Implements logic starting with: { return this.#dir; } (missing: @returns)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:67` — `ArtifactManager.allocateId()` — exported: **yes** — Implements logic starting with: { return this.#nextId++; } (missing: @returns)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:75` — `ArtifactManager.allocatePath(toolType: string)` — exported: **yes** — Implements logic starting with: { this.#ensureDir(); const id = String(this.allocateId()); const filename = `${id}.${toolType}.log`; return { id, path: join(this.#dir, filename) }… (missing: @returns)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:98` — `ArtifactManager.exists(id: string)` — exported: **yes** — Implements logic starting with: { const files = this.listFiles(); return files.some((f) => f.startsWith(`${id}.`)); } (missing: @returns)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:107` — `ArtifactManager.listFiles()` — exported: **yes** — Implements logic starting with: { try { return readdirSync(this.#dir); } catch { return []; } } (missing: @returns)
- `packages/pi-coding-agent/src/core/artifact-manager.ts:120` — `ArtifactManager.getPath(id: string)` — exported: **yes** — Implements logic starting with: { const files = this.listFiles(); const match = files.find((f) => f.startsWith(`${id}.`)); return match ? join(this.#dir, match) : null; } (missing: @returns)

### `packages/pi-coding-agent/src/core/auth-storage.ts`
- `packages/pi-coding-agent/src/core/auth-storage.ts:55` — `isGoogleOAuthToken(key: string)` — exported: **yes** — Implements logic starting with: { return key.startsWith("ya29."); } (missing: @param key, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:64` — `validateNotGoogleOAuthToken(provider: string, key: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param provider, @param key)
- `packages/pi-coding-agent/src/core/auth-storage.ts:209` — `getBackoffDuration(errorType: UsageLimitErrorType)` — exported: **no** — Implements logic starting with: { switch (errorType) { case "rate_limit": return BACKOFF_RATE_LIMIT_MS; case "quota_exhausted": return BACKOFF_QUOTA_EXHAUSTED_MS; case "server_err… (missing: @param errorType, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:226` — `hashString(str: string)` — exported: **no** — Implements logic starting with: { let hash = 0; for (let i = 0; i < str.length; i++) { const char = str.charCodeAt(i); hash = ((hash << 5) - hash + char) | 0; } return Math.abs(ha… (missing: @param str, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:239` — `class AuthStorage` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:288` — `AuthStorage.setRuntimeApiKey(provider: string, apiKey: string)` — exported: **yes** — Implements logic starting with: { this.runtimeOverrides.set(provider, apiKey); } (missing: @param provider, @param apiKey)
- `packages/pi-coding-agent/src/core/auth-storage.ts:295` — `AuthStorage.removeRuntimeApiKey(provider: string)` — exported: **yes** — Implements logic starting with: { this.runtimeOverrides.delete(provider); } (missing: @param provider)
- `packages/pi-coding-agent/src/core/auth-storage.ts:303` — `AuthStorage.setFallbackResolver(resolver: (provider: string) => string | undefined)` — exported: **yes** — Implements logic starting with: { this.fallbackResolver = resolver; } (missing: @param resolver)
- `packages/pi-coding-agent/src/core/auth-storage.ts:311` — `AuthStorage.onCredentialChange(listener: () => void)` — exported: **yes** — Implements logic starting with: { this.credentialChangeListeners.add(listener); return () => this.credentialChangeListeners.delete(listener); } (missing: @param listener, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:342` — `AuthStorage.getCredentialsForProvider(provider: string)` — exported: **yes** — Implements logic starting with: { const entry = this.data[provider]; if (!entry) return []; if (Array.isArray(entry)) return entry; return [entry]; } (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:391` — `AuthStorage.get(provider: string)` — exported: **yes** — Implements logic starting with: { const creds = this.getCredentialsForProvider(provider); return creds[0] ?? undefined; } (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:401` — `AuthStorage.set(provider: string, credential: AuthCredential)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param provider, @param credential)
- `packages/pi-coding-agent/src/core/auth-storage.ts:434` — `AuthStorage.remove(provider: string)` — exported: **yes** — Implements logic starting with: { delete this.data[provider]; this.providerRoundRobinIndex.delete(provider); this.credentialBackoff.delete(provider); this.providerBackoff.delete(p… (missing: @param provider)
- `packages/pi-coding-agent/src/core/auth-storage.ts:445` — `AuthStorage.list()` — exported: **yes** — Implements logic starting with: { return Object.keys(this.data); } (missing: @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:452` — `AuthStorage.has(provider: string)` — exported: **yes** — Implements logic starting with: { return provider in this.data; } (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:460` — `AuthStorage.hasAuth(provider: string)` — exported: **yes** — Implements logic starting with: { if (this.runtimeOverrides.has(provider)) return true; if (this.data[provider]) return true; if (getEnvApiKey(provider)) return true; if (this.fal… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:474` — `AuthStorage.hasLegacyOAuthCredential(provider: string)` — exported: **yes** — Implements logic starting with: { return this.getCredentialsForProvider(provider).some((c) => c.type === "oauth"); } (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:484` — `AuthStorage.removeLegacyOAuthCredential(provider: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:512` — `AuthStorage.getAll()` — exported: **yes** — Implements logic starting with: { const result: Record<string, AuthCredential> = {}; for (const [provider, entry] of Object.entries(this.data)) { result[provider] = Array.isArray(… (missing: @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:529` — `AuthStorage.login(providerId: OAuthProviderId, callbacks: OAuthLoginCallbacks)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param providerId, @param callbacks)
- `packages/pi-coding-agent/src/core/auth-storage.ts:542` — `AuthStorage.logout(provider: string)` — exported: **yes** — Implements logic starting with: { this.remove(provider); } (missing: @param provider)
- `packages/pi-coding-agent/src/core/auth-storage.ts:551` — `AuthStorage.areAllCredentialsBackedOff(provider: string)` — exported: **yes** — Implements logic starting with: { const credentials = this.getCredentialsForProvider(provider); if (credentials.length === 0) return false; for (let i = 0; i < credentials.length;… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:564` — `AuthStorage.markProviderExhausted(provider: string, errorType: UsageLimitErrorType)` — exported: **yes** — Implements logic starting with: { const backoffMs = getBackoffDuration(errorType); this.providerBackoff.set(provider, Date.now() + backoffMs); } (missing: @param provider, @param errorType)
- `packages/pi-coding-agent/src/core/auth-storage.ts:572` — `AuthStorage.isProviderAvailable(provider: string)` — exported: **yes** — Implements logic starting with: { const expiresAt = this.providerBackoff.get(provider); if (expiresAt === undefined) return true; if (Date.now() >= expiresAt) { this.providerBacko… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:586` — `AuthStorage.getProviderBackoffRemaining(provider: string)` — exported: **yes** — Implements logic starting with: { const expiresAt = this.providerBackoff.get(provider); if (expiresAt === undefined) return 0; const remaining = expiresAt - Date.now(); if (remain… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:606` — `AuthStorage.getEarliestBackoffExpiry(provider: string)` — exported: **yes** — Implements logic starting with: { const providerMap = this.credentialBackoff.get(provider); if (!providerMap || providerMap.size === 0) return undefined; const now = Date.now(); l… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:683` — `AuthStorage.markUsageLimitReached(provider: string, sessionId?: string, options?: { errorType?: UsageLimitErrorType })` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param provider, @param sessionId, @param options)
- `packages/pi-coding-agent/src/core/auth-storage.ts:855` — `AuthStorage.getApiKey(providerId: string, sessionId?: string, options?: { baseUrl?: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)
- `packages/pi-coding-agent/src/core/auth-storage.ts:922` — `AuthStorage.getOAuthProviders()` — exported: **yes** — Implements logic starting with: { return getOAuthProviders(); } (missing: @returns)

### `packages/pi-coding-agent/src/core/bash-executor.ts`
- `packages/pi-coding-agent/src/core/bash-executor.ts:221` — `executeBashWithOperations(command: string, cwd: string, operations: BashOperations, options?: BashExecutorOptions)` — exported: **yes** — Executes a subprocess command. (missing: @param command, @param cwd, @param operations, @param options, @returns)

### `packages/pi-coding-agent/src/core/blob-store.ts`
- `packages/pi-coding-agent/src/core/blob-store.ts:29` — `BlobStore.put(data: Buffer)` — exported: **yes** — Performs filesystem I/O. (missing: @param data, @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:50` — `BlobStore.get(hash: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param hash, @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:61` — `BlobStore.has(hash: string)` — exported: **yes** — Implements logic starting with: { if (!SHA256_HEX_RE.test(hash)) return false; try { accessSync(join(this.dir, hash)); return true; } catch { return false; } } (missing: @param hash, @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:98` — `BlobStore.totalSize()` — exported: **yes** — Implements logic starting with: { try { const entries = readdirSync(this.dir); let total = 0; for (const entry of entries) { if (!SHA256_HEX_RE.test(entry)) continue; try { total… (missing: @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:118` — `isBlobRef(data: string)` — exported: **yes** — Implements logic starting with: { return data.startsWith(BLOB_PREFIX); } (missing: @param data, @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:123` — `parseBlobRef(data: string)` — exported: **yes** — Implements logic starting with: { if (!data.startsWith(BLOB_PREFIX)) return null; const hash = data.slice(BLOB_PREFIX.length); if (!SHA256_HEX_RE.test(hash)) return null; return h… (missing: @param data, @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:134` — `externalizeImageData(blobStore: BlobStore, base64Data: string)` — exported: **yes** — Implements logic starting with: { if (isBlobRef(base64Data)) return base64Data; const buffer = Buffer.from(base64Data, "base64"); const { ref } = blobStore.put(buffer); return ref… (missing: @param blobStore, @param base64Data, @returns)
- `packages/pi-coding-agent/src/core/blob-store.ts:146` — `resolveImageData(blobStore: BlobStore, data: string)` — exported: **yes** — Implements logic starting with: { const hash = parseBlobRef(data); if (!hash) return data; const buffer = blobStore.get(hash); if (!buffer) return data; // Missing blob — return r… (missing: @param blobStore, @param data, @returns)

### `packages/pi-coding-agent/src/core/compaction-orchestrator.ts`
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:56` — `CompactionOrchestrator.get isCompacting()` — exported: **yes** — Implements logic starting with: { return ( this._autoCompactionAbortController !== undefined || this._compactionAbortController !== undefined || this._branchSummaryAbortController… (missing: @returns)
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:75` — `CompactionOrchestrator.get branchSummaryAbortController()` — exported: **yes** — Implements logic starting with: { return this._branchSummaryAbortController; } (missing: @returns)
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:87` — `CompactionOrchestrator.compact(customInstructions?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:278` — `CompactionOrchestrator.setAutoCompactionEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this._deps.settingsManager.setCompactionEnabled(enabled); } (missing: @param enabled)
- `packages/pi-coding-agent/src/core/compaction-orchestrator.ts:283` — `CompactionOrchestrator.get autoCompactionEnabled()` — exported: **yes** — Implements logic starting with: { return this._deps.settingsManager.getCompactionEnabled(); } (missing: @returns)

### `packages/pi-coding-agent/src/core/compaction/branch-summarization.ts`
- `packages/pi-coding-agent/src/core/compaction/branch-summarization.ts:148` — `prepareBranchEntries(entries: SessionEntry[], tokenBudget: number = 0)` — exported: **yes** — Implements logic starting with: { const messages: AgentMessage[] = []; const fileOps = createFileOps(); let totalTokens = 0; // First pass: collect file ops from ALL entries (even… (missing: @returns)
- `packages/pi-coding-agent/src/core/compaction/branch-summarization.ts:246` — `generateBranchSummary(entries: SessionEntry[], options: GenerateBranchSummaryOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)

### `packages/pi-coding-agent/src/core/compaction/compaction.test.ts`
- `packages/pi-coding-agent/src/core/compaction/compaction.test.ts:20` — `makeUserMessage(tokenCount: number)` — exported: **no** — Implements logic starting with: { const text = "x".repeat(tokenCount * 4); return { role: "user", content: text } as unknown as AgentMessage; } (missing: @param tokenCount, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.test.ts:34` — `makeToolResultMessage(rawTokenCount: number)` — exported: **no** — Implements logic starting with: { const text = "y".repeat(rawTokenCount * 4); return { role: "toolResult", toolCallId: `call_${rawTokenCount}`, content: [{ type: "text", text }],… (missing: @param rawTokenCount, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.test.ts:48` — `makeBranchSummaryMessage(approxTokens: number)` — exported: **no** — Implements logic starting with: { const summary = "z".repeat(approxTokens * 4); return { role: "branchSummary", summary, fromId: "test", timestamp: 0, } as unknown as AgentMessage… (missing: @param approxTokens, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.test.ts:59` — `makeModel(contextWindow: number)` — exported: **no** — Implements logic starting with: { return { id: "test-model", name: "Test Model", api: "anthropic-messages", provider: "anthropic", baseUrl: "https://api.test", reasoning: false, i… (missing: @param contextWindow, @returns)

### `packages/pi-coding-agent/src/core/compaction/compaction.ts`
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:42` — `extractFileOperations(messages: AgentMessage[], entries: SessionEntry[], prevCompactionIndex: number)` — exported: **no** — Implements logic starting with: { const fileOps = createFileOps(); // Collect from previous compaction's details (if pi-generated) if (prevCompactionIndex >= 0) { const prevCompac… (missing: @param messages, @param entries, @param prevCompactionIndex, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:112` — `calculateContextTokens(usage: Usage)` — exported: **yes** — Implements logic starting with: { return usage.totalTokens || usage.input + usage.output + usage.cacheRead + usage.cacheWrite; } (missing: @param usage, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:120` — `getAssistantUsage(msg: AgentMessage)` — exported: **no** — Implements logic starting with: { if (msg.role === "assistant" && "usage" in msg) { const assistantMsg = msg as AssistantMessage; if (assistantMsg.stopReason !== "aborted" && assi… (missing: @param msg, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:133` — `getLastAssistantUsage(entries: SessionEntry[])` — exported: **yes** — Implements logic starting with: { for (let i = entries.length - 1; i >= 0; i--) { const entry = entries[i]; if (entry.type === "message") { const usage = getAssistantUsage(entry.m… (missing: @param entries, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:163` — `estimateContextTokens(messages: AgentMessage[])` — exported: **yes** — Implements logic starting with: { const usageInfo = getLastAssistantUsageInfo(messages); if (!usageInfo) { let estimated = 0; for (const message of messages) { estimated += estima… (missing: @param messages, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:200` — `shouldCompact(contextTokens: number, contextWindow: number, settings: CompactionSettings)` — exported: **yes** — Implements logic starting with: { if (!settings.enabled) return false; if ( settings.thresholdPercent !== undefined && settings.thresholdPercent > 0 && settings.thresholdPercent <… (missing: @param contextTokens, @param contextWindow, @param settings, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:220` — `estimateTokens(message: AgentMessage)` — exported: **yes** — Serializes a value to JSON. (missing: @param message, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:287` — `findValidCutPoints(entries: SessionEntry[], startIndex: number, endIndex: number)` — exported: **no** — Implements logic starting with: { const cutPoints: number[] = []; for (let i = startIndex; i < endIndex; i++) { const entry = entries[i]; switch (entry.type) { case "message": { c… (missing: @param entries, @param startIndex, @param endIndex, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:329` — `findTurnStartIndex(entries: SessionEntry[], entryIndex: number, startIndex: number)` — exported: **yes** — Implements logic starting with: { for (let i = entryIndex; i >= startIndex; i--) { const entry = entries[i]; // branch_summary and custom_message are user-role messages, can start… (missing: @param entries, @param entryIndex, @param startIndex, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:371` — `findCutPoint(entries: SessionEntry[], startIndex: number, endIndex: number, keepRecentTokens: number)` — exported: **yes** — Implements logic starting with: { const cutPoints = findValidCutPoints(entries, startIndex, endIndex); if (cutPoints.length === 0) { return { firstKeptEntryIndex: startIndex, turn… (missing: @param entries, @param startIndex, @param endIndex, @param keepRecentTokens, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:516` — `chunkMessages(messages: AgentMessage[], maxTokensPerChunk: number)` — exported: **yes** — Implements logic starting with: { const chunks: AgentMessage[][] = []; let currentChunk: AgentMessage[] = []; let currentTokens = 0; for (const msg of messages) { // Use POST-trun… (missing: @param messages, @param maxTokensPerChunk, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:567` — `isDegenerateSummary(summary: string | undefined)` — exported: **yes** — Implements logic starting with: { // undefined means "no summary was produced yet" (first chunk before any call) // — not degenerate. Empty string IS degenerate: the LLM returned… (missing: @param summary, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:594` — `generateSummary(currentMessages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string | undefined, signal?: AbortSignal, customInstructions?: string, previousSummary?: string, _completeFn?: CompleteFn)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param currentMessages, @param model, @param reserveTokens, @param apiKey, @param signal, @param customInstructions, @param previousSummary, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:711` — `class CompactionProducedNoSummaryError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:722` — `singlePassSummary(currentMessages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string | undefined, signal?: AbortSignal, customInstructions?: string, previousSummary?: string, complete: CompleteFn = completeSimple)` — exported: **no** — Validates inputs and throws on failure. (missing: @param currentMessages, @param model, @param reserveTokens, @param apiKey, @param signal, @param customInstructions, @param previousSummary, @param complete, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:887` — `compact(preparation: CompactionPreparation, model: Model<any>, apiKey: string | undefined, customInstructions?: string, signal?: AbortSignal)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param model, @param apiKey, @param signal, @returns)
- `packages/pi-coding-agent/src/core/compaction/compaction.ts:958` — `generateTurnPrefixSummary(messages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string | undefined, signal?: AbortSignal)` — exported: **no** — Validates inputs and throws on failure. (missing: @param messages, @param model, @param reserveTokens, @param apiKey, @param signal, @returns)

### `packages/pi-coding-agent/src/core/compaction/utils.ts`
- `packages/pi-coding-agent/src/core/compaction/utils.ts:42` — `extractFileOpsFromMessage(message: AgentMessage, fileOps: FileOperations)` — exported: **yes** — Implements logic starting with: { if (message.role !== "assistant") return; if (!("content" in message) || !Array.isArray(message.content)) return; for (const block of message.con… (missing: @param message, @param fileOps)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:75` — `computeFileLists(fileOps: FileOperations)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param fileOps, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:85` — `formatFileOperations(readFiles: string[], modifiedFiles: string[])` — exported: **yes** — Implements logic starting with: { const sections: string[] = []; if (readFiles.length > 0) { sections.push(`<read-files>\n${readFiles.join("\n")}\n</read-files>`); } if (modifiedF… (missing: @param readFiles, @param modifiedFiles, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:110` — `getMessageFromEntry(entry: SessionEntry, skipToolResults = false)` — exported: **yes** — Implements logic starting with: { switch (entry.type) { case "message": if (skipToolResults && entry.message.role === "toolResult") return undefined; return entry.message; case "c… (missing: @param entry, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:141` — `collectMessages(entries: SessionEntry[], startIndex: number, endIndex: number, skipToolResults = false)` — exported: **yes** — Implements logic starting with: { const result: AgentMessage[] = []; for (let i = startIndex; i < endIndex; i++) { const msg = getMessageFromEntry(entries[i], skipToolResults); if… (missing: @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:163` — `extractTextContent(content: Array<{ type: string; text?: string }>, separator = "\n")` — exported: **yes** — Transforms a collection into a derived result. (missing: @param content, @param separator, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:181` — `createSummarizationMessage(promptText: string)` — exported: **yes** — Implements logic starting with: { return [ { role: "user" as const, content: [{ type: "text" as const, text: promptText }], timestamp: Date.now(), }, ]; } (missing: @param promptText, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:205` — `truncateForSummary(text: string, maxChars: number)` — exported: **yes** — Implements logic starting with: { if (text.length <= maxChars) return text; const half = Math.floor(maxChars / 2); const head = text.slice(0, half); const tail = text.slice(text.l… (missing: @param text, @param maxChars, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:225` — `serializeConversation(messages: Message[])` — exported: **yes** — Serializes a value to JSON. (missing: @param messages, @returns)
- `packages/pi-coding-agent/src/core/compaction/utils.ts:300` — `estimateSerializedTokens(message: AgentMessage)` — exported: **yes** — Serializes a value to JSON. (missing: @param message, @returns)

### `packages/pi-coding-agent/src/core/contextual-tips.ts`
- `packages/pi-coding-agent/src/core/contextual-tips.ts:99` — `firstToken(input: string)` — exported: **no** — Implements logic starting with: { const trimmed = input.trimStart(); const spaceIdx = trimmed.search(/\s/); const token = spaceIdx === -1 ? trimmed : trimmed.slice(0, spaceIdx); r… (missing: @param input, @returns)
- `packages/pi-coding-agent/src/core/contextual-tips.ts:109` — `looksLikeShellCommand(input: string)` — exported: **no** — Implements logic starting with: { const trimmed = input.trimStart(); // Already prefixed — user knows what they're doing if (trimmed.startsWith("!") || trimmed.startsWith("/")) re… (missing: @param input, @returns)
- `packages/pi-coding-agent/src/core/contextual-tips.ts:207` — `ContextualTips.evaluate(ctx: Omit<TipContext, "bashIncludedCount">)` — exported: **yes** — Implements logic starting with: { const fullCtx: TipContext = { ...ctx, bashIncludedCount: this._bashIncludedCount, }; for (const tip of TIPS) { const shown = this.showCounts.get(… (missing: @param ctx, @returns)

### `packages/pi-coding-agent/src/core/db-snapshot.ts`
- `packages/pi-coding-agent/src/core/db-snapshot.ts:40` — `atomicWriteDbSnapshotSync(dbPath: string, snapshot: DbSnapshot)` — exported: **yes** — Performs filesystem I/O. (missing: @param dbPath, @param snapshot)

### `packages/pi-coding-agent/src/core/exec.ts`
- `packages/pi-coding-agent/src/core/exec.ts:33` — `execCommand(command: string, args: string[], cwd: string, options?: ExecOptions)` — exported: **yes** — Executes a subprocess command. (missing: @param command, @param args, @param cwd, @param options, @returns)

### `packages/pi-coding-agent/src/core/export-html/ansi-to-html.ts`
- `packages/pi-coding-agent/src/core/export-html/ansi-to-html.ts:37` — `color256ToHex(index: number)` — exported: **no** — Implements logic starting with: { // Standard colors (0-15) if (index < 16) { return ANSI_COLORS[index]; } // Color cube (16-231): 6x6x6 = 216 colors if (index < 232) { const cube… (missing: @param index, @returns)
- `packages/pi-coding-agent/src/core/export-html/ansi-to-html.ts:63` — `escapeHtml(text: string)` — exported: **no** — Implements logic starting with: { return text .replace(/&/g, "&amp;") .replace(/</g, "&lt;") .replace(/>/g, "&gt;") .replace(/"/g, "&quot;") .replace(/'/g, "&#039;"); } (missing: @param text, @returns)
- `packages/pi-coding-agent/src/core/export-html/ansi-to-html.ts:110` — `applySgrCode(params: number[], style: TextStyle)` — exported: **no** — Implements logic starting with: { let i = 0; while (i < params.length) { const code = params[i]; if (code === 0) { // Reset all style.fg = null; style.bg = null; style.bold = fals… (missing: @param params, @param style)
- `packages/pi-coding-agent/src/core/export-html/ansi-to-html.ts:198` — `ansiToHtml(text: string)` — exported: **no** — Executes a subprocess command. (missing: @param text, @returns)
- `packages/pi-coding-agent/src/core/export-html/ansi-to-html.ts:256` — `ansiLinesToHtml(lines: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param lines, @returns)

### `packages/pi-coding-agent/src/core/export-html/index.ts`
- `packages/pi-coding-agent/src/core/export-html/index.ts:41` — `parseColor(color: string)` — exported: **no** — Implements logic starting with: { const hexMatch = color.match(/^#([0-9a-fA-F]{2})([0-9a-fA-F]{2})([0-9a-fA-F]{2})$/); if (hexMatch) { return { r: Number.parseInt(hexMatch[1], 16)… (missing: @param color, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:62` — `getLuminance(r: number, g: number, b: number)` — exported: **no** — Implements logic starting with: { const toLinear = (c: number) => { const s = c / 255; return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4; }; return 0.2126 * toLinear(… (missing: @param r, @param g, @param b, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:71` — `adjustBrightness(color: string, factor: number)` — exported: **no** — Implements logic starting with: { const parsed = parseColor(color); if (!parsed) return color; const adjust = (c: number) => Math.min(255, Math.max(0, Math.round(c * factor))); re… (missing: @param color, @param factor, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:79` — `deriveExportColors(baseColor: string)` — exported: **no** — Implements logic starting with: { const parsed = parseColor(baseColor); if (!parsed) { return { pageBg: "rgb(24, 24, 30)", cardBg: "rgb(30, 30, 36)", infoBg: "rgb(60, 55, 40)", };… (missing: @param baseColor, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:109` — `generateThemeVars(themeName?: string)` — exported: **no** — Implements logic starting with: { const colors = getResolvedThemeColors(themeName); const lines: string[] = []; for (const [key, value] of Object.entries(colors)) { lines.push(`--… (missing: @param themeName, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:141` — `generateHtml(sessionData: SessionData, themeName?: string)` — exported: **no** — Performs filesystem I/O. (missing: @param sessionData, @param themeName, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:180` — `preRenderCustomTools(entries: SessionEntry[], toolRenderer: ToolHtmlRenderer)` — exported: **no** — Implements logic starting with: { const renderedTools: Record<string, RenderedToolHtml> = {}; for (const entry of entries) { if (entry.type !== "message") continue; const msg = en… (missing: @param entries, @param toolRenderer, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:227` — `exportSessionToHtml(sm: SessionManager, state?: AgentState, options?: ExportOptions | string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param sm, @param state, @param options, @returns)
- `packages/pi-coding-agent/src/core/export-html/index.ts:279` — `exportFromFile(inputPath: string, options?: ExportOptions | string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param inputPath, @param options, @returns)

### `packages/pi-coding-agent/src/core/export-html/template.js`
- `packages/pi-coding-agent/src/core/export-html/template.js:73` — `buildTree()` — exported: **no** — Builds an in-memory lookup structure. (missing: @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:116` — `buildActivePathIds(targetId)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param targetId, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:133` — `getPath(targetId)` — exported: **no** — Implements logic starting with: { const path = []; let current = byId.get(targetId); while (current) { path.unshift(current); // Stop if no parent or self-referencing (root) if (!… (missing: @param targetId, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:155` — `findNewestLeaf(nodeId)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param nodeId, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:183` — `flattenTree(roots, activePathIds)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param roots, @param activePathIds, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:258` — `buildTreePrefix(flatNode)` — exported: **no** — Implements logic starting with: { const { indent, showConnector, isLast, gutters, isVirtualRootChild, multipleRoots } = flatNode; const displayIndent = multipleRoots ? Math.max(0,… (missing: @param flatNode, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:352` — `filterNodes(flatNodes, currentLeafId)` — exported: **no** — Transforms a collection into a derived result. (missing: @param flatNodes, @param currentLeafId, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:416` — `recalculateVisualStructure(filteredNodes, allFlatNodes)` — exported: **no** — Transforms a collection into a derived result. (missing: @param filteredNodes, @param allFlatNodes, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:600` — `truncate(s, maxLen = 100)` — exported: **no** — Implements logic starting with: { if (s.length <= maxLen) return s; return s.slice(0, maxLen) + '...'; } (missing: @param s, @param maxLen, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:608` — `getTreeNodeDisplayHtml(entry, label)` — exported: **no** — Implements logic starting with: { const normalize = s => s.replace(/[\n\t]/g, ' ').trim(); const labelHtml = label ? `<span class="tree-label">[${escapeHtml(label)}]</span> ` : ''… (missing: @param entry, @param label, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:776` — `str(value)` — exported: **no** — Implements logic starting with: { if (typeof value === 'string') return value; if (value == null) return ''; return null; } (missing: @param value, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:1038` — `buildShareUrl(entryId)` — exported: **no** — Implements logic starting with: { // Check for injected base URL (used when loaded in iframe via srcdoc) const baseUrlMeta = document.querySelector('meta[name="pi-share-base-url"]… (missing: @param entryId, @returns)
- `packages/pi-coding-agent/src/core/export-html/template.js:1066` — `copyToClipboard(text, button)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param text, @param button)
- `packages/pi-coding-agent/src/core/export-html/template.js:1107` — `renderCopyLinkButton(entryId)` — exported: **no** — Implements logic starting with: { return `<button class="copy-link-btn" data-entry-id="${entryId}" title="Copy link to this message"> <svg width="14" height="14" viewBox="0 0 24 2… (missing: @param entryId, @returns)

### `packages/pi-coding-agent/src/core/export-html/tool-renderer.ts`
- `packages/pi-coding-agent/src/core/export-html/tool-renderer.ts:40` — `createToolHtmlRenderer(deps: ToolHtmlRendererDeps)` — exported: **yes** — Implements logic starting with: { const { getToolDefinition, theme, width = 100 } = deps; return { renderCall(toolName: string, args: unknown): string | undefined { try { const to… (missing: @param deps, @returns)

### `packages/pi-coding-agent/src/core/extensions/extension-discovery.ts`
- `packages/pi-coding-agent/src/core/extensions/extension-discovery.ts:18` — `resolveExtensionEntries(dir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param dir, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-discovery.ts:61` — `discoverExtensionEntryPaths(extensionsDir: string)` — exported: **yes** — Implements logic starting with: { if (!existsSync(extensionsDir)) { return [] } const discovered: string[] = [] for (const entry of readdirSync(extensionsDir, { withFileTypes: tru… (missing: @param extensionsDir, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-discovery.ts:88` — `mergeExtensionEntryPaths(bundledPaths: string[], installedExtDir: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param bundledPaths, @param installedExtDir, @returns)

### `packages/pi-coding-agent/src/core/extensions/extension-manifest.ts`
- `packages/pi-coding-agent/src/core/extensions/extension-manifest.ts:44` — `readManifest(extensionDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param extensionDir, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-manifest.ts:59` — `readManifestFromEntryPath(entryPath: string)` — exported: **yes** — Implements logic starting with: { const dir = dirname(entryPath); return readManifest(dir); } (missing: @param entryPath, @returns)

### `packages/pi-coding-agent/src/core/extensions/extension-registry.ts`
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:108` — `isExtensionEnabled(registry: ExtensionRegistry, id: string)` — exported: **yes** — Implements logic starting with: { const entry = registry.entries[id]; if (!entry) return true; return entry.enabled; } (missing: @param registry, @param id, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:131` — `disableExtension(registry: ExtensionRegistry, id: string, manifest: ExtensionManifest | null, reason?: string)` — exported: **yes** — Implements logic starting with: { if (manifest?.tier === "core") { return `Cannot disable "${id}" — it is a core extension.`; } const entry = registry.entries[id]; if (entry) { en… (missing: @param registry, @param id, @param manifest, @param reason, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:160` — `readManifest(extensionDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param extensionDir, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:175` — `readManifestFromEntryPath(entryPath: string)` — exported: **yes** — Implements logic starting with: { const dir = dirname(entryPath); return readManifest(dir); } (missing: @param entryPath, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:183` — `discoverAllManifests(extensionsDir: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param extensionsDir, @returns)
- `packages/pi-coding-agent/src/core/extensions/extension-registry.ts:201` — `ensureRegistryEntries(extensionsDir: string)` — exported: **yes** — Implements logic starting with: { const manifests = discoverAllManifests(extensionsDir); if (manifests.size === 0) return; const registry = loadRegistry(); let changed = false; fo… (missing: @param extensionsDir)

### `packages/pi-coding-agent/src/core/extensions/extension-sort.ts`
- `packages/pi-coding-agent/src/core/extensions/extension-sort.ts:25` — `sortExtensionPaths(paths: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param paths, @returns)

### `packages/pi-coding-agent/src/core/extensions/loader.ts`
- `packages/pi-coding-agent/src/core/extensions/loader.ts:119` — `resolveSubpathExports(packageName: string)` — exported: **no** — Performs filesystem I/O. (missing: @param packageName, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:214` — `resolveWildcardExports(packageName: string, packageDir: string, subpathPattern: string, target: unknown, aliases: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param packageName, @param packageDir, @param subpathPattern, @param target, @param aliases)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:249` — `scanDirForExports(packageName: string, dir: string, relativePath: string, aliases: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param packageName, @param dir, @param relativePath, @param aliases)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:402` — `createExtensionRuntime()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:450` — `createExtensionAPI(extension: Extension, runtime: ExtensionRuntime, cwd: string, eventBus: EventBus)` — exported: **no** — Executes a subprocess command. (missing: @param extension, @param runtime, @param cwd, @param eventBus, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:650` — `containsTypeScriptSyntax(source: string)` — exported: **yes** — Implements logic starting with: { return TS_SYNTAX_PATTERNS.some((pattern) => pattern.test(source)); } (missing: @param source, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:784` — `isNonExtensionLibrary(resolvedPath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param resolvedPath, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:814` — `createExtension(extensionPath: string, resolvedPath: string)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param extensionPath, @param resolvedPath, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:904` — `loadExtensionFromFactory(factory: ExtensionFactory, cwd: string, eventBus: EventBus, runtime: ExtensionRuntime, extensionPath = "<inline>")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param factory, @param cwd, @param eventBus, @param runtime, @param extensionPath, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:924` — `loadExtensions(paths: string[], cwd: string, eventBus?: EventBus)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param paths, @param cwd, @param eventBus, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:981` — `resolveExtensionEntries(dir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param dir, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:1027` — `discoverExtensionsInDir(dir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param dir, @returns)
- `packages/pi-coding-agent/src/core/extensions/loader.ts:1069` — `discoverAndLoadExtensions(configuredPaths: string[], cwd: string, agentDir: string = getAgentDir(), eventBus?: EventBus)` — exported: **yes** — Performs filesystem I/O. (missing: @param configuredPaths, @param cwd, @param agentDir, @param eventBus, @returns)

### `packages/pi-coding-agent/src/core/extensions/runner.ts`
- `packages/pi-coding-agent/src/core/extensions/runner.ts:376` — `ExtensionRunner.installHookBridge(path: string, handlers: Map<string, Array<(event: unknown, ctx: unknown) => Promise<unknown>>>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param path, @param handlers, @returns)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:482` — `ExtensionRunner.getAllRegisteredTools()` — exported: **yes** — Builds an in-memory lookup structure. (missing: @returns)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:495` — `ExtensionRunner.getToolDefinition(toolName: string)` — exported: **yes** — Implements logic starting with: { for (const ext of this.extensions) { const tool = ext.tools.get(toolName); if (tool) { return tool.definition; } } return undefined; } (missing: @param toolName, @returns)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:701` — `ExtensionRunner.createContext()` — exported: **yes** — Implements logic starting with: { const getModel = this.getModel; return { ui: this.uiContext, hasUI: this.hasUI(), cwd: this.currentCwd(), sessionManager: this.sessionManager, mo… (missing: @returns)
- `packages/pi-coding-agent/src/core/extensions/runner.ts:1007` — `ExtensionRunner.emitInput(text: string, images: ImageContent[] | undefined, source: InputSource)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param text, @param images, @param source, @returns)

### `packages/pi-coding-agent/src/core/extensions/types.ts`
- `packages/pi-coding-agent/src/core/extensions/types.ts:1105` — `isToolResultEventType(toolName: "bash", event: ToolResultEvent)` — exported: **yes** — Defines an API surface. (missing: @param toolName, @param event, @returns)
- `packages/pi-coding-agent/src/core/extensions/types.ts:1140` — `isToolCallEventType(toolName: "bash", event: ToolCallEvent)` — exported: **yes** — Defines an API surface. (missing: @param toolName, @param event, @returns)

### `packages/pi-coding-agent/src/core/extensions/wrapper.ts`
- `packages/pi-coding-agent/src/core/extensions/wrapper.ts:13` — `wrapRegisteredTool(registeredTool: RegisteredTool, runner: ExtensionRunner)` — exported: **yes** — Implements logic starting with: { const { definition } = registeredTool; return { name: definition.name, label: definition.label, description: definition.description, parameters:… (missing: @param registeredTool, @param runner, @returns)
- `packages/pi-coding-agent/src/core/extensions/wrapper.ts:29` — `wrapRegisteredTools(registeredTools: RegisteredTool[], runner: ExtensionRunner)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param registeredTools, @param runner, @returns)
- `packages/pi-coding-agent/src/core/extensions/wrapper.ts:38` — `wrapToolWithExtensions(tool: AgentTool<any, T>, runner: ExtensionRunner)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param tool, @param runner, @returns)
- `packages/pi-coding-agent/src/core/extensions/wrapper.ts:125` — `wrapToolsWithExtensions(tools: AgentTool<any, T>[], runner: ExtensionRunner)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tools, @param runner, @returns)

### `packages/pi-coding-agent/src/core/fallback-resolver.ts`
- `packages/pi-coding-agent/src/core/fallback-resolver.ts:37` — `FallbackResolver.findFallback(currentModel: Model<Api>, errorType: UsageLimitErrorType)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param currentModel, @param errorType)
- `packages/pi-coding-agent/src/core/fallback-resolver.ts:73` — `FallbackResolver.checkForRestoration(currentModel: Model<Api>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param currentModel)
- `packages/pi-coding-agent/src/core/fallback-resolver.ts:103` — `FallbackResolver.getBestAvailable(chainName: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param chainName, @returns)
- `packages/pi-coding-agent/src/core/fallback-resolver.ts:116` — `FallbackResolver.findChainsForModel(provider: string, modelId: string)` — exported: **yes** — Implements logic starting with: { const { chains } = this.settingsManager.getFallbackSettings(); const result: string[] = []; for (const [chainName, entries] of Object.entries(cha… (missing: @param provider, @param modelId, @returns)

### `packages/pi-coding-agent/src/core/footer-data-provider.ts`
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:8` — `findGitHeadPath()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:40` — `class FooterDataProvider` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:52` — `FooterDataProvider.getGitBranch()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:70` — `FooterDataProvider.getExtensionStatuses()` — exported: **yes** — Implements logic starting with: { return this.extensionStatuses; } (missing: @returns)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:75` — `FooterDataProvider.onBranchChange(callback: () => void)` — exported: **yes** — Implements logic starting with: { this.branchChangeCallbacks.add(callback); return () => this.branchChangeCallbacks.delete(callback); } (missing: @param callback, @returns)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:81` — `FooterDataProvider.setExtensionStatus(key: string, text: string | undefined)` — exported: **yes** — Implements logic starting with: { if (text === undefined) { this.extensionStatuses.delete(key); } else { this.extensionStatuses.set(key, text); } } (missing: @param key, @param text)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:95` — `FooterDataProvider.getAvailableProviderCount()` — exported: **yes** — Implements logic starting with: { return this.availableProviderCount; } (missing: @returns)
- `packages/pi-coding-agent/src/core/footer-data-provider.ts:100` — `FooterDataProvider.setAvailableProviderCount(count: number)` — exported: **yes** — Implements logic starting with: { this.availableProviderCount = count; } (missing: @param count)

### `packages/pi-coding-agent/src/core/fs-utils.ts`
- `packages/pi-coding-agent/src/core/fs-utils.ts:8` — `atomicWriteFileSync(filePath: string, content: string | Buffer, encoding?: BufferEncoding)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @param content, @param encoding)

### `packages/pi-coding-agent/src/core/image-overflow-recovery.ts`
- `packages/pi-coding-agent/src/core/image-overflow-recovery.ts:40` — `isImageDimensionError(errorMessage: string | undefined | null)` — exported: **yes** — Implements logic starting with: { if (!errorMessage) return false; return IMAGE_DIMENSION_ERROR_RE.test(errorMessage); } (missing: @param errorMessage, @returns)
- `packages/pi-coding-agent/src/core/image-overflow-recovery.ts:64` — `downsizeConversationImages(messages: Message[])` — exported: **yes** — Implements logic starting with: { // First pass: collect all image locations (message index + content index) const imageLocations: Array<{ msgIdx: number; contentIdx: number }> =… (missing: @param messages, @returns)

### `packages/pi-coding-agent/src/core/keybindings.ts`
- `packages/pi-coding-agent/src/core/keybindings.ts:113` — `class KeybindingsManager` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/keybindings.ts:126` — `KeybindingsManager.create(agentDir: string = getAgentDir())` — exported: **yes** — Implements logic starting with: { const configPath = join(agentDir, "keybindings.json"); const config = KeybindingsManager.loadFromFile(configPath); const manager = new Keybinding… (missing: @param agentDir, @returns)
- `packages/pi-coding-agent/src/core/keybindings.ts:147` — `KeybindingsManager.inMemory(config: KeybindingsConfig = {})` — exported: **yes** — Implements logic starting with: { return new KeybindingsManager(config); } (missing: @param config, @returns)
- `packages/pi-coding-agent/src/core/keybindings.ts:180` — `KeybindingsManager.matches(data: string, action: AppAction)` — exported: **yes** — Implements logic starting with: { const keys = this.appActionToKeys.get(action); if (!keys) return false; for (const key of keys) { if (matchesKey(data, key)) return true; } retur… (missing: @param data, @param action, @returns)
- `packages/pi-coding-agent/src/core/keybindings.ts:192` — `KeybindingsManager.getKeys(action: AppAction)` — exported: **yes** — Implements logic starting with: { return this.appActionToKeys.get(action) ?? []; } (missing: @param action, @returns)
- `packages/pi-coding-agent/src/core/keybindings.ts:199` — `KeybindingsManager.getEffectiveConfig()` — exported: **yes** — Implements logic starting with: { const result = { ...DEFAULT_KEYBINDINGS }; for (const [action, keys] of Object.entries(this.config)) { if (keys !== undefined) { (result as Keybi… (missing: @returns)

### `packages/pi-coding-agent/src/core/local-model-check.ts`
- `packages/pi-coding-agent/src/core/local-model-check.ts:13` — `isLocalModel(model: { baseUrl: string })` — exported: **yes** — Implements logic starting with: { const url = model.baseUrl; if (!url) return false; // Unix socket paths if (url.startsWith("unix://") || url.startsWith("unix:")) return true; tr… (missing: @param model, @returns)

### `packages/pi-coding-agent/src/core/lock-utils.ts`
- `packages/pi-coding-agent/src/core/lock-utils.ts:23` — `acquireLockSyncWithRetry(lockPath: string, maxAttempts: number = DEFAULT_MAX_ATTEMPTS, delayMs: number = DEFAULT_DELAY_MS)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param lockPath, @param maxAttempts, @param delayMs)
- `packages/pi-coding-agent/src/core/lock-utils.ts:58` — `tryAcquireLockSync(lockPath: string, maxAttempts: number = DEFAULT_MAX_ATTEMPTS, delayMs: number = DEFAULT_DELAY_MS)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param lockPath, @param maxAttempts, @param delayMs, @returns)
- `packages/pi-coding-agent/src/core/lock-utils.ts:98` — `acquireLockAsync(lockPath: string, options?: AsyncLockOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param lockPath, @param options)

### `packages/pi-coding-agent/src/core/lsp/client.ts`
- `packages/pi-coding-agent/src/core/lsp/client.ts:49` — `setIdleTimeout(ms: number | null | undefined)` — exported: **yes** — Implements logic starting with: { idleTimeoutMs = ms ?? null; if (idleTimeoutMs && idleTimeoutMs > 0) { startIdleChecker(); } else { stopIdleChecker(); } } (missing: @param ms)
- `packages/pi-coding-agent/src/core/lsp/client.ts:458` — `getOrCreateClient(config: ServerConfig, cwd: string, initTimeoutMs?: number)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param config, @param cwd, @param initTimeoutMs, @returns)
- `packages/pi-coding-agent/src/core/lsp/client.ts:611` — `ensureFileOpen(client: LspClient, filePath: string, signal?: AbortSignal)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param client, @param filePath, @param signal)
- `packages/pi-coding-agent/src/core/lsp/client.ts:668` — `refreshFile(client: LspClient, filePath: string, signal?: AbortSignal)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param client, @param filePath, @param signal)
- `packages/pi-coding-agent/src/core/lsp/client.ts:725` — `notifyFileChanged(filePath: string)` — exported: **yes** — Implements logic starting with: { const uri = fileToUri(filePath); for (const client of clients.values()) { if (client.openFiles.has(uri)) { refreshFile(client, filePath).catch(()… (missing: @param filePath)
- `packages/pi-coding-agent/src/core/lsp/client.ts:737` — `removeStreamHandlers(client: LspClient)` — exported: **no** — Implements logic starting with: { const handlers = clientStreamHandlers.get(client.name); if (!handlers) return; if (handlers.stdoutData) client.proc.stdout?.removeListener("data"… (missing: @param client)
- `packages/pi-coding-agent/src/core/lsp/client.ts:754` — `shutdownClient(key: string)` — exported: **no** — Implements logic starting with: { const client = clients.get(key); if (!client) return; for (const pending of Array.from(client.pendingRequests.values())) { pending.reject(new Err… (missing: @param key)

### `packages/pi-coding-agent/src/core/lsp/config.ts`
- `packages/pi-coding-agent/src/core/lsp/config.ts:240` — `getConfigPaths(cwd: string)` — exported: **no** — Implements logic starting with: { const filenames = ["lsp.json", ".lsp.json", "lsp.yaml", ".lsp.yaml", "lsp.yml", ".lsp.yml"]; const paths: string[] = []; // Project root files (h… (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/core/lsp/config.ts:279` — `loadConfig(cwd: string)` — exported: **yes** — Implements logic starting with: { let mergedServers = coerceServerConfigs(DEFAULTS); const configPaths = getConfigPaths(cwd).reverse(); let hasOverrides = false; let idleTimeoutMs… (missing: @param cwd, @returns)

### `packages/pi-coding-agent/src/core/lsp/edits.ts`
- `packages/pi-coding-agent/src/core/lsp/edits.ts:14` — `applyTextEditsToString(content: string, edits: TextEdit[])` — exported: **no** — Implements logic starting with: { const lines = content.split("\n"); // Sort edits in reverse order (bottom-to-top, right-to-left) const sortedEdits = [...edits].sort((a, b) => {… (missing: @param content, @param edits, @returns)
- `packages/pi-coding-agent/src/core/lsp/edits.ts:48` — `applyTextEdits(filePath: string, edits: TextEdit[])` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @param edits)
- `packages/pi-coding-agent/src/core/lsp/edits.ts:62` — `applyWorkspaceEdit(edit: WorkspaceEdit, cwd: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param edit, @param cwd, @returns)

### `packages/pi-coding-agent/src/core/lsp/helpers.ts`
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:5` — `class ToolAbortError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/lsp/helpers.ts:33` — `untilAborted(signal: AbortSignal | undefined, fn: () => Promise<T>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param signal, @param fn, @returns)

### `packages/pi-coding-agent/src/core/lsp/index.ts`
- `packages/pi-coding-agent/src/core/lsp/index.ts:403` — `createLspTool(cwd: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param cwd, @returns)

### `packages/pi-coding-agent/src/core/lsp/utils.ts`
- `packages/pi-coding-agent/src/core/lsp/utils.ts:139` — `detectLanguageId(filePath: string)` — exported: **yes** — Implements logic starting with: { const ext = path.extname(filePath).toLowerCase(); const basename = path.basename(filePath).toLowerCase(); if (basename === "dockerfile" || basena… (missing: @param filePath, @returns)

### `packages/pi-coding-agent/src/core/messages.test.ts`
- `packages/pi-coding-agent/src/core/messages.test.ts:14` — `firstTextBlock(msg: ReturnType<typeof convertToLlm>[number])` — exported: **no** — Implements logic starting with: { const { content } = msg; assert.ok(Array.isArray(content), "Expected content to be an array"); const block = content[0]; assert.ok(typeof block =… (missing: @param msg, @returns)

### `packages/pi-coding-agent/src/core/messages.ts`
- `packages/pi-coding-agent/src/core/messages.ts:88` — `bashExecutionToText(msg: BashExecutionMessage)` — exported: **no** — Implements logic starting with: { let text = `Ran \`${msg.command}\`\n`; if (msg.output) { text += `\`\`\`\n${msg.output}\n\`\`\``; } else { text += "(no output)"; } if (msg.cance… (missing: @param msg, @returns)
- `packages/pi-coding-agent/src/core/messages.ts:129` — `createCustomMessage(customType: string, content: string | (TextContent | ImageContent)[], display: boolean, details: unknown | undefined, timestamp: string)` — exported: **yes** — Implements logic starting with: { return { role: "custom", customType, content, display, details, timestamp: new Date(timestamp).getTime(), }; } (missing: @param customType, @param content, @param display, @param details, @param timestamp, @returns)
- `packages/pi-coding-agent/src/core/messages.ts:154` — `convertToLlm(messages: AgentMessage[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param messages, @returns)

### `packages/pi-coding-agent/src/core/model-registry-auth-mode.test.ts`
- `packages/pi-coding-agent/src/core/model-registry-auth-mode.test.ts:82` — `createStreamSpy()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)

### `packages/pi-coding-agent/src/core/model-registry.ts`
- `packages/pi-coding-agent/src/core/model-registry.ts:203` — `applyModelOverride(model: Model<Api>, override: ModelOverride)` — exported: **no** — Implements logic starting with: { const result = { ...model }; // Simple field overrides if (override.name !== undefined) result.name = override.name; if (override.reasoning !== u… (missing: @param model, @param override, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:244` — `class ModelRegistry` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:296` — `ModelRegistry.getError()` — exported: **yes** — Implements logic starting with: { return this.loadError; } (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:545` — `ModelRegistry.getAll()` — exported: **yes** — Implements logic starting with: { return this.models; } (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:553` — `ModelRegistry.getAvailable()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:561` — `ModelRegistry.setDisabledModelProviders(providers: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param providers)
- `packages/pi-coding-agent/src/core/model-registry.ts:572` — `ModelRegistry.getDisabledModelProviders()` — exported: **yes** — Implements logic starting with: { return Array.from(this.disabledModelProviders); } (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:580` — `ModelRegistry.getProviderAuthMode(provider: string)` — exported: **yes** — Implements logic starting with: { // E2E-test-only: the fake provider is keyless. Sentinel is project- // internal ("gsd-fake") so it cannot collide with a real provider. // See p… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:596` — `ModelRegistry.isProviderRequestReady(provider: string)` — exported: **yes** — Implements logic starting with: { if (this.disabledModelProviders.has(provider.trim().toLowerCase())) return false; const config = this.registeredProviders.get(provider); if (conf… (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:608` — `ModelRegistry.find(provider: string, modelId: string)` — exported: **yes** — Implements logic starting with: { return this.models.find((m) => m.provider === provider && m.id === modelId); } (missing: @param provider, @param modelId, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:617` — `ModelRegistry.getApiKey(model: Model<Api>, sessionId?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param model, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:628` — `ModelRegistry.getApiKeyForProvider(provider: string, sessionId?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param provider, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:637` — `ModelRegistry.isUsingOAuth(model: Model<Api>)` — exported: **yes** — Implements logic starting with: { const cred = this.authStorage.get(model.provider); return cred?.type === "oauth"; } (missing: @param model, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:649` — `ModelRegistry.registerProvider(providerName: string, config: ProviderConfigInput)` — exported: **yes** — Implements logic starting with: { this.registeredProviders.set(providerName, config); this.applyProviderConfig(providerName, config); } (missing: @param providerName, @param config)
- `packages/pi-coding-agent/src/core/model-registry.ts:663` — `ModelRegistry.unregisterProvider(providerName: string)` — exported: **yes** — Implements logic starting with: { if (!this.registeredProviders.has(providerName)) return; this.registeredProviders.delete(providerName); this.customProviderApiKeys.delete(provide… (missing: @param providerName)
- `packages/pi-coding-agent/src/core/model-registry.ts:831` — `ModelRegistry.discoverModels(providers?: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param providers, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:885` — `ModelRegistry.getAllWithDiscovered()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:894` — `ModelRegistry.isDiscovered(model: Model<Api>)` — exported: **yes** — Implements logic starting with: { return this.discoveredModels.some((m) => m.provider === model.provider && m.id === model.id); } (missing: @param model, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:901` — `ModelRegistry.getDiscoveryCache()` — exported: **yes** — Implements logic starting with: { return this.discoveryCache; } (missing: @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:1007` — `ModelRegistry.isLocalModel(model: Model<Api>)` — exported: **yes** — Implements logic starting with: { return isLocalModel(model); } (missing: @param model, @returns)
- `packages/pi-coding-agent/src/core/model-registry.ts:1016` — `ModelRegistry.isAllLocalChain()` — exported: **yes** — Implements logic starting with: { const models = this.getAll(); if (models.length === 0) return false; return models.every((m) => isLocalModel(m)); } (missing: @returns)

### `packages/pi-coding-agent/src/core/model-resolver.ts`
- `packages/pi-coding-agent/src/core/model-resolver.ts:23` — `isAlias(id: string)` — exported: **no** — Implements logic starting with: { // Check if ID ends with -latest if (id.endsWith("-latest")) return true; // Check if ID ends with a date pattern (-YYYYMMDD) const datePattern =… (missing: @param id, @returns)
- `packages/pi-coding-agent/src/core/model-resolver.ts:36` — `tryMatchModel(modelPattern: string, availableModels: Model<Api>[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param modelPattern, @param availableModels, @returns)
- `packages/pi-coding-agent/src/core/model-resolver.ts:120` — `parseModelPattern(pattern: string, availableModels: Model<Api>[], options?: { allowInvalidThinkingLevelFallback?: boolean })` — exported: **no** — Implements logic starting with: { // Try exact match first const exactMatch = tryMatchModel(pattern, availableModels); if (exactMatch) { return { model: exactMatch, thinkingLevel:… (missing: @param pattern, @param availableModels, @param options, @returns)
- `packages/pi-coding-agent/src/core/model-resolver.ts:186` — `resolveModelScope(patterns: string[], modelRegistry: ModelRegistry)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param patterns, @param modelRegistry, @returns)
- `packages/pi-coding-agent/src/core/model-resolver.ts:280` — `resolveCliModel(options: { cliProvider?: string; cliModel?: string; modelRegistry: ModelRegistry; })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param options, @returns)
- `packages/pi-coding-agent/src/core/model-resolver.ts:426` — `findInitialModel(options: { cliProvider?: string; cliModel?: string; scopedModels: ScopedModel[]; isContinuing: boolean; defaultProvider?: string; defaultModelId?: string; defaultThinkingLevel?: ThinkingLevel; modelRegistry: ModelRegistry; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)

### `packages/pi-coding-agent/src/core/models-json-writer.ts`
- `packages/pi-coding-agent/src/core/models-json-writer.ts:47` — `ModelsJsonWriter.addModel(provider: string, model: ModelDefinition, providerConfig?: Partial<ProviderConfig>)` — exported: **yes** — Implements logic starting with: { this.withLock((config) => { if (!config.providers[provider]) { config.providers[provider] = { ...providerConfig, models: [], }; } const providerE… (missing: @param provider, @param model, @param providerConfig)
- `packages/pi-coding-agent/src/core/models-json-writer.ts:76` — `ModelsJsonWriter.removeModel(provider: string, modelId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param provider, @param modelId)
- `packages/pi-coding-agent/src/core/models-json-writer.ts:95` — `ModelsJsonWriter.setProvider(provider: string, providerConfig: ProviderConfig)` — exported: **yes** — Implements logic starting with: { this.withLock((config) => { config.providers[provider] = providerConfig; return config; }); } (missing: @param provider, @param providerConfig)
- `packages/pi-coding-agent/src/core/models-json-writer.ts:105` — `ModelsJsonWriter.removeProvider(provider: string)` — exported: **yes** — Implements logic starting with: { this.withLock((config) => { delete config.providers[provider]; return config; }); } (missing: @param provider)
- `packages/pi-coding-agent/src/core/models-json-writer.ts:115` — `ModelsJsonWriter.listProviders()` — exported: **yes** — Implements logic starting with: { return this.readConfig(); } (missing: @returns)

### `packages/pi-coding-agent/src/core/package-manager.ts`
- `packages/pi-coding-agent/src/core/package-manager.ts:505` — `collectResourceFiles(dir: string, resourceType: ResourceType)` — exported: **no** — Implements logic starting with: { if (resourceType === "skills") { return collectSkillEntries(dir); } if (resourceType === "extensions") { return collectAutoExtensionEntries(dir);… (missing: @param dir, @param resourceType, @returns)
- `packages/pi-coding-agent/src/core/package-manager.ts:588` — `applyPatterns(allPaths: string[], patterns: string[], baseDir: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param allPaths, @param patterns, @param baseDir, @returns)

### `packages/pi-coding-agent/src/core/prompt-templates.ts`
- `packages/pi-coding-agent/src/core/prompt-templates.ts:22` — `parseCommandArgs(argsString: string)` — exported: **no** — Implements logic starting with: { const args: string[] = []; let current = ""; let inQuote: string | null = null; for (let i = 0; i < argsString.length; i++) { const char = argsSt… (missing: @param argsString, @returns)
- `packages/pi-coding-agent/src/core/prompt-templates.ts:66` — `substituteArgs(content: string, args: string[])` — exported: **no** — Implements logic starting with: { let result = content; // Replace $1, $2, etc. with positional args FIRST (before wildcards) // This prevents wildcard replacement values containi… (missing: @param content, @param args, @returns)
- `packages/pi-coding-agent/src/core/prompt-templates.ts:138` — `loadTemplatesFromDir(dir: string, source: string, sourceLabel: string)` — exported: **no** — Implements logic starting with: { const templates: PromptTemplate[] = []; if (!existsSync(dir)) { return templates; } try { const entries = readdirSync(dir, { withFileTypes: true… (missing: @param dir, @param source, @param sourceLabel, @returns)
- `packages/pi-coding-agent/src/core/prompt-templates.ts:212` — `loadPromptTemplates(options: LoadPromptTemplatesOptions = {})` — exported: **yes** — Implements logic starting with: { const resolvedCwd = options.cwd ?? process.cwd(); const resolvedAgentDir = options.agentDir ?? getPromptsDir(); const promptPaths = options.promp… (missing: @param options, @returns)
- `packages/pi-coding-agent/src/core/prompt-templates.ts:285` — `expandPromptTemplate(text: string, templates: PromptTemplate[])` — exported: **yes** — Implements logic starting with: { if (!text.startsWith("/")) return text; const spaceIndex = text.indexOf(" "); const templateName = spaceIndex === -1 ? text.slice(1) : text.slice… (missing: @param text, @param templates, @returns)

### `packages/pi-coding-agent/src/core/resolve-config-value.ts`
- `packages/pi-coding-agent/src/core/resolve-config-value.ts:38` — `setAllowedCommandPrefixes(prefixes: string[])` — exported: **yes** — Implements logic starting with: { if (prefixes.length === 0) { process.stderr.write("[resolve-config-value] Warning: empty command prefix allowlist — all !commands will be blocked… (missing: @param prefixes)
- `packages/pi-coding-agent/src/core/resolve-config-value.ts:47` — `getAllowedCommandPrefixes()` — exported: **yes** — Implements logic starting with: { return activeCommandPrefixes; } (missing: @returns)
- `packages/pi-coding-agent/src/core/resolve-config-value.ts:56` — `resolveConfigValue(config: string)` — exported: **yes** — Implements logic starting with: { if (config.startsWith("!")) { return executeCommand(config); } const envValue = process.env[config]; return envValue || config; } (missing: @param config, @returns)
- `packages/pi-coding-agent/src/core/resolve-config-value.ts:103` — `resolveHeaders(headers: Record<string, string> | undefined)` — exported: **yes** — Implements logic starting with: { if (!headers) return undefined; const resolved: Record<string, string> = {}; for (const [key, value] of Object.entries(headers)) { const resolved… (missing: @param headers, @returns)

### `packages/pi-coding-agent/src/core/resource-loader.ts`
- `packages/pi-coding-agent/src/core/resource-loader.ts:848` — `extractExtensionKey(ownerPath: string, extensionsDir: string)` — exported: **yes** — Implements logic starting with: { const normalizedDir = resolve(extensionsDir); const normalizedPath = resolve(ownerPath); const prefix = normalizedDir.endsWith(sep) ? normalizedD… (missing: @param ownerPath, @param extensionsDir, @returns)
- `packages/pi-coding-agent/src/core/resource-loader.ts:868` — `detectExtensionConflicts(extensions: Extension[], bundledExtensionKeys: Set<string>, extensionsDir: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param extensions, @param bundledExtensionKeys, @param extensionsDir, @returns)

### `packages/pi-coding-agent/src/core/retry-handler.ts`
- `packages/pi-coding-agent/src/core/retry-handler.ts:50` — `RetryHandler.get retryAttempt()` — exported: **yes** — Implements logic starting with: { return this._retryAttempt; } (missing: @returns)
- `packages/pi-coding-agent/src/core/retry-handler.ts:55` — `RetryHandler.get isRetrying()` — exported: **yes** — Implements logic starting with: { return this._retryPromise !== undefined; } (missing: @returns)
- `packages/pi-coding-agent/src/core/retry-handler.ts:60` — `RetryHandler.get autoRetryEnabled()` — exported: **yes** — Implements logic starting with: { return this._deps.settingsManager.getRetryEnabled(); } (missing: @returns)
- `packages/pi-coding-agent/src/core/retry-handler.ts:65` — `RetryHandler.setAutoRetryEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this._deps.settingsManager.setRetryEnabled(enabled); } (missing: @param enabled)
- `packages/pi-coding-agent/src/core/retry-handler.ts:74` — `RetryHandler.createRetryPromiseForAgentEnd(messages: Array<{ role: string } & Record<string, any>>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param messages)
- `packages/pi-coding-agent/src/core/retry-handler.ts:108` — `RetryHandler.isRetryableError(message: AssistantMessage)` — exported: **yes** — Implements logic starting with: { if (message.stopReason !== "error" || !message.errorMessage) return false; // Context overflow is handled by compaction, not retry const contextW… (missing: @param message, @returns)
- `packages/pi-coding-agent/src/core/retry-handler.ts:124` — `RetryHandler.handleRetryableError(message: AssistantMessage)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message)

### `packages/pi-coding-agent/src/core/sdk.ts`
- `packages/pi-coding-agent/src/core/sdk.ts:11` — `isClaudeCodeBinaryInPath()` — exported: **no** — Implements logic starting with: { const pathDirs = (process.env.PATH ?? "").split(":"); return pathDirs.some((dir) => dir && existsSync(join(dir, "claude"))); } (missing: @returns)
- `packages/pi-coding-agent/src/core/sdk.ts:21` — `class CredentialCooldownError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/sdk.ts:293` — `createAgentSession(options: CreateAgentSessionOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param options, @returns)

### `packages/pi-coding-agent/src/core/session-manager.ts`
- `packages/pi-coding-agent/src/core/session-manager.ts:32` — `pLimit(concurrency: number)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param concurrency, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:244` — `generateId(byId: { has(id: string): boolean })` — exported: **no** — Implements logic starting with: { for (let i = 0; i < 100; i++) { const id = randomUUID().slice(0, 8); if (!byId.has(id)) return id; } // Fallback to full UUID if somehow we have… (missing: @param byId, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:254` — `migrateV1ToV2(entries: FileEntry[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param entries)
- `packages/pi-coding-agent/src/core/session-manager.ts:283` — `migrateV2ToV3(entries: FileEntry[])` — exported: **no** — Implements logic starting with: { for (const entry of entries) { if (entry.type === "session") { entry.version = 3; continue; } // Update message entries with hookMessage role if… (missing: @param entries)
- `packages/pi-coding-agent/src/core/session-manager.ts:304` — `migrateToCurrentVersion(entries: FileEntry[])` — exported: **no** — Implements logic starting with: { const header = entries.find((e) => e.type === "session") as SessionHeader | undefined; const version = header?.version ?? 1; if (version >= CURRE… (missing: @param entries, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:317` — `migrateSessionEntries(entries: FileEntry[])` — exported: **yes** — Implements logic starting with: { migrateToCurrentVersion(entries); } (missing: @param entries)
- `packages/pi-coding-agent/src/core/session-manager.ts:322` — `parseSessionEntries(content: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param content, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:353` — `buildSessionContext(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param entries, @param leafId, @param byId, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:466` — `getDefaultSessionDir(cwd: string)` — exported: **no** — Implements logic starting with: { const safePath = `--${cwd.replace(/^[/\\]/, "").replace(/[/\\:]/g, "-")}--`; const sessionDir = join(getDefaultAgentDir(), "sessions", safePath);… (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:499` — `prepareForPersistence(obj: unknown, blobStore: BlobStore, key?: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param obj, @param blobStore, @param key, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:562` — `resolveBlobRefsInEntries(entries: FileEntry[], blobStore: BlobStore)` — exported: **no** — Implements logic starting with: { for (const entry of entries) { if (entry.type === "session") continue; let contentArray: unknown[] | undefined; if (entry.type === "message") { c… (missing: @param entries, @param blobStore)
- `packages/pi-coding-agent/src/core/session-manager.ts:816` — `class SessionManager` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:852` — `SessionManager.wasInterrupted()` — exported: **yes** — Implements logic starting with: { // Walk backwards to find the last message entry for (let i = this.fileEntries.length - 1; i >= 0; i--) { const entry = this.fileEntries[i]; if (… (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:879` — `SessionManager.setSessionFile(sessionFile: string)` — exported: **yes** — Implements logic starting with: { this.sessionFile = resolve(sessionFile); if (existsSync(this.sessionFile)) { this.fileEntries = loadEntriesFromFile(this.sessionFile); // If file… (missing: @param sessionFile)
- `packages/pi-coding-agent/src/core/session-manager.ts:1052` — `SessionManager.appendMessage(message: Message | CustomMessage | BashExecutionMessage)` — exported: **yes** — Implements logic starting with: { const entry: SessionMessageEntry = { type: "message", id: generateId(this.byId), parentId: this.leafId, timestamp: new Date().toISOString(), mess… (missing: @param message, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1065` — `SessionManager.appendThinkingLevelChange(thinkingLevel: string)` — exported: **yes** — Implements logic starting with: { const entry: ThinkingLevelChangeEntry = { type: "thinking_level_change", id: generateId(this.byId), parentId: this.leafId, timestamp: new Date().… (missing: @param thinkingLevel, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1078` — `SessionManager.appendModelChange(provider: string, modelId: string)` — exported: **yes** — Implements logic starting with: { const entry: ModelChangeEntry = { type: "model_change", id: generateId(this.byId), parentId: this.leafId, timestamp: new Date().toISOString(), pr… (missing: @param provider, @param modelId, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1092` — `SessionManager.appendCompaction(summary: string, firstKeptEntryId: string, tokensBefore: number, details?: T, fromHook?: boolean)` — exported: **yes** — Implements logic starting with: { const entry: CompactionEntry<T> = { type: "compaction", id: generateId(this.byId), parentId: this.leafId, timestamp: new Date().toISOString(), su… (missing: @param summary, @param firstKeptEntryId, @param tokensBefore, @param details, @param fromHook, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1115` — `SessionManager.appendCustomEntry(customType: string, data?: unknown)` — exported: **yes** — Implements logic starting with: { const entry: CustomEntry = { type: "custom", customType, data, id: generateId(this.byId), parentId: this.leafId, timestamp: new Date().toISOStrin… (missing: @param customType, @param data, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1129` — `SessionManager.appendSessionInfo(name: string)` — exported: **yes** — Implements logic starting with: { const entry: SessionInfoEntry = { type: "session_info", id: generateId(this.byId), parentId: this.leafId, timestamp: new Date().toISOString(), na… (missing: @param name, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1142` — `SessionManager.getSessionName()` — exported: **yes** — Implements logic starting with: { // Walk entries in reverse to find the latest session_info with a name const entries = this.getEntries(); for (let i = entries.length - 1; i >= 0… (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1201` — `SessionManager.getChildren(parentId: string)` — exported: **yes** — Implements logic starting with: { const children: SessionEntry[] = []; for (const entry of this.byId.values()) { if (entry.parentId === parentId) { children.push(entry); } } retur… (missing: @param parentId, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1214` — `SessionManager.getLabel(id: string)` — exported: **yes** — Implements logic starting with: { return this.labelsById.get(id); } (missing: @param id, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1223` — `SessionManager.appendLabelChange(targetId: string, label: string | undefined)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param targetId, @param label, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1249` — `SessionManager.getBranch(fromId?: string)` — exported: **yes** — Implements logic starting with: { const path: SessionEntry[] = []; const startId = fromId ?? this.leafId; let current = startId ? this.byId.get(startId) : undefined; while (curren… (missing: @param fromId, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1264` — `SessionManager.buildSessionContext()` — exported: **yes** — Implements logic starting with: { return buildSessionContext(this.getEntries(), this.leafId, this.byId); } (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1271` — `SessionManager.getHeader()` — exported: **yes** — Implements logic starting with: { const h = this.fileEntries.find((e) => e.type === "session"); return h ? (h as SessionHeader) : null; } (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1281` — `SessionManager.getEntries()` — exported: **yes** — Implements logic starting with: { return [...this.sessionEntries]; } (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1294` — `SessionManager.getTree()` — exported: **yes** — Builds an in-memory lookup structure. (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1343` — `SessionManager.branch(branchFromId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param branchFromId)
- `packages/pi-coding-agent/src/core/session-manager.ts:1364` — `SessionManager.branchWithSummary(branchFromId: string | null, summary: string, details?: unknown, fromHook?: boolean)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param branchFromId, @param summary, @param details, @param fromHook, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1388` — `SessionManager.createBranchedSession(leafId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param leafId, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1487` — `SessionManager.create(cwd: string, sessionDir?: string)` — exported: **yes** — Implements logic starting with: { const dir = sessionDir ?? getDefaultSessionDir(cwd); return new SessionManager(cwd, dir, undefined, true); } (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1497` — `SessionManager.open(path: string, sessionDir?: string)` — exported: **yes** — Implements logic starting with: { // Extract cwd from session header if possible, otherwise use process.cwd() const entries = loadEntriesFromFile(path); const header = entries.fin… (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1512` — `SessionManager.continueRecent(cwd: string, sessionDir?: string)` — exported: **yes** — Implements logic starting with: { const dir = sessionDir ?? getDefaultSessionDir(cwd); const mostRecent = findMostRecentSession(dir); if (mostRecent) { return new SessionManager(c… (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1522` — `SessionManager.inMemory(cwd: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return new SessionManager(cwd, "", undefined, false); } (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1533` — `SessionManager.forkFrom(sourcePath: string, targetCwd: string, sessionDir?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1582` — `SessionManager.list(cwd: string, sessionDir?: string, onProgress?: SessionListProgress)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/core/session-manager.ts:1593` — `SessionManager.listAll(onProgress?: SessionListProgress)` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `packages/pi-coding-agent/src/core/settings-manager.ts`
- `packages/pi-coding-agent/src/core/settings-manager.ts:232` — `stripGlobalOnlyKeys(settings: Settings)` — exported: **no** — Implements logic starting with: { const result = { ...settings }; for (const key of GLOBAL_ONLY_KEYS) { delete (result as Record<string, unknown>)[key]; } return result; } (missing: @param settings, @returns)
- `packages/pi-coding-agent/src/core/settings-manager.ts:241` — `deepMergeSettings(base: Settings, overrides: Settings)` — exported: **no** — Implements logic starting with: { const result: Settings = { ...base }; for (const key of Object.keys(overrides) as (keyof Settings)[]) { const overrideValue = overrides[key]; con… (missing: @param base, @param overrides, @returns)
- `packages/pi-coding-agent/src/core/settings-manager.ts:398` — `SettingsManager.create(cwd: string = process.cwd(), agentDir: string = getAgentDir())` — exported: **yes** — Implements logic starting with: { const storage = new FileSettingsStorage(cwd, agentDir); return SettingsManager.fromStorage(storage); } (missing: @param cwd, @param agentDir, @returns)
- `packages/pi-coding-agent/src/core/settings-manager.ts:404` — `SettingsManager.fromStorage(storage: SettingsStorage)` — exported: **yes** — Implements logic starting with: { const globalLoad = SettingsManager.tryLoadFromStorage(storage, "global"); const projectLoad = SettingsManager.tryLoadFromStorage(storage, "projec… (missing: @param storage, @returns)
- `packages/pi-coding-agent/src/core/settings-manager.ts:426` — `SettingsManager.inMemory(settings: Partial<Settings> = {})` — exported: **yes** — Implements logic starting with: { const storage = new InMemorySettingsStorage(); return new SettingsManager(storage, settings, {}); } (missing: @param settings, @returns)
- `packages/pi-coding-agent/src/core/settings-manager.ts:538` — `SettingsManager.applyOverrides(overrides: Partial<Settings>)` — exported: **yes** — Implements logic starting with: { this.settings = deepMergeSettings(this.settings, overrides); } (missing: @param overrides)
- `packages/pi-coding-agent/src/core/settings-manager.ts:836` — `SettingsManager.setCompactionThresholdOverride(percent: number | undefined)` — exported: **yes** — Implements logic starting with: { if (!this.settings.compaction) { this.settings.compaction = {}; } if (percent === undefined) { delete this.settings.compaction.thresholdPercent;… (missing: @param percent)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1228` — `SettingsManager.getAllowedCommandPrefixes()` — exported: **yes** — Implements logic starting with: { return this.globalSettings.allowedCommandPrefixes; } (missing: @returns)
- `packages/pi-coding-agent/src/core/settings-manager.ts:1240` — `SettingsManager.getFetchAllowedUrls()` — exported: **yes** — Implements logic starting with: { return this.globalSettings.fetchAllowedUrls; } (missing: @returns)

### `packages/pi-coding-agent/src/core/skills.ts`
- `packages/pi-coding-agent/src/core/skills.ts:112` — `validateName(name: string, parentDirName: string)` — exported: **no** — Implements logic starting with: { const errors: string[] = []; if (name !== parentDirName) { errors.push(`name "${name}" does not match parent directory "${parentDirName}"`); } if… (missing: @param name, @param parentDirName, @returns)
- `packages/pi-coding-agent/src/core/skills.ts:141` — `validateDescription(description: string | undefined)` — exported: **no** — Implements logic starting with: { const errors: string[] = []; if (!description || description.trim() === "") { errors.push("description is required"); } else if (description.leng… (missing: @param description, @returns)
- `packages/pi-coding-agent/src/core/skills.ts:167` — `loadSkillsFromDir(options: LoadSkillsFromDirOptions)` — exported: **yes** — Implements logic starting with: { const { dir, source } = options; return loadSkillsFromDirInternal(dir, source, true); } (missing: @param options, @returns)
- `packages/pi-coding-agent/src/core/skills.ts:311` — `formatSkillsForPrompt(skills: Skill[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param skills, @returns)
- `packages/pi-coding-agent/src/core/skills.ts:377` — `loadSkills(options: LoadSkillsOptions = {})` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param options, @returns)

### `packages/pi-coding-agent/src/core/system-prompt.ts`
- `packages/pi-coding-agent/src/core/system-prompt.ts:73` — `buildSystemPrompt(options: BuildSystemPromptOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: @param options, @returns)

### `packages/pi-coding-agent/src/core/token-telemetry.ts`
- `packages/pi-coding-agent/src/core/token-telemetry.ts:40` — `buildTokenTelemetryRecord(msg: AssistantMessage)` — exported: **yes** — Implements logic starting with: { const input = msg.usage?.input ?? 0; const output = msg.usage?.output ?? 0; const cacheRead = msg.usage?.cacheRead ?? 0; const cacheWrite = msg.u… (missing: @param msg, @returns)
- `packages/pi-coding-agent/src/core/token-telemetry.ts:69` — `emitTokenTelemetry(msg: AssistantMessage)` — exported: **yes** — Serializes a value to JSON. (missing: @param msg)

### `packages/pi-coding-agent/src/core/tools/bash-interceptor.ts`
- `packages/pi-coding-agent/src/core/tools/bash-interceptor.ts:73` — `compileInterceptor(rules: BashInterceptorRule[])` — exported: **yes** — Implements logic starting with: { const compiled = rules.flatMap((rule) => { try { return [{ regex: new RegExp(rule.pattern, rule.flags), rule }]; } catch { return []; // skip inv… (missing: @param rules, @returns)
- `packages/pi-coding-agent/src/core/tools/bash-interceptor.ts:108` — `checkBashInterception(command: string, availableTools: string[], rules?: BashInterceptorRule[])` — exported: **yes** — Implements logic starting with: { const effectiveRules = rules ?? DEFAULT_BASH_INTERCEPTOR_RULES; return compileInterceptor(effectiveRules).check(command, availableTools); } (missing: @returns)

### `packages/pi-coding-agent/src/core/tools/bash.ts`
- `packages/pi-coding-agent/src/core/tools/bash.ts:41` — `getTempFilePath()` — exported: **no** — Implements logic starting with: { const id = randomBytes(8).toString("hex"); return join(tmpdir(), `pi-bash-${id}.log`); } (missing: @returns)
- `packages/pi-coding-agent/src/core/tools/bash.ts:51` — `endsWithBackgroundOperator(fragment: string)` — exported: **no** — Implements logic starting with: { // Remove content inside single-quoted strings to avoid false positives const stripped = fragment.replace(/'[^']*'/g, "''"); // Match trailing &… (missing: @param fragment, @returns)
- `packages/pi-coding-agent/src/core/tools/bash.ts:62` — `hasOutputRedirect(segment: string)` — exported: **no** — Implements logic starting with: { // Remove single-quoted strings to avoid matching inside them const stripped = segment.replace(/'[^']*'/g, "''"); // Match >, >> not preceded by… (missing: @param segment, @returns)
- `packages/pi-coding-agent/src/core/tools/bash.ts:84` — `rewriteBackgroundCommand(command: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param command, @returns)

### `packages/pi-coding-agent/src/core/tools/edit-diff.ts`
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:37` — `normalizeForFuzzyMatch(text: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param text, @returns)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:72` — `fuzzyFindText(content: string, oldText: string)` — exported: **yes** — Implements logic starting with: { const exactIndex = content.indexOf(oldText); if (exactIndex !== -1) { return { found: true, index: exactIndex, matchLength: oldText.length, usedF… (missing: @param content, @param oldText, @returns)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:108` — `stripBom(content: string)` — exported: **yes** — Implements logic starting with: { return content.startsWith("\uFEFF") ? { bom: "\uFEFF", text: content.slice(1) } : { bom: "", text: content }; } (missing: @param content, @returns)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:118` — `generateDiffString(oldContent: string, newContent: string, contextLines = 4)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param oldContent, @param newContent, @param contextLines, @returns)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:236` — `buildLineDiffLCS(oldLines: string[], newLines: string[])` — exported: **no** — Implements logic starting with: { const dp: number[][] = Array.from({ length: oldLines.length + 1 }, () => Array<number>(newLines.length + 1).fill(0), ); for (let i = oldLines.len… (missing: @param oldLines, @param newLines, @returns)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:290` — `buildLineDiffLinear(oldLines: string[], newLines: string[])` — exported: **no** — Implements logic starting with: { const ops: LineDiffOp[] = []; // Match common prefix let prefixLen = 0; const minLen = Math.min(oldLines.length, newLines.length); while (prefixL… (missing: @param oldLines, @param newLines, @returns)
- `packages/pi-coding-agent/src/core/tools/edit-diff.ts:336` — `computeEditDiff(path: string, oldText: string, newText: string, cwd: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param path, @param oldText, @param newText, @param cwd, @returns)

### `packages/pi-coding-agent/src/core/tools/hashline-edit.ts`
- `packages/pi-coding-agent/src/core/tools/hashline-edit.ts:88` — `tryParseTag(raw: string)` — exported: **no** — Implements logic starting with: { try { return parseTag(raw); } catch { return undefined; } } (missing: @param raw, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline-edit.ts:99` — `resolveEditAnchors(edits: HashlineEditItem[])` — exported: **no** — Validates inputs and throws on failure. (missing: @param edits, @returns)

### `packages/pi-coding-agent/src/core/tools/hashline.ts`
- `packages/pi-coding-agent/src/core/tools/hashline.ts:47` — `computeLineHash(idx: number, line: string)` — exported: **yes** — Implements logic starting with: { line = line.replace(/\r/g, "").trimEnd(); let seed = 0; if (!RE_SIGNIFICANT.test(line)) { seed = idx; } return DICT[xxHash32(line, seed) & 0xff];… (missing: @param idx, @param line, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:60` — `formatLineTag(line: number, text: string)` — exported: **yes** — Implements logic starting with: { return `${line}#${computeLineHash(line, text)}`; } (missing: @param line, @param text, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:69` — `formatHashLines(text: string, startLine = 1)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param text, @param startLine, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:84` — `parseTag(ref: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param ref, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:113` — `class HashlineMismatchError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:180` — `validateLineRef(ref: Anchor, fileLines: string[])` — exported: **yes** — Validates inputs and throws on failure. (missing: @param ref, @param fileLines)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:206` — `stripNewLinePrefixes(lines: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param lines, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:233` — `parseHashlineText(edit: string[] | string | null)` — exported: **yes** — Implements logic starting with: { if (edit === null) return []; if (typeof edit === "string") { const normalizedEdit = edit.endsWith("\n") ? edit.slice(0, -1) : edit; edit = norma… (missing: @param edit, @returns)
- `packages/pi-coding-agent/src/core/tools/hashline.ts:295` — `applyHashlineEdits(text: string, edits: HashlineEdit[])` — exported: **yes** — Validates inputs and throws on failure. (missing: @param text, @param edits)

### `packages/pi-coding-agent/src/core/tools/index.ts`
- `packages/pi-coding-agent/src/core/tools/index.ts:181` — `createCodingTools(cwd: string, options?: ToolsOptions)` — exported: **yes** — Implements logic starting with: { return [ createReadTool(cwd, options?.read), createBashTool(cwd, options?.bash), createEditTool(cwd), createWriteTool(cwd), ]; } (missing: @param cwd, @param options, @returns)
- `packages/pi-coding-agent/src/core/tools/index.ts:193` — `createReadOnlyTools(cwd: string, options?: ToolsOptions)` — exported: **yes** — Implements logic starting with: { return [createReadTool(cwd, options?.read), createGrepTool(cwd), createFindTool(cwd), createLsTool(cwd)]; } (missing: @param cwd, @param options, @returns)
- `packages/pi-coding-agent/src/core/tools/index.ts:200` — `createAllTools(cwd: string, options?: ToolsOptions)` — exported: **yes** — Implements logic starting with: { return { read: createReadTool(cwd, options?.read), bash: createBashTool(cwd, options?.bash), edit: createEditTool(cwd), write: createWriteTool(cw… (missing: @param cwd, @param options, @returns)
- `packages/pi-coding-agent/src/core/tools/index.ts:219` — `createHashlineCodingTools(cwd: string, options?: ToolsOptions)` — exported: **yes** — Implements logic starting with: { return [ createHashlineReadTool(cwd, options?.read), createBashTool(cwd, options?.bash), createHashlineEditTool(cwd), createWriteTool(cwd), ]; } (missing: @param cwd, @param options, @returns)

### `packages/pi-coding-agent/src/core/tools/path-utils.ts`
- `packages/pi-coding-agent/src/core/tools/path-utils.ts:55` — `normalizeMsysPath(p: string)` — exported: **no** — Implements logic starting with: { if (process.platform === "win32" && /^\/[a-zA-Z]\//.test(p)) { return `${p[1]!.toUpperCase()}:\\${p.slice(3).replace(/\//g, "\\")}`; } return p; } (missing: @param p, @returns)
- `packages/pi-coding-agent/src/core/tools/path-utils.ts:66` — `resolveToCwd(filePath: string, cwd: string)` — exported: **yes** — Implements logic starting with: { const expanded = normalizeMsysPath(expandPath(filePath)); if (isAbsolute(expanded)) { return expanded; } return resolvePath(cwd, expanded); } (missing: @param filePath, @param cwd, @returns)

### `packages/pi-coding-agent/src/core/tools/tool-compatibility-registry.ts`
- `packages/pi-coding-agent/src/core/tools/tool-compatibility-registry.ts:47` — `registerToolCompatibility(toolName: string, compatibility: ToolCompatibility)` — exported: **yes** — Implements logic starting with: { registry.set(toolName, compatibility); } (missing: @param toolName, @param compatibility)
- `packages/pi-coding-agent/src/core/tools/tool-compatibility-registry.ts:56` — `getToolCompatibility(toolName: string)` — exported: **yes** — Implements logic starting with: { return registry.get(toolName); } (missing: @param toolName, @returns)
- `packages/pi-coding-agent/src/core/tools/tool-compatibility-registry.ts:63` — `getAllToolCompatibility()` — exported: **yes** — Implements logic starting with: { return registry; } (missing: @returns)
- `packages/pi-coding-agent/src/core/tools/tool-compatibility-registry.ts:71` — `registerMcpToolCompatibility(toolName: string, overrides?: Partial<ToolCompatibility>)` — exported: **yes** — Implements logic starting with: { registry.set(toolName, { ...MCP_TOOL_DEFAULTS, ...overrides }); } (missing: @param toolName, @param overrides)

### `packages/pi-coding-agent/src/core/tools/truncate.ts`
- `packages/pi-coding-agent/src/core/tools/truncate.ts:52` — `formatSize(bytes: number)` — exported: **yes** — Implements logic starting with: { if (bytes < 1024) { return `${bytes}B`; } else if (bytes < 1024 * 1024) { return `${(bytes / 1024).toFixed(1)}KB`; } else { return `${(bytes / (1… (missing: @param bytes, @returns)
- `packages/pi-coding-agent/src/core/tools/truncate.ts:69` — `truncateHead(content: string, options: TruncationOptions = {})` — exported: **yes** — Implements logic starting with: { const maxLines = options.maxLines ?? DEFAULT_MAX_LINES; const maxBytes = options.maxBytes ?? DEFAULT_MAX_BYTES; const totalBytes = Buffer.byteLen… (missing: @param content, @param options, @returns)
- `packages/pi-coding-agent/src/core/tools/truncate.ts:159` — `truncateTail(content: string, options: TruncationOptions = {})` — exported: **yes** — Implements logic starting with: { const maxLines = options.maxLines ?? DEFAULT_MAX_LINES; const maxBytes = options.maxBytes ?? DEFAULT_MAX_BYTES; const totalBytes = Buffer.byteLen… (missing: @param content, @param options, @returns)
- `packages/pi-coding-agent/src/core/tools/truncate.ts:238` — `truncateStringToBytesFromEnd(str: string, maxBytes: number)` — exported: **no** — Implements logic starting with: { const buf = Buffer.from(str, "utf-8"); if (buf.length <= maxBytes) { return str; } // Start from the end, skip maxBytes back let start = buf.leng… (missing: @param str, @param maxBytes, @returns)
- `packages/pi-coding-agent/src/core/tools/truncate.ts:259` — `truncateLine(line: string, maxChars: number = GREP_MAX_LINE_LENGTH)` — exported: **yes** — Implements logic starting with: { if (line.length <= maxChars) { return { text: line, wasTruncated: false }; } return { text: `${line.slice(0, maxChars)}... [truncated]`, wasTrunc… (missing: @param line, @param maxChars, @returns)

### `packages/pi-coding-agent/src/main.ts`
- `packages/pi-coding-agent/src/main.ts:39` — `readPipedStdin()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/main.ts:111` — `resolveSessionPath(sessionArg: string, cwd: string, sessionDir?: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param sessionArg, @param cwd, @param sessionDir, @returns)
- `packages/pi-coding-agent/src/main.ts:139` — `promptConfirm(message: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param message, @returns)
- `packages/pi-coding-agent/src/main.ts:153` — `callSessionDirectoryHook(extensions: LoadExtensionsResult, cwd: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param extensions, @param cwd, @returns)

### `packages/pi-coding-agent/src/migrations.ts`
- `packages/pi-coding-agent/src/migrations.ts:135` — `migrateCommandsToPrompts(baseDir: string, label: string)` — exported: **no** — Logs diagnostic information. (missing: @param baseDir, @param label, @returns)
- `packages/pi-coding-agent/src/migrations.ts:203` — `checkDeprecatedExtensionDirs(baseDir: string, label: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param baseDir, @param label, @returns)
- `packages/pi-coding-agent/src/migrations.ts:238` — `migrateExtensionSystem(cwd: string)` — exported: **no** — Implements logic starting with: { const agentDir = getAgentDir(); const projectDir = join(cwd, CONFIG_DIR_NAME); // Migrate commands/ to prompts/ migrateCommandsToPrompts(agentDir… (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/migrations.ts:258` — `showDeprecationWarnings(warnings: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param warnings)
- `packages/pi-coding-agent/src/migrations.ts:286` — `runMigrations(cwd: string = process.cwd())` — exported: **yes** — Implements logic starting with: { const migratedAuthProviders = migrateAuthToAuthJson(); migrateSessionsFromAgentRoot(); migrateToolsToBin(); const deprecationWarnings = migrateEx… (missing: @param cwd)

### `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/assistant-message.ts:20` — `class AssistantMessageComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:74` — `BashExecutionComponent.setExpanded(expanded: boolean)` — exported: **yes** — Implements logic starting with: { this.expanded = expanded; this.updateDisplay(); } (missing: @param expanded)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:263` — `BashExecutionComponent.getOutput()` — exported: **yes** — Implements logic starting with: { return this.outputLines.join("\n"); } (missing: @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/bash-execution.ts:270` — `BashExecutionComponent.getCommand()` — exported: **yes** — Implements logic starting with: { return this.command; } (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/bordered-loader.ts:7` — `class BorderedLoader` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/branch-summary-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/branch-summary-message.ts:10` — `class BranchSummaryMessageComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/compaction-summary-message.ts:13` — `class CompactionSummaryMessageComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/custom-editor.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/custom-editor.ts:7` — `class CustomEditor` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/custom-editor.ts:26` — `CustomEditor.onAction(action: AppAction, handler: () => void)` — exported: **yes** — Implements logic starting with: { this.actionHandlers.set(action, handler); } (missing: @param action, @param handler)

### `packages/pi-coding-agent/src/modes/interactive/components/custom-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/custom-message.ts:12` — `class CustomMessageComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/diff.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/diff.ts:8` — `parseDiffLine(line: string)` — exported: **no** — Implements logic starting with: { const match = line.match(/^([+\- ])(\s*\d*)\s(.*)$/); if (!match) return null; return { prefix: match[1], lineNum: match[2], content: match[3] };… (missing: @param line, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/diff.ts:17` — `replaceTabs(text: string)` — exported: **no** — Implements logic starting with: { return text.replace(/\t/g, " "); } (missing: @param text, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/diff.ts:26` — `renderIntraLineDiff(oldContent: string, newContent: string)` — exported: **no** — Implements logic starting with: { const wordDiff = Diff.diffWords(oldContent, newContent); let removedLine = ""; let addedLine = ""; let isFirstRemoved = true; let isFirstAdded =… (missing: @param oldContent, @param newContent, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/diff.ts:84` — `renderDiff(diffText: string, options: RenderDiffOptions = {})` — exported: **yes** — Implements logic starting with: { const lines = diffText.split("\n"); const result: string[] = []; const lang = options.filePath ? getLanguageFromPath(options.filePath) : undefine… (missing: @param diffText, @param options, @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:13` — `class DynamicBorder` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/dynamic-border.ts:37` — `DynamicBorder.startSpinner(ui: TUI, colorFn: (str: string) => string)` — exported: **yes** — Implements logic starting with: { this.stopSpinner(); this.spinnerColorFn = colorFn; this.spinnerIndex = 0; this.spinnerInterval = setInterval(() => { this.spinnerIndex = (this.sp… (missing: @param ui, @param colorFn)

### `packages/pi-coding-agent/src/modes/interactive/components/footer.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:15` — `sanitizeStatusText(text: string)` — exported: **no** — Implements logic starting with: { // Replace newlines, tabs, carriage returns with space, then collapse multiple spaces return text .replace(/[\r\n\t]/g, " ") .replace(/ +/g, " ")… (missing: @param text, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:26` — `formatTokens(count: number)` — exported: **no** — Implements logic starting with: { if (count < 1000) return count.toString(); if (count < 10000) return `${(count / 1000).toFixed(1)}k`; if (count < 1000000) return `${Math.round(c… (missing: @param count, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:39` — `formatPromptCost(cost: number)` — exported: **yes** — Implements logic starting with: { if (cost < 0.001) return `$${cost.toFixed(4)}`; if (cost < 0.01) return `$${cost.toFixed(3)}`; if (cost < 1) return `$${cost.toFixed(3)}`; return… (missing: @param cost, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/footer.ts:50` — `class FooterComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts:15` — `formatKeyForDisplay(key: string)` — exported: **yes** — Implements logic starting with: { if (!isMac) return key; return key.replace(/\balt\+/gi, "⌥"); } (missing: @param key, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts:24` — `formatKeys(keys: KeyId[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param keys, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts:33` — `editorKey(action: EditorAction)` — exported: **yes** — Implements logic starting with: { return formatKeys(getEditorKeybindings().getKeys(action)); } (missing: @param action, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/keybinding-hints.ts:40` — `appKey(keybindings: KeybindingsManager, action: AppAction)` — exported: **yes** — Implements logic starting with: { return formatKeys(keybindings.getKeys(action)); } (missing: @param keybindings, @param action, @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:39` — `class LoginDialogComponent` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:142` — `LoginDialogComponent.showAuth(url: string, instructions?: string)` — exported: **yes** — Implements logic starting with: { this.contentContainer.clear(); this.contentContainer.addChild(new Spacer(1)); // Truncate the visible URL text so it never wraps (which would bre… (missing: @param url, @param instructions)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:182` — `LoginDialogComponent.showManualInput(prompt: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param prompt, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:202` — `LoginDialogComponent.showPrompt(message: string, placeholder?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param placeholder, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:228` — `LoginDialogComponent.showWaiting(message: string)` — exported: **yes** — Implements logic starting with: { this.contentContainer.addChild(new Spacer(1)); this.contentContainer.addChild(new Text(theme.fg("dim", message), 1, 0)); this.contentContainer.ad… (missing: @param message)
- `packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts:238` — `LoginDialogComponent.showProgress(message: string)` — exported: **yes** — Implements logic starting with: { this.contentContainer.addChild(new Text(theme.fg("dim", message), 1, 0)); this.tui.requestRender(); } (missing: @param message)

### `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:27` — `providerAuthBadge(authMode?: ProviderAuthMode)` — exported: **yes** — Implements logic starting with: { switch (authMode) { case "apiKey": return "API key"; case "oauth": return "OAuth"; case "externalCli": return "CLI"; default: return ""; } } (missing: @param authMode, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/model-selector.ts:80` — `class ModelSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/oauth-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/oauth-selector.ts:11` — `class OAuthSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/scoped-models-selector.ts:97` — `class ScopedModelsSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:207` — `buildSessionTree(sessions: SessionInfo[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param sessions, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:242` — `flattenSessionTree(roots: SessionTreeNode[])` — exported: **no** — Implements logic starting with: { const result: FlatSessionNode[] = []; const walk = (node: SessionTreeNode, depth: number, ancestorContinues: boolean[], isLast: boolean): void =>… (missing: @param roots, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:615` — `deleteSessionFile(sessionPath: string)` — exported: **no** — Executes a subprocess command. (missing: @param sessionPath, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/session-selector.ts:655` — `class SessionSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts:83` — `class SelectSubmenu` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/settings-selector.ts:145` — `class SettingsSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/show-images-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/show-images-selector.ts:8` — `class ShowImagesSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/skill-invocation-message.ts:13` — `class SkillInvocationMessageComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/theme-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/theme-selector.ts:8` — `class ThemeSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/thinking-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/thinking-selector.ts:18` — `class ThinkingSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/timestamp.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/timestamp.ts:39` — `formatTimestamp(timestamp: number, format: TimestampFormat = "date-time-iso")` — exported: **yes** — Implements logic starting with: { const d = new Date(timestamp); switch (format) { case "date-time-iso": return `${isoDate(d)} ${isoTime(d)}`; case "date-time-us": return `${usDat… (missing: @param timestamp, @param format, @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:40` — `replaceTabs(text: string)` — exported: **no** — Implements logic starting with: { return text.replace(/\t/g, " "); } (missing: @param text, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:48` — `normalizeDisplayText(text: string)` — exported: **no** — Implements logic starting with: { return text.replace(/\r/g, ""); } (missing: @param text, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:53` — `str(value: unknown)` — exported: **no** — Implements logic starting with: { if (typeof value === "string") return value; if (value == null) return ""; return null; // Invalid type } (missing: @param value, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:65` — `parseMcpToolName(name: string)` — exported: **no** — Implements logic starting with: { if (!name.startsWith("mcp__")) return null; const rest = name.slice("mcp__".length); const delim = rest.indexOf("__"); if (delim <= 0 || delim ==… (missing: @param name, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:78` — `prettifyToolName(name: string, label?: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param name, @param label, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:256` — `formatCompactArgs(args: unknown, expanded: boolean)` — exported: **no** — Serializes a value to JSON. (missing: @param args, @param expanded, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:306` — `class ToolExecutionComponent` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts:591` — `ToolExecutionComponent.completeWithError(message?: string)` — exported: **yes** — Implements logic starting with: { this.isPartial = false; this.endedAt = this.endedAt ?? Date.now(); if (this.result) { let content = this.result.content; if (message) { const alr… (missing: @param message)

### `packages/pi-coding-agent/src/modes/interactive/components/tree-render-utils.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/tree-render-utils.ts:20` — `buildTreePrefix(ancestorContinues: boolean[], isLast: boolean, depth: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ancestorContinues, @param isLast, @param depth, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-render-utils.ts:42` — `computeScrollWindow(selectedIndex: number, totalItems: number, maxVisible: number)` — exported: **yes** — Implements logic starting with: { const startIndex = Math.max(0, Math.min(selectedIndex - Math.floor(maxVisible / 2), totalItems - maxVisible)); const endIndex = Math.min(startInd… (missing: @param selectedIndex, @param totalItems, @param maxVisible, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-render-utils.ts:56` — `renderCursor(isSelected: boolean)` — exported: **yes** — Implements logic starting with: { return isSelected ? theme.fg("accent", "\u203A ") : " "; } (missing: @param isSelected, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-render-utils.ts:63` — `applyRowHighlight(line: string, isSelected: boolean, width: number)` — exported: **yes** — Implements logic starting with: { const truncated = truncateToWidth(line, width); return isSelected ? theme.bg("selectedBg", truncated) : truncated; } (missing: @param line, @param isSelected, @param width, @returns)
- `packages/pi-coding-agent/src/modes/interactive/components/tree-render-utils.ts:73` — `renderScrollPosition(selectedIndex: number, totalItems: number, width: number, suffixLabel?: string)` — exported: **yes** — Implements logic starting with: { const suffix = suffixLabel ?? ""; return truncateToWidth(theme.fg("muted", ` (${selectedIndex + 1}/${totalItems})${suffix}`), width); } (missing: @param selectedIndex, @param totalItems, @param width, @param suffixLabel, @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/tree-selector.ts:1075` — `class TreeSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/user-message-selector.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/user-message-selector.ts:109` — `class UserMessageSelectorComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/components/user-message.ts`
- `packages/pi-coding-agent/src/modes/interactive/components/user-message.ts:15` — `class UserMessageComponent` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.test.ts`
- `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.test.ts:13` — `createMockHost()` — exported: **no** — Builds an in-memory lookup structure. (missing: @returns)

### `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.ts`
- `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.ts:10` — `consumePendingImages(host: InteractiveModeStateHost)` — exported: **no** — Implements logic starting with: { if (host.pendingImages.length === 0) return undefined; const images = [...host.pendingImages]; host.pendingImages.length = 0; return images; } (missing: @param host, @returns)
- `packages/pi-coding-agent/src/modes/interactive/controllers/input-controller.ts:152` — `looksLikeFilePath(text: string)` — exported: **no** — Implements logic starting with: { const firstToken = text.split(/\s/)[0]; // Slash commands: /help, /gsd, /commit — single "/" at start only. // File paths: /Users/name/file, /hom… (missing: @param text, @returns)

### `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts`
- `packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts:141` — `buildAssistantReplaySegments(contentBlocks: Array<any>)` — exported: **yes** — Implements logic starting with: { const segments: AssistantReplaySegment[] = []; let runStart = -1; for (let i = 0; i < contentBlocks.length; i++) { const block = contentBlocks[i]… (missing: @param contentBlocks, @returns)

### `packages/pi-coding-agent/src/modes/interactive/slash-command-handlers.ts`
- `packages/pi-coding-agent/src/modes/interactive/slash-command-handlers.ts:122` — `dispatchSlashCommand(text: string, ctx: SlashCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param text, @param ctx)

### `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts`
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:747` — `ansi256ToHex(index: number)` — exported: **no** — Implements logic starting with: { // Basic colors (0-15) - approximate common terminal values const basicColors = [ "#000000", "#800000", "#008000", "#808000", "#000080", "#800080… (missing: @param index, @returns)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:791` — `getResolvedThemeColors(themeName?: string)` — exported: **yes** — Implements logic starting with: { const name = themeName ?? currentThemeName ?? getDefaultTheme(); const isLight = name === "light"; const themeJson = loadThemeJson(name); const r… (missing: @param themeName, @returns)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:818` — `getThemeExportColors(themeName?: string)` — exported: **yes** — Implements logic starting with: { const name = themeName ?? currentThemeName ?? getDefaultTheme(); try { const themeJson = loadThemeJson(name); const exportSection = themeJson.exp… (missing: @param themeName, @returns)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:990` — `highlightCode(code: string, lang?: string)` — exported: **yes** — Implements logic starting with: { if (!NATIVE_TUI_HIGHLIGHT_ENABLED) { return lang ? lightweightHighlightCode(code) : code.split("\n"); } const validLang = lang && supportsLanguag… (missing: @param code, @param lang, @returns)
- `packages/pi-coding-agent/src/modes/interactive/theme/theme.ts:1006` — `getLanguageFromPath(filePath: string)` — exported: **yes** — Implements logic starting with: { const ext = filePath.split(".").pop()?.toLowerCase(); if (!ext) return undefined; const extToLang: Record<string, string> = { ts: "typescript", t… (missing: @param filePath, @returns)

### `packages/pi-coding-agent/src/modes/interactive/utils/shorten-path.ts`
- `packages/pi-coding-agent/src/modes/interactive/utils/shorten-path.ts:7` — `shortenPath(path: unknown)` — exported: **yes** — Implements logic starting with: { if (typeof path !== "string" || !path) return ""; const home = os.homedir(); if (path.startsWith(home)) { return `~${path.slice(home.length)}`; }… (missing: @param path, @returns)

### `packages/pi-coding-agent/src/modes/print-mode.ts`
- `packages/pi-coding-agent/src/modes/print-mode.ts:31` — `runPrintMode(session: AgentSession, options: PrintModeOptions)` — exported: **yes** — Serializes a value to JSON. (missing: @param session, @param options)

### `packages/pi-coding-agent/src/modes/rpc/jsonl.ts`
- `packages/pi-coding-agent/src/modes/rpc/jsonl.ts:10` — `serializeJsonLine(value: unknown)` — exported: **yes** — Serializes a value to JSON. (missing: @param value, @returns)
- `packages/pi-coding-agent/src/modes/rpc/jsonl.ts:21` — `attachJsonlLineReader(stream: Readable, onLine: (line: string) => void)` — exported: **yes** — Implements logic starting with: { const decoder = new StringDecoder("utf8"); let buffer = ""; const emitLine = (line: string) => { onLine(line.endsWith("\r") ? line.slice(0, -1) :… (missing: @param stream, @param onLine, @returns)

### `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts`
- `packages/pi-coding-agent/src/modes/rpc/remote-terminal.ts:14` — `class RemoteTerminal` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts`
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:158` — `RpcClient.onEvent(listener: RpcEventListener)` — exported: **yes** — Implements logic starting with: { this.eventListeners.push(listener); return () => { const index = this.eventListeners.indexOf(listener); if (index !== -1) { this.eventListeners.s… (missing: @param listener, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:171` — `RpcClient.getStderr()` — exported: **yes** — Implements logic starting with: { return this.stderr; } (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:184` — `RpcClient.prompt(message: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:191` — `RpcClient.steer(message: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:198` — `RpcClient.followUp(message: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:222` — `RpcClient.getState()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:230` — `RpcClient.setModel(provider: string, modelId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param provider, @param modelId, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:238` — `RpcClient.cycleModel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:250` — `RpcClient.getAvailableModels()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:258` — `RpcClient.setThinkingLevel(level: ThinkingLevel)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param level)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:265` — `RpcClient.cycleThinkingLevel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:273` — `RpcClient.setSteeringMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mode)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:280` — `RpcClient.setFollowUpMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mode)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:287` — `RpcClient.compact(customInstructions?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param customInstructions, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:295` — `RpcClient.setAutoCompaction(enabled: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param enabled)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:302` — `RpcClient.setAutoRetry(enabled: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param enabled)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:316` — `RpcClient.bash(command: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param command, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:331` — `RpcClient.getSessionStats()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:339` — `RpcClient.exportHtml(outputPath?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param outputPath, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:348` — `RpcClient.switchSession(sessionPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param sessionPath)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:357` — `RpcClient.fork(entryId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param entryId)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:365` — `RpcClient.getForkMessages()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:373` — `RpcClient.getLastAssistantText()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:381` — `RpcClient.setSessionName(name: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param name)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:388` — `RpcClient.getMessages()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:396` — `RpcClient.getCommands()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:405` — `RpcClient.sendUIResponse(id: string, response: { value?: string; values?: string[]; confirmed?: boolean; cancelled?: boolean })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param id, @param response)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:420` — `RpcClient.init(options?: { clientId?: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:450` — `RpcClient.subscribe(events: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param events)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:462` — `RpcClient.waitForIdle(timeout = 60000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param timeout)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:482` — `RpcClient.collectEvents(timeout = 60000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param timeout, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-client.ts:504` — `RpcClient.promptAndWait(message: string, images?: ImageContent[], timeout = 60000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images, @param timeout, @returns)

### `packages/pi-coding-agent/src/modes/rpc/rpc-mode.ts`
- `packages/pi-coding-agent/src/modes/rpc/rpc-mode.ts:52` — `runRpcMode(session: AgentSession)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param session, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-mode.ts:170` — `createDialogPromise(opts: ExtensionUIDialogOptions | undefined, defaultValue: T, request: Record<string, unknown>, parseResponse: (response: RpcExtensionUIResponse) => T)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param opts, @param defaultValue, @param request, @param parseResponse, @returns)

### `packages/pi-coding-agent/src/modes/rpc/rpc-protocol-v2.test.ts`
- `packages/pi-coding-agent/src/modes/rpc/rpc-protocol-v2.test.ts:29` — `collectLines(stream: PassThrough)` — exported: **no** — Parses JSON input into a structured value. (missing: @param stream, @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-protocol-v2.test.ts:42` — `writeLine(stream: PassThrough, obj: unknown)` — exported: **no** — Implements logic starting with: { stream.write(serializeJsonLine(obj)); } (missing: @param stream, @param obj)
- `packages/pi-coding-agent/src/modes/rpc/rpc-protocol-v2.test.ts:53` — `createMockProcess()` — exported: **no** — Implements logic starting with: { // Client writes to this → server reads from it const clientStdin = new PassThrough(); // Server writes to this → client reads from it const clie… (missing: @returns)
- `packages/pi-coding-agent/src/modes/rpc/rpc-protocol-v2.test.ts:63` — `tick(ms = 10)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ms)
- `packages/pi-coding-agent/src/modes/rpc/rpc-protocol-v2.test.ts:858` — `shouldEmit(filter: Set<string> | null, eventType: string)` — exported: **no** — Implements logic starting with: { return !filter || filter.has(eventType); } (missing: @param filter, @param eventType, @returns)

### `packages/pi-coding-agent/src/modes/shared/command-context-actions.ts`
- `packages/pi-coding-agent/src/modes/shared/command-context-actions.ts:20` — `createDefaultCommandContextActions(session: AgentSession)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param session, @returns)

### `packages/pi-coding-agent/src/resources/extensions/memory/index.ts`
- `packages/pi-coding-agent/src/resources/extensions/memory/index.ts:24` — `encodeCwd(cwd: string)` — exported: **no** — Implements logic starting with: { return createHash("sha256").update(cwd).digest("hex").slice(0, 16); } (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/index.ts:29` — `getMemoryDir(cwd: string)` — exported: **no** — Implements logic starting with: { return join(getAgentDir(), "memories", encodeCwd(cwd)); } (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/index.ts:34` — `getDbPath()` — exported: **no** — Implements logic starting with: { return join(getAgentDir(), "agent.db"); } (missing: @returns)

### `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts`
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:15` — `pLimit(concurrency: number)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param concurrency, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:94` — `readFirstLine(filePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param filePath, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:112` — `scanSessionFiles(sessionsDir: string, cwd: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param sessionsDir, @param cwd, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:162` — `filterSessionContent(filePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param filePath, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:323` — `runPhase1(storage: MemoryStorage, config: PipelineConfig, llmCall: LLMCallFn, workerId: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param storage, @param config, @param llmCall, @param workerId, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:401` — `runPhase2(storage: MemoryStorage, config: PipelineConfig, llmCall: LLMCallFn, workerId: string)` — exported: **no** — Performs filesystem I/O. (missing: @param storage, @param config, @param llmCall, @param workerId, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:485` — `runStartup(storage: MemoryStorage, config: PipelineConfig, llmCall: LLMCallFn)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param storage, @param config, @param llmCall, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:534` — `getMemorySummary(memoryDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param memoryDir, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/pipeline.ts:556` — `getFullMemory(memoryDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param memoryDir, @returns)

### `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts`
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:153` — `MemoryStorage.upsertThreads(threads: Array<{ threadId: string; filePath: string; fileSize: number; fileMtime: number; cwd: string; }>)` — exported: **yes** — Implements logic starting with: { let inserted = 0; let updated = 0; let skipped = 0; for (const t of threads) { const existing = this.queryOne<{ file_size: number; file_mtime: nu… (missing: @param threads, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:207` — `MemoryStorage.claimStage1Jobs(workerId: string, limit: number, leaseSeconds: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param workerId, @param limit, @param leaseSeconds, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:248` — `MemoryStorage.completeStage1Job(threadId: string, output: string)` — exported: **yes** — Implements logic starting with: { this.db.run( "UPDATE jobs SET status = 'done', updated_at = datetime('now') WHERE thread_id = ? AND phase = 'stage1' AND status = 'claimed'", [th… (missing: @param threadId, @param output)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:267` — `MemoryStorage.failStage1Job(threadId: string, errorMessage: string)` — exported: **yes** — Implements logic starting with: { this.db.run( "UPDATE jobs SET status = 'error', error_message = ?, updated_at = datetime('now') WHERE thread_id = ? AND phase = 'stage1' AND stat… (missing: @param threadId, @param errorMessage)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:283` — `MemoryStorage.tryClaimGlobalPhase2Job(workerId: string, leaseSeconds: number)` — exported: **yes** — Implements logic starting with: { const token = randomUUID(); const expiresAt = new Date(Date.now() + leaseSeconds * 1000).toISOString(); const pendingStage1 = this.queryOne<{ cnt… (missing: @param workerId, @param leaseSeconds, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:327` — `MemoryStorage.completePhase2Job(jobId: string)` — exported: **yes** — Implements logic starting with: { this.db.run( "UPDATE jobs SET status = 'done', updated_at = datetime('now') WHERE id = ? AND phase = 'stage2'", [jobId], ); this.schedulePersist(… (missing: @param jobId)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:338` — `MemoryStorage.getStage1Outputs()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:352` — `MemoryStorage.getStage1OutputsForCwd(cwd: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param cwd, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:369` — `MemoryStorage.getThread(threadId: string)` — exported: **yes** — Implements logic starting with: { return this.queryOne<ThreadRow>( "SELECT * FROM threads WHERE thread_id = ?", [threadId], ); } (missing: @param threadId, @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:379` — `MemoryStorage.getStats()` — exported: **yes** — Implements logic starting with: { const threads = this.queryOne<{ total: number; pending: number; done: number; errors: number }>(` SELECT COUNT(*) as total, SUM(CASE WHEN status… (missing: @returns)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:427` — `MemoryStorage.clearForCwd(cwd: string)` — exported: **yes** — Implements logic starting with: { this.db.run( "DELETE FROM stage1_outputs WHERE thread_id IN (SELECT thread_id FROM threads WHERE cwd = ?)", [cwd], ); this.db.run( "DELETE FROM j… (missing: @param cwd)
- `packages/pi-coding-agent/src/resources/extensions/memory/storage.ts:443` — `MemoryStorage.resetAllForCwd(cwd: string)` — exported: **yes** — Implements logic starting with: { this.db.run( "DELETE FROM stage1_outputs WHERE thread_id IN (SELECT thread_id FROM threads WHERE cwd = ?)", [cwd], ); this.db.run( "DELETE FROM j… (missing: @param cwd)

### `packages/pi-coding-agent/src/utils/changelog.ts`
- `packages/pi-coding-agent/src/utils/changelog.ts:14` — `parseChangelog(changelogPath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param changelogPath, @returns)
- `packages/pi-coding-agent/src/utils/changelog.ts:76` — `compareVersions(v1: ChangelogEntry, v2: ChangelogEntry)` — exported: **no** — Implements logic starting with: { if (v1.major !== v2.major) return v1.major - v2.major; if (v1.minor !== v2.minor) return v1.minor - v2.minor; return v1.patch - v2.patch; } (missing: @param v1, @param v2, @returns)
- `packages/pi-coding-agent/src/utils/changelog.ts:85` — `getNewEntries(entries: ChangelogEntry[], lastVersion: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param entries, @param lastVersion, @returns)

### `packages/pi-coding-agent/src/utils/clipboard-image.ts`
- `packages/pi-coding-agent/src/utils/clipboard-image.ts:66` — `convertToPng(bytes: Uint8Array)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param bytes, @returns)

### `packages/pi-coding-agent/src/utils/error.ts`
- `packages/pi-coding-agent/src/utils/error.ts:4` — `getErrorMessage(err: unknown)` — exported: **yes** — Implements logic starting with: { return err instanceof Error ? err.message : String(err); } (missing: @param err, @returns)

### `packages/pi-coding-agent/src/utils/git.ts`
- `packages/pi-coding-agent/src/utils/git.ts:134` — `parseGitUrl(source: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param source, @returns)

### `packages/pi-coding-agent/src/utils/image-convert.ts`
- `packages/pi-coding-agent/src/utils/image-convert.ts:7` — `convertToPng(base64Data: string, mimeType: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base64Data, @param mimeType, @returns)

### `packages/pi-coding-agent/src/utils/image-resize.ts`
- `packages/pi-coding-agent/src/utils/image-resize.ts:33` — `pickSmaller(a: { buffer: Uint8Array; mimeType: string }, b: { buffer: Uint8Array; mimeType: string })` — exported: **no** — Implements logic starting with: { return a.buffer.length <= b.buffer.length ? a : b; } (missing: @param a, @param b, @returns)
- `packages/pi-coding-agent/src/utils/image-resize.ts:52` — `resizeImage(img: ImageContent, options?: ImageResizeOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param img, @param options, @returns)
- `packages/pi-coding-agent/src/utils/image-resize.ts:218` — `formatDimensionNote(result: ResizedImage)` — exported: **yes** — Implements logic starting with: { if (!result.wasResized) { return undefined; } const scale = result.originalWidth / result.width; return `[Image: original ${result.originalWidth}… (missing: @param result, @returns)

### `packages/pi-coding-agent/src/utils/path-display.ts`
- `packages/pi-coding-agent/src/utils/path-display.ts:34` — `toPosixPath(fsPath: string)` — exported: **yes** — Implements logic starting with: { return fsPath.replaceAll("\\", "/"); } (missing: @param fsPath, @returns)

### `packages/pi-coding-agent/src/utils/shell.ts`
- `packages/pi-coding-agent/src/utils/shell.ts:12` — `findBashOnPath()` — exported: **no** — Executes a subprocess command. (missing: @returns)
- `packages/pi-coding-agent/src/utils/shell.ts:51` — `getShellConfig()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `packages/pi-coding-agent/src/utils/shell.ts:127` — `sanitizeCommand(command: string)` — exported: **yes** — Implements logic starting with: { if (process.platform !== "win32") return command; return command.replace(/(\d*>>?) *\bNUL\b(?=\s|;|\||&|\)|$)/gi, "$1 /dev/null"); } (missing: @param command, @returns)
- `packages/pi-coding-agent/src/utils/shell.ts:154` — `sanitizeBinaryOutput(str: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param str, @returns)
- `packages/pi-coding-agent/src/utils/shell.ts:189` — `killProcessTree(pid: number)` — exported: **yes** — Executes a subprocess command. (missing: @param pid)

### `packages/pi-coding-agent/src/utils/sleep.ts`
- `packages/pi-coding-agent/src/utils/sleep.ts:4` — `sleep(ms: number, signal?: AbortSignal)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ms, @param signal)

### `packages/pi-tui/src/components/box.ts`
- `packages/pi-tui/src/components/box.ts:14` — `class Box` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/components/cancellable-loader.ts`
- `packages/pi-tui/src/components/cancellable-loader.ts:13` — `class CancellableLoader` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-tui/src/components/cancellable-loader.ts:20` — `CancellableLoader.get signal()` — exported: **yes** — Implements logic starting with: { return this.abortController.signal; } (missing: @returns)
- `packages/pi-tui/src/components/cancellable-loader.ts:25` — `CancellableLoader.get aborted()` — exported: **yes** — Implements logic starting with: { return this.abortController.signal.aborted; } (missing: @returns)

### `packages/pi-tui/src/components/editor.ts`
- `packages/pi-tui/src/components/editor.ts:272` — `Editor.addToHistory(text: string)` — exported: **yes** — Implements logic starting with: { const trimmed = text.trim(); if (!trimmed) return; // Don't add consecutive duplicates if (this.history.length > 0 && this.history[0] === trimmed… (missing: @param text)
- `packages/pi-tui/src/components/editor.ts:863` — `Editor.getExpandedText()` — exported: **yes** — Implements logic starting with: { let result = this.state.lines.join("\n"); for (const [pasteId, pasteContent] of this.pastes) { const markerRegex = new RegExp(`\\[paste #${pasteI… (missing: @returns)
- `packages/pi-tui/src/components/editor.ts:895` — `Editor.insertTextAtCursor(text: string)` — exported: **yes** — Implements logic starting with: { if (!text) return; this.pushUndoSnapshot(); this.lastAction = null; this.historyIndex = -1; this.insertTextAtCursorInternal(text); } (missing: @param text)

### `packages/pi-tui/src/components/image.ts`
- `packages/pi-tui/src/components/image.ts:66` — `Image.setOnDimensionsResolved(cb: () => void)` — exported: **yes** — Implements logic starting with: { this.onDimensionsResolved = cb; } (missing: @param cb)
- `packages/pi-tui/src/components/image.ts:71` — `Image.getImageId()` — exported: **yes** — Implements logic starting with: { return this.imageId; } (missing: @returns)
- `packages/pi-tui/src/components/image.ts:76` — `Image.getDimensions()` — exported: **yes** — Implements logic starting with: { return this.dimensionsResolved ? this.dimensions : undefined; } (missing: @returns)

### `packages/pi-tui/src/components/input.ts`
- `packages/pi-tui/src/components/input.ts:18` — `class Input` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/components/loader.ts`
- `packages/pi-tui/src/components/loader.ts:9` — `class Loader` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/components/settings-list.ts`
- `packages/pi-tui/src/components/settings-list.ts:70` — `SettingsList.updateValue(id: string, newValue: string)` — exported: **yes** — Implements logic starting with: { const item = this.items.find((i) => i.id === id); if (item) { item.currentValue = newValue; } } (missing: @param id, @param newValue)

### `packages/pi-tui/src/components/spacer.ts`
- `packages/pi-tui/src/components/spacer.ts:6` — `class Spacer` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/components/text.ts`
- `packages/pi-tui/src/components/text.ts:7` — `class Text` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/components/truncated-text.ts`
- `packages/pi-tui/src/components/truncated-text.ts:7` — `class TruncatedText` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/fuzzy.ts`
- `packages/pi-tui/src/fuzzy.ts:95` — `fuzzyFilter(items: T[], query: string, getText: (item: T) => string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param items, @param query, @param getText, @returns)

### `packages/pi-tui/src/keybindings.ts`
- `packages/pi-tui/src/keybindings.ts:125` — `class EditorKeybindingsManager` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-tui/src/keybindings.ts:153` — `EditorKeybindingsManager.matches(data: string, action: EditorAction)` — exported: **yes** — Implements logic starting with: { const keys = this.actionToKeys.get(action); if (!keys) return false; for (const key of keys) { if (matchesKey(data, key)) return true; } return f… (missing: @param data, @param action, @returns)
- `packages/pi-tui/src/keybindings.ts:165` — `EditorKeybindingsManager.getKeys(action: EditorAction)` — exported: **yes** — Implements logic starting with: { return this.actionToKeys.get(action) ?? []; } (missing: @param action, @returns)
- `packages/pi-tui/src/keybindings.ts:172` — `EditorKeybindingsManager.setConfig(config: EditorKeybindingsConfig)` — exported: **yes** — Implements logic starting with: { this.buildMaps(config); } (missing: @param config)

### `packages/pi-tui/src/keys.ts`
- `packages/pi-tui/src/keys.ts:31` — `setKittyProtocolActive(active: boolean)` — exported: **yes** — Implements logic starting with: { _kittyProtocolActive = active; } (missing: @param active)
- `packages/pi-tui/src/keys.ts:38` — `isKittyProtocolActive()` — exported: **yes** — Implements logic starting with: { return _kittyProtocolActive; } (missing: @returns)
- `packages/pi-tui/src/keys.ts:453` — `hasKittyEventType(data: string, eventType: number)` — exported: **no** — Implements logic starting with: { if (data.includes("\x1b[200~")) { return false; } const marker = `:${eventType}`; return ( data.includes(`${marker}u`) || data.includes(`${marker… (missing: @param data, @param eventType, @returns)
- `packages/pi-tui/src/keys.ts:478` — `isKeyRepeat(data: string)` — exported: **yes** — Implements logic starting with: { return hasKittyEventType(data, 2); } (missing: @param data, @returns)
- `packages/pi-tui/src/keys.ts:603` — `matchesModifyOtherKeys(data: string, expectedKeycode: number, expectedModifier: number)` — exported: **no** — Implements logic starting with: { const parsed = parseModifyOtherKeysSequence(data); if (!parsed) return false; return parsed.codepoint === expectedKeycode && parsed.modifier ===… (missing: @param data, @param expectedKeycode, @param expectedModifier, @returns)
- `packages/pi-tui/src/keys.ts:622` — `rawCtrlChar(key: string)` — exported: **no** — Implements logic starting with: { const char = key.toLowerCase(); const code = char.charCodeAt(0); if ((code >= 97 && code <= 122) || char === "[" || char === "\\" || char === "]"… (missing: @param key, @returns)
- `packages/pi-tui/src/keys.ts:683` — `matchesKey(data: string, keyId: KeyId)` — exported: **yes** — Implements logic starting with: { const parsed = parseKeyId(keyId); if (!parsed) return false; const { key, ctrl, shift, alt } = parsed; let modifier = 0; if (shift) modifier |= M… (missing: @returns)
- `packages/pi-tui/src/keys.ts:1034` — `formatParsedKey(codepoint: number, modifier: number, baseLayoutKey?: number)` — exported: **no** — Implements logic starting with: { // Use base layout key only when codepoint is not a recognized Latin // letter (a-z), digit (0-9), or symbol (/, -, [, ;, etc.). For those, // th… (missing: @param codepoint, @param modifier, @param baseLayoutKey)

### `packages/pi-tui/src/kill-ring.ts`
- `packages/pi-tui/src/kill-ring.ts:8` — `class KillRing` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-tui/src/kill-ring.ts:31` — `KillRing.peek()` — exported: **yes** — Implements logic starting with: { return this.ring.length > 0 ? this.ring[this.ring.length - 1] : undefined; } (missing: @returns)

### `packages/pi-tui/src/overlay-layout.ts`
- `packages/pi-tui/src/overlay-layout.ts:16` — `parseSizeValue(value: SizeValue | undefined, referenceSize: number)` — exported: **yes** — Implements logic starting with: { if (value === undefined) return undefined; if (typeof value === "number") return value; // Parse percentage string like "50%" const match = value… (missing: @param value, @param referenceSize, @returns)
- `packages/pi-tui/src/overlay-layout.ts:86` — `resolveOverlayLayout(options: OverlayOptions | undefined, overlayHeight: number, termWidth: number, termHeight: number)` — exported: **yes** — Implements logic starting with: { const opt = options ?? {}; // Parse margin (clamp to non-negative) const margin = typeof opt.margin === "number" ? { top: opt.margin, right: opt.… (missing: @param options, @param overlayHeight, @param termWidth, @param termHeight, @returns)
- `packages/pi-tui/src/overlay-layout.ts:191` — `applyLineResets(lines: string[])` — exported: **yes** — Implements logic starting with: { for (let i = 0; i < lines.length; i++) { const line = lines[i]; if (!isImageLine(line)) { lines[i] = line + SEGMENT_RESET; } } return lines; } (missing: @param lines, @returns)
- `packages/pi-tui/src/overlay-layout.ts:202` — `compositeLineAt(baseLine: string, overlayLine: string, startCol: number, overlayWidth: number, totalWidth: number)` — exported: **yes** — Implements logic starting with: { if (isImageLine(baseLine)) return baseLine; // Single pass through baseLine extracts both before and after segments const afterStart = startCol +… (missing: @param baseLine, @param overlayLine, @param startCol, @param overlayWidth, @param totalWidth, @returns)
- `packages/pi-tui/src/overlay-layout.ts:262` — `isOverlayVisible(entry: OverlayEntry, termWidth: number, termHeight: number)` — exported: **yes** — Implements logic starting with: { if (entry.hidden) return false; if (entry.options?.visible) { return entry.options.visible(termWidth, termHeight); } return true; } (missing: @param entry, @param termWidth, @param termHeight, @returns)
- `packages/pi-tui/src/overlay-layout.ts:278` — `compositeOverlays(lines: string[], overlayStack: OverlayEntry[], termWidth: number, termHeight: number, maxLinesRendered: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param lines, @param overlayStack, @param termWidth, @param termHeight, @param maxLinesRendered, @returns)

### `packages/pi-tui/src/stdin-buffer.ts`
- `packages/pi-tui/src/stdin-buffer.ts:29` — `isCompleteSequence(data: string)` — exported: **no** — Implements logic starting with: { if (!data.startsWith(ESC)) { return "not-escape"; } if (data.length === 1) { return "incomplete"; } const afterEsc = data.slice(1); // CSI sequen… (missing: @param data, @returns)
- `packages/pi-tui/src/stdin-buffer.ts:84` — `isCompleteCsiSequence(data: string)` — exported: **no** — Implements logic starting with: { if (!data.startsWith(`${ESC}[`)) { return "complete"; } // Need at least ESC [ and one more character if (data.length < 3) { return "incomplete";… (missing: @param data, @returns)
- `packages/pi-tui/src/stdin-buffer.ts:132` — `isCompleteOscSequence(data: string)` — exported: **no** — Implements logic starting with: { if (!data.startsWith(`${ESC}]`)) { return "complete"; } // OSC sequences end with ST (ESC \) or BEL (\x07) if (data.endsWith(`${ESC}\\`) || data.… (missing: @param data, @returns)
- `packages/pi-tui/src/stdin-buffer.ts:150` — `isCompleteDcsSequence(data: string)` — exported: **no** — Implements logic starting with: { if (!data.startsWith(`${ESC}P`)) { return "complete"; } // DCS sequences end with ST (ESC \) if (data.endsWith(`${ESC}\\`)) { return "complete";… (missing: @param data, @returns)
- `packages/pi-tui/src/stdin-buffer.ts:168` — `isCompleteApcSequence(data: string)` — exported: **no** — Implements logic starting with: { if (!data.startsWith(`${ESC}_`)) { return "complete"; } // APC sequences end with ST (ESC \) if (data.endsWith(`${ESC}\\`)) { return "complete";… (missing: @param data, @returns)
- `packages/pi-tui/src/stdin-buffer.ts:184` — `extractCompleteSequences(buffer: string)` — exported: **no** — Implements logic starting with: { const sequences: string[] = []; let pos = 0; while (pos < buffer.length) { const remaining = buffer.slice(pos); // Try to extract a sequence star… (missing: @param buffer, @returns)
- `packages/pi-tui/src/stdin-buffer.ts:248` — `class StdinBuffer` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/terminal-image.ts`
- `packages/pi-tui/src/terminal-image.ts:106` — `allocateImageId()` — exported: **yes** — Implements logic starting with: { // Use random ID in range [1, 0xffffffff] to avoid collisions return Math.floor(Math.random() * 0xfffffffe) + 1; } (missing: @returns)
- `packages/pi-tui/src/terminal-image.ts:158` — `deleteKittyImage(imageId: number)` — exported: **yes** — Implements logic starting with: { return `\x1b_Ga=d,d=I,i=${imageId}\x1b\\`; } (missing: @param imageId, @returns)
- `packages/pi-tui/src/terminal-image.ts:166` — `deleteAllKittyImages()` — exported: **yes** — Implements logic starting with: { return `\x1b_Ga=d,d=A\x1b\\`; } (missing: @returns)
- `packages/pi-tui/src/terminal-image.ts:211` — `getImageDimensions(base64Data: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base64Data, @returns)

### `packages/pi-tui/src/terminal.ts`
- `packages/pi-tui/src/terminal.ts:58` — `class ProcessTerminal` — exported: **yes** — Defines an API surface. (missing: @returns)

### `packages/pi-tui/src/tui.ts`
- `packages/pi-tui/src/tui.ts:64` — `isFocusable(component: Component | null)` — exported: **yes** — Implements logic starting with: { return component !== null && "focused" in component; } (missing: @param component, @returns)
- `packages/pi-tui/src/tui.ts:170` — `class Container` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-tui/src/tui.ts:242` — `class TUI` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-tui/src/tui.ts:310` — `TUI.setClearOnShrink(enabled: boolean)` — exported: **yes** — Implements logic starting with: { this.clearOnShrink = enabled; } (missing: @param enabled)
- `packages/pi-tui/src/tui.ts:332` — `TUI.showOverlay(component: Component, options?: OverlayOptions)` — exported: **yes** — Implements logic starting with: { const entry = { component, options, preFocus: this.focusedComponent, hidden: false, focusOrder: ++this.focusOrderCounter, }; this.overlayStack.pu… (missing: @param component, @param options, @returns)
- `packages/pi-tui/src/tui.ts:415` — `TUI.hasOverlay()` — exported: **yes** — Implements logic starting with: { return this.overlayStack.some((o) => this.isOverlayVisible(o)); } (missing: @returns)

### `packages/pi-tui/src/undo-stack.ts`
- `packages/pi-tui/src/undo-stack.ts:7` — `class UndoStack` — exported: **yes** — Defines an API surface. (missing: @returns)
- `packages/pi-tui/src/undo-stack.ts:11` — `UndoStack.push(state: S)` — exported: **yes** — Implements logic starting with: { this.stack.push(structuredClone(state)); } (missing: @param state)
- `packages/pi-tui/src/undo-stack.ts:16` — `UndoStack.pop()` — exported: **yes** — Implements logic starting with: { return this.stack.pop(); } (missing: @returns)

### `packages/pi-tui/src/utils.ts`
- `packages/pi-tui/src/utils.ts:16` — `getSegmenter()` — exported: **yes** — Implements logic starting with: { return segmenter; } (missing: @returns)
- `packages/pi-tui/src/utils.ts:25` — `isWhitespaceChar(char: string)` — exported: **yes** — Implements logic starting with: { return /\s/.test(char); } (missing: @param char, @returns)
- `packages/pi-tui/src/utils.ts:32` — `isPunctuationChar(char: string)` — exported: **yes** — Implements logic starting with: { return PUNCTUATION_REGEX.test(char); } (missing: @param char, @returns)
- `packages/pi-tui/src/utils.ts:44` — `visibleWidth(str: string)` — exported: **yes** — Implements logic starting with: { return nativeVisibleWidth(str); } (missing: @param str, @returns)
- `packages/pi-tui/src/utils.ts:63` — `ellipsisStringToKind(ellipsis: string)` — exported: **no** — Implements logic starting with: { if (ellipsis === "\u2026") return EllipsisKind.Unicode; if (ellipsis === "..." || ellipsis === undefined) return EllipsisKind.Ascii; if (ellipsis… (missing: @param ellipsis, @returns)
- `packages/pi-tui/src/utils.ts:95` — `sliceByColumn(line: string, startCol: number, length: number, strict = false)` — exported: **yes** — Implements logic starting with: { return sliceWithWidth(line, startCol, length, strict).text; } (missing: @param line, @param startCol, @param length, @returns)
- `packages/pi-tui/src/utils.ts:100` — `sliceWithWidth(line: string, startCol: number, length: number, strict = false)` — exported: **yes** — Implements logic starting with: { return nativeSliceWithWidth(line, startCol, length, strict); } (missing: @param line, @param startCol, @param length, @param strict, @returns)
- `packages/pi-tui/src/utils.ts:113` — `extractSegments(line: string, beforeEnd: number, afterStart: number, afterLen: number, strictAfter = false)` — exported: **yes** — Implements logic starting with: { return nativeExtractSegments(line, beforeEnd, afterStart, afterLen, strictAfter); } (missing: @param line, @param beforeEnd, @param afterStart, @param afterLen, @param strictAfter, @returns)

### `packages/rpc-client/src/jsonl.ts`
- `packages/rpc-client/src/jsonl.ts:10` — `serializeJsonLine(value: unknown)` — exported: **yes** — Serializes a value to JSON. (missing: @param value, @returns)
- `packages/rpc-client/src/jsonl.ts:21` — `attachJsonlLineReader(stream: Readable, onLine: (line: string) => void)` — exported: **yes** — Implements logic starting with: { const decoder = new StringDecoder("utf8"); let buffer = ""; const emitLine = (line: string) => { onLine(line.endsWith("\r") ? line.slice(0, -1) :… (missing: @param stream, @param onLine, @returns)

### `packages/rpc-client/src/rpc-client.ts`
- `packages/rpc-client/src/rpc-client.ts:168` — `RpcClient.onEvent(listener: RpcEventListener)` — exported: **yes** — Implements logic starting with: { this.eventListeners.push(listener); return () => { const index = this.eventListeners.indexOf(listener); if (index !== -1) { this.eventListeners.s… (missing: @param listener, @returns)
- `packages/rpc-client/src/rpc-client.ts:260` — `RpcClient.getStderr()` — exported: **yes** — Implements logic starting with: { return this.stderr; } (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:273` — `RpcClient.prompt(message: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images)
- `packages/rpc-client/src/rpc-client.ts:280` — `RpcClient.steer(message: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images)
- `packages/rpc-client/src/rpc-client.ts:287` — `RpcClient.followUp(message: string, images?: ImageContent[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images)
- `packages/rpc-client/src/rpc-client.ts:311` — `RpcClient.getState()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:319` — `RpcClient.setModel(provider: string, modelId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param provider, @param modelId, @returns)
- `packages/rpc-client/src/rpc-client.ts:327` — `RpcClient.cycleModel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:339` — `RpcClient.getAvailableModels()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:347` — `RpcClient.setThinkingLevel(level: ThinkingLevel)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param level)
- `packages/rpc-client/src/rpc-client.ts:354` — `RpcClient.cycleThinkingLevel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:362` — `RpcClient.setSteeringMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mode)
- `packages/rpc-client/src/rpc-client.ts:369` — `RpcClient.setFollowUpMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mode)
- `packages/rpc-client/src/rpc-client.ts:376` — `RpcClient.compact(customInstructions?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param customInstructions, @returns)
- `packages/rpc-client/src/rpc-client.ts:384` — `RpcClient.setAutoCompaction(enabled: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param enabled)
- `packages/rpc-client/src/rpc-client.ts:391` — `RpcClient.setAutoRetry(enabled: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param enabled)
- `packages/rpc-client/src/rpc-client.ts:405` — `RpcClient.bash(command: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param command, @returns)
- `packages/rpc-client/src/rpc-client.ts:420` — `RpcClient.getSessionStats()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:428` — `RpcClient.exportHtml(outputPath?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param outputPath, @returns)
- `packages/rpc-client/src/rpc-client.ts:437` — `RpcClient.switchSession(sessionPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param sessionPath)
- `packages/rpc-client/src/rpc-client.ts:446` — `RpcClient.fork(entryId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param entryId)
- `packages/rpc-client/src/rpc-client.ts:454` — `RpcClient.getForkMessages()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:462` — `RpcClient.getLastAssistantText()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:470` — `RpcClient.setSessionName(name: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param name)
- `packages/rpc-client/src/rpc-client.ts:478` — `RpcClient.getMessages()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:486` — `RpcClient.getCommands()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `packages/rpc-client/src/rpc-client.ts:495` — `RpcClient.sendUIResponse(id: string, response: { value?: string; values?: string[]; confirmed?: boolean; cancelled?: boolean })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param id, @param response)
- `packages/rpc-client/src/rpc-client.ts:510` — `RpcClient.init(options?: { clientId?: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)
- `packages/rpc-client/src/rpc-client.ts:540` — `RpcClient.subscribe(events: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param events)
- `packages/rpc-client/src/rpc-client.ts:552` — `RpcClient.waitForIdle(timeout = 60000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param timeout)
- `packages/rpc-client/src/rpc-client.ts:572` — `RpcClient.collectEvents(timeout = 60000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param timeout, @returns)
- `packages/rpc-client/src/rpc-client.ts:594` — `RpcClient.promptAndWait(message: string, images?: ImageContent[], timeout = 60000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message, @param images, @param timeout, @returns)

### `scripts/build-web-if-stale.cjs`
- `scripts/build-web-if-stale.cjs:40` — `newestMtime(dir)` — exported: **no** — Implements logic starting with: { let max = 0 let stack = [dir] while (stack.length > 0) { const current = stack.pop() let entries try { entries = readdirSync(current, { withFileT… (missing: @param dir, @returns)

### `scripts/check-skill-references.mjs`
- `scripts/check-skill-references.mjs:31` — `collectMdFiles(dir)` — exported: **no** — Implements logic starting with: { const results = []; for (const entry of readdirSync(dir, { withFileTypes: true })) { const full = join(dir, entry.name); if (entry.isDirectory())… (missing: @param dir, @returns)
- `scripts/check-skill-references.mjs:45` — `shouldValidate(ref)` — exported: **no** — Implements logic starting with: { // Must end with .md (we only validate markdown cross-references) if (!ref.endsWith(".md")) return false; // Skip URLs if (/^https?:\/\//.test(re… (missing: @param ref, @returns)
- `scripts/check-skill-references.mjs:78` — `stripAnchor(ref)` — exported: **no** — Implements logic starting with: { const idx = ref.indexOf("#"); return idx >= 0 ? ref.slice(0, idx) : ref; } (missing: @param ref, @returns)
- `scripts/check-skill-references.mjs:87` — `extractReferences(content)` — exported: **no** — Executes a subprocess command. (missing: @param content, @returns)

### `scripts/compile-tests.mjs`
- `scripts/compile-tests.mjs:85` — `copyAssets(srcDir, destDir)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param srcDir, @param destDir)

### `scripts/ensure-workspace-builds.cjs`
- `scripts/ensure-workspace-builds.cjs:25` — `newestSrcMtime(dir)` — exported: **yes** — Implements logic starting with: { if (!existsSync(dir)) return 0 let newest = 0 for (const entry of readdirSync(dir, { withFileTypes: true })) { if (entry.name === 'node_modules')… (missing: @param dir, @returns)

### `scripts/lib/workspace-manifest.cjs`
- `scripts/lib/workspace-manifest.cjs:30` — `getLinkablePackages()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `scripts/lib/workspace-manifest.cjs:77` — `getCorePackages()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `scripts/parallel-monitor.mjs`
- `scripts/parallel-monitor.mjs:252` — `extractCostFromNdjson(mid)` — exported: **no** — Performs filesystem I/O. (missing: @param mid, @returns)
- `scripts/parallel-monitor.mjs:317` — `respawnWorker(mid)` — exported: **no** — Performs filesystem I/O. (missing: @param mid, @returns)
- `scripts/parallel-monitor.mjs:363` — `healWorkers(workers)` — exported: **no** — Implements logic starting with: { if (!HEAL_MODE) return []; const events = []; const now = Date.now(); for (const wk of workers) { if (wk.alive) { // Worker is alive — reset its… (missing: @param workers, @returns)
- `scripts/parallel-monitor.mjs:516` — `queryRecentCompletions(mid)` — exported: **no** — Performs filesystem I/O. (missing: @param mid, @returns)

### `scripts/pr-risk-check.mjs`
- `scripts/pr-risk-check.mjs:88` — `buildAgentPrompt(systemRisks)` — exported: **no** — Transforms a collection into a derived result. (missing: @param systemRisks, @returns)
- `scripts/pr-risk-check.mjs:106` — `parseMap(mapPath)` — exported: **no** — Validates inputs and throws on failure. (missing: @param mapPath, @returns)
- `scripts/pr-risk-check.mjs:152` — `normalizePath(filePath)` — exported: **no** — Implements logic starting with: { return filePath .replace(/^\.\//, '') .replace(/\\/g, '/'); } (missing: @param filePath, @returns)
- `scripts/pr-risk-check.mjs:165` — `fileMatchesPattern(filePath, pattern)` — exported: **no** — Implements logic starting with: { const file = normalizePath(filePath); const pat = normalizePath(pattern); // Glob prefix: ends with /* or /** if (pat.endsWith('/*') || pat.endsW… (missing: @param filePath, @param pattern, @returns)
- `scripts/pr-risk-check.mjs:187` — `classifyFiles(changedFiles, mapEntries)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param changedFiles, @param mapEntries, @returns)
- `scripts/pr-risk-check.mjs:211` — `tierForSystem(system)` — exported: **no** — Implements logic starting with: { for (const tier of TIER_ORDER) { if (RISK_TIERS[tier].some(s => system.includes(s) || s.includes(system))) { return tier; } } return 'low'; } (missing: @param system, @returns)
- `scripts/pr-risk-check.mjs:223` — `overallRisk(allSystems)` — exported: **no** — Implements logic starting with: { let worst = 'low'; for (const system of allSystems) { const tier = tierForSystem(system); if (TIER_ORDER.indexOf(tier) < TIER_ORDER.indexOf(worst… (missing: @param allSystems, @returns)

### `src/bundled-resource-path.ts`
- `src/bundled-resource-path.ts:69` — `resolveBundledSourceResource(importUrl: string, ...segments: string[])` — exported: **yes** — Implements logic starting with: { const packageRoot = resolvePackageRoot(importUrl); return join(packageRoot, "src", "resources", ...segments); } (missing: @param importUrl, @param segments, @returns)

### `src/claude-cli-check.ts`
- `src/claude-cli-check.ts:19` — `buildClaudeSpawnInvocation(command: string, args: string[], platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { if (platform === 'win32') { return { command: 'cmd', args: ['/c', command, ...args] } } return { command, args } } (missing: @param command, @param args, @param platform, @returns)
- `src/claude-cli-check.ts:44` — `getClaudeCommand(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { return platform === 'win32' ? 'claude.cmd' : 'claude' } (missing: @param platform, @returns)
- `src/claude-cli-check.ts:57` — `getClaudeCommandCandidates(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { const command = getClaudeCommand(platform) return platform === 'win32' ? [command, 'claude.exe', 'claude'] : [command] } (missing: @param platform, @returns)
- `src/claude-cli-check.ts:84` — `findWorkingCommand()` — exported: **no** — Implements logic starting with: { for (const command of CLAUDE_COMMAND_CANDIDATES) { try { spawnClaude(command, ['--version'], { timeout: VERSION_TIMEOUT_MS, stdio: 'pipe', }) deb… (missing: @returns)
- `src/claude-cli-check.ts:108` — `parseAuthStatus(output: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param output, @returns)
- `src/claude-cli-check.ts:164` — `isClaudeBinaryInstalled()` — exported: **yes** — Implements logic starting with: { return findWorkingCommand() !== null } (missing: @returns)
- `src/claude-cli-check.ts:171` — `isClaudeCliReady()` — exported: **yes** — Implements logic starting with: { const command = findWorkingCommand() if (!command) return false return probeAuth(command) === true } (missing: @returns)

### `src/cli-policy.ts`
- `src/cli-policy.ts:16` — `shouldBypassManagedResourceMismatchGate(firstMessage: string | undefined)` — exported: **yes** — Implements logic starting with: { return firstMessage === 'update' } (missing: @param firstMessage, @returns)

### `src/cli-web-branch.ts`
- `src/cli-web-branch.ts:146` — `resolveContextAwareCwd(currentCwd: string, prefsPath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param currentCwd, @param prefsPath, @returns)

### `src/cli.ts`
- `src/cli.ts:77` — `printNonTtyErrorAndExit(missing: string | undefined, includeWebHint: boolean)` — exported: **no** — Implements logic starting with: { const suffix = missing ? ` but ${missing} not a TTY` : '' process.stderr.write(`[gsd] Error: Interactive mode requires a terminal (TTY)${suffix}.… (missing: @param missing, @param includeWebHint)
- `src/cli.ts:99` — `printExtensionErrors(errors: ReadonlyArray<{ error: string }>)` — exported: **no** — Implements logic starting with: { for (const err of errors) { const isConflict = err.error.includes('supersedes') || err.error.includes('conflicts with') const prefix = isConflict… (missing: @param errors)
- `src/cli.ts:112` — `printExtensionWarnings(warnings: ReadonlyArray<{ message: string }> | undefined)` — exported: **no** — Implements logic starting with: { if (!warnings) return for (const w of warnings) { process.stderr.write(`[gsd] Extension warning: ${w.message}\n`) } } (missing: @param warnings)
- `src/cli.ts:124` — `reapplyValidatedModelOnFallback(session: { setModel(model: { provider: string; id: string }): unknown | Promise<unknown> }, modelRegistry: ModelRegistryInstance, settingsManager: SettingsManagerInstance, fallbackMessage: string | undefined)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param session, @param modelRegistry, @param settingsManager, @param fallbackMessage)
- `src/cli.ts:442` — `runHeadlessFromAuto(headlessArgs: string[])` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param headlessArgs)

### `src/extension-discovery.ts`
- `src/extension-discovery.ts:18` — `resolveExtensionEntries(dir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param dir, @returns)
- `src/extension-discovery.ts:61` — `discoverExtensionEntryPaths(extensionsDir: string)` — exported: **yes** — Implements logic starting with: { if (!existsSync(extensionsDir)) { return [] } const discovered: string[] = [] for (const entry of readdirSync(extensionsDir, { withFileTypes: tru… (missing: @param extensionsDir, @returns)
- `src/extension-discovery.ts:88` — `mergeExtensionEntryPaths(bundledPaths: string[], installedExtDir: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param bundledPaths, @param installedExtDir, @returns)

### `src/extension-registry.ts`
- `src/extension-registry.ts:108` — `isExtensionEnabled(registry: ExtensionRegistry, id: string)` — exported: **yes** — Implements logic starting with: { const entry = registry.entries[id]; if (!entry) return true; return entry.enabled; } (missing: @param registry, @param id, @returns)
- `src/extension-registry.ts:131` — `disableExtension(registry: ExtensionRegistry, id: string, manifest: ExtensionManifest | null, reason?: string)` — exported: **yes** — Implements logic starting with: { if (manifest?.tier === "core") { return `Cannot disable "${id}" — it is a core extension.`; } const entry = registry.entries[id]; if (entry) { en… (missing: @param registry, @param id, @param manifest, @param reason, @returns)
- `src/extension-registry.ts:160` — `readManifest(extensionDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param extensionDir, @returns)
- `src/extension-registry.ts:175` — `readManifestFromEntryPath(entryPath: string)` — exported: **yes** — Implements logic starting with: { const dir = dirname(entryPath); return readManifest(dir); } (missing: @param entryPath, @returns)
- `src/extension-registry.ts:183` — `discoverAllManifests(extensionsDir: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param extensionsDir, @returns)
- `src/extension-registry.ts:201` — `ensureRegistryEntries(extensionsDir: string)` — exported: **yes** — Implements logic starting with: { const manifests = discoverAllManifests(extensionsDir); if (manifests.size === 0) return; const registry = loadRegistry(); let changed = false; fo… (missing: @param extensionsDir)

### `src/extension-sort.ts`
- `src/extension-sort.ts:25` — `sortExtensionPaths(paths: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param paths, @returns)

### `src/extension-validator.ts`
- `src/extension-validator.ts:40` — `checkInstallDiscriminator(pkg: unknown)` — exported: **yes** — Implements logic starting with: { if (typeof pkg !== 'object' || pkg === null) { return { code: 'MISSING_GSD_MARKER', message: 'package.json must declare "gsd": { "extension": tru… (missing: @param pkg, @returns)
- `src/extension-validator.ts:77` — `checkNamespaceReservation(extensionId: string, opts: ValidationOptions)` — exported: **yes** — Implements logic starting with: { if (opts.allowGsdNamespace === true) { return null } if (extensionId.startsWith('gsd.')) { return { code: 'RESERVED_NAMESPACE', message: `Extensi… (missing: @param extensionId, @param opts, @returns)
- `src/extension-validator.ts:98` — `checkDependencyPlacement(pkg: unknown)` — exported: **yes** — Implements logic starting with: { const errors: ValidationError[] = [] if (typeof pkg !== 'object' || pkg === null) { return errors } const obj = pkg as Record<string, unknown> co… (missing: @param pkg, @returns)
- `src/extension-validator.ts:145` — `validateExtensionPackage(pkg: unknown, opts: ValidationOptions = {})` — exported: **yes** — Implements logic starting with: { const errors: ValidationError[] = [] const warnings: ValidationWarning[] = [] // Check 1: Install discriminator const discriminatorError = checkI… (missing: @param pkg, @param opts, @returns)

### `src/headless-answers.ts`
- `src/headless-answers.ts:123` — `AnswerInjector.observeEvent(event: Record<string, unknown>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param event)
- `src/headless-answers.ts:162` — `AnswerInjector.tryHandle(event: Record<string, unknown>, writeToStdin: (data: string) => void)` — exported: **yes** — Implements logic starting with: { const method = String(event.method ?? '') // Only handle 'select' — let auto-responder handle confirm, input, etc. if (method !== 'select') retur… (missing: @param event, @param writeToStdin, @returns)
- `src/headless-answers.ts:199` — `AnswerInjector.getSecretEnvVars()` — exported: **yes** — Implements logic starting with: { return this.answerFile.secrets ?? {} } (missing: @returns)
- `src/headless-answers.ts:206` — `AnswerInjector.getStats()` — exported: **yes** — Implements logic starting with: { return { ...this.stats } } (missing: @returns)
- `src/headless-answers.ts:213` — `AnswerInjector.getUnusedWarnings()` — exported: **yes** — Implements logic starting with: { const warnings: string[] = [] if (this.answerFile.questions) { for (const id of Object.keys(this.answerFile.questions)) { if (!this.usedQuestionI… (missing: @returns)

### `src/headless-context.ts`
- `src/headless-context.ts:55` — `bootstrapGsdProject(basePath: string)` — exported: **yes** — Implements logic starting with: { const gsdDir = join(basePath, '.gsd') mkdirSync(join(gsdDir, 'milestones'), { recursive: true }) mkdirSync(join(gsdDir, 'runtime'), { recursive:… (missing: @param basePath)

### `src/headless-events.ts`
- `src/headless-events.ts:32` — `mapStatusToExitCode(status: string)` — exported: **yes** — Implements logic starting with: { switch (status) { case 'success': case 'complete': case 'completed': return EXIT_SUCCESS case 'error': case 'timeout': return EXIT_ERROR case 'bl… (missing: @param status, @returns)

### `src/headless-query.ts`
- `src/headless-query.ts:36` — `resolveGsdAgentExtensionsDir(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Implements logic starting with: { const agentRoot = env.GSD_AGENT_DIR || join(env.GSD_HOME || join(homedir(), '.gsd'), 'agent') return join(agentRoot, 'extensions', 'gsd') } (missing: @param env, @returns)
- `src/headless-query.ts:46` — `shouldUseAgentExtensionsDir(opts: { env?: NodeJS.ProcessEnv fileExists?: (path: string) => boolean })` — exported: **yes** — Implements logic starting with: { const env = opts.env ?? process.env const fileExists = opts.fileExists ?? existsSync const agentDir = resolveGsdAgentExtensionsDir(env) return {… (missing: @param opts, @returns)

### `src/headless-ui.ts`
- `src/headless-ui.ts:57` — `noColor()` — exported: **no** — Implements logic starting with: { const nc: Record<string, string> = {} for (const k of Object.keys(_c)) nc[k] = '' return nc as typeof _c } (missing: @returns)
- `src/headless-ui.ts:75` — `summarizeToolArgs(toolName: unknown, toolInput: unknown)` — exported: **yes** — Implements logic starting with: { const name = String(toolName ?? '') const input = (toolInput && typeof toolInput === 'object') ? toolInput as Record<string, unknown> : {} // Hel… (missing: @param toolName, @param toolInput, @returns)
- `src/headless-ui.ts:164` — `summarizeGsdTool(name: string, input: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { const parts: string[] = [] if (input.milestoneId) parts.push(String(input.milestoneId)) if (input.sliceId) parts.push(String(input.sliceId)) if (… (missing: @param name, @param input, @returns)
- `src/headless-ui.ts:337` — `formatThinkingLine(text: string)` — exported: **yes** — Implements logic starting with: { const trimmed = text.replace(/\s+/g, ' ').trim() const truncated = trimmed.length > 120 ? trimmed.slice(0, 117) + '...' : trimmed return `${c.dim… (missing: @param text, @returns)
- `src/headless-ui.ts:350` — `formatTextStart()` — exported: **yes** — Implements logic starting with: { return `${c.dim}[text]${c.reset}` } (missing: @returns)
- `src/headless-ui.ts:357` — `formatTextEnd()` — exported: **yes** — Implements logic starting with: { return '' // empty — newline handled by caller } (missing: @returns)
- `src/headless-ui.ts:364` — `formatThinkingStart()` — exported: **yes** — Implements logic starting with: { return `${c.dim}${c.italic}[thinking]${c.reset}` } (missing: @returns)
- `src/headless-ui.ts:371` — `formatThinkingEnd()` — exported: **yes** — Implements logic starting with: { return '' // empty — newline handled by caller } (missing: @returns)
- `src/headless-ui.ts:378` — `formatCostLine(costUsd: number, inputTokens: number, outputTokens: number)` — exported: **yes** — Implements logic starting with: { return `${c.dim}[cost] $${costUsd.toFixed(4)} (${inputTokens + outputTokens} tokens)${c.reset}` } (missing: @param costUsd, @param inputTokens, @param outputTokens, @returns)
- `src/headless-ui.ts:391` — `parsePhaseLabel(statusKey: string, message: string)` — exported: **no** — Implements logic starting with: { // Direct phase/milestone/slice/task keys const parts = statusKey.split(':') if (parts.length >= 2) { const [kind, value] = parts switch (kind.to… (missing: @param statusKey, @param message, @returns)

### `src/headless.ts`
- `src/headless.ts:95` — `isMultiTurnHeadlessCommand(command: string)` — exported: **yes** — Implements logic starting with: { return ( command === 'auto' || command === 'next' || command === 'discuss' || command === 'plan' ) } (missing: @param command, @returns)
- `src/headless.ts:124` — `resolveResumeSession(sessions: SessionInfo[], prefix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param sessions, @param prefix, @returns)

### `src/mcp-server.ts`
- `src/mcp-server.ts:11` — `isPlainObject(value: unknown)` — exported: **no** — Implements logic starting with: { if (value === null || typeof value !== 'object') return false if (Array.isArray(value)) return false const proto = Object.getPrototypeOf(value) r… (missing: @param value, @returns)
- `src/mcp-server.ts:75` — `startMcpServer(options: { tools: McpToolDef[] version?: string })` — exported: **yes** — Serializes a value to JSON. (missing: @param options)

### `src/onboarding.ts`
- `src/onboarding.ts:124` — `loadClack()` — exported: **no** — Validates inputs and throws on failure. (missing: @returns)
- `src/onboarding.ts:137` — `loadPico()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/onboarding.ts:159` — `openBrowser(url: string)` — exported: **no** — Implements logic starting with: { if (process.platform === 'win32') { // PowerShell's Start-Process handles URLs with '&' safely; cmd /c start does not. execFile('powershell', ['-… (missing: @param url)
- `src/onboarding.ts:176` — `persistDefaultProvider(providerId: string)` — exported: **no** — Performs filesystem I/O. (missing: @param providerId)
- `src/onboarding.ts:191` — `persistDefaultModel(modelId: string)` — exported: **no** — Performs filesystem I/O. (missing: @param modelId)
- `src/onboarding.ts:229` — `runStep(p: ClackModule, warnLabel: string, fn: () => Promise<T>, opts: { cancelMessage?: string; errorInfo?: string } = {})` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param p, @param warnLabel, @param fn, @param opts, @returns)
- `src/onboarding.ts:263` — `shouldRunOnboarding(authStorage: AuthStorage, settingsDefaultProvider?: string)` — exported: **yes** — Implements logic starting with: { if (!process.stdin.isTTY) return false // Explicit completion record wins — user has already finished onboarding (and // our flowVersion hasn't b… (missing: @param authStorage, @param settingsDefaultProvider, @returns)
- `src/onboarding.ts:286` — `runOnboarding(authStorage: AuthStorage, opts: RunOnboardingOptions = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: @param authStorage, @param opts)

### `src/pi-migration.ts`
- `src/pi-migration.ts:36` — `migratePiCredentials(authStorage: AuthStorage)` — exported: **yes** — Performs filesystem I/O. (missing: @param authStorage, @returns)

### `src/resource-loader.ts`
- `src/resource-loader.ts:155` — `computeResourceFingerprint(rootDir: string = resourcesDir)` — exported: **yes** — Implements logic starting with: { const entries: string[] = [] collectFileEntries(rootDir, rootDir, entries) entries.sort() return createHash('sha256').update(entries.join('\n')).… (missing: @param rootDir, @returns)
- `src/resource-loader.ts:222` — `makeTreeWritable(dirPath: string)` — exported: **no** — Implements logic starting with: { if (!existsSync(dirPath)) return // Use lstatSync to avoid following symlinks into immutable filesystems // (e.g., Nix store on NixOS/nix-darwin)… (missing: @param dirPath)
- `src/resource-loader.ts:265` — `syncResourceDir(srcDir: string, destDir: string)` — exported: **yes** — Implements logic starting with: { makeTreeWritable(destDir) if (existsSync(srcDir)) { pruneStaleSiblingFiles(srcDir, destDir) for (const entry of readdirSync(srcDir, { withFileTyp… (missing: @param srcDir, @param destDir)
- `src/resource-loader.ts:311` — `copyDirRecursive(src: string, dest: string)` — exported: **no** — Implements logic starting with: { mkdirSync(dest, { recursive: true }) for (const entry of readdirSync(src, { withFileTypes: true })) { const srcPath = join(src, entry.name) const… (missing: @param src, @param dest)
- `src/resource-loader.ts:340` — `ensureNodeModulesSymlink(agentDir: string)` — exported: **no** — Implements logic starting with: { const agentNodeModules = join(agentDir, 'node_modules') const internalNodeModules = join(packageRoot, 'node_modules') const hoistedNodeModules =… (missing: @param agentDir)
- `src/resource-loader.ts:365` — `hasMissingWorkspaceScopes(hoisted: string, internal: string)` — exported: **yes** — Implements logic starting with: { if (!existsSync(internal)) return false try { for (const entry of readdirSync(internal, { withFileTypes: true })) { if (entry.isDirectory() && en… (missing: @param hoisted, @param internal, @returns)
- `src/resource-loader.ts:379` — `reconcileSymlink(link: string, target: string)` — exported: **no** — Logs diagnostic information. (missing: @param link, @param target)
- `src/resource-loader.ts:406` — `reconcileMergedNodeModules(agentNodeModules: string, hoisted: string, internal: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param agentNodeModules, @param hoisted, @param internal)
- `src/resource-loader.ts:467` — `mergedFingerprint(hoisted: string, internal: string)` — exported: **yes** — Implements logic starting with: { try { const h = readdirSync(hoisted).sort().join(',') const i = readdirSync(internal).sort().join(',') return `${packageRoot}\n${h}\n${i}` } catc… (missing: @param hoisted, @param internal, @returns)
- `src/resource-loader.ts:487` — `pruneRemovedBundledExtensions(manifest: ManagedResourceManifest | null, agentDir: string)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param manifest, @param agentDir)
- `src/resource-loader.ts:568` — `initResources(agentDir: string, skillsDir: string = join(homedir(), '.agents', 'skills'))` — exported: **yes** — Implements logic starting with: { mkdirSync(agentDir, { recursive: true }) const currentVersion = getBundledGsdVersion() const manifest = readManagedResourceManifest(agentDir) con… (missing: @param agentDir, @param skillsDir)
- `src/resource-loader.ts:640` — `migrateSkillsToEcosystemDir(agentDir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param agentDir)

### `src/resources/extensions/ask-user-questions.ts`
- `src/resources/extensions/ask-user-questions.ts:134` — `raceRemoteAndLocal(startRemote: () => Promise<RaceableResult | null>, startLocal: () => Promise<RoundResult | null | undefined>, controller: AbortController, questions: Question[])` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param startRemote, @param startLocal, @param controller, @param questions, @returns)
- `src/resources/extensions/ask-user-questions.ts:190` — `formatForLLM(result: RoundResult)` — exported: **no** — Serializes a value to JSON. (missing: @param result, @returns)

### `src/resources/extensions/async-jobs/async-bash-tool.ts`
- `src/resources/extensions/async-jobs/async-bash-tool.ts:44` — `killTree(pid: number)` — exported: **no** — Executes a subprocess command. (missing: @param pid)
- `src/resources/extensions/async-jobs/async-bash-tool.ts:113` — `executeBashInBackground(command: string, cwd: string, signal: AbortSignal, timeout?: number)` — exported: **no** — Executes a subprocess command. (missing: @param command, @param cwd, @param signal, @param timeout, @returns)

### `src/resources/extensions/async-jobs/job-manager.ts`
- `src/resources/extensions/async-jobs/job-manager.ts:64` — `AsyncJobManager.register(type: JobType, label: string, runFn: (signal: AbortSignal) => Promise<string>)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param type, @param label, @param runFn)
- `src/resources/extensions/async-jobs/job-manager.ts:129` — `AsyncJobManager.cancel(id: string)` — exported: **yes** — Implements logic starting with: { const job = this.jobs.get(id); if (!job) return "not_found"; if (job.status !== "running") return "already_completed"; job.status = "cancelled";… (missing: @param id, @returns)
- `src/resources/extensions/async-jobs/job-manager.ts:184` — `AsyncJobManager.suppressFollowUp(id: string)` — exported: **yes** — Implements logic starting with: { const job = this.jobs.get(id); if (!job) return; job.awaited = true; if (job.deliveryTimer !== undefined) { clearTimeout(job.deliveryTimer); job.… (missing: @param id)

### `src/resources/extensions/aws-auth/index.ts`
- `src/resources/extensions/aws-auth/index.ts:60` — `getAwsAuthRefreshCommand()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/aws-auth/index.ts:81` — `runRefresh(command: string, notify: (msg: string, level: "info" | "warning" | "error") => void)` — exported: **no** — Executes a subprocess command. (missing: @param command, @param notify, @returns)

### `src/resources/extensions/bg-shell/bg-shell-lifecycle.ts`
- `src/resources/extensions/bg-shell/bg-shell-lifecycle.ts:69` — `buildProcessStateAlert(reason: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param reason)

### `src/resources/extensions/bg-shell/process-manager.ts`
- `src/resources/extensions/bg-shell/process-manager.ts:39` — `setPendingAlerts(alerts: string[])` — exported: **yes** — Implements logic starting with: { pendingAlerts = alerts; } (missing: @param alerts)

### `src/resources/extensions/browser-tools/capture.ts`
- `src/resources/extensions/browser-tools/capture.ts:32` — `__setSharpForTesting(value: typeof import("sharp") | null | undefined)` — exported: **yes** — Implements logic starting with: { _sharp = value; } (missing: @param value)
- `src/resources/extensions/browser-tools/capture.ts:53` — `parseScreenshotDimension(value: string | undefined, fallback: number)` — exported: **no** — Implements logic starting with: { if (value === undefined || value === "") return fallback; const n = parseInt(value, 10); if (isNaN(n) || n < 0) return fallback; if (n === 0) ret… (missing: @param value, @param fallback, @returns)
- `src/resources/extensions/browser-tools/capture.ts:62` — `getScreenshotFormatOverride()` — exported: **yes** — Implements logic starting with: { const fmt = process.env.SCREENSHOT_FORMAT?.toLowerCase(); if (fmt === "png") return "png"; if (fmt === "jpeg" || fmt === "jpg") return "jpeg"; re… (missing: @returns)
- `src/resources/extensions/browser-tools/capture.ts:70` — `getScreenshotQualityDefault(fallback: number)` — exported: **yes** — Implements logic starting with: { const q = process.env.SCREENSHOT_QUALITY; if (q === undefined || q === "") return fallback; const n = parseInt(q, 10); if (isNaN(n) || n < 1 || n… (missing: @param fallback, @returns)
- `src/resources/extensions/browser-tools/capture.ts:170` — `postActionSummary(p: Page, target?: Page | Frame)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param p, @param target, @returns)
- `src/resources/extensions/browser-tools/capture.ts:192` — `constrainScreenshot(_page: Page, buffer: Buffer, mimeType: string, quality: number)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _page, @param buffer, @param mimeType, @param quality, @returns)
- `src/resources/extensions/browser-tools/capture.ts:220` — `captureErrorScreenshot(p: Page | null)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param p, @returns)

### `src/resources/extensions/browser-tools/core.ts`
- `src/resources/extensions/browser-tools/core.ts:359` — `parseThreshold(value: string | null | undefined)` — exported: **yes** — Implements logic starting with: { if (value == null) return null; const str = String(value).trim(); if (str === "") return null; const match = str.match(/^(>=|<=|==|>|<)?\s*(\d+)$… (missing: @param value, @returns)
- `src/resources/extensions/browser-tools/core.ts:373` — `meetsThreshold(count: number, threshold: Threshold)` — exported: **yes** — Implements logic starting with: { switch (threshold.op) { case ">=": return count >= threshold.n; case "<=": return count <= threshold.n; case "==": return count === threshold.n;… (missing: @param count, @param threshold, @returns)
- `src/resources/extensions/browser-tools/core.ts:388` — `getEntriesSince(entries: Array<{ timestamp?: number }>, sinceActionId: number | undefined, timeline: ActionTimeline)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param entries, @param sinceActionId, @param timeline, @returns)
- `src/resources/extensions/browser-tools/core.ts:652` — `validateWaitParams(params: { condition: string; value?: string; threshold?: string })` — exported: **yes** — Implements logic starting with: { const { condition, value, threshold } = params ?? {}; if (!condition) { return { error: "condition is required" }; } const spec = WAIT_CONDITIONS… (missing: @param params, @returns)
- `src/resources/extensions/browser-tools/core.ts:687` — `createRegionStableScript(selector: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param selector, @returns)

### `src/resources/extensions/browser-tools/lifecycle.ts`
- `src/resources/extensions/browser-tools/lifecycle.ts:49` — `attachPageListeners(p: Page, pageId: number)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param p, @param pageId)
- `src/resources/extensions/browser-tools/lifecycle.ts:246` — `getActivePage()` — exported: **yes** — Implements logic starting with: { return registryGetActive(pageRegistry).page; } (missing: @returns)
- `src/resources/extensions/browser-tools/lifecycle.ts:251` — `getActiveTarget()` — exported: **yes** — Implements logic starting with: { return getActiveFrame() ?? getActivePage(); } (missing: @returns)
- `src/resources/extensions/browser-tools/lifecycle.ts:256` — `getActivePageOrNull()` — exported: **yes** — Implements logic starting with: { try { return getActivePage(); } catch { return null; } } (missing: @returns)

### `src/resources/extensions/browser-tools/settle.ts`
- `src/resources/extensions/browser-tools/settle.ts:77` — `readSettleState(target: Page | Frame, checkFocus: boolean)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param target, @param checkFocus, @returns)

### `src/resources/extensions/browser-tools/tools/codegen.ts`
- `src/resources/extensions/browser-tools/tools/codegen.ts:10` — `registerCodegenTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param pi, @param deps)
- `src/resources/extensions/browser-tools/tools/codegen.ts:262` — `parseParamsSummary(summary: string)` — exported: **no** — Executes a subprocess command. (missing: @param summary, @returns)

### `src/resources/extensions/browser-tools/tools/device.ts`
- `src/resources/extensions/browser-tools/tools/device.ts:9` — `registerDeviceTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param pi, @param deps)
- `src/resources/extensions/browser-tools/tools/device.ts:172` — `fuzzyScore(needle: string, haystack: string)` — exported: **no** — Implements logic starting with: { let score = 0; let hi = 0; for (let ni = 0; ni < needle.length && hi < haystack.length; ni++) { const idx = haystack.indexOf(needle[ni], hi); if… (missing: @param needle, @param haystack, @returns)

### `src/resources/extensions/browser-tools/tools/extract.ts`
- `src/resources/extensions/browser-tools/tools/extract.ts:9` — `registerExtractTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Serializes a value to JSON. (missing: @param pi, @param deps)

### `src/resources/extensions/browser-tools/tools/forms.ts`
- `src/resources/extensions/browser-tools/tools/forms.ts:53` — `buildFormAnalysisScript(selector?: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param selector, @returns)

### `src/resources/extensions/browser-tools/tools/intent.ts`
- `src/resources/extensions/browser-tools/tools/intent.ts:55` — `buildIntentScoringScript(intent: string, scope?: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param intent, @param scope, @returns)

### `src/resources/extensions/browser-tools/tools/zoom.ts`
- `src/resources/extensions/browser-tools/tools/zoom.ts:9` — `registerZoomTools(pi: ExtensionAPI, deps: ToolDeps)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param pi, @param deps)

### `src/resources/extensions/browser-tools/utils.ts`
- `src/resources/extensions/browser-tools/utils.ts:188` — `createGetLivePagesSnapshot(ensureBrowser: () => Promise<{ page: Page }>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ensureBrowser, @returns)
- `src/resources/extensions/browser-tools/utils.ts:235` — `captureAccessibilityMarkdown(target: Page | Frame, selector?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param target, @param selector, @returns)

### `src/resources/extensions/claude-code-cli/partial-builder.ts`
- `src/resources/extensions/claude-code-cli/partial-builder.ts:33` — `parseMcpToolName(name: string)` — exported: **yes** — Implements logic starting with: { if (!name.startsWith("mcp__")) return null; const rest = name.slice("mcp__".length); const delim = rest.indexOf("__"); if (delim <= 0 || delim ==… (missing: @param name, @returns)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:47` — `toolCallFromBlock(id: string, rawName: string, input: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { const parsed = parseMcpToolName(rawName); const toolCall: ToolCall = { type: "toolCall", id, name: parsed ? parsed.tool : rawName, arguments: inp… (missing: @param id, @param rawName, @param input, @returns)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:72` — `mapContentBlock(block: BetaContentBlock)` — exported: **yes** — Serializes a value to JSON. (missing: @param block, @returns)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:131` — `mapUsage(sdkUsage: NonNullableUsage, totalCostUsd: number)` — exported: **yes** — Implements logic starting with: { return { input: sdkUsage.input_tokens, output: sdkUsage.output_tokens, cacheRead: sdkUsage.cache_read_input_tokens, cacheWrite: sdkUsage.cache_cr… (missing: @param sdkUsage, @param totalCostUsd, @returns)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:174` — `class PartialMessageBuilder` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/claude-code-cli/partial-builder.ts:202` — `PartialMessageBuilder.handleEvent(event: BetaRawMessageStreamEvent)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param event, @returns)

### `src/resources/extensions/claude-code-cli/readiness.ts`
- `src/resources/extensions/claude-code-cli/readiness.ts:28` — `buildClaudeSpawnInvocation(command: string, args: string[], platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { if (platform === "win32") { return { command: "cmd", args: ["/c", command, ...args] }; } return { command, args }; } (missing: @param command, @param args, @param platform, @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:50` — `getClaudeCommand(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { return platform === "win32" ? "claude.cmd" : "claude"; } (missing: @param platform, @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:62` — `getClaudeCommandCandidates(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { const command = getClaudeCommand(platform); return platform === "win32" ? [command, "claude.exe", "claude"] : [command]; } (missing: @param platform, @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:93` — `findWorkingCommand()` — exported: **no** — Implements logic starting with: { for (const command of CLAUDE_COMMAND_CANDIDATES) { try { spawnClaude(command, ["--version"], { timeout: VERSION_TIMEOUT_MS, stdio: "pipe", }); de… (missing: @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:118` — `parseAuthStatus(output: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param output, @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:211` — `isClaudeBinaryPresent()` — exported: **yes** — Implements logic starting with: { refreshCache(); return cachedBinaryPresent ?? false; } (missing: @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:220` — `isClaudeCodeAuthed()` — exported: **yes** — Implements logic starting with: { refreshCache(); return (cachedBinaryPresent ?? false) && (cachedAuthed ?? false); } (missing: @returns)
- `src/resources/extensions/claude-code-cli/readiness.ts:229` — `isClaudeCodeReady()` — exported: **yes** — Implements logic starting with: { refreshCache(); return (cachedBinaryPresent ?? false) && (cachedAuthed ?? false); } (missing: @returns)

### `src/resources/extensions/claude-code-cli/stream-adapter.ts`
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:66` — `resolveClaudeCodeCwd(options?: SimpleStreamOptions)` — exported: **yes** — Implements logic starting with: { return options?.cwd && options.cwd.trim().length > 0 ? options.cwd : process.cwd(); } (missing: @param options, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:163` — `createAssistantStream()` — exported: **no** — Validates inputs and throws on failure. (missing: @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:175` — `getResultErrorMessage(result: SDKResultMessage)` — exported: **yes** — Implements logic starting with: { if ("errors" in result && Array.isArray(result.errors) && result.errors.length > 0) { return result.errors.join("; "); } if ("result" in result &… (missing: @param result, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:196` — `getClaudeLookupCommand(platform: NodeJS.Platform = process.platform)` — exported: **yes** — Implements logic starting with: { return platform === "win32" ? "where claude" : "which claude"; } (missing: @param platform, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:208` — `parseClaudeLookupOutput(output: Buffer | string, platform: NodeJS.Platform = process.platform)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param output, @param platform, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:228` — `resolveBundledClaudeCliPath()` — exported: **yes** — Implements logic starting with: { try { const sdkEntry = requireFromHere.resolve("@anthropic-ai/claude-agent-sdk"); const cliPath = join(dirname(sdkEntry), "cli.js"); return exist… (missing: @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:246` — `normalizeClaudePathForSdk(resolvedPath: string, platform: NodeJS.Platform = process.platform, bundledCliPath: string | null = resolveBundledClaudeCliPath())` — exported: **yes** — Implements logic starting with: { if (platform !== "win32") return resolvedPath; if (/\.exe$/i.test(resolvedPath)) return resolvedPath; if (bundledCliPath) return bundledCliPath;… (missing: @param resolvedPath, @param platform, @param bundledCliPath, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:258` — `getClaudePath()` — exported: **no** — Executes a subprocess command. (missing: @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:283` — `extractMessageText(msg: { role: string; content: unknown })` — exported: **no** — Transforms a collection into a derived result. (missing: @param msg, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:308` — `buildPromptFromContext(context: Context)` — exported: **yes** — Implements logic starting with: { const hasContent = Boolean(context.systemPrompt) || context.messages.some((m) => extractMessageText(m)); if (!hasContent) return ""; const parts:… (missing: @param context, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:337` — `stripDataUriPrefix(value: string)` — exported: **no** — Implements logic starting with: { const commaIndex = value.indexOf(","); if (value.startsWith("data:") && commaIndex !== -1) { return value.slice(commaIndex + 1); } return value; } (missing: @param value, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:346` — `inferMimeTypeFromDataUri(value: string)` — exported: **no** — Executes a subprocess command. (missing: @param value, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:352` — `extractImageBlocksFromContext(context: Context)` — exported: **yes** — Implements logic starting with: { const imageBlocks: SDKInputImageBlock[] = []; for (const msg of context.messages) { if (msg.role !== "user" || !Array.isArray(msg.content)) conti… (missing: @param context, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:383` — `buildSdkQueryPrompt(context: Context, textPrompt: string = buildPromptFromContext(context))` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param context, @param textPrompt, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:413` — `makeErrorMessage(model: string, errorMsg: string)` — exported: **no** — Implements logic starting with: { return { role: "assistant", content: [{ type: "text", text: `Claude Code error: ${errorMsg}` }], api: "anthropic-messages", provider: "claude-cod… (missing: @param model, @param errorMsg, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:453` — `makeStreamExhaustedErrorMessage(model: string, lastTextContent: string)` — exported: **yes** — Implements logic starting with: { const errorMsg = "stream_exhausted_without_result"; const message = makeErrorMessage(model, errorMsg); if (lastTextContent) { message.content = [… (missing: @param model, @param lastTextContent, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:463` — `readElicitationChoices(options: SdkElicitationRequestOption[] | undefined)` — exported: **no** — Transforms a collection into a derived result. (missing: @param options, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:471` — `parseAskUserQuestionsElicitation(request: Pick<SdkElicitationRequest, "mode" | "requestedSchema">)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param request, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:525` — `isSecureElicitationField(requestMessage: string, fieldId: string, field: SdkElicitationFieldSchema)` — exported: **no** — Implements logic starting with: { if (field.format === "password") return true; if (field.writeOnly === true) return true; const rawField = field as Record<string, unknown>; if (r… (missing: @param requestMessage, @param fieldId, @param field, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:549` — `parseTextInputElicitation(request: Pick<SdkElicitationRequest, "message" | "mode" | "requestedSchema">)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param request, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:588` — `roundResultToElicitationContent(questions: ParsedElicitationQuestion[], result: RoundResult)` — exported: **yes** — Implements logic starting with: { const content: Record<string, string | string[]> = {}; for (const question of questions) { const answer = result.answers[question.id]; if (!answe… (missing: @param questions, @param result, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:615` — `buildElicitationPromptTitle(request: SdkElicitationRequest, question: ParsedElicitationQuestion)` — exported: **no** — Transforms a collection into a derived result. (missing: @param request, @param question, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:625` — `promptElicitationWithDialogs(request: SdkElicitationRequest, questions: ParsedElicitationQuestion[], ui: ExtensionUIContext, signal: AbortSignal)` — exported: **no** — Transforms a collection into a derived result. (missing: @param request, @param questions, @param ui, @param signal, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:672` — `buildTextInputPromptTitle(request: SdkElicitationRequest, field: ParsedTextInputField)` — exported: **no** — Transforms a collection into a derived result. (missing: @param request, @param field, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:682` — `buildTextInputPlaceholder(field: ParsedTextInputField)` — exported: **no** — Transforms a collection into a derived result. (missing: @param field, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:697` — `promptTextInputElicitation(request: SdkElicitationRequest, fields: ParsedTextInputField[], ui: ExtensionUIContext, signal: AbortSignal)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param request, @param fields, @param ui, @param signal, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:781` — `buildBashPermissionPattern(command: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param command, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:846` — `buildBashPermissionPatternOptions(command: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param command, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:892` — `readBashAllowRulesFromSettings()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:938` — `bashCommandMatchesSavedRules(command: string)` — exported: **yes** — Executes a subprocess command. (missing: @param command, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:979` — `formatToolInput(toolName: string, input: Record<string, unknown>)` — exported: **no** — Serializes a value to JSON. (missing: @param toolName, @param input, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1012` — `createClaudeCodeCanUseToolHandler(ui: ExtensionUIContext | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ui, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1168` — `createClaudeCodeElicitationHandler(ui: ExtensionUIContext | undefined)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ui, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1205` — `makeAbortedMessage(model: string, lastTextContent: string)` — exported: **yes** — Implements logic starting with: { const message: AssistantMessage = { role: "assistant", content: lastTextContent ? [{ type: "text", text: lastTextContent }] : [{ type: "text", te… (missing: @param model, @param lastTextContent, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1245` — `resolveClaudePermissionMode(env: NodeJS.ProcessEnv = process.env)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param env, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1265` — `modelSupportsAdaptiveThinking(modelId: string)` — exported: **no** — Implements logic starting with: { return ( modelId.includes("opus-4-6") || modelId.includes("opus-4.6") || modelId.includes("opus-4-7") || modelId.includes("opus-4.7") || modelId.… (missing: @param modelId, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1281` — `mapThinkingLevelToAnthropicEffort(level: ThinkingLevel | undefined, modelId: string)` — exported: **no** — Implements logic starting with: { switch (level) { case "minimal": case "low": return "low"; case "medium": return "medium"; case "high": return "high"; case "xhigh": if (modelId.… (missing: @param level, @param modelId, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1310` — `buildSdkOptions(modelId: string, prompt: string, overrides?: { permissionMode?: "bypassPermissions" | "acceptEdits" | "default" | "plan" }, extraOptions: Record<string, unknown> & { reasoning?: ThinkingLevel } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: @param modelId, @param prompt, @param overrides, @param extraOptions, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1375` — `normalizeToolResultContent(content: unknown)` — exported: **no** — Serializes a value to JSON. (missing: @param content, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1429` — `extractStructuredDetailsFromBlock(block: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { const sibling = block.structuredContent ?? (block as Record<string, unknown>).structured_content; if (sibling && typeof sibling === "object" && !… (missing: @param block, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1463` — `isStructuredContentPseudoBlock(item: unknown)` — exported: **no** — Implements logic starting with: { if (!item || typeof item !== "object") return false; const type = (item as Record<string, unknown>).type; return type === "structuredContent" ||… (missing: @param item, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1475` — `stripStructuredContentPseudoBlocks(content: unknown)` — exported: **no** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1481` — `extractToolResultsFromSdkUserMessage(message: SDKUserMessage)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param message, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1532` — `attachExternalResultsToToolBlocks(toolBlocks: AssistantMessage["content"], toolResultsById: ReadonlyMap<string, ExternalToolResultPayload>)` — exported: **no** — Implements logic starting with: { for (const block of toolBlocks) { if (block.type !== "toolCall" && block.type !== "serverToolUse") continue; const externalResult = toolResultsBy… (missing: @param toolBlocks, @param toolResultsById)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1550` — `buildFinalAssistantContent(params: { intermediateToolBlocks: AssistantMessage["content"]; pendingContent?: AssistantMessage["content"]; toolResultsById: ReadonlyMap<string, ExternalToolResultPayload>; lastThinkingContent?: string; lastTextContent?: string; fallbackResultText?: string; })` — exported: **yes** — Implements logic starting with: { const mergedToolBlocks = [...params.intermediateToolBlocks]; if (params.pendingContent) { mergePendingToolCalls(mergedToolBlocks, params.pendingC… (missing: @param params, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1593` — `mergePendingToolCalls(intermediate: AssistantMessage["content"], pending: AssistantMessage["content"])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param intermediate, @param pending, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1622` — `streamViaClaudeCode(model: Model<any>, context: Context, options?: SimpleStreamOptions)` — exported: **yes** — Implements logic starting with: { const stream = createAssistantStream(); void pumpSdkMessages(model, context, options, stream); return stream; } (missing: @param model, @param context, @param options, @returns)
- `src/resources/extensions/claude-code-cli/stream-adapter.ts:1635` — `pumpSdkMessages(model: Model<any>, context: Context, options: SimpleStreamOptions | undefined, stream: AssistantMessageEventStream)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param model, @param context, @param options, @param stream)

### `src/resources/extensions/claude-code-cli/tests/partial-builder.test.ts`
- `src/resources/extensions/claude-code-cli/tests/partial-builder.test.ts:11` — `feedToolCall(builder: PartialMessageBuilder, jsonFragments: string[])` — exported: **no** — Implements logic starting with: { // Start the tool_use block at stream index 0 builder.handleEvent({ type: "content_block_start", index: 0, content_block: { type: "tool_use", id:… (missing: @param builder, @param jsonFragments, @returns)

### `src/resources/extensions/cmux/index.ts`
- `src/resources/extensions/cmux/index.ts:338` — `CmuxClient.createGridLayout(count: number)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param count, @returns)
- `src/resources/extensions/cmux/index.ts:464` — `initCmuxEventListeners(events: EventBus)` — exported: **yes** — Implements logic starting with: { events.on(CMUX_CHANNELS.SIDEBAR, (data) => { const event = data as CmuxSidebarEvent; if (event.action === "sync" && event.state) { syncCmuxSideba… (missing: @param events)

### `src/resources/extensions/context7/index.ts`
- `src/resources/extensions/context7/index.ts:104` — `formatLibraryList(libs: C7Library[], query: string)` — exported: **no** — Implements logic starting with: { if (libs.length === 0) { return `No libraries found for "${query}". Try a different name or spelling.`; } const lines: string[] = [ `Found ${libs… (missing: @param libs, @param query, @returns)

### `src/resources/extensions/get-secrets-from-user.ts`
- `src/resources/extensions/get-secrets-from-user.ts:98` — `detectDestination(basePath: string)` — exported: **yes** — Implements logic starting with: { if (existsSync(resolve(basePath, "vercel.json"))) { return "vercel"; } const convexPath = resolve(basePath, "convex"); try { if (existsSync(conve… (missing: @param basePath, @returns)
- `src/resources/extensions/get-secrets-from-user.ts:119` — `collectOneSecret(ctx: { ui: any; hasUI: boolean }, pageIndex: number, totalPages: number, keyName: string, hint: string | undefined, guidance?: string[])` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pageIndex, @param totalPages, @param keyName, @param hint, @param guidance, @returns)
- `src/resources/extensions/get-secrets-from-user.ts:269` — `showSecretsSummary(ctx: { ui: any; hasUI: boolean }, entries: SecretsManifestEntry[], existingKeys: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param ctx, @param entries, @param existingKeys)
- `src/resources/extensions/get-secrets-from-user.ts:338` — `applySecrets(provided: Array<{ key: string; value: string }>, destination: "dotenv" | "vercel" | "convex", opts: { envFilePath: string; environment?: string; exec?: (cmd: string, args: string[]) => Promise<{ code: number; stderr: string }>; })` — exported: **no** — Executes a subprocess command. (missing: @param provided, @param destination, @param opts, @returns)
- `src/resources/extensions/get-secrets-from-user.ts:404` — `collectSecretsFromManifest(base: string, milestoneId: string, ctx: { ui: any; hasUI: boolean; cwd: string })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param base, @param milestoneId, @param ctx, @returns)

### `src/resources/extensions/github-sync/sync.ts`
- `src/resources/extensions/github-sync/sync.ts:64` — `runGitHubSync(basePath: string, unitType: string, unitId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param unitType, @param unitId)
- `src/resources/extensions/github-sync/sync.ts:484` — `bootstrapSync(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/github-sync/sync.ts:575` — `getTaskIssueNumberForCommit(basePath: string, mid: string, sid: string, tid: string)` — exported: **yes** — Implements logic starting with: { try { const config = loadGitHubSyncConfig(basePath); if (!config?.enabled) return null; if (config.auto_link_commits === false) return null; cons… (missing: @param basePath, @param mid, @param sid, @param tid, @returns)

### `src/resources/extensions/github-sync/templates.ts`
- `src/resources/extensions/github-sync/templates.ts:26` — `inlineCode(s: string)` — exported: **yes** — Implements logic starting with: { if (s.length === 0) return ""; let longestRun = 0; let currentRun = 0; for (const ch of s) { if (ch === "`") { currentRun++; if (currentRun > lon… (missing: @param s, @returns)

### `src/resources/extensions/gsd/abandon-detect.ts`
- `src/resources/extensions/gsd/abandon-detect.ts:38` — `detectAbandonMilestone(overrides: Override[], currentMilestoneId: string | null | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param overrides, @param currentMilestoneId, @returns)

### `src/resources/extensions/gsd/activity-log.ts`
- `src/resources/extensions/gsd/activity-log.ts:66` — `snapshotKey(unitType: string, unitId: string, entries: unknown[])` — exported: **no** — Serializes a value to JSON. (missing: @param unitType, @param unitId, @param entries, @returns)

### `src/resources/extensions/gsd/atomic-write.ts`
- `src/resources/extensions/gsd/atomic-write.ts:175` — `atomicWriteSync(filePath: string, content: string, encoding: BufferEncoding = "utf-8")` — exported: **yes** — Implements logic starting with: { return atomicWriteSyncWithOps(filePath, content, encoding, DEFAULT_SYNC_OPS); } (missing: @param filePath, @param content, @param encoding)
- `src/resources/extensions/gsd/atomic-write.ts:183` — `atomicWriteAsync(filePath: string, content: string, encoding: BufferEncoding = "utf-8")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param filePath, @param content, @param encoding)

### `src/resources/extensions/gsd/auto-artifact-paths.ts`
- `src/resources/extensions/gsd/auto-artifact-paths.ts:48` — `resolveExpectedArtifactPath(unitType: string, unitId: string, base: string)` — exported: **yes** — Implements logic starting with: { const { milestone: mid, slice: sid, task: tid } = parseUnitId(unitId); switch (unitType) { case "workflow-preferences": return join(gsdRoot(base)… (missing: @param unitType, @param unitId, @param base, @returns)

### `src/resources/extensions/gsd/auto-dashboard.ts`
- `src/resources/extensions/gsd/auto-dashboard.ts:53` — `extractUatSliceId(unitId: string)` — exported: **yes** — Implements logic starting with: { const { slice } = parseUnitId(unitId); if (slice?.startsWith("S")) return slice; return null; } (missing: @param unitId, @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:197` — `describeNextUnit(state: GSDState)` — exported: **yes** — Implements logic starting with: { const sid = state.activeSlice?.id; const sTitle = state.activeSlice?.title; const tid = state.activeTask?.id; const tTitle = state.activeTask?.ti… (missing: @param state, @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:228` — `formatAutoElapsed(autoStartTime: number)` — exported: **yes** — Implements logic starting with: { if (!autoStartTime || autoStartTime <= 0 || !Number.isFinite(autoStartTime)) return ""; const ms = Date.now() - autoStartTime; if (ms < 0 || ms >… (missing: @param autoStartTime, @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:243` — `formatWidgetTokens(count: number)` — exported: **yes** — Implements logic starting with: { if (count < 1000) return count.toString(); if (count < 10000) return `${(count / 1000).toFixed(1)}k`; if (count < 1000000) return `${Math.round(c… (missing: @param count, @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:287` — `estimateTimeRemaining()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:471` — `sanitizeFooterStatus(text: string)` — exported: **no** — Implements logic starting with: { return text.replace(/\s+/g, " ").trim(); } (missing: @param text, @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:536` — `ensureWidgetModeLoaded(projectPath?: string, globalPath?: string)` — exported: **no** — Implements logic starting with: { if (widgetModeInitialized) return; widgetModeInitialized = true; try { const resolvedProjectPath = projectPath ?? getProjectGSDPreferencesPath();… (missing: @param projectPath, @param globalPath)
- `src/resources/extensions/gsd/auto-dashboard.ts:559` — `persistWidgetMode(mode: WidgetMode, prefsPath = widgetModePreferencePath ?? resolveWidgetModePreferencePath())` — exported: **no** — Performs filesystem I/O. (missing: @param mode, @param prefsPath)
- `src/resources/extensions/gsd/auto-dashboard.ts:582` — `cycleWidgetMode(projectPath?: string, globalPath?: string)` — exported: **yes** — Implements logic starting with: { ensureWidgetModeLoaded(projectPath, globalPath); const idx = WIDGET_MODES.indexOf(widgetMode); widgetMode = WIDGET_MODES[(idx + 1) % WIDGET_MODES… (missing: @param projectPath, @param globalPath, @returns)
- `src/resources/extensions/gsd/auto-dashboard.ts:591` — `setWidgetMode(mode: WidgetMode, projectPath?: string, globalPath?: string)` — exported: **yes** — Implements logic starting with: { ensureWidgetModeLoaded(projectPath, globalPath); widgetMode = mode; persistWidgetMode(widgetMode, widgetModePreferencePath ?? resolveWidgetModePr… (missing: @param mode, @param projectPath, @param globalPath)
- `src/resources/extensions/gsd/auto-dashboard.ts:598` — `getWidgetMode(projectPath?: string, globalPath?: string)` — exported: **yes** — Implements logic starting with: { ensureWidgetModeLoaded(projectPath, globalPath); return widgetMode; } (missing: @param projectPath, @param globalPath, @returns)

### `src/resources/extensions/gsd/auto-dispatch.ts`
- `src/resources/extensions/gsd/auto-dispatch.ts:208` — `getDeepStageGate(prefs: GSDPreferences | undefined, basePath: string)` — exported: **yes** — Implements logic starting with: { return resolveDeepProjectSetupState(prefs, basePath); } (missing: @param prefs, @param basePath, @returns)
- `src/resources/extensions/gsd/auto-dispatch.ts:249` — `findMissingSummaries(basePath: string, mid: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param mid, @returns)
- `src/resources/extensions/gsd/auto-dispatch.ts:325` — `isVerificationNotApplicable(value: string)` — exported: **yes** — Implements logic starting with: { const v = (value ?? "").toLowerCase().trim().replace(/[.\s]+$/, ""); if (!v || v === "none") return true; return /^(?:none(?:[\s._\u2014-]+[\s\S]… (missing: @param value, @returns)
- `src/resources/extensions/gsd/auto-dispatch.ts:1458` — `resolveDispatch(ctx: DispatchContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/auto-dispatch.ts:1503` — `getDispatchRuleNames()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `src/resources/extensions/gsd/auto-model-selection.ts`
- `src/resources/extensions/gsd/auto-model-selection.ts:32` — `class ModelPolicyDispatchBlockedError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/gsd/auto-model-selection.ts:89` — `clearToolBaseline(pi: ExtensionAPI | object)` — exported: **yes** — Implements logic starting with: { TOOL_BASELINE.delete(pi as unknown as object); } (missing: @param pi)
- `src/resources/extensions/gsd/auto-model-selection.ts:170` — `selectAndApplyModel(ctx: ExtensionContext, pi: ExtensionAPI, unitType: string, unitId: string, basePath: string, prefs: GSDPreferences | undefined, verbose: boolean, autoModeStartModel: { provider: string; id: string; flatRateCtx?: FlatRateContext } | null, retryContext?: { isRetry: boolean; previousTier?: string }, isAutoMode = true, sessionModelOverride?: { provider: string; id: string } | null, autoModeStartThinkingLevel?: ReturnType<ExtensionAPI["getThinkingLevel"]> | null)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param pi, @param unitType, @param unitId, @param basePath, @param prefs, @param verbose, @param autoModeStartModel, @param retryContext, @param isAutoMode, @param sessionModelOverride, @param autoModeStartThinkingLevel, @returns)
- `src/resources/extensions/gsd/auto-model-selection.ts:617` — `resolveModelId(modelId: string, availableModels: T[], currentProvider: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param modelId, @param availableModels, @param currentProvider, @returns)
- `src/resources/extensions/gsd/auto-model-selection.ts:728` — `buildFlatRateContext(provider: string, ctx?: { modelRegistry?: { getProviderAuthMode?: (p: string) => string } }, prefs?: { flat_rate_providers?: readonly string[] })` — exported: **yes** — Implements logic starting with: { let authMode: FlatRateContext["authMode"]; const registry = ctx?.modelRegistry; if (registry && typeof registry.getProviderAuthMode === "function… (missing: @param provider, @param ctx, @param prefs, @returns)

### `src/resources/extensions/gsd/auto-post-unit.ts`
- `src/resources/extensions/gsd/auto-post-unit.ts:89` — `isSamePathLocal(a: string, b: string)` — exported: **no** — Implements logic starting with: { return normalizeWorktreePathForCompare(a) === normalizeWorktreePathForCompare(b); } (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/auto-post-unit.ts:187` — `enqueueSidecar(s: AutoSession, ctx: ExtensionContext, entry: SidecarItem, debugExtra: Record<string, unknown>, notification?: string)` — exported: **no** — Implements logic starting with: { s.sidecarQueue.push(entry); debugLog("postUnitPostVerification", { phase: "sidecar-enqueue", kind: entry.kind, unitId: entry.unitId, ...debugExtr… (missing: @param s, @param ctx, @param entry, @param debugExtra, @param notification, @returns)
- `src/resources/extensions/gsd/auto-post-unit.ts:265` — `hasNonEmptyFields(row: Record<string, any> | null, fields: string[])` — exported: **no** — Implements logic starting with: { if (!row) return false; return fields.some(f => String(row[f] || "").trim().length > 0); } (missing: @param row, @param fields, @returns)
- `src/resources/extensions/gsd/auto-post-unit.ts:614` — `postUnitPreVerification(pctx: PostUnitContext, opts?: PreVerificationOpts)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param pctx, @param opts, @returns)
- `src/resources/extensions/gsd/auto-post-unit.ts:1218` — `postUnitPostVerification(pctx: PostUnitContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param pctx, @returns)

### `src/resources/extensions/gsd/auto-prompts.ts`
- `src/resources/extensions/gsd/auto-prompts.ts:65` — `resolvePromptBudgets()` — exported: **no** — Implements logic starting with: { try { const prefs = loadEffectiveGSDPreferences(); const sessionWindow = prefs?.preferences.context_window_override; const windowTokens = resolve… (missing: @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:81` — `resolveSummaryBudgetChars()` — exported: **no** — Implements logic starting with: { return resolvePromptBudgets().summaryBudgetChars; } (missing: @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:243` — `formatExecutorConstraints(sessionContextWindow?: number, modelRegistry?: MinimalModelRegistry, sessionProvider?: string)` — exported: **no** — Implements logic starting with: { let windowTokens: number; try { const prefs = loadEffectiveGSDPreferences(); windowTokens = resolveExecutorContextWindow(modelRegistry, prefs?.pr… (missing: @param sessionContextWindow, @param modelRegistry, @param sessionProvider, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:345` — `inlineFile(absPath: string | null, relPath: string, label: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param absPath, @param relPath, @param label, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:359` — `inlineFileOptional(absPath: string | null, relPath: string, label: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param absPath, @param relPath, @param label, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:378` — `inlineFileSmart(absPath: string | null, relPath: string, label: string, query?: string, threshold = 3000)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:578` — `buildSliceSummaryExcerpt(absPath: string | null, relPath: string, sid: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param absPath, @param relPath, @param sid, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:719` — `inlineDependencySummaries(mid: string, sid: string, base: string, budgetChars?: number)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param mid, @param sid, @param base, @param budgetChars, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:782` — `inlineGsdRootFile(base: string, filename: string, label: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param filename, @param label, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:802` — `inlineDecisionsFromDb(base: string, milestoneId?: string, scope?: string, level?: InlineLevel)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param milestoneId, @param scope, @param level, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:848` — `inlineRequirementsFromDb(base: string, milestoneId?: string, sliceId?: string, level?: InlineLevel)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param milestoneId, @param sliceId, @param level, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:875` — `inlineProjectFromDb(base: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:959` — `extractKeywords(title: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param title, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:971` — `inlineKnowledgeScoped(base: string, keywords: string[])` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param keywords, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1002` — `inlineKnowledgeBudgeted(base: string, keywords: string[], options?: { maxChars?: number })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param keywords, @param options, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1038` — `inlineRoadmapExcerpt(base: string, mid: string, sid: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param mid, @param sid, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1182` — `formatSkillRecommendationsBlock(unitType: string | undefined, skillNames: string[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param unitType, @param skillNames, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1308` — `buildSkillDiscoveryVars()` — exported: **yes** — Implements logic starting with: { const mode = resolveSkillDiscoveryMode(); if (mode === "off") { return { skillDiscoveryMode: "off", skillDiscoveryInstructions: " Skill discovery… (missing: @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1484` — `getDependencyTaskSummaryPaths(mid: string, sid: string, currentTid: string, dependsOn: string[], base: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param mid, @param sid, @param currentTid, @param dependsOn, @param base, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1521` — `checkNeedsReassessment(base: string, mid: string, state: GSDState)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param base, @param mid, @param state, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1577` — `checkNeedsRunUat(base: string, mid: string, state: GSDState, prefs: GSDPreferences | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param base, @param mid, @param state, @param prefs, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1652` — `buildDiscussMilestonePrompt(mid: string, midTitle: string, base: string, structuredQuestionsAvailable = "false")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mid, @param midTitle, @param base, @param structuredQuestionsAvailable, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1689` — `buildWorkflowPreferencesPrompt(base: string, structuredQuestionsAvailable = "false")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param structuredQuestionsAvailable, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1706` — `buildResearchProjectPrompt(base: string, structuredQuestionsAvailable = "false")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param structuredQuestionsAvailable, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1722` — `buildResearchDecisionPrompt(base: string, structuredQuestionsAvailable = "false")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param structuredQuestionsAvailable, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1738` — `buildDiscussProjectPrompt(base: string, structuredQuestionsAvailable = "false")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param structuredQuestionsAvailable, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:1758` — `buildDiscussRequirementsPrompt(base: string, structuredQuestionsAvailable = "false")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param base, @param structuredQuestionsAvailable, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:2028` — `renderSlicePrompt(options: { mid: string; sid: string; sTitle: string; base: string; level: InlineLevel; promptTemplate: "plan-slice" | "refine-slice"; prependBlocks?: string[]; extraVars?: Record<string, string>; sessionContextWindow?: number; modelRegistry?: MinimalModelRegistry; sessionProvider?: string; contextModeRenderMode?: ContextModeRenderMode; })` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:2200` — `buildRefineSlicePrompt(mid: string, _midTitle: string, sid: string, sTitle: string, base: string, level?: InlineLevel, options?: { sessionContextWindow?: number; modelRegistry?: MinimalModelRegistry; sessionProvider?: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mid, @param _midTitle, @param sid, @param sTitle, @param base, @param level, @param options, @returns)
- `src/resources/extensions/gsd/auto-prompts.ts:3154` — `renderGatesToCloseBlock(gates: ReadonlyArray<GateDefinition>, opts: { pending: ReadonlySet<string>; allowOmit: boolean })` — exported: **no** — Transforms a collection into a derived result. (missing: @param gates, @param opts, @returns)

### `src/resources/extensions/gsd/auto-recovery.ts`
- `src/resources/extensions/gsd/auto-recovery.ts:163` — `hasImplementationArtifacts(basePath: string, milestoneId?: string)` — exported: **yes** — Implements logic starting with: { try { // Verify we're in a git repo try { execFileSync("git", ["rev-parse", "--is-inside-work-tree"], { cwd: basePath, stdio: ["ignore", "pipe",… (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-recovery.ts:252` — `detectMainBranch(basePath: string)` — exported: **no** — Implements logic starting with: { try { const result = execFileSync("git", ["rev-parse", "--verify", "main"], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8"… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/auto-recovery.ts:284` — `getChangedFilesSinceBranch(basePath: string, targetBranch: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param targetBranch, @returns)
- `src/resources/extensions/gsd/auto-recovery.ts:588` — `verifyExpectedArtifact(unitType: string, unitId: string, base: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param unitType, @param unitId, @param base, @returns)
- `src/resources/extensions/gsd/auto-recovery.ts:914` — `writeBlockerPlaceholder(unitType: string, unitId: string, base: string, reason: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param unitType, @param unitId, @param base, @param reason, @returns)
- `src/resources/extensions/gsd/auto-recovery.ts:1008` — `buildLoopRemediationSteps(unitType: string, unitId: string, base: string)` — exported: **yes** — Implements logic starting with: { const { milestone: mid, slice: sid, task: tid } = parseUnitId(unitId); switch (unitType) { case "execute-task": { if (!mid || !sid || !tid) break… (missing: @param unitType, @param unitId, @param base, @returns)

### `src/resources/extensions/gsd/auto-start.ts`
- `src/resources/extensions/gsd/auto-start.ts:371` — `_selectResumableMilestone(branchNames: readonly string[], mergedBranches: ReadonlySet<string>, isComplete: (milestoneId: string) => boolean, commitsAhead: (branch: string) => number)` — exported: **yes** — Implements logic starting with: { const candidates: string[] = []; for (const branch of branchNames) { if (!branch.startsWith("milestone/")) continue; const milestoneId = branch.s… (missing: @param branchNames, @param mergedBranches, @param isComplete, @param commitsAhead, @returns)
- `src/resources/extensions/gsd/auto-start.ts:410` — `findUnmergedCompletedMilestone(basePath: string, isolationMode: "worktree" | "branch" | "none")` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param basePath, @param isolationMode, @returns)
- `src/resources/extensions/gsd/auto-start.ts:465` — `_finalizeSurvivorBranch(lifecycle: WorktreeLifecycle, milestoneId: string, ui: { notify: (msg: string, level?: "info" | "warning" | "error" | "success") => void })` — exported: **yes** — Implements logic starting with: { ui.notify( `Milestone ${milestoneId} is complete but branch/worktree was not finalized. Running merge now.`, "info", ); const result = lifecycle.… (missing: @param lifecycle, @param milestoneId, @param ui, @returns)
- `src/resources/extensions/gsd/auto-start.ts:502` — `_mergeOrphanCompletedMilestone(lifecycle: WorktreeLifecycle, orphanId: string, ui: { notify: (msg: string, level?: "info" | "warning" | "error" | "success") => void })` — exported: **yes** — Implements logic starting with: { ui.notify(`Detected unmerged completed milestone ${orphanId}. Merging now.`, "info"); const result = lifecycle.exitMilestone( orphanId, { merge:… (missing: @param lifecycle, @param orphanId, @param ui, @returns)

### `src/resources/extensions/gsd/auto-supervisor.ts`
- `src/resources/extensions/gsd/auto-supervisor.ts:32` — `registerSigtermHandler(currentBasePath: string, previousHandler: (() => void) | null, onSignalCleanup?: () => void)` — exported: **yes** — Implements logic starting with: { // Remove the explicitly-passed previous handler if (previousHandler) { for (const sig of CLEANUP_SIGNALS) process.off(sig, previousHandler); } /… (missing: @param currentBasePath, @param previousHandler, @param onSignalCleanup, @returns)
- `src/resources/extensions/gsd/auto-supervisor.ts:63` — `deregisterSigtermHandler(handler: (() => void) | null)` — exported: **yes** — Implements logic starting with: { if (handler) { for (const sig of CLEANUP_SIGNALS) process.off(sig, handler); } if (_currentSigtermHandler === handler) { _currentSigtermHandler =… (missing: @param handler)
- `src/resources/extensions/gsd/auto-supervisor.ts:80` — `detectWorkingTreeActivity(cwd: string)` — exported: **yes** — Implements logic starting with: { try { return nativeHasChanges(cwd); } catch { return false; } } (missing: @param cwd, @returns)

### `src/resources/extensions/gsd/auto-timers.ts`
- `src/resources/extensions/gsd/auto-timers.ts:82` — `parseEstimateMinutes(estimate: string)` — exported: **yes** — Implements logic starting with: { if (!estimate || typeof estimate !== "string") return null; const trimmed = estimate.trim(); if (!trimmed) return null; let totalMinutes = 0; let… (missing: @param estimate, @returns)

### `src/resources/extensions/gsd/auto-tool-tracking.ts`
- `src/resources/extensions/gsd/auto-tool-tracking.ts:25` — `markToolStart(toolCallId: string, isActive: boolean, toolName?: string)` — exported: **yes** — Implements logic starting with: { if (!isActive) return; inFlightTools.set(toolCallId, { startedAt: Date.now(), toolName: toolName ?? "unknown" }); } (missing: @param toolCallId, @param isActive, @param toolName)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:33` — `markToolEnd(toolCallId: string)` — exported: **yes** — Implements logic starting with: { inFlightTools.delete(toolCallId); } (missing: @param toolCallId)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:40` — `getOldestInFlightToolAgeMs()` — exported: **yes** — Implements logic starting with: { if (inFlightTools.size === 0) return 0; let oldestStart = Infinity; for (const t of inFlightTools.values()) { if (t.startedAt < oldestStart) olde… (missing: @returns)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:52` — `getInFlightToolCount()` — exported: **yes** — Implements logic starting with: { return inFlightTools.size; } (missing: @returns)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:59` — `getOldestInFlightToolStart()` — exported: **yes** — Implements logic starting with: { if (inFlightTools.size === 0) return undefined; let oldest = Infinity; for (const t of inFlightTools.values()) { if (t.startedAt < oldest) oldest… (missing: @returns)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:73` — `hasInteractiveToolInFlight()` — exported: **yes** — Implements logic starting with: { for (const { toolName } of inFlightTools.values()) { if (INTERACTIVE_TOOLS.has(toolName)) return true; } return false; } (missing: @returns)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:102` — `isToolInvocationError(errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!errorMsg) return false; return TOOL_INVOCATION_ERROR_RE.test(errorMsg) || isDeterministicPolicyError(errorMsg); } (missing: @param errorMsg, @returns)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:112` — `isQueuedUserMessageSkip(errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!errorMsg) return false; return /^Skipped due to queued user message\.?$/i.test(errorMsg.trim()); } (missing: @param errorMsg, @returns)
- `src/resources/extensions/gsd/auto-tool-tracking.ts:152` — `isDeterministicPolicyError(errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!errorMsg) return false; return DETERMINISTIC_POLICY_ERROR_RE.test(errorMsg) || DETERMINISTIC_POLICY_ERROR_STRINGS.some(s => errorMsg.include… (missing: @param errorMsg, @returns)

### `src/resources/extensions/gsd/auto-unit-closeout.ts`
- `src/resources/extensions/gsd/auto-unit-closeout.ts:82` — `closeoutUnit(ctx: ExtensionContext, basePath: string, unitType: string, unitId: string, startedAt: number, opts?: CloseoutOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param basePath, @param unitType, @param unitId, @param startedAt, @param opts, @returns)

### `src/resources/extensions/gsd/auto-verification.ts`
- `src/resources/extensions/gsd/auto-verification.ts:67` — `runValidateMilestonePostCheck(vctx: VerificationContext, pauseAuto: (ctx?: ExtensionContext, pi?: ExtensionAPI) => Promise<void>)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param vctx, @param pauseAuto, @returns)
- `src/resources/extensions/gsd/auto-verification.ts:166` — `countIncompleteSlices(basePath: string, milestoneId: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-verification.ts:198` — `runPostUnitVerification(vctx: VerificationContext, pauseAuto: (ctx?: ExtensionContext, pi?: ExtensionAPI) => Promise<void>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param vctx, @param pauseAuto, @returns)
- `src/resources/extensions/gsd/auto-verification.ts:638` — `writeVerificationJSONWithPostExec(result: VerificationGateResult, tasksDir: string, taskId: string, unitId: string, postExecutionChecks: PostExecutionCheckJSON[], retryAttempt?: number, maxRetries?: number)` — exported: **no** — Performs filesystem I/O. (missing: @param result, @param tasksDir, @param taskId, @param unitId, @param postExecutionChecks, @param retryAttempt, @param maxRetries)

### `src/resources/extensions/gsd/auto-worktree.ts`
- `src/resources/extensions/gsd/auto-worktree.ts:137` — `popStashByRef(basePath: string, stashMarker: string | null)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param stashMarker, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:186` — `isSamePath(a: string, b: string)` — exported: **no** — Implements logic starting with: { try { return realpathSync(a) === realpathSync(b); } catch (e) { if ((e as NodeJS.ErrnoException).code === "ENOENT") return false; logWarning("wor… (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:421` — `syncProjectRootToWorktree(projectRoot: string, worktreePath_: string, milestoneId: string | null)` — exported: **yes** — Implements logic starting with: { _projectRootToWorktreeImpl(projectRoot, worktreePath_, milestoneId); } (missing: @param projectRoot, @param worktreePath_, @param milestoneId)
- `src/resources/extensions/gsd/auto-worktree.ts:434` — `syncStateToProjectRoot(worktreePath_: string, projectRoot: string, milestoneId: string | null)` — exported: **yes** — Implements logic starting with: { _projectWorktreeToRootImpl(worktreePath_, projectRoot, milestoneId); } (missing: @param worktreePath_, @param projectRoot, @param milestoneId)
- `src/resources/extensions/gsd/auto-worktree.ts:449` — `readResourceVersion()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:468` — `checkResourcesStale(versionOnStart: string | null)` — exported: **yes** — Implements logic starting with: { if (versionOnStart === null) return null; const current = readResourceVersion(); if (current === null) return null; if (current !== versionOnStar… (missing: @param versionOnStart, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:493` — `escapeStaleWorktree(base: string)` — exported: **yes** — Implements logic starting with: { // Direct layout: /.gsd/worktrees/ const directMarker = `${pathSep}.gsd${pathSep}worktrees${pathSep}`; let idx = base.indexOf(directMarker); if (… (missing: @param base, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:540` — `cleanStaleRuntimeUnits(gsdRootPath: string, hasMilestoneSummary: (mid: string) => boolean)` — exported: **yes** — Implements logic starting with: { const runtimeUnitsDir = join(gsdRootPath, "runtime", "units"); if (!existsSync(runtimeUnitsDir)) return 0; let cleaned = 0; try { for (const file… (missing: @param gsdRootPath, @param hasMilestoneSummary, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:601` — `syncGsdStateToWorktreeByScope(rootScope: MilestoneScope, worktreeScope: MilestoneScope)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param rootScope, @param worktreeScope, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:634` — `syncGsdStateToWorktree(mainBasePath: string, worktreePath_: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param mainBasePath, @param worktreePath_, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:796` — `syncWorktreeStateBack(mainBasePath: string, worktreePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { return _finalizeProjectionForMergeImpl(mainBasePath, worktreePath, milestoneId); } (missing: @param mainBasePath, @param worktreePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:813` — `runWorktreePostCreateHook(sourceDir: string, worktreeDir: string, hookPath?: string)` — exported: **yes** — Implements logic starting with: { if (hookPath === undefined) { const prefs = loadEffectiveGSDPreferences()?.preferences?.git; hookPath = prefs?.worktree_post_create; } if (!hookP… (missing: @param sourceDir, @param worktreeDir, @param hookPath, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:863` — `autoWorktreeBranch(milestoneId: string)` — exported: **yes** — Implements logic starting with: { return `milestone/${milestoneId}`; } (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:887` — `enterBranchModeForMilestone(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const branch = autoWorktreeBranch(milestoneId); const branchExists = nativeBranchExists(basePath, branch); if (!branchExists) { // Create the mil… (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/auto-worktree.ts:987` — `_isBranchCheckedOutElsewhere(basePath: string, branch: string)` — exported: **yes** — Implements logic starting with: { try { const entries = nativeWorktreeList(basePath); return entries.some((entry) => entry.branch === branch); } catch { return true; } } (missing: @param basePath, @param branch, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1004` — `_resolveIntegrationBranchForReuse(basePath: string, milestoneId: string)` — exported: **no** — Implements logic starting with: { const fromMeta = readIntegrationBranch(basePath, milestoneId); if (fromMeta) return fromMeta; const gitPrefs = loadEffectiveGSDPreferences()?.pre… (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1039` — `fastForwardReusedMilestoneBranchIfSafe(basePath: string, milestoneId: string, branch: string)` — exported: **yes** — Implements logic starting with: { try { const integrationBranch = _resolveIntegrationBranchForReuse(basePath, milestoneId); if (!integrationBranch || integrationBranch === branch)… (missing: @param basePath, @param milestoneId, @param branch)
- `src/resources/extensions/gsd/auto-worktree.ts:1199` — `teardownAutoWorktree(originalBasePath: string, milestoneId: string, opts: { preserveBranch?: boolean } = {})` — exported: **yes** — Logs diagnostic information. (missing: @param originalBasePath, @param milestoneId, @param opts)
- `src/resources/extensions/gsd/auto-worktree.ts:1299` — `isInAutoWorktree(basePath: string)` — exported: **yes** — Implements logic starting with: { const targetPath = isGsdWorktreePath(basePath) ? basePath : process.cwd(); if (!isGsdWorktreePath(targetPath)) return false; const storedBase = g… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1329` — `getAutoWorktreePath(basePath: string, milestoneId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1359` — `enterAutoWorktree(basePath: string, milestoneId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1417` — `getAutoWorktreeOriginalBase()` — exported: **yes** — Implements logic starting with: { return getActiveWorkspace()?.projectRoot ?? null; } (missing: @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1468` — `autoCommitDirtyState(cwd: string)` — exported: **no** — Implements logic starting with: { try { const status = nativeWorkingTreeStatus(cwd); if (!status) return false; nativeAddAllWithExclusions(cwd, RUNTIME_EXCLUSION_PATHS); const res… (missing: @param cwd, @returns)
- `src/resources/extensions/gsd/auto-worktree.ts:1514` — `mergeMilestoneToMain(originalBasePath_: string, milestoneId: string, roadmapContent: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param originalBasePath_, @param milestoneId, @param roadmapContent, @returns)

### `src/resources/extensions/gsd/auto.ts`
- `src/resources/extensions/gsd/auto.ts:325` — `registerAutoWorkerForSession(session: AutoSession, projectRootOverride?: string)` — exported: **no** — Implements logic starting with: { if (session.workerId) return; // already registered (e.g. resume re-runs) try { const projectRootRealpath = normalizeRealPath( projectRootOverrid… (missing: @param session, @param projectRootOverride)
- `src/resources/extensions/gsd/auto.ts:416` — `rebuildScope(rawPath: string, milestoneId: string | null)` — exported: **no** — Implements logic starting with: { if (!milestoneId) { s.scope = null; return; } try { const workspace = createWorkspace(rawPath); s.scope = scopeMilestone(workspace, milestoneId);… (missing: @param rawPath, @param milestoneId)
- `src/resources/extensions/gsd/auto.ts:500` — `shouldUseWorktreeIsolation(basePath?: string)` — exported: **yes** — Implements logic starting with: { return getIsolationMode(basePath) === "worktree"; } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/auto.ts:592` — `registerSigtermHandler(currentBasePath: string)` — exported: **no** — Implements logic starting with: { s.sigtermHandler = _registerSigtermHandler( currentBasePath, s.sigtermHandler, () => closeOutSignalInterruptedUnit(currentBasePath), ); } (missing: @param currentBasePath)
- `src/resources/extensions/gsd/auto.ts:612` — `startAutoCommandPolling(basePath: string)` — exported: **no** — Implements logic starting with: { if (!isRemoteConfigured()) return; // Clear any existing interval before starting a new one (e.g. resume path). stopAutoCommandPolling(); s.comma… (missing: @param basePath)
- `src/resources/extensions/gsd/auto.ts:676` — `_setAutoActiveForTest(active: boolean)` — exported: **yes** — Implements logic starting with: { s.active = active; } (missing: @param active)
- `src/resources/extensions/gsd/auto.ts:685` — `_warnIfWorktreeMissingForTest(worktreePath: string | null | undefined, milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (worktreePath && !existsSync(worktreePath)) { logWarning( "session", `Worktree was expected at ${worktreePath} but is missing. Continuing in p… (missing: @param worktreePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto.ts:749` — `getAutoModeStartModel()` — exported: **yes** — Implements logic starting with: { return s.autoModeStartModel; } (missing: @returns)
- `src/resources/extensions/gsd/auto.ts:761` — `setCurrentDispatchedModelId(model: { provider: string; id: string } | null)` — exported: **yes** — Implements logic starting with: { s.currentDispatchedModelId = model ? `${model.provider}/${model.id}` : null; } (missing: @param model)
- `src/resources/extensions/gsd/auto.ts:782` — `recordToolInvocationError(toolName: string, errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!s.active) return; if (isToolInvocationError(errorMsg) || isQueuedUserMessageSkip(errorMsg) || isDeterministicPolicyError(errorMsg)) { s.last… (missing: @param toolName, @param errorMsg)
- `src/resources/extensions/gsd/auto.ts:800` — `lockBase()` — exported: **no** — Implements logic starting with: { return s.lockBasePath; } (missing: @returns)
- `src/resources/extensions/gsd/auto.ts:811` — `stopAutoRemote(projectRoot: string)` — exported: **yes** — Implements logic starting with: { const lock = readCrashLock(projectRoot); if (!lock) return { found: false }; // Never SIGTERM ourselves — a stale lock with our own PID is not a… (missing: @param projectRoot, @returns)
- `src/resources/extensions/gsd/auto.ts:847` — `checkRemoteAutoSession(projectRoot: string)` — exported: **yes** — Implements logic starting with: { const lock = readCrashLock(projectRoot); if (!lock) return { running: false }; // Our own PID is not a "remote" session — it is a stale lock left… (missing: @param projectRoot, @returns)
- `src/resources/extensions/gsd/auto.ts:900` — `buildSnapshotOpts(_unitType: string, _unitId: string)` — exported: **no** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(s.basePath || undefined)?.preferences; const uokFlags = resolveUokFlags(prefs); return { ...(s.autoStar… (missing: @param _unitType, @param _unitId, @returns)
- `src/resources/extensions/gsd/auto.ts:1007` — `rerootCommandSession(cmdCtx: Pick<ExtensionCommandContext, "newSession"> | null | undefined, workspaceRoot: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param cmdCtx, @param workspaceRoot, @returns)
- `src/resources/extensions/gsd/auto.ts:1622` — `pauseAuto(ctx?: ExtensionContext, _pi?: ExtensionAPI, _errorContext?: ErrorContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param _pi, @param _errorContext)
- `src/resources/extensions/gsd/auto.ts:1759` — `buildWorktreeLifecycleDeps()` — exported: **yes** — Implements logic starting with: { // ADR-016 phase 2 / C-track close-out: // C1 (#5624) — fs + git-CLI primitives inlined // C2 (#5625) — worktree-manager helpers inlined // C3 (#… (missing: @returns)
- `src/resources/extensions/gsd/auto.ts:1791` — `createWiredDispatchAdapter(ctx: ExtensionContext, pi: ExtensionAPI, dispatchBasePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param dispatchBasePath, @returns)
- `src/resources/extensions/gsd/auto.ts:1860` — `createWiredAutoOrchestrationModule(ctx: ExtensionContext, pi: ExtensionAPI, dispatchBasePath: string, runtimeBasePath = resolveProjectRoot(dispatchBasePath))` — exported: **yes** — Validates inputs and throws on failure. (missing: @param ctx, @param pi, @param dispatchBasePath, @param runtimeBasePath, @returns)
- `src/resources/extensions/gsd/auto.ts:2076` — `buildLoopDeps(pi: ExtensionAPI)` — exported: **no** — Performs filesystem I/O. (missing: @param pi, @returns)
- `src/resources/extensions/gsd/auto.ts:2211` — `startAuto(ctx: ExtensionCommandContext, pi: ExtensionAPI, base: string, verboseMode: boolean, options?: { step?: boolean; interrupted?: InterruptedSessionAssessment; milestoneLock?: string | null; })` — exported: **yes** — Performs filesystem I/O. (missing: @param ctx, @param pi, @param base, @param verboseMode, @param options)
- `src/resources/extensions/gsd/auto.ts:2663` — `updateProgressWidget(ctx: ExtensionContext, unitType: string, unitId: string, state: GSDState)` — exported: **no** — Implements logic starting with: { const badge = s.currentUnitRouting?.tier ? ({ light: "L", standard: "S", heavy: "H" }[s.currentUnitRouting.tier] ?? undefined) : undefined; _upda… (missing: @param ctx, @param unitType, @param unitId, @param state)
- `src/resources/extensions/gsd/auto.ts:2700` — `ensurePreconditions(unitType: string, unitId: string, base: string, state: GSDState)` — exported: **yes** — Implements logic starting with: { const { milestone: mid, slice: sid } = parseUnitId(unitId); const mDir = resolveMilestonePath(base, mid); if (!mDir) { // Fix #4996: When dispatc… (missing: @param unitType, @param unitId, @param base, @param state)

### `src/resources/extensions/gsd/auto/detect-stuck.ts`
- `src/resources/extensions/gsd/auto/detect-stuck.ts:31` — `retryBudgetSuppresses(unitKey: string)` — exported: **no** — Implements logic starting with: { try { const latest = getLatestForUnit(unitKey); if (!latest) return false; if (latest.attempt_n >= latest.max_attempts) return false; if (!latest… (missing: @param unitKey, @returns)
- `src/resources/extensions/gsd/auto/detect-stuck.ts:58` — `detectStuck(window: readonly WindowEntry[])` — exported: **yes** — Executes a subprocess command. (missing: @param window, @returns)

### `src/resources/extensions/gsd/auto/finalize-timeout.ts`
- `src/resources/extensions/gsd/auto/finalize-timeout.ts:27` — `withTimeout(promise: Promise<T>, timeoutMs: number, label: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param promise, @param timeoutMs, @param label, @returns)

### `src/resources/extensions/gsd/auto/infra-errors.ts`
- `src/resources/extensions/gsd/auto/infra-errors.ts:35` — `isInfrastructureError(err: unknown)` — exported: **yes** — Implements logic starting with: { if (err && typeof err === "object") { const code = (err as Record<string, unknown>).code; if (typeof code === "string" && INFRA_ERROR_CODES.has(c… (missing: @param err, @returns)
- `src/resources/extensions/gsd/auto/infra-errors.ts:68` — `isTransientCooldownError(err: unknown)` — exported: **yes** — Implements logic starting with: { if (err && typeof err === "object" && (err as Record<string, unknown>).code === "AUTH_COOLDOWN") { return true; } // Fallback: message match for… (missing: @param err, @returns)
- `src/resources/extensions/gsd/auto/infra-errors.ts:81` — `getCooldownRetryAfterMs(err: unknown)` — exported: **yes** — Implements logic starting with: { if (err && typeof err === "object" && (err as Record<string, unknown>).code === "AUTH_COOLDOWN") { return (err as Record<string, unknown>).retryA… (missing: @param err, @returns)

### `src/resources/extensions/gsd/auto/loop.ts`
- `src/resources/extensions/gsd/auto/loop.ts:267` — `autoLoop(ctx: ExtensionContext, pi: ExtensionAPI, s: AutoSession, deps: LoopDeps, options?: AutoLoopOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param s, @param deps, @param options)

### `src/resources/extensions/gsd/auto/phases.ts`
- `src/resources/extensions/gsd/auto/phases.ts:83` — `isSamePathLocal(a: string, b: string)` — exported: **no** — Implements logic starting with: { return normalizeWorktreePathForCompare(a) === normalizeWorktreePathForCompare(b); } (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:260` — `_resolveReportBasePath(s: Pick<AutoSession, "originalBasePath" | "basePath">)` — exported: **yes** — Implements logic starting with: { return resolveWorktreeProjectRoot(s.basePath, s.originalBasePath); } (missing: @param s, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:269` — `_resolveDispatchGuardBasePath(s: Pick<AutoSession, "originalBasePath" | "basePath">)` — exported: **yes** — Implements logic starting with: { return resolveWorktreeProjectRoot(s.basePath, s.originalBasePath); } (missing: @param s, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:303` — `generateMilestoneReport(s: AutoSession, ctx: ExtensionContext, milestoneId: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param s, @param ctx, @param milestoneId)
- `src/resources/extensions/gsd/auto/phases.ts:369` — `closeoutAndStop(ctx: ExtensionContext, pi: ExtensionAPI, s: AutoSession, deps: LoopDeps, reason: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param s, @param deps, @param reason)
- `src/resources/extensions/gsd/auto/phases.ts:434` — `_runMilestoneMergeWithStashRestore(ic: IterationContext, milestoneId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param ic, @param milestoneId, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:633` — `runPreDispatch(ic: IterationContext, loopState: LoopState)` — exported: **yes** — Serializes a value to JSON. (missing: @param ic, @param loopState, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:1228` — `runDispatch(ic: IterationContext, preData: PreDispatchData, loopState: LoopState)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ic, @param preData, @param loopState, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:1507` — `runGuards(ic: IterationContext, mid: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ic, @param mid, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:1733` — `runUnitPhase(ic: IterationContext, iterData: IterationData, loopState: LoopState, sidecarItem?: SidecarItem)` — exported: **yes** — Serializes a value to JSON. (missing: @param ic, @param iterData, @param loopState, @param sidecarItem, @returns)
- `src/resources/extensions/gsd/auto/phases.ts:2399` — `runFinalize(ic: IterationContext, iterData: IterationData, loopState: LoopState, sidecarItem?: SidecarItem)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ic, @param iterData, @param loopState, @param sidecarItem, @returns)

### `src/resources/extensions/gsd/auto/resolve.ts`
- `src/resources/extensions/gsd/auto/resolve.ts:75` — `resolveAgentEnd(event: AgentEndEvent)` — exported: **yes** — Implements logic starting with: { if (_sessionSwitchInFlight) { debugLog("resolveAgentEnd", { status: "ignored-during-switch" }); return; } if (_currentResolve) { debugLog("resolv… (missing: @param event)
- `src/resources/extensions/gsd/auto/resolve.ts:110` — `bumpAndResolveSynthetic(reason: string)` — exported: **yes** — Implements logic starting with: { bumpTurnGeneration(reason); resolveAgentEnd({ messages: [], _synthetic: reason } as unknown as AgentEndEvent); } (missing: @param reason)
- `src/resources/extensions/gsd/auto/resolve.ts:124` — `resolveAgentEndCancelled(errorContext?: ErrorContext)` — exported: **yes** — Implements logic starting with: { if (_currentResolve) { // Cancellation supersedes the in-flight turn the same way timeout // recovery does — bump the turn epoch so any lingering… (missing: @param errorContext, @returns)
- `src/resources/extensions/gsd/auto/resolve.ts:174` — `_setActiveSession(_session: AutoSession | null)` — exported: **yes** — Implements logic starting with: { // No-op — kept for test backward compatibility } (missing: @param _session)

### `src/resources/extensions/gsd/auto/run-unit.ts`
- `src/resources/extensions/gsd/auto/run-unit.ts:67` — `runUnit(ctx: ExtensionContext, pi: ExtensionAPI, s: AutoSession, unitType: string, unitId: string, prompt: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param s, @param unitType, @param unitId, @param prompt, @returns)

### `src/resources/extensions/gsd/auto/session.ts`
- `src/resources/extensions/gsd/auto/session.ts:278` — `AutoSession.get canonicalProjectRoot()` — exported: **yes** — Implements logic starting with: { const root = this.scope?.workspace.projectRoot ?? resolveWorktreeProjectRoot(this.basePath, this.originalBasePath); return normalizeRealPath(root… (missing: @returns)

### `src/resources/extensions/gsd/auto/turn-epoch.ts`
- `src/resources/extensions/gsd/auto/turn-epoch.ts:36` — `getCurrentTurnGeneration()` — exported: **yes** — Implements logic starting with: { return _currentGeneration; } (missing: @returns)
- `src/resources/extensions/gsd/auto/turn-epoch.ts:45` — `bumpTurnGeneration(reason: string)` — exported: **yes** — Implements logic starting with: { _currentGeneration += 1; debugLog("turnEpoch.bump", { reason, newGeneration: _currentGeneration }); return _currentGeneration; } (missing: @param reason, @returns)
- `src/resources/extensions/gsd/auto/turn-epoch.ts:56` — `runWithTurnGeneration(capturedGen: number, fn: () => T)` — exported: **yes** — Implements logic starting with: { return turnContext.run({ capturedGen }, fn); } (missing: @param capturedGen, @param fn, @returns)
- `src/resources/extensions/gsd/auto/turn-epoch.ts:69` — `isStaleWrite(component?: string)` — exported: **yes** — Implements logic starting with: { const store = turnContext.getStore(); if (!store) return false; const captured = store.capturedGen; const current = _currentGeneration; if (captu… (missing: @param component, @returns)
- `src/resources/extensions/gsd/auto/turn-epoch.ts:90` — `describeTurnEpoch()` — exported: **yes** — Implements logic starting with: { const store = turnContext.getStore(); const captured = store?.capturedGen ?? null; const current = _currentGeneration; return { captured, current… (missing: @returns)

### `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts`
- `src/resources/extensions/gsd/bootstrap/agent-end-recovery.ts:158` — `_handleSessionSwitchAgentEnd(lastMsg: unknown, resolveCancelled: (ctx: ErrorContext) => boolean)` — exported: **yes** — Implements logic starting with: { if (!lastMsg || typeof lastMsg !== "object") return; const m = lastMsg as { stopReason?: unknown; errorMessage?: unknown; content?: unknown }; if… (missing: @param lastMsg, @param resolveCancelled)

### `src/resources/extensions/gsd/bootstrap/crash-log.ts`
- `src/resources/extensions/gsd/bootstrap/crash-log.ts:17` — `writeCrashLog(err: Error, source: string)` — exported: **yes** — Implements logic starting with: { try { const crashDir = join(process.env.GSD_HOME ?? join(homedir(), ".gsd"), "crash"); mkdirSync(crashDir, { recursive: true }); const ts = new D… (missing: @param err, @param source)

### `src/resources/extensions/gsd/bootstrap/db-tools.ts`
- `src/resources/extensions/gsd/bootstrap/db-tools.ts:25` — `registerAlias(pi: ExtensionAPI, toolDef: any, aliasName: string, canonicalName: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param pi, @param toolDef, @param aliasName, @param canonicalName)
- `src/resources/extensions/gsd/bootstrap/db-tools.ts:61` — `readDetails(result: any)` — exported: **no** — Implements logic starting with: { return result?.details ?? result?.structuredContent; } (missing: @param result, @returns)
- `src/resources/extensions/gsd/bootstrap/db-tools.ts:430` — `ensureMilestoneDbRow(milestoneId: string, basePath: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param milestoneId, @param basePath)

### `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts`
- `src/resources/extensions/gsd/bootstrap/dynamic-tools.ts:38` — `resolveProjectRootDbPath(basePath: string)` — exported: **yes** — Implements logic starting with: { return resolveGsdPathContract(basePath).projectDb; } (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/bootstrap/notify-interceptor.ts`
- `src/resources/extensions/gsd/bootstrap/notify-interceptor.ts:19` — `installNotifyInterceptor(ctx: ExtensionContext)` — exported: **yes** — Implements logic starting with: { if (_wrappedContexts.has(ctx.ui)) return; const originalNotify = ctx.ui.notify.bind(ctx.ui); (ctx.ui as any).notify = (message: string, type?: "i… (missing: @param ctx)

### `src/resources/extensions/gsd/bootstrap/register-hooks.ts`
- `src/resources/extensions/gsd/bootstrap/register-hooks.ts:323` — `applyCompactionThresholdOverride(ctx: ExtensionContext)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx)

### `src/resources/extensions/gsd/bootstrap/sanitize-complete-milestone.ts`
- `src/resources/extensions/gsd/bootstrap/sanitize-complete-milestone.ts:20` — `toStr(v: unknown)` — exported: **no** — Implements logic starting with: { if (v == null) return ""; return String(v).trim(); } (missing: @param v, @returns)
- `src/resources/extensions/gsd/bootstrap/sanitize-complete-milestone.ts:30` — `toStrArray(v: unknown)` — exported: **no** — Transforms a collection into a derived result. (missing: @param v, @returns)
- `src/resources/extensions/gsd/bootstrap/sanitize-complete-milestone.ts:41` — `sanitizeCompleteMilestoneParams(raw: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { const actorName = toStr(raw.actorName); const triggerReason = toStr(raw.triggerReason); return { milestoneId: toStr(raw.milestoneId), title: toSt… (missing: @param raw, @returns)

### `src/resources/extensions/gsd/bootstrap/system-context.ts`
- `src/resources/extensions/gsd/bootstrap/system-context.ts:293` — `buildContextMessage(opts: { memoryBlock: string; injection: string | null; forensicsInjection: string | null; })` — exported: **yes** — Implements logic starting with: { const contextCharLimit = getContextMessageCharLimit(); const memoryContent = markMemoryContextSupplied(opts.memoryBlock.trim()); if (opts.injecti… (missing: @param opts, @returns)
- `src/resources/extensions/gsd/bootstrap/system-context.ts:365` — `loadMemoryBlock(userPrompt: string, opts: { includePromptRelevant?: boolean } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: @param userPrompt, @param opts, @returns)
- `src/resources/extensions/gsd/bootstrap/system-context.ts:737` — `buildForensicsContextInjection(basePath: string, prompt: string)` — exported: **yes** — Implements logic starting with: { const marker = readForensicsMarker(basePath); if (!marker) return null; // Expire markers older than 2 hours to avoid stale context const age = D… (missing: @param basePath, @param prompt, @returns)
- `src/resources/extensions/gsd/bootstrap/system-context.ts:761` — `clearForensicsMarker(basePath: string)` — exported: **yes** — Implements logic starting with: { const markerPath = join(basePath, ".gsd", "runtime", "active-forensics.json"); if (existsSync(markerPath)) { try { unlinkSync(markerPath); } catc… (missing: @param basePath)

### `src/resources/extensions/gsd/bootstrap/tool-call-loop-guard.ts`
- `src/resources/extensions/gsd/bootstrap/tool-call-loop-guard.ts:29` — `hashToolCall(toolName: string, args: Record<string, unknown>)` — exported: **no** — Serializes a value to JSON. (missing: @param toolName, @param args, @returns)
- `src/resources/extensions/gsd/bootstrap/tool-call-loop-guard.ts:50` — `checkToolCallLoop(toolName: string, args: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { if (!enabled) return { block: false, count: 0 }; const sig = hashToolCall(toolName, args); if (sig === lastSignature) { consecutiveCount++; } els… (missing: @param toolName, @param args, @returns)
- `src/resources/extensions/gsd/bootstrap/tool-call-loop-guard.ts:101` — `getToolCallLoopCount()` — exported: **yes** — Implements logic starting with: { return consecutiveCount; } (missing: @returns)

### `src/resources/extensions/gsd/bootstrap/write-gate.ts`
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:142` — `shouldPersistWriteGateSnapshot(env: NodeJS.ProcessEnv = process.env)` — exported: **no** — Implements logic starting with: { const v = env.GSD_PERSIST_WRITE_GATE_STATE; return v !== "0" && v !== "false"; } (missing: @param env, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:253` — `isMilestoneDepthVerified(milestoneId: string | null | undefined, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { if (!milestoneId) return false; return getWriteGateState(basePath).verifiedDepthMilestones.has(milestoneId); } (missing: @param milestoneId, @param basePath, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:314` — `isGateQuestionId(questionId: string)` — exported: **yes** — Implements logic starting with: { return GATE_QUESTION_PATTERNS.some(pattern => questionId.includes(pattern)); } (missing: @param questionId, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:322` — `extractDepthVerificationMilestoneId(questionId: string)` — exported: **yes** — Implements logic starting with: { const match = questionId.match(DEPTH_VERIFICATION_MILESTONE_RE); return match?.[1] ?? null; } (missing: @param questionId, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:330` — `extractContextMilestoneId(inputPath: string)` — exported: **no** — Implements logic starting with: { const match = inputPath.match(CONTEXT_MILESTONE_RE); return match?.[1] ?? null; } (missing: @param inputPath, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:338` — `setPendingGate(gateId: string, basePath: string)` — exported: **yes** — Implements logic starting with: { const state = getWriteGateState(basePath); state.pendingGateId = gateId; state.verifiedApprovalGates.delete(gateId); const milestoneId = extractD… (missing: @param gateId, @param basePath)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:350` — `clearPendingGate(basePath: string)` — exported: **yes** — Implements logic starting with: { getWriteGateState(basePath).pendingGateId = null; persistWriteGateSnapshot(basePath); } (missing: @param basePath)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:358` — `getPendingGate(basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return getWriteGateState(basePath).pendingGateId; } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:369` — `shouldBlockPendingGate(toolName: string, milestoneId: string | null, queuePhaseActive?: boolean, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return shouldBlockPendingGateInSnapshot(currentWriteGateSnapshot(basePath), toolName, milestoneId, queuePhaseActive); } (missing: @param toolName, @param milestoneId, @param queuePhaseActive, @param basePath, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:405` — `shouldBlockPendingGateBash(command: string, milestoneId: string | null, queuePhaseActive?: boolean, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return shouldBlockPendingGateBashInSnapshot(currentWriteGateSnapshot(basePath), command, milestoneId, queuePhaseActive); } (missing: @param command, @param milestoneId, @param queuePhaseActive, @param basePath, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:444` — `isDepthConfirmationAnswer(selected: unknown, options?: Array<{ label?: string }>)` — exported: **yes** — Implements logic starting with: { const value = Array.isArray(selected) ? selected[0] : selected; if (typeof value !== "string" || !value) return false; // If options are availabl… (missing: @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:504` — `shouldBlockContextArtifactSave(artifactType: string, milestoneId: string | null, sliceId?: string | null, basePath: string = process.cwd())` — exported: **yes** — Implements logic starting with: { return shouldBlockContextArtifactSaveInSnapshot(currentWriteGateSnapshot(basePath), artifactType, milestoneId, sliceId); } (missing: @param artifactType, @param milestoneId, @param sliceId, @param basePath, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:558` — `shouldBlockRootArtifactSaveInSnapshot(snapshot: WriteGateSnapshot, artifactType: string, opts: { requireVerifiedApproval?: boolean } = {})` — exported: **yes** — Implements logic starting with: { if (!FINAL_ROOT_ARTIFACTS.has(artifactType)) return { block: false }; if (snapshot.pendingGateId) { return { block: true, reason: [ `HARD BLOCK:… (missing: @param snapshot, @param artifactType, @param opts, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:783` — `shouldBlockPlanningUnit(toolName: string, pathOrCommand: string, basePath: string, unitType: string, policy: ToolsPolicy | null | undefined, agentClasses?: readonly string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param toolName, @param pathOrCommand, @param basePath, @param unitType, @param policy, @param agentClasses, @returns)
- `src/resources/extensions/gsd/bootstrap/write-gate.ts:972` — `shouldBlockWorktreeWrite(toolName: string, targetPath: string, effectiveBasePath: string, isAutoLive: boolean, currentUnitType?: string | null)` — exported: **yes** — Implements logic starting with: { const tool = canonicalToolName(toolName); if (!PLANNING_WRITE_TOOLS.has(tool)) return { block: false }; if (process.env.GSD_DISABLE_WORKTREE_WRIT… (missing: @param toolName, @param targetPath, @param effectiveBasePath, @param isAutoLive, @param currentUnitType, @returns)

### `src/resources/extensions/gsd/captures.ts`
- `src/resources/extensions/gsd/captures.ts:61` — `resolveCapturesPath(basePath: string)` — exported: **yes** — Implements logic starting with: { const resolved = resolve(basePath); // Direct layout: /.gsd/worktrees/ const worktreeMarker = `${sep}.gsd${sep}worktrees${sep}`; let idx = resolv… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:89` — `appendCapture(basePath: string, text: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param text, @returns)
- `src/resources/extensions/gsd/captures.ts:122` — `loadAllCaptures(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:133` — `loadPendingCaptures(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:142` — `hasPendingCaptures(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:158` — `countPendingCaptures(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:174` — `markCaptureResolved(basePath: string, captureId: string, classification: Classification, resolution: string, rationale: string, milestoneId?: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param captureId, @param classification, @param resolution, @param rationale, @param milestoneId)
- `src/resources/extensions/gsd/captures.ts:234` — `markCaptureExecuted(basePath: string, captureId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param captureId)
- `src/resources/extensions/gsd/captures.ts:273` — `loadActionableCaptures(basePath: string, currentMilestoneId?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param currentMilestoneId, @returns)
- `src/resources/extensions/gsd/captures.ts:293` — `loadStopCaptures(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:304` — `loadBacktrackCaptures(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:321` — `revertExecutorResolvedCaptures(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/captures.ts:365` — `stampCaptureMilestone(basePath: string, captureId: string, milestoneId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param captureId, @param milestoneId)
- `src/resources/extensions/gsd/captures.ts:402` — `parseCapturesContent(content: string)` — exported: **no** — Implements logic starting with: { const entries: CaptureEntry[] = []; // Split on H3 headings const sections = content.split(/^### /m).slice(1); // skip content before first H3 fo… (missing: @param content, @returns)
- `src/resources/extensions/gsd/captures.ts:451` — `extractBoldField(text: string, key: string)` — exported: **no** — Executes a subprocess command. (missing: @param text, @param key, @returns)
- `src/resources/extensions/gsd/captures.ts:474` — `parseTriageOutput(llmResponse: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param llmResponse, @returns)
- `src/resources/extensions/gsd/captures.ts:498` — `extractJsonSubstring(text: string)` — exported: **no** — Implements logic starting with: { // Find first [ or { const arrStart = text.indexOf("["); const objStart = text.indexOf("{"); let start: number; let openChar: string; let closeCh… (missing: @param text, @returns)

### `src/resources/extensions/gsd/claude-import.ts`
- `src/resources/extensions/gsd/claude-import.ts:99` — `isMarketplacePath(pluginPath: string)` — exported: **no** — Implements logic starting with: { const marketplaceJson = join(pluginPath, ".claude-plugin", "marketplace.json"); return existsSync(marketplaceJson); } (missing: @param pluginPath, @returns)
- `src/resources/extensions/gsd/claude-import.ts:113` — `categorizePluginRoots(pluginRoots: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param pluginRoots, @returns)
- `src/resources/extensions/gsd/claude-import.ts:328` — `formatComponentForSelection(comp: NamespacedComponent)` — exported: **no** — Implements logic starting with: { const typeLabel = comp.type === 'skill' ? '🔧' : '🤖'; const nsLabel = comp.namespace ? `${comp.namespace}:` : ''; return `${typeLabel} ${nsLabel… (missing: @param comp, @returns)
- `src/resources/extensions/gsd/claude-import.ts:338` — `selectMarketplaceComponents(ctx: ExtensionCommandContext, importer: PluginImporter, scope: "global" | "project")` — exported: **no** — Transforms a collection into a derived result. (missing: @param ctx, @param importer, @param scope, @returns)
- `src/resources/extensions/gsd/claude-import.ts:437` — `formatDiagnosticsForUser(diagnostics: Array<{ severity: string; class: string; remediation: string; involvedCanonicalNames: string[] }>)` — exported: **no** — Transforms a collection into a derived result. (missing: @param diagnostics, @returns)
- `src/resources/extensions/gsd/claude-import.ts:467` — `persistManifestToSettings(manifestEntries: ImportManifestEntry[], settingsManager: SettingsManager, scope: "global" | "project")` — exported: **no** — Transforms a collection into a derived result. (missing: @param manifestEntries, @param settingsManager, @param scope)

### `src/resources/extensions/gsd/clean-root-preflight.ts`
- `src/resources/extensions/gsd/clean-root-preflight.ts:215` — `preflightCleanRoot(basePath: string, milestoneId: string, notify: (message: string, level: "info" | "warning" | "error") => void)` — exported: **yes** — Implements logic starting with: { // Fast-path: clean tree — nothing to do let isDirty = false; try { isDirty = nativeHasChanges(basePath); } catch (err) { // If the status check… (missing: @param basePath, @param milestoneId, @param notify, @returns)
- `src/resources/extensions/gsd/clean-root-preflight.ts:269` — `postflightPopStash(basePath: string, milestoneId: string, stashMarker: string | undefined, notify: (message: string, level: "info" | "warning" | "error") => void)` — exported: **yes** — Implements logic starting with: { let stashRef: string | null = null; try { stashRef = findPreflightStashRef(basePath, milestoneId, stashMarker); if (!stashRef) { const msg = `No… (missing: @param basePath, @param milestoneId, @param stashMarker, @param notify, @returns)

### `src/resources/extensions/gsd/codebase-generator.ts`
- `src/resources/extensions/gsd/codebase-generator.ts:122` — `parseCodebaseMap(content: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param content, @returns)
- `src/resources/extensions/gsd/codebase-generator.ts:213` — `enumerateFiles(basePath: string, excludes: string[], maxFiles: number)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param excludes, @param maxFiles, @returns)
- `src/resources/extensions/gsd/codebase-generator.ts:391` — `generateCodebaseMap(basePath: string, options?: CodebaseMapOptions, existingDescriptions?: Map<string, string>)` — exported: **yes** — Implements logic starting with: { const resolved = resolveGeneratorOptions(options); return buildCodebaseMap(basePath, resolved, existingDescriptions); } (missing: @param basePath, @param options, @param existingDescriptions, @returns)
- `src/resources/extensions/gsd/codebase-generator.ts:404` — `updateCodebaseMap(basePath: string, options?: CodebaseMapOptions)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param options, @returns)
- `src/resources/extensions/gsd/codebase-generator.ts:563` — `writeCodebaseMap(basePath: string, content: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param content, @returns)
- `src/resources/extensions/gsd/codebase-generator.ts:575` — `readCodebaseMap(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/codebase-generator.ts:588` — `getCodebaseMapStats(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/commands-cmux.ts`
- `src/resources/extensions/gsd/commands-cmux.ts:16` — `autoEnableCmuxPreferences()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/commands-codebase.ts`
- `src/resources/extensions/gsd/commands-codebase.ts:157` — `resolveCodebaseOptions(args: string, ctx: ExtensionCommandContext)` — exported: **no** — Implements logic starting with: { // Load preferences defaults const prefs = loadEffectiveGSDPreferences()?.preferences?.codebase; // Parse CLI flags const maxFilesStr = extractFl… (missing: @param args, @param ctx, @returns)

### `src/resources/extensions/gsd/commands-eval-review.ts`
- `src/resources/extensions/gsd/commands-eval-review.ts:138` — `class EvalReviewArgError` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/gsd/commands-extensions.ts`
- `src/resources/extensions/gsd/commands-extensions.ts:32` — `isVersionGreater(a: string, b: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/commands-extensions.ts:139` — `withRegistryLock(mutate: (registry: ExtensionRegistry) => T)` — exported: **no** — Performs filesystem I/O. (missing: @param mutate, @returns)
- `src/resources/extensions/gsd/commands-extensions.ts:350` — `postInstallValidate(destPath: string, specifier: string, ctx: ExtensionCommandContext)` — exported: **no** — Performs filesystem I/O. (missing: @param destPath, @param specifier, @param ctx, @returns)
- `src/resources/extensions/gsd/commands-extensions.ts:403` — `writeInstalledRegistryEntry(id: string, manifest: ExtensionManifest, specifier: string, installType: "npm" | "git" | "local")` — exported: **no** — Implements logic starting with: { withRegistryLock((registry) => { registry.entries[id] = { id, enabled: true, source: "user", version: manifest.version, installedFrom: specifier,… (missing: @param id, @param manifest, @param specifier, @param installType)
- `src/resources/extensions/gsd/commands-extensions.ts:427` — `findDependents(targetId: string, installedExtDir: string)` — exported: **no** — Implements logic starting with: { const dependents: string[] = []; if (!existsSync(installedExtDir)) return dependents; for (const entry of readdirSync(installedExtDir, { withFile… (missing: @param targetId, @param installedExtDir, @returns)
- `src/resources/extensions/gsd/commands-extensions.ts:593` — `parseNpmSpecifier(specifier: string)` — exported: **no** — Implements logic starting with: { const isScoped = specifier.startsWith("@"); const searchFrom = isScoped ? specifier.indexOf("/") + 1 : 0; const atIdx = specifier.indexOf("@", se… (missing: @param specifier, @returns)

### `src/resources/extensions/gsd/commands-extract-learnings.ts`
- `src/resources/extensions/gsd/commands-extract-learnings.ts:136` — `parseExtractLearningsArgs(args: string)` — exported: **yes** — Implements logic starting with: { const trimmed = args.trim(); return { milestoneId: trimmed || null }; } (missing: @param args, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:145` — `buildLearningsOutputPath(milestoneDir: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { return join(milestoneDir, `${milestoneId}-LEARNINGS.md`); } (missing: @param milestoneDir, @param milestoneId, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:156` — `resolvePhaseArtifacts(milestoneDir: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const missingRequired: string[] = []; const roadmapFile = `${milestoneId}-ROADMAP.md`; const summaryFile = `${milestoneId}-SUMMARY.md`; const ver… (missing: @param milestoneDir, @param milestoneId, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:192` — `buildExtractionStepsBlock(ctx: ExtractionStepsContext)` — exported: **yes** — Implements logic starting with: { return `## Structured Learnings Extraction Perform the following steps IN ORDER. Each step is mandatory unless explicitly marked optional. These… (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:302` — `buildExtractLearningsPrompt(ctx: ExtractLearningsPromptContext)` — exported: **yes** — Implements logic starting with: { const optionalSections: string[] = []; if (ctx.verificationContent) { optionalSections.push(`### Verification Report\n\n${ctx.verificationContent… (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:364` — `buildFrontmatter(ctx: FrontmatterContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:395` — `extractProjectName(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/commands-extract-learnings.ts:422` — `handleExtractLearnings(args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Performs filesystem I/O. (missing: @param args, @param ctx, @param pi)

### `src/resources/extensions/gsd/commands-handlers.ts`
- `src/resources/extensions/gsd/commands-handlers.ts:104` — `parseDoctorArgs(args: string)` — exported: **yes** — Implements logic starting with: { const trimmed = args.trim(); const jsonMode = trimmed.includes("--json"); const dryRun = trimmed.includes("--dry-run"); const fixFlag = trimmed.i… (missing: @param args, @returns)

### `src/resources/extensions/gsd/commands-logs.ts`
- `src/resources/extensions/gsd/commands-logs.ts:118` — `summarizeActivityLog(filePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param filePath, @returns)
- `src/resources/extensions/gsd/commands-logs.ts:187` — `summarizeDebugLog(filePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param filePath, @returns)

### `src/resources/extensions/gsd/commands-maintenance.ts`
- `src/resources/extensions/gsd/commands-maintenance.ts:490` — `handleRecover(ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param basePath)

### `src/resources/extensions/gsd/commands-mcp-status.ts`
- `src/resources/extensions/gsd/commands-mcp-status.ts:180` — `handleMcpStatus(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param args, @param ctx)

### `src/resources/extensions/gsd/commands-pr-branch.ts`
- `src/resources/extensions/gsd/commands-pr-branch.ts:84` — `cherryPickFiltered(basePath: string, sha: string)` — exported: **no** — Implements logic starting with: { git(basePath, ["cherry-pick", "--no-commit", "--allow-empty", sha]); // Unstage any excluded paths introduced by the cherry-pick. gitAllowFail(ba… (missing: @param basePath, @param sha, @returns)

### `src/resources/extensions/gsd/commands-prefs-wizard.ts`
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:26` — `extractBodyAfterFrontmatter(content: string)` — exported: **no** — Implements logic starting with: { const closingIdx = content.indexOf("\n---", content.indexOf("---")); if (closingIdx === -1) return null; const afterFrontmatter = content.slice(c… (missing: @param content, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:36` — `tryParseInteger(val: string)` — exported: **no** — Implements logic starting with: { return /^\d+$/.test(val) ? Number(val) : null; } (missing: @param val, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:41` — `tryParseNumber(val: string)` — exported: **no** — Implements logic starting with: { const n = Number(val); return !isNaN(n) && isFinite(n) ? n : null; } (missing: @param val, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:47` — `tryParsePercentage(val: string)` — exported: **no** — Implements logic starting with: { const n = Number(val); return !isNaN(n) && n >= 0 && n <= 100 ? n : null; } (missing: @param val, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:55` — `promptBoolean(ctx: ExtensionCommandContext, label: string, current: unknown, defaultVal?: boolean)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param label, @param current, @param defaultVal, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:71` — `promptEnum(ctx: ExtensionCommandContext, label: string, current: unknown, values: readonly string[], defaultVal?: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param label, @param current, @param values, @param defaultVal, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:93` — `promptInteger(ctx: ExtensionCommandContext, label: string, current: unknown, defaultVal?: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param label, @param current, @param defaultVal, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:115` — `promptNumber(ctx: ExtensionCommandContext, label: string, current: unknown, defaultVal?: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param label, @param current, @param defaultVal, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:137` — `applyNumber(prefs: Record<string, unknown>, key: string, result: number | "clear" | undefined)` — exported: **no** — Implements logic starting with: { if (result === undefined) return; if (result === "clear") delete prefs[key]; else prefs[key] = result; } (missing: @param prefs, @param key, @param result)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:144` — `promptString(ctx: ExtensionCommandContext, label: string, current: unknown, defaultVal?: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param label, @param current, @param defaultVal, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:158` — `parseStringList(input: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param input, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:166` — `editStringListField(ctx: ExtensionCommandContext, prefs: Record<string, unknown>, key: string, label: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param ctx, @param prefs, @param key, @param label)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:208` — `setNested(parent: Record<string, unknown>, parentKey: string, childKey: string, value: unknown)` — exported: **no** — Implements logic starting with: { let child = parent[parentKey] as Record<string, unknown> | undefined; if (!child || typeof child !== "object") child = {}; if (value === undefine… (missing: @param parent, @param parentKey, @param childKey, @param value)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:326` — `buildCategorySummaries(prefs: Record<string, unknown>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param prefs, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1628` — `writePreferencesFile(path: string, prefs: Record<string, unknown>, ctx: ExtensionCommandContext | null, opts?: { scope?: "global" | "project"; defaultBody?: string; notifyOnSave?: boolean })` — exported: **yes** — Performs filesystem I/O. (missing: @param path, @param prefs, @param ctx, @param opts)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1662` — `yamlSafeString(val: unknown)` — exported: **yes** — Implements logic starting with: { if (typeof val !== "string") return String(val); if (/[:#{\[\]'"`,|>&*!?@%\r\n]/.test(val) || val.trim() !== val || val === "") { return `"${val.… (missing: @param val, @returns)
- `src/resources/extensions/gsd/commands-prefs-wizard.ts:1789` — `handleLanguage(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Performs filesystem I/O. (missing: @param args, @param ctx)

### `src/resources/extensions/gsd/commands-scan.ts`
- `src/resources/extensions/gsd/commands-scan.ts:48` — `parseScanArgs(args: string)` — exported: **yes** — Implements logic starting with: { const match = args.match(/--focus\s+([^\s]+)/i); if (!match) return { focus: DEFAULT_FOCUS }; const raw = match[1].toLowerCase(); if ((VALID_FOCU… (missing: @param args, @returns)
- `src/resources/extensions/gsd/commands-scan.ts:63` — `resolveScanDocuments(focus: string)` — exported: **yes** — Implements logic starting with: { return FOCUS_DOCUMENTS[focus as FocusArea] ?? FOCUS_DOCUMENTS[DEFAULT_FOCUS]; } (missing: @param focus, @returns)
- `src/resources/extensions/gsd/commands-scan.ts:71` — `buildScanOutputPaths(focus: string, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param focus, @param basePath, @returns)
- `src/resources/extensions/gsd/commands-scan.ts:79` — `checkExistingDocuments(paths: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param paths, @returns)

### `src/resources/extensions/gsd/commands-workflow-templates.ts`
- `src/resources/extensions/gsd/commands-workflow-templates.ts:39` — `slugify(text: string)` — exported: **no** — Implements logic starting with: { return text .toLowerCase() .replace(/[^a-z0-9]+/g, "-") .replace(/^-|-$/g, "") .slice(0, 40) .replace(/-$/, ""); } (missing: @param text, @returns)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:51` — `getNextWorkflowNum(workflowDir: string)` — exported: **no** — Implements logic starting with: { if (!existsSync(workflowDir)) return 1; try { const entries = readdirSync(workflowDir, { withFileTypes: true }); let max = 0; for (const entry of… (missing: @param workflowDir, @returns)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:73` — `datePrefix()` — exported: **no** — Implements logic starting with: { const d = new Date(); const yy = String(d.getFullYear()).slice(2); const mm = String(d.getMonth() + 1).padStart(2, "0"); const dd = String(d.getD… (missing: @returns)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:105` — `writeWorkflowState(artifactDir: string, templateId: string, templateName: string, phases: string[], description: string, branch: string)` — exported: **no** — Performs filesystem I/O. (missing: @param artifactDir, @param templateId, @param templateName, @param phases, @param description, @param branch)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:136` — `findInProgressWorkflows(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:554` — `getTemplateCompletions(prefix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param prefix, @returns)
- `src/resources/extensions/gsd/commands-workflow-templates.ts:579` — `dispatchMarkdownPhasePlugin(plugin: WorkflowPlugin, description: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **yes** — Performs filesystem I/O. (missing: @param plugin, @param description, @param ctx, @param pi)

### `src/resources/extensions/gsd/commands/context.ts`
- `src/resources/extensions/gsd/commands/context.ts:20` — `class GSDNoProjectError` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/gsd/commands/handlers/auto.ts`
- `src/resources/extensions/gsd/commands/handlers/auto.ts:16` — `parseYoloFlag(trimmed: string)` — exported: **no** — Implements logic starting with: { const yoloRe = /(?:--yolo|-y)\s+("(?:[^"\\]|\\.)*"|'(?:[^'\\]|\\.)*'|\S+)/; const match = trimmed.match(yoloRe); if (!match) return { yoloSeedFil… (missing: @param trimmed, @returns)
- `src/resources/extensions/gsd/commands/handlers/auto.ts:38` — `parseMilestoneTarget(input: string)` — exported: **yes** — Implements logic starting with: { const match = input.match(/\b(M\d+(?:-[a-z0-9]{6})?)\b/); if (!match) return { milestoneId: null, rest: input }; const rest = input.replace(match… (missing: @param input, @returns)

### `src/resources/extensions/gsd/commands/handlers/workflow.ts`
- `src/resources/extensions/gsd/commands/handlers/workflow.ts:56` — `requireNotAutoActive(commandName: string, ctx: ExtensionCommandContext)` — exported: **no** — Implements logic starting with: { if (!isAutoActive()) return false; ctx.ui.notify( `${commandName} cannot run while auto-mode is active.\n` + `Stop auto-mode first with /gsd stop… (missing: @param commandName, @param ctx, @returns)
- `src/resources/extensions/gsd/commands/handlers/workflow.ts:154` — `parseWorkflowOverridesOnly(args: string)` — exported: **yes** — Implements logic starting with: { const parts = splitWorkflowRunArgs(args); const overrides: Record<string, string> = {}; for (const part of parts) { const eqIdx = part.indexOf("=… (missing: @param args, @returns)
- `src/resources/extensions/gsd/commands/handlers/workflow.ts:169` — `dispatchPluginByMode(plugin: WorkflowPlugin, args: string, ctx: ExtensionCommandContext, pi: ExtensionAPI)` — exported: **no** — Implements logic starting with: { switch (plugin.meta.mode) { case "oneshot": { dispatchOneshot(plugin, pi, args.trim()); ctx.ui.notify(`Running oneshot workflow: ${plugin.meta.di… (missing: @param plugin, @param args, @param ctx, @param pi)

### `src/resources/extensions/gsd/compaction-snapshot.ts`
- `src/resources/extensions/gsd/compaction-snapshot.ts:42` — `buildSnapshot(sources: SnapshotSources, opts: BuildSnapshotOptions = {})` — exported: **yes** — Implements logic starting with: { const maxBytes = opts.maxBytes ?? DEFAULT_SNAPSHOT_BYTES; const maxMemories = opts.maxMemories ?? 6; const maxExec = opts.maxExec ?? 5; const lin… (missing: @param sources, @param opts, @returns)

### `src/resources/extensions/gsd/complexity-classifier.ts`
- `src/resources/extensions/gsd/complexity-classifier.ts:68` — `classifyUnitComplexity(unitType: string, unitId: string, basePath: string, budgetPct?: number, metadata?: TaskMetadata)` — exported: **yes** — Implements logic starting with: { // Hook units default to light if (unitType.startsWith("hook/")) { const result: ClassificationResult = { tier: "light", reason: "hook unit", dow… (missing: @returns)
- `src/resources/extensions/gsd/complexity-classifier.ts:120` — `tierLabel(tier: ComplexityTier)` — exported: **yes** — Implements logic starting with: { switch (tier) { case "light": return "L"; case "standard": return "S"; case "heavy": return "H"; } } (missing: @param tier, @returns)
- `src/resources/extensions/gsd/complexity-classifier.ts:131` — `tierOrdinal(tier: ComplexityTier)` — exported: **yes** — Implements logic starting with: { switch (tier) { case "light": return 0; case "standard": return 1; case "heavy": return 2; } } (missing: @param tier, @returns)
- `src/resources/extensions/gsd/complexity-classifier.ts:222` — `extractTaskMetadata(unitId: string, basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param unitId, @param basePath, @returns)
- `src/resources/extensions/gsd/complexity-classifier.ts:297` — `applyBudgetPressure(result: ClassificationResult, budgetPct?: number)` — exported: **no** — Implements logic starting with: { if (budgetPct === undefined || budgetPct < 0.5) return result; const original = result.tier; if (budgetPct >= 0.9) { // >90%: almost everything g… (missing: @param result, @param budgetPct, @returns)

### `src/resources/extensions/gsd/component-loader.ts`
- `src/resources/extensions/gsd/component-loader.ts:51` — `loadComponentFromDir(dir: string, source: ComponentSource)` — exported: **yes** — Implements logic starting with: { const diagnostics: ComponentDiagnostic[] = []; // Try new format first: component.yaml const componentYamlPath = join(dir, 'component.yaml'); if… (missing: @param dir, @param source, @returns)
- `src/resources/extensions/gsd/component-loader.ts:76` — `loadComponentFromAgentFile(filePath: string, source: ComponentSource)` — exported: **yes** — Implements logic starting with: { return loadFromLegacyAgent(filePath, source); } (missing: @param filePath, @param source, @returns)
- `src/resources/extensions/gsd/component-loader.ts:358` — `scanComponentDir(dir: string, source: ComponentSource, kind?: ComponentKind)` — exported: **yes** — Implements logic starting with: { const components: Component[] = []; const diagnostics: ComponentDiagnostic[] = []; if (!existsSync(dir)) { return { components, diagnostics }; }… (missing: @param dir, @param source, @param kind, @returns)
- `src/resources/extensions/gsd/component-loader.ts:423` — `scanAgentDir(dir: string, source: ComponentSource)` — exported: **yes** — Implements logic starting with: { const components: Component[] = []; const diagnostics: ComponentDiagnostic[] = []; if (!existsSync(dir)) { return { components, diagnostics }; }… (missing: @param dir, @param source, @returns)
- `src/resources/extensions/gsd/component-loader.ts:492` — `loadFromFile(filePath: string, source: ComponentSource)` — exported: **no** — Performs filesystem I/O. (missing: @param filePath, @param source, @returns)

### `src/resources/extensions/gsd/component-types.ts`
- `src/resources/extensions/gsd/component-types.ts:312` — `validateComponentName(name: string)` — exported: **yes** — Implements logic starting with: { const errors: string[] = []; if (!name || name.trim() === '') { errors.push('name is required'); return errors; } if (name.length > MAX_NAME_LENG… (missing: @param name)
- `src/resources/extensions/gsd/component-types.ts:345` — `validateComponentDescription(description: string | undefined)` — exported: **yes** — Implements logic starting with: { const errors: string[] = []; if (!description || description.trim() === '') { errors.push('description is required'); } else if (description.leng… (missing: @param description)
- `src/resources/extensions/gsd/component-types.ts:360` — `computeComponentId(name: string, namespace?: string)` — exported: **yes** — Implements logic starting with: { return namespace ? `${namespace}:${name}` : name; } (missing: @param name, @param namespace, @returns)

### `src/resources/extensions/gsd/context-budget.ts`
- `src/resources/extensions/gsd/context-budget.ts:124` — `computeBudgets(contextWindow: number, provider?: TokenProvider)` — exported: **yes** — Implements logic starting with: { const effectiveWindow = contextWindow > 0 ? contextWindow : DEFAULT_CONTEXT_WINDOW; const charsPerToken = provider ? getCharsPerToken(provider) :… (missing: @param contextWindow, @param provider, @returns)
- `src/resources/extensions/gsd/context-budget.ts:169` — `truncateAtSectionBoundary(content: string, budgetChars: number)` — exported: **yes** — Implements logic starting with: { if (!content || content.length <= budgetChars) { return { content, droppedSections: 0 }; } // Split on section markers: ### headings or --- divid… (missing: @param content, @param budgetChars, @returns)
- `src/resources/extensions/gsd/context-budget.ts:219` — `resolveExecutorContextWindow(registry: MinimalModelRegistry | undefined, preferences: MinimalPreferences | undefined, sessionContextWindow?: number, sessionProvider?: string)` — exported: **yes** — Implements logic starting with: { // Step 1: Try configured executor model if (preferences?.models?.execution && registry) { const executionConfig = preferences.models.execution;… (missing: @param registry, @param preferences, @param sessionContextWindow, @param sessionProvider, @returns)
- `src/resources/extensions/gsd/context-budget.ts:252` — `reduceToFit(content: string, budgetChars: number)` — exported: **yes** — Implements logic starting with: { if (!content || content.length <= budgetChars) { return { content, droppedSections: 0 }; } return truncateAtSectionBoundary(content, budgetChars)… (missing: @param content, @param budgetChars, @returns)
- `src/resources/extensions/gsd/context-budget.ts:265` — `resolveTaskCountMax(contextWindow: number)` — exported: **no** — Implements logic starting with: { for (const [threshold, max] of TASK_COUNT_TIERS) { if (contextWindow >= threshold) return max; } return 3; // fallback — unreachable given tiers… (missing: @param contextWindow, @returns)
- `src/resources/extensions/gsd/context-budget.ts:283` — `splitIntoSections(content: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/context-budget.ts:295` — `findModelById(registry: MinimalModelRegistry, modelId: string)` — exported: **no** — Implements logic starting with: { const allModels = registry.getAll(); const slashIdx = modelId.indexOf("/"); if (slashIdx !== -1) { const provider = modelId.substring(0, slashIdx… (missing: @param registry, @param modelId, @returns)

### `src/resources/extensions/gsd/context-masker.ts`
- `src/resources/extensions/gsd/context-masker.ts:45` — `isBashResultUserMessage(m: MaskableMessage)` — exported: **no** — Implements logic starting with: { if (m.role !== "user" || !Array.isArray(m.content)) return false; const first = m.content[0]; return first && typeof first === "object" && "text"… (missing: @param m, @returns)

### `src/resources/extensions/gsd/context-store.ts`
- `src/resources/extensions/gsd/context-store.ts:30` — `queryDecisions(opts?: DecisionQueryOpts)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param opts, @returns)
- `src/resources/extensions/gsd/context-store.ts:79` — `readDecisionsFromMemories(opts: DecisionQueryOpts | undefined, includeSuperseded: boolean)` — exported: **no** — Parses JSON input into a structured value. (missing: @param opts, @returns)
- `src/resources/extensions/gsd/context-store.ts:168` — `queryDecisionsFromMemories(opts?: DecisionQueryOpts)` — exported: **yes** — Implements logic starting with: { return readDecisionsFromMemories(opts, /* includeSuperseded */ false); } (missing: @param opts, @returns)
- `src/resources/extensions/gsd/context-store.ts:184` — `getAllDecisionsFromMemories()` — exported: **yes** — Implements logic starting with: { return readDecisionsFromMemories(undefined, /* includeSuperseded */ true); } (missing: @returns)
- `src/resources/extensions/gsd/context-store.ts:196` — `queryRequirements(opts?: RequirementQueryOpts)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param opts, @returns)
- `src/resources/extensions/gsd/context-store.ts:253` — `formatDecisionsForPrompt(decisions: Decision[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param decisions, @returns)
- `src/resources/extensions/gsd/context-store.ts:269` — `formatRequirementsForPrompt(requirements: Requirement[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param requirements, @returns)
- `src/resources/extensions/gsd/context-store.ts:304` — `queryArtifact(path: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const adapter = _getAdapter(); if (!adapter) return null; try { const row = adapter.prepare('SELECT full_conte… (missing: @param path, @returns)
- `src/resources/extensions/gsd/context-store.ts:325` — `queryProject()` — exported: **yes** — Implements logic starting with: { return queryArtifact('PROJECT.md'); } (missing: @returns)
- `src/resources/extensions/gsd/context-store.ts:413` — `formatRoadmapExcerpt(roadmapContent: string, sliceId: string, roadmapPath = 'ROADMAP.md')` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `src/resources/extensions/gsd/crash-recovery.ts`
- `src/resources/extensions/gsd/crash-recovery.ts:90` — `getLatestDispatchForWorker(workerId: string)` — exported: **no** — Implements logic starting with: { if (!isDbAvailable()) return null; const db = _getAdapter()!; const row = db.prepare( `SELECT unit_type, unit_id, started_at, status FROM unit_di… (missing: @param workerId, @returns)
- `src/resources/extensions/gsd/crash-recovery.ts:168` — `writeLock(basePath: string, unitType: string, unitId: string, sessionFile?: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @param unitType, @param unitId, @param sessionFile)
- `src/resources/extensions/gsd/crash-recovery.ts:206` — `clearLock(basePath: string)` — exported: **yes** — Implements logic starting with: { try { const p = lockPath(basePath); if (existsSync(p)) unlinkSync(p); } catch { // Best-effort. } if (!isDbAvailable()) return; try { const proje… (missing: @param basePath)
- `src/resources/extensions/gsd/crash-recovery.ts:233` — `readCrashLock(basePath: string)` — exported: **yes** — Implements logic starting with: { if (isDbAvailable()) { try { const projectRoot = normalizeRealPath(basePath); const stale = findStaleWorkerForProject(projectRoot); if (stale) re… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/crash-recovery.ts:253` — `isLockProcessAlive(lock: LockData)` — exported: **yes** — Implements logic starting with: { const pid = lock.pid; if (!Number.isInteger(pid) || pid <= 0) return false; if (pid === process.pid) return true; try { process.kill(pid, 0); ret… (missing: @param lock, @returns)
- `src/resources/extensions/gsd/crash-recovery.ts:267` — `formatCrashInfo(lock: LockData)` — exported: **yes** — Implements logic starting with: { const lines = [ `Previous auto-mode session was interrupted.`, ` Was executing: ${lock.unitType} (${lock.unitId})`, ` Started at: ${lock.unitStar… (missing: @param lock, @returns)
- `src/resources/extensions/gsd/crash-recovery.ts:292` — `emitCrashRecoveredUnitEnd(basePath: string, lock: LockData)` — exported: **yes** — Implements logic starting with: { if (!lock.unitType || !lock.unitId || lock.unitType === "starting") return; emitOpenUnitEndForUnit(basePath, lock.unitType, lock.unitId, "crash-r… (missing: @param basePath, @param lock)
- `src/resources/extensions/gsd/crash-recovery.ts:368` — `findStaleAutoWorker(basePath: string)` — exported: **yes** — Implements logic starting with: { return readCrashLock(basePath); } (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/custom-execution-policy.ts`
- `src/resources/extensions/gsd/custom-execution-policy.ts:27` — `CustomExecutionPolicy.prepareWorkspace(_basePath: string, _milestoneId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _basePath, @param _milestoneId)
- `src/resources/extensions/gsd/custom-execution-policy.ts:32` — `CustomExecutionPolicy.selectModel(_unitType: string, _unitId: string, _context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _unitType, @param _unitId, @param _context, @returns)
- `src/resources/extensions/gsd/custom-execution-policy.ts:47` — `CustomExecutionPolicy.verify(_unitType: string, unitId: string, _context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _unitType, @param unitId, @param _context, @returns)
- `src/resources/extensions/gsd/custom-execution-policy.ts:58` — `CustomExecutionPolicy.recover(_unitType: string, _unitId: string, _context: { basePath: string })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _unitType, @param _unitId, @param _context, @returns)
- `src/resources/extensions/gsd/custom-execution-policy.ts:67` — `CustomExecutionPolicy.closeout(_unitType: string, _unitId: string, _context: { basePath: string; startedAt: number })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _unitType, @param _unitId, @param _context, @returns)

### `src/resources/extensions/gsd/custom-verification.ts`
- `src/resources/extensions/gsd/custom-verification.ts:66` — `dispatchPolicy(runDir: string, step: StepDefinition, verify: VerifyPolicy)` — exported: **no** — Implements logic starting with: { switch (verify.policy) { case "content-heuristic": return handleContentHeuristic(runDir, step, verify); case "shell-command": return handleShellC… (missing: @param runDir, @param step, @param verify, @returns)
- `src/resources/extensions/gsd/custom-verification.ts:97` — `handleContentHeuristic(runDir: string, step: StepDefinition, verify: { policy: "content-heuristic"; minSize?: number; pattern?: string })` — exported: **no** — Performs filesystem I/O. (missing: @param runDir, @param step, @param verify, @returns)
- `src/resources/extensions/gsd/custom-verification.ts:156` — `handleShellCommand(runDir: string, verify: { policy: "shell-command"; command: string })` — exported: **no** — Executes a subprocess command. (missing: @param runDir, @param verify, @returns)

### `src/resources/extensions/gsd/custom-workflow-engine.ts`
- `src/resources/extensions/gsd/custom-workflow-engine.ts:76` — `CustomWorkflowEngine.deriveState(_basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param _basePath, @returns)
- `src/resources/extensions/gsd/custom-workflow-engine.ts:109` — `CustomWorkflowEngine.resolveDispatch(state: EngineState, _context: { basePath: string })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param state, @param _context, @returns)
- `src/resources/extensions/gsd/custom-workflow-engine.ts:229` — `CustomWorkflowEngine.reconcile(state: EngineState, completedStep: CompletedStep)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param state, @param completedStep, @returns)
- `src/resources/extensions/gsd/custom-workflow-engine.ts:262` — `CustomWorkflowEngine.getDisplayMetadata(state: EngineState)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param state, @returns)

### `src/resources/extensions/gsd/db-coordination-schema.ts`
- `src/resources/extensions/gsd/db-coordination-schema.ts:18` — `createCoordinationTablesV24(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: @param db)

### `src/resources/extensions/gsd/db-memory-fts-schema.ts`
- `src/resources/extensions/gsd/db-memory-fts-schema.ts:21` — `tryCreateMemoriesFtsSchema(db: DbAdapter, options: MemoryFtsSchemaOptions = {})` — exported: **yes** — Executes a subprocess command. (missing: @param db, @param options, @returns)

### `src/resources/extensions/gsd/db-runtime-kv-schema.ts`
- `src/resources/extensions/gsd/db-runtime-kv-schema.ts:19` — `createRuntimeKvTableV25(db: DbAdapter)` — exported: **yes** — Executes a subprocess command. (missing: @param db)

### `src/resources/extensions/gsd/db-writer.ts`
- `src/resources/extensions/gsd/db-writer.ts:36` — `isDecisionsTableFormat(content: string)` — exported: **yes** — Implements logic starting with: { // The canonical format always starts with "# Decisions Register" const firstLine = content.split('\n')[0]?.trim() ?? ''; if (firstLine !== '# De… (missing: @param content, @returns)
- `src/resources/extensions/gsd/db-writer.ts:49` — `generateDecisionsAppendBlock(decisions: Decision[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param decisions, @returns)
- `src/resources/extensions/gsd/db-writer.ts:85` — `generateDecisionsMd(decisions: Decision[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param decisions, @returns)
- `src/resources/extensions/gsd/db-writer.ts:133` — `generateRequirementsMd(requirements: Requirement[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param requirements, @returns)
- `src/resources/extensions/gsd/db-writer.ts:217` — `nextDecisionId()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/gsd/db-writer.ts:249` — `nextDecisionIdAcrossSurfaces(adapter: ReturnType<typeof import('./gsd-db.js')._getAdapter>)` — exported: **no** — Parses JSON input into a structured value. (missing: @param adapter, @returns)
- `src/resources/extensions/gsd/db-writer.ts:306` — `nextRequirementId()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/gsd/db-writer.ts:350` — `saveRequirementToDb(fields: SaveRequirementFields, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param fields, @param basePath, @returns)
- `src/resources/extensions/gsd/db-writer.ts:489` — `saveDecisionToDb(fields: SaveDecisionFields, basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param fields, @param basePath, @returns)
- `src/resources/extensions/gsd/db-writer.ts:634` — `mirrorDecisionToMemory(id: string, normalizedFields: NormalizedSaveDecisionFields)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param id, @param normalizedFields, @returns)
- `src/resources/extensions/gsd/db-writer.ts:684` — `extractDeferredSliceRef(fields: Pick<SaveDecisionFields, 'scope' | 'decision' | 'choice'>)` — exported: **yes** — Implements logic starting with: { const isDeferral = /\bdefer(?:ral|red|ring|s)?\b/i.test(fields.scope) || /\bdefer(?:ral|red|ring|s)?\b/i.test(fields.choice) || /\bdefer(?:ral|re… (missing: @param fields, @returns)
- `src/resources/extensions/gsd/db-writer.ts:714` — `updateRequirementInDb(id: string, updates: Partial<Requirement>, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param id, @param updates, @param basePath)
- `src/resources/extensions/gsd/db-writer.ts:807` — `saveArtifactToDbForWorkspace(workspace: GsdWorkspace, opts: SaveArtifactOpts)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param workspace, @param opts)
- `src/resources/extensions/gsd/db-writer.ts:873` — `saveArtifactToDbByScope(scope: MilestoneScope, opts: SaveArtifactOpts)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param scope, @param opts)
- `src/resources/extensions/gsd/db-writer.ts:956` — `saveArtifactToDb(opts: SaveArtifactOpts, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param opts, @param basePath)

### `src/resources/extensions/gsd/db/auto-workers.ts`
- `src/resources/extensions/gsd/db/auto-workers.ts:56` — `registerAutoWorker(opts: { projectRootRealpath: string; })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param opts, @returns)
- `src/resources/extensions/gsd/db/auto-workers.ts:109` — `heartbeatAutoWorker(workerId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const now = new Date().toISOString(); const db = _getAdapter()!; db.prepare( `UPDATE workers SET last_heartbeat_at… (missing: @param workerId)
- `src/resources/extensions/gsd/db/auto-workers.ts:122` — `markWorkerCrashed(workerId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const db = _getAdapter()!; let changes = 0; transaction(() => { const result = db.prepare( `UPDATE workers SET stat… (missing: @param workerId)
- `src/resources/extensions/gsd/db/auto-workers.ts:150` — `markWorkerStopping(workerId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const db = _getAdapter()!; transaction(() => { db.prepare( `UPDATE workers SET status = 'stopping' WHERE worker_id… (missing: @param workerId)
- `src/resources/extensions/gsd/db/auto-workers.ts:166` — `getActiveAutoWorkers()` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const db = _getAdapter()!; const cutoffMs = Date.now() - HEARTBEAT_TTL_SECONDS * 1000; const cutoffIso = new Dat… (missing: @returns)
- `src/resources/extensions/gsd/db/auto-workers.ts:182` — `getAllAutoWorkers()` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const db = _getAdapter()!; const rows = db.prepare( `SELECT worker_id, host, pid, started_at, version, last_hear… (missing: @returns)
- `src/resources/extensions/gsd/db/auto-workers.ts:197` — `getAutoWorker(workerId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const db = _getAdapter()!; const row = db.prepare( `SELECT worker_id, host, pid, started_at, version, last_hea… (missing: @param workerId, @returns)
- `src/resources/extensions/gsd/db/auto-workers.ts:209` — `autoWorkerHeartbeatTtlSeconds()` — exported: **yes** — Implements logic starting with: { return HEARTBEAT_TTL_SECONDS; } (missing: @returns)
- `src/resources/extensions/gsd/db/auto-workers.ts:238` — `findStaleWorkerForProject(projectRootRealpath: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const db = _getAdapter()!; const cutoffMs = Date.now() - HEARTBEAT_TTL_SECONDS * 1000; const cutoffIso = new D… (missing: @param projectRootRealpath, @returns)

### `src/resources/extensions/gsd/db/command-queue.ts`
- `src/resources/extensions/gsd/db/command-queue.ts:46` — `enqueueCommand(input: EnqueueInput)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param input, @returns)
- `src/resources/extensions/gsd/db/command-queue.ts:73` — `claimNextCommand(workerId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const now = new Date().toISOString(); const db = _getAdapter()!; return transaction((): CommandQueueRow | null… (missing: @param workerId, @returns)
- `src/resources/extensions/gsd/db/command-queue.ts:117` — `completeCommand(id: number, workerId: string, result?: Record<string, unknown>)` — exported: **yes** — Serializes a value to JSON. (missing: @param id, @param workerId, @param result)
- `src/resources/extensions/gsd/db/command-queue.ts:140` — `getCommand(id: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const db = _getAdapter()!; const row = db.prepare( `SELECT id, target_worker, command, args_json, enqueued_at,… (missing: @param id, @returns)

### `src/resources/extensions/gsd/db/milestone-leases.ts`
- `src/resources/extensions/gsd/db/milestone-leases.ts:74` — `claimMilestoneLease(workerId: string, milestoneId: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param workerId, @param milestoneId, @returns)
- `src/resources/extensions/gsd/db/milestone-leases.ts:187` — `refreshMilestoneLease(workerId: string, milestoneId: string, fencingToken: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; const now = new Date(); const expiresIso = ttlExpiry(now); const db = _getAdapter()!; const result = db.prepa… (missing: @param workerId, @param milestoneId, @param fencingToken, @returns)
- `src/resources/extensions/gsd/db/milestone-leases.ts:220` — `releaseMilestoneLease(workerId: string, milestoneId: string, fencingToken: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; const db = _getAdapter()!; return transaction(() => { const result = db.prepare( `UPDATE milestone_leases SET… (missing: @param workerId, @param milestoneId, @param fencingToken, @returns)
- `src/resources/extensions/gsd/db/milestone-leases.ts:261` — `getMilestoneLease(milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const db = _getAdapter()!; const row = db.prepare( `SELECT milestone_id, worker_id, fencing_token, acquired_at… (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/db/milestone-leases.ts:272` — `milestoneLeaseTtlSeconds()` — exported: **yes** — Implements logic starting with: { return LEASE_TTL_SECONDS; } (missing: @returns)

### `src/resources/extensions/gsd/db/runtime-kv.ts`
- `src/resources/extensions/gsd/db/runtime-kv.ts:37` — `setRuntimeKv(scope: RuntimeKvScope, scopeId: string, key: string, value: unknown)` — exported: **yes** — Serializes a value to JSON. (missing: @param scope, @param scopeId, @param key, @param value)
- `src/resources/extensions/gsd/db/runtime-kv.ts:76` — `getRuntimeKv(scope: RuntimeKvScope, scopeId: string, key: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param scope, @param scopeId, @param key, @returns)
- `src/resources/extensions/gsd/db/runtime-kv.ts:99` — `deleteRuntimeKv(scope: RuntimeKvScope, scopeId: string, key: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const db = _getAdapter()!; db.prepare( `DELETE FROM runtime_kv WHERE scope = :scope AND scope_id = :scope_id AND ke… (missing: @param scope, @param scopeId, @param key)
- `src/resources/extensions/gsd/db/runtime-kv.ts:115` — `listRuntimeKv(scope: RuntimeKvScope, scopeId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const db = _getAdapter()!; return db.prepare( `SELECT scope, scope_id, key, value_json, updated_at FROM runtime_… (missing: @param scope, @param scopeId, @returns)

### `src/resources/extensions/gsd/db/unit-dispatches.ts`
- `src/resources/extensions/gsd/db/unit-dispatches.ts:171` — `recordDispatchClaim(input: RecordClaimInput)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param input, @returns)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:273` — `markRunning(dispatchId: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const db = _getAdapter()!; db.prepare( `UPDATE unit_dispatches SET status = 'running' WHERE id = :id AND status = '… (missing: @param dispatchId)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:288` — `markCompleted(dispatchId: number, opts?: CompleteOpts)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const now = new Date().toISOString(); const db = _getAdapter()!; let changes = 0; transaction(() => { const result… (missing: @param dispatchId, @param opts)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:331` — `markFailed(dispatchId: number, opts: FailureOpts)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const now = new Date(); const nowIso = now.toISOString(); const nextRunIso = opts.retryAfterMs ? new Date(now.getTi… (missing: @param dispatchId, @param opts)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:377` — `markStuck(dispatchId: number, reason: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const now = new Date().toISOString(); const db = _getAdapter()!; const result = transaction(() => { return db.prepa… (missing: @param dispatchId, @param reason)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:405` — `markPaused(dispatchId: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const now = new Date().toISOString(); const db = _getAdapter()!; db.prepare( `UPDATE unit_dispatches SET status = '… (missing: @param dispatchId)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:417` — `markCanceled(dispatchId: number, reason: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const now = new Date().toISOString(); const db = _getAdapter()!; db.prepare( `UPDATE unit_dispatches SET status = '… (missing: @param dispatchId, @param reason)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:432` — `markLatestActiveForWorkerCanceled(workerId: string, reason: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; const now = new Date().toISOString(); const db = _getAdapter()!; const result = transaction(() => { return db… (missing: @param workerId, @param reason, @returns)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:474` — `getRecentForUnit(unitId: string, limit = 10)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const db = _getAdapter()!; return db.prepare( `SELECT * FROM unit_dispatches WHERE unit_id = :unit_id ORDER BY i… (missing: @param unitId, @param limit, @returns)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:486` — `getLatestForUnit(unitId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const db = _getAdapter()!; const row = db.prepare( `SELECT * FROM unit_dispatches WHERE unit_id = :unit_id ORD… (missing: @param unitId, @returns)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:504` — `getRecentUnitKeysForWorker(workerId: string, limit = 20)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param workerId, @param limit, @returns)
- `src/resources/extensions/gsd/db/unit-dispatches.ts:544` — `getDispatchesByStatus(milestoneId: string, status: DispatchStatus)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return []; const db = _getAdapter()!; return db.prepare( `SELECT * FROM unit_dispatches WHERE milestone_id = :mid AND statu… (missing: @param milestoneId, @param status, @returns)

### `src/resources/extensions/gsd/debug-logger.ts`
- `src/resources/extensions/gsd/debug-logger.ts:39` — `enableDebug(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath)
- `src/resources/extensions/gsd/debug-logger.ts:66` — `disableDebug()` — exported: **yes** — Implements logic starting with: { const path = _logPath; _enabled = false; _logPath = null; _startTime = 0; return path; } (missing: @returns)
- `src/resources/extensions/gsd/debug-logger.ts:75` — `isDebugEnabled()` — exported: **yes** — Implements logic starting with: { return _enabled; } (missing: @returns)
- `src/resources/extensions/gsd/debug-logger.ts:80` — `getDebugLogPath()` — exported: **yes** — Implements logic starting with: { return _logPath; } (missing: @returns)
- `src/resources/extensions/gsd/debug-logger.ts:89` — `debugLog(event: string, data?: Record<string, unknown>)` — exported: **yes** — Serializes a value to JSON. (missing: @param event, @param data)
- `src/resources/extensions/gsd/debug-logger.ts:116` — `debugTime(event: string)` — exported: **yes** — Implements logic starting with: { if (!_enabled) return _noop; const start = performance.now(); return (data?: Record<string, unknown>) => { const elapsed_ms = Math.round((perform… (missing: @param event, @returns)
- `src/resources/extensions/gsd/debug-logger.ts:129` — `debugCount(counter: keyof typeof _counters, value = 1)` — exported: **yes** — Implements logic starting with: { if (!_enabled) return; _counters[counter] += value; } (missing: @param counter, @param value)
- `src/resources/extensions/gsd/debug-logger.ts:135` — `debugPeak(counter: keyof typeof _counters, value: number)` — exported: **yes** — Implements logic starting with: { if (!_enabled) return; if (value > _counters[counter]) { _counters[counter] = value; } } (missing: @param counter, @param value)
- `src/resources/extensions/gsd/debug-logger.ts:146` — `writeDebugSummary()` — exported: **yes** — Implements logic starting with: { if (!_enabled || !_logPath) return null; const totalElapsed_ms = Date.now() - _startTime; const avgDeriveState_ms = _counters.deriveStateCalls >… (missing: @returns)

### `src/resources/extensions/gsd/deep-project-setup-policy.ts`
- `src/resources/extensions/gsd/deep-project-setup-policy.ts:115` — `downstreamSetupArtifactsValid(root: string, basePath: string)` — exported: **no** — Implements logic starting with: { const projectPath = join(root, "PROJECT.md"); if (!existsSync(projectPath) || !validateArtifact(projectPath, "project").ok) return false; const r… (missing: @param root, @param basePath, @returns)

### `src/resources/extensions/gsd/definition-io.ts`
- `src/resources/extensions/gsd/definition-io.ts:14` — `readFrozenDefinition(runDir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param runDir, @returns)

### `src/resources/extensions/gsd/definition-loader.ts`
- `src/resources/extensions/gsd/definition-loader.ts:102` — `validateDefinition(parsed: unknown)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param parsed, @returns)
- `src/resources/extensions/gsd/definition-loader.ts:341` — `loadDefinitionFromFile(filePath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param filePath, @returns)
- `src/resources/extensions/gsd/definition-loader.ts:407` — `substitutePromptString(prompt: string, merged: Record<string, string>)` — exported: **yes** — Implements logic starting with: { return prompt.replace(PARAM_PATTERN, (match, key: string) => { const value = merged[key]; return value !== undefined ? value : match; }); } (missing: @param prompt, @param merged, @returns)
- `src/resources/extensions/gsd/definition-loader.ts:426` — `substituteParams(definition: WorkflowDefinition, overrides?: Record<string, string>)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param definition, @param overrides, @returns)

### `src/resources/extensions/gsd/delegation-policy.ts`
- `src/resources/extensions/gsd/delegation-policy.ts:192` — `annotateBackgroundable(action: T)` — exported: **yes** — Implements logic starting with: { if (action.action !== "dispatch") return action; const verdict = getVerdictByUnitType(action.unitType); action.backgroundable = verdict?.verdict… (missing: @param action, @returns)

### `src/resources/extensions/gsd/detection.ts`
- `src/resources/extensions/gsd/detection.ts:312` — `detectProjectState(basePath: string)` — exported: **yes** — Implements logic starting with: { const v1 = detectV1Planning(basePath); const v2 = detectV2Gsd(basePath); const projectSignals = detectProjectSignals(basePath); const globalSetup… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/detection.ts:346` — `detectV1Planning(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/detection.ts:413` — `detectProjectSignals(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/detection.ts:611` — `classifyProject(basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/detection.ts:686` — `readBounded(filePath: string, maxBytes: number)` — exported: **no** — Implements logic starting with: { const buf = Buffer.alloc(maxBytes); const fd = openSync(filePath, "r"); try { const bytesRead = readSync(fd, buf, 0, maxBytes, 0); return buf.toS… (missing: @param filePath, @param maxBytes, @returns)
- `src/resources/extensions/gsd/detection.ts:708` — `detectXcodePlatforms(basePath: string)` — exported: **no** — Executes a subprocess command. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/detection.ts:778` — `detectVerificationCommands(basePath: string, detectedFiles: string[], packageManager?: string)` — exported: **no** — Implements logic starting with: { const commands: string[] = []; const pm = packageManager ?? "npm"; const run = pm === "npm" ? "npm run" : pm === "yarn" ? "yarn" : pm === "bun" ?… (missing: @param basePath, @param detectedFiles, @param packageManager, @returns)
- `src/resources/extensions/gsd/detection.ts:849` — `hasGlobalSetup()` — exported: **yes** — Implements logic starting with: { return ( existsSync(join(gsdHome(), "PREFERENCES.md")) || existsSync(join(gsdHome(), "preferences.md")) ); } (missing: @returns)
- `src/resources/extensions/gsd/detection.ts:860` — `isFirstEverLaunch()` — exported: **yes** — Implements logic starting with: { if (!existsSync(gsdHome())) return true; // If we have preferences, not first launch if ( existsSync(join(gsdHome(), "PREFERENCES.md")) || exists… (missing: @returns)
- `src/resources/extensions/gsd/detection.ts:1265` — `hasProjectFileInAncestor(startDir: string, existsFn: (p: string) => boolean = existsSync)` — exported: **yes** — Implements logic starting with: { let checkDir = dirname(startDir); const { root } = parsePath(checkDir); while (checkDir !== root) { if (PROJECT_FILES.some((f) => existsFn(join(c… (missing: @param startDir, @param existsFn, @returns)
- `src/resources/extensions/gsd/detection.ts:1296` — `hasGsdBootstrapArtifacts(gsdPath: string, existsFn: (p: string) => boolean = existsSync)` — exported: **yes** — Implements logic starting with: { return ( existsFn(gsdPath) && (existsFn(join(gsdPath, "PREFERENCES.md")) || existsFn(join(gsdPath, "preferences.md")) || existsFn(join(gsdPath, "… (missing: @param gsdPath, @param existsFn, @returns)

### `src/resources/extensions/gsd/dev-workflow-engine.ts`
- `src/resources/extensions/gsd/dev-workflow-engine.ts:32` — `bridgeDispatchAction(da: DispatchAction)` — exported: **yes** — Implements logic starting with: { switch (da.action) { case "dispatch": return { action: "dispatch", step: { unitType: da.unitType, unitId: da.unitId, prompt: da.prompt, }, }; cas… (missing: @param da, @returns)

### `src/resources/extensions/gsd/diff-context.ts`
- `src/resources/extensions/gsd/diff-context.ts:37` — `gitSync(args: string[], cwd: string)` — exported: **no** — Implements logic starting with: { return execFileSync("git", args, { ...EXEC_OPTS, cwd }).trim(); } (missing: @param args, @param cwd, @returns)
- `src/resources/extensions/gsd/diff-context.ts:42` — `gitAsync(args: string[], cwd: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param args, @param cwd, @returns)
- `src/resources/extensions/gsd/diff-context.ts:69` — `getRecentlyChangedFiles(cwd: string, options?: RecentFilesOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param cwd, @param options, @returns)
- `src/resources/extensions/gsd/diff-context.ts:119` — `getChangedFilesWithContext(cwd: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param cwd, @returns)
- `src/resources/extensions/gsd/diff-context.ts:193` — `rankFilesByRelevance(files: string[], changedFiles: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param files, @param changedFiles, @returns)

### `src/resources/extensions/gsd/doctor-environment.ts`
- `src/resources/extensions/gsd/doctor-environment.ts:52` — `resolveWorktreeProjectRoot(basePath: string)` — exported: **no** — Implements logic starting with: { const envRoot = process.env.GSD_WORKTREE; if (envRoot) return envRoot; const normalised = basePath.replace(/\\/g, "/"); const idx = normalised.in… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:87` — `checkNodeVersion(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:132` — `checkDependenciesInstalled(basePath: string)` — exported: **no** — Implements logic starting with: { const pkgPath = join(basePath, "package.json"); if (!existsSync(pkgPath)) return null; const nodeModules = join(basePath, "node_modules"); if (!e… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:205` — `checkEnvFiles(basePath: string)` — exported: **no** — Implements logic starting with: { const examplePath = join(basePath, ".env.example"); if (!existsSync(examplePath)) return null; const envPath = join(basePath, ".env"); const envL… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:228` — `checkPortConflicts(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:292` — `checkDiskSpace(basePath: string)` — exported: **no** — Implements logic starting with: { // Only run on macOS/Linux if (process.platform === "win32") return null; const dfOutput = tryExec(`df -k "${basePath}" | tail -1`, basePath); if… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:335` — `checkDocker(basePath: string)` — exported: **no** — Implements logic starting with: { const hasDockerfile = existsSync(join(basePath, "Dockerfile")) || existsSync(join(basePath, "docker-compose.yml")) || existsSync(join(basePath, "… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:368` — `checkProjectTools(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:446` — `checkGitRemote(basePath: string)` — exported: **no** — Implements logic starting with: { // Only check if it's a git repo with a remote const remote = tryExec("git remote get-url origin", basePath); if (!remote) return null; // Quick… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:469` — `checkBuildHealth(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:497` — `checkTestHealth(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:528` — `runEnvironmentChecks(basePath: string)` — exported: **yes** — Implements logic starting with: { const results: EnvironmentCheckResult[] = []; const nodeCheck = checkNodeVersion(basePath); if (nodeCheck) results.push(nodeCheck); const depsChe… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:560` — `runFullEnvironmentChecks(basePath: string)` — exported: **yes** — Implements logic starting with: { const results = runEnvironmentChecks(basePath); const remoteCheck = checkGitRemote(basePath); if (remoteCheck) results.push(remoteCheck); return… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:573` — `runSlowEnvironmentChecks(basePath: string, options?: { includeBuild?: boolean; includeTests?: boolean })` — exported: **yes** — Implements logic starting with: { const results: EnvironmentCheckResult[] = []; if (options?.includeBuild) { const buildCheck = checkBuildHealth(basePath); if (buildCheck) results… (missing: @param basePath, @param options, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:592` — `environmentResultsToDoctorIssues(results: EnvironmentCheckResult[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param results, @returns)
- `src/resources/extensions/gsd/doctor-environment.ts:609` — `checkEnvironmentHealth(basePath: string, issues: DoctorIssue[], options?: { includeRemote?: boolean; includeBuild?: boolean; includeTests?: boolean })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param issues, @param options)
- `src/resources/extensions/gsd/doctor-environment.ts:628` — `formatEnvironmentReport(results: EnvironmentCheckResult[])` — exported: **yes** — Implements logic starting with: { if (results.length === 0) return "No environment checks applicable."; const lines: string[] = []; lines.push("Environment Health:"); for (const r… (missing: @param results, @returns)

### `src/resources/extensions/gsd/doctor-format.ts`
- `src/resources/extensions/gsd/doctor-format.ts:85` — `formatDoctorReportJson(report: DoctorReport)` — exported: **yes** — Serializes a value to JSON. (missing: @param report, @returns)

### `src/resources/extensions/gsd/doctor-git-checks.ts`
- `src/resources/extensions/gsd/doctor-git-checks.ts:25` — `isDoctorArtifactOnly(dirPath: string)` — exported: **no** — Implements logic starting with: { try { const entries = readdirSync(dirPath); // Empty dir — not a doctor artifact, still orphaned if (entries.length === 0) return false; // Only… (missing: @param dirPath, @returns)

### `src/resources/extensions/gsd/doctor-global-checks.ts`
- `src/resources/extensions/gsd/doctor-global-checks.ts:17` — `checkGlobalHealth(issues: DoctorIssue[], fixesApplied: string[], shouldFix: (code: DoctorIssueCode) => boolean)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param issues, @param fixesApplied, @param shouldFix)

### `src/resources/extensions/gsd/doctor-proactive.ts`
- `src/resources/extensions/gsd/doctor-proactive.ts:73` — `setLevelChangeCallback(cb: ((from: string, to: string, summary: string) => void) | null)` — exported: **yes** — Implements logic starting with: { onLevelChange = cb; previousProgressLevel = "green"; } (missing: @param cb)
- `src/resources/extensions/gsd/doctor-proactive.ts:82` — `recordHealthSnapshot(errors: number, warnings: number, fixesApplied: number, issues?: HealthIssueDetail[], fixes?: string[], scope?: string)` — exported: **yes** — Implements logic starting with: { healthUnitIndex++; healthHistory.push({ timestamp: Date.now(), errors, warnings, fixesApplied, unitIndex: healthUnitIndex, issues: issues ?? [],… (missing: @param errors, @param warnings, @param fixesApplied, @param issues, @param fixes, @param scope)
- `src/resources/extensions/gsd/doctor-proactive.ts:131` — `getHealthTrend()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:151` — `getConsecutiveErrorUnits()` — exported: **yes** — Implements logic starting with: { return consecutiveErrorUnits; } (missing: @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:158` — `getHealthHistory()` — exported: **yes** — Implements logic starting with: { return healthHistory; } (missing: @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:166` — `getLatestHealthIssues()` — exported: **yes** — Implements logic starting with: { for (let i = healthHistory.length - 1; i >= 0; i--) { if (healthHistory[i]!.issues.length > 0) return healthHistory[i]!.issues; } return []; } (missing: @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:176` — `getLatestHealthFixes()` — exported: **yes** — Implements logic starting with: { for (let i = healthHistory.length - 1; i >= 0; i--) { if (healthHistory[i]!.fixes.length > 0) return healthHistory[i]!.fixes; } return []; } (missing: @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:215` — `preDispatchHealthGate(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:380` — `checkHealEscalation(errors: number, unresolvedIssues: Array<{ code: string; message: string; unitId: string }>)` — exported: **yes** — Implements logic starting with: { if (escalationTriggered) { return { shouldEscalate: false, reason: "already escalated this session", issues: [] }; } if (consecutiveErrorUnits <… (missing: @param errors, @param unresolvedIssues, @returns)
- `src/resources/extensions/gsd/doctor-proactive.ts:424` — `formatHealthSummary()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `src/resources/extensions/gsd/doctor-providers.ts`
- `src/resources/extensions/gsd/doctor-providers.ts:71` — `modelToProviderId(model: string)` — exported: **no** — Implements logic starting with: { if (!model) return null; // Explicit provider prefix (e.g. "openrouter/deepseek-r1") if (model.includes("/")) { const rawPrefix = model.split("/"… (missing: @param model, @returns)
- `src/resources/extensions/gsd/doctor-providers.ts:106` — `collectConfiguredModelProviders()` — exported: **no** — Builds an in-memory lookup structure. (missing: @returns)
- `src/resources/extensions/gsd/doctor-providers.ts:170` — `isCliBinaryInPath(providerId: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param providerId, @returns)
- `src/resources/extensions/gsd/doctor-providers.ts:457` — `runProviderChecks()` — exported: **yes** — Implements logic starting with: { const results: ProviderCheckResult[] = []; results.push(...checkLlmProviders()); const remoteCheck = checkRemoteQuestionsProvider(); if (remoteCh… (missing: @returns)
- `src/resources/extensions/gsd/doctor-providers.ts:473` — `formatProviderReport(results: ProviderCheckResult[])` — exported: **yes** — Implements logic starting with: { if (results.length === 0) return "No provider checks run."; const lines: string[] = []; const groups: Record<string, ProviderCheckResult[]> = {};… (missing: @param results, @returns)
- `src/resources/extensions/gsd/doctor-providers.ts:511` — `summariseProviderIssues(results: ProviderCheckResult[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param results, @returns)

### `src/resources/extensions/gsd/doctor-runtime-checks.ts`
- `src/resources/extensions/gsd/doctor-runtime-checks.ts:711` — `buildStateMarkdownForCheck(state: Awaited<ReturnType<typeof deriveState>>)` — exported: **no** — Implements logic starting with: { const lines: string[] = []; lines.push("# GSD State", ""); const activeMilestone = state.activeMilestone ? `${state.activeMilestone.id}: ${state.… (missing: @param state, @returns)

### `src/resources/extensions/gsd/doctor.ts`
- `src/resources/extensions/gsd/doctor.ts:46` — `validateTitle(title: string)` — exported: **yes** — Implements logic starting with: { if (TITLE_DELIMITER_RE.test(title)) { const found: string[] = []; if (/[\u2014\u2013]/.test(title)) found.push("em/en dash (\u2014 or \u2013)");… (missing: @param title, @returns)
- `src/resources/extensions/gsd/doctor.ts:92` — `buildStateMarkdown(state: Awaited<ReturnType<typeof deriveState>>)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push("# GSD State", ""); const activeMilestone = state.activeMilestone ? `${state.activeMilestone.id}: ${state.… (missing: @param state, @returns)
- `src/resources/extensions/gsd/doctor.ts:149` — `rebuildState(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath)
- `src/resources/extensions/gsd/doctor.ts:323` — `readDoctorHistory(basePath: string, lastN = 50)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param lastN, @returns)

### `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts`
- `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts:70` — `mapAutoLoopPhase(raw: string)` — exported: **yes** — Implements logic starting with: { return AUTO_LOOP_PHASE_MAP[raw] ?? null; } (missing: @param raw, @returns)
- `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts:114` — `updateSnapshot(state: GSDState | null)` — exported: **yes** — Implements logic starting with: { _snapshot = { phase: resolvePhase(state), activeUnit: resolveActiveUnit(state), }; } (missing: @param state)
- `src/resources/extensions/gsd/ecosystem/gsd-extension-api.ts:144` — `createGSDExtensionAPI(pi: ExtensionAPI, sharedHandlers: GSDEcosystemBeforeAgentStartHandler[])` — exported: **yes** — Executes a subprocess command. (missing: @param pi, @param sharedHandlers, @returns)

### `src/resources/extensions/gsd/ecosystem/loader.ts`
- `src/resources/extensions/gsd/ecosystem/loader.ts:50` — `loadEcosystemExtensions(pi: ExtensionAPI, sharedHandlers: GSDEcosystemBeforeAgentStartHandler[], cwd: string = process.cwd())` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param pi, @param sharedHandlers, @param cwd)

### `src/resources/extensions/gsd/engine-resolver.ts`
- `src/resources/extensions/gsd/engine-resolver.ts:32` — `resolveEngine(session: { activeEngineId: string | null; activeRunDir?: string | null })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param session, @returns)

### `src/resources/extensions/gsd/env-utils.ts`
- `src/resources/extensions/gsd/env-utils.ts:14` — `checkExistingEnvKeys(keys: string[], envFilePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param keys, @param envFilePath, @returns)

### `src/resources/extensions/gsd/error-classifier.ts`
- `src/resources/extensions/gsd/error-classifier.ts:94` — `classifyError(errorMsg: string, retryAfterMs?: number)` — exported: **yes** — Implements logic starting with: { const isPermanent = PERMANENT_RE.test(errorMsg); const isRateLimit = RATE_LIMIT_RE.test(errorMsg) || AFFORDABILITY_RE.test(errorMsg); const isUns… (missing: @param errorMsg, @param retryAfterMs, @returns)
- `src/resources/extensions/gsd/error-classifier.ts:156` — `isTransient(cls: ErrorClass)` — exported: **yes** — Implements logic starting with: { switch (cls.kind) { case "network": case "rate-limit": case "server": case "stream": case "connection": return true; default: return false; } } (missing: @param cls, @returns)
- `src/resources/extensions/gsd/error-classifier.ts:176` — `isTransientNetworkError(errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!errorMsg) return false; const cls = classifyError(errorMsg); return cls.kind === "network"; } (missing: @param errorMsg, @returns)

### `src/resources/extensions/gsd/error-utils.ts`
- `src/resources/extensions/gsd/error-utils.ts:4` — `getErrorMessage(err: unknown)` — exported: **yes** — Implements logic starting with: { return err instanceof Error ? err.message : String(err); } (missing: @param err, @returns)

### `src/resources/extensions/gsd/escalation.ts`
- `src/resources/extensions/gsd/escalation.ts:35` — `escalationArtifactPath(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { const sDir = resolveSlicePath(basePath, milestoneId, sliceId); if (!sDir) return null; return join(sDir, "tasks", `${taskId}-ESCALATION.json`); } (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId, @returns)
- `src/resources/extensions/gsd/escalation.ts:46` — `buildEscalationArtifact(params: { taskId: string; sliceId: string; milestoneId: string; question: string; options: EscalationOption[]; recommendation: string; recommendationRationale: string; continueWithDefault: boolean; })` — exported: **yes** — Validates inputs and throws on failure. (missing: @param params, @returns)
- `src/resources/extensions/gsd/escalation.ts:84` — `writeEscalationArtifact(basePath: string, artifact: EscalationArtifact)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param basePath, @param artifact, @returns)
- `src/resources/extensions/gsd/escalation.ts:120` — `readEscalationArtifact(path: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param path, @returns)
- `src/resources/extensions/gsd/escalation.ts:166` — `detectPendingEscalation(tasks: TaskRow[], basePath: string)` — exported: **yes** — Implements logic starting with: { for (const t of tasks) { if (t.escalation_pending !== 1) continue; if (!t.escalation_artifact_path) continue; const art = readEscalationArtifact(… (missing: @param tasks, @param basePath, @returns)
- `src/resources/extensions/gsd/escalation.ts:196` — `resolveEscalation(basePath: string, milestoneId: string, sliceId: string, taskId: string, choice: string, rationale: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId, @param choice, @param rationale, @returns)
- `src/resources/extensions/gsd/escalation.ts:278` — `claimOverrideForInjection(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const unapplied = findUnappliedEscalationOverride(milestoneId, sliceId); if (!unapplied) return null; // Validate artifact BEFORE claiming so a m… (missing: @param basePath, @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/escalation.ts:339` — `formatEscalationForDisplay(art: EscalationArtifact)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param art, @returns)
- `src/resources/extensions/gsd/escalation.ts:362` — `listActionableEscalations(milestoneId: string)` — exported: **yes** — Implements logic starting with: { return listEscalationArtifacts(milestoneId, /* includeResolved */ false); } (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/escalation.ts:367` — `listAllEscalations(milestoneId: string)` — exported: **yes** — Implements logic starting with: { return listEscalationArtifacts(milestoneId, /* includeResolved */ true); } (missing: @param milestoneId, @returns)

### `src/resources/extensions/gsd/exec-sandbox.ts`
- `src/resources/extensions/gsd/exec-sandbox.ts:136` — `runExecSandbox(request: ExecSandboxRequest, opts: ExecSandboxOptions)` — exported: **yes** — Performs filesystem I/O. (missing: @param request, @param opts, @returns)

### `src/resources/extensions/gsd/export.ts`
- `src/resources/extensions/gsd/export.ts:22` — `openInBrowser(filePath: string)` — exported: **yes** — Implements logic starting with: { if (process.platform === "win32") { // PowerShell's Start-Process handles paths with '&' and spaces safely. execFile("powershell", ["-c", `Start-… (missing: @param filePath)
- `src/resources/extensions/gsd/export.ts:37` — `writeExportFile(basePath: string, format: "markdown" | "json", visualizerData?: { totals: any; byPhase: any[]; bySlice: any[]; byModel: any[]; units: any[]; criticalPath?: any; remainingSliceCount?: number })` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param format, @param visualizerData, @returns)
- `src/resources/extensions/gsd/export.ts:115` — `handleExport(args: string, ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param args, @param ctx, @param basePath)

### `src/resources/extensions/gsd/files.ts`
- `src/resources/extensions/gsd/files.ts:35` — `cacheKey(content: string)` — exported: **no** — Implements logic starting with: { const len = content.length; const head = content.slice(0, 100); const midStart = Math.max(0, Math.floor(len / 2) - 50); const mid = len > 200 ? c… (missing: @param content, @returns)
- `src/resources/extensions/gsd/files.ts:62` — `registerCacheClearCallback(cb: () => void)` — exported: **yes** — Implements logic starting with: { _cacheClearCallbacks.push(cb); } (missing: @param cb)
- `src/resources/extensions/gsd/files.ts:82` — `formatShortcut(combo: string)` — exported: **yes** — Implements logic starting with: { if (!IS_MAC) return combo; return combo .replace(/Ctrl\+Alt\+/i, "⌃⌥") .replace(/Ctrl\+/i, "⌃") .replace(/Alt\+/i, "⌥") .replace(/Shift\+/i, "⇧")… (missing: @param combo, @returns)
- `src/resources/extensions/gsd/files.ts:95` — `extractSection(body: string, heading: string, level: number = 2)` — exported: **yes** — Executes a subprocess command. (missing: @param body, @param heading, @param level, @returns)
- `src/resources/extensions/gsd/files.ts:115` — `extractAllSections(body: string, level: number = 2)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param body, @param level, @returns)
- `src/resources/extensions/gsd/files.ts:141` — `normalizePlannedFileReference(value: string)` — exported: **yes** — Executes a subprocess command. (missing: @param value, @returns)
- `src/resources/extensions/gsd/files.ts:155` — `parseBullets(text: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param text, @returns)
- `src/resources/extensions/gsd/files.ts:162` — `extractBoldField(text: string, key: string)` — exported: **yes** — Executes a subprocess command. (missing: @param text, @param key, @returns)
- `src/resources/extensions/gsd/files.ts:484` — `loadFile(path: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param path, @returns)
- `src/resources/extensions/gsd/files.ts:498` — `saveFile(path: string, content: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param path, @param content)
- `src/resources/extensions/gsd/files.ts:542` — `parseTaskPlanMustHaves(content: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/files.ts:586` — `countMustHavesMentionedInSummary(mustHaves: Array<{ text: string; checked: boolean }>, summaryContent: string)` — exported: **yes** — Executes a subprocess command. (missing: @param mustHaves, @param summaryContent, @returns)
- `src/resources/extensions/gsd/files.ts:632` — `parseTaskPlanIO(content: string)` — exported: **yes** — Executes a subprocess command. (missing: @param content, @returns)
- `src/resources/extensions/gsd/files.ts:685` — `extractUatType(content: string)` — exported: **yes** — Implements logic starting with: { const sectionText = extractSection(content, 'UAT Type'); if (!sectionText) return undefined; const bullets = parseBullets(sectionText); const mod… (missing: @param content, @returns)
- `src/resources/extensions/gsd/files.ts:710` — `parseContextDependsOn(content: string | null)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/files.ts:726` — `inlinePriorMilestoneSummary(mid: string, base: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mid, @param base, @returns)
- `src/resources/extensions/gsd/files.ts:748` — `getManifestStatus(base: string, milestoneId: string, projectRoot?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param base, @param milestoneId, @param projectRoot, @returns)

### `src/resources/extensions/gsd/forensics.ts`
- `src/resources/extensions/gsd/forensics.ts:436` — `resolveActivityDirs(basePath: string, activeMilestone?: string | null)` — exported: **no** — Implements logic starting with: { const dirs: string[] = []; // Check for active auto-worktree activity logs if (activeMilestone) { const wtPath = getAutoWorktreePath(basePath, ac… (missing: @param basePath, @param activeMilestone, @returns)
- `src/resources/extensions/gsd/forensics.ts:477` — `scanJournalForForensics(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/forensics.ts:620` — `splitCompletedKey(key: string)` — exported: **yes** — Implements logic starting with: { if (key.startsWith("hook/")) { const secondSlash = key.indexOf("/", 5); // skip past "hook/" if (secondSlash === -1) return null; // malformed —… (missing: @param key, @returns)
- `src/resources/extensions/gsd/forensics.ts:690` — `detectStuckLoops(units: UnitMetrics[], anomalies: ForensicAnomaly[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param units, @param anomalies)
- `src/resources/extensions/gsd/forensics.ts:807` — `detectWorktreeOrphans(summary: WorktreeTelemetrySummary, anomalies: ForensicAnomaly[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param summary, @param anomalies)
- `src/resources/extensions/gsd/forensics.ts:1112` — `writeForensicsMarker(basePath: string, reportPath: string, promptContent: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param reportPath, @param promptContent)
- `src/resources/extensions/gsd/forensics.ts:1126` — `readForensicsMarker(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/gate-registry.ts`
- `src/resources/extensions/gsd/gate-registry.ts:176` — `getGatesForTurn(turn: OwnerTurn)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param turn, @returns)
- `src/resources/extensions/gsd/gate-registry.ts:181` — `getGateIdsForTurn(turn: OwnerTurn)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param turn, @returns)
- `src/resources/extensions/gsd/gate-registry.ts:186` — `getGateDefinition(id: string)` — exported: **yes** — Implements logic starting with: { return (GATE_REGISTRY as Record<string, GateDefinition>)[id]; } (missing: @param id, @returns)
- `src/resources/extensions/gsd/gate-registry.ts:191` — `getOwnerTurn(id: GateId)` — exported: **yes** — Implements logic starting with: { const def = GATE_REGISTRY[id]; if (!def) { throw new GSDError(GSD_PARSE_ERROR, `gate-registry: unknown gate id "${id}"`); } return def.ownerTurn;… (missing: @param id, @returns)
- `src/resources/extensions/gsd/gate-registry.ts:211` — `assertGateCoverage(pending: ReadonlyArray<Pick<GateRow, "gate_id">>, turn: OwnerTurn, options: { requireAll?: boolean } = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: @param pending, @param turn, @param options)

### `src/resources/extensions/gsd/git-self-heal.ts`
- `src/resources/extensions/gsd/git-self-heal.ts:50` — `abortAndReset(cwd: string)` — exported: **yes** — Implements logic starting with: { const gitDir = join(cwd, ".git"); const cleaned: string[] = []; // Abort in-progress merge if (existsSync(join(gitDir, "MERGE_HEAD"))) { try { na… (missing: @param cwd)
- `src/resources/extensions/gsd/git-self-heal.ts:148` — `formatGitError(error: string | Error)` — exported: **yes** — Implements logic starting with: { const errorStr = error instanceof Error ? error.message : error; for (const { pattern, message } of ERROR_PATTERNS) { if (pattern.test(errorStr))… (missing: @param error, @returns)

### `src/resources/extensions/gsd/git-service.ts`
- `src/resources/extensions/gsd/git-service.ts:158` — `buildTaskCommitMessage(ctx: TaskCommitContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/git-service.ts:278` — `class MergeConflictError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/gsd/git-service.ts:344` — `milestoneMetaPath(basePath: string, milestoneId: string)` — exported: **no** — Implements logic starting with: { return join(gsdRoot(basePath), "milestones", milestoneId, `${milestoneId}-META.json`); } (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/git-service.ts:352` — `readIntegrationBranch(basePath: string, milestoneId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/git-service.ts:436` — `resolveMilestoneIntegrationBranch(basePath: string, milestoneId: string, prefs: GitPreferences = {})` — exported: **yes** — Implements logic starting with: { const recordedBranch = readIntegrationBranch(basePath, milestoneId); if (!recordedBranch) { return { recordedBranch: null, effectiveBranch: null,… (missing: @param basePath, @param milestoneId, @param prefs, @returns)
- `src/resources/extensions/gsd/git-service.ts:519` — `tokenizePreMergeCommand(input: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param input, @returns)
- `src/resources/extensions/gsd/git-service.ts:617` — `filterGitSvnNoise(message: string)` — exported: **no** — Implements logic starting with: { return message .replace(/Duplicate specification "[^"]*" for option "[^"]*"\n?/g, "") .replace(/Unable to determine upstream SVN information from… (missing: @param message, @returns)
- `src/resources/extensions/gsd/git-service.ts:630` — `runGit(basePath: string, args: string[], options: { allowFailure?: boolean; input?: string } = {})` — exported: **yes** — Implements logic starting with: { try { return execFileSync("git", args, { cwd: basePath, stdio: [options.input != null ? "pipe" : "ignore", "pipe", "pipe"], encoding: "utf-8", en… (missing: @param basePath, @param args, @param options, @returns)
- `src/resources/extensions/gsd/git-service.ts:680` — `GitServiceImpl.setMilestoneId(milestoneId: string | null)` — exported: **yes** — Implements logic starting with: { this._milestoneId = milestoneId; } (missing: @param milestoneId)
- `src/resources/extensions/gsd/git-service.ts:819` — `GitServiceImpl.commit(opts: CommitOptions)` — exported: **yes** — Implements logic starting with: { this.smartStage(); // Check if anything was actually staged if (!nativeHasStagedChanges(this.basePath) && !opts.allowEmpty) return null; nativeCo… (missing: @param opts, @returns)
- `src/resources/extensions/gsd/git-service.ts:840` — `GitServiceImpl.autoCommit(unitType: string, unitId: string, extraExclusions: readonly string[] = [], taskContext?: TaskCommitContext)` — exported: **yes** — Implements logic starting with: { // Quick check: is there anything dirty at all? // Native path uses libgit2 (single syscall), fallback spawns git. if (!nativeHasChanges(this.bas… (missing: @param unitType, @param unitId, @param taskContext, @returns)
- `src/resources/extensions/gsd/git-service.ts:970` — `GitServiceImpl.getMainBranch()` — exported: **yes** — Implements logic starting with: { // Explicit preference takes priority (double-check validity as defense-in-depth) if (this.prefs.main_branch && VALID_BRANCH_NAME.test(this.prefs… (missing: @returns)
- `src/resources/extensions/gsd/git-service.ts:1008` — `GitServiceImpl.getCurrentBranch()` — exported: **yes** — Implements logic starting with: { return nativeGetCurrentBranch(this.basePath); } (missing: @returns)
- `src/resources/extensions/gsd/git-service.ts:1018` — `GitServiceImpl.createSnapshot(label: string)` — exported: **yes** — Implements logic starting with: { if (this.prefs.snapshots === false) return; const now = new Date(); const ts = now.getFullYear().toString() + String(now.getMonth() + 1).padStart… (missing: @param label)
- `src/resources/extensions/gsd/git-service.ts:1039` — `GitServiceImpl.runPreMergeCheck()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/git-service.ts:1107` — `createDraftPR(basePath: string, milestoneId: string, title: string, body: string, opts?: { head?: string; base?: string; env?: NodeJS.ProcessEnv })` — exported: **yes** — Implements logic starting with: { try { const args = [ "pr", "create", "--draft", "--title", title, "--body", body, ]; if (opts?.head) args.push("--head", opts.head); if (opts?.ba… (missing: @param basePath, @param milestoneId, @param title, @param body, @param opts, @returns)
- `src/resources/extensions/gsd/git-service.ts:1137` — `createGitService(basePath: string)` — exported: **yes** — Implements logic starting with: { const gitPrefs = loadEffectiveGSDPreferences()?.preferences?.git ?? {}; return new GitServiceImpl(basePath, gitPrefs); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/git-service.ts:1214` — `inferCommitType(title: string, oneLiner?: string)` — exported: **yes** — Implements logic starting with: { const lower = `${title} ${oneLiner || ""}`.toLowerCase(); for (const [keywords, commitType] of COMMIT_TYPE_RULES) { for (const keyword of keyword… (missing: @param title, @param oneLiner, @returns)

### `src/resources/extensions/gsd/gitignore.ts`
- `src/resources/extensions/gsd/gitignore.ts:109` — `isGsdGitignored(basePath: string)` — exported: **yes** — Implements logic starting with: { // Check both `.gsd` and `.gsd/` because `.gsd/` in .gitignore (trailing // slash = directory-only pattern) only matches the directory form. Usin… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/gitignore.ts:141` — `hasGitTrackedGsdFiles(basePath: string)` — exported: **yes** — Implements logic starting with: { const localGsd = join(basePath, ".gsd"); // If .gsd doesn't exist or is already a symlink, no tracked files concern if (!existsSync(localGsd)) re… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/gitignore.ts:184` — `ensureGitignore(basePath: string, options?: { manageGitignore?: boolean })` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param options, @returns)
- `src/resources/extensions/gsd/gitignore.ts:246` — `untrackRuntimeFiles(basePath: string)` — exported: **yes** — Implements logic starting with: { const runtimePaths = GSD_RUNTIME_PATTERNS; for (const pattern of runtimePaths) { // Use -r for directory patterns (trailing slash), strip the sla… (missing: @param basePath)
- `src/resources/extensions/gsd/gitignore.ts:268` — `ensurePreferences(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/graph-context.ts`
- `src/resources/extensions/gsd/graph-context.ts:159` — `inlineGraphSubgraph(projectDir: string, term: string, opts: GraphSubgraphOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param projectDir, @param term, @param opts, @returns)

### `src/resources/extensions/gsd/graph.ts`
- `src/resources/extensions/gsd/graph.ts:157` — `isTerminalStepStatus(status: GraphStep["status"] | undefined)` — exported: **yes** — Implements logic starting with: { return status === "complete" || status === "expanded"; } (missing: @param status, @returns)

### `src/resources/extensions/gsd/gsd-db.ts`
- `src/resources/extensions/gsd/gsd-db.ts:165` — `tryCreateMemoriesFts(db: DbAdapter)` — exported: **yes** — Implements logic starting with: { return tryCreateMemoriesFtsSchema(db, { onUnavailable: (message) => logWarning("db", message), }); } (missing: @param db, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:386` — `_getDbCache()` — exported: **yes** — Implements logic starting with: { return _dbCache.asReadonlyMap(); } (missing: @returns)
- `src/resources/extensions/gsd/gsd-db.ts:518` — `openDatabaseByScope(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { return openDatabaseByWorkspace(scope.workspace); } (missing: @param scope, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:528` — `closeDatabaseByWorkspace(workspace: GsdWorkspace)` — exported: **yes** — Implements logic starting with: { const key = workspace.identityKey; const cached = _dbCache.get(key); if (!cached) return; _dbCache.delete(key); if (currentDb === cached.db) { //… (missing: @param workspace)
- `src/resources/extensions/gsd/gsd-db.ts:559` — `wasDbOpenAttempted()` — exported: **yes** — Implements logic starting with: { return _dbOpenState.snapshot().attempted; } (missing: @returns)
- `src/resources/extensions/gsd/gsd-db.ts:680` — `refreshOpenDatabaseFromDisk()` — exported: **yes** — Implements logic starting with: { if (!currentDb || !currentPath) return false; if (currentPath === ":memory:") return false; const dbPath = currentPath; const identityKey = _curr… (missing: @returns)
- `src/resources/extensions/gsd/gsd-db.ts:734` — `isInTransaction()` — exported: **yes** — Implements logic starting with: { return _transactionRunner.isInTransaction(); } (missing: @returns)
- `src/resources/extensions/gsd/gsd-db.ts:750` — `readTransaction(fn: () => T)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); return _transactionRunner.readTransaction(createTransactionContr… (missing: @param fn, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1144` — `getSketchedSliceIds(milestoneId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1399` — `setTaskEscalationPending(milestoneId: string, sliceId: string, taskId: string, artifactPath: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE tasks SET escalation_pending = 1, esc… (missing: @param milestoneId, @param sliceId, @param taskId, @param artifactPath)
- `src/resources/extensions/gsd/gsd-db.ts:1414` — `setTaskEscalationAwaitingReview(milestoneId: string, sliceId: string, taskId: string, artifactPath: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE tasks SET escalation_awaiting_review… (missing: @param milestoneId, @param sliceId, @param taskId, @param artifactPath)
- `src/resources/extensions/gsd/gsd-db.ts:1429` — `clearTaskEscalationFlags(milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE tasks SET escalation_pending = 0, esc… (missing: @param milestoneId, @param sliceId, @param taskId)
- `src/resources/extensions/gsd/gsd-db.ts:1446` — `claimEscalationOverride(milestoneId: string, sliceId: string, sourceTaskId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return false; const now = new Date().toISOString(); const result = currentDb.prepare( `UPDATE tasks SET escalation_override_appli… (missing: @param milestoneId, @param sliceId, @param sourceTaskId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1464` — `findUnappliedEscalationOverride(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; // Filter BOTH flags: escalation_pending=0 AND escalation_awaiting_review=0 // ensures we only claim overrides the u… (missing: @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1491` — `setTaskBlockerSource(milestoneId: string, sliceId: string, taskId: string, source: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE tasks SET blocker_discovered = 1, blo… (missing: @param milestoneId, @param sliceId, @param taskId, @param source)
- `src/resources/extensions/gsd/gsd-db.ts:1504` — `listEscalationArtifacts(milestoneId: string, includeResolved: boolean = false)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestoneId, @param includeResolved, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1596` — `updateMilestoneStatus(milestoneId: string, status: string, completedAt?: string | null)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `UPDATE milestones SET status = :status, comp… (missing: @param milestoneId, @param status, @param completedAt)
- `src/resources/extensions/gsd/gsd-db.ts:1659` — `getActiveMilestoneIdFromDb()` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( "SELECT id, status FROM milestones WHERE status NOT IN ('complete', 'parked') ORDER B… (missing: @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1669` — `getSliceStatusSummary(milestoneId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1677` — `getActiveTaskIdFromDb(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return null; const row = currentDb.prepare( "SELECT id, status, title FROM tasks WHERE milestone_id = :mid AND slice_id = :sid AN… (missing: @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1687` — `getSliceTaskCounts(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return emptyTaskStatusCounts(); const row = currentDb.prepare( `SELECT COUNT(*) as total, SUM(CASE WHEN status IN ('complete', 'd… (missing: @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:1702` — `syncSliceDependencies(milestoneId: string, sliceId: string, depends: string[])` — exported: **yes** — Implements logic starting with: { if (!currentDb) return; currentDb.prepare( "DELETE FROM slice_dependencies WHERE milestone_id = :mid AND slice_id = :sid", ).run({ ":mid": milest… (missing: @param milestoneId, @param sliceId, @param depends)
- `src/resources/extensions/gsd/gsd-db.ts:1715` — `getDependentSlices(milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) return []; const rows = currentDb.prepare( "SELECT slice_id FROM slice_dependencies WHERE milestone_id = :mid AND depends_on_slic… (missing: @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:2337` — `getPendingGatesForTurn(milestoneId: string, sliceId: string, turn: OwnerTurn, taskId?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestoneId, @param sliceId, @param turn, @param taskId, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:2371` — `getPendingGateCountForTurn(milestoneId: string, sliceId: string, turn: OwnerTurn)` — exported: **yes** — Implements logic starting with: { return getPendingGatesForTurn(milestoneId, sliceId, turn).length; } (missing: @param milestoneId, @param sliceId, @param turn, @returns)
- `src/resources/extensions/gsd/gsd-db.ts:2606` — `deleteDecisionById(id: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare("DELETE FROM decisions WHERE id = :id").run({… (missing: @param id)
- `src/resources/extensions/gsd/gsd-db.ts:2612` — `deleteRequirementById(id: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare("DELETE FROM requirements WHERE id = :id").run… (missing: @param id)
- `src/resources/extensions/gsd/gsd-db.ts:2618` — `deleteArtifactByPath(path: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare("DELETE FROM artifacts WHERE path = :path").ru… (missing: @param path)
- `src/resources/extensions/gsd/gsd-db.ts:2649` — `insertOrIgnoreSlice(args: { milestoneId: string; sliceId: string; title: string; createdAt: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR IGNORE INTO slices (milestone_id,… (missing: @param args)
- `src/resources/extensions/gsd/gsd-db.ts:2671` — `insertOrIgnoreTask(args: { milestoneId: string; sliceId: string; taskId: string; title: string; createdAt: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR IGNORE INTO tasks (milestone_id, s… (missing: @param args)
- `src/resources/extensions/gsd/gsd-db.ts:2696` — `setSliceReplanTriggeredAt(milestoneId: string, sliceId: string, ts: string)` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( "UPDATE slices SET replan_triggered_at = :ts… (missing: @param milestoneId, @param sliceId, @param ts)
- `src/resources/extensions/gsd/gsd-db.ts:2707` — `upsertQualityGate(g: { milestoneId: string; sliceId: string; gateId: string; scope: string; taskId: string; status: string; verdict: string; rationale: string; findings: string; evaluatedAt: string; })` — exported: **yes** — Implements logic starting with: { if (!currentDb) throw new GSDError(GSD_STALE_STATE, "gsd-db: No database open"); currentDb.prepare( `INSERT OR REPLACE INTO quality_gates (milest… (missing: @param g)
- `src/resources/extensions/gsd/gsd-db.ts:2743` — `restoreManifest(manifest: StateManifest)` — exported: **yes** — Executes a subprocess command. (missing: @param manifest)
- `src/resources/extensions/gsd/gsd-db.ts:2874` — `bulkInsertLegacyHierarchy(payload: { milestones: LegacyMilestoneInsert[]; slices: LegacySliceInsert[]; tasks: LegacyTaskInsert[]; clearMilestoneIds: string[]; createdAt: string; })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param payload)

### `src/resources/extensions/gsd/gsd-home.ts`
- `src/resources/extensions/gsd/gsd-home.ts:26` — `gsdHome()` — exported: **yes** — Implements logic starting with: { return process.env.GSD_HOME ? resolve(process.env.GSD_HOME) : join(homedir(), ".gsd"); } (missing: @returns)

### `src/resources/extensions/gsd/guided-flow-queue.ts`
- `src/resources/extensions/gsd/guided-flow-queue.ts:44` — `showQueue(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param pi, @param basePath)
- `src/resources/extensions/gsd/guided-flow-queue.ts:223` — `buildExistingMilestonesContext(basePath: string, milestoneIds: string[], state: import("./types.js").GSDState)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneIds, @param state, @returns)
- `src/resources/extensions/gsd/guided-flow-queue.ts:317` — `removeDependsOnFromContextFiles(basePath: string, depsToRemove: Array<{ milestone: string; dep: string }>)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param depsToRemove)

### `src/resources/extensions/gsd/guided-flow.ts`
- `src/resources/extensions/gsd/guided-flow.ts:135` — `verifyExpectedArtifactForScope(scope: MilestoneScope, unitType: string, unitId: string)` — exported: **yes** — Implements logic starting with: { return verifyExpectedArtifact(unitType, unitId, scope.workspace.projectRoot); } (missing: @param scope, @param unitType, @param unitId, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:147` — `resolveExpectedArtifactPathForScope(scope: MilestoneScope, unitType: string, unitId: string)` — exported: **yes** — Implements logic starting with: { return resolveExpectedArtifactPath(unitType, unitId, scope.workspace.projectRoot); } (missing: @param scope, @param unitType, @param unitId, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:176` — `isGhostMilestoneByScope(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { return isGhostMilestone(scope.workspace.projectRoot, scope.milestoneId); } (missing: @param scope, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:225` — `buildDocsCommitInstruction(_message: string)` — exported: **no** — Implements logic starting with: { return "Do not commit planning artifacts — .gsd/ is managed externally."; } (missing: @param _message, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:308` — `_getPendingAutoStart(basePath?: string)` — exported: **yes** — Implements logic starting with: { if (basePath) return pendingAutoStartMap.get(basePath) ?? null; if (pendingAutoStartMap.size === 1) return pendingAutoStartMap.values().next().va… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:351` — `setPendingAutoStart(basePath: string, entry: { basePath: string; milestoneId: string; ctx?: ExtensionCommandContext; pi?: ExtensionAPI; step?: boolean; createdAt?: number })` — exported: **yes** — Implements logic starting with: { const ws = createWorkspace(entry.basePath); const scope = scopeMilestone(ws, entry.milestoneId); pendingAutoStartMap.set(basePath, { createdAt: D… (missing: @param basePath, @param entry)
- `src/resources/extensions/gsd/guided-flow.ts:362` — `clearPendingAutoStart(basePath?: string)` — exported: **yes** — Implements logic starting with: { if (basePath) { pendingAutoStartMap.delete(basePath); } else { pendingAutoStartMap.clear(); } } (missing: @param basePath)
- `src/resources/extensions/gsd/guided-flow.ts:384` — `getDiscussionMilestoneId(basePath?: string)` — exported: **yes** — Implements logic starting with: { if (basePath) { return pendingAutoStartMap.get(basePath)?.milestoneId ?? null; } // Backward compat: return the sole entry's milestoneId, or null… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:552` — `checkAutoStartAfterDiscuss()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/guided-flow.ts:748` — `extractAssistantText(msg: any)` — exported: **no** — Implements logic starting with: { if (!msg) return ""; const content = msg.content; if (typeof content === "string") return content; if (!Array.isArray(content)) return ""; const… (missing: @param msg, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:776` — `hasToolUse(msg: any)` — exported: **no** — Implements logic starting with: { if (!msg) return false; const content = msg.content; if (!Array.isArray(content)) return false; return content.some( (b: any) => b && typeof b ==… (missing: @param msg, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:809` — `maybeHandleReadyPhraseWithoutFiles(event: { messages: any[] })` — exported: **yes** — Implements logic starting with: { const entry = _getPendingAutoStart(); if (!entry) return false; const { ctx, pi, basePath, milestoneId } = entry; // Gate: last assistant message… (missing: @param event, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:930` — `resetEmptyTurnCounter(basePath?: string)` — exported: **yes** — Implements logic starting with: { if (basePath) emptyTurnCounterByBase.delete(basePath); else emptyTurnCounterByBase.clear(); } (missing: @param basePath)
- `src/resources/extensions/gsd/guided-flow.ts:1011` — `parseMilestoneSequenceFromProject(content: string)` — exported: **no** — Implements logic starting with: { const ids: string[] = []; const lines = content.split(/\r?\n/); for (const line of lines) { const match = line.match(/^\|\s*(M\d{3}[A-Z0-9-]*)\s*… (missing: @param content, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:1036` — `dispatchWorkflow(pi: ExtensionAPI, note: string, customType = "gsd-run", ctx?: ExtensionContext, unitType?: string)` — exported: **no** — Performs filesystem I/O. (missing: @param pi, @param note, @param customType, @param ctx, @param unitType)
- `src/resources/extensions/gsd/guided-flow.ts:1159` — `resolveAvailableModel(modelId: string, availableModels: T[], currentProvider: string | undefined)` — exported: **no** — Transforms a collection into a derived result. (missing: @param modelId, @param availableModels, @param currentProvider, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:1198` — `buildDiscussPrompt(nextId: string, preamble: string, _basePath: string, pi: ExtensionAPI, ctx: ExtensionCommandContext, preparationContext?: string)` — exported: **no** — Implements logic starting with: { const milestoneRel = `.gsd/milestones/${nextId}`; const structuredQuestionsAvailable = getStructuredQuestionsAvailability(pi, ctx); const inlined… (missing: @param nextId, @param preamble, @param _basePath, @param pi, @param ctx, @param preparationContext, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:1225` — `buildHeadlessDiscussPrompt(nextId: string, seedContext: string, _basePath: string)` — exported: **no** — Implements logic starting with: { const milestoneRel = `.gsd/milestones/${nextId}`; const inlinedTemplates = [ inlineTemplate("project", "Project"), inlineTemplate("requirements",… (missing: @param nextId, @param seedContext, @param _basePath, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:1258` — `prepareAndBuildDiscussPrompt(ctx: ExtensionCommandContext, pi: ExtensionAPI, nextId: string, preamble: string, basePath: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param pi)
- `src/resources/extensions/gsd/guided-flow.ts:1302` — `bootstrapGsdProject(basePath: string)` — exported: **no** — Implements logic starting with: { if (!nativeIsRepo(basePath) || isInheritedRepo(basePath)) { const mainBranch = loadEffectiveGSDPreferences()?.preferences?.git?.main_branch || "m… (missing: @param basePath)
- `src/resources/extensions/gsd/guided-flow.ts:1322` — `showHeadlessMilestoneCreation(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, seedContext: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param basePath, @param seedContext)
- `src/resources/extensions/gsd/guided-flow.ts:1372` — `buildDiscussSlicePrompt(mid: string, sid: string, sTitle: string, base: string, options?: { rediscuss?: boolean; structuredQuestionsAvailable?: string })` — exported: **no** — Transforms a collection into a derived result. (missing: @param mid, @param sid, @param sTitle, @param base, @param options, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:1467` — `showDiscuss(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param pi, @param basePath)
- `src/resources/extensions/gsd/guided-flow.ts:1731` — `showDiscussQueuedMilestone(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, pendingMilestones: Array<{ id: string; title: string; status: string }>)` — exported: **no** — Transforms a collection into a derived result. (missing: @param ctx, @param pi, @param basePath, @param pendingMilestones)
- `src/resources/extensions/gsd/guided-flow.ts:1801` — `dispatchDiscussForMilestone(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, mid: string, milestoneTitle: string, opts: { fastPath?: boolean } = {})` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param basePath, @param mid, @param milestoneTitle, @param opts)
- `src/resources/extensions/gsd/guided-flow.ts:1850` — `selfHealRuntimeRecords(basePath: string, ctx: ExtensionContext)` — exported: **no** — Implements logic starting with: { try { const records = listUnitRuntimeRecords(basePath); let cleared = 0; for (const record of records) { const { unitType, unitId, phase } = reco… (missing: @param basePath, @param ctx, @returns)
- `src/resources/extensions/gsd/guided-flow.ts:1889` — `handleMilestoneActions(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, milestoneId: string, milestoneTitle: string, options?: { step?: boolean })` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param pi, @param basePath, @param milestoneId, @param milestoneTitle, @param options, @returns)

### `src/resources/extensions/gsd/health-widget-core.ts`
- `src/resources/extensions/gsd/health-widget-core.ts:43` — `formatRelativeTime(epochSeconds: number)` — exported: **yes** — Implements logic starting with: { const diffSeconds = Math.floor(Date.now() / 1000) - epochSeconds; if (diffSeconds < 60) return "just now"; const minutes = Math.floor(diffSeconds… (missing: @param epochSeconds, @returns)
- `src/resources/extensions/gsd/health-widget-core.ts:57` — `truncateMessage(msg: string, maxLen: number)` — exported: **no** — Implements logic starting with: { if (msg.length <= maxLen) return msg; return msg.slice(0, maxLen - 1) + "…"; } (missing: @param msg, @param maxLen, @returns)
- `src/resources/extensions/gsd/health-widget-core.ts:67` — `buildHealthLines(data: HealthWidgetData, width?: number)` — exported: **yes** — Implements logic starting with: { if (data.projectState === "none") { return [" GSD No project loaded — run /gsd to start"]; } if (data.projectState === "initialized") { return ["… (missing: @param data, @param width, @returns)

### `src/resources/extensions/gsd/health-widget.ts`
- `src/resources/extensions/gsd/health-widget.ts:93` — `initHealthWidget(ctx: ExtensionContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx)

### `src/resources/extensions/gsd/history.ts`
- `src/resources/extensions/gsd/history.ts:16` — `handleHistory(args: string, ctx: ExtensionCommandContext, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param args, @param ctx, @param basePath)

### `src/resources/extensions/gsd/init-wizard.ts`
- `src/resources/extensions/gsd/init-wizard.ts:67` — `showProjectInit(ctx: ExtensionCommandContext, pi: ExtensionAPI, basePath: string, detection: ProjectDetection)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param pi, @param basePath, @param detection, @returns)
- `src/resources/extensions/gsd/init-wizard.ts:364` — `offerMigration(ctx: ExtensionCommandContext, v1: NonNullable<ProjectDetection["v1"]>)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param v1, @returns)
- `src/resources/extensions/gsd/init-wizard.ts:407` — `handleReinit(ctx: ExtensionCommandContext, detection: ProjectDetection)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param detection)
- `src/resources/extensions/gsd/init-wizard.ts:533` — `bootstrapGsdDirectoryStructure(basePath: string, signals: ProjectSignals)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param signals)
- `src/resources/extensions/gsd/init-wizard.ts:555` — `mapInitPrefsToWizardShape(prefs: ProjectPreferences)` — exported: **yes** — Implements logic starting with: { const out: Record<string, unknown> = { mode: prefs.mode, git: { isolation: prefs.gitIsolation, main_branch: prefs.mainBranch, auto_push: prefs.au… (missing: @param prefs, @returns)

### `src/resources/extensions/gsd/journal.ts`
- `src/resources/extensions/gsd/journal.ts:111` — `emitJournalEvent(basePath: string, entry: JournalEntry)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @param entry)
- `src/resources/extensions/gsd/journal.ts:173` — `queryJournal(basePath: string, filters?: JournalQueryFilters)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param filters, @returns)

### `src/resources/extensions/gsd/json-persistence.ts`
- `src/resources/extensions/gsd/json-persistence.ts:9` — `loadJsonFile(filePath: string, validate: (data: unknown) => data is T, defaultFactory: () => T)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @param validate, @param defaultFactory, @returns)
- `src/resources/extensions/gsd/json-persistence.ts:28` — `loadJsonFileOrNull(filePath: string, validate: (data: unknown) => data is T)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @param validate, @returns)
- `src/resources/extensions/gsd/json-persistence.ts:52` — `saveJsonFile(filePath: string, data: T)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @param data)
- `src/resources/extensions/gsd/json-persistence.ts:69` — `writeJsonFileAtomic(filePath: string, data: T)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @param data)

### `src/resources/extensions/gsd/jsonl-utils.ts`
- `src/resources/extensions/gsd/jsonl-utils.ts:16` — `parseJSONL(raw: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param raw, @returns)

### `src/resources/extensions/gsd/key-manager.ts`
- `src/resources/extensions/gsd/key-manager.ts:84` — `maskKey(key: string)` — exported: **yes** — Implements logic starting with: { if (!key) return "(empty)"; if (key.length <= 8) return key.slice(0, 2) + "***" + key.slice(-2); return key.slice(0, 4) + "***" + key.slice(-4); } (missing: @param key, @returns)
- `src/resources/extensions/gsd/key-manager.ts:93` — `formatDuration(ms: number)` — exported: **yes** — Implements logic starting with: { if (ms <= 0) return "expired"; const seconds = Math.floor(ms / 1000); if (seconds < 60) return `${seconds}s`; const minutes = Math.floor(seconds… (missing: @param ms, @returns)
- `src/resources/extensions/gsd/key-manager.ts:107` — `describeCredential(cred: AuthCredential)` — exported: **yes** — Implements logic starting with: { if (cred.type === "api_key") { const apiCred = cred as ApiKeyCredential; if (!apiCred.key) return "empty key"; return `API key (${maskKey(apiCred… (missing: @param cred, @returns)
- `src/resources/extensions/gsd/key-manager.ts:125` — `getAuthPath()` — exported: **yes** — Implements logic starting with: { return join(gsdHome(), "agent", "auth.json"); } (missing: @returns)
- `src/resources/extensions/gsd/key-manager.ts:132` — `getKeyManagerAuthStorage()` — exported: **yes** — Implements logic starting with: { const authPath = getAuthPath(); mkdirSync(dirname(authPath), { recursive: true }); return AuthStorage.create(authPath); } (missing: @returns)
- `src/resources/extensions/gsd/key-manager.ts:141` — `findProvider(idOrLabel: string)` — exported: **yes** — Implements logic starting with: { const lower = idOrLabel.toLowerCase(); return PROVIDER_REGISTRY.find( (p) => p.id.toLowerCase() === lower || p.label.toLowerCase() === lower, ); } (missing: @param idOrLabel, @returns)
- `src/resources/extensions/gsd/key-manager.ts:162` — `getAllKeyStatuses(auth: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param auth, @returns)
- `src/resources/extensions/gsd/key-manager.ts:214` — `formatKeyDashboard(statuses: KeyStatus[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param statuses, @returns)
- `src/resources/extensions/gsd/key-manager.ts:264` — `handleAddKey(providerArg: string, ctx: ExtensionCommandContext, auth: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param providerArg, @param ctx, @param auth, @returns)
- `src/resources/extensions/gsd/key-manager.ts:349` — `handleRemoveKey(providerArg: string, ctx: ExtensionCommandContext, auth: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param providerArg, @param ctx, @param auth, @returns)
- `src/resources/extensions/gsd/key-manager.ts:521` — `testProviderKey(provider: ProviderInfo, auth: AuthStorage)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: @param provider, @param auth, @returns)
- `src/resources/extensions/gsd/key-manager.ts:589` — `formatTestResults(results: TestResult[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param results, @returns)
- `src/resources/extensions/gsd/key-manager.ts:627` — `handleRotateKey(providerArg: string, ctx: ExtensionCommandContext, auth: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param providerArg, @param ctx, @param auth, @returns)
- `src/resources/extensions/gsd/key-manager.ts:747` — `runKeyDoctor(auth: AuthStorage)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param auth, @returns)
- `src/resources/extensions/gsd/key-manager.ts:878` — `formatDoctorFindings(findings: DoctorFinding[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param findings, @returns)
- `src/resources/extensions/gsd/key-manager.ts:915` — `handleKeys(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param args, @param ctx)

### `src/resources/extensions/gsd/knowledge-backfill.ts`
- `src/resources/extensions/gsd/knowledge-backfill.ts:46` — `backfillKnowledgeToMemories(basePath: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return 0; const adapter = _getAdapter(); if (!adapter) return 0; try { const content = readKnowledgeMd(basePath); if (!cont… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/knowledge-backfill.ts:97` — `synthesize(row: KnowledgeRow)` — exported: **no** — Implements logic starting with: { if (row.table === "rules") return null; if (row.table === "patterns") { // Cells: [P###, Pattern, Where, Notes] const [, pattern, where, notes] =… (missing: @param row, @returns)

### `src/resources/extensions/gsd/knowledge-capture.ts`
- `src/resources/extensions/gsd/knowledge-capture.ts:43` — `captureKnowledgeEntry(basePath: string, type: PatternLessonType, entryText: string, scope: string)` — exported: **yes** — Implements logic starting with: { const cleaned = entryText.trim(); const idPrefix = type === "pattern" ? "P" : "L"; const id = nextKnowledgeId(basePath, idPrefix); if (!cleaned)… (missing: @param basePath, @param type, @param entryText, @param scope, @returns)
- `src/resources/extensions/gsd/knowledge-capture.ts:108` — `nextKnowledgeId(basePath: string, prefix: "K" | "P" | "L")` — exported: **yes** — Implements logic starting with: { const fromFile = maxIdInFile(basePath, prefix); const fromMemories = maxIdInMemories(prefix); const next = Math.max(fromFile, fromMemories) + 1;… (missing: @param basePath, @param prefix, @returns)

### `src/resources/extensions/gsd/knowledge-parser.ts`
- `src/resources/extensions/gsd/knowledge-parser.ts:59` — `readKnowledgeMd(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/knowledge-parser.ts:70` — `knowledgeMdPath(basePath: string)` — exported: **yes** — Implements logic starting with: { return resolveGsdRootFile(basePath, "KNOWLEDGE"); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/knowledge-parser.ts:79` — `parseKnowledgeRows(content: string)` — exported: **yes** — Executes a subprocess command. (missing: @param content, @returns)
- `src/resources/extensions/gsd/knowledge-parser.ts:121` — `extractIntroAndRules(content: string)` — exported: **yes** — Implements logic starting with: { if (!content.trim()) return ""; const lines = content.split("\n"); const rulesIdx = lines.findIndex((l) => l.trim() === "## Rules"); if (rulesIdx… (missing: @param content, @returns)
- `src/resources/extensions/gsd/knowledge-parser.ts:150` — `splitPipeRow(row: string)` — exported: **yes** — Implements logic starting with: { const cells: string[] = []; let current = ""; for (let i = 0; i < row.length; i++) { const ch = row[i]; if (ch === "\\" && row[i + 1] === "|") {… (missing: @param row, @returns)

### `src/resources/extensions/gsd/knowledge-projection.ts`
- `src/resources/extensions/gsd/knowledge-projection.ts:71` — `readKnowledgeMemories(category: "pattern" | "gotcha")` — exported: **no** — Parses JSON input into a structured value. (missing: @param category, @returns)
- `src/resources/extensions/gsd/knowledge-projection.ts:142` — `extractRulesSection(existing: string)` — exported: **no** — Implements logic starting with: { const lines = existing.split("\n"); const startIdx = lines.findIndex((l) => l.trim() === RULES_HEADING); if (startIdx === -1) { return [ RULES_HE… (missing: @param existing, @returns)
- `src/resources/extensions/gsd/knowledge-projection.ts:180` — `extractIntro(existing: string)` — exported: **no** — Implements logic starting with: { if (!existing.trim()) return DEFAULT_INTRO; const lines = existing.split("\n"); const rulesIdx = lines.findIndex((l) => l.trim() === RULES_HEADIN… (missing: @param existing, @returns)
- `src/resources/extensions/gsd/knowledge-projection.ts:202` — `renderKnowledgeProjection(basePath: string)` — exported: **yes** — Implements logic starting with: { try { const existing = readKnowledgeMd(basePath); const intro = extractIntro(existing); const rules = extractRulesSection(existing); const patter… (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/markdown-renderer.ts`
- `src/resources/extensions/gsd/markdown-renderer.ts:50` — `toArtifactPath(absPath: string, basePath: string)` — exported: **no** — Implements logic starting with: { const root = gsdRoot(basePath); const rel = relative(root, absPath); // Normalize to forward slashes for consistent DB keys return rel.replace(/\… (missing: @param absPath, @param basePath, @returns)
- `src/resources/extensions/gsd/markdown-renderer.ts:94` — `loadArtifactContent(artifactPath: string)` — exported: **no** — Implements logic starting with: { const artifact = getArtifact(artifactPath); if (artifact && artifact.full_content) { return artifact.full_content; } return null; } (missing: @param artifactPath, @returns)
- `src/resources/extensions/gsd/markdown-renderer.ts:108` — `writeAndStore(absPath: string, artifactPath: string, content: string, opts: { artifact_type: string; milestone_id: string; slice_id?: string; task_id?: string; })` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param absPath, @param artifactPath, @param content, @param opts)
- `src/resources/extensions/gsd/markdown-renderer.ts:463` — `renderRoadmapCheckboxes(basePath: string, milestoneId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/markdown-renderer.ts:530` — `renderPlanCheckboxes(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneId, @param sliceId)
- `src/resources/extensions/gsd/markdown-renderer.ts:596` — `renderTaskSummary(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId)
- `src/resources/extensions/gsd/markdown-renderer.ts:639` — `renderSliceSummary(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneId, @param sliceId)
- `src/resources/extensions/gsd/markdown-renderer.ts:702` — `renderAllFromDb(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/markdown-renderer.ts:784` — `detectStaleRenders(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/marketplace-discovery.ts`
- `src/resources/extensions/gsd/marketplace-discovery.ts:138` — `isLocalSource(source: PluginSource)` — exported: **no** — Implements logic starting with: { if (typeof source === 'string') { return !source.startsWith('http://') && !source.startsWith('https://') && !source.startsWith('git@') && !source… (missing: @param source, @returns)
- `src/resources/extensions/gsd/marketplace-discovery.ts:151` — `resolvePluginRoot(repoRoot: string, source: PluginSource)` — exported: **yes** — Implements logic starting with: { if (!isLocalSource(source)) { // External source (URL, git repo) - can't resolve locally return null; } // Handle both ./plugins/name and plugins… (missing: @param repoRoot, @param source, @returns)

### `src/resources/extensions/gsd/md-importer.ts`
- `src/resources/extensions/gsd/md-importer.ts:46` — `parseDecisionsTable(content: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/md-importer.ts:129` — `parseRequirementsSections(content: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param content, @returns)
- `src/resources/extensions/gsd/md-importer.ts:275` — `importDecisions(gsdDir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param gsdDir, @returns)
- `src/resources/extensions/gsd/md-importer.ts:292` — `importRequirements(gsdDir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param gsdDir, @returns)
- `src/resources/extensions/gsd/md-importer.ts:317` — `importHierarchyArtifacts(gsdDir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param gsdDir, @returns)
- `src/resources/extensions/gsd/md-importer.ts:419` — `importFilesAtLevel(dirPath: string, idPrefix: string, suffixes: string[], relativeBase: string, milestoneId: string, sliceId: string | null, taskId: string | null)` — exported: **no** — Performs filesystem I/O. (missing: @param dirPath, @param idPrefix, @param suffixes, @param relativeBase, @param milestoneId, @param sliceId, @param taskId, @returns)
- `src/resources/extensions/gsd/md-importer.ts:458` — `findDirByPrefix(parentDir: string, idPrefix: string)` — exported: **no** — Implements logic starting with: { if (!existsSync(parentDir)) return null; try { const entries = readdirSync(parentDir, { withFileTypes: true }); // Exact match first const exact… (missing: @param parentDir, @param idPrefix, @returns)
- `src/resources/extensions/gsd/md-importer.ts:477` — `findFileByPrefixAndSuffix(dir: string, idPrefix: string, suffix: string)` — exported: **no** — Implements logic starting with: { if (!existsSync(dir)) return null; try { const entries = readdirSync(dir); // Direct: ID-SUFFIX.md const target = `${idPrefix}-${suffix}.md`.toUp… (missing: @param dir, @param idPrefix, @param suffix, @returns)
- `src/resources/extensions/gsd/md-importer.ts:511` — `migrateHierarchyToDb(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/md-importer.ts:699` — `migrateFromMarkdown(gsdDir: string)` — exported: **yes** — Implements logic starting with: { const dbPath = join(gsdRoot(gsdDir), 'gsd.db'); // Open DB if not already open if (!_getAdapter()) { openDatabase(dbPath); } let decisions = 0; l… (missing: @param gsdDir, @returns)

### `src/resources/extensions/gsd/memory-backfill.ts`
- `src/resources/extensions/gsd/memory-backfill.ts:67` — `backfillDecisionsToMemories()` — exported: **yes** — Serializes a value to JSON. (missing: @returns)
- `src/resources/extensions/gsd/memory-backfill.ts:199` — `synthesizeDecisionMemoryContent(row: DecisionContentFields)` — exported: **yes** — Implements logic starting with: { const trim = (value: string, max: number): string => { const cleaned = value.replace(/\s+/g, " ").trim(); return cleaned.length > max ? cleaned.s… (missing: @param row, @returns)

### `src/resources/extensions/gsd/memory-consolidation-scanner.ts`
- `src/resources/extensions/gsd/memory-consolidation-scanner.ts:73` — `parseKnowledgeRows(content: string)` — exported: **yes** — Executes a subprocess command. (missing: @param content, @returns)
- `src/resources/extensions/gsd/memory-consolidation-scanner.ts:150` — `memoryHasSourceMarker(markerKey: string, value: string)` — exported: **no** — Implements logic starting with: { if (!isDbAvailable()) return false; const adapter = _getAdapter(); if (!adapter) return false; try { const pattern = `%"${markerKey}":"${value}"%… (missing: @param markerKey, @param value, @returns)
- `src/resources/extensions/gsd/memory-consolidation-scanner.ts:174` — `scanConsolidationGaps(basePath: string)` — exported: **yes** — Implements logic starting with: { // ── Decisions ──────────────────────────────────────────────────────── const decisions = getActiveDecisions(); const decisionSamples: Decisions… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/memory-consolidation-scanner.ts:263` — `reportConsolidationGaps(basePath: string)` — exported: **yes** — Implements logic starting with: { try { const report = scanConsolidationGaps(basePath); if (report.totalGaps === 0) return report; appendNotification(report.summary, "warning", "w… (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/memory-embeddings.ts`
- `src/resources/extensions/gsd/memory-embeddings.ts:53` — `buildEmbeddingFn(ctx: ExtensionContext)` — exported: **yes** — Implements logic starting with: { try { const available = ctx.modelRegistry?.getAvailable?.(); if (!available || available.length === 0) return null; const candidate = available.f… (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/memory-embeddings.ts:216` — `embedMemories(memories: Array<{ id: string; content: string }>, embedFn: EmbedFn | null, model: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param memories, @param embedFn, @param model, @returns)

### `src/resources/extensions/gsd/memory-extractor.ts`
- `src/resources/extensions/gsd/memory-extractor.ts:74` — `buildMemoryLLMCall(ctx: ExtensionContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/memory-extractor.ts:184` — `extractTranscriptFromActivity(raw: string, maxChars = 30_000)` — exported: **no** — Parses JSON input into a structured value. (missing: @param raw, @param maxChars, @returns)
- `src/resources/extensions/gsd/memory-extractor.ts:232` — `parseMemoryResponse(raw: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param raw, @returns)
- `src/resources/extensions/gsd/memory-extractor.ts:325` — `extractMemoriesFromTranscript(transcript: string, llmCallFn: LLMCallFn, opts: ExtractFromTranscriptOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param transcript, @param llmCallFn, @param opts, @returns)
- `src/resources/extensions/gsd/memory-extractor.ts:383` — `extractMemoriesFromUnit(activityFile: string, unitType: string, unitId: string, llmCallFn: LLMCallFn)` — exported: **yes** — Performs filesystem I/O. (missing: @param activityFile, @param unitType, @param unitId, @param llmCallFn)

### `src/resources/extensions/gsd/memory-ingest.ts`
- `src/resources/extensions/gsd/memory-ingest.ts:236` — `ingestArtifact(basePath: string, milestoneId: string, artifactType: string, ctx: ExtensionContext | null, opts: IngestOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param basePath, @param milestoneId, @param artifactType, @param ctx, @param opts, @returns)

### `src/resources/extensions/gsd/memory-source-store.ts`
- `src/resources/extensions/gsd/memory-source-store.ts:73` — `createMemorySource(opts: CreateSourceOptions)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const adapter = _getAdapter(); if (!adapter) return null; try { const contentHash = hashContent(opts.content);… (missing: @param opts, @returns)

### `src/resources/extensions/gsd/memory-store.ts`
- `src/resources/extensions/gsd/memory-store.ts:118` — `memoryDecayFactor(lastHitAt: string | null)` — exported: **yes** — Implements logic starting with: { if (!lastHitAt) return 1.0; const ts = Date.parse(lastHitAt); if (!Number.isFinite(ts)) return 1.0; const daysAgo = Math.max(0, (Date.now() - ts)… (missing: @param lastHitAt, @returns)
- `src/resources/extensions/gsd/memory-store.ts:176` — `getActiveMemories()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/memory-store.ts:193` — `getActiveMemoriesRanked(limit = 30)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param limit, @returns)
- `src/resources/extensions/gsd/memory-store.ts:533` — `nextMemoryId()` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return 'MEM001'; const adapter = _getAdapter(); if (!adapter) return 'MEM001'; try { const row = adapter .prepare('SELECT M… (missing: @returns)
- `src/resources/extensions/gsd/memory-store.ts:565` — `createMemory(fields: { category: string; content: string; confidence?: number; source_unit_type?: string; source_unit_id?: string; scope?: string; tags?: string[]; structuredFields?: Record<string, unknown> | null; })` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return null; const adapter = _getAdapter(); if (!adapter) return null; try { return transaction(() => doCreateMemory(adapte… (missing: @param fields, @returns)
- `src/resources/extensions/gsd/memory-store.ts:647` — `updateMemoryContent(id: string, content: string, confidence?: number)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { updateMemoryContentRow(id, content, confidence, new Date().toISOString()); return true; } catch { retur… (missing: @param id, @param content, @param confidence, @returns)
- `src/resources/extensions/gsd/memory-store.ts:661` — `reinforceMemory(id: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { incrementMemoryHitCount(id, new Date().toISOString()); return true; } catch { return false; } } (missing: @param id, @returns)
- `src/resources/extensions/gsd/memory-store.ts:675` — `supersedeMemory(oldId: string, newId: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { supersedeMemoryRow(oldId, newId, new Date().toISOString()); return true; } catch { return false; } } (missing: @param oldId, @param newId, @returns)
- `src/resources/extensions/gsd/memory-store.ts:691` — `isUnitProcessed(unitKey: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; const adapter = _getAdapter(); if (!adapter) return false; try { const row = adapter.prepare( 'SELECT 1 FROM… (missing: @param unitKey, @returns)
- `src/resources/extensions/gsd/memory-store.ts:709` — `markUnitProcessed(unitKey: string, activityFile: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return false; try { markMemoryUnitProcessed(unitKey, activityFile, new Date().toISOString()); return true; } catch { return… (missing: @param unitKey, @param activityFile, @returns)
- `src/resources/extensions/gsd/memory-store.ts:727` — `decayStaleMemories(thresholdUnits = 20)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param thresholdUnits, @returns)
- `src/resources/extensions/gsd/memory-store.ts:759` — `enforceMemoryCap(max = 50)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param max)
- `src/resources/extensions/gsd/memory-store.ts:798` — `applyMemoryActions(actions: MemoryAction[], unitType?: string, unitId?: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable() || actions.length === 0) return; try { transaction(() => { for (const action of actions) { switch (action.action) { case 'CR… (missing: @param actions, @param unitType, @param unitId)
- `src/resources/extensions/gsd/memory-store.ts:871` — `formatMemoriesForPrompt(memories: Memory[], tokenBudget = 2000)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param memories, @param tokenBudget, @returns)

### `src/resources/extensions/gsd/metrics.ts`
- `src/resources/extensions/gsd/metrics.ts:127` — `initMetrics(base: string)` — exported: **yes** — Implements logic starting with: { basePath = base; ledger = loadLedger(base); } (missing: @param base)
- `src/resources/extensions/gsd/metrics.ts:148` — `snapshotUnitMetrics(ctx: ExtensionContext, unitType: string, unitId: string, startedAt: number, model: string, opts?: { tier?: string; modelDowngraded?: boolean; contextWindowTokens?: number; truncationSections?: number; continueHereFired?: boolean; promptCharCount?: number; baselineCharCount?: number; autoSessionKey?: string; traceId?: string; turnId?: string; causedBy?: string; })` — exported: **yes** — Implements logic starting with: { if (!ledger) return null; const entries = ctx.sessionManager.getEntries(); if (!entries || entries.length === 0) return null; const tokens: Token… (missing: @param ctx, @param unitType, @param unitId, @param startedAt, @param model, @param opts, @returns)
- `src/resources/extensions/gsd/metrics.ts:285` — `getLedger()` — exported: **yes** — Implements logic starting with: { return ledger; } (missing: @returns)
- `src/resources/extensions/gsd/metrics.ts:296` — `initMetricsByScope(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { const base = scope.workspace.projectRoot; const loaded = loadLedger(base); scopedLedgers.set(scope.workspace.identityKey, loaded); } (missing: @param scope)
- `src/resources/extensions/gsd/metrics.ts:305` — `getLedgerByScope(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { return scopedLedgers.get(scope.workspace.identityKey) ?? null; } (missing: @param scope, @returns)
- `src/resources/extensions/gsd/metrics.ts:312` — `resetMetricsByScope(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { scopedLedgers.delete(scope.workspace.identityKey); } (missing: @param scope)
- `src/resources/extensions/gsd/metrics.ts:329` — `snapshotUnitMetricsByScope(scope: MilestoneScope, ctx: ExtensionContext, unitType: string, unitId: string, startedAt: number, model: string, opts?: { tier?: string; modelDowngraded?: boolean; contextWindowTokens?: number; truncationSections?: number; continueHereFired?: boolean; promptCharCount?: number; baselineCharCount?: number; autoSessionKey?: string; traceId?: string; turnId?: string; causedBy?: string; })` — exported: **yes** — Implements logic starting with: { const base = scope.workspace.projectRoot; const key = scope.workspace.identityKey; // Lazy init: load from disk if not yet in scoped map. if (!sc… (missing: @param scope, @param ctx, @param unitType, @param unitId, @param startedAt, @param model, @param opts, @returns)
- `src/resources/extensions/gsd/metrics.ts:632` — `formatTierSavings(units: UnitMetrics[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param units, @returns)
- `src/resources/extensions/gsd/metrics.ts:647` — `aggregateCacheHitRate()` — exported: **yes** — Implements logic starting with: { if (!ledger || ledger.units.length === 0) return 0; let totalInput = 0; let totalCacheRead = 0; for (const unit of ledger.units) { totalInput +=… (missing: @returns)
- `src/resources/extensions/gsd/metrics.ts:674` — `getAverageCostPerUnitType(units: UnitMetrics[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param units, @returns)
- `src/resources/extensions/gsd/metrics.ts:722` — `formatCostProjection(completedSlices: SliceAggregate[], remainingCount: number, budgetCeiling?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param completedSlices, @param remainingCount, @param budgetCeiling, @returns)
- `src/resources/extensions/gsd/metrics.ts:776` — `pruneMetricsLedger(base: string, keepCount: number)` — exported: **yes** — Implements logic starting with: { const disk = loadLedgerFromDisk(base); if (!disk || disk.units.length <= keepCount) return 0; const removed = disk.units.length - keepCount; disk… (missing: @param base, @param keepCount)
- `src/resources/extensions/gsd/metrics.ts:799` — `loadLedgerFromDisk(base: string)` — exported: **yes** — Implements logic starting with: { return loadJsonFileOrNull(metricsPath(base), isMetricsLedger); } (missing: @param base, @returns)
- `src/resources/extensions/gsd/metrics.ts:822` — `deduplicateUnits(units: UnitMetrics[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param units, @returns)
- `src/resources/extensions/gsd/metrics.ts:880` — `acquireLock(lockPath: string, timeoutMs = 2000)` — exported: **no** — Performs filesystem I/O. (missing: @param lockPath, @param timeoutMs, @returns)
- `src/resources/extensions/gsd/metrics.ts:933` — `saveLedger(base: string, data: MetricsLedger)` — exported: **no** — Implements logic starting with: { const path = metricsPath(base); const lockPath = `${path}.lock`; const acquired = acquireLock(lockPath); if (acquired) { try { // Read current on… (missing: @param base, @param data)

### `src/resources/extensions/gsd/migrate-external.ts`
- `src/resources/extensions/gsd/migrate-external.ts:36` — `migrateToExternalState(basePath: string)` — exported: **yes** — Implements logic starting with: { // Worktrees get their .gsd via syncGsdStateToWorktree(), not migration. // Migration inside a worktree would compute the same external hash as t… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/migrate-external.ts:197` — `recoverFailedMigration(basePath: string)` — exported: **yes** — Implements logic starting with: { const localGsd = join(basePath, ".gsd"); const migratingPath = join(basePath, ".gsd.migrating"); if (!existsSync(migratingPath)) return false; if… (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/migrate/command.ts`
- `src/resources/extensions/gsd/migrate/command.ts:77` — `formatPreviewStats(preview: MigrationPreview)` — exported: **no** — Implements logic starting with: { const lines = [ `- Decisions: ${preview.decisions.total}`, `- Milestones: ${preview.milestoneCount}`, `- Slices: ${preview.totalSlices} (${previe… (missing: @param preview, @returns)
- `src/resources/extensions/gsd/migrate/command.ts:93` — `buildReviewPrompt(sourcePath: string, gsdPath: string, preview: MigrationPreview)` — exported: **no** — Performs filesystem I/O. (missing: @param sourcePath, @param gsdPath, @param preview, @returns)
- `src/resources/extensions/gsd/migrate/command.ts:110` — `dispatchReview(pi: ExtensionAPI, sourcePath: string, gsdPath: string, preview: MigrationPreview)` — exported: **no** — Implements logic starting with: { const prompt = buildReviewPrompt(sourcePath, gsdPath, preview); pi.sendMessage( { customType: "gsd-migrate-review", content: prompt, display: fal… (missing: @param pi, @param sourcePath, @param gsdPath, @param preview)

### `src/resources/extensions/gsd/migrate/parser.ts`
- `src/resources/extensions/gsd/migrate/parser.ts:32` — `readOptional(path: string)` — exported: **no** — Performs filesystem I/O. (missing: @param path, @returns)
- `src/resources/extensions/gsd/migrate/parser.ts:41` — `listDir(path: string)` — exported: **no** — Implements logic starting with: { try { return readdirSync(path); } catch { return []; } } (missing: @param path, @returns)
- `src/resources/extensions/gsd/migrate/parser.ts:50` — `isDir(path: string)` — exported: **no** — Implements logic starting with: { try { return statSync(path).isDirectory(); } catch { return false; } } (missing: @param path, @returns)
- `src/resources/extensions/gsd/migrate/parser.ts:59` — `parsePhaseDir(dirName: string)` — exported: **no** — Implements logic starting with: { const match = dirName.match(/^(\d+(?:\.\d+)?)-(.+)$/); if (!match) return null; return { number: parseFloat(match[1]), slug: match[2] }; } (missing: @param dirName, @returns)
- `src/resources/extensions/gsd/migrate/parser.ts:66` — `parseQuickDir(dirName: string)` — exported: **no** — Implements logic starting with: { const match = dirName.match(/^(\d+)-(.+)$/); if (!match) return null; return { number: parseInt(match[1], 10), slug: match[2] }; } (missing: @param dirName, @returns)
- `src/resources/extensions/gsd/migrate/parser.ts:257` — `parsePlanningDirectory(path: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param path, @returns)

### `src/resources/extensions/gsd/migrate/parsers.ts`
- `src/resources/extensions/gsd/migrate/parsers.ts:33` — `extractXmlTag(content: string, tagName: string)` — exported: **no** — Executes a subprocess command. (missing: @param content, @param tagName, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:42` — `extractTasks(content: string)` — exported: **no** — Executes a subprocess command. (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:59` — `parsePhaseEntry(line: string)` — exported: **no** — Implements logic starting with: { // Strip bold markers (**) for uniform matching — old roadmaps often bold phase entries const stripped = line.replace(/\*\*/g, ''); // Format 1:… (missing: @param line, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:108` — `parseOldRoadmap(content: string)` — exported: **yes** — Implements logic starting with: { const result: PlanningRoadmap = { raw: content, milestones: [], phases: [], }; const lines = content.split('\n'); // ─── Strategy 1: Detect <deta… (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:215` — `parseDetailsBlockMilestones(lines: string[])` — exported: **no** — Implements logic starting with: { const milestones: PlanningRoadmapMilestone[] = []; let inDetails = false; let currentMilestone: PlanningRoadmapMilestone | null = null; for (cons… (missing: @param lines, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:265` — `unquote(val: unknown)` — exported: **no** — Implements logic starting with: { const s = String(val ?? ''); if ((s.startsWith('"') && s.endsWith('"')) || (s.startsWith("'") && s.endsWith("'"))) { return s.slice(1, -1); } ret… (missing: @param val, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:277` — `parseMustHavesFromLines(fmLines: string[])` — exported: **no** — Implements logic starting with: { const start = fmLines.findIndex(l => /^must_haves\s*:/.test(l)); if (start === -1) return null; const truths: string[] = []; const artifacts: str… (missing: @param fmLines, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:332` — `parseOldPlan(content: string, fileName: string = '', planNumber: string = '')` — exported: **yes** — Implements logic starting with: { const [fmLines, body] = splitFrontmatter(content); const fm = fmLines ? parseFrontmatterMap(fmLines) : {}; const frontmatter = parsePlanFrontmatt… (missing: @param content, @param fileName, @param planNumber, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:393` — `parseOldSummary(content: string, fileName: string = '', planNumber: string = '')` — exported: **yes** — Implements logic starting with: { const [fmLines, body] = splitFrontmatter(content); const fm = fmLines ? parseFrontmatterMap(fmLines) : {}; return { fileName, planNumber: planNum… (missing: @param content, @param fileName, @param planNumber, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:412` — `parseOldRequirements(content: string)` — exported: **yes** — Implements logic starting with: { const requirements: PlanningRequirement[] = []; const lines = content.split('\n'); let currentStatus = ''; let currentReq: Partial<PlanningRequir… (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:504` — `parseOldProject(content: string)` — exported: **yes** — Implements logic starting with: { return content; } (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:514` — `parseOldState(content: string)` — exported: **yes** — Implements logic starting with: { const currentPhase = extractBoldField(content, 'Current Phase'); const status = extractBoldField(content, 'Status'); return { raw: content, curre… (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/parsers.ts:531` — `parseOldConfig(content: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param content, @returns)

### `src/resources/extensions/gsd/migrate/preview.ts`
- `src/resources/extensions/gsd/migrate/preview.ts:18` — `generatePreview(project: GSDProject)` — exported: **yes** — Implements logic starting with: { let totalSlices = 0; let totalTasks = 0; let doneSlices = 0; let doneTasks = 0; for (const milestone of project.milestones) { for (const slice of… (missing: @param project, @returns)

### `src/resources/extensions/gsd/migrate/validator.ts`
- `src/resources/extensions/gsd/migrate/validator.ts:20` — `validatePlanningDirectory(path: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param path, @returns)

### `src/resources/extensions/gsd/migrate/writer.ts`
- `src/resources/extensions/gsd/migrate/writer.ts:70` — `serializeFrontmatter(data: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { const lines: string[] = ['---']; for (const [key, value] of Object.entries(data)) { if (value === undefined || value === null) continue; if (type… (missing: @param data, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:115` — `formatRoadmap(milestone: GSDMilestone)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`# ${milestone.id}: ${milestone.title}`); lines.push(''); lines.push(`**Vision:** ${milestone.vision || '(… (missing: @param milestone, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:152` — `formatPlan(slice: GSDSlice)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`# ${slice.id}: ${slice.title}`); lines.push(''); lines.push(`**Goal:** ${slice.goal || slice.title}`); li… (missing: @param slice, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:193` — `formatSliceSummary(slice: GSDSlice, milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (!slice.summary) return ''; const s = slice.summary; const fm = serializeFrontmatter({ id: slice.id, parent: milestoneId, milestone: milestone… (missing: @param slice, @param milestoneId, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:233` — `formatTaskSummary(task: GSDTask, sliceId: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (!task.summary) return ''; const s = task.summary; const fm = serializeFrontmatter({ id: task.id, parent: sliceId, milestone: milestoneId, pro… (missing: @param task, @param sliceId, @param milestoneId, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:274` — `formatTaskPlan(task: GSDTask, sliceId: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`# ${task.id}: ${task.title}`); lines.push(''); lines.push(`**Slice:** ${sliceId} — **Milestone:** ${miles… (missing: @param task, @param sliceId, @param milestoneId, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:312` — `formatRequirements(requirements: GSDRequirement[])` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push('# Requirements'); lines.push(''); const groups: Record<string, GSDRequirement[]> = { active: [], validate… (missing: @param requirements, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:367` — `formatProject(content: string)` — exported: **yes** — Implements logic starting with: { if (!content || !content.trim()) { return '# Project\n\n(Migrated project — no description available.)\n'; } return content.endsWith('\n') ? cont… (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:378` — `formatDecisions(content: string)` — exported: **yes** — Implements logic starting with: { if (!content || !content.trim()) { return [ '# Decisions Register', '', '<!-- Append-only. Never edit or remove existing rows.', ' To reverse a d… (missing: @param content, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:399` — `formatContext(milestoneId: string)` — exported: **yes** — Implements logic starting with: { return `# ${milestoneId} Context\n\nMigrated milestone — no upstream dependencies.\n`; } (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:408` — `formatState(milestones: GSDMilestone[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestones, @returns)
- `src/resources/extensions/gsd/migrate/writer.ts:434` — `writeGSDDirectory(project: GSDProject, targetPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param project, @param targetPath, @returns)

### `src/resources/extensions/gsd/milestone-actions.ts`
- `src/resources/extensions/gsd/milestone-actions.ts:34` — `assertNotAutoActive(action: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param action)
- `src/resources/extensions/gsd/milestone-actions.ts:49` — `parkMilestone(basePath: string, milestoneId: string, reason: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @param reason, @returns)
- `src/resources/extensions/gsd/milestone-actions.ts:92` — `unparkMilestone(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { assertNotAutoActive("unpark milestone"); const mDir = resolveMilestonePath(basePath, milestoneId); if (!mDir || !existsSync(mDir)) return false;… (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/milestone-actions.ts:127` — `discardMilestone(basePath: string, milestoneId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/milestone-actions.ts:166` — `isParked(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { return !!resolveMilestoneFile(basePath, milestoneId, "PARKED"); } (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/milestone-actions.ts:174` — `getParkedReason(basePath: string, milestoneId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @returns)

### `src/resources/extensions/gsd/milestone-id-reservation.ts`
- `src/resources/extensions/gsd/milestone-id-reservation.ts:19` — `nextMilestoneIdReserved(existingIds: string[], uniqueEnabled: boolean, basePath?: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param existingIds, @param uniqueEnabled, @param basePath, @returns)

### `src/resources/extensions/gsd/milestone-id-utils.ts`
- `src/resources/extensions/gsd/milestone-id-utils.ts:9` — `extractMilestoneSeq(id: string)` — exported: **yes** — Implements logic starting with: { const match = id.match(/^M(\d{3})(?:-[a-z0-9]{6})?$/); return match ? parseInt(match[1], 10) : 0; } (missing: @param id, @returns)
- `src/resources/extensions/gsd/milestone-id-utils.ts:15` — `milestoneIdSort(a: string, b: string)` — exported: **yes** — Implements logic starting with: { return extractMilestoneSeq(a) - extractMilestoneSeq(b); } (missing: @param a, @param b, @returns)

### `src/resources/extensions/gsd/milestone-ids.ts`
- `src/resources/extensions/gsd/milestone-ids.ts:23` — `extractMilestoneSeq(id: string)` — exported: **yes** — Implements logic starting with: { const m = id.match(/^M(\d{3})(?:-[a-z0-9]{6})?$/); return m ? parseInt(m[1], 10) : 0; } (missing: @param id, @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:29` — `parseMilestoneId(id: string)` — exported: **yes** — Implements logic starting with: { const m = id.match(/^M(\d{3})(?:-([a-z0-9]{6}))?$/); if (!m) return { num: 0 }; return { ...(m[2] ? { suffix: m[2] } : {}), num: parseInt(m[1], 1… (missing: @param id, @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:41` — `milestoneIdSort(a: string, b: string)` — exported: **yes** — Implements logic starting with: { return extractMilestoneSeq(a) - extractMilestoneSeq(b); } (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:48` — `generateMilestoneSuffix()` — exported: **yes** — Implements logic starting with: { const chars = "abcdefghijklmnopqrstuvwxyz0123456789"; let result = ""; for (let i = 0; i < 6; i++) { result += chars[randomInt(36)]; } return res… (missing: @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:58` — `maxMilestoneNum(milestoneIds: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestoneIds, @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:66` — `nextMilestoneId(milestoneIds: string[], uniqueEnabled?: boolean)` — exported: **yes** — Implements logic starting with: { const seq = String(maxMilestoneNum(milestoneIds) + 1).padStart(3, "0"); if (uniqueEnabled) { return `M${seq}-${generateMilestoneSuffix()}`; } ret… (missing: @param milestoneIds, @param uniqueEnabled, @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:85` — `reserveMilestoneId(id: string)` — exported: **yes** — Implements logic starting with: { reservedMilestoneIds.add(id); } (missing: @param id)
- `src/resources/extensions/gsd/milestone-ids.ts:93` — `claimReservedId()` — exported: **yes** — Implements logic starting with: { const first = reservedMilestoneIds.values().next().value; if (first !== undefined) { reservedMilestoneIds.delete(first); return first; } return u… (missing: @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:103` — `getReservedMilestoneIds()` — exported: **yes** — Implements logic starting with: { return reservedMilestoneIds; } (missing: @returns)
- `src/resources/extensions/gsd/milestone-ids.ts:115` — `findMilestoneIds(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/milestone-scope-classifier.ts`
- `src/resources/extensions/gsd/milestone-scope-classifier.ts:108` — `extractFileCountHint(text: string)` — exported: **no** — Implements logic starting with: { // Explicit phrasing: "a single file", "two files", "3 files" const singleFileMatch = /\b(a|one|single)\s+(file|page)\b/i.test(text); if (singleF… (missing: @param text, @returns)
- `src/resources/extensions/gsd/milestone-scope-classifier.ts:159` — `mentionsWithoutNegation(text: string, term: string)` — exported: **no** — Implements logic starting with: { const lower = text.toLowerCase(); const termPattern = new RegExp(String.raw`\b${term}\b`, "gi"); const matches = Array.from(lower.matchAll(termPa… (missing: @param text, @param term, @returns)
- `src/resources/extensions/gsd/milestone-scope-classifier.ts:210` — `milestoneRowToScopeInput(row: { title?: string; vision?: string; success_criteria?: string[]; key_risks?: Array<{ risk?: string; whyItMatters?: string }>; definition_of_done?: string[]; requirement_coverage?: string; verification_contract?: string; verification_integration?: string; verification_operational?: string; verification_uat?: string; })` — exported: **yes** — Implements logic starting with: { return { title: row.title, vision: row.vision, successCriteria: row.success_criteria, keyRisks: row.key_risks, definitionOfDone: row.definition_o… (missing: @param row, @returns)
- `src/resources/extensions/gsd/milestone-scope-classifier.ts:243` — `getMilestonePipelineVariant(mid: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mid, @returns)
- `src/resources/extensions/gsd/milestone-scope-classifier.ts:274` — `classifyMilestoneScope(input: MilestoneScopeInput)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param input, @returns)

### `src/resources/extensions/gsd/milestone-summary-classifier.ts`
- `src/resources/extensions/gsd/milestone-summary-classifier.ts:40` — `isTerminalMilestoneSummaryContent(content: string)` — exported: **yes** — Implements logic starting with: { return classifyMilestoneSummaryContent(content) !== "failure"; } (missing: @param content, @returns)

### `src/resources/extensions/gsd/milestone-validation-gates.ts`
- `src/resources/extensions/gsd/milestone-validation-gates.ts:28` — `insertMilestoneValidationGates(milestoneId: string, sliceId: string, verdict: string, evaluatedAt: string)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const gateVerdict = verdict === "pass" ? "pass" : "flag"; const milestoneGates = getGatesForTurn("validate-mileston… (missing: @param milestoneId, @param sliceId, @param verdict, @param evaluatedAt)

### `src/resources/extensions/gsd/model-cost-table.ts`
- `src/resources/extensions/gsd/model-cost-table.ts:76` — `lookupModelCost(modelId: string)` — exported: **yes** — Implements logic starting with: { const bareId = modelId.includes("/") ? modelId.split("/").pop()! : modelId; return BUNDLED_COST_TABLE.find(e => e.id === bareId) ?? BUNDLED_COST_… (missing: @param modelId, @returns)
- `src/resources/extensions/gsd/model-cost-table.ts:85` — `compareModelCost(modelIdA: string, modelIdB: string)` — exported: **yes** — Implements logic starting with: { const costA = lookupModelCost(modelIdA)?.inputPer1k ?? 999; const costB = lookupModelCost(modelIdB)?.inputPer1k ?? 999; return costA - costB; } (missing: @param modelIdA, @param modelIdB, @returns)

### `src/resources/extensions/gsd/model-router.ts`
- `src/resources/extensions/gsd/model-router.ts:237` — `scoreModel(model: ModelCapabilities, requirements: Partial<Record<keyof ModelCapabilities, number>>)` — exported: **yes** — Implements logic starting with: { let weightedSum = 0; let weightSum = 0; for (const [dim, weight] of Object.entries(requirements)) { const capability = model[dim as keyof ModelCa… (missing: @param model, @param requirements, @returns)
- `src/resources/extensions/gsd/model-router.ts:255` — `computeTaskRequirements(unitType: string, metadata?: TaskMetadata)` — exported: **yes** — Implements logic starting with: { const base = BASE_REQUIREMENTS[unitType] ?? { reasoning: 0.5 }; if (unitType === "execute-task" && metadata) { if (metadata.tags?.some(t => /^(do… (missing: @param unitType, @param metadata, @returns)
- `src/resources/extensions/gsd/model-router.ts:282` — `scoreEligibleModels(eligibleModelIds: string[], requirements: Partial<Record<keyof ModelCapabilities, number>>, capabilityOverrides?: Record<string, Partial<ModelCapabilities>>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param eligibleModelIds, @param requirements, @param capabilityOverrides, @returns)
- `src/resources/extensions/gsd/model-router.ts:312` — `getEligibleModels(tier: ComplexityTier, availableModelIds: string[], routingConfig: DynamicRoutingConfig)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tier, @param availableModelIds, @param routingConfig, @returns)
- `src/resources/extensions/gsd/model-router.ts:342` — `buildFallbackChain(selectedModelId: string, phaseConfig: ResolvedModelConfig)` — exported: **no** — Transforms a collection into a derived result. (missing: @param selectedModelId, @param phaseConfig, @returns)
- `src/resources/extensions/gsd/model-router.ts:355` — `loadCapabilityOverrides(prefs: { modelOverrides?: Record<string, { capabilities?: Partial<ModelCapabilities> }> })` — exported: **yes** — Implements logic starting with: { const result: Record<string, Partial<ModelCapabilities>> = {}; if (!prefs.modelOverrides) return result; for (const [modelId, overrideEntry] of O… (missing: @param prefs, @returns)
- `src/resources/extensions/gsd/model-router.ts:387` — `resolveModelForComplexity(classification: ClassificationResult, phaseConfig: ResolvedModelConfig | undefined, routingConfig: DynamicRoutingConfig, availableModelIds: string[], unitType?: string, taskMetadata?: TaskMetadata, capabilityOverrides?: Record<string, Partial<ModelCapabilities>>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/model-router.ts:523` — `escalateTier(currentTier: ComplexityTier)` — exported: **yes** — Implements logic starting with: { switch (currentTier) { case "light": return "standard"; case "standard": return "heavy"; case "heavy": return null; } } (missing: @param currentTier, @returns)
- `src/resources/extensions/gsd/model-router.ts:534` — `defaultRoutingConfig()` — exported: **yes** — Implements logic starting with: { return { enabled: true, capability_routing: true, escalate_on_failure: true, budget_pressure: true, cross_provider: true, hooks: true, }; } (missing: @returns)
- `src/resources/extensions/gsd/model-router.ts:578` — `findModelForTier(tier: ComplexityTier, routingConfig: DynamicRoutingConfig, availableModelIds: string[], crossProvider: boolean)` — exported: **no** — Transforms a collection into a derived result. (missing: @param tier, @param routingConfig, @param availableModelIds, @param crossProvider, @returns)
- `src/resources/extensions/gsd/model-router.ts:618` — `resolveModelForTier(tier: ComplexityTier, availableModelIds: string[], crossProvider?: boolean)` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/gsd/model-router.ts:664` — `isModelAvailable(modelId: string, availableModelIds: string[])` — exported: **no** — Implements logic starting with: { if (availableModelIds.includes(modelId)) return true; // Strip provider prefix for comparison. Treat trailing-slash IDs ("provider/") // as no-ba… (missing: @param modelId, @param availableModelIds, @returns)
- `src/resources/extensions/gsd/model-router.ts:691` — `isKnownModel(modelId: string)` — exported: **no** — Implements logic starting with: { const bareId = bareModelId(modelId); if (MODEL_CAPABILITY_TIER[bareId]) return true; for (const knownId of Object.keys(MODEL_CAPABILITY_TIER)) {… (missing: @param modelId, @returns)
- `src/resources/extensions/gsd/model-router.ts:760` — `isToolCompatibleWithProvider(toolName: string, providerCaps: ProviderCapabilities)` — exported: **yes** — Implements logic starting with: { const compat = getToolCompatibility(toolName); if (!compat) return true; // no metadata = always compatible // Hard filter: provider doesn't supp… (missing: @param toolName, @param providerCaps, @returns)
- `src/resources/extensions/gsd/model-router.ts:788` — `filterToolsForProvider(toolNames: string[], providerApi: string, provider?: string)` — exported: **yes** — Logs diagnostic information. (missing: @returns)
- `src/resources/extensions/gsd/model-router.ts:837` — `adjustToolSet(activeToolNames: string[], selectedModelApi: string, provider?: string)` — exported: **yes** — Implements logic starting with: { const { compatible, filtered } = filterToolsForProvider(activeToolNames, selectedModelApi, provider); return { toolNames: compatible, removedTool… (missing: @returns)

### `src/resources/extensions/gsd/namespaced-registry.ts`
- `src/resources/extensions/gsd/namespaced-registry.ts:159` — `class NamespacedRegistry` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/gsd/namespaced-resolver.ts`
- `src/resources/extensions/gsd/namespaced-resolver.ts:156` — `class NamespacedResolver` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/gsd/native-git-bridge.ts`
- `src/resources/extensions/gsd/native-git-bridge.ts:139` — `gitExec(basePath: string, args: string[], allowFailure = false)` — exported: **no** — Implements logic starting with: { try { return execFileSync("git", args, { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8", env: GIT_NO_PROMPT_ENV, }).trim();… (missing: @param basePath, @param args, @param allowFailure, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:154` — `gitFileExec(basePath: string, args: string[], allowFailure = false)` — exported: **no** — Implements logic starting with: { try { return execFileSync("git", args, { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8", env: GIT_NO_PROMPT_ENV, }).trim();… (missing: @param basePath, @param args, @param allowFailure, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:175` — `nativeGetCurrentBranch(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { const branch = native.gitCurrentBranch(basePath); return branch ?? ""; } return gitExec(basePath, ["br… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:189` — `nativeDetectMainBranch(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitMainBranch(basePath); } const symbolic = gitExec(basePath, ["symbolic-ref", "refs/rem… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:217` — `nativeBranchExists(basePath: string, branch: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitBranchExists(basePath, branch); } const result = gitExec(basePath, ["show-ref", "--ve… (missing: @param basePath, @param branch, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:236` — `nativeHasMergeConflicts(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitHasMergeConflicts(basePath); } const result = gitExec(basePath, ["diff", "--name-only… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:250` — `nativeWorkingTreeStatus(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitWorkingTreeStatus(basePath); } return gitExec(basePath, ["status", "--porcelain"], tr… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:269` — `nativeHasChanges(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitHasChanges(basePath); } const now = Date.now(); if ( basePath === _hasChangesCachedPa… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:305` — `nativeCommitCountBetween(basePath: string, fromRef: string, toRef: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitCommitCountBetween(basePath, fromRef, toRef); } const result = gitExec(basePath, ["re… (missing: @param basePath, @param fromRef, @param toRef, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:321` — `nativeIsRepo(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitIsRepo(basePath); } try { execFileSync("git", ["rev-parse", "--git-dir"], { cwd: base… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:335` — `nativeHasCommittedHead(basePath: string)` — exported: **yes** — Implements logic starting with: { try { execFileSync("git", ["rev-parse", "--verify", "HEAD"], { cwd: basePath, stdio: ["ignore", "ignore", "ignore"], env: GIT_NO_PROMPT_ENV, });… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:353` — `nativeHasStagedChanges(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitHasStagedChanges(basePath); } const result = gitExec(basePath, ["diff", "--cached", "… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:369` — `nativeDiffStat(basePath: string, fromRef: string, toRef: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitDiffStat(basePath, fromRef, toRef); } // Fallback let args: string[]; if (fromRef ===… (missing: @param basePath, @param fromRef, @param toRef, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:403` — `nativeDiffNameStatus(basePath: string, fromRef: string, toRef: string, pathspec?: string, useMergeBase?: boolean)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param fromRef, @param toRef, @param pathspec, @param useMergeBase, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:434` — `nativeDiffNumstat(basePath: string, fromRef: string, toRef: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param fromRef, @param toRef, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:459` — `nativeDiffContent(basePath: string, fromRef: string, toRef: string, pathspec?: string, exclude?: string, useMergeBase?: boolean)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitDiffContent(basePath, fromRef, toRef, pathspec, exclude, useMergeBase); } const separ… (missing: @param basePath, @param fromRef, @param toRef, @param pathspec, @param exclude, @param useMergeBase, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:488` — `nativeLogOneline(basePath: string, fromRef: string, toRef: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param fromRef, @param toRef, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:509` — `nativeWorktreeList(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:544` — `nativeBranchList(basePath: string, pattern?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param pattern, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:564` — `nativeBranchListMerged(basePath: string, target: string, pattern?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param target, @param pattern, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:584` — `nativeLsFiles(basePath: string, pathspec: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param pathspec, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:600` — `nativeForEachRef(basePath: string, prefix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param prefix, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:616` — `nativeConflictFiles(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:632` — `nativeBatchInfo(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitBatchInfo(basePath); } const branch = gitExec(basePath, ["branch", "--show-current"],… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:672` — `nativeInit(basePath: string, initialBranch?: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitInit(basePath, initialBranch); return; } const args = ["init"]; if (initialBranch) args.push… (missing: @param basePath, @param initialBranch)
- `src/resources/extensions/gsd/native-git-bridge.ts:689` — `nativeAddAll(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitAddAll(basePath); return; } gitFileExec(basePath, ["add", "-A"]); } (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:704` — `nativeAddTracked(basePath: string)` — exported: **yes** — Implements logic starting with: { gitFileExec(basePath, ["add", "-u"]); } (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:734` — `isGitignoreManagementDisabled(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:757` — `trySelfHealGsdGitignore(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:783` — `stageUntrackedExcludingDotGsd(basePath: string)` — exported: **no** — Implements logic starting with: { // Stage tracked modifications first. `git add -u` never fails on pathspec // issues because it doesn't walk untracked trees. gitFileExec(basePat… (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:824` — `fallbackStageWithSymlinkedDotGsd(basePath: string)` — exported: **no** — Implements logic starting with: { if (isDotGsdIgnored(basePath)) { gitFileExec(basePath, ["add", "-A"]); return; } if (trySelfHealGsdGitignore(basePath)) { gitFileExec(basePath, [… (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:850` — `nativeAddAllWithExclusions(basePath: string, exclusions: readonly string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param exclusions)
- `src/resources/extensions/gsd/native-git-bridge.ts:893` — `nativeAddPaths(basePath: string, paths: string[])` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitAddPaths(basePath, paths); return; } gitFileExec(basePath, ["add", "--", ...paths]); } (missing: @param basePath, @param paths)
- `src/resources/extensions/gsd/native-git-bridge.ts:907` — `nativeResetPaths(basePath: string, paths: string[])` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitResetPaths(basePath, paths); return; } for (const p of paths) { gitExec(basePath, ["reset",… (missing: @param basePath, @param paths)
- `src/resources/extensions/gsd/native-git-bridge.ts:927` — `nativeCommit(basePath: string, message: string, options?: { allowEmpty?: boolean; input?: string })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param message, @param options, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:961` — `nativeCheckoutBranch(basePath: string, branch: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitCheckoutBranch(basePath, branch); return; } execFileSync("git", ["checkout", branch], { cwd:… (missing: @param basePath, @param branch)
- `src/resources/extensions/gsd/native-git-bridge.ts:979` — `nativeCheckoutTheirs(basePath: string, paths: string[])` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitCheckoutTheirs(basePath, paths); return; } for (const path of paths) { gitFileExec(basePath,… (missing: @param basePath, @param paths)
- `src/resources/extensions/gsd/native-git-bridge.ts:995` — `nativeMergeSquash(basePath: string, branch: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param branch, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:1051` — `nativeMergeAbort(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitMergeAbort(basePath); return; } gitExec(basePath, ["merge", "--abort"], true); } (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:1065` — `nativeRebaseAbort(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitRebaseAbort(basePath); return; } gitExec(basePath, ["rebase", "--abort"], true); } (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:1079` — `nativeResetHard(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitResetHard(basePath); return; } execFileSync("git", ["reset", "--hard", "HEAD"], { cwd: baseP… (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:1093` — `nativeResetSoft(basePath: string, target: string)` — exported: **yes** — Implements logic starting with: { execFileSync("git", ["reset", "--soft", target], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8", env: GIT_NO_PROMPT_ENV, }… (missing: @param basePath, @param target)
- `src/resources/extensions/gsd/native-git-bridge.ts:1106` — `nativeCommitSubject(basePath: string, ref: string)` — exported: **yes** — Implements logic starting with: { try { return execFileSync("git", ["log", "-1", "--format=%s", ref], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8", env: G… (missing: @param basePath, @param ref, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:1124` — `nativeBranchDelete(basePath: string, branch: string, force = true)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitBranchDelete(basePath, branch, force); return; } gitFileExec(basePath, ["branch", force ? "-… (missing: @param basePath, @param branch, @param force)
- `src/resources/extensions/gsd/native-git-bridge.ts:1138` — `nativeBranchForceReset(basePath: string, branch: string, target: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitBranchForceReset(basePath, branch, target); return; } gitExec(basePath, ["branch", "-f", bra… (missing: @param basePath, @param branch, @param target)
- `src/resources/extensions/gsd/native-git-bridge.ts:1153` — `nativeRmCached(basePath: string, paths: string[], recursive = true)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { return native.gitRmCached(basePath, paths, recursive); } const removed: string[] = []; for (const path… (missing: @param basePath, @param paths, @param recursive, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:1176` — `nativeRmForce(basePath: string, paths: string[])` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitRmForce(basePath, paths); return; } for (const path of paths) { gitFileExec(basePath, ["rm",… (missing: @param basePath, @param paths)
- `src/resources/extensions/gsd/native-git-bridge.ts:1219` — `nativeWorktreeAdd(basePath: string, wtPath: string, branch: string, createBranch?: boolean, startPoint?: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitWorktreeAdd(basePath, wtPath, branch, createBranch, startPoint); try { assertWorktreeMateria… (missing: @param basePath, @param wtPath, @param branch, @param createBranch, @param startPoint)
- `src/resources/extensions/gsd/native-git-bridge.ts:1250` — `nativeWorktreeRemove(basePath: string, wtPath: string, force = false)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitWorktreeRemove(basePath, wtPath, force); return; } const args = ["worktree", "remove"]; if (… (missing: @param basePath, @param wtPath, @param force)
- `src/resources/extensions/gsd/native-git-bridge.ts:1268` — `nativeWorktreePrune(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitWorktreePrune(basePath); return; } gitExec(basePath, ["worktree", "prune"], true); } (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:1282` — `nativeRevertCommit(basePath: string, sha: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitRevertCommit(basePath, sha); return; } gitFileExec(basePath, ["revert", "--no-commit", sha])… (missing: @param basePath, @param sha)
- `src/resources/extensions/gsd/native-git-bridge.ts:1296` — `nativeRevertAbort(basePath: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitRevertAbort(basePath); return; } gitFileExec(basePath, ["revert", "--abort"], true); } (missing: @param basePath)
- `src/resources/extensions/gsd/native-git-bridge.ts:1311` — `nativeUpdateRef(basePath: string, refname: string, target?: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (native) { native.gitUpdateRef(basePath, refname, target); return; } if (target !== undefined) { gitExec(basePath… (missing: @param basePath, @param refname, @param target)
- `src/resources/extensions/gsd/native-git-bridge.ts:1328` — `isNativeGitAvailable()` — exported: **yes** — Implements logic starting with: { return loadNative() !== null; } (missing: @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:1337` — `nativeIsAncestor(basePath: string, ancestor: string, descendant: string)` — exported: **yes** — Implements logic starting with: { try { execFileSync("git", ["merge-base", "--is-ancestor", ancestor, descendant], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], env: GIT_NO_… (missing: @param basePath, @param ancestor, @param descendant, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:1355` — `nativeLastCommitEpoch(basePath: string, ref: string)` — exported: **yes** — Implements logic starting with: { try { const result = execFileSync("git", ["log", "-1", "--format=%ct", ref], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8… (missing: @param basePath, @param ref, @returns)
- `src/resources/extensions/gsd/native-git-bridge.ts:1374` — `nativeUnpushedCount(basePath: string, branch: string)` — exported: **yes** — Implements logic starting with: { try { const result = execFileSync("git", ["rev-list", branch, "--not", "--remotes", "--count"], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"… (missing: @param basePath, @param branch, @returns)

### `src/resources/extensions/gsd/native-parser-bridge.ts`
- `src/resources/extensions/gsd/native-parser-bridge.ts:56` — `nativeSplitFrontmatter(content: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param content, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:75` — `nativeExtractSection(content: string, heading: string, level: number = 2)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (!native) return NATIVE_UNAVAILABLE; const result = native.extractSection(content, heading, level); return result… (missing: @param content, @param heading, @param level, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:89` — `nativeParseRoadmap(content: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:127` — `nativeBatchParseGsdFiles(directory: string)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param directory, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:144` — `isNativeParserAvailable()` — exported: **yes** — Implements logic starting with: { return loadNative() !== null; } (missing: @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:160` — `nativeScanGsdTree(directory: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (!native) return null; return native.scanGsdTree(directory); } (missing: @param directory, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:179` — `nativeParseJsonlTail(filePath: string, maxBytes?: number, maxEntries?: number)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param filePath, @param maxBytes, @param maxEntries, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:216` — `nativeParsePlanFile(content: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (!native) return null; return native.parsePlanFile(content) as NativePlanResult; } (missing: @param content, @returns)
- `src/resources/extensions/gsd/native-parser-bridge.ts:263` — `nativeParseSummaryFile(content: string)` — exported: **yes** — Implements logic starting with: { const native = loadNative(); if (!native) return null; return native.parseSummaryFile(content) as NativeSummaryResult; } (missing: @param content, @returns)

### `src/resources/extensions/gsd/notification-store.ts`
- `src/resources/extensions/gsd/notification-store.ts:46` — `initNotificationStore(basePath: string)` — exported: **yes** — Implements logic starting with: { if (_basePath !== basePath) { _recentMessageTimestamps.clear(); } _basePath = basePath; // Seed line count hint for rotation — public counters re… (missing: @param basePath)
- `src/resources/extensions/gsd/notification-store.ts:59` — `appendNotification(message: string, severity: NotifySeverity, source: NotificationSource = "notify")` — exported: **yes** — Serializes a value to JSON. (missing: @param message, @param severity, @param source)
- `src/resources/extensions/gsd/notification-store.ts:106` — `readNotifications(basePath?: string)` — exported: **yes** — Implements logic starting with: { const bp = basePath ?? _basePath; if (!bp) return []; return _readEntriesFromDisk(bp).reverse(); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/notification-store.ts:116` — `markAllRead(basePath?: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath)
- `src/resources/extensions/gsd/notification-store.ts:145` — `clearNotifications(basePath?: string)` — exported: **yes** — Implements logic starting with: { const bp = basePath ?? _basePath; if (!bp) return; try { _withLock(bp, () => { _atomicWrite(bp, ""); }); _lineCount = 0; _emitChange(); } catch {… (missing: @param basePath)
- `src/resources/extensions/gsd/notification-store.ts:164` — `getUnreadCount()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/notification-store.ts:177` — `getLineCount()` — exported: **yes** — Implements logic starting with: { if (!_basePath) return 0; try { return _readEntriesFromDisk(_basePath).length; } catch { return 0; } } (missing: @returns)
- `src/resources/extensions/gsd/notification-store.ts:203` — `onNotificationStoreChange(listener: () => void)` — exported: **yes** — Implements logic starting with: { _changeListeners.add(listener); return () => { _changeListeners.delete(listener); }; } (missing: @param listener, @returns)
- `src/resources/extensions/gsd/notification-store.ts:278` — `_atomicWrite(basePath: string, content: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param content)
- `src/resources/extensions/gsd/notification-store.ts:293` — `_withLock(basePath: string, fn: () => T)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param fn, @returns)

### `src/resources/extensions/gsd/notifications.ts`
- `src/resources/extensions/gsd/notifications.ts:21` — `sendDesktopNotification(title: string, message: string, level: NotifyLevel = "info", kind: NotificationKind = "complete", projectName?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param title, @param message, @param level, @param kind, @param projectName)
- `src/resources/extensions/gsd/notifications.ts:95` — `formatNotificationTitle(projectName?: string)` — exported: **yes** — Implements logic starting with: { const trimmed = projectName?.trim(); if (trimmed) return `GSD — ${trimmed}`; return "GSD"; } (missing: @param projectName, @returns)
- `src/resources/extensions/gsd/notifications.ts:147` — `findExecutable(name: string)` — exported: **no** — Implements logic starting with: { try { return execFileSync("which", [name], { timeout: 2000, stdio: ["ignore", "pipe", "ignore"] }).toString().trim() || null; } catch { return nu… (missing: @param name, @returns)

### `src/resources/extensions/gsd/onboarding-state.ts`
- `src/resources/extensions/gsd/onboarding-state.ts:104` — `isOnboardingComplete()` — exported: **yes** — Implements logic starting with: { const r = readOnboardingRecord() return r.completedAt !== null && r.flowVersion === FLOW_VERSION } (missing: @returns)

### `src/resources/extensions/gsd/orphan-stash-audit.ts`
- `src/resources/extensions/gsd/orphan-stash-audit.ts:25` — `_isAlreadyRestoredApplyError(err: unknown)` — exported: **yes** — Implements logic starting with: { if (!err || typeof err !== "object") return false; const stderr = (err as { stderr?: unknown }).stderr; const stderrText = typeof stderr === "str… (missing: @param err, @returns)
- `src/resources/extensions/gsd/orphan-stash-audit.ts:51` — `auditOrphanedPreflightStashes(basePath: string, isMilestoneComplete: (milestoneId: string) => boolean)` — exported: **yes** — Executes a subprocess command. (missing: @param basePath, @param isMilestoneComplete, @returns)

### `src/resources/extensions/gsd/parallel-eligibility.ts`
- `src/resources/extensions/gsd/parallel-eligibility.ts:35` — `collectTouchedFiles(basePath: string, milestoneId: string)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/parallel-eligibility.ts:65` — `detectFileOverlaps(fileSets: Map<string, string[]>)` — exported: **no** — Transforms a collection into a derived result. (missing: @param fileSets, @returns)
- `src/resources/extensions/gsd/parallel-eligibility.ts:96` — `analyzeParallelEligibility(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/parallel-eligibility.ts:196` — `formatEligibilityReport(candidates: ParallelCandidates)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push("# Parallel Eligibility Report"); lines.push(""); // Eligible milestones lines.push(`## Eligible for Paral… (missing: @param candidates, @returns)

### `src/resources/extensions/gsd/parallel-merge.ts`
- `src/resources/extensions/gsd/parallel-merge.ts:44` — `isMilestoneCompleteInProjectDb(basePath: string, mid: string)` — exported: **yes** — Executes a subprocess command. (missing: @param basePath, @param mid, @returns)
- `src/resources/extensions/gsd/parallel-merge.ts:66` — `discoverDbCompletedMilestones(basePath: string)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/parallel-merge.ts:91` — `determineMergeOrder(workers: WorkerInfo[], order: MergeOrder = "sequential", basePath?: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param workers, @param order, @param basePath, @returns)
- `src/resources/extensions/gsd/parallel-merge.ts:149` — `mergeCompletedMilestone(basePath: string, milestoneId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/parallel-merge.ts:225` — `mergeAllCompleted(basePath: string, workers: WorkerInfo[], order: MergeOrder = "sequential")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param workers, @param order, @returns)
- `src/resources/extensions/gsd/parallel-merge.ts:249` — `formatMergeResults(results: MergeResult[])` — exported: **yes** — Implements logic starting with: { if (results.length === 0) return "No completed milestones to merge."; const lines: string[] = ["# Merge Results\n"]; for (const r of results) { i… (missing: @param results, @returns)

### `src/resources/extensions/gsd/parallel-orchestrator.ts`
- `src/resources/extensions/gsd/parallel-orchestrator.ts:107` — `persistState(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:142` — `removeStateFile(basePath: string)` — exported: **no** — Implements logic starting with: { try { const p = stateFilePath(basePath); if (existsSync(p)) unlinkSync(p); } catch (e) { logWarning("parallel", `clear parallel state file failed… (missing: @param basePath)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:167` — `restoreState(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:312` — `isParallelActive()` — exported: **yes** — Implements logic starting with: { return state?.active ?? false; } (missing: @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:317` — `getOrchestratorState()` — exported: **yes** — Implements logic starting with: { return state; } (missing: @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:322` — `getWorkerStatuses(basePath?: string)` — exported: **yes** — Implements logic starting with: { if (basePath) { refreshWorkerStatuses(basePath, { restoreIfNeeded: true }); } if (!state) return []; return [...state.workers.values()]; } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:337` — `prepareParallelStart(basePath: string, _prefs: GSDPreferences | undefined)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param _prefs, @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:363` — `startParallel(basePath: string, milestoneIds: string[], prefs: GSDPreferences | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param milestoneIds, @param prefs, @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:539` — `_createMilestoneWorktree(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const branch = autoWorktreeBranch(milestoneId); const branchExists = nativeBranchExists(basePath, branch); let info: { name: string; path: string… (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:587` — `spawnWorker(basePath: string, milestoneId: string)` — exported: **yes** — Executes a subprocess command. (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:745` — `resolveGsdBin()` — exported: **no** — Implements logic starting with: { // GSD_BIN_PATH is set by loader.ts to the absolute path of dist/loader.js if (process.env.GSD_BIN_PATH && existsSync(process.env.GSD_BIN_PATH))… (missing: @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:779` — `processWorkerLine(basePath: string, milestoneId: string, line: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param basePath, @param milestoneId, @param line)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:854` — `stopParallel(basePath: string, milestoneId?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:931` — `pauseWorker(basePath: string, milestoneId?: string)` — exported: **yes** — Implements logic starting with: { if (!state) return; const targets = milestoneId ? [milestoneId] : [...state.workers.keys()]; for (const mid of targets) { const worker = state.wo… (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:951` — `resumeWorker(basePath: string, milestoneId?: string)` — exported: **yes** — Implements logic starting with: { if (!state) return; const targets = milestoneId ? [milestoneId] : [...state.workers.keys()]; for (const mid of targets) { const worker = state.wo… (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:976` — `refreshWorkerStatuses(basePath: string, options: { restoreIfNeeded?: boolean } = {})` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param basePath, @param options)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:1047` — `getAggregateCost()` — exported: **yes** — Implements logic starting with: { if (!state) return 0; return state.totalCost; } (missing: @returns)
- `src/resources/extensions/gsd/parallel-orchestrator.ts:1053` — `isBudgetExceeded()` — exported: **yes** — Implements logic starting with: { if (!state) return false; if (state.config.budget_ceiling == null) return false; return state.totalCost >= state.config.budget_ceiling; } (missing: @returns)

### `src/resources/extensions/gsd/parsers-legacy.ts`
- `src/resources/extensions/gsd/parsers-legacy.ts:29` — `cacheKey(content: string)` — exported: **no** — Implements logic starting with: { const len = content.length; const head = content.slice(0, 100); const midStart = Math.max(0, Math.floor(len / 2) - 50); const mid = len > 200 ? c… (missing: @param content, @returns)

### `src/resources/extensions/gsd/paths.ts`
- `src/resources/extensions/gsd/paths.ts:59` — `nativeTreeKey(dirPath: string, gsdDir: string)` — exported: **no** — Implements logic starting with: { if (!dirPath.startsWith(gsdDir)) return null; const rel = dirPath.slice(gsdDir.length).replace(/^\//, ''); return rel || '.'; } (missing: @param dirPath, @param gsdDir, @returns)
- `src/resources/extensions/gsd/paths.ts:155` — `buildMilestoneFileName(milestoneId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { return `${milestoneId}-${suffix}.md`; } (missing: @param milestoneId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:163` — `buildSliceFileName(sliceId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { return `${sliceId}-${suffix}.md`; } (missing: @param sliceId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:172` — `buildTaskFileName(taskId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { return `${taskId}-${suffix}.md`; } (missing: @param taskId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:184` — `resolveDir(parentDir: string, idPrefix: string)` — exported: **yes** — Implements logic starting with: { if (!existsSync(parentDir)) return null; try { const entries = cachedReaddirWithTypes(parentDir); // Exact match first (current convention: bare… (missing: @param parentDir, @param idPrefix, @returns)
- `src/resources/extensions/gsd/paths.ts:213` — `resolveFile(dir: string, idPrefix: string, suffix: string)` — exported: **yes** — Implements logic starting with: { if (!existsSync(dir)) return null; const target = `${idPrefix}-${suffix}.md`.toUpperCase(); try { const entries = cachedReaddir(dir); // Direct m… (missing: @param dir, @param idPrefix, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:240` — `resolveTaskFiles(tasksDir: string, suffix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tasksDir, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:259` — `resolveTaskJsonFiles(tasksDir: string, suffix: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tasksDir, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:380` — `normalizeRealPath(p: string)` — exported: **yes** — Implements logic starting with: { try { return realpathSync.native(p); } catch { return resolve(p); } } (missing: @param p, @returns)
- `src/resources/extensions/gsd/paths.ts:385` — `normCacheKey(p: string)` — exported: **no** — Implements logic starting with: { const r = normalizeRealPath(p); const s = r.replaceAll("\\", "/").replace(/\/+$/, ""); return process.platform === "win32" ? s.toLowerCase() : s;… (missing: @param p, @returns)
- `src/resources/extensions/gsd/paths.ts:403` — `gsdRoot(basePath: string)` — exported: **yes** — Implements logic starting with: { const cacheKey = normCacheKey(basePath); const cached = gsdRootCache.get(cacheKey); if (cached) return cached; // Canonicalize result via realpat… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/paths.ts:461` — `isInsideGsdWorktree(p: string)` — exported: **no** — Implements logic starting with: { // Match /.gsd/worktrees/<name> where <name> is the final segment or // followed by a separator. The <name> segment must be non-empty. const sepF… (missing: @param p, @returns)
- `src/resources/extensions/gsd/paths.ts:578` — `resolveMilestonePath(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const dir = resolveDir(milestonesDir(basePath), milestoneId); return dir ? join(milestonesDir(basePath), dir) : null; } (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/paths.ts:586` — `resolveMilestoneFile(basePath: string, milestoneId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const mDir = resolveMilestonePath(basePath, milestoneId); if (!mDir) return null; const file = resolveFile(mDir, milestoneId, suffix); return fil… (missing: @param basePath, @param milestoneId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:598` — `resolveSlicePath(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const mDir = resolveMilestonePath(basePath, milestoneId); if (!mDir) return null; const slicesDir = join(mDir, "slices"); const dir = resolveDir(… (missing: @param basePath, @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/paths.ts:611` — `resolveSliceFile(basePath: string, milestoneId: string, sliceId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const sDir = resolveSlicePath(basePath, milestoneId, sliceId); if (!sDir) return null; const file = resolveFile(sDir, sliceId, suffix); return fi… (missing: @param basePath, @param milestoneId, @param sliceId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:623` — `resolveTasksDir(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const sDir = resolveSlicePath(basePath, milestoneId, sliceId); if (!sDir) return null; const tDir = join(sDir, "tasks"); return existsSync(tDir)… (missing: @param basePath, @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/paths.ts:635` — `resolveTaskFile(basePath: string, milestoneId: string, sliceId: string, taskId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const tDir = resolveTasksDir(basePath, milestoneId, sliceId); if (!tDir) return null; const file = resolveFile(tDir, taskId, suffix); return file… (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:651` — `relMilestonePath(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const dir = resolveDir(milestonesDir(basePath), milestoneId); if (dir) return `.gsd/milestones/${dir}`; return `.gsd/milestones/${milestoneId}`; } (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/paths.ts:660` — `relMilestoneFile(basePath: string, milestoneId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const mRel = relMilestonePath(basePath, milestoneId); const mDir = resolveMilestonePath(basePath, milestoneId); if (mDir) { const file = resolveF… (missing: @param basePath, @param milestoneId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:675` — `relSlicePath(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const mRel = relMilestonePath(basePath, milestoneId); const mDir = resolveMilestonePath(basePath, milestoneId); if (mDir) { const slicesDir = joi… (missing: @param basePath, @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/paths.ts:691` — `relSliceFile(basePath: string, milestoneId: string, sliceId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const sRel = relSlicePath(basePath, milestoneId, sliceId); const sDir = resolveSlicePath(basePath, milestoneId, sliceId); if (sDir) { const file… (missing: @param basePath, @param milestoneId, @param sliceId, @param suffix, @returns)
- `src/resources/extensions/gsd/paths.ts:706` — `relTaskFile(basePath: string, milestoneId: string, sliceId: string, taskId: string, suffix: string)` — exported: **yes** — Implements logic starting with: { const sRel = relSlicePath(basePath, milestoneId, sliceId); const tDir = resolveTasksDir(basePath, milestoneId, sliceId); if (tDir) { const file =… (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId, @param suffix, @returns)

### `src/resources/extensions/gsd/planning-depth.ts`
- `src/resources/extensions/gsd/planning-depth.ts:26` — `setPlanningDepth(basePath: string, depth: "light" | "deep")` — exported: **yes** — Implements logic starting with: { const path = getProjectGSDPreferencesPath(basePath); const { frontmatter, body } = readProjectPreferencesParts(path); frontmatter.planning_depth… (missing: @param basePath, @param depth)

### `src/resources/extensions/gsd/planning-path-scope.ts`
- `src/resources/extensions/gsd/planning-path-scope.ts:21` — `validatePlanningPathScope(basePath: string, fields: PlanningPathScopeField[])` — exported: **yes** — Implements logic starting with: { for (const { field, values } of fields) { for (const raw of values) { const candidate = normalizePlannedFileReference(raw); if (!isAbsolute(candi… (missing: @param basePath, @param fields, @returns)

### `src/resources/extensions/gsd/plugin-importer.ts`
- `src/resources/extensions/gsd/plugin-importer.ts:167` — `class PluginImporter` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/gsd/post-execution-checks.ts`
- `src/resources/extensions/gsd/post-execution-checks.ts:53` — `extractRelativeImports(source: string)` — exported: **yes** — Executes a subprocess command. (missing: @param source, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:131` — `resolveImportPath(importPath: string, sourceFile: string, basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param importPath, @param sourceFile, @param basePath, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:215` — `checkImportResolution(taskRow: TaskRow, _priorTasks: TaskRow[], basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param taskRow, @param _priorTasks, @param basePath, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:292` — `extractFunctionSignatures(source: string, fileName: string)` — exported: **no** — Executes a subprocess command. (missing: @param source, @param fileName, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:332` — `normalizeParams(params: string)` — exported: **no** — Implements logic starting with: { return params .replace(/\/\*[\s\S]*?\*\//g, "") // Remove block comments .replace(/\/\/[^\n]*/g, "") // Remove line comments .replace(/\s*=\s*[^,… (missing: @param params, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:344` — `normalizeType(type: string)` — exported: **no** — Implements logic starting with: { return type.replace(/\s+/g, " ").trim(); } (missing: @param type, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:353` — `checkCrossTaskSignatures(taskRow: TaskRow, priorTasks: TaskRow[], basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param taskRow, @param priorTasks, @param basePath, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:444` — `checkPatternConsistency(taskRow: TaskRow, _priorTasks: TaskRow[], basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param taskRow, @param _priorTasks, @param basePath, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:482` — `checkAsyncStyleDrift(source: string, fileName: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param source, @param fileName, @returns)
- `src/resources/extensions/gsd/post-execution-checks.ts:511` — `checkNamingConsistency(source: string, fileName: string)` — exported: **no** — Executes a subprocess command. (missing: @param source, @param fileName, @returns)

### `src/resources/extensions/gsd/pre-execution-checks.ts`
- `src/resources/extensions/gsd/pre-execution-checks.ts:49` — `extractPackageReferences(description: string)` — exported: **yes** — Executes a subprocess command. (missing: @param description, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:120` — `normalizePackageName(raw: string)` — exported: **no** — Implements logic starting with: { // Scoped package: @org/pkg or @org/pkg/subpath if (raw.startsWith("@")) { const parts = raw.split("/"); return parts.length >= 2 ? `${parts[0]}/… (missing: @param raw, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:135` — `checkPackageOnNpm(packageName: string, timeoutMs = 5000)` — exported: **no** — Executes a subprocess command. (missing: @param packageName, @param timeoutMs, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:187` — `checkPackageExistence(tasks: TaskRow[], _basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tasks, @param _basePath, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:250` — `normalizeFilePath(filePath: string)` — exported: **yes** — Implements logic starting with: { if (!filePath) return filePath; let normalized = extractPathFromAnnotation(filePath); // Normalize path separators to forward slashes normalized… (missing: @param filePath, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:290` — `isDirectoryReference(raw: string)` — exported: **no** — Implements logic starting with: { const candidate = extractPathFromAnnotation(raw.trim()); if (!candidate) return false; if (containsGlobPattern(candidate)) return false; return c… (missing: @param raw, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:301` — `anyOutputUnderDirectory(normalizedDir: string, knownOutputs: Iterable<string>)` — exported: **no** — Implements logic starting with: { const prefix = normalizedDir + "/"; for (const output of knownOutputs) { if (output === normalizedDir) return true; if (output.startsWith(prefix)… (missing: @param normalizedDir, @param knownOutputs, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:379` — `shouldValidateInputAsPath(raw: string)` — exported: **no** — Implements logic starting with: { const trimmed = raw.trim(); if (!trimmed) return false; if (isRuntimeOnlyInput(trimmed)) return false; const candidate = extractPathFromAnnotatio… (missing: @param raw, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:424` — `getExpectedOutputsUpTo(tasks: TaskRow[], taskIndex: number)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param tasks, @param taskIndex, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:451` — `checkFilePathConsistency(tasks: TaskRow[], basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tasks, @param basePath, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:528` — `checkTaskOrdering(tasks: TaskRow[], basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param tasks, @param basePath, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:606` — `extractFunctionSignatures(description: string, taskId: string)` — exported: **no** — Executes a subprocess command. (missing: @param description, @param taskId, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:661` — `normalizeParams(params: string)` — exported: **no** — Implements logic starting with: { return params .replace(/\/\*[\s\S]*?\*\//g, "") // Remove block comments .replace(/\/\/[^\n]*/g, "") // Remove line comments .replace(/\s*=\s*[^,… (missing: @param params, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:673` — `normalizeType(type: string)` — exported: **no** — Implements logic starting with: { return type .replace(/\s+/g, " ") .trim(); } (missing: @param type, @returns)
- `src/resources/extensions/gsd/pre-execution-checks.ts:683` — `checkInterfaceContracts(tasks: TaskRow[], _basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param tasks, @param _basePath, @returns)

### `src/resources/extensions/gsd/preferences-models.ts`
- `src/resources/extensions/gsd/preferences-models.ts:34` — `resolveModelForUnit(unitType: string)` — exported: **yes** — Implements logic starting with: { const resolved = resolveModelWithFallbacksForUnit(unitType); return resolved?.primary; } (missing: @param unitType, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:47` — `resolveModelWithFallbacksForUnit(unitType: string)` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(undefined, { availableModelIds: [] }); if (!prefs?.preferences.models) return undefined; const m = pref… (missing: @param unitType, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:150` — `resolveDefaultSessionModel(sessionProvider?: string)` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(undefined, { availableModelIds: [] }); if (!prefs?.preferences.models) return undefined; const m = pref… (missing: @param sessionProvider, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:227` — `isCustomProvider(provider: string | undefined)` — exported: **yes** — Performs filesystem I/O. (missing: @param provider, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:253` — `getNextFallbackModel(currentModelId: string | undefined, modelConfig: ResolvedModelConfig)` — exported: **yes** — Implements logic starting with: { const modelsToTry = [modelConfig.primary, ...modelConfig.fallbacks]; if (!currentModelId) { return modelsToTry[0]; } let foundCurrent = false; fo… (missing: @param currentModelId, @param modelConfig, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:284` — `isTransientNetworkError(errorMsg: string)` — exported: **yes** — Implements logic starting with: { if (!errorMsg) return false; const hasNetworkSignal = /network|ECONNRESET|ETIMEDOUT|ECONNREFUSED|socket hang up|fetch failed|connection.*reset|dn… (missing: @param errorMsg, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:295` — `validateModelId(modelId: string)` — exported: **yes** — Implements logic starting with: { if (!modelId || typeof modelId !== "string") return false; const trimmed = modelId.trim(); if (trimmed.length === 0 || trimmed.length > 256) retu… (missing: @param modelId, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:308` — `updatePreferencesModels(models: GSDModelConfigV2)` — exported: **yes** — Performs filesystem I/O. (missing: @param models)
- `src/resources/extensions/gsd/preferences-models.ts:353` — `resolveDynamicRoutingConfig()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(); const configured = prefs?.preferences.dynamic_routing; if (!configured) return defaultRoutingConfig(… (missing: @returns)
- `src/resources/extensions/gsd/preferences-models.ts:431` — `resolveProfileDefaults(profile: TokenProfile, availableModelIds?: string[], routingConfig: DynamicRoutingConfig = defaultRoutingConfig())` — exported: **yes** — Implements logic starting with: { // burn-max never writes model defaults — preserve user-selected models. // For the other three profiles, derive concrete model IDs from the tier… (missing: @returns)
- `src/resources/extensions/gsd/preferences-models.ts:507` — `getProfileTierMap(profile: TokenProfile)` — exported: **yes** — Implements logic starting with: { return { ...PROFILE_TIER_MAP[profile] }; } (missing: @param profile, @returns)
- `src/resources/extensions/gsd/preferences-models.ts:515` — `resolveEffectiveProfile()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(); const profile = prefs?.preferences.token_profile; if (profile && VALID_TOKEN_PROFILES.has(profile))… (missing: @returns)
- `src/resources/extensions/gsd/preferences-models.ts:526` — `resolveInlineLevel()` — exported: **yes** — Implements logic starting with: { const profile = resolveEffectiveProfile(); switch (profile) { case "budget": return "minimal"; case "balanced": return "standard"; case "quality"… (missing: @returns)
- `src/resources/extensions/gsd/preferences-models.ts:541` — `resolveContextSelection()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(); if (prefs?.preferences.context_selection) return prefs.preferences.context_selection; const profile… (missing: @returns)
- `src/resources/extensions/gsd/preferences-models.ts:552` — `resolveSearchProviderFromPreferences()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(); return prefs?.preferences.search_provider; } (missing: @returns)
- `src/resources/extensions/gsd/preferences-models.ts:561` — `resolveDisabledModelProvidersFromPreferences()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `src/resources/extensions/gsd/preferences-skills.ts`
- `src/resources/extensions/gsd/preferences-skills.ts:32` — `getSkillSearchDirs(cwd: string)` — exported: **yes** — Implements logic starting with: { const dirs: Array<{ dir: string; method: SkillResolution["method"] }> = [ { dir: join(homedir(), ".agents", "skills"), method: "user-skill" }, {… (missing: @param cwd, @returns)
- `src/resources/extensions/gsd/preferences-skills.ts:56` — `resolveSkillReference(ref: string, cwd: string)` — exported: **yes** — Implements logic starting with: { const trimmed = ref.trim(); // Expand tilde const expanded = trimmed.startsWith("~/") ? join(homedir(), trimmed.slice(2)) : trimmed; // Absolute… (missing: @param ref, @param cwd, @returns)
- `src/resources/extensions/gsd/preferences-skills.ts:113` — `resolveAllSkillReferences(preferences: GSDPreferences, cwd: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param preferences, @param cwd, @returns)

### `src/resources/extensions/gsd/preferences-types.ts`
- `src/resources/extensions/gsd/preferences-types.ts:59` — `isContextModeEnabled(prefs: { context_mode?: ContextModeConfig } | null | undefined)` — exported: **yes** — Implements logic starting with: { return prefs?.context_mode?.enabled !== false; } (missing: @param prefs, @returns)
- `src/resources/extensions/gsd/preferences-types.ts:533` — `formatSkillRef(ref: string, resolutions: Map<string, SkillResolution>)` — exported: **yes** — Implements logic starting with: { const resolution = resolutions.get(ref); if (!resolution || resolution.method === "unresolved") { return `${ref} (⚠ not found — check skill name… (missing: @param ref, @param resolutions, @returns)

### `src/resources/extensions/gsd/preferences.ts`
- `src/resources/extensions/gsd/preferences.ts:306` — `parseHeadingListFormat(content: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/preferences.ts:368` — `applyModeDefaults(mode: WorkflowMode, prefs: GSDPreferences)` — exported: **yes** — Implements logic starting with: { const defaults = MODE_DEFAULTS[mode]; if (!defaults) return prefs; return mergePreferences(defaults, prefs); } (missing: @param mode, @param prefs, @returns)
- `src/resources/extensions/gsd/preferences.ts:612` — `resolvePostUnitHooks()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/preferences.ts:622` — `resolvePreDispatchHooks()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/preferences.ts:638` — `getIsolationMode(basePath?: string)` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences(basePath)?.preferences?.git; if (prefs?.isolation === "worktree") { if (basePath && nativeIsRepo(basePa… (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/preparation.ts`
- `src/resources/extensions/gsd/preparation.ts:718` — `determineAsyncStyle(counts: { asyncAwait: number; callbacks: number; promises: number; })` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param counts, @returns)
- `src/resources/extensions/gsd/preparation.ts:741` — `determineErrorHandling(counts: { tryCatch: number; errorCallbacks: number; resultTypes: number; })` — exported: **no** — Implements logic starting with: { const total = counts.tryCatch + counts.errorCallbacks + counts.resultTypes; if (total === 0) return "unknown"; const tryCatchRatio = counts.tryCa… (missing: @param counts, @returns)
- `src/resources/extensions/gsd/preparation.ts:763` — `determineNamingConvention(counts: { camelCase: number; snakeCase: number; pascalCase: number; })` — exported: **no** — Implements logic starting with: { const total = counts.camelCase + counts.snakeCase + counts.pascalCase; if (total === 0) return "unknown"; // PascalCase is usually for types/clas… (missing: @param counts, @returns)
- `src/resources/extensions/gsd/preparation.ts:908` — `parseDecisions(content: string | null)` — exported: **no** — Transforms a collection into a derived result. (missing: @param content, @returns)
- `src/resources/extensions/gsd/preparation.ts:960` — `parseRequirements(content: string | null)` — exported: **no** — Implements logic starting with: { const result: PriorContextBrief["requirements"] = { active: [], validated: [], deferred: [], totalCount: 0, }; if (!content) { return result; } /… (missing: @param content, @returns)
- `src/resources/extensions/gsd/preparation.ts:1009` — `loadMilestoneSummaries(gsdPath: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param gsdPath, @returns)
- `src/resources/extensions/gsd/preparation.ts:1045` — `extractOneLiner(content: string)` — exported: **no** — Implements logic starting with: { const lines = content.split("\n"); for (const line of lines) { const trimmed = line.trim(); // Look for **bold text** that's the whole line if (t… (missing: @param content, @returns)
- `src/resources/extensions/gsd/preparation.ts:1069` — `truncateSection(content: string, maxChars: number)` — exported: **no** — Implements logic starting with: { if (content.length <= maxChars) { return content; } const SECTION_SUFFIX = "\n\n[truncated]"; // 14 chars const WORD_SUFFIX = "... [truncated]";… (missing: @param content, @param maxChars, @returns)

### `src/resources/extensions/gsd/progress-score.ts`
- `src/resources/extensions/gsd/progress-score.ts:51` — `computeProgressScore()` — exported: **yes** — Implements logic starting with: { const signals: ProgressSignal[] = []; let level: ProgressLevel = "green"; // Check consecutive errors const consecutiveErrors = getConsecutiveErr… (missing: @returns)
- `src/resources/extensions/gsd/progress-score.ts:115` — `computeProgressScoreWithContext(context: { sameUnitCount?: number; recoveryCount?: number; completedCount?: number; })` — exported: **yes** — Implements logic starting with: { const base = computeProgressScore(); if (context.sameUnitCount && context.sameUnitCount >= 3) { base.signals.push({ kind: "negative", label: `Sam… (missing: @param context, @returns)
- `src/resources/extensions/gsd/progress-score.ts:146` — `formatProgressLine(score: ProgressScore)` — exported: **yes** — Implements logic starting with: { const icon = score.level === "green" ? "●" : score.level === "yellow" ? "◐" : "○"; return `${icon} ${score.summary}`; } (missing: @param score, @returns)
- `src/resources/extensions/gsd/progress-score.ts:154` — `formatProgressReport(score: ProgressScore)` — exported: **yes** — Implements logic starting with: { const lines = [formatProgressLine(score)]; for (const signal of score.signals) { const prefix = signal.kind === "positive" ? " ✓" : signal.kind =… (missing: @param score, @returns)

### `src/resources/extensions/gsd/prompt-cache-optimizer.ts`
- `src/resources/extensions/gsd/prompt-cache-optimizer.ts:80` — `classifySection(label: string)` — exported: **yes** — Implements logic starting with: { if (STATIC_LABELS.has(label)) return "static"; if (STATIC_PREFIXES.some((p) => label.startsWith(p))) return "static"; if (SEMI_STATIC_LABELS.has(… (missing: @param label, @returns)
- `src/resources/extensions/gsd/prompt-cache-optimizer.ts:96` — `section(label: string, content: string, role?: ContentRole)` — exported: **yes** — Implements logic starting with: { return { label, content, role: role ?? classifySection(label), }; } (missing: @returns)
- `src/resources/extensions/gsd/prompt-cache-optimizer.ts:209` — `computeCacheHitRate(usage: { cacheRead: number; cacheWrite: number; input: number; })` — exported: **yes** — Implements logic starting with: { const denominator = usage.cacheRead + usage.input; if (denominator === 0) return 0; return (usage.cacheRead / denominator) * 100; } (missing: @param usage, @returns)

### `src/resources/extensions/gsd/prompt-loader.ts`
- `src/resources/extensions/gsd/prompt-loader.ts:65` — `resolveExtensionDir()` — exported: **no** — Implements logic starting with: { const moduleDir = dirname(fileURLToPath(import.meta.url)); const agentGsdDir = join(gsdHome(), "agent", "extensions", "gsd"); return resolveExten… (missing: @returns)
- `src/resources/extensions/gsd/prompt-loader.ts:79` — `getTemplatesDir()` — exported: **yes** — Implements logic starting with: { return templatesDir; } (missing: @returns)
- `src/resources/extensions/gsd/prompt-loader.ts:174` — `loadPrompt(name: string, vars: Record<string, string> = {})` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/prompt-loader.ts:229` — `loadTemplate(name: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param name, @returns)
- `src/resources/extensions/gsd/prompt-loader.ts:245` — `inlineTemplate(name: string, label: string)` — exported: **yes** — Implements logic starting with: { const content = loadTemplate(name); return `${content}\n\n### Output Template: ${label}\nSource: \`templates/${name}.md\``; } (missing: @param name, @param label, @returns)

### `src/resources/extensions/gsd/prompt-ordering.ts`
- `src/resources/extensions/gsd/prompt-ordering.ts:52` — `extractHeadingText(line: string)` — exported: **no** — Implements logic starting with: { return line.replace(/^##\s+/, "").trim(); } (missing: @param line, @returns)
- `src/resources/extensions/gsd/prompt-ordering.ts:61` — `classifyHeading(heading: string)` — exported: **no** — Implements logic starting with: { for (const [key, role] of Object.entries(HEADING_ROLES)) { if (heading === key || heading.startsWith(key)) { return role; } } return "dynamic"; } (missing: @param heading, @returns)
- `src/resources/extensions/gsd/prompt-ordering.ts:75` — `splitSections(prompt: string)` — exported: **no** — Implements logic starting with: { const lines = prompt.split("\n"); let preamble = ""; const sections: ExtractedSection[] = []; let currentHeading = ""; let currentContent: string… (missing: @param prompt, @returns)
- `src/resources/extensions/gsd/prompt-ordering.ts:161` — `analyzeCacheEfficiency(prompt: string)` — exported: **yes** — Implements logic starting with: { const { preamble, sections } = splitSections(prompt); let staticChars = preamble.length; let semiStaticChars = 0; let dynamicChars = 0; for (cons… (missing: @param prompt, @returns)

### `src/resources/extensions/gsd/prompt-validation.ts`
- `src/resources/extensions/gsd/prompt-validation.ts:106` — `escapeRegExp(value: string)` — exported: **no** — Implements logic starting with: { return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"); } (missing: @param value, @returns)
- `src/resources/extensions/gsd/prompt-validation.ts:118` — `validateGateSections(content: string, turn: OwnerTurn)` — exported: **yes** — Implements logic starting with: { const missing: string[] = []; for (const def of getGatesForTurn(turn)) { const pattern = new RegExp(`^##\\s+${escapeRegExp(def.promptSection)}\\b… (missing: @param content, @param turn, @returns)
- `src/resources/extensions/gsd/prompt-validation.ts:138` — `validateSliceSummaryOutput(content: string)` — exported: **yes** — Implements logic starting with: { return validateGateSections(content, "complete-slice"); } (missing: @param content, @returns)
- `src/resources/extensions/gsd/prompt-validation.ts:147` — `validateTaskSummaryOutput(content: string)` — exported: **yes** — Implements logic starting with: { return validateGateSections(content, "execute-task"); } (missing: @param content, @returns)
- `src/resources/extensions/gsd/prompt-validation.ts:155` — `validateMilestoneValidationOutput(content: string)` — exported: **yes** — Implements logic starting with: { return validateGateSections(content, "validate-milestone"); } (missing: @param content, @returns)

### `src/resources/extensions/gsd/provider-error-pause.ts`
- `src/resources/extensions/gsd/provider-error-pause.ts:12` — `pauseAutoForProviderError(ui: ProviderErrorPauseUI, errorDetail: string, pause: () => Promise<void>, options?: { isRateLimit?: boolean; isTransient?: boolean; retryAfterMs?: number; resume?: () => void; })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ui, @param errorDetail, @param pause, @param options)

### `src/resources/extensions/gsd/provider-switch-observer.ts`
- `src/resources/extensions/gsd/provider-switch-observer.ts:70` — `summarize(report: ProviderSwitchReport)` — exported: **no** — Implements logic starting with: { const parts: string[] = []; if (report.thinkingBlocksDropped > 0) parts.push(`${report.thinkingBlocksDropped} thinking dropped`); if (report.thin… (missing: @param report, @returns)
- `src/resources/extensions/gsd/provider-switch-observer.ts:160` — `getProviderSwitchStats()` — exported: **yes** — Implements logic starting with: { const trace: Record<string, TraceSwitchStats> = {}; for (const [key, value] of byTrace) { trace[key] = { switches: value.switches, lastReport: {… (missing: @returns)

### `src/resources/extensions/gsd/python-resolver.ts`
- `src/resources/extensions/gsd/python-resolver.ts:32` — `detectPythonExecutable()` — exported: **yes** — Executes a subprocess command. (missing: @returns)

### `src/resources/extensions/gsd/queue-order.ts`
- `src/resources/extensions/gsd/queue-order.ts:61` — `loadQueueOrder(basePath: string)` — exported: **yes** — Implements logic starting with: { const data = loadJsonFileOrNull(queueOrderPath(basePath), isQueueOrderFile); return data?.order ?? null; } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/queue-order.ts:70` — `saveQueueOrder(basePath: string, order: string[])` — exported: **yes** — Implements logic starting with: { if (isDbAvailable()) { setMilestoneQueueOrder(order); } const data: QueueOrderFile = { order, updatedAt: new Date().toISOString(), }; saveJsonFil… (missing: @param basePath, @param order)
- `src/resources/extensions/gsd/queue-order.ts:92` — `sortByQueueOrder(ids: string[], customOrder: string[] | null)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param ids, @param customOrder, @returns)
- `src/resources/extensions/gsd/queue-order.ts:117` — `pruneQueueOrder(basePath: string, validIds: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param validIds)
- `src/resources/extensions/gsd/queue-order.ts:140` — `validateQueueOrder(order: string[], depsMap: Map<string, string[]>, completedIds: Set<string>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param order, @param depsMap, @param completedIds, @returns)

### `src/resources/extensions/gsd/queue-reorder-ui.ts`
- `src/resources/extensions/gsd/queue-reorder-ui.ts:34` — `showQueueReorder(ctx: ExtensionContext, completed: ReorderItem[], pending: ReorderItem[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param completed, @param pending, @returns)

### `src/resources/extensions/gsd/quick.ts`
- `src/resources/extensions/gsd/quick.ts:38` — `slugify(text: string)` — exported: **no** — Implements logic starting with: { return text .toLowerCase() .replace(/[^a-z0-9]+/g, "-") .replace(/^-|-$/g, "") .slice(0, 40) .replace(/-$/, ""); } (missing: @param text, @returns)
- `src/resources/extensions/gsd/quick.ts:50` — `getNextTaskNum(quickDir: string)` — exported: **no** — Implements logic starting with: { if (!existsSync(quickDir)) return 1; try { const entries = readdirSync(quickDir, { withFileTypes: true }); let max = 0; for (const entry of entri… (missing: @param quickDir, @returns)
- `src/resources/extensions/gsd/quick.ts:73` — `ensureQuickDir(basePath: string, taskNum: number, slug: string)` — exported: **no** — Implements logic starting with: { const quickDir = join(gsdRoot(basePath), "quick"); const taskDir = join(quickDir, `${taskNum}-${slug}`); mkdirSync(taskDir, { recursive: true });… (missing: @param basePath, @param taskNum, @param slug, @returns)

### `src/resources/extensions/gsd/reactive-graph.ts`
- `src/resources/extensions/gsd/reactive-graph.ts:31` — `deriveTaskGraph(tasks: TaskIO[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tasks, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:68` — `getReadyTasks(graph: DerivedTaskNode[], completed: Set<string>, inFlight: Set<string>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param graph, @param completed, @param inFlight, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:90` — `chooseNonConflictingSubset(readyIds: string[], graph: DerivedTaskNode[], maxParallel: number, inFlightOutputs: Set<string>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param readyIds, @param graph, @param maxParallel, @param inFlightOutputs, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:125` — `isGraphAmbiguous(graph: DerivedTaskNode[])` — exported: **yes** — Implements logic starting with: { return graph.some( (node) => !node.done && node.inputFiles.length === 0 && node.outputFiles.length === 0, ); } (missing: @param graph, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:139` — `getMissingAnnotationTasks(graph: DerivedTaskNode[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param graph, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:156` — `detectDeadlock(graph: DerivedTaskNode[], completed: Set<string>, inFlight: Set<string>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param graph, @param completed, @param inFlight, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:175` — `graphMetrics(graph: DerivedTaskNode[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param graph, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:201` — `loadSliceTaskIO(basePath: string, mid: string, sid: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param mid, @param sid, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:303` — `loadReactiveState(basePath: string, mid: string, sid: string)` — exported: **yes** — Implements logic starting with: { return loadJsonFileOrNull(reactiveStatePath(basePath, mid, sid), isReactiveState); } (missing: @param basePath, @param mid, @param sid, @returns)
- `src/resources/extensions/gsd/reactive-graph.ts:314` — `saveReactiveState(basePath: string, mid: string, sid: string, state: ReactiveExecutionState)` — exported: **yes** — Implements logic starting with: { saveJsonFile(reactiveStatePath(basePath, mid, sid), state); } (missing: @param basePath, @param mid, @param sid, @param state)
- `src/resources/extensions/gsd/reactive-graph.ts:326` — `clearReactiveState(basePath: string, mid: string, sid: string)` — exported: **yes** — Implements logic starting with: { const path = reactiveStatePath(basePath, mid, sid); try { if (existsSync(path)) unlinkSync(path); } catch { // Non-fatal } } (missing: @param basePath, @param mid, @param sid)

### `src/resources/extensions/gsd/repo-identity.ts`
- `src/resources/extensions/gsd/repo-identity.ts:41` — `writeRepoMeta(externalPath: string, remoteUrl: string, gitRoot: string)` — exported: **no** — Performs filesystem I/O. (missing: @param externalPath, @param remoteUrl, @param gitRoot)
- `src/resources/extensions/gsd/repo-identity.ts:85` — `readRepoMeta(externalPath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param externalPath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:118` — `isInheritedRepo(basePath: string)` — exported: **yes** — Implements logic starting with: { try { const root = resolveGitRoot(basePath); const normalizedBase = canonicalizeExistingPath(basePath); const normalizedRoot = canonicalizeExisti… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:157` — `isProjectGsd(gsdPath: string)` — exported: **no** — Implements logic starting with: { if (!existsSync(gsdPath)) return false; try { const stat = lstatSync(gsdPath); // Symlinks are always project .gsd (created by ensureGsdSymlink).… (missing: @param gsdPath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:189` — `getRemoteUrl(basePath: string)` — exported: **no** — Implements logic starting with: { try { return execFileSync("git", ["config", "--get", "remote.origin.url"], { cwd: basePath, encoding: "utf-8", stdio: ["ignore", "pipe", "ignore"… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:206` — `canonicalizeExistingPath(path: string)` — exported: **no** — Implements logic starting with: { try { // Use native realpath on Windows to resolve 8.3 short paths (e.g. RUNNER~1) return process.platform === "win32" ? realpathSync.native(path… (missing: @param path, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:268` — `validateProjectId(id: string)` — exported: **yes** — Implements logic starting with: { return /^[a-zA-Z0-9_-]+$/.test(id); } (missing: @param id, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:287` — `repoIdentity(basePath: string)` — exported: **yes** — Implements logic starting with: { const projectId = process.env.GSD_PROJECT_ID; if (projectId) { return projectId; } const remoteUrl = getRemoteUrl(basePath); if (remoteUrl) { //… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:312` — `externalGsdRoot(basePath: string)` — exported: **yes** — Implements logic starting with: { const base = process.env.GSD_STATE_DIR || gsdHome(); return join(base, "projects", repoIdentity(basePath)); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:321` — `externalProjectsRoot()` — exported: **yes** — Implements logic starting with: { const base = process.env.GSD_STATE_DIR || gsdHome(); return join(base, "projects"); } (missing: @returns)
- `src/resources/extensions/gsd/repo-identity.ts:376` — `writeGsdIdMarker(projectPath: string, identity: string)` — exported: **no** — Performs filesystem I/O. (missing: @param projectPath, @param identity)
- `src/resources/extensions/gsd/repo-identity.ts:395` — `readGsdIdMarker(projectPath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param projectPath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:411` — `hasProjectState(externalPath: string)` — exported: **no** — Implements logic starting with: { try { if (!existsSync(externalPath)) return false; const entries = readdirSync(externalPath); return entries.some(e => e !== "repo-meta.json"); }… (missing: @param externalPath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:430` — `resolveExternalPathWithRecovery(projectPath: string)` — exported: **no** — Implements logic starting with: { const computedPath = externalGsdRoot(projectPath); const computedId = repoIdentity(projectPath); // Check if computed path already has state — fa… (missing: @param projectPath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:491` — `ensureGsdSymlink(projectPath: string)` — exported: **yes** — Implements logic starting with: { const result = ensureGsdSymlinkCore(projectPath); // Write .gsd-id marker so future relocations can recover this state (#2750). // Only write for… (missing: @param projectPath, @returns)
- `src/resources/extensions/gsd/repo-identity.ts:653` — `isInsideWorktree(cwd: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param cwd, @returns)

### `src/resources/extensions/gsd/reports.ts`
- `src/resources/extensions/gsd/reports.ts:116` — `writeReportSnapshot(args: WriteReportSnapshotArgs)` — exported: **yes** — Implements logic starting with: { const dir = reportsDir(args.basePath); mkdirSync(dir, { recursive: true }); const timestamp = new Date().toISOString().replace(/[:.]/g, '-').slic… (missing: @param args, @returns)

### `src/resources/extensions/gsd/roadmap-mutations.ts`
- `src/resources/extensions/gsd/roadmap-mutations.ts:19` — `markSliceDoneInRoadmap(basePath: string, mid: string, sid: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param mid, @param sid)
- `src/resources/extensions/gsd/roadmap-mutations.ts:61` — `markSliceUndoneInRoadmap(basePath: string, mid: string, sid: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param mid, @param sid)
- `src/resources/extensions/gsd/roadmap-mutations.ts:90` — `markTaskDoneInPlan(basePath: string, planPath: string, tid: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param planPath, @param tid)
- `src/resources/extensions/gsd/roadmap-mutations.ts:116` — `markTaskUndoneInPlan(basePath: string, planPath: string, tid: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param planPath, @param tid)

### `src/resources/extensions/gsd/roadmap-slices.ts`
- `src/resources/extensions/gsd/roadmap-slices.ts:14` — `expandDependencies(deps: string[])` — exported: **yes** — Implements logic starting with: { const result: string[] = []; for (const dep of deps) { const trimmed = dep.trim(); if (!trimmed) continue; // Match range syntax: S01-S04 or S01.… (missing: @param deps, @returns)
- `src/resources/extensions/gsd/roadmap-slices.ts:65` — `parseTableSlices(section: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param section, @returns)
- `src/resources/extensions/gsd/roadmap-slices.ts:235` — `parseProseSliceHeaders(content: string)` — exported: **no** — Executes a subprocess command. (missing: @param content, @returns)

### `src/resources/extensions/gsd/routing-history.ts`
- `src/resources/extensions/gsd/routing-history.ts:58` — `initRoutingHistory(base: string)` — exported: **yes** — Implements logic starting with: { historyBasePath = base; history = loadHistory(base); } (missing: @param base)
- `src/resources/extensions/gsd/routing-history.ts:125` — `recordFeedback(unitType: string, unitId: string, tier: ComplexityTier, rating: "over" | "under" | "ok")` — exported: **yes** — Implements logic starting with: { if (!history) return; history.feedback.push({ unitType, unitId, tier, rating, timestamp: new Date().toISOString(), }); // Cap feedback array at 2… (missing: @param unitType, @param unitId, @param tier, @param rating)
- `src/resources/extensions/gsd/routing-history.ts:174` — `getAdaptiveTierAdjustment(unitType: string, currentTier: ComplexityTier, tags?: string[])` — exported: **yes** — Implements logic starting with: { if (!history) return null; // Check tag-specific patterns first (more specific) if (tags && tags.length > 0) { for (const tag of tags) { const ta… (missing: @param unitType, @param currentTier, @param tags, @returns)
- `src/resources/extensions/gsd/routing-history.ts:197` — `clearRoutingHistory(base: string)` — exported: **yes** — Implements logic starting with: { history = createEmptyHistory(); saveHistory(base, history); } (missing: @param base)
- `src/resources/extensions/gsd/routing-history.ts:205` — `getRoutingHistory()` — exported: **yes** — Implements logic starting with: { return history; } (missing: @returns)

### `src/resources/extensions/gsd/rule-registry.ts`
- `src/resources/extensions/gsd/rule-registry.ts:45` — `convertDispatchRules(rules: DispatchRule[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param rules, @returns)
- `src/resources/extensions/gsd/rule-registry.ts:86` — `RuleRegistry.listRules()` — exported: **yes** — Implements logic starting with: { const rules: UnifiedRule[] = [...this.dispatchRules]; // Convert post-unit hooks to unified rules const postHooks = resolvePostUnitHooks(); for (… (missing: @returns)
- `src/resources/extensions/gsd/rule-registry.ts:129` — `RuleRegistry.evaluateDispatch(ctx: DispatchContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @returns)
- `src/resources/extensions/gsd/rule-registry.ts:151` — `RuleRegistry.evaluatePostUnit(completedUnitType: string, completedUnitId: string, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param completedUnitType, @param completedUnitId, @param basePath, @returns)
- `src/resources/extensions/gsd/rule-registry.ts:276` — `RuleRegistry.evaluatePreDispatch(unitType: string, unitId: string, prompt: string, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param unitType, @param unitId, @param prompt, @param basePath, @returns)
- `src/resources/extensions/gsd/rule-registry.ts:358` — `RuleRegistry.consumeRetryTrigger()` — exported: **yes** — Implements logic starting with: { if (!this.retryPending || !this.retryTrigger) return null; const trigger = { ...this.retryTrigger }; this.retryPending = false; this.retryTrigger… (missing: @returns)
- `src/resources/extensions/gsd/rule-registry.ts:382` — `RuleRegistry.persistState(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath)
- `src/resources/extensions/gsd/rule-registry.ts:397` — `RuleRegistry.restoreState(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath)
- `src/resources/extensions/gsd/rule-registry.ts:417` — `RuleRegistry.clearPersistedState(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath)
- `src/resources/extensions/gsd/rule-registry.ts:435` — `RuleRegistry.getHookStatus()` — exported: **yes** — Implements logic starting with: { const entries: HookStatusEntry[] = []; const postHooks = resolvePostUnitHooks(); for (const hook of postHooks) { const activeCycles: Record<strin… (missing: @returns)
- `src/resources/extensions/gsd/rule-registry.ts:473` — `RuleRegistry.triggerHookManually(hookName: string, unitType: string, unitId: string, basePath: string)` — exported: **yes** — Logs diagnostic information. (missing: @param hookName, @param unitType, @param unitId, @param basePath, @returns)
- `src/resources/extensions/gsd/rule-registry.ts:525` — `RuleRegistry.formatHookStatus()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/rule-registry.ts:565` — `getRegistry()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `src/resources/extensions/gsd/rule-registry.ts:573` — `setRegistry(r: RuleRegistry)` — exported: **yes** — Implements logic starting with: { _registry = r; } (missing: @param r)
- `src/resources/extensions/gsd/rule-registry.ts:578` — `initRegistry(dispatchRules: UnifiedRule[])` — exported: **yes** — Implements logic starting with: { const registry = new RuleRegistry(dispatchRules); setRegistry(registry); return registry; } (missing: @param dispatchRules, @returns)
- `src/resources/extensions/gsd/rule-registry.ts:589` — `getOrCreateRegistry()` — exported: **yes** — Implements logic starting with: { if (!_registry) { _registry = new RuleRegistry([]); } return _registry; } (missing: @returns)

### `src/resources/extensions/gsd/run-manager.ts`
- `src/resources/extensions/gsd/run-manager.ts:52` — `makeTimestamp(date: Date = new Date())` — exported: **no** — Implements logic starting with: { return date.toISOString().replace(/:/g, "-").replace(/\.\d{3}Z$/, ""); } (missing: @param date, @returns)
- `src/resources/extensions/gsd/run-manager.ts:59` — `deriveStatus(graph: WorkflowGraph)` — exported: **no** — Implements logic starting with: { const hasActive = graph.steps.some((s) => s.status === "active"); const allDone = graph.steps.every( (s) => s.status === "complete" || s.status =… (missing: @param graph, @returns)
- `src/resources/extensions/gsd/run-manager.ts:76` — `createRunFromDefinition(basePath: string, defName: string, definitionFile: string, overrides?: Record<string, string>)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param defName, @param definitionFile, @param overrides, @returns)
- `src/resources/extensions/gsd/run-manager.ts:119` — `createRun(basePath: string, defName: string, overrides?: Record<string, string>)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param defName, @param overrides, @returns)

### `src/resources/extensions/gsd/safe-fs.ts`
- `src/resources/extensions/gsd/safe-fs.ts:9` — `safeMkdir(dirPath: string)` — exported: **yes** — Implements logic starting with: { try { mkdirSync(dirPath, { recursive: true }) return true } catch (err) { logWarning("fs", `mkdir failed: ${dirPath}: ${(err as Error).message}`)… (missing: @param dirPath, @returns)
- `src/resources/extensions/gsd/safe-fs.ts:23` — `safeCopy(src: string, dst: string, opts?: CopySyncOptions)` — exported: **yes** — Implements logic starting with: { if (!existsSync(src)) return false try { cpSync(src, dst, opts) return true } catch (err) { logWarning("fs", `copy failed: ${src} → ${dst}: ${(er… (missing: @param src, @param dst, @param opts, @returns)
- `src/resources/extensions/gsd/safe-fs.ts:38` — `safeCopyRecursive(src: string, dst: string, opts?: Omit<CopySyncOptions, 'recursive'>)` — exported: **yes** — Implements logic starting with: { if (!existsSync(src)) return false try { mkdirSync(dirname(dst), { recursive: true }) cpSync(src, dst, { ...opts, recursive: true }) return true… (missing: @param src, @param dst, @param opts, @returns)

### `src/resources/extensions/gsd/safety/content-validator.ts`
- `src/resources/extensions/gsd/safety/content-validator.ts:27` — `validateContent(unitType: string, artifactPath: string | null)` — exported: **yes** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/safety/destructive-guard.ts`
- `src/resources/extensions/gsd/safety/destructive-guard.ts:40` — `classifyCommand(command: string)` — exported: **yes** — Implements logic starting with: { const labels: string[] = []; for (const { pattern, label } of DESTRUCTIVE_PATTERNS) { if (pattern.test(command)) { // Deduplicate labels (e.g., t… (missing: @param command, @returns)

### `src/resources/extensions/gsd/safety/evidence-collector.ts`
- `src/resources/extensions/gsd/safety/evidence-collector.ts:74` — `getEvidence()` — exported: **yes** — Implements logic starting with: { return unitEvidence; } (missing: @returns)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:79` — `getBashEvidence()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:84` — `getFilePaths()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:96` — `evidencePath(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **no** — Implements logic starting with: { return join(basePath, ".gsd", "safety", `evidence-${milestoneId}-${sliceId}-${taskId}.json`); } (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId, @returns)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:104` — `isEvidenceArray(data: unknown)` — exported: **no** — Implements logic starting with: { if (!Array.isArray(data)) return false; return data.every((e) => { if (e === null || typeof e !== "object") return false; const rec = e as Record… (missing: @param data, @returns)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:130` — `saveEvidenceToDisk(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:153` — `loadEvidenceFromDisk(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:177` — `clearEvidenceFromDisk(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { try { const path = evidencePath(basePath, milestoneId, sliceId, taskId); if (existsSync(path)) { unlinkSync(path); } } catch { // Non-fatal } } (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:199` — `recordToolCall(toolCallId: string, toolName: string, input: Record<string, unknown>)` — exported: **yes** — Implements logic starting with: { if (EXECUTION_TOOL_NAMES.has(toolName)) { unitEvidence.push({ kind: "bash", toolCallId, command: String(input.command ?? input.cmd ?? input.query… (missing: @param toolCallId, @param toolName, @param input)
- `src/resources/extensions/gsd/safety/evidence-collector.ts:231` — `recordToolResult(toolCallId: string, toolName: string, result: unknown, isError: boolean)` — exported: **yes** — Implements logic starting with: { const entry = unitEvidence.find(e => e.toolCallId === toolCallId); if (!entry) return; if (entry.kind === "bash") { const text = extractResultTex… (missing: @param toolCallId, @param toolName, @param result, @param isError)

### `src/resources/extensions/gsd/safety/evidence-cross-ref.ts`
- `src/resources/extensions/gsd/safety/evidence-cross-ref.ts:34` — `crossReferenceEvidence(claimedEvidence: readonly ClaimedEvidence[], actualEvidence: readonly EvidenceEntry[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param claimedEvidence, @param actualEvidence, @returns)
- `src/resources/extensions/gsd/safety/evidence-cross-ref.ts:86` — `findBestMatch(claimedCommand: string, bashCalls: readonly BashEvidence[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param claimedCommand, @param bashCalls, @returns)

### `src/resources/extensions/gsd/safety/file-change-validator.ts`
- `src/resources/extensions/gsd/safety/file-change-validator.ts:50` — `validateFileChanges(basePath: string, expectedOutput: string[], plannedFiles: string[], fileChangeAllowlist: string[] = [])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param fileChangeAllowlist, @returns)

### `src/resources/extensions/gsd/safety/git-checkpoint.ts`
- `src/resources/extensions/gsd/safety/git-checkpoint.ts:24` — `createCheckpoint(basePath: string, unitId: string)` — exported: **yes** — Implements logic starting with: { try { const sha = execFileSync("git", ["rev-parse", "--verify", "HEAD"], { cwd: basePath, stdio: ["ignore", "pipe", "pipe"], encoding: "utf-8", }… (missing: @param basePath, @param unitId, @returns)
- `src/resources/extensions/gsd/safety/git-checkpoint.ts:66` — `rollbackToCheckpoint(basePath: string, unitId: string, sha: string)` — exported: **yes** — Implements logic starting with: { try { // Get current branch name const branch = execFileSync("git", ["rev-parse", "--abbrev-ref", "HEAD"], { cwd: basePath, stdio: ["ignore", "pi… (missing: @param basePath, @param unitId, @param sha, @returns)
- `src/resources/extensions/gsd/safety/git-checkpoint.ts:120` — `cleanupCheckpoint(basePath: string, unitId: string)` — exported: **yes** — Implements logic starting with: { try { const safeUnitId = unitId.replace(/\//g, "-"); execFileSync("git", ["update-ref", "-d", `${CHECKPOINT_PREFIX}${safeUnitId}`], { cwd: basePa… (missing: @param basePath, @param unitId)

### `src/resources/extensions/gsd/safety/safety-harness.ts`
- `src/resources/extensions/gsd/safety/safety-harness.ts:53` — `resolveSafetyHarnessConfig(raw: Record<string, unknown> | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param raw, @returns)
- `src/resources/extensions/gsd/safety/safety-harness.ts:78` — `isHarnessEnabled(raw: Record<string, unknown> | undefined)` — exported: **yes** — Implements logic starting with: { if (!raw) return DEFAULTS.enabled; if (typeof raw.enabled === "boolean") return raw.enabled; return DEFAULTS.enabled; } (missing: @param raw, @returns)

### `src/resources/extensions/gsd/schemas/parsers.ts`
- `src/resources/extensions/gsd/schemas/parsers.ts:226` — `parseDependsCell(raw: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param raw, @returns)
- `src/resources/extensions/gsd/schemas/parsers.ts:245` — `parseSliceOverviewTable(body: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param body, @returns)

### `src/resources/extensions/gsd/service-tier.ts`
- `src/resources/extensions/gsd/service-tier.ts:49` — `supportsServiceTier(modelId: string)` — exported: **yes** — Implements logic starting with: { if (!modelId) return false; // Strip provider prefix if present (e.g. "openai/gpt-5.4" → "gpt-5.4") const bare = modelId.includes("/") ? modelId.… (missing: @param modelId, @returns)
- `src/resources/extensions/gsd/service-tier.ts:61` — `formatServiceTierStatus(tier: ServiceTierSetting)` — exported: **yes** — Implements logic starting with: { if (!tier) { return [ "Service tier: disabled", "", "Usage:", " /gsd fast on Set to priority (2x cost, faster)", " /gsd fast flex Set to flex (0.… (missing: @param tier, @returns)
- `src/resources/extensions/gsd/service-tier.ts:103` — `resolveServiceTierIcon(tier: ServiceTierSetting, modelId: string)` — exported: **yes** — Implements logic starting with: { if (!tier || !supportsServiceTier(modelId)) return ""; return tier === "priority" ? "⚡" : "💰"; } (missing: @param tier, @param modelId, @returns)
- `src/resources/extensions/gsd/service-tier.ts:113` — `getEffectiveServiceTier()` — exported: **yes** — Implements logic starting with: { const prefs = loadEffectiveGSDPreferences()?.preferences; const raw = prefs?.service_tier; if (raw === "priority" || raw === "flex") return raw;… (missing: @returns)
- `src/resources/extensions/gsd/service-tier.ts:165` — `handleFast(args: string, ctx: ExtensionCommandContext)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param args, @param ctx)

### `src/resources/extensions/gsd/session-forensics.ts`
- `src/resources/extensions/gsd/session-forensics.ts:75` — `extractLastSession(entries: unknown[])` — exported: **no** — Implements logic starting with: { let lastSessionIdx = -1; for (let i = entries.length - 1; i >= 0; i--) { const entry = entries[i] as Record<string, unknown>; if (entry.type ===… (missing: @param entries, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:93` — `extractTrace(entries: unknown[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param entries, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:238` — `synthesizeCrashRecovery(basePath: string, unitType: string, unitId: string, sessionFile?: string, activityDir?: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param unitType, @param unitId, @param sessionFile, @param activityDir, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:297` — `getDeepDiagnostic(basePath: string, worktreePath?: string)` — exported: **yes** — Implements logic starting with: { // Try worktree activity logs first if a worktree path is provided let trace: ExecutionTrace | null = null; try { if (worktreePath) { const wtAct… (missing: @param basePath, @param worktreePath, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:321` — `readActiveMilestoneId(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:420` — `compressToolCallTrace(calls: ToolCall[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param calls, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:527` — `redactInput(name: string, input: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { const safe: Record<string, unknown> = {}; for (const [key, value] of Object.entries(input)) { if (key === "content" || key === "oldText" || key =… (missing: @param name, @param input, @returns)
- `src/resources/extensions/gsd/session-forensics.ts:540` — `findLast(arr: T[], predicate: (item: T) => boolean)` — exported: **no** — Implements logic starting with: { for (let i = arr.length - 1; i >= 0; i--) { if (predicate(arr[i]!)) return arr[i]; } return undefined; } (missing: @param arr, @param predicate, @returns)

### `src/resources/extensions/gsd/session-lock.ts`
- `src/resources/extensions/gsd/session-lock.ts:104` — `effectiveLockFile()` — exported: **yes** — Implements logic starting with: { const mid = process.env.GSD_PARALLEL_WORKER ? process.env.GSD_MILESTONE_LOCK : null; return mid ? `auto-${mid}.lock` : LOCK_FILE; } (missing: @returns)
- `src/resources/extensions/gsd/session-lock.ts:114` — `effectiveLockTarget(gsdDir: string)` — exported: **yes** — Implements logic starting with: { const mid = process.env.GSD_PARALLEL_WORKER ? process.env.GSD_MILESTONE_LOCK : null; return mid ? join(gsdDir, "parallel", mid) : gsdDir; } (missing: @param gsdDir, @returns)
- `src/resources/extensions/gsd/session-lock.ts:134` — `cleanupStrayLockFiles(basePath: string)` — exported: **yes** — Implements logic starting with: { const gsdDir = gsdRoot(basePath); // Clean numbered auto lock files inside .gsd/ try { if (existsSync(gsdDir)) { for (const entry of readdirSync(… (missing: @param basePath)
- `src/resources/extensions/gsd/session-lock.ts:176` — `ensureExitHandler(_gsdDir: string)` — exported: **no** — Implements logic starting with: { // Register the gsdDir so exit cleanup covers it _lockDirRegistry.add(_gsdDir); if (_exitHandlerRegistered) return; _exitHandlerRegistered = true… (missing: @param _gsdDir)
- `src/resources/extensions/gsd/session-lock.ts:221` — `createLockCompromisedHandler(lockFilePath: string)` — exported: **no** — Implements logic starting with: { return () => { const elapsed = Date.now() - _lockAcquiredAt; if (elapsed < 1_800_000) { process.stderr.write( `[gsd] Lock heartbeat caught up aft… (missing: @param lockFilePath, @returns)
- `src/resources/extensions/gsd/session-lock.ts:245` — `assignLockState(basePath: string, release: () => void, lockFilePath: string)` — exported: **no** — Implements logic starting with: { _releaseFunction = release; _lockedPath = basePath; _lockPid = process.pid; _lockCompromised = false; _lockAcquiredAt = Date.now(); _snapshotLock… (missing: @param basePath, @param release, @param lockFilePath)
- `src/resources/extensions/gsd/session-lock.ts:265` — `acquireSessionLock(basePath: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-lock.ts:389` — `acquireFallbackLock(basePath: string, lp: string, lockData: SessionLockData)` — exported: **no** — Serializes a value to JSON. (missing: @param basePath, @param lp, @param lockData, @returns)
- `src/resources/extensions/gsd/session-lock.ts:419` — `updateSessionLock(basePath: string, unitType: string, unitId: string, sessionFile?: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @param unitType, @param unitId, @param sessionFile)
- `src/resources/extensions/gsd/session-lock.ts:451` — `getSessionLockStatus(basePath: string)` — exported: **yes** — Implements logic starting with: { // Lock was compromised by proper-lockfile (mtime drift from sleep, stall, etc.) if (_lockCompromised) { // Recovery gate (#1512): Before declari… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-lock.ts:519` — `releaseSessionLock(basePath: string)` — exported: **yes** — Implements logic starting with: { // Release the OS-level lock if (_releaseFunction) { try { _releaseFunction(); } catch { // Lock may already be released } _releaseFunction = nul… (missing: @param basePath)
- `src/resources/extensions/gsd/session-lock.ts:589` — `readSessionLockData(basePath: string)` — exported: **yes** — Implements logic starting with: { return readExistingLockData(lockPath(basePath)); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-lock.ts:596` — `isSessionLockProcessAlive(data: SessionLockData)` — exported: **yes** — Implements logic starting with: { return isPidAlive(data.pid); } (missing: @param data, @returns)
- `src/resources/extensions/gsd/session-lock.ts:609` — `removeStaleSessionLock(basePath: string)` — exported: **yes** — Implements logic starting with: { const lp = lockPath(basePath); const gsdDir = gsdRoot(basePath); const lockTarget = effectiveLockTarget(gsdDir); const lockDir = lockTarget + ".l… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-lock.ts:644` — `isSessionLockHeld(basePath: string)` — exported: **yes** — Implements logic starting with: { return _lockedPath === basePath && _lockPid === process.pid; } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-lock.ts:652` — `_getRegisteredLockDirs()` — exported: **yes** — Implements logic starting with: { return [..._lockDirRegistry]; } (missing: @returns)

### `src/resources/extensions/gsd/session-status-io.ts`
- `src/resources/extensions/gsd/session-status-io.ts:93` — `writeSessionStatus(basePath: string, status: SessionStatus)` — exported: **yes** — Implements logic starting with: { ensureParallelDir(basePath); writeJsonFileAtomic(statusPath(basePath, status.milestoneId), status); } (missing: @param basePath, @param status)
- `src/resources/extensions/gsd/session-status-io.ts:99` — `readSessionStatus(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { return loadJsonFileOrNull(statusPath(basePath, milestoneId), isSessionStatus); } (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/session-status-io.ts:104` — `readAllSessionStatuses(basePath: string)` — exported: **yes** — Implements logic starting with: { const dir = parallelDir(basePath); if (!existsSync(dir)) return []; const results: SessionStatus[] = []; try { for (const entry of readdirSync(di… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/session-status-io.ts:120` — `removeSessionStatus(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { try { const p = statusPath(basePath, milestoneId); if (existsSync(p)) unlinkSync(p); } catch { /* non-fatal */ } } (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/session-status-io.ts:130` — `sendSignal(basePath: string, milestoneId: string, signal: SessionSignal)` — exported: **yes** — Implements logic starting with: { ensureParallelDir(basePath); const msg: SignalMessage = { signal, sentAt: Date.now(), from: "coordinator" }; writeJsonFileAtomic(signalPath(baseP… (missing: @param basePath, @param milestoneId, @param signal)
- `src/resources/extensions/gsd/session-status-io.ts:137` — `consumeSignal(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const p = signalPath(basePath, milestoneId); const msg = loadJsonFileOrNull(p, isSignalMessage); if (msg) { try { unlinkSync(p); } catch { /* non… (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/session-status-io.ts:149` — `isSessionStale(status: SessionStatus, timeoutMs: number = DEFAULT_STALE_TIMEOUT_MS)` — exported: **yes** — Implements logic starting with: { if (!isPidAlive(status.pid)) return true; const elapsed = Date.now() - status.lastHeartbeat; return elapsed > timeoutMs; } (missing: @param status, @param timeoutMs, @returns)
- `src/resources/extensions/gsd/session-status-io.ts:159` — `cleanupStaleSessions(basePath: string, timeoutMs: number = DEFAULT_STALE_TIMEOUT_MS)` — exported: **yes** — Implements logic starting with: { const removed: string[] = []; const statuses = readAllSessionStatuses(basePath); for (const status of statuses) { if (isSessionStale(status, time… (missing: @param basePath, @param timeoutMs, @returns)

### `src/resources/extensions/gsd/setup-catalog.ts`
- `src/resources/extensions/gsd/setup-catalog.ts:67` — `nearestResumeStep(lastResumePoint: string | null, completedSteps: string[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param lastResumePoint, @param completedSteps, @returns)
- `src/resources/extensions/gsd/setup-catalog.ts:103` — `getLlmProviderIds()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `src/resources/extensions/gsd/skill-catalog.ts`
- `src/resources/extensions/gsd/skill-catalog.ts:839` — `matchPacksForProject(signals: ProjectSignals)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param signals, @returns)
- `src/resources/extensions/gsd/skill-catalog.ts:884` — `installSkillPack(pack: SkillPack)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param pack, @returns)
- `src/resources/extensions/gsd/skill-catalog.ts:904` — `installPacksBatched(packs: SkillPack[], onProgress?: (label: string) => void)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param packs, @param onProgress, @returns)
- `src/resources/extensions/gsd/skill-catalog.ts:940` — `isPackInstalled(pack: SkillPack)` — exported: **yes** — Implements logic starting with: { const skillsDirs = [ join(homedir(), ".agents", "skills"), join(homedir(), ".claude", "skills"), ]; return pack.skills.every((name) => skillsDirs… (missing: @param pack, @returns)
- `src/resources/extensions/gsd/skill-catalog.ts:968` — `runSkillInstallStep(ctx: ExtensionCommandContext, signals: ProjectSignals)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param signals, @returns)

### `src/resources/extensions/gsd/skill-discovery.ts`
- `src/resources/extensions/gsd/skill-discovery.ts:45` — `hasSkillSnapshot()` — exported: **yes** — Implements logic starting with: { return baselineSkills !== null; } (missing: @returns)
- `src/resources/extensions/gsd/skill-discovery.ts:53` — `detectNewSkills()` — exported: **yes** — Implements logic starting with: { if (!baselineSkills) return []; const current = listSkillDirs(); const newSkills: DiscoveredSkill[] = []; for (const dir of current) { if (baseli… (missing: @returns)
- `src/resources/extensions/gsd/skill-discovery.ts:83` — `formatSkillsXml(skills: DiscoveredSkill[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param skills, @returns)

### `src/resources/extensions/gsd/skill-health.ts`
- `src/resources/extensions/gsd/skill-health.ts:84` — `generateSkillHealthReport(basePath: string, staleDays?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param staleDays, @returns)
- `src/resources/extensions/gsd/skill-health.ts:108` — `formatSkillHealthReport(report: SkillHealthReport)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push("Skill Health Report"); lines.push("═".repeat(60)); lines.push(`Generated: ${report.generatedAt}`); lines.… (missing: @param report, @returns)
- `src/resources/extensions/gsd/skill-health.ts:177` — `formatSkillDetail(basePath: string, skillName: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param skillName, @returns)
- `src/resources/extensions/gsd/skill-health.ts:236` — `buildHealSkillPrompt(unitId: string)` — exported: **yes** — Implements logic starting with: { return `## Skill Heal Analysis Analyze the just-completed unit (${unitId}) for skill drift. ### Steps 1. **Identify loaded skill**: Check which S… (missing: @param unitId, @returns)
- `src/resources/extensions/gsd/skill-health.ts:284` — `computeStaleAvoidList(basePath: string, currentAvoidList: string[], staleDays?: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param currentAvoidList, @param staleDays, @returns)

### `src/resources/extensions/gsd/skill-manifest.ts`
- `src/resources/extensions/gsd/skill-manifest.ts:21` — `normalize(name: string)` — exported: **no** — Implements logic starting with: { return name.trim().toLowerCase(); } (missing: @param name, @returns)
- `src/resources/extensions/gsd/skill-manifest.ts:131` — `resolveSkillManifest(unitType: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param unitType)
- `src/resources/extensions/gsd/skill-manifest.ts:142` — `filterSkillsByManifest(skills: T[], unitType: string | undefined)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param skills, @param unitType, @returns)

### `src/resources/extensions/gsd/skill-telemetry.ts`
- `src/resources/extensions/gsd/skill-telemetry.ts:51` — `recordSkillRead(skillName: string)` — exported: **yes** — Implements logic starting with: { activelyLoadedSkills.add(skillName); } (missing: @param skillName)
- `src/resources/extensions/gsd/skill-telemetry.ts:61` — `getAndClearSkills()` — exported: **yes** — Implements logic starting with: { const result = activelyLoadedSkills.size > 0 ? Array.from(activelyLoadedSkills) : [...availableSkills]; availableSkills = []; activelyLoadedSkill… (missing: @returns)
- `src/resources/extensions/gsd/skill-telemetry.ts:82` — `getSkillLastUsed(units: Array<{ finishedAt: number; skills?: string[] }>)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param units, @returns)
- `src/resources/extensions/gsd/skill-telemetry.ts:100` — `detectStaleSkills(units: Array<{ finishedAt: number; skills?: string[] }>, thresholdDays: number)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param units, @param thresholdDays, @returns)

### `src/resources/extensions/gsd/slice-cadence.ts`
- `src/resources/extensions/gsd/slice-cadence.ts:51` — `milestoneBranchName(milestoneId: string)` — exported: **no** — Implements logic starting with: { return `milestone/${milestoneId}`; } (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/slice-cadence.ts:190` — `mergeSliceToMain(projectRoot: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const started = Date.now(); const worktreeCwd = process.cwd(); const milestoneBranch = milestoneBranchName(milestoneId); const mainBranch = resol… (missing: @param projectRoot, @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/slice-cadence.ts:321` — `resquashMilestoneOnMain(projectRoot: string, milestoneId: string, startSha: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param projectRoot, @param milestoneId, @param startSha, @returns)
- `src/resources/extensions/gsd/slice-cadence.ts:399` — `getCollapseCadence(prefs: { git?: { collapse_cadence?: "milestone" | "slice" } } | undefined | null)` — exported: **yes** — Implements logic starting with: { return prefs?.git?.collapse_cadence ?? "milestone"; } (missing: @param prefs, @returns)

### `src/resources/extensions/gsd/slice-parallel-conflict.ts`
- `src/resources/extensions/gsd/slice-parallel-conflict.ts:20` — `extractFilePaths(content: string)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param content, @returns)

### `src/resources/extensions/gsd/slice-parallel-orchestrator.ts`
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:286` — `restoreSliceState(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:336` — `isSliceParallelActive(basePath?: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:374` — `getSliceOrchestratorState()` — exported: **yes** — Implements logic starting with: { return sliceState; } (missing: @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:384` — `startSliceParallel(basePath: string, milestoneId: string, eligibleSlices: Array<{ id: string }>, opts: StartSliceParallelOpts = {})` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param basePath, @param milestoneId, @param eligibleSlices, @param opts, @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:521` — `getSliceAggregateCost()` — exported: **yes** — Implements logic starting with: { if (!sliceState) return 0; let total = 0; for (const w of sliceState.workers.values()) { total += w.cost; } return total; } (missing: @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:533` — `isSliceBudgetExceeded()` — exported: **yes** — Implements logic starting with: { if (!sliceState?.budgetCeiling) return false; return getSliceAggregateCost() >= sliceState.budgetCeiling; } (missing: @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:558` — `filterConflictingSlices(basePath: string, milestoneId: string, slices: Array<{ id: string }>, useExecutionGraph: boolean)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param milestoneId, @param slices, @param useExecutionGraph, @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:599` — `resolveGsdBin()` — exported: **no** — Implements logic starting with: { if (process.env.GSD_BIN_PATH && existsSync(process.env.GSD_BIN_PATH)) { return process.env.GSD_BIN_PATH; } let thisDir: string; try { thisDir = d… (missing: @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:631` — `spawnSliceWorker(basePath: string, milestoneId: string, sliceId: string)` — exported: **no** — Executes a subprocess command. (missing: @param basePath, @param milestoneId, @param sliceId, @returns)
- `src/resources/extensions/gsd/slice-parallel-orchestrator.ts:768` — `processSliceWorkerLine(_basePath: string, _milestoneId: string, sliceId: string, line: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param _basePath, @param _milestoneId, @param sliceId, @param line)

### `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts:59` — `abortAndResetMerge(basePath: string, hasMergeHead: boolean, squashMsgPath: string)` — exported: **no** — Implements logic starting with: { if (hasMergeHead) { try { nativeMergeAbort(basePath); } catch (err) { /* best-effort */ logWarning( "recovery", `git merge-abort failed: ${err in… (missing: @param basePath, @param hasMergeHead, @param squashMsgPath)
- `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts:105` — `reconcileOtherInProgressGitOps(basePath: string, notify: NotifyFn)` — exported: **no** — Validates inputs and throws on failure. (missing: @param basePath, @param notify, @returns)
- `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts:181` — `reconcileMergeStateCore(basePath: string, notify: NotifyFn)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @param notify, @returns)
- `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts:281` — `reconcileMergeState(basePath: string, ctx: ExtensionContext)` — exported: **yes** — Implements logic starting with: { return reconcileMergeStateCore(basePath, (message, severity) => ctx.ui.notify(message, severity), ); } (missing: @param basePath, @param ctx, @returns)
- `src/resources/extensions/gsd/state-reconciliation/drift/merge-state.ts:322` — `repairMergeStateDrift(record: MergeStateDrift)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param record)

### `src/resources/extensions/gsd/state-reconciliation/drift/project-md.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/project-md.ts:58` — `repairUnregisteredMilestone(_record: UnregisteredMilestoneDrift, ctx: DriftContext)` — exported: **yes** — Implements logic starting with: { migrateHierarchyToDb(ctx.basePath); } (missing: @param _record, @param ctx)

### `src/resources/extensions/gsd/state-reconciliation/drift/roadmap.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/roadmap.ts:86` — `repairRoadmapDivergence(record: RoadmapDivergenceDrift, ctx: DriftContext)` — exported: **yes** — Performs filesystem I/O. (missing: @param record, @param ctx)

### `src/resources/extensions/gsd/state-reconciliation/drift/sketch-flag.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/sketch-flag.ts:57` — `autoHealSketchFlags(milestoneId: string, hasPlanFile: (sliceId: string) => boolean)` — exported: **yes** — Implements logic starting with: { if (!isDbAvailable()) return; const sliceIds = getSketchedSliceIds(milestoneId); for (const sid of sliceIds) { if (hasPlanFile(sid)) { setSliceSk… (missing: @param milestoneId, @param hasPlanFile)

### `src/resources/extensions/gsd/state-reconciliation/drift/stale-render.ts`
- `src/resources/extensions/gsd/state-reconciliation/drift/stale-render.ts:161` — `repairStaleRenders(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/state-reconciliation/errors.ts`
- `src/resources/extensions/gsd/state-reconciliation/errors.ts:35` — `class ReconciliationFailedError` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/gsd/state-reconciliation/index.ts`
- `src/resources/extensions/gsd/state-reconciliation/index.ts:56` — `reconcileBeforeDispatch(basePath: string, deps: ReconciliationDeps = defaultDeps)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param basePath, @param deps, @returns)

### `src/resources/extensions/gsd/state-reconciliation/spawn-gate.ts`
- `src/resources/extensions/gsd/state-reconciliation/spawn-gate.ts:33` — `reconcileBeforeSpawn(basePath: string, deps: SpawnGateDeps = {})` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param deps, @returns)

### `src/resources/extensions/gsd/state.ts`
- `src/resources/extensions/gsd/state.ts:84` — `isGhostMilestone(basePath: string, mid: string)` — exported: **yes** — Implements logic starting with: { // If the milestone has a DB row, it's usually a known milestone — not a ghost. // Exception: a "queued" row with no disk artifacts is a phantom… (missing: @param basePath, @param mid, @returns)
- `src/resources/extensions/gsd/state.ts:137` — `isReusableGhostMilestone(basePath: string, mid: string)` — exported: **yes** — Implements logic starting with: { // Condition 1: no DB row (any status). if (!isDbAvailable()) return false; const dbRow = getMilestone(mid); if (dbRow != null) return false; //… (missing: @param basePath, @param mid, @returns)
- `src/resources/extensions/gsd/state.ts:161` — `isSliceComplete(plan: SlicePlan)` — exported: **yes** — Implements logic starting with: { return plan.tasks.length > 0 && plan.tasks.every(t => t.done); } (missing: @param plan, @returns)
- `src/resources/extensions/gsd/state.ts:168` — `isMilestoneComplete(roadmap: Roadmap)` — exported: **yes** — Implements logic starting with: { return roadmap.slices.length > 0 && roadmap.slices.every(s => s.done); } (missing: @param roadmap, @returns)
- `src/resources/extensions/gsd/state.ts:178` — `isValidationTerminal(validationContent: string)` — exported: **yes** — Implements logic starting with: { return extractVerdict(validationContent) != null; } (missing: @param validationContent, @returns)
- `src/resources/extensions/gsd/state.ts:222` — `getActiveMilestoneId(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/state.ts:298` — `deriveState(basePath: string, opts?: DeriveStateOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @param opts, @returns)
- `src/resources/extensions/gsd/state.ts:369` — `stripMilestonePrefix(title: string)` — exported: **no** — Implements logic starting with: { return title.replace(/^M\d+(?:-[a-z0-9]{6})?[^:]*:\s*/, '') || title; } (missing: @param title, @returns)
- `src/resources/extensions/gsd/state.ts:393` — `buildCompletenessSet(basePath: string, milestones: MilestoneRow[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param basePath, @param milestones, @returns)
- `src/resources/extensions/gsd/state.ts:921` — `cachedLoadFile(path: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param path, @returns)

### `src/resources/extensions/gsd/status-guards.ts`
- `src/resources/extensions/gsd/status-guards.ts:12` — `isClosedStatus(status: string)` — exported: **yes** — Implements logic starting with: { return status === "complete" || status === "done" || status === "skipped"; } (missing: @param status, @returns)
- `src/resources/extensions/gsd/status-guards.ts:17` — `isDeferredStatus(status: string)` — exported: **yes** — Implements logic starting with: { return status === "deferred"; } (missing: @param status, @returns)
- `src/resources/extensions/gsd/status-guards.ts:25` — `isInactiveStatus(status: string)` — exported: **yes** — Implements logic starting with: { return isClosedStatus(status) || isDeferredStatus(status); } (missing: @param status, @returns)

### `src/resources/extensions/gsd/structured-data-formatter.ts`
- `src/resources/extensions/gsd/structured-data-formatter.ts:56` — `formatDecisionCompact(decision: DecisionInput)` — exported: **yes** — Implements logic starting with: { return [ decision.id, decision.when_context, decision.scope, decision.decision, decision.choice, decision.rationale, decision.revisable, decision… (missing: @param decision, @returns)
- `src/resources/extensions/gsd/structured-data-formatter.ts:70` — `formatDecisionsCompact(decisions: DecisionInput[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param decisions, @returns)
- `src/resources/extensions/gsd/structured-data-formatter.ts:85` — `formatRequirementCompact(req: RequirementInput)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push(`${req.id} [${req.class}] (${req.status}) owner:${req.primary_owner}`); lines.push(` ${req.description}`);… (missing: @param req, @returns)
- `src/resources/extensions/gsd/structured-data-formatter.ts:95` — `formatRequirementsCompact(requirements: RequirementInput[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param requirements, @returns)
- `src/resources/extensions/gsd/structured-data-formatter.ts:110` — `formatTaskPlanCompact(tasks: TaskPlanInput[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param tasks, @returns)
- `src/resources/extensions/gsd/structured-data-formatter.ts:142` — `measureSavings(compactContent: string, markdownContent: string)` — exported: **yes** — Implements logic starting with: { if (markdownContent.length === 0) return 0; const saved = markdownContent.length - compactContent.length; return (saved / markdownContent.length)… (missing: @param compactContent, @param markdownContent, @returns)

### `src/resources/extensions/gsd/sync-lock.ts`
- `src/resources/extensions/gsd/sync-lock.ts:26` — `isPidAlive(pid: number)` — exported: **no** — Implements logic starting with: { try { process.kill(pid, 0); return true; } catch (err) { if ((err as NodeJS.ErrnoException).code === "EPERM") return true; return false; } } (missing: @param pid, @returns)
- `src/resources/extensions/gsd/sync-lock.ts:41` — `tryCreateLockFile(lp: string, payload: string)` — exported: **no** — Implements logic starting with: { // Ensure parent dir exists (`atomicWriteSync` previously did this implicitly). try { mkdirSync(dirname(lp), { recursive: true }); } catch { /* b… (missing: @param lp, @param payload, @returns)
- `src/resources/extensions/gsd/sync-lock.ts:79` — `acquireSyncLock(basePath: string, timeoutMs: number = DEFAULT_TIMEOUT_MS)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param timeoutMs, @returns)
- `src/resources/extensions/gsd/sync-lock.ts:143` — `releaseSyncLock(basePath: string)` — exported: **yes** — Implements logic starting with: { const lp = lockFilePath(basePath); try { if (existsSync(lp)) { unlinkSync(lp); } } catch { // Non-fatal — lock may have been released by another… (missing: @param basePath)

### `src/resources/extensions/gsd/tests/auto-loop.test.ts`
- `src/resources/extensions/gsd/tests/auto-loop.test.ts:63` — `makeMockSession(opts?: { newSessionResult?: { cancelled: boolean }; newSessionThrows?: string; newSessionDelayMs?: number; onNewSessionStart?: (session: any) => void; onNewSessionSettle?: (session: any) => void; /** Called after the delay with the aborted state of any passed abortSignal. * Used to verify that runUnit passes an aborted signal on late resolution (#3731). */ onSignalCheck?: (aborted: boolean) => void; })` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param opts, @returns)
- `src/resources/extensions/gsd/tests/auto-loop.test.ts:111` — `makeMockCtx()` — exported: **no** — Implements logic starting with: { return { ui: { notify: () => {} }, model: { id: "test-model" }, } as any; } (missing: @returns)
- `src/resources/extensions/gsd/tests/auto-loop.test.ts:121` — `makeMockPi()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/gsd/tests/auto-loop.test.ts:737` — `makeMockDeps(overrides?: Partial<LoopDeps>)` — exported: **no** — Performs filesystem I/O. (missing: @param overrides, @returns)
- `src/resources/extensions/gsd/tests/auto-loop.test.ts:876` — `makeLoopSession(overrides?: Partial<Record<string, unknown>>)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/auto-session-scope.test.ts`
- `src/resources/extensions/gsd/tests/auto-session-scope.test.ts:35` — `applyRebuildScope(s: AutoSession, rawPath: string, milestoneId: string | null)` — exported: **no** — Implements logic starting with: { if (!milestoneId) { s.scope = null; return; } try { const workspace = createWorkspace(rawPath); s.scope = scopeMilestone(workspace, milestoneId);… (missing: @param s, @param rawPath, @param milestoneId)

### `src/resources/extensions/gsd/tests/canonical-milestone-root.test.ts`
- `src/resources/extensions/gsd/tests/canonical-milestone-root.test.ts:36` — `makeLiveWorktree(base: string, mid: string)` — exported: **no** — Performs filesystem I/O. (missing: @param base, @param mid, @returns)

### `src/resources/extensions/gsd/tests/commands-workflow-custom.test.ts`
- `src/resources/extensions/gsd/tests/commands-workflow-custom.test.ts:76` — `writeDefinition(basePath: string, name: string, content: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param name, @param content)

### `src/resources/extensions/gsd/tests/complete-milestone.test.ts`
- `src/resources/extensions/gsd/tests/complete-milestone.test.ts:24` — `loadPromptFromWorktree(name: string, vars: Record<string, string> = {})` — exported: **no** — Performs filesystem I/O. (missing: @param name, @param vars, @returns)

### `src/resources/extensions/gsd/tests/complete-slice-string-coercion.test.ts`
- `src/resources/extensions/gsd/tests/complete-slice-string-coercion.test.ts:24` — `splitPair(s: string)` — exported: **no** — Implements logic starting with: { const m = s.match(/^(.+?)\s*(?:—|-)\s+(.+)$/); return m ? [m[1].trim(), m[2].trim()] : [s.trim(), ""]; } (missing: @param s, @returns)
- `src/resources/extensions/gsd/tests/complete-slice-string-coercion.test.ts:134` — `wrapArray(v: any)` — exported: **no** — Implements logic starting with: { return v == null ? [] : Array.isArray(v) ? v : [v]; } (missing: @param v, @returns)

### `src/resources/extensions/gsd/tests/complete-slice.test.ts`
- `src/resources/extensions/gsd/tests/complete-slice.test.ts:56` — `createTempProject()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/complete-task.test.ts`
- `src/resources/extensions/gsd/tests/complete-task.test.ts:56` — `createTempProject()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/context-injector.test.ts`
- `src/resources/extensions/gsd/tests/context-injector.test.ts:19` — `makeTempRun(def: WorkflowDefinition, files?: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param def, @param files, @returns)
- `src/resources/extensions/gsd/tests/context-injector.test.ts:39` — `makeDef(steps: WorkflowDefinition["steps"])` — exported: **no** — Implements logic starting with: { return { version: 1, name: "test-workflow", steps, }; } (missing: @param steps, @returns)

### `src/resources/extensions/gsd/tests/context-masker.test.ts`
- `src/resources/extensions/gsd/tests/context-masker.test.ts:18` — `toolResult(text: string)` — exported: **no** — Implements logic starting with: { return { role: "toolResult", content: [{ type: "text", text }], toolCallId: "toolu_test", toolName: "Read", isError: false }; } (missing: @param text, @returns)
- `src/resources/extensions/gsd/tests/context-masker.test.ts:23` — `bashResult(text: string)` — exported: **no** — Implements logic starting with: { return { role: "user", content: [{ type: "text", text: `Ran \`echo test\`\n\`\`\`\n${text}\n\`\`\`` }] }; } (missing: @param text, @returns)

### `src/resources/extensions/gsd/tests/crash-recovery-via-db.test.ts`
- `src/resources/extensions/gsd/tests/crash-recovery-via-db.test.ts:47` — `expireWorker(workerId: string)` — exported: **no** — Implements logic starting with: { const db = _getAdapter()!; db.prepare( `UPDATE workers SET last_heartbeat_at = '1970-01-01T00:00:00.000Z' WHERE worker_id = :w`, ).run({ ":w": wo… (missing: @param workerId)

### `src/resources/extensions/gsd/tests/crash-recovery.test.ts`
- `src/resources/extensions/gsd/tests/crash-recovery.test.ts:61` — `writeTestLock(base: string, unitType: string, unitId: string, sessionFile?: string)` — exported: **no** — Implements logic starting with: { const projectRoot = normalizeRealPath(base); const workerId = `test-fake-${randomUUID().slice(0, 8)}`; const fakePid = 999999999; const stalePast… (missing: @param base, @param unitType, @param unitId, @param sessionFile)

### `src/resources/extensions/gsd/tests/custom-engine-loop-integration.test.ts`
- `src/resources/extensions/gsd/tests/custom-engine-loop-integration.test.ts:71` — `writeDefinition(runDir: string, steps: GraphStep[], name = "test-wf")` — exported: **no** — Performs filesystem I/O. (missing: @param runDir, @param steps, @param name)

### `src/resources/extensions/gsd/tests/custom-verification.test.ts`
- `src/resources/extensions/gsd/tests/custom-verification.test.ts:21` — `makeTempRun(def: WorkflowDefinition, files?: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param def, @param files, @returns)
- `src/resources/extensions/gsd/tests/custom-verification.test.ts:42` — `makeDef(steps: WorkflowDefinition["steps"])` — exported: **no** — Implements logic starting with: { return { version: 1, name: "test-workflow", steps, }; } (missing: @param steps, @returns)

### `src/resources/extensions/gsd/tests/custom-workflow-engine.test.ts`
- `src/resources/extensions/gsd/tests/custom-workflow-engine.test.ts:55` — `setupEngine(steps: GraphStep[], name = "test-wf")` — exported: **no** — Performs filesystem I/O. (missing: @param steps, @param name, @returns)

### `src/resources/extensions/gsd/tests/dashboard-budget.test.ts`
- `src/resources/extensions/gsd/tests/dashboard-budget.test.ts:45` — `renderCompletedBudgetMarkers(completedUnit: { type: string; id: string }, ledgerUnits: UnitMetrics[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param completedUnit, @param ledgerUnits, @returns)
- `src/resources/extensions/gsd/tests/dashboard-budget.test.ts:72` — `renderCostBudgetLine(units: UnitMetrics[])` — exported: **no** — Implements logic starting with: { const totals = getProjectTotals(units); if (totals.totalTruncationSections > 0 || totals.continueHereFiredCount > 0) { const parts: string[] = []… (missing: @param units, @returns)
- `src/resources/extensions/gsd/tests/dashboard-budget.test.ts:91` — `renderModelContextWindow(units: UnitMetrics[], modelName: string)` — exported: **no** — Implements logic starting with: { const models = aggregateByModel(units); const m = models.find(agg => agg.model === modelName); if (!m) return null; if (m.contextWindowTokens !==… (missing: @param units, @param modelName, @returns)

### `src/resources/extensions/gsd/tests/definition-loader.test.ts`
- `src/resources/extensions/gsd/tests/definition-loader.test.ts:31` — `writeDefYaml(yaml: string, name = "test-workflow")` — exported: **no** — Performs filesystem I/O. (missing: @param yaml, @param name, @returns)

### `src/resources/extensions/gsd/tests/derive-state-crossval.test.ts`
- `src/resources/extensions/gsd/tests/derive-state-crossval.test.ts:48` — `assertStatesEqual(dbState: GSDState, fileState: GSDState, prefix: string)` — exported: **no** — Serializes a value to JSON. (missing: @param dbState, @param fileState, @param prefix)

### `src/resources/extensions/gsd/tests/derive-state-deps.test.ts`
- `src/resources/extensions/gsd/tests/derive-state-deps.test.ts:38` — `writeContext(base: string, mid: string, frontmatter: string)` — exported: **no** — Performs filesystem I/O. (missing: @param base, @param mid, @param frontmatter)

### `src/resources/extensions/gsd/tests/dispatch-guard.test.ts`
- `src/resources/extensions/gsd/tests/dispatch-guard.test.ts:10` — `setupRepo()` — exported: **no** — Implements logic starting with: { const repo = mkdtempSync(join(tmpdir(), "gsd-dispatch-guard-")); mkdirSync(join(repo, ".gsd"), { recursive: true }); openDatabase(join(repo, ".gs… (missing: @returns)
- `src/resources/extensions/gsd/tests/dispatch-guard.test.ts:18` — `teardownRepo(repo: string)` — exported: **no** — Implements logic starting with: { closeDatabase(); rmSync(repo, { recursive: true, force: true }); } (missing: @param repo)

### `src/resources/extensions/gsd/tests/enhanced-verification-integration.test.ts`
- `src/resources/extensions/gsd/tests/enhanced-verification-integration.test.ts:46` — `createTask(overrides: Partial<TaskRow> = {})` — exported: **no** — Implements logic starting with: { return { milestone_id: "M001", slice_id: "S01", id: overrides.id ?? "T01", title: overrides.title ?? "Test Task", status: overrides.status ?? "pe… (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/forensics-error-filter.test.ts`
- `src/resources/extensions/gsd/tests/forensics-error-filter.test.ts:14` — `makeToolPair(toolName: string, input: Record<string, unknown>, resultText: string, isError: boolean)` — exported: **no** — Implements logic starting with: { const toolCallId = `toolu_${Math.random().toString(36).slice(2, 10)}`; return [ { type: "message", message: { role: "assistant", content: [ { typ… (missing: @param toolName, @param input, @param resultText, @param isError, @returns)

### `src/resources/extensions/gsd/tests/freeform-decisions.test.ts`
- `src/resources/extensions/gsd/tests/freeform-decisions.test.ts:41` — `queryAllDecisions()` — exported: **no** — Transforms a collection into a derived result. (missing: @returns)

### `src/resources/extensions/gsd/tests/gate-1b-orphan-discrimination.test.ts`
- `src/resources/extensions/gsd/tests/gate-1b-orphan-discrimination.test.ts:64` — `mkBase()` — exported: **no** — Implements logic starting with: { const base = mkdtempSync(join(tmpdir(), "gsd-gate1b-")); mkdirSync(join(base, ".gsd", "milestones", "M001"), { recursive: true }); return base; } (missing: @returns)

### `src/resources/extensions/gsd/tests/gate-1b-recovery-bound-corrections.test.ts`
- `src/resources/extensions/gsd/tests/gate-1b-recovery-bound-corrections.test.ts:47` — `mkPiThrowOnce(cap: MockCapture)` — exported: **no** — Validates inputs and throws on failure. (missing: @param cap, @returns)

### `src/resources/extensions/gsd/tests/graph-context.test.ts`
- `src/resources/extensions/gsd/tests/graph-context.test.ts:57` — `staleTimestamp(hoursAgo = 26)` — exported: **no** — Implements logic starting with: { return new Date(Date.now() - hoursAgo * 60 * 60 * 1000).toISOString(); } (missing: @param hoursAgo, @returns)
- `src/resources/extensions/gsd/tests/graph-context.test.ts:62` — `freshTimestamp()` — exported: **no** — Implements logic starting with: { return new Date(Date.now() - 30 * 60 * 1000).toISOString(); // 30 minutes ago } (missing: @returns)
- `src/resources/extensions/gsd/tests/graph-context.test.ts:70` — `makeProjectDir(fixture: GraphFixture)` — exported: **no** — Performs filesystem I/O. (missing: @param fixture, @returns)
- `src/resources/extensions/gsd/tests/graph-context.test.ts:87` — `cleanup(dir: string)` — exported: **no** — Implements logic starting with: { try { rmSync(dir, { recursive: true, force: true }); } catch { /* ignore */ } } (missing: @param dir)
- `src/resources/extensions/gsd/tests/graph-context.test.ts:92` — `makeNode(overrides: Partial<TestNode> & { id: string; label: string })` — exported: **no** — Implements logic starting with: { return { type: "CLASS", confidence: "INFERRED", ...overrides, }; } (missing: @param overrides, @returns)
- `src/resources/extensions/gsd/tests/graph-context.test.ts:101` — `makeEdge(overrides: Partial<TestEdge> & { from: string; to: string })` — exported: **no** — Implements logic starting with: { return { type: "CALLS", confidence: "INFERRED", ...overrides, }; } (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/graph-operations.test.ts`
- `src/resources/extensions/gsd/tests/graph-operations.test.ts:38` — `makeGraph(steps: GraphStep[], name = "test-workflow")` — exported: **no** — Implements logic starting with: { return { steps, metadata: { name, createdAt: "2026-01-01T00:00:00.000Z" }, }; } (missing: @param steps, @param name, @returns)

### `src/resources/extensions/gsd/tests/gsd-db-workspace-scope.test.ts`
- `src/resources/extensions/gsd/tests/gsd-db-workspace-scope.test.ts:31` — `makeProjectDir()` — exported: **no** — Implements logic starting with: { const dir = realpathSync(mkdtempSync(join(tmpdir(), "gsd-db-ws-scope-"))); // hasGsdBootstrapArtifacts checks for .gsd/milestones or .gsd/PREFERE… (missing: @returns)
- `src/resources/extensions/gsd/tests/gsd-db-workspace-scope.test.ts:43` — `makeWorktreeDir(projectDir: string, mid: string)` — exported: **no** — Implements logic starting with: { const worktreeDir = join(projectDir, ".gsd", "worktrees", mid); mkdirSync(worktreeDir, { recursive: true }); return worktreeDir; } (missing: @param projectDir, @param mid, @returns)

### `src/resources/extensions/gsd/tests/help-menu-coverage.test.ts`
- `src/resources/extensions/gsd/tests/help-menu-coverage.test.ts:13` — `extractHelpCommands(lines: string[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param lines, @returns)

### `src/resources/extensions/gsd/tests/integration/auto-secrets-gate.test.ts`
- `src/resources/extensions/gsd/tests/integration/auto-secrets-gate.test.ts:29` — `writeManifest(base: string, content: string)` — exported: **no** — Performs filesystem I/O. (missing: @param base, @param content)
- `src/resources/extensions/gsd/tests/integration/auto-secrets-gate.test.ts:36` — `makeNoUICtx(cwd: string)` — exported: **no** — Implements logic starting with: { return { ui: {}, hasUI: false, cwd, }; } (missing: @param cwd, @returns)

### `src/resources/extensions/gsd/tests/integration/auto-worktree-milestone-merge.test.ts`
- `src/resources/extensions/gsd/tests/integration/auto-worktree-milestone-merge.test.ts:79` — `makeRoadmap(milestoneId: string, title: string, slices: Array<{ id: string; title: string }>)` — exported: **no** — Transforms a collection into a derived result. (missing: @param milestoneId, @param title, @param slices, @returns)
- `src/resources/extensions/gsd/tests/integration/auto-worktree-milestone-merge.test.ts:85` — `addSliceToMilestone(repo: string, wtPath: string, milestoneId: string, sliceId: string, sliceTitle: string, commits: Array<{ file: string; content: string; message: string }>)` — exported: **no** — Performs filesystem I/O. (missing: @param repo, @param wtPath, @param milestoneId, @param sliceId, @param sliceTitle, @param commits)

### `src/resources/extensions/gsd/tests/integration/doctor-environment-worktree.test.ts`
- `src/resources/extensions/gsd/tests/integration/doctor-environment-worktree.test.ts:25` — `createDir(files: Record<string, string> = {})` — exported: **no** — Performs filesystem I/O. (missing: @param files, @returns)

### `src/resources/extensions/gsd/tests/integration/doctor-fixlevel.test.ts`
- `src/resources/extensions/gsd/tests/integration/doctor-fixlevel.test.ts:32` — `buildScaffold(base: string)` — exported: **no** — Performs filesystem I/O. (missing: @param base)

### `src/resources/extensions/gsd/tests/integration/doctor-git.test.ts`
- `src/resources/extensions/gsd/tests/integration/doctor-git.test.ts:26` — `createRepoWithCompletedMilestone()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/tests/integration/doctor-git.test.ts:81` — `createRepoWithSlicesDoneButNoMilestoneSummary()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/tests/integration/doctor-git.test.ts:122` — `writePreferencesFile(dir: string, isolation: "none" | "worktree" | "branch")` — exported: **no** — Performs filesystem I/O. (missing: @param dir, @param isolation)
- `src/resources/extensions/gsd/tests/integration/doctor-git.test.ts:129` — `createRepoWithActiveMilestone()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/integration/doctor-runtime.test.ts`
- `src/resources/extensions/gsd/tests/integration/doctor-runtime.test.ts:23` — `createMinimalProject()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/tests/integration/doctor-runtime.test.ts:46` — `createGitProject()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/integration/e2e-workflow-pipeline-integration.test.ts`
- `src/resources/extensions/gsd/tests/integration/e2e-workflow-pipeline-integration.test.ts:61` — `dispatch(engine: CustomWorkflowEngine)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param engine, @returns)
- `src/resources/extensions/gsd/tests/integration/e2e-workflow-pipeline-integration.test.ts:67` — `reconcile(engine: CustomWorkflowEngine, unitId: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param engine, @param unitId, @returns)
- `src/resources/extensions/gsd/tests/integration/e2e-workflow-pipeline-integration.test.ts:146` — `setupProject(overrides?: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/integration/feature-branch-lifecycle-integration.test.ts`
- `src/resources/extensions/gsd/tests/integration/feature-branch-lifecycle-integration.test.ts:68` — `createFeatureBranchRepo(featureBranch: string)` — exported: **no** — Performs filesystem I/O. (missing: @param featureBranch, @returns)
- `src/resources/extensions/gsd/tests/integration/feature-branch-lifecycle-integration.test.ts:106` — `addSliceToMilestone(wtPath: string, milestoneId: string, sliceId: string, sliceTitle: string, commits: Array<{ file: string; content: string; message: string }>)` — exported: **no** — Performs filesystem I/O. (missing: @param wtPath, @param milestoneId, @param sliceId, @param sliceTitle, @param commits)

### `src/resources/extensions/gsd/tests/integration/integration-proof.test.ts`
- `src/resources/extensions/gsd/tests/integration/integration-proof.test.ts:119` — `createRealisticFixture()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts`
- `src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts:100` — `setupBranchIsolation(repo: string)` — exported: **no** — Performs filesystem I/O. (missing: @param repo)
- `src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts:131` — `setupRoadmap(repo: string, mid: string, title: string, slices: string[])` — exported: **no** — Performs filesystem I/O. (missing: @param repo, @param mid, @param title, @param slices)
- `src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts:146` — `createMilestoneBranch(repo: string, mid: string, files: Array<{ name: string; content: string }>)` — exported: **no** — Performs filesystem I/O. (missing: @param repo, @param mid, @param files)
- `src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts:568` — `setupCanonicalDbWithWorktree(basePath: string, mid: string)` — exported: **no** — Implements logic starting with: { mkdirSync(join(basePath, ".gsd", "worktrees", mid), { recursive: true }); mkdirSync(join(basePath, ".gsd"), { recursive: true }); const dbPath =… (missing: @param basePath, @param mid)

### `src/resources/extensions/gsd/tests/integration/paths.test.ts`
- `src/resources/extensions/gsd/tests/integration/paths.test.ts:10` — `tmp()` — exported: **no** — Implements logic starting with: { const p = mkdtempSync(join(tmpdir(), "gsd-paths-test-")); try { return realpathSync.native(p); } catch { return p; } } (missing: @returns)

### `src/resources/extensions/gsd/tests/integration/state-machine-edge-cases.test.ts`
- `src/resources/extensions/gsd/tests/integration/state-machine-edge-cases.test.ts:101` — `createFullFixture()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/tests/integration/state-machine-edge-cases.test.ts:196` — `createMultiMilestoneFixture()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/integration/state-machine-live-validation.test.ts`
- `src/resources/extensions/gsd/tests/integration/state-machine-live-validation.test.ts:86` — `createFullFixture()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/integration/token-savings.test.ts`
- `src/resources/extensions/gsd/tests/integration/token-savings.test.ts:31` — `generateDecisionsMarkdown(count: number, milestones: string[])` — exported: **no** — Implements logic starting with: { const lines: string[] = [ '# Decisions Register', '', '<!-- Append-only. Never edit or remove existing rows. -->', '', '| # | When | Scope | Deci… (missing: @param count, @param milestones, @returns)
- `src/resources/extensions/gsd/tests/integration/token-savings.test.ts:62` — `generateRequirementsMarkdown(count: number, sliceAssignments: { milestone: string; slice: string }[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param count, @param sliceAssignments, @returns)

### `src/resources/extensions/gsd/tests/issue-4540-regressions.test.ts`
- `src/resources/extensions/gsd/tests/issue-4540-regressions.test.ts:41` — `createBrokenV12Db(dbPath: string)` — exported: **no** — Executes a subprocess command. (missing: @param dbPath)

### `src/resources/extensions/gsd/tests/iterate-engine-integration.test.ts`
- `src/resources/extensions/gsd/tests/iterate-engine-integration.test.ts:51` — `makeTempRun(def: WorkflowDefinition, graphSteps: GraphStep[], files?: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param def, @param graphSteps, @param files, @returns)
- `src/resources/extensions/gsd/tests/iterate-engine-integration.test.ts:81` — `makeStep(overrides: Partial<GraphStep> & { id: string })` — exported: **no** — Implements logic starting with: { return { title: overrides.id, status: "pending", prompt: `Do ${overrides.id}`, dependsOn: [], ...overrides, }; } (missing: @param overrides, @returns)
- `src/resources/extensions/gsd/tests/iterate-engine-integration.test.ts:92` — `dispatch(engine: CustomWorkflowEngine)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param engine, @returns)
- `src/resources/extensions/gsd/tests/iterate-engine-integration.test.ts:98` — `reconcile(engine: CustomWorkflowEngine, unitId: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param engine, @param unitId, @returns)

### `src/resources/extensions/gsd/tests/journal-integration.test.ts`
- `src/resources/extensions/gsd/tests/journal-integration.test.ts:36` — `createEventCapture()` — exported: **no** — Implements logic starting with: { const events: JournalEntry[] = []; return { events, emitJournalEvent: (entry: JournalEntry) => { events.push(entry); }, }; } (missing: @returns)
- `src/resources/extensions/gsd/tests/journal-integration.test.ts:45` — `makeMockDeps(capture: ReturnType<typeof createEventCapture>, overrides?: Partial<LoopDeps>)` — exported: **no** — Performs filesystem I/O. (missing: @param capture, @param overrides, @returns)
- `src/resources/extensions/gsd/tests/journal-integration.test.ts:148` — `makeIC(deps: LoopDeps, overrides?: Partial<IterationContext>)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param deps, @param overrides, @returns)
- `src/resources/extensions/gsd/tests/journal-integration.test.ts:175` — `makeSession()` — exported: **no** — Builds an in-memory lookup structure. (missing: @returns)

### `src/resources/extensions/gsd/tests/manifest-status.test.ts`
- `src/resources/extensions/gsd/tests/manifest-status.test.ts:25` — `writeManifest(base: string, content: string)` — exported: **no** — Performs filesystem I/O. (missing: @param base, @param content)

### `src/resources/extensions/gsd/tests/markdown-renderer.test.ts`
- `src/resources/extensions/gsd/tests/markdown-renderer.test.ts:70` — `scaffoldDirs(tmpDir: string, mid: string, sliceIds: string[])` — exported: **no** — Performs filesystem I/O. (missing: @param tmpDir, @param mid, @param sliceIds)

### `src/resources/extensions/gsd/tests/merge-conflict-stops-loop.test.ts`
- `src/resources/extensions/gsd/tests/merge-conflict-stops-loop.test.ts:65` — `makeResolver(s: AutoSession, deps: LegacyTestDeps)` — exported: **no** — Implements logic starting with: { const lifecycle = new WorktreeLifecycle(s, deps); return { mergeAndExit: (mid: string, ctx: { notify: (msg: string, level?: "info" | "warning" |… (missing: @param s, @param deps, @returns)
- `src/resources/extensions/gsd/tests/merge-conflict-stops-loop.test.ts:91` — `makeDeps(overrides: Partial<LegacyTestDeps> = {})` — exported: **no** — Performs filesystem I/O. (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/orphaned-worktree-audit.test.ts`
- `src/resources/extensions/gsd/tests/orphaned-worktree-audit.test.ts:17` — `createRepo()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/parallel-budget-atomicity.test.ts`
- `src/resources/extensions/gsd/tests/parallel-budget-atomicity.test.ts:66` — `writeWorkerCost(base: string, milestoneId: string, cost: number, completedUnits = 1)` — exported: **no** — Implements logic starting with: { writeSessionStatus(base, { milestoneId, pid: process.pid, state: "running", currentUnit: null, completedUnits, cost, lastHeartbeat: Date.now(), s… (missing: @param base, @param milestoneId, @param cost, @param completedUnits)

### `src/resources/extensions/gsd/tests/parallel-orchestrator-zombie-cleanup.test.ts`
- `src/resources/extensions/gsd/tests/parallel-orchestrator-zombie-cleanup.test.ts:41` — `writePersistedState(basePath: string, data: PersistedState)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param data)
- `src/resources/extensions/gsd/tests/parallel-orchestrator-zombie-cleanup.test.ts:47` — `writeSessionStatusFile(basePath: string, milestoneId: string, state: "running" | "paused" | "stopped" | "error", pid: number)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param milestoneId, @param state, @param pid)

### `src/resources/extensions/gsd/tests/parallel-worker-monitoring.test.ts`
- `src/resources/extensions/gsd/tests/parallel-worker-monitoring.test.ts:32` — `makeMessageEndLine(cost: number, role = "assistant")` — exported: **no** — Serializes a value to JSON. (missing: @param cost, @param role, @returns)

### `src/resources/extensions/gsd/tests/path-normalization-unified.test.ts`
- `src/resources/extensions/gsd/tests/path-normalization-unified.test.ts:25` — `isCaseInsensitiveFs(dir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param dir, @returns)

### `src/resources/extensions/gsd/tests/planning-crossval.test.ts`
- `src/resources/extensions/gsd/tests/planning-crossval.test.ts:38` — `scaffoldDirs(base: string, milestoneId: string, sliceIds: string[])` — exported: **no** — Implements logic starting with: { mkdirSync(join(base, '.gsd', 'milestones', milestoneId), { recursive: true }); for (const sid of sliceIds) { mkdirSync(join(base, '.gsd', 'milest… (missing: @param base, @param milestoneId, @param sliceIds)

### `src/resources/extensions/gsd/tests/plugin-importer.test.ts`
- `src/resources/extensions/gsd/tests/plugin-importer.test.ts:34` — `createMockPlugin(overrides: Partial<DiscoveredPlugin> = {})` — exported: **no** — Implements logic starting with: { return { name: 'test-plugin', canonicalName: 'test-plugin', source: './plugins/test-plugin', resolvedPath: '/plugins/test-plugin', status: 'ok',… (missing: @param overrides, @returns)
- `src/resources/extensions/gsd/tests/plugin-importer.test.ts:60` — `createMockDiscoveryResult(plugins: DiscoveredPlugin[] = [], overrides: Partial<MarketplaceDiscoveryResult> = {})` — exported: **no** — Transforms a collection into a derived result. (missing: @param plugins, @param overrides, @returns)

### `src/resources/extensions/gsd/tests/post-execution-checks.test.ts`
- `src/resources/extensions/gsd/tests/post-execution-checks.test.ts:32` — `createTask(overrides: Partial<TaskRow> = {})` — exported: **no** — Implements logic starting with: { return { milestone_id: "M001", slice_id: "S01", id: overrides.id ?? "T01", title: "Test Task", status: "complete", one_liner: "", narrative: "",… (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/post-mutation-hook.test.ts`
- `src/resources/extensions/gsd/tests/post-mutation-hook.test.ts:24` — `createProject(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath)

### `src/resources/extensions/gsd/tests/pre-execution-checks.test.ts`
- `src/resources/extensions/gsd/tests/pre-execution-checks.test.ts:36` — `createTask(overrides: Partial<TaskRow> = {})` — exported: **no** — Implements logic starting with: { return { milestone_id: "M001", slice_id: "S01", id: overrides.id ?? "T01", title: "Test Task", status: "pending", one_liner: "", narrative: "", v… (missing: @param overrides, @returns)

### `src/resources/extensions/gsd/tests/pre-execution-pause-wiring.test.ts`
- `src/resources/extensions/gsd/tests/pre-execution-pause-wiring.test.ts:40` — `makeMockCtx()` — exported: **no** — Implements logic starting with: { return { ui: { notify: mock.fn(), setStatus: () => {}, setWidget: () => {}, setFooter: () => {}, }, model: { id: "test-model" }, } as any; } (missing: @returns)
- `src/resources/extensions/gsd/tests/pre-execution-pause-wiring.test.ts:55` — `makeMockPi()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/gsd/tests/pre-execution-pause-wiring.test.ts:65` — `makeMockSession(basePath: string, currentUnit?: { type: string; id: string })` — exported: **no** — Implements logic starting with: { const s = new AutoSession(); s.basePath = basePath; s.active = true; if (currentUnit) { s.currentUnit = { type: currentUnit.type, id: currentUnit… (missing: @param basePath, @param currentUnit, @returns)
- `src/resources/extensions/gsd/tests/pre-execution-pause-wiring.test.ts:82` — `makePostUnitContext(s: AutoSession, ctx: ReturnType<typeof makeMockCtx>, pi: ReturnType<typeof makeMockPi>, pauseAutoMock: ReturnType<typeof mock.fn>)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param s, @param ctx, @param pi, @param pauseAutoMock, @returns)
- `src/resources/extensions/gsd/tests/pre-execution-pause-wiring.test.ts:159` — `writePreferences(prefs: Record<string, unknown>)` — exported: **no** — Performs filesystem I/O. (missing: @param prefs)

### `src/resources/extensions/gsd/tests/prompt-budget-enforcement.test.ts`
- `src/resources/extensions/gsd/tests/prompt-budget-enforcement.test.ts:34` — `setupDependencyFixture(base: string, mid: string, sid: string, deps: string[], summaries: Record<string, string>)` — exported: **no** — Performs filesystem I/O. (missing: @param base, @param mid, @param sid, @param deps, @param summaries)

### `src/resources/extensions/gsd/tests/prompt-contracts.test.ts`
- `src/resources/extensions/gsd/tests/prompt-contracts.test.ts:521` — `renderProjectMd(verdict: "simple" | "complex")` — exported: **no** — Implements logic starting with: { return readTemplate("project") .replace("{{simple | complex}}", verdict) .replace("{{one-line rationale citing the signals that decided it}}", "T… (missing: @param verdict, @returns)

### `src/resources/extensions/gsd/tests/prompt-loader-replacement.test.ts`
- `src/resources/extensions/gsd/tests/prompt-loader-replacement.test.ts:19` — `substituteBuggy(template: string, vars: Record<string, string>)` — exported: **no** — Implements logic starting with: { let content = template; for (const [key, value] of Object.entries(vars)) { content = content.replaceAll(`{{${key}}}`, value); } return content.tr… (missing: @param template, @param vars, @returns)
- `src/resources/extensions/gsd/tests/prompt-loader-replacement.test.ts:31` — `substituteFixed(template: string, vars: Record<string, string>)` — exported: **no** — Implements logic starting with: { let content = template; for (const [key, value] of Object.entries(vars)) { content = content.split(`{{${key}}}`).join(value); } return content.tr… (missing: @param template, @param vars, @returns)

### `src/resources/extensions/gsd/tests/prompt-tool-names.test.ts`
- `src/resources/extensions/gsd/tests/prompt-tool-names.test.ts:19` — `mdFiles(dir: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param dir, @returns)

### `src/resources/extensions/gsd/tests/reassess-prompt.test.ts`
- `src/resources/extensions/gsd/tests/reassess-prompt.test.ts:16` — `loadPromptFromWorktree(name: string, vars: Record<string, string> = {})` — exported: **no** — Performs filesystem I/O. (missing: @param name, @param vars, @returns)

### `src/resources/extensions/gsd/tests/replan-slice.test.ts`
- `src/resources/extensions/gsd/tests/replan-slice.test.ts:21` — `loadPromptFromWorktree(name: string, vars: Record<string, string> = {})` — exported: **no** — Performs filesystem I/O. (missing: @param name, @param vars, @returns)
- `src/resources/extensions/gsd/tests/replan-slice.test.ts:81` — `makePlanT01DoneT02Pending()` — exported: **no** — Implements logic starting with: { return `# S01: Test Slice **Goal:** Do things. **Demo:** It works. ## Tasks - [x] **T01: First task** \`est:15m\` First task description. - [ ] *… (missing: @returns)
- `src/resources/extensions/gsd/tests/replan-slice.test.ts:98` — `makePlanT01T02DoneT03Pending()` — exported: **no** — Implements logic starting with: { return `# S01: Test Slice **Goal:** Do things. **Demo:** It works. ## Tasks - [x] **T01: First task** \`est:15m\` First task description. - [x] *… (missing: @returns)
- `src/resources/extensions/gsd/tests/replan-slice.test.ts:118` — `makeTaskSummary(tid: string, blockerDiscovered: boolean)` — exported: **no** — Implements logic starting with: { return `--- id: ${tid} parent: S01 milestone: M001 provides: [] key_files: [] key_decisions: [] patterns_established: [] observability_surfaces:… (missing: @param tid, @param blockerDiscovered, @returns)

### `src/resources/extensions/gsd/tests/repo-identity-worktree.test.ts`
- `src/resources/extensions/gsd/tests/repo-identity-worktree.test.ts:15` — `normalizePath(p: string)` — exported: **no** — Implements logic starting with: { const resolved = process.platform === "win32" ? realpathSync.native(p) : realpathSync(p); return process.platform === "win32" ? resolved.toLowerC… (missing: @param p, @returns)

### `src/resources/extensions/gsd/tests/retry-diagnostic-reasoning.test.ts`
- `src/resources/extensions/gsd/tests/retry-diagnostic-reasoning.test.ts:20` — `makeAssistantText(text: string)` — exported: **no** — Implements logic starting with: { return { type: "message", message: { role: "assistant", content: [{ type: "text", text }], }, }; } (missing: @param text, @returns)
- `src/resources/extensions/gsd/tests/retry-diagnostic-reasoning.test.ts:31` — `makeToolPair(toolName: string, input: Record<string, unknown>, resultText: string, isError: boolean)` — exported: **no** — Implements logic starting with: { const toolCallId = `toolu_${Math.random().toString(36).slice(2, 10)}`; return [ { type: "message", message: { role: "assistant", content: [ { typ… (missing: @param toolName, @param input, @param resultText, @param isError, @returns)

### `src/resources/extensions/gsd/tests/rogue-file-detection.test.ts`
- `src/resources/extensions/gsd/tests/rogue-file-detection.test.ts:25` — `createTaskSummaryOnDisk(basePath: string, mid: string, sid: string, tid: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param mid, @param sid, @param tid, @returns)
- `src/resources/extensions/gsd/tests/rogue-file-detection.test.ts:36` — `createSliceSummaryOnDisk(basePath: string, mid: string, sid: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param mid, @param sid, @returns)

### `src/resources/extensions/gsd/tests/run-manager.test.ts`
- `src/resources/extensions/gsd/tests/run-manager.test.ts:43` — `writeDefinition(basePath: string, name: string, content: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @param name, @param content)

### `src/resources/extensions/gsd/tests/schema-v21-sequence.test.ts`
- `src/resources/extensions/gsd/tests/schema-v21-sequence.test.ts:52` — `createV20Db(dbPath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param dbPath)

### `src/resources/extensions/gsd/tests/secure-env-collect.test.ts`
- `src/resources/extensions/gsd/tests/secure-env-collect.test.ts:200` — `loadOrchestrator()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)

### `src/resources/extensions/gsd/tests/sidecar-queue.test.ts`
- `src/resources/extensions/gsd/tests/sidecar-queue.test.ts:36` — `getPostUnitPostVerificationBody()` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @returns)

### `src/resources/extensions/gsd/tests/silent-catch-diagnostics.test.ts`
- `src/resources/extensions/gsd/tests/silent-catch-diagnostics.test.ts:148` — `findEmptyCatches(filePath: string)` — exported: **no** — Implements logic starting with: { const sf = parseSourceFile(filePath); const results: Array<{ line: number }> = []; forEachCatchClause(sf, (cc) => { if (cc.block.statements.lengt… (missing: @param filePath, @returns)
- `src/resources/extensions/gsd/tests/silent-catch-diagnostics.test.ts:169` — `findRawStderrCatches(filePath: string)` — exported: **no** — Implements logic starting with: { const sf = parseSourceFile(filePath); const results: Array<{ line: number }> = []; forEachCatchClause(sf, (cc) => { const bodyText = cc.block.get… (missing: @param filePath, @returns)

### `src/resources/extensions/gsd/tests/single-writer-invariant.test.ts`
- `src/resources/extensions/gsd/tests/single-writer-invariant.test.ts:33` — `walkTsFiles(root: string)` — exported: **no** — Implements logic starting with: { const out: string[] = []; const stack: string[] = [root]; while (stack.length > 0) { const dir = stack.pop()!; let entries; try { entries = readd… (missing: @param root, @returns)

### `src/resources/extensions/gsd/tests/slice-cadence.test.ts`
- `src/resources/extensions/gsd/tests/slice-cadence.test.ts:32` — `createRepo()` — exported: **no** — Performs filesystem I/O. (missing: @returns)

### `src/resources/extensions/gsd/tests/state-machine-full-walkthrough.test.ts`
- `src/resources/extensions/gsd/tests/state-machine-full-walkthrough.test.ts:162` — `standardRoadmap()` — exported: **no** — Implements logic starting with: { return [ "# M001: Test Milestone", "", "**Vision:** Test state machine.", "", "## Slices", "", "- [ ] **S01: First Slice** `risk:low` `depends:[]… (missing: @returns)
- `src/resources/extensions/gsd/tests/state-machine-full-walkthrough.test.ts:176` — `doneSliceRoadmap()` — exported: **no** — Implements logic starting with: { return [ "# M001: Test Milestone", "", "**Vision:** Test state machine.", "", "## Slices", "", "- [x] **S01: Done Slice** `risk:low` `depends:[]`… (missing: @returns)
- `src/resources/extensions/gsd/tests/state-machine-full-walkthrough.test.ts:190` — `standardPlan()` — exported: **no** — Implements logic starting with: { return [ "# S01: First Slice", "", "**Goal:** Test.", "**Demo:** Tests pass.", "", "## Tasks", "", "- [ ] **T01: First Task** `est:10m`", " First… (missing: @returns)
- `src/resources/extensions/gsd/tests/state-machine-full-walkthrough.test.ts:208` — `allDonePlan()` — exported: **no** — Implements logic starting with: { return [ "# S01: First Slice", "", "**Goal:** Test.", "**Demo:** Tests pass.", "", "## Tasks", "", "- [x] **T01: First Task** `est:10m`", " First… (missing: @returns)
- `src/resources/extensions/gsd/tests/state-machine-full-walkthrough.test.ts:226` — `partialDonePlan()` — exported: **no** — Implements logic starting with: { return [ "# S01: First Slice", "", "**Goal:** Test.", "**Demo:** Tests pass.", "", "## Tasks", "", "- [x] **T01: First Task** `est:10m`", " First… (missing: @returns)

### `src/resources/extensions/gsd/tests/test-helpers.ts`
- `src/resources/extensions/gsd/tests/test-helpers.ts:11` — `createTestContext()` — exported: **yes** — Serializes a value to JSON. (missing: @returns)
- `src/resources/extensions/gsd/tests/test-helpers.ts:164` — `waitForCondition(condition: () => T | Promise<T>, opts: { timeoutMs?: number; intervalMs?: number; description?: string } = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param opts, @returns)
- `src/resources/extensions/gsd/tests/test-helpers.ts:193` — `findLine(output: string, predicate: RegExp | ((line: string) => boolean))` — exported: **yes** — Validates inputs and throws on failure. (missing: @param output, @param predicate, @returns)

### `src/resources/extensions/gsd/tests/test-utils.ts`
- `src/resources/extensions/gsd/tests/test-utils.ts:75` — `cleanup(dir: string)` — exported: **yes** — Implements logic starting with: { try { rmSync(dir, { recursive: true, force: true }); } catch { // ignore — Windows may hold file descriptors briefly after test } } (missing: @param dir)
- `src/resources/extensions/gsd/tests/test-utils.ts:102` — `safeReadFile(filePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @returns)

### `src/resources/extensions/gsd/tests/vacuum-recovery.test.ts`
- `src/resources/extensions/gsd/tests/vacuum-recovery.test.ts:48` — `createCorruptFreelistDb(dbPath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param dbPath)

### `src/resources/extensions/gsd/tests/validation-gate-patterns.test.ts`
- `src/resources/extensions/gsd/tests/validation-gate-patterns.test.ts:23` — `hasOperationalEvidence(validationContent: string)` — exported: **no** — Implements logic starting with: { const structuredMatch = validationContent.includes("Operational") && (validationContent.includes("MET") || validationContent.includes("N/A") || v… (missing: @param validationContent, @returns)

### `src/resources/extensions/gsd/tests/verification-gate.test.ts`
- `src/resources/extensions/gsd/tests/verification-gate.test.ts:827` — `makeAuditJson(vulns: Record<string, { severity: string; fixAvailable: boolean; via: unknown[] }>)` — exported: **no** — Serializes a value to JSON. (missing: @param vulns, @returns)

### `src/resources/extensions/gsd/tests/verify-artifact-tightened.test.ts`
- `src/resources/extensions/gsd/tests/verify-artifact-tightened.test.ts:25` — `scaffoldProject(t: { after: (fn: () => void) => void })` — exported: **no** — Performs filesystem I/O. (missing: @param t, @returns)

### `src/resources/extensions/gsd/tests/worktree-health-dispatch.test.ts`
- `src/resources/extensions/gsd/tests/worktree-health-dispatch.test.ts:23` — `createGitRepo()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/tests/worktree-health-dispatch.test.ts:52` — `wouldPassHealthCheck(basePath: string, existsSyncFn: (p: string) => boolean)` — exported: **no** — Implements logic starting with: { const hasGit = existsSyncFn(join(basePath, ".git")); if (!hasGit) return false; // .git is sufficient — greenfield projects proceed with a warnin… (missing: @param basePath, @param existsSyncFn, @returns)
- `src/resources/extensions/gsd/tests/worktree-health-dispatch.test.ts:61` — `hasRecognizedProjectFiles(basePath: string, existsSyncFn: (p: string) => boolean)` — exported: **no** — Implements logic starting with: { for (const file of PROJECT_FILES) { if (existsSyncFn(join(basePath, file))) return true; } if (existsSyncFn(join(basePath, "src"))) return true;… (missing: @param basePath, @param existsSyncFn, @returns)
- `src/resources/extensions/gsd/tests/worktree-health-dispatch.test.ts:70` — `hasXcodeBundle(basePath: string)` — exported: **no** — Implements logic starting with: { try { return readdirSync(basePath).some((e) => e.endsWith(".xcodeproj") || e.endsWith(".xcworkspace")); } catch { return false; } } (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/tests/worktree-journal-events.test.ts`
- `src/resources/extensions/gsd/tests/worktree-journal-events.test.ts:14` — `initGitRepoIn(base: string, isolation: "worktree" | "branch" | "none")` — exported: **no** — Performs filesystem I/O. (missing: @param base, @param isolation)
- `src/resources/extensions/gsd/tests/worktree-journal-events.test.ts:126` — `readJournalEntries(basePath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/tests/worktree-lifecycle.test.ts`
- `src/resources/extensions/gsd/tests/worktree-lifecycle.test.ts:88` — `makeGitRepoBase(opts?: { isolation?: "worktree" | "branch" | "none"; })` — exported: **no** — Performs filesystem I/O. (missing: @param opts, @returns)

### `src/resources/extensions/gsd/tests/worktree-post-create-hook.test.ts`
- `src/resources/extensions/gsd/tests/worktree-post-create-hook.test.ts:25` — `hookPath(base: string)` — exported: **no** — Implements logic starting with: { return isWin ? `${base}.bat` : base; } (missing: @param base, @returns)
- `src/resources/extensions/gsd/tests/worktree-post-create-hook.test.ts:30` — `writeNodeHookScript(filePath: string, code: string)` — exported: **no** — Performs filesystem I/O. (missing: @param filePath, @param code)

### `src/resources/extensions/gsd/tests/worktree.test.ts`
- `src/resources/extensions/gsd/tests/worktree.test.ts:32` — `normalizePath(p: string)` — exported: **no** — Implements logic starting with: { const resolved = process.platform === "win32" ? realpathSync.native(p) : realpathSync(p); return process.platform === "win32" ? resolved.toLowerC… (missing: @param p, @returns)

### `src/resources/extensions/gsd/token-counter.ts`
- `src/resources/extensions/gsd/token-counter.ts:45` — `countTokens(text: string, provider?: TokenProvider)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param text, @param provider, @returns)
- `src/resources/extensions/gsd/token-counter.ts:60` — `countTokensSync(text: string, provider?: TokenProvider)` — exported: **yes** — Implements logic starting with: { if (encoder) { return encoder.encode(text).length; } return estimateTokensForProvider(text, provider ?? "unknown"); } (missing: @param text, @param provider, @returns)

### `src/resources/extensions/gsd/tools/complete-slice.ts`
- `src/resources/extensions/gsd/tools/complete-slice.ts:60` — `sliceGateFieldForId(id: string, params: CompleteSliceParams)` — exported: **no** — Implements logic starting with: { switch (id) { case "Q8": return params.operationalReadiness; default: return undefined; } } (missing: @param id, @param params, @returns)
- `src/resources/extensions/gsd/tools/complete-slice.ts:76` — `renderSliceSummaryMarkdown(params: CompleteSliceParams)` — exported: **no** — Transforms a collection into a derived result. (missing: @param params, @returns)
- `src/resources/extensions/gsd/tools/complete-slice.ts:227` — `renderUatMarkdown(params: CompleteSliceParams)` — exported: **no** — Implements logic starting with: { return `# ${params.sliceId}: ${params.sliceTitle} — UAT **Milestone:** ${params.milestoneId} **Written:** ${new Date().toISOString()} ${params.ua… (missing: @param params, @returns)
- `src/resources/extensions/gsd/tools/complete-slice.ts:248` — `handleCompleteSlice(params: CompleteSliceParams, basePath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param params, @param basePath, @returns)

### `src/resources/extensions/gsd/tools/complete-task.ts`
- `src/resources/extensions/gsd/tools/complete-task.ts:68` — `taskGateFieldForId(id: string, params: CompleteTaskParams)` — exported: **no** — Implements logic starting with: { switch (id) { case "Q5": return params.failureModes; case "Q6": return params.loadProfile; case "Q7": return params.negativeTests; default: retur… (missing: @param id, @param params, @returns)
- `src/resources/extensions/gsd/tools/complete-task.ts:88` — `normalizeListParam(value: unknown)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param value, @returns)
- `src/resources/extensions/gsd/tools/complete-task.ts:100` — `paramsToTaskRow(params: CompleteTaskParams, completedAt: string)` — exported: **no** — Implements logic starting with: { return { milestone_id: params.milestoneId, slice_id: params.sliceId, id: params.taskId, title: params.oneLiner || params.taskId, status: "complet… (missing: @param params, @param completedAt, @returns)
- `src/resources/extensions/gsd/tools/complete-task.ts:145` — `handleCompleteTask(params: CompleteTaskParams, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param params, @param basePath, @returns)

### `src/resources/extensions/gsd/tools/skip-slice.ts`
- `src/resources/extensions/gsd/tools/skip-slice.ts:76` — `handleSkipSlice(params: SkipSliceParams)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param params, @returns)

### `src/resources/extensions/gsd/triage-resolution.ts`
- `src/resources/extensions/gsd/triage-resolution.ts:36` — `executeInject(basePath: string, mid: string, sid: string, capture: CaptureEntry)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @param mid, @param sid, @param capture, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:86` — `executeReplan(basePath: string, mid: string, sid: string, capture: CaptureEntry)` — exported: **yes** — Implements logic starting with: { try { const triggerPath = join( basePath, ".gsd", "milestones", mid, "slices", sid, `${sid}-REPLAN-TRIGGER.md`, ); const ts = new Date().toISOStr… (missing: @param basePath, @param mid, @param sid, @param capture, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:141` — `executeBacktrack(basePath: string, currentMilestoneId: string, capture: CaptureEntry)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param currentMilestoneId, @param capture, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:220` — `readBacktrackTrigger(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:249` — `clearBacktrackTrigger(basePath: string)` — exported: **yes** — Implements logic starting with: { const triggerPath = join(gsdRoot(basePath), "BACKTRACK-TRIGGER.md"); try { if (existsSync(triggerPath)) { unlinkSync(triggerPath); } } catch { /*… (missing: @param basePath)
- `src/resources/extensions/gsd/triage-resolution.ts:376` — `loadDeferredCaptures(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:383` — `loadReplanCaptures(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:390` — `buildQuickTaskPrompt(capture: CaptureEntry)` — exported: **yes** — Implements logic starting with: { return [ `You are executing a quick one-off task captured during a GSD auto-mode session.`, ``, `## Quick Task`, ``, `**Capture ID:** ${capture.i… (missing: @param capture, @returns)
- `src/resources/extensions/gsd/triage-resolution.ts:449` — `executeTriageResolutions(basePath: string, mid: string, sid: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param mid, @param sid, @returns)

### `src/resources/extensions/gsd/triage-ui.ts`
- `src/resources/extensions/gsd/triage-ui.ts:80` — `showTriageConfirmation(ctx: ExtensionCommandContext, triageResults: TriageResult[], captures: CaptureEntry[], basePath: string, fileOverlaps?: Map<string, string[]>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param triageResults, @param captures, @param basePath, @returns)

### `src/resources/extensions/gsd/undo.ts`
- `src/resources/extensions/gsd/undo.ts:24` — `handleUndo(args: string, ctx: ExtensionCommandContext, _pi: ExtensionAPI, basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param args, @param ctx, @param _pi, @param basePath)
- `src/resources/extensions/gsd/undo.ts:147` — `parseTaskId(raw: string, basePath: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param raw, @param basePath, @returns)
- `src/resources/extensions/gsd/undo.ts:179` — `parseSliceId(raw: string, basePath: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param raw, @param basePath, @returns)
- `src/resources/extensions/gsd/undo.ts:202` — `handleUndoTask(args: string, ctx: ExtensionCommandContext, _pi: ExtensionAPI, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param args, @param ctx, @param _pi, @param basePath)
- `src/resources/extensions/gsd/undo.ts:281` — `handleResetSlice(args: string, ctx: ExtensionCommandContext, _pi: ExtensionAPI, basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param args, @param ctx, @param _pi, @param basePath)

### `src/resources/extensions/gsd/unit-context-composer.ts`
- `src/resources/extensions/gsd/unit-context-composer.ts:69` — `composeInlinedContext(unitType: string, resolveArtifact: ArtifactResolver)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param unitType, @param resolveArtifact, @returns)
- `src/resources/extensions/gsd/unit-context-composer.ts:91` — `manifestBudgetChars(unitType: string)` — exported: **yes** — Implements logic starting with: { const manifest = resolveManifest(unitType); return manifest ? manifest.maxSystemPromptChars : null; } (missing: @param unitType, @returns)
- `src/resources/extensions/gsd/unit-context-composer.ts:137` — `composeContextModeInstructions(unitType: string, opts: ComposeContextModeInstructionOptions)` — exported: **yes** — Implements logic starting with: { if (!opts.enabled) return ""; const manifest = resolveManifest(unitType); if (!manifest || manifest.contextMode === "none") return ""; const lane… (missing: @param unitType, @param opts, @returns)
- `src/resources/extensions/gsd/unit-context-composer.ts:213` — `composeUnitContext(unitType: string, opts: ComposeUnitContextOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param unitType, @param opts, @returns)
- `src/resources/extensions/gsd/unit-context-composer.ts:258` — `runComputed(ids: readonly ComputedArtifactId[], opts: ComposeUnitContextOptions)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param ids, @param opts, @returns)

### `src/resources/extensions/gsd/unit-context-manifest.ts`
- `src/resources/extensions/gsd/unit-context-manifest.ts:738` — `resolveManifest(unitType: string)` — exported: **yes** — Implements logic starting with: { return (UNIT_MANIFESTS as Record<string, UnitContextManifest>)[unitType] ?? null; } (missing: @param unitType, @returns)

### `src/resources/extensions/gsd/unit-id.ts`
- `src/resources/extensions/gsd/unit-id.ts:11` — `parseUnitId(unitId: string)` — exported: **yes** — Implements logic starting with: { const [milestone, slice, task] = unitId.split("/"); return { milestone: milestone!, slice, task }; } (missing: @param unitId, @returns)

### `src/resources/extensions/gsd/unit-ownership.ts`
- `src/resources/extensions/gsd/unit-ownership.ts:173` — `initOwnershipTable(basePath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param basePath)
- `src/resources/extensions/gsd/unit-ownership.ts:205` — `closeOwnershipDb(basePath: string)` — exported: **yes** — Implements logic starting with: { const db = dbPool.get(basePath); if (!db) return; try { db.close(); } catch { /* swallow */ } dbPool.delete(basePath); } (missing: @param basePath)
- `src/resources/extensions/gsd/unit-ownership.ts:220` — `claimUnit(basePath: string, unitKey: string, agentName: string)` — exported: **yes** — Implements logic starting with: { const db = getDb(basePath); if (!db) { // Auto-init if not already initialized (backward compat) initOwnershipTable(basePath); return claimUnit(b… (missing: @param basePath, @param unitKey, @param agentName, @returns)
- `src/resources/extensions/gsd/unit-ownership.ts:244` — `releaseUnit(basePath: string, unitKey: string)` — exported: **yes** — Implements logic starting with: { const db = getDb(basePath); if (!db) return; db.prepare("DELETE FROM unit_claims WHERE unit_key = ?").run(unitKey); } (missing: @param basePath, @param unitKey)
- `src/resources/extensions/gsd/unit-ownership.ts:253` — `getOwner(basePath: string, unitKey: string)` — exported: **yes** — Implements logic starting with: { const db = getDb(basePath); if (!db) return null; const row = db.prepare("SELECT agent_name FROM unit_claims WHERE unit_key = ?").get(unitKey); r… (missing: @param basePath, @param unitKey, @returns)
- `src/resources/extensions/gsd/unit-ownership.ts:265` — `checkOwnership(basePath: string, unitKey: string, actorName: string | undefined)` — exported: **yes** — Implements logic starting with: { if (!actorName) return null; // no actor identity provided — opt-in, so allow const owner = getOwner(basePath, unitKey); if (owner === null) retu… (missing: @param basePath, @param unitKey, @param actorName, @returns)

### `src/resources/extensions/gsd/unit-runtime.ts`
- `src/resources/extensions/gsd/unit-runtime.ts:181` — `listUnitRuntimeRecords(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/validate-directory.ts`
- `src/resources/extensions/gsd/validate-directory.ts:102` — `validateDirectory(dirPath: string)` — exported: **yes** — Implements logic starting with: { // Resolve to absolute + follow symlinks so aliases can't bypass checks let resolved: string; try { resolved = realpathSync(resolve(dirPath)); }… (missing: @param dirPath, @returns)
- `src/resources/extensions/gsd/validate-directory.ts:180` — `assertSafeDirectory(dirPath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param dirPath, @returns)

### `src/resources/extensions/gsd/validation.ts`
- `src/resources/extensions/gsd/validation.ts:6` — `isNonEmptyString(value: unknown)` — exported: **yes** — Implements logic starting with: { return typeof value === "string" && value.trim().length > 0; } (missing: @param value, @returns)
- `src/resources/extensions/gsd/validation.ts:15` — `validateStringArray(value: unknown, field: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param value, @param field, @returns)

### `src/resources/extensions/gsd/verdict-parser.ts`
- `src/resources/extensions/gsd/verdict-parser.ts:22` — `extractVerdict(content: string)` — exported: **yes** — Implements logic starting with: { // Primary: YAML frontmatter verdict (canonical format) const fmMatch = content.match(/^---\n([\s\S]*?)\n---/); if (fmMatch) { const verdictMatch… (missing: @param content, @returns)
- `src/resources/extensions/gsd/verdict-parser.ts:50` — `hasVerdict(content: string)` — exported: **yes** — Implements logic starting with: { return /verdict:\s*[\w-]+/i.test(content); } (missing: @param content, @returns)
- `src/resources/extensions/gsd/verdict-parser.ts:77` — `isAcceptableUatVerdict(verdict: string, uatType: UatType | undefined)` — exported: **yes** — Implements logic starting with: { if (UAT_ACCEPTABLE_VERDICTS.includes(verdict)) return true; if (verdict === "partial" && uatType && (PARTIAL_ELIGIBLE_UAT_TYPES as readonly strin… (missing: @param verdict, @param uatType, @returns)
- `src/resources/extensions/gsd/verdict-parser.ts:96` — `isValidMilestoneVerdict(verdict: string)` — exported: **yes** — Implements logic starting with: { return (VALIDATION_VERDICTS as readonly string[]).includes(verdict); } (missing: @param verdict, @returns)
- `src/resources/extensions/gsd/verdict-parser.ts:108` — `getUatType(content: string)` — exported: **yes** — Implements logic starting with: { return extractUatType(content) ?? "artifact-driven"; } (missing: @param content, @returns)

### `src/resources/extensions/gsd/verification-evidence.ts`
- `src/resources/extensions/gsd/verification-evidence.ts:107` — `writeVerificationJSON(result: VerificationResult, tasksDir: string, taskId: string, unitId?: string, retryAttempt?: number, maxRetries?: number)` — exported: **yes** — Performs filesystem I/O. (missing: @param result, @param tasksDir, @param taskId, @param unitId, @param retryAttempt, @param maxRetries)
- `src/resources/extensions/gsd/verification-evidence.ts:173` — `writePreExecutionEvidence(result: { status: "pass" | "warn" | "fail"; checks: PreExecutionCheckJSON[]; durationMs: number }, sliceDir: string, milestoneId: string, sliceId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param result, @param sliceDir, @param milestoneId, @param sliceId)
- `src/resources/extensions/gsd/verification-evidence.ts:204` — `formatDurationSecs(ms: number)` — exported: **no** — Implements logic starting with: { return `${(ms / 1000).toFixed(1)}s`; } (missing: @param ms, @returns)
- `src/resources/extensions/gsd/verification-evidence.ts:214` — `formatEvidenceTable(result: VerificationResult)` — exported: **yes** — Implements logic starting with: { if (result.checks.length === 0) { return "_No verification checks discovered._"; } const lines: string[] = [ "| # | Command | Exit Code | Verdict… (missing: @param result, @returns)

### `src/resources/extensions/gsd/verification-gate.ts`
- `src/resources/extensions/gsd/verification-gate.ts:18` — `truncate(value: string | null | undefined, maxBytes: number)` — exported: **no** — Implements logic starting with: { if (!value) return ""; if (Buffer.byteLength(value, "utf-8") <= maxBytes) return value; // Slice conservatively then trim to last full character… (missing: @param value, @param maxBytes, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:49` — `discoverCommands(options: DiscoverCommandsOptions)` — exported: **yes** — Performs filesystem I/O. (missing: @param options, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:115` — `formatFailureContext(result: VerificationResult)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param result, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:186` — `isLikelyCommand(cmd: string)` — exported: **yes** — Implements logic starting with: { const trimmed = cmd.trim(); if (!trimmed) return false; const tokens = trimmed.split(/\s+/); const firstToken = tokens[0]; // Known command prefi… (missing: @param cmd, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:218` — `sanitizeCommand(cmd: string)` — exported: **no** — Implements logic starting with: { if (SHELL_INJECTION_PATTERN.test(cmd)) return null; if (!isLikelyCommand(cmd)) return null; return cmd; } (missing: @param cmd, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:240` — `runVerificationGate(options: RunVerificationGateOptions)` — exported: **yes** — Executes a subprocess command. (missing: @param options, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:341` — `captureRuntimeErrors(options?: CaptureRuntimeErrorsOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:475` — `buildBgShellMessage(name: string, exitCode: number | null | undefined, signal: string | null | undefined, recentErrors: string[] | undefined)` — exported: **no** — Implements logic starting with: { const parts: string[] = [`[${name}]`]; if (signal) parts.push(`signal=${signal}`); if (exitCode !== null && exitCode !== undefined) parts.push(`e… (missing: @param name, @param exitCode, @param signal, @param recentErrors, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:516` — `defaultGitDiff(cwd: string)` — exported: **no** — Executes a subprocess command. (missing: @param cwd, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:534` — `defaultNpmAudit(cwd: string)` — exported: **no** — Executes a subprocess command. (missing: @param cwd, @returns)
- `src/resources/extensions/gsd/verification-gate.ts:555` — `runDependencyAudit(cwd: string, options?: DependencyAuditOptions)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param cwd, @param options, @returns)

### `src/resources/extensions/gsd/watch/header-renderer.ts`
- `src/resources/extensions/gsd/watch/header-renderer.ts:79` — `readModelFromPreferences()` — exported: **yes** — Implements logic starting with: { try { const prefs = loadEffectiveGSDPreferences(); if (!prefs?.preferences.models) return "default"; const m = prefs.preferences.models as Record… (missing: @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:102` — `deriveProvider(modelId: string)` — exported: **yes** — Implements logic starting with: { if (modelId.startsWith("claude")) return "anthropic"; if (modelId.startsWith("gpt") || modelId.startsWith("o1") || modelId.startsWith("o3")) retu… (missing: @param modelId, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:114` — `shortenPath(fullPath: string)` — exported: **yes** — Implements logic starting with: { const home = homedir(); if (fullPath.startsWith(home)) { return "~" + fullPath.slice(home.length); } return fullPath; } (missing: @param fullPath, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:125` — `readGitBranch(projectRoot: string)` — exported: **yes** — Implements logic starting with: { try { return execFileSync("git", ["rev-parse", "--abbrev-ref", "HEAD"], { cwd: projectRoot, encoding: "utf-8", timeout: 2000, }).trim(); } catch… (missing: @param projectRoot, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:142` — `readMcpServerNames(projectRoot: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectRoot, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:187` — `gatherHeaderData(projectRoot: string)` — exported: **yes** — Implements logic starting with: { const model = readModelFromPreferences(); const provider = deriveProvider(model); const directory = shortenPath(projectRoot); const branch = read… (missing: @param projectRoot, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:201` — `formatInfoLine(label: string, value: string, availableWidth: number)` — exported: **no** — Implements logic starting with: { const labelText = bold(color(label, colors.text)); const padding = " ".repeat(Math.max(1, LABEL_COL_WIDTH - label.length)); const maxValueWidth =… (missing: @param label, @param value, @param availableWidth, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:213` — `formatMcpRow(servers: string[], width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param servers, @param width, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:236` — `renderHeaderLines(data: HeaderData, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param data, @param width, @returns)
- `src/resources/extensions/gsd/watch/header-renderer.ts:297` — `renderStackedHeader(data: HeaderData, width: number)` — exported: **no** — Implements logic starting with: { const outerWidth = Math.max(0, width); if (outerWidth < 3) { return [color(truncateToWidth("GSD", outerWidth, ""), colors.accent)]; } const lines… (missing: @param data, @param width, @returns)

### `src/resources/extensions/gsd/workflow-dispatch.ts`
- `src/resources/extensions/gsd/workflow-dispatch.ts:21` — `stripTemplateMeta(content: string)` — exported: **no** — Implements logic starting with: { return content.replace(/<template_meta>[\s\S]*?<\/template_meta>\s*/, ""); } (missing: @param content, @returns)
- `src/resources/extensions/gsd/workflow-dispatch.ts:29` — `extractYamlOneshotPrompt(yamlContent: string)` — exported: **no** — Implements logic starting with: { // Simple: just include the raw YAML so the model can follow it. // This keeps the oneshot format flexible without re-parsing. return `\`\`\`yaml… (missing: @param yamlContent, @returns)
- `src/resources/extensions/gsd/workflow-dispatch.ts:39` — `dispatchOneshot(plugin: WorkflowPlugin, pi: ExtensionAPI, userArgs: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param plugin, @param pi, @param userArgs)
- `src/resources/extensions/gsd/workflow-dispatch.ts:82` — `dispatchMarkdownPhase(opts: MarkdownPhaseDispatchOptions, pi: ExtensionAPI)` — exported: **yes** — Implements logic starting with: { const prompt = loadPrompt("workflow-start", { templateId: opts.templateId, templateName: opts.templateName, templateDescription: opts.templateDes… (missing: @param opts, @param pi, @returns)

### `src/resources/extensions/gsd/workflow-events.ts`
- `src/resources/extensions/gsd/workflow-events.ts:41` — `appendEvent(basePath: string, event: Omit<WorkflowEvent, "hash" | "session_id"> & { actor_name?: string; trigger_reason?: string })` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @param event)
- `src/resources/extensions/gsd/workflow-events.ts:68` — `readEvents(logPath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param logPath, @returns)
- `src/resources/extensions/gsd/workflow-events.ts:95` — `findForkPoint(logA: WorkflowEvent[], logB: WorkflowEvent[])` — exported: **yes** — Implements logic starting with: { const minLen = Math.min(logA.length, logB.length); let lastCommon = -1; for (let i = 0; i < minLen; i++) { if (logA[i]!.hash === logB[i]!.hash) {… (missing: @param logA, @param logB, @returns)

### `src/resources/extensions/gsd/workflow-install.ts`
- `src/resources/extensions/gsd/workflow-install.ts:87` — `assertSafePluginName(name: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param name)
- `src/resources/extensions/gsd/workflow-install.ts:101` — `safeResolveInDir(dir: string, child: string)` — exported: **no** — Validates inputs and throws on failure. (missing: @param dir, @param child, @returns)
- `src/resources/extensions/gsd/workflow-install.ts:119` — `resolveSourceUrl(source: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param source, @returns)
- `src/resources/extensions/gsd/workflow-install.ts:180` — `fetchWorkflowSource(url: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param url, @returns)
- `src/resources/extensions/gsd/workflow-install.ts:259` — `validateFetchedContent(fetched: FetchedContent)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param fetched)
- `src/resources/extensions/gsd/workflow-install.ts:314` — `inferPluginName(fetched: FetchedContent)` — exported: **yes** — Implements logic starting with: { if (fetched.ext === ".yaml" || fetched.ext === ".yml") { try { const parsed = parseYaml(fetched.content); if (parsed && typeof parsed === "object… (missing: @param fetched, @returns)
- `src/resources/extensions/gsd/workflow-install.ts:353` — `installPlugin(target: InstallTarget, fetched: FetchedContent, name: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param target, @param fetched, @param name, @returns)
- `src/resources/extensions/gsd/workflow-install.ts:386` — `uninstallPlugin(basePath: string, name: string)` — exported: **yes** — Implements logic starting with: { assertSafePluginName(name); for (const dir of [globalInstallDir(), projectInstallDir(basePath)]) { const prov = readProvenance(dir); const entry… (missing: @param basePath, @param name, @returns)
- `src/resources/extensions/gsd/workflow-install.ts:419` — `previewContent(content: string, maxLines = 20)` — exported: **yes** — Implements logic starting with: { const lines = content.split(/\r?\n/).slice(0, maxLines); return lines.join("\n"); } (missing: @param content, @param maxLines, @returns)

### `src/resources/extensions/gsd/workflow-logger.ts`
- `src/resources/extensions/gsd/workflow-logger.ts:98` — `setLogBasePath(basePath: string)` — exported: **yes** — Implements logic starting with: { _auditBasePath = basePath; } (missing: @param basePath)
- `src/resources/extensions/gsd/workflow-logger.ts:106` — `setStderrLoggingEnabled(enabled: boolean)` — exported: **yes** — Implements logic starting with: { const previous = _stderrEnabled; _stderrEnabled = enabled; return previous; } (missing: @param enabled, @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:118` — `logWarning(component: LogComponent, message: string, context?: Record<string, string>)` — exported: **yes** — Implements logic starting with: { _push("warn", component, message, context); } (missing: @param component, @param message, @param context)
- `src/resources/extensions/gsd/workflow-logger.ts:129` — `logError(component: LogComponent, message: string, context?: Record<string, string>)` — exported: **yes** — Implements logic starting with: { _push("error", component, message, context); } (missing: @param component, @param message, @param context)
- `src/resources/extensions/gsd/workflow-logger.ts:144` — `drainLogs()` — exported: **yes** — Implements logic starting with: { const entries = _buffer; _buffer = []; return entries; } (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:155` — `drainAndSummarize()` — exported: **yes** — Implements logic starting with: { const summary = summarizeLogs(); const logs = drainLogs(); return { logs, summary }; } (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:164` — `peekLogs()` — exported: **yes** — Implements logic starting with: { return _buffer; } (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:171` — `hasErrors()` — exported: **yes** — Implements logic starting with: { return _buffer.some((e) => e.severity === "error"); } (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:179` — `hasWarnings()` — exported: **yes** — Implements logic starting with: { return _buffer.some((e) => e.severity === "warn"); } (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:186` — `hasAnyIssues()` — exported: **yes** — Implements logic starting with: { return _buffer.length > 0; } (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:196` — `summarizeLogs()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:215` — `formatForNotification(entries: readonly LogEntry[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param entries, @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:235` — `readAuditLog(basePath?: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/workflow-logger.ts:359` — `_sanitizeForAudit(entry: LogEntry)` — exported: **no** — Builds an in-memory lookup structure. (missing: @param entry, @returns)

### `src/resources/extensions/gsd/workflow-manifest.ts`
- `src/resources/extensions/gsd/workflow-manifest.ts:42` — `toNumeric(value: unknown, fallback: number | null = null)` — exported: **yes** — Implements logic starting with: { if (value === null || value === undefined) return fallback; if (typeof value === "number") return Number.isFinite(value) ? value : fallback; if (… (missing: @param value, @param fallback, @returns)
- `src/resources/extensions/gsd/workflow-manifest.ts:64` — `snapshotState()` — exported: **yes** — Parses JSON input into a structured value. (missing: @returns)
- `src/resources/extensions/gsd/workflow-manifest.ts:206` — `writeManifest(basePath: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath)
- `src/resources/extensions/gsd/workflow-manifest.ts:219` — `readManifest(basePath: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/workflow-manifest.ts:249` — `bootstrapFromManifest(basePath: string)` — exported: **yes** — Implements logic starting with: { const manifest = readManifest(basePath); if (!manifest) { return false; } restoreManifest(manifest); return true; } (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/workflow-migration.ts`
- `src/resources/extensions/gsd/workflow-migration.ts:20` — `needsAutoMigration(basePath: string)` — exported: **yes** — Implements logic starting with: { const db = _getAdapter(); if (!db) return false; // If milestones table already has rows, migration already done try { const row = db.prepare("SE… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/workflow-migration.ts:56` — `migrateFromMarkdown(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath)
- `src/resources/extensions/gsd/workflow-migration.ts:254` — `validateMigration(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/workflow-plugins.ts`
- `src/resources/extensions/gsd/workflow-plugins.ts:85` — `parseTemplateMeta(content: string)` — exported: **no** — Implements logic starting with: { const match = content.match(/<template_meta>([\s\S]*?)<\/template_meta>/); if (!match) return {}; const body = match[1]; const result: Record<str… (missing: @param content, @returns)
- `src/resources/extensions/gsd/workflow-plugins.ts:295` — `discoverPlugins(basePath: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/workflow-plugins.ts:310` — `resolvePlugin(basePath: string, name: string)` — exported: **yes** — Implements logic starting with: { const plugins = discoverPlugins(basePath); return plugins.get(name) ?? null; } (missing: @param basePath, @param name, @returns)
- `src/resources/extensions/gsd/workflow-plugins.ts:318` — `listPluginsFormatted(basePath: string)` — exported: **yes** — Implements logic starting with: { const plugins = discoverPlugins(basePath); if (plugins.size === 0) { return "No workflow plugins found.\n\nRun /gsd workflow new to author one.";… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/workflow-plugins.ts:360` — `formatPluginInfo(plugin: WorkflowPlugin)` — exported: **yes** — Implements logic starting with: { const lines = [ `Plugin: ${plugin.meta.displayName} (${plugin.name})`, "", `Source: ${plugin.source}`, `Format: ${plugin.format}`, `Mode: ${plugi… (missing: @param plugin, @returns)
- `src/resources/extensions/gsd/workflow-plugins.ts:395` — `getPluginDirs(basePath: string)` — exported: **yes** — Implements logic starting with: { return { project: projectPluginsDir(basePath), global: globalPluginsDir(), bundled: resolveBundledDir(), legacy: legacyDefsDir(basePath), }; } (missing: @param basePath, @returns)

### `src/resources/extensions/gsd/workflow-projections.ts`
- `src/resources/extensions/gsd/workflow-projections.ts:35` — `stripIdPrefix(title: string, id: string)` — exported: **yes** — Implements logic starting with: { const prefix = `${id}: `; let result = title; while (result.startsWith(prefix)) { result = result.slice(prefix.length); } return result.trim() ||… (missing: @param title, @param id, @returns)
- `src/resources/extensions/gsd/workflow-projections.ts:50` — `renderPlanContent(sliceRow: SliceRow, taskRows: TaskRow[])` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; const displayTitle = stripIdPrefix(sliceRow.title, sliceRow.id); lines.push(`# ${sliceRow.id}: ${displayTitle}`); lin… (missing: @param sliceRow, @param taskRows, @returns)
- `src/resources/extensions/gsd/workflow-projections.ts:101` — `renderPlanProjection(basePath: string, milestoneId: string, sliceId: string)` — exported: **yes** — Implements logic starting with: { const sliceRows = getMilestoneSlices(milestoneId); const sliceRow = sliceRows.find(s => s.id === sliceId); if (!sliceRow) return; const taskRows… (missing: @param basePath, @param milestoneId, @param sliceId)
- `src/resources/extensions/gsd/workflow-projections.ts:120` — `renderRoadmapContent(milestoneRow: MilestoneRow, sliceRows: SliceRow[])` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; const displayTitle = stripIdPrefix(milestoneRow.title, milestoneRow.id); lines.push(`# ${milestoneRow.id}: ${displayT… (missing: @param milestoneRow, @param sliceRows, @returns)
- `src/resources/extensions/gsd/workflow-projections.ts:159` — `renderRoadmapProjection(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const milestoneRow = getMilestone(milestoneId); if (!milestoneRow) return; const sliceRows = getMilestoneSlices(milestoneId); const content = ren… (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/workflow-projections.ts:182` — `renderSummaryContent(taskRow: TaskRow, sliceId: string, milestoneId: string, evidence?: Array<{ command: string; exitCode?: number; exit_code?: number; verdict: string; durationMs?: number; duration_ms?: number }>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param taskRow, @param sliceId, @param milestoneId, @returns)
- `src/resources/extensions/gsd/workflow-projections.ts:276` — `renderSummaryProjection(basePath: string, milestoneId: string, sliceId: string, taskId: string)` — exported: **yes** — Implements logic starting with: { const taskRows = getSliceTasks(milestoneId, sliceId); const taskRow = taskRows.find(t => t.id === taskId); if (!taskRow) return; const evidenceRo… (missing: @param basePath, @param milestoneId, @param sliceId, @param taskId)
- `src/resources/extensions/gsd/workflow-projections.ts:295` — `renderStateContent(state: GSDState)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; lines.push("# GSD State", ""); const activeSlice = state.activeSlice ? `${state.activeSlice.id}: ${stripIdPrefix(stat… (missing: @param state, @returns)
- `src/resources/extensions/gsd/workflow-projections.ts:352` — `renderStateProjection(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath)
- `src/resources/extensions/gsd/workflow-projections.ts:382` — `renderAllProjections(basePath: string, milestoneId: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/workflow-projections.ts:430` — `regenerateIfMissing(basePath: string, milestoneId: string, sliceId: string, fileType: "PLAN" | "ROADMAP" | "SUMMARY" | "STATE")` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param milestoneId, @param sliceId, @param fileType, @returns)

### `src/resources/extensions/gsd/workflow-reconcile.ts`
- `src/resources/extensions/gsd/workflow-reconcile.ts:44` — `replaySliceComplete(milestoneId: string, sliceId: string, ts: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param milestoneId, @param sliceId, @param ts)
- `src/resources/extensions/gsd/workflow-reconcile.ts:81` — `replayEvents(events: WorkflowEvent[])` — exported: **no** — Serializes a value to JSON. (missing: @param events)
- `src/resources/extensions/gsd/workflow-reconcile.ts:228` — `extractEntityKey(event: WorkflowEvent)` — exported: **yes** — Implements logic starting with: { const p = event.params; // Normalize cmd format: accept both hyphens and underscores if (typeof event.cmd !== "string") return null; const cmd =… (missing: @param event, @returns)
- `src/resources/extensions/gsd/workflow-reconcile.ts:283` — `detectConflicts(mainDiverged: WorkflowEvent[], wtDiverged: WorkflowEvent[])` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param mainDiverged, @param wtDiverged, @returns)
- `src/resources/extensions/gsd/workflow-reconcile.ts:368` — `writeConflictsFile(basePath: string, conflicts: ConflictEntry[], worktreePath: string)` — exported: **yes** — Serializes a value to JSON. (missing: @param basePath, @param conflicts, @param worktreePath)
- `src/resources/extensions/gsd/workflow-reconcile.ts:422` — `reconcileWorktreeLogs(mainBasePath: string, worktreeBasePath: string)` — exported: **yes** — Implements logic starting with: { // Acquire advisory lock to prevent concurrent reconcile + append races const lock = acquireSyncLock(mainBasePath); if (!lock.acquired) { logWarn… (missing: @param mainBasePath, @param worktreeBasePath, @returns)
- `src/resources/extensions/gsd/workflow-reconcile.ts:529` — `listConflicts(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/workflow-reconcile.ts:568` — `parseEventBlock(block: string)` — exported: **no** — Parses JSON input into a structured value. (missing: @param block, @returns)
- `src/resources/extensions/gsd/workflow-reconcile.ts:614` — `resolveConflict(basePath: string, worktreeBasePath: string, entityKey: string, pick: "main" | "worktree")` — exported: **yes** — Validates inputs and throws on failure. (missing: @param basePath, @param worktreeBasePath, @param entityKey, @param pick)
- `src/resources/extensions/gsd/workflow-reconcile.ts:676` — `removeConflictsFile(basePath: string)` — exported: **yes** — Implements logic starting with: { const conflictsPath = join(basePath, ".gsd", "CONFLICTS.md"); if (existsSync(conflictsPath)) { unlinkSync(conflictsPath); } } (missing: @param basePath)

### `src/resources/extensions/gsd/workflow-templates.ts`
- `src/resources/extensions/gsd/workflow-templates.ts:21` — `resolveGsdExtensionDir()` — exported: **no** — Implements logic starting with: { const moduleDir = dirname(fileURLToPath(import.meta.url)); if (existsSync(join(moduleDir, "workflow-templates"))) return moduleDir; const agentGs… (missing: @returns)
- `src/resources/extensions/gsd/workflow-templates.ts:72` — `loadRegistry()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/gsd/workflow-templates.ts:93` — `resolveByName(nameOrAlias: string)` — exported: **yes** — Implements logic starting with: { const registry = loadRegistry(); const normalized = nameOrAlias.toLowerCase().trim(); // Exact key match if (registry.templates[normalized]) { re… (missing: @param nameOrAlias, @returns)
- `src/resources/extensions/gsd/workflow-templates.ts:156` — `autoDetect(description: string)` — exported: **yes** — Implements logic starting with: { const registry = loadRegistry(); const lower = description.toLowerCase(); const words = lower.split(/\s+/); const matches: TemplateMatch[] = [];… (missing: @param description, @returns)
- `src/resources/extensions/gsd/workflow-templates.ts:209` — `listTemplates()` — exported: **yes** — Implements logic starting with: { const registry = loadRegistry(); const lines: string[] = ["Workflow Templates\n"]; for (const [id, entry] of Object.entries(registry.templates))… (missing: @returns)
- `src/resources/extensions/gsd/workflow-templates.ts:234` — `getTemplateInfo(name: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param name, @returns)
- `src/resources/extensions/gsd/workflow-templates.ts:270` — `loadWorkflowTemplate(templateId: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param templateId, @returns)

### `src/resources/extensions/gsd/workspace.ts`
- `src/resources/extensions/gsd/workspace.ts:39` — `createWorkspace(rawBasePath: string)` — exported: **yes** — Implements logic starting with: { const resolvedBase = resolve(rawBasePath); const isWorktree = isGsdWorktreePath(resolvedBase); const projectRootRaw = resolveWorktreeProjectRoot(… (missing: @param rawBasePath, @returns)
- `src/resources/extensions/gsd/workspace.ts:79` — `scopeMilestone(workspace: GsdWorkspace, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const { contract } = workspace; const gsd = contract.projectGsd; const scope: MilestoneScope = Object.freeze({ workspace, milestoneId, contextFil… (missing: @param workspace, @param milestoneId, @returns)

### `src/resources/extensions/gsd/worktree-command.ts`
- `src/resources/extensions/gsd/worktree-command.ts:54` — `worktreeCompletions(prefix: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param prefix, @returns)
- `src/resources/extensions/gsd/worktree-command.ts:263` — `hasExistingMilestones(wtPath: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param wtPath, @returns)
- `src/resources/extensions/gsd/worktree-command.ts:279` — `clearGSDPlans(wtPath: string)` — exported: **no** — Implements logic starting with: { const mDir = milestonesDir(wtPath); if (existsSync(mDir)) { rmSync(mDir, { recursive: true, force: true }); } // Remove root planning files — PRO… (missing: @param wtPath)

### `src/resources/extensions/gsd/worktree-health.ts`
- `src/resources/extensions/gsd/worktree-health.ts:58` — `getWorktreeHealth(basePath: string, wt: WorktreeInfo, staleDays = DEFAULT_STALE_DAYS)` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/worktree-health.ts:129` — `getAllWorktreeHealth(basePath: string, staleDays = DEFAULT_STALE_DAYS)` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)
- `src/resources/extensions/gsd/worktree-health.ts:141` — `formatWorktreeStatusLine(health: WorktreeHealthStatus)` — exported: **yes** — Implements logic starting with: { const parts: string[] = []; if (health.mergedIntoMain) { parts.push("✓ merged into main"); if (health.safeToRemove) { parts.push("safe to remove"… (missing: @param health, @returns)

### `src/resources/extensions/gsd/worktree-lifecycle.ts`
- `src/resources/extensions/gsd/worktree-lifecycle.ts:194` — `isSamePathPhysical(a: string, b: string)` — exported: **no** — Implements logic starting with: { return normalizeWorktreePathForCompare(a) === normalizeWorktreePathForCompare(b); } (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:474` — `validateMilestoneId(milestoneId: string)` — exported: **no** — Implements logic starting with: { if (!isValidMilestoneId(milestoneId)) { throw invalidMilestoneIdError(milestoneId); } } (missing: @param milestoneId)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:497` — `_enterMilestoneCore(s: AutoSession, deps: WorktreeLifecycleDeps, milestoneId: string, ctx: NotifyCtx)` — exported: **yes** — Implements logic starting with: { if (!isValidMilestoneId(milestoneId)) { debugLog("WorktreeLifecycle", { action: "enterMilestone", milestoneId, rejected: "invalid-milestone-id",… (missing: @param s, @param deps, @param milestoneId, @param ctx, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:818` — `resolvePausedResumeBasePath(base: string, persistedWorktreePath: string | null | undefined, pathExists: (p: string) => boolean = existsSync)` — exported: **yes** — Implements logic starting with: { return persistedWorktreePath && pathExists(persistedWorktreePath) ? persistedWorktreePath : base; } (missing: @param base, @param persistedWorktreePath, @param pathExists, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:850` — `_mergeWorktreeModeImpl(deps: WorktreeLifecycleDeps, mctx: MergeContext)` — exported: **no** — Implements logic starting with: { const { originalBasePath, worktreeBasePath, milestoneId, notify } = mctx; if (!originalBasePath) { debugLog("WorktreeLifecycle", { action: "merge… (missing: @param deps, @param mctx, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1040` — `_mergeBranchModeImpl(deps: WorktreeLifecycleDeps, mctx: MergeContext)` — exported: **no** — Implements logic starting with: { const { worktreeBasePath, milestoneId, notify } = mctx; try { const currentBranch = currentLifecycleBranch(deps, worktreeBasePath); const milesto… (missing: @param deps, @param mctx, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1178` — `mergeMilestoneStandalone(deps: WorktreeLifecycleDeps, mctx: MergeContext)` — exported: **yes** — Implements logic starting with: { const { originalBasePath, worktreeBasePath, milestoneId, notify } = mctx; validateMilestoneId(milestoneId); if (mctx.isolationDegraded) { if (ori… (missing: @param deps, @param mctx, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1308` — `class WorktreeLifecycle` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1325` — `WorktreeLifecycle.enterMilestone(milestoneId: string, ctx: NotifyCtx)` — exported: **yes** — Implements logic starting with: { return _enterMilestoneCore(this.s, this.deps, milestoneId, ctx); } (missing: @param milestoneId, @param ctx, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1342` — `WorktreeLifecycle.exitMilestone(milestoneId: string, opts: { merge: boolean; preserveBranch?: boolean }, ctx: NotifyCtx)` — exported: **yes** — Implements logic starting with: { if (opts.merge) { try { const merged = this._mergeAndExit(milestoneId, ctx); return { ok: true, merged: merged.merged, codeFilesChanged: merged.c… (missing: @param milestoneId, @param opts, @param ctx, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1378` — `WorktreeLifecycle.mergeAndEnterNext(currentMilestoneId: string, nextMilestoneId: string, ctx: NotifyCtx)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param currentMilestoneId, @param nextMilestoneId, @param ctx)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1671` — `WorktreeLifecycle.degradeToBranchMode(milestoneId: string, ctx: NotifyCtx)` — exported: **yes** — Implements logic starting with: { if (this.s.isolationDegraded) { debugLog("WorktreeLifecycle", { action: "degradeToBranchMode", milestoneId, skipped: true, reason: "already-degra… (missing: @param milestoneId, @param ctx)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1756` — `WorktreeLifecycle.adoptSessionRoot(base: string, originalBase?: string)` — exported: **yes** — Implements logic starting with: { this.s.basePath = base; if (originalBase !== undefined) { this.s.originalBasePath = originalBase; } else if (!this.s.originalBasePath) { this.s.o… (missing: @param base, @param originalBase)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1781` — `WorktreeLifecycle.resumeFromPausedSession(base: string, persistedWorktreePath: string | null)` — exported: **yes** — Implements logic starting with: { this.s.basePath = resolvePausedResumeBasePath(base, persistedWorktreePath); } (missing: @param base, @param persistedWorktreePath)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1812` — `WorktreeLifecycle.adoptOrphanWorktree(milestoneId: string, base: string, run: () => T)` — exported: **yes** — Implements logic starting with: { validateMilestoneId(milestoneId); const priorBasePath = this.s.basePath; const priorOriginalBasePath = this.s.originalBasePath; const restorePrio… (missing: @param milestoneId, @param base, @param run, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1892` — `WorktreeLifecycle.isInMilestone(milestoneId: string)` — exported: **yes** — Implements logic starting with: { return this.s.currentMilestoneId === milestoneId; } (missing: @param milestoneId, @returns)
- `src/resources/extensions/gsd/worktree-lifecycle.ts:1897` — `WorktreeLifecycle.getCurrentMilestoneIfAny()` — exported: **yes** — Implements logic starting with: { return this.s.currentMilestoneId; } (missing: @returns)

### `src/resources/extensions/gsd/worktree-manager.ts`
- `src/resources/extensions/gsd/worktree-manager.ts:120` — `resolveGitDir(basePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:156` — `isInsideWorktreesDir(basePath: string, targetPath: string)` — exported: **yes** — Implements logic starting with: { const wtDirPath = worktreesDir(basePath); const wtDir = existsSync(wtDirPath) ? realpathSync(wtDirPath) : resolve(wtDirPath); const resolved = ex… (missing: @param basePath, @param targetPath, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:184` — `resolveCanonicalMilestoneRoot(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { if (!milestoneId || /[\/\\]|\.\./.test(milestoneId)) return basePath; const wtPath = worktreePath(basePath, milestoneId); if (!existsSync(wtPath)… (missing: @param basePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:225` — `createWorktree(basePath: string, name: string, opts: { branch?: string; startPoint?: string; reuseExistingBranch?: boolean } = {})` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); // Validate name: alphanumeric, hyphens, underscores only if (!/^[a-zA-Z0-9_-]+$/.test(name… (missing: @param basePath, @param name, @param opts, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:329` — `listWorktrees(basePath: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:432` — `findNestedGitDirs(rootPath: string)` — exported: **yes** — Implements logic starting with: { const results: string[] = []; function walk(dir: string, depth: number): void { // Cap recursion depth to avoid runaway scanning if (depth > 10)… (missing: @param rootPath, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:491` — `removeWorktree(basePath: string, name: string, opts: { deleteBranch?: boolean; force?: boolean; branch?: string } = {})` — exported: **yes** — Logs diagnostic information. (missing: @param basePath, @param name, @param opts)
- `src/resources/extensions/gsd/worktree-manager.ts:752` — `diffWorktreeGSD(basePath: string, name: string)` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); const branch = worktreeBranchName(name); const mainBranch = nativeDetectMainBranch(basePath… (missing: @param basePath, @param name, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:769` — `diffWorktreeAll(basePath: string, name: string, branchOverride?: string)` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); const branch = branchOverride ?? worktreeBranchName(name); const mainBranch = nativeDetectM… (missing: @param basePath, @param name, @param branchOverride, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:784` — `diffWorktreeNumstat(basePath: string, name: string, branchOverride?: string)` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); const branch = branchOverride ?? worktreeBranchName(name); const mainBranch = nativeDetectM… (missing: @param basePath, @param name, @param branchOverride, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:804` — `getWorktreeGSDDiff(basePath: string, name: string)` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); const branch = worktreeBranchName(name); const mainBranch = nativeDetectMainBranch(basePath… (missing: @param basePath, @param name, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:817` — `getWorktreeCodeDiff(basePath: string, name: string)` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); const branch = worktreeBranchName(name); const mainBranch = nativeDetectMainBranch(basePath… (missing: @param basePath, @param name, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:829` — `getWorktreeLog(basePath: string, name: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param basePath, @param name, @returns)
- `src/resources/extensions/gsd/worktree-manager.ts:845` — `mergeWorktreeToMain(basePath: string, name: string, commitMessage: string, branchOverride?: string)` — exported: **yes** — Implements logic starting with: { basePath = normalizeBasePathForWorktreeOps(basePath); const branch = branchOverride ?? worktreeBranchName(name); const mainBranch = nativeDetectM… (missing: @param basePath, @param name, @param commitMessage, @param branchOverride, @returns)

### `src/resources/extensions/gsd/worktree-root.ts`
- `src/resources/extensions/gsd/worktree-root.ts:26` — `findWorktreeSegment(normalizedPath: string)` — exported: **yes** — Implements logic starting with: { const directMarker = "/.gsd/worktrees/"; const directIdx = normalizedPath.indexOf(directMarker); if (directIdx !== -1) { return { gsdIdx: directI… (missing: @param normalizedPath, @returns)
- `src/resources/extensions/gsd/worktree-root.ts:57` — `resolveWorktreeProjectRoot(basePath: string, originalBasePath?: string | null)` — exported: **yes** — Implements logic starting with: { const explicitOriginal = originalBasePath?.trim(); if (explicitOriginal) return resolveProjectRootFromPath(explicitOriginal); const envProjectRoo… (missing: @param basePath, @param originalBasePath, @returns)

### `src/resources/extensions/gsd/worktree-state-projection.ts`
- `src/resources/extensions/gsd/worktree-state-projection.ts:50` — `isSamePath(a: string, b: string)` — exported: **no** — Implements logic starting with: { try { return realpathSync(a) === realpathSync(b); } catch (e) { if ((e as NodeJS.ErrnoException).code === "ENOENT") return false; logWarning("wor… (missing: @param a, @param b, @returns)
- `src/resources/extensions/gsd/worktree-state-projection.ts:77` — `forceOverwriteAssessmentsWithVerdict(srcMilestoneDir: string, dstMilestoneDir: string)` — exported: **no** — Performs filesystem I/O. (missing: @param srcMilestoneDir, @param dstMilestoneDir)
- `src/resources/extensions/gsd/worktree-state-projection.ts:151` — `_projectRootToWorktreeImpl(projectRoot: string, worktreePath_: string, milestoneId: string | null)` — exported: **yes** — Implements logic starting with: { if (!worktreePath_ || !projectRoot || worktreePath_ === projectRoot) return; if (!milestoneId) return; const contract = resolveGsdPathContract(wo… (missing: @param projectRoot, @param worktreePath_, @param milestoneId)
- `src/resources/extensions/gsd/worktree-state-projection.ts:245` — `_projectWorktreeToRootImpl(worktreePath_: string, projectRoot: string, milestoneId: string | null)` — exported: **yes** — Implements logic starting with: { if (!worktreePath_ || !projectRoot || worktreePath_ === projectRoot) return; if (!milestoneId) return; const contract = resolveGsdPathContract(wo… (missing: @param worktreePath_, @param projectRoot, @param milestoneId)
- `src/resources/extensions/gsd/worktree-state-projection.ts:294` — `_finalizeProjectionForMergeImpl(mainBasePath: string, worktreePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { const contract = resolveGsdPathContract(worktreePath, mainBasePath); const mainGsd = contract.projectGsd; const wtGsd = contract.worktreeGsd ?? j… (missing: @param mainBasePath, @param worktreePath, @param milestoneId, @returns)
- `src/resources/extensions/gsd/worktree-state-projection.ts:360` — `class WorktreeStateProjection` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/gsd/worktree-state-projection.ts:368` — `WorktreeStateProjection.projectRootToWorktree(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { _projectRootToWorktreeImpl( scope.workspace.projectRoot, scope.workspace.worktreeRoot ?? scope.workspace.projectRoot, scope.milestoneId, ); } (missing: @param scope)
- `src/resources/extensions/gsd/worktree-state-projection.ts:380` — `WorktreeStateProjection.projectWorktreeToRoot(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { _projectWorktreeToRootImpl( scope.workspace.worktreeRoot ?? scope.workspace.projectRoot, scope.workspace.projectRoot, scope.milestoneId, ); } (missing: @param scope)
- `src/resources/extensions/gsd/worktree-state-projection.ts:397` — `WorktreeStateProjection.finalizeProjectionForMerge(scope: MilestoneScope)` — exported: **yes** — Implements logic starting with: { return _finalizeProjectionForMergeImpl( scope.workspace.projectRoot, scope.workspace.worktreeRoot ?? scope.workspace.projectRoot, scope.milestone… (missing: @param scope, @returns)

### `src/resources/extensions/gsd/worktree-telemetry.ts`
- `src/resources/extensions/gsd/worktree-telemetry.ts:246` — `summarizeWorktreeTelemetry(projectRoot: string, filters?: { after?: string; before?: string })` — exported: **yes** — Implements logic starting with: { const entries = queryJournal(projectRoot, filters); const summary: WorktreeTelemetrySummary = { worktreesCreated: 0, worktreesMerged: 0, orphansD… (missing: @param projectRoot, @param filters, @returns)
- `src/resources/extensions/gsd/worktree-telemetry.ts:318` — `percentile(sortedValues: number[], q: number)` — exported: **yes** — Implements logic starting with: { if (sortedValues.length === 0) return null; if (q <= 0) return sortedValues[0]; if (q >= 1) return sortedValues[sortedValues.length - 1]; const i… (missing: @param sortedValues, @param q, @returns)

### `src/resources/extensions/gsd/worktree.ts`
- `src/resources/extensions/gsd/worktree.ts:39` — `getService(basePath: string)` — exported: **no** — Implements logic starting with: { if (cachedService === null || cachedBasePath !== basePath) { const loaded = loadEffectiveGSDPreferences(); const gitPrefs = loaded?.preferences?.… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/worktree.ts:63` — `setActiveMilestoneId(basePath: string, milestoneId: string | null)` — exported: **yes** — Implements logic starting with: { getService(basePath).setMilestoneId(milestoneId); } (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/worktree.ts:74` — `captureIntegrationBranch(basePath: string, milestoneId: string)` — exported: **yes** — Implements logic starting with: { // In a worktree, the base branch is implicit (worktree/<name>). // Writing it to META.json would leave stale metadata after merge back to main.… (missing: @param basePath, @param milestoneId)
- `src/resources/extensions/gsd/worktree.ts:89` — `detectWorktreeName(basePath: string)` — exported: **yes** — Implements logic starting with: { const normalizedPath = basePath.replaceAll("\\", "/"); const seg = findWorktreeSegment(normalizedPath); if (!seg) return null; const afterMarker… (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/worktree.ts:116` — `resolveProjectRoot(basePath: string)` — exported: **yes** — Implements logic starting with: { return resolveWorktreeProjectRoot(basePath); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/worktree.ts:130` — `getSliceBranchName(milestoneId: string, sliceId: string, worktreeName?: string | null)` — exported: **yes** — Implements logic starting with: { if (worktreeName) { return `gsd/${worktreeName}/${milestoneId}/${sliceId}`; } return `gsd/${milestoneId}/${sliceId}`; } (missing: @param milestoneId, @param sliceId, @param worktreeName, @returns)
- `src/resources/extensions/gsd/worktree.ts:145` — `parseSliceBranch(branchName: string)` — exported: **yes** — Implements logic starting with: { const match = branchName.match(SLICE_BRANCH_RE); if (!match) return null; return { worktreeName: match[1] ?? null, milestoneId: match[2]!, sliceI… (missing: @param branchName, @returns)
- `src/resources/extensions/gsd/worktree.ts:172` — `getMainBranch(basePath: string)` — exported: **yes** — Implements logic starting with: { return getService(basePath).getMainBranch(); } (missing: @param basePath, @returns)
- `src/resources/extensions/gsd/worktree.ts:189` — `autoCommitCurrentBranch(basePath: string, unitType: string, unitId: string, taskContext?: TaskCommitContext)` — exported: **yes** — Implements logic starting with: { return getService(basePath).autoCommit(unitType, unitId, [], taskContext); } (missing: @param basePath, @param unitType, @param unitId, @param taskContext, @returns)
- `src/resources/extensions/gsd/worktree.ts:203` — `resolveGitHeadPath(dir: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param dir, @returns)
- `src/resources/extensions/gsd/worktree.ts:225` — `nudgeGitBranchCache(previousCwd: string)` — exported: **yes** — Implements logic starting with: { const now = new Date(); for (const dir of [previousCwd, process.cwd()]) { try { const headPath = resolveGitHeadPath(dir); if (headPath) utimesSyn… (missing: @param previousCwd)

### `src/resources/extensions/gsd/write-intercept.ts`
- `src/resources/extensions/gsd/write-intercept.ts:59` — `isBlockedStateFile(filePath: string)` — exported: **yes** — Implements logic starting with: { // Check raw path first if (matchesBlockedPattern(filePath)) return true; // Resolve ".." segments (works even for non-existing files) const reso… (missing: @param filePath, @returns)
- `src/resources/extensions/gsd/write-intercept.ts:81` — `isBashWriteToStateFile(command: string)` — exported: **yes** — Implements logic starting with: { return BASH_STATE_PATTERNS.some((pattern) => pattern.test(command)); } (missing: @param command, @returns)

### `src/resources/extensions/mac-tools/index.ts`
- `src/resources/extensions/mac-tools/index.ts:37` — `getSourceMtime()` — exported: **no** — Implements logic starting with: { let latest = 0; // Check Package.swift try { latest = Math.max(latest, statSync(PACKAGE_SWIFT).mtimeMs); } catch {} // Check all files in Sources… (missing: @returns)
- `src/resources/extensions/mac-tools/index.ts:57` — `getBinaryMtime()` — exported: **no** — Implements logic starting with: { try { return statSync(BINARY_PATH).mtimeMs; } catch { return 0; } } (missing: @returns)
- `src/resources/extensions/mac-tools/index.ts:104` — `execMacAgent(command: string, params?: Record<string, any>)` — exported: **no** — Validates inputs and throws on failure. (missing: @param command, @param params, @returns)

### `src/resources/extensions/mcp-client/auth.ts`
- `src/resources/extensions/mcp-client/auth.ts:37` — `resolveEnvValue(value: string)` — exported: **no** — Logs diagnostic information. (missing: @param value, @returns)
- `src/resources/extensions/mcp-client/auth.ts:77` — `createCliOAuthProvider(config: NonNullable<McpHttpOAuthConfig["oauth"]>)` — exported: **no** — Logs diagnostic information. (missing: @param config, @returns)
- `src/resources/extensions/mcp-client/auth.ts:141` — `buildHttpTransportOpts(authConfig: McpHttpAuthConfig)` — exported: **yes** — Implements logic starting with: { const opts: StreamableHTTPClientTransportOptions = {}; // OAuth takes precedence if (authConfig.oauth) { opts.authProvider = createCliOAuthProvid… (missing: @param authConfig, @returns)

### `src/resources/extensions/mcp-client/index.ts`
- `src/resources/extensions/mcp-client/index.ts:220` — `resolveEnv(env: Record<string, string>)` — exported: **no** — Implements logic starting with: { const resolved: Record<string, string> = {}; for (const [key, value] of Object.entries(env)) { if (typeof value === "string") { resolved[key] = v… (missing: @param env, @returns)
- `src/resources/extensions/mcp-client/index.ts:367` — `getConnectionStatus(name: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param name, @returns)

### `src/resources/extensions/ollama/index.ts`
- `src/resources/extensions/ollama/index.ts:53` — `probeAndRegister(pi: ExtensionAPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param pi, @returns)

### `src/resources/extensions/ollama/model-capabilities.ts`
- `src/resources/extensions/ollama/model-capabilities.ts:122` — `getModelCapabilities(modelName: string)` — exported: **yes** — Implements logic starting with: { // Strip tag (everything after the colon) for matching const baseName = modelName.split(":")[0].toLowerCase(); for (const [pattern, caps] of KNOW… (missing: @param modelName, @returns)
- `src/resources/extensions/ollama/model-capabilities.ts:139` — `estimateContextFromParams(parameterSize: string)` — exported: **yes** — Implements logic starting with: { const match = parameterSize.match(/([\d.]+)\s*([BbMm])/); if (!match) return 8192; const size = parseFloat(match[1]); const unit = match[2].toUpp… (missing: @param parameterSize, @returns)
- `src/resources/extensions/ollama/model-capabilities.ts:160` — `humanizeModelName(modelName: string)` — exported: **yes** — Implements logic starting with: { const [base, tag] = modelName.split(":"); // Capitalize first letter, add spaces around version numbers let name = base .replace(/([a-z])(\d)/g,… (missing: @param modelName, @returns)
- `src/resources/extensions/ollama/model-capabilities.ts:182` — `formatModelSize(bytes: number)` — exported: **yes** — Implements logic starting with: { if (bytes >= 1e9) return `${(bytes / 1e9).toFixed(1)} GB`; if (bytes >= 1e6) return `${(bytes / 1e6).toFixed(1)} MB`; return `${(bytes / 1e3).toF… (missing: @param bytes, @returns)

### `src/resources/extensions/ollama/ndjson-stream.ts`
- `src/resources/extensions/ollama/ndjson-stream.ts:12` — `parseNDJsonStream(body: ReadableStream<Uint8Array>, signal?: AbortSignal, strict = false)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param body, @param signal)

### `src/resources/extensions/ollama/ollama-chat-provider.ts`
- `src/resources/extensions/ollama/ollama-chat-provider.ts:41` — `createStream()` — exported: **no** — Validates inputs and throws on failure. (missing: @returns)

### `src/resources/extensions/ollama/ollama-client.ts`
- `src/resources/extensions/ollama/ollama-client.ts:34` — `envPositiveInt(name: string, fallback: number)` — exported: **yes** — Implements logic starting with: { const raw = process.env[name]; if (!raw) return fallback; const parsed = Number.parseInt(raw, 10); if (!Number.isFinite(parsed) || parsed <= 0) r… (missing: @param name, @param fallback, @returns)
- `src/resources/extensions/ollama/ollama-client.ts:50` — `getProbeTimeoutMs()` — exported: **yes** — Implements logic starting with: { return envPositiveInt("OLLAMA_PROBE_TIMEOUT_MS", DEFAULT_PROBE_TIMEOUT_MS); } (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:58` — `getRequestTimeoutMs()` — exported: **yes** — Implements logic starting with: { return envPositiveInt("OLLAMA_REQUEST_TIMEOUT_MS", DEFAULT_REQUEST_TIMEOUT_MS); } (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:65` — `getOllamaHost()` — exported: **yes** — Implements logic starting with: { const host = process.env.OLLAMA_HOST; if (!host) return DEFAULT_HOST; // OLLAMA_HOST can be just a host:port without scheme if (host.startsWith("… (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:79` — `getAuthHeaders()` — exported: **no** — Implements logic starting with: { const apiKey = process.env.OLLAMA_API_KEY; if (!apiKey) return {}; return { Authorization: `Bearer ${apiKey}` }; } (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:88` — `withAuth(options: RequestInit = {})` — exported: **no** — Implements logic starting with: { const authHeaders = getAuthHeaders(); if (Object.keys(authHeaders).length === 0) return options; return { ...options, headers: { ...authHeaders,… (missing: @param options, @returns)
- `src/resources/extensions/ollama/ollama-client.ts:112` — `isRunning()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:128` — `getVersion()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:142` — `listModels()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:153` — `showModel(name: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param name, @returns)
- `src/resources/extensions/ollama/ollama-client.ts:168` — `getRunningModels()` — exported: **yes** — Validates inputs and throws on failure. (missing: @returns)
- `src/resources/extensions/ollama/ollama-client.ts:181` — `pullModel(name: string, onProgress?: (progress: OllamaPullProgress) => void, signal?: AbortSignal)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param name, @param onProgress, @param signal)
- `src/resources/extensions/ollama/ollama-client.ts:211` — `chat(request: OllamaChatRequest, signal?: AbortSignal)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param request, @param signal)
- `src/resources/extensions/ollama/ollama-client.ts:237` — `deleteModel(name: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param name)
- `src/resources/extensions/ollama/ollama-client.ts:252` — `copyModel(source: string, destination: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param source, @param destination)

### `src/resources/extensions/ollama/ollama-discovery.ts`
- `src/resources/extensions/ollama/ollama-discovery.ts:24` — `extractContextFromModelInfo(modelInfo: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { for (const [key, value] of Object.entries(modelInfo)) { if (key.endsWith(".context_length") && typeof value === "number" && value > 0) { return v… (missing: @param modelInfo, @returns)
- `src/resources/extensions/ollama/ollama-discovery.ts:107` — `discoverModels(deps: ClientDeps = { listModels, showModel })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param deps, @returns)
- `src/resources/extensions/ollama/ollama-discovery.ts:117` — `formatModelForDisplay(model: DiscoveredOllamaModel)` — exported: **yes** — Implements logic starting with: { const parts = [model.id]; if (model.sizeBytes > 0) { parts.push(`(${formatModelSize(model.sizeBytes)})`); } const flags: string[] = []; if (model… (missing: @param model, @returns)

### `src/resources/extensions/ollama/thinking-parser.ts`
- `src/resources/extensions/ollama/thinking-parser.ts:28` — `ThinkingTagParser.push(chunk: string)` — exported: **yes** — Implements logic starting with: { const results: ParsedChunk[] = []; let input = this.buffer + chunk; this.buffer = ""; while (input.length > 0) { if (this.inThinking) { const clo… (missing: @param chunk, @returns)
- `src/resources/extensions/ollama/thinking-parser.ts:83` — `ThinkingTagParser.flush()` — exported: **yes** — Implements logic starting with: { if (!this.buffer) return []; const result: ParsedChunk = { type: this.inThinking ? "thinking" : "text", text: this.buffer, }; this.buffer = ""; r… (missing: @returns)

### `src/resources/extensions/remote-questions/commands.ts`
- `src/resources/extensions/remote-questions/commands.ts:33` — `isCommand(text: string)` — exported: **yes** — Implements logic starting with: { return /^\/\w/.test(text); } (missing: @param text, @returns)
- `src/resources/extensions/remote-questions/commands.ts:118` — `buildStatus(basePath: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @returns)
- `src/resources/extensions/remote-questions/commands.ts:175` — `buildProgress(basePath: string)` — exported: **no** — Transforms a collection into a derived result. (missing: @param basePath, @returns)
- `src/resources/extensions/remote-questions/commands.ts:350` — `tryImportModule(specifier: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param specifier, @returns)

### `src/resources/extensions/remote-questions/discord-adapter.ts`
- `src/resources/extensions/remote-questions/discord-adapter.ts:91` — `DiscordAdapter.acknowledgeAnswer(ref: RemotePromptRef)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ref)

### `src/resources/extensions/remote-questions/http-client.ts`
- `src/resources/extensions/remote-questions/http-client.ts:32` — `apiRequest(url: string, method: "GET" | "POST" | "PUT" | "DELETE", body?: unknown, options: ApiRequestOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param url, @param method, @param body, @param options, @returns)

### `src/resources/extensions/remote-questions/manager.ts`
- `src/resources/extensions/remote-questions/manager.ts:97` — `isRemoteConfigured()` — exported: **yes** — Implements logic starting with: { return resolveRemoteConfig() !== null; } (missing: @returns)
- `src/resources/extensions/remote-questions/manager.ts:178` — `toRoundResultResponse(answer: RemoteAnswer)` — exported: **yes** — Implements logic starting with: { const normalized: RoundResult["answers"] = {}; for (const [id, data] of Object.entries(answer.answers)) { const list = data.answers ?? []; const… (missing: @param answer, @returns)

### `src/resources/extensions/remote-questions/notify.ts`
- `src/resources/extensions/remote-questions/notify.ts:21` — `sendRemoteNotification(title: string, message: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param title, @param message)

### `src/resources/extensions/remote-questions/telegram-adapter.ts`
- `src/resources/extensions/remote-questions/telegram-adapter.ts:141` — `TelegramAdapter.pollAndHandleCommands(basePath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param basePath, @returns)
- `src/resources/extensions/remote-questions/telegram-adapter.ts:187` — `TelegramAdapter.acknowledgeAnswer(ref: RemotePromptRef)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ref)

### `src/resources/extensions/search-the-web/cache.ts`
- `src/resources/extensions/search-the-web/cache.ts:10` — `class LRUTTLCache` — exported: **yes** — Defines an API surface. (missing: @returns)

### `src/resources/extensions/search-the-web/format.ts`
- `src/resources/extensions/search-the-web/format.ts:27` — `snippetsPerResult(resultCount: number)` — exported: **no** — Implements logic starting with: { if (resultCount <= 2) return 5; // show all available if (resultCount <= 4) return 3; if (resultCount <= 6) return 2; if (resultCount <= 8) retur… (missing: @param resultCount, @returns)
- `src/resources/extensions/search-the-web/format.ts:59` — `formatSearchResults(query: string, results: SearchResultFormatted[], options: FormatSearchOptions = {})` — exported: **yes** — Implements logic starting with: { const parts: string[] = []; // Header const cacheTag = options.cached ? " (cached)" : ""; parts.push(`Search: "${query}"${cacheTag}`); // Spellch… (missing: @param query, @param results, @param options, @returns)
- `src/resources/extensions/search-the-web/format.ts:139` — `formatPageContent(url: string, content: string, options: FormatPageOptions)` — exported: **yes** — Implements logic starting with: { const domain = extractDomain(url); const title = options.title ? ` — ${options.title}` : ""; const truncNote = options.truncated && options.origi… (missing: @param url, @param content, @param options, @returns)
- `src/resources/extensions/search-the-web/format.ts:186` — `formatLLMContext(query: string, grounding: LLMContextSnippet[], sources: Record<string, LLMContextSource>, options: { cached?: boolean; tokenCount?: number } = {})` — exported: **yes** — Implements logic starting with: { const parts: string[] = []; const cacheTag = options.cached ? " (cached)" : ""; const tokenTag = options.tokenCount ? ` (~${Math.round(options.to… (missing: @param query, @param grounding, @param sources, @param options, @returns)
- `src/resources/extensions/search-the-web/format.ts:232` — `formatMultiplePages(pages: Array<{ url: string; title?: string; content: string; charCount: number; error?: string; }>)` — exported: **yes** — Implements logic starting with: { const parts: string[] = []; for (const page of pages) { const domain = extractDomain(page.url); if (page.error) { parts.push(`[✗] ${domain}: ${pa… (missing: @param pages, @returns)

### `src/resources/extensions/search-the-web/http.ts`
- `src/resources/extensions/search-the-web/http.ts:10` — `class HttpError` — exported: **yes** — Defines an API surface. (missing: @returns)
- `src/resources/extensions/search-the-web/http.ts:76` — `extractRateLimitInfo(response: Response)` — exported: **yes** — Implements logic starting with: { const remaining = response.headers.get("x-ratelimit-remaining"); const limit = response.headers.get("x-ratelimit-limit"); const reset = response.… (missing: @param response, @returns)
- `src/resources/extensions/search-the-web/http.ts:115` — `anySignal(signals: AbortSignal[])` — exported: **yes** — Implements logic starting with: { const controller = new AbortController(); for (const sig of signals) { if (sig.aborted) { controller.abort(sig.reason); break; } sig.addEventList… (missing: @param signals, @returns)
- `src/resources/extensions/search-the-web/http.ts:135` — `fetchWithRetry(url: string, options: RequestInit, maxRetries: number = 2)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: @param url, @param options, @param maxRetries, @returns)
- `src/resources/extensions/search-the-web/http.ts:194` — `fetchSimple(url: string, options: RequestInit & { timeoutMs?: number } = {})` — exported: **yes** — Performs an HTTP request and returns a result. (missing: @param url, @param options, @returns)
- `src/resources/extensions/search-the-web/http.ts:228` — `fetchWithRetryTimed(url: string, options: RequestInit, maxRetries: number = 2)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param url, @param options, @param maxRetries, @returns)

### `src/resources/extensions/search-the-web/native-search.ts`
- `src/resources/extensions/search-the-web/native-search.ts:47` — `supportsNativeWebSearch(model: { api?: string; provider?: string } | null | undefined)` — exported: **yes** — Implements logic starting with: { if (!isAnthropicApi(model)) return false; const provider = model?.provider; return typeof provider === "string" && NATIVE_WEB_SEARCH_PROVIDERS.ha… (missing: @param model, @returns)
- `src/resources/extensions/search-the-web/native-search.ts:66` — `preferBraveSearch()` — exported: **yes** — Implements logic starting with: { // PREFERENCES.md takes priority over env var const prefsPref = resolveSearchProviderFromPreferences(); if (prefsPref === "brave" || prefsPref ==… (missing: @returns)
- `src/resources/extensions/search-the-web/native-search.ts:98` — `stripThinkingFromHistory(messages: Array<Record<string, unknown>>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param messages)
- `src/resources/extensions/search-the-web/native-search.ts:119` — `registerNativeSearchHooks(pi: NativeSearchPI)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param pi, @returns)

### `src/resources/extensions/search-the-web/provider.ts`
- `src/resources/extensions/search-the-web/provider.ts:31` — `getTavilyApiKey()` — exported: **yes** — Implements logic starting with: { return process.env.TAVILY_API_KEY || '' } (missing: @returns)
- `src/resources/extensions/search-the-web/provider.ts:36` — `getBraveApiKey()` — exported: **yes** — Implements logic starting with: { return process.env.BRAVE_API_KEY || '' } (missing: @returns)
- `src/resources/extensions/search-the-web/provider.ts:41` — `braveHeaders()` — exported: **yes** — Implements logic starting with: { return { "Accept": "application/json", "Accept-Encoding": "gzip", "X-Subscription-Token": getBraveApiKey(), } } (missing: @returns)
- `src/resources/extensions/search-the-web/provider.ts:50` — `getOllamaApiKey()` — exported: **yes** — Implements logic starting with: { return process.env.OLLAMA_API_KEY || '' } (missing: @returns)
- `src/resources/extensions/search-the-web/provider.ts:60` — `getSearchProviderPreference(authPath?: string)` — exported: **yes** — Implements logic starting with: { const auth = AuthStorage.create(authPath ?? authFilePath()) const cred = auth.get(PREFERENCE_KEY) if (cred?.type === 'api_key' && typeof cred.key… (missing: @returns)
- `src/resources/extensions/search-the-web/provider.ts:95` — `resolveSearchProvider(overridePreference?: string)` — exported: **yes** — Implements logic starting with: { const tavilyKey = getTavilyApiKey() const braveKey = getBraveApiKey() const ollamaKey = getOllamaApiKey() const hasTavily = tavilyKey.length > 0… (missing: @returns)

### `src/resources/extensions/search-the-web/tavily.ts`
- `src/resources/extensions/search-the-web/tavily.ts:47` — `normalizeTavilyResult(r: TavilyResult)` — exported: **yes** — Implements logic starting with: { return { title: r.title || "(untitled)", url: r.url, description: r.content || "", age: r.published_date ? publishedDateToAge(r.published_date) :… (missing: @param r, @returns)
- `src/resources/extensions/search-the-web/tavily.ts:66` — `publishedDateToAge(isoDate: string)` — exported: **yes** — Implements logic starting with: { const date = new Date(isoDate); if (isNaN(date.getTime())) return undefined; const now = Date.now(); const diffMs = now - date.getTime(); // Futu… (missing: @param isoDate, @returns)
- `src/resources/extensions/search-the-web/tavily.ts:113` — `mapFreshnessToTavily(braveFreshness: string | null)` — exported: **yes** — Implements logic starting with: { if (braveFreshness === null) return null; return BRAVE_TO_TAVILY_FRESHNESS[braveFreshness] ?? null; } (missing: @param braveFreshness, @returns)

### `src/resources/extensions/search-the-web/tool-fetch-page.ts`
- `src/resources/extensions/search-the-web/tool-fetch-page.ts:44` — `fetchViaJina(url: string, options: { signal?: AbortSignal; selector?: string } = {})` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param url, @param options, @returns)
- `src/resources/extensions/search-the-web/tool-fetch-page.ts:102` — `fetchDirectFallback(url: string, signal?: AbortSignal)` — exported: **no** — Parses JSON input into a structured value. (missing: @param url, @param signal, @returns)
- `src/resources/extensions/search-the-web/tool-fetch-page.ts:191` — `fetchViaOllama(url: string, signal?: AbortSignal)` — exported: **no** — Serializes a value to JSON. (missing: @param url, @param signal, @returns)
- `src/resources/extensions/search-the-web/tool-fetch-page.ts:222` — `smartTruncate(content: string, maxChars: number, offset: number = 0)` — exported: **no** — Implements logic starting with: { // Apply offset first const sliced = offset > 0 ? content.slice(offset) : content; if (sliced.length <= maxChars) { return { content: sliced, tru… (missing: @param content, @param maxChars, @param offset, @returns)

### `src/resources/extensions/search-the-web/tool-llm-context.ts`
- `src/resources/extensions/search-the-web/tool-llm-context.ts:98` — `estimateTokens(text: string)` — exported: **no** — Implements logic starting with: { return Math.ceil(text.length / 4); } (missing: @param text, @returns)
- `src/resources/extensions/search-the-web/tool-llm-context.ts:191` — `executeTavilyLLMContext(params: { query: string; maxTokens: number; maxUrls: number; threshold: string; count: number }, signal?: AbortSignal)` — exported: **no** — Serializes a value to JSON. (missing: @param params, @param signal, @returns)
- `src/resources/extensions/search-the-web/tool-llm-context.ts:241` — `executeOllamaLLMContext(params: { query: string; maxTokens: number; count: number; threshold: string }, signal?: AbortSignal)` — exported: **no** — Serializes a value to JSON. (missing: @param params, @param signal, @returns)

### `src/resources/extensions/search-the-web/tool-search.ts`
- `src/resources/extensions/search-the-web/tool-search.ts:135` — `normalizeBraveResult(r: BraveWebResult)` — exported: **no** — Implements logic starting with: { return { title: r.title || "(untitled)", url: r.url, description: r.description || "", age: r.age || r.page_age || undefined, extra_snippets: r.e… (missing: @param r, @returns)
- `src/resources/extensions/search-the-web/tool-search.ts:148` — `deduplicateResults(results: SearchResultFormatted[])` — exported: **no** — Builds an in-memory lookup structure. (missing: @param results, @returns)
- `src/resources/extensions/search-the-web/tool-search.ts:162` — `fetchSummary(summarizerKey: string, signal?: AbortSignal)` — exported: **no** — Transforms a collection into a derived result. (missing: @param summarizerKey, @param signal, @returns)
- `src/resources/extensions/search-the-web/tool-search.ts:205` — `executeTavilySearch(params: { query: string; freshness: string | null; domain?: string; wantSummary: boolean }, signal?: AbortSignal)` — exported: **no** — Serializes a value to JSON. (missing: @param params, @param signal, @returns)
- `src/resources/extensions/search-the-web/tool-search.ts:272` — `executeOllamaSearch(params: { query: string; count: number }, signal?: AbortSignal)` — exported: **no** — Serializes a value to JSON. (missing: @param params, @param signal, @returns)

### `src/resources/extensions/search-the-web/url-utils.ts`
- `src/resources/extensions/search-the-web/url-utils.ts:33` — `setFetchAllowedUrls(hostnames: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param hostnames)
- `src/resources/extensions/search-the-web/url-utils.ts:38` — `getFetchAllowedUrls()` — exported: **yes** — Implements logic starting with: { return [...fetchAllowedHostnames]; } (missing: @returns)
- `src/resources/extensions/search-the-web/url-utils.ts:59` — `normalizeQuery(query: string)` — exported: **yes** — Implements logic starting with: { return query.trim().toLowerCase().replace(/\s+/g, " ").normalize("NFC"); } (missing: @param query, @returns)
- `src/resources/extensions/search-the-web/url-utils.ts:68` — `toDedupeKey(url: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param url, @returns)
- `src/resources/extensions/search-the-web/url-utils.ts:98` — `extractDomain(url: string)` — exported: **yes** — Implements logic starting with: { try { return new URL(url).hostname.replace(/^www\./, ""); } catch { return url; } } (missing: @param url, @returns)
- `src/resources/extensions/search-the-web/url-utils.ts:110` — `detectFreshness(query: string)` — exported: **yes** — Implements logic starting with: { const q = query.toLowerCase(); // Explicit year references for current/recent years const currentYear = new Date().getFullYear(); for (let y = cu… (missing: @param query, @returns)
- `src/resources/extensions/search-the-web/url-utils.ts:137` — `detectDomainHints(query: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param query, @returns)

### `src/resources/extensions/shared/confirm-ui.ts`
- `src/resources/extensions/shared/confirm-ui.ts:38` — `showConfirm(ctx: ExtensionContext, opts: ConfirmOptions)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param ctx, @param opts, @returns)

### `src/resources/extensions/shared/format-utils.ts`
- `src/resources/extensions/shared/format-utils.ts:13` — `formatDuration(ms: number)` — exported: **yes** — Implements logic starting with: { if (ms > 0 && ms < 1000) return `${ms}ms`; const s = Math.floor(ms / 1000); if (s < 60) return `${s}s`; const m = Math.floor(s / 60); const rs =… (missing: @param ms, @returns)
- `src/resources/extensions/shared/format-utils.ts:28` — `formatTokenCount(count: number)` — exported: **yes** — Implements logic starting with: { if (count < 1000) return `${count}`; if (count < 1_000_000) return `${(count / 1000).toFixed(1)}k`; return `${(count / 1_000_000).toFixed(2)}M`; } (missing: @param count, @returns)
- `src/resources/extensions/shared/format-utils.ts:37` — `truncateWithEllipsis(text: string, maxLength: number)` — exported: **yes** — Implements logic starting with: { if (text.length <= maxLength) return text return text.slice(0, maxLength - 1) + "…" } (missing: @param text, @param maxLength, @returns)
- `src/resources/extensions/shared/format-utils.ts:48` — `sparkline(values: number[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param values, @returns)
- `src/resources/extensions/shared/format-utils.ts:62` — `formatDateShort(iso: string)` — exported: **yes** — Implements logic starting with: { try { const d = new Date(iso); return d.toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: '2-digit', minute: '2-di… (missing: @param iso, @returns)
- `src/resources/extensions/shared/format-utils.ts:72` — `fileLink(filePath: string, displayText?: string)` — exported: **yes** — Implements logic starting with: { const uri = `file://${filePath}`; const label = displayText ?? filePath; return `\x1b]8;;${uri}\x07${label}\x1b]8;;\x07`; } (missing: @param filePath, @param displayText, @returns)
- `src/resources/extensions/shared/format-utils.ts:81` — `stripAnsi(s: string)` — exported: **yes** — Implements logic starting with: { return s.replace(/\x1b\[[0-9;]*m/g, ""); } (missing: @param s, @returns)
- `src/resources/extensions/shared/format-utils.ts:92` — `normalizeStringArray(value: unknown, options?: { dedupe?: boolean })` — exported: **yes** — Transforms a collection into a derived result. (missing: @param value, @param options, @returns)

### `src/resources/extensions/shared/frontmatter.ts`
- `src/resources/extensions/shared/frontmatter.ts:5` — `stripQuotes(s: string)` — exported: **no** — Implements logic starting with: { if (s.length >= 2 && ((s[0] === '"' && s[s.length - 1] === '"') || (s[0] === "'" && s[s.length - 1] === "'"))) { return s.slice(1, -1); } return… (missing: @param s, @returns)
- `src/resources/extensions/shared/frontmatter.ts:16` — `splitFrontmatter(content: string)` — exported: **yes** — Implements logic starting with: { const trimmed = content.trimStart(); if (!trimmed.startsWith('---')) return [null, content]; const afterFirst = trimmed.indexOf('\n'); if (afterF… (missing: @param content, @returns)
- `src/resources/extensions/shared/frontmatter.ts:38` — `parseFrontmatterMap(lines: string[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param lines, @returns)

### `src/resources/extensions/shared/gsd-phase-state.ts`
- `src/resources/extensions/shared/gsd-phase-state.ts:49` — `activateGSD(context?: GSDPhaseAuditContext)` — exported: **yes** — Implements logic starting with: { if (context) _auditContext = context; const previousPhase = _currentPhase; _active = true; emitPhaseChange("activate", previousPhase, _currentPha… (missing: @param context)
- `src/resources/extensions/shared/gsd-phase-state.ts:66` — `setCurrentPhase(phase: string, context?: GSDPhaseAuditContext)` — exported: **yes** — Implements logic starting with: { if (context) _auditContext = context; if (!_active) { process.emitWarning(`Ignoring GSD phase "${phase}" while GSD auto-mode is inactive`, { code… (missing: @param phase, @param context, @returns)
- `src/resources/extensions/shared/gsd-phase-state.ts:88` — `isGSDActive()` — exported: **yes** — Implements logic starting with: { return _active; } (missing: @returns)
- `src/resources/extensions/shared/gsd-phase-state.ts:93` — `getCurrentPhase()` — exported: **yes** — Implements logic starting with: { return _active ? _currentPhase : null; } (missing: @returns)

### `src/resources/extensions/shared/layout-utils.ts`
- `src/resources/extensions/shared/layout-utils.ts:15` — `padRight(content: string, width: number)` — exported: **yes** — Implements logic starting with: { const vis = visibleWidth(content); return content + " ".repeat(Math.max(0, width - vis)); } (missing: @param content, @param width, @returns)
- `src/resources/extensions/shared/layout-utils.ts:21` — `joinColumns(left: string, right: string, width: number)` — exported: **yes** — Implements logic starting with: { const leftW = visibleWidth(left); const rightW = visibleWidth(right); if (leftW + rightW + 2 > width) { return truncateToWidth(`${left} ${right}`… (missing: @param left, @param right, @param width, @returns)
- `src/resources/extensions/shared/layout-utils.ts:31` — `centerLine(content: string, width: number)` — exported: **yes** — Implements logic starting with: { const vis = visibleWidth(content); if (vis >= width) return truncateToWidth(content, width); const leftPad = Math.floor((width - vis) / 2); retur… (missing: @param content, @param width, @returns)
- `src/resources/extensions/shared/layout-utils.ts:39` — `fitColumns(parts: string[], width: number, separator = " ")` — exported: **yes** — Transforms a collection into a derived result. (missing: @param parts, @param width, @param separator, @returns)
- `src/resources/extensions/shared/layout-utils.ts:57` — `mergeSideBySide(leftLines: string[], rightLines: string[], leftWidth: number, divider: string, totalWidth: number)` — exported: **yes** — Implements logic starting with: { const maxLen = Math.max(leftLines.length, rightLines.length); const merged: string[] = []; for (let i = 0; i < maxLen; i++) { const left = i < le… (missing: @param leftLines, @param rightLines, @param leftWidth, @param divider, @param totalWidth, @returns)

### `src/resources/extensions/shared/next-action-ui.ts`
- `src/resources/extensions/shared/next-action-ui.ts:97` — `showNextAction(ctx: ExtensionCommandContext, opts: NextActionOptions)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param ctx, @param opts, @returns)

### `src/resources/extensions/shared/path-display.ts`
- `src/resources/extensions/shared/path-display.ts:17` — `toPosixPath(fsPath: string)` — exported: **yes** — Implements logic starting with: { return fsPath.replaceAll("\\", "/"); } (missing: @param fsPath, @returns)

### `src/resources/extensions/shared/sanitize.ts`
- `src/resources/extensions/shared/sanitize.ts:28` — `maskEditorLine(line: string)` — exported: **yes** — Executes a subprocess command. (missing: @param line, @returns)

### `src/resources/extensions/shared/terminal.ts`
- `src/resources/extensions/shared/terminal.ts:25` — `shortcutDesc(base: string, fallbackCmd: string)` — exported: **yes** — Implements logic starting with: { if (supportsCtrlAltShortcuts()) return base; return `${base} — shortcut may not work in this terminal, use ${fallbackCmd}`; } (missing: @param base, @param fallbackCmd, @returns)

### `src/resources/extensions/shared/tests/interview-notes-loop.test.ts`
- `src/resources/extensions/shared/tests/interview-notes-loop.test.ts:30` — `runWithInputs(questions: Question[], inputs: string[])` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param questions, @param inputs, @returns)

### `src/resources/extensions/shared/tests/interview-preview.test.ts`
- `src/resources/extensions/shared/tests/interview-preview.test.ts:100` — `questionHasAnyPreview(options: QuestionOption[])` — exported: **no** — Implements logic starting with: { return options.some((o) => o.preview != null && o.preview.trim().length > 0); } (missing: @param options, @returns)

### `src/resources/extensions/shared/ui.ts`
- `src/resources/extensions/shared/ui.ts:201` — `makeUI(theme: Theme, width: number)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param theme, @param width, @returns)

### `src/resources/extensions/subagent/worker-registry.ts`
- `src/resources/extensions/subagent/worker-registry.ts:28` — `registerWorker(agent: string, task: string, index: number, batchSize: number, batchId: string)` — exported: **yes** — Implements logic starting with: { const id = `worker-${++workerIdCounter}`; activeWorkers.set(id, { id, agent, task, startedAt: Date.now(), status: "running", index, batchSize, ba… (missing: @param agent, @param task, @param index, @param batchSize, @param batchId, @returns)
- `src/resources/extensions/subagent/worker-registry.ts:52` — `updateWorker(id: string, status: "completed" | "failed")` — exported: **yes** — Implements logic starting with: { const entry = activeWorkers.get(id); if (entry) { entry.status = status; // Remove after a brief display window (5 seconds) // unref() so the tim… (missing: @param id, @param status)
- `src/resources/extensions/subagent/worker-registry.ts:67` — `getActiveWorkers()` — exported: **yes** — Implements logic starting with: { return Array.from(activeWorkers.values()); } (missing: @returns)
- `src/resources/extensions/subagent/worker-registry.ts:74` — `getWorkerBatches()` — exported: **yes** — Builds an in-memory lookup structure. (missing: @returns)
- `src/resources/extensions/subagent/worker-registry.ts:87` — `hasActiveWorkers()` — exported: **yes** — Implements logic starting with: { for (const worker of activeWorkers.values()) { if (worker.status === "running") return true; } return false; } (missing: @returns)

### `src/resources/extensions/ttsr/index.ts`
- `src/resources/extensions/ttsr/index.ts:43` — `extractDeltaContext(event: AssistantMessageEvent)` — exported: **no** — Implements logic starting with: { if (event.type === "text_delta") { return { delta: event.delta, context: { source: "text", streamKey: "text" }, }; } if (event.type === "thinking… (missing: @param event, @returns)

### `src/resources/extensions/ttsr/rule-loader.ts`
- `src/resources/extensions/ttsr/rule-loader.ts:62` — `loadRules(cwd: string)` — exported: **yes** — Builds an in-memory lookup structure. (missing: @param cwd, @returns)

### `src/resources/extensions/ttsr/ttsr-manager.ts`
- `src/resources/extensions/ttsr/ttsr-manager.ts:324` — `TtsrManager.addRule(rule: Rule)` — exported: **yes** — Implements logic starting with: { if (this.#rules.has(rule.name)) return false; const conditions = this.#compileConditions(rule); if (conditions.length === 0) return false; const… (missing: @param rule, @returns)
- `src/resources/extensions/ttsr/ttsr-manager.ts:349` — `TtsrManager.checkDelta(delta: string, context: TtsrMatchContext)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param delta, @param context, @returns)
- `src/resources/extensions/ttsr/ttsr-manager.ts:406` — `TtsrManager.markInjected(rulesToMark: Rule[])` — exported: **yes** — Transforms a collection into a derived result. (missing: @param rulesToMark)
- `src/resources/extensions/ttsr/ttsr-manager.ts:411` — `TtsrManager.markInjectedByNames(ruleNames: string[])` — exported: **yes** — Implements logic starting with: { for (const rawName of ruleNames) { const ruleName = rawName.trim(); if (ruleName.length === 0) continue; const record = this.#injectionRecords.ge… (missing: @param ruleNames)
- `src/resources/extensions/ttsr/ttsr-manager.ts:425` — `TtsrManager.getInjectedRuleNames()` — exported: **yes** — Implements logic starting with: { return Array.from(this.#injectionRecords.keys()); } (missing: @returns)
- `src/resources/extensions/ttsr/ttsr-manager.ts:430` — `TtsrManager.restoreInjected(ruleNames: string[])` — exported: **yes** — Implements logic starting with: { for (const name of ruleNames) { this.#injectionRecords.set(name, { lastInjectedAt: 0 }); } } (missing: @param ruleNames)
- `src/resources/extensions/ttsr/ttsr-manager.ts:443` — `TtsrManager.hasRules()` — exported: **yes** — Implements logic starting with: { return this.#rules.size > 0; } (missing: @returns)
- `src/resources/extensions/ttsr/ttsr-manager.ts:453` — `TtsrManager.getMessageCount()` — exported: **yes** — Implements logic starting with: { return this.#messageCount; } (missing: @returns)
- `src/resources/extensions/ttsr/ttsr-manager.ts:458` — `TtsrManager.getSettings()` — exported: **yes** — Implements logic starting with: { return this.#settings; } (missing: @returns)

### `src/resources/extensions/universal-config/discovery.ts`
- `src/resources/extensions/universal-config/discovery.ts:14` — `pLimit(concurrency: number)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param concurrency, @returns)

### `src/resources/extensions/universal-config/format.ts`
- `src/resources/extensions/universal-config/format.ts:12` — `formatDiscoveryForTool(result: DiscoveryResult)` — exported: **yes** — Implements logic starting with: { const lines: string[] = []; const { summary } = result; lines.push(`Universal Config Discovery — ${summary.toolsWithConfig}/${summary.toolsScanne… (missing: @param result, @returns)
- `src/resources/extensions/universal-config/format.ts:111` — `formatDiscoveryForCommand(result: DiscoveryResult)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param result, @returns)

### `src/resources/extensions/universal-config/scanners.ts`
- `src/resources/extensions/universal-config/scanners.ts:103` — `parseFrontmatter(content: string)` — exported: **no** — Implements logic starting with: { const match = content.match(/^---\s*\n([\s\S]*?)\n---\s*\n([\s\S]*)$/); if (!match) { return { frontmatter: {}, body: content }; } const rawFm =… (missing: @param content, @returns)
- `src/resources/extensions/universal-config/scanners.ts:139` — `parseMcpServersFromJson(json: Record<string, unknown>, filePath: string, tool: ToolInfo, level: ConfigLevel)` — exported: **no** — Implements logic starting with: { const servers: DiscoveredMCPServer[] = []; const mcpServers = json.mcpServers as Record<string, unknown> | undefined; if (!mcpServers || typeof m… (missing: @param json, @param filePath, @param tool, @param level, @returns)

### `src/resources/extensions/voice/linux-ready.ts`
- `src/resources/extensions/voice/linux-ready.ts:22` — `linuxPython()` — exported: **yes** — Performs filesystem I/O. (missing: @returns)
- `src/resources/extensions/voice/linux-ready.ts:39` — `diagnoseSounddeviceError(stderr: string)` — exported: **yes** — Implements logic starting with: { // Check for missing Python module FIRST — the error message // "ModuleNotFoundError: No module named 'sounddevice'" contains the word // "soundd… (missing: @param stderr, @returns)
- `src/resources/extensions/voice/linux-ready.ts:67` — `ensureVoiceVenv(cb: ReadinessCallbacks)` — exported: **yes** — Performs filesystem I/O. (missing: @param cb, @returns)

### `src/runtime-checks.ts`
- `src/runtime-checks.ts:19` — `checkNodeVersion(versionString: string, min: number = MIN_NODE_MAJOR)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param versionString, @param min, @returns)
- `src/runtime-checks.ts:36` — `requireGit(execFn: (cmd: string, args: ReadonlyArray<string>) => unknown)` — exported: **yes** — Implements logic starting with: { try { execFn('git', ['--version']) return true } catch { return false } } (missing: @param execFn, @returns)

### `src/startup-model-validation.ts`
- `src/startup-model-validation.ts:44` — `validateConfiguredModel(modelRegistry: MinimalModelRegistry, settingsManager: MinimalSettingsManager)` — exported: **yes** — Implements logic starting with: { const configuredProvider = settingsManager.getDefaultProvider() const configuredModel = settingsManager.getDefaultModel() const availableModels =… (missing: @param modelRegistry, @param settingsManager)

### `src/tests/auto-piped-io.test.ts`
- `src/tests/auto-piped-io.test.ts:39` — `canEnterInteractiveMode(stdinIsTTY: boolean, stdoutIsTTY: boolean)` — exported: **no** — Implements logic starting with: { return stdinIsTTY && stdoutIsTTY; } (missing: @param stdinIsTTY, @param stdoutIsTTY, @returns)
- `src/tests/auto-piped-io.test.ts:50` — `isExplicitSubcommand(subcommand: string | undefined)` — exported: **no** — Implements logic starting with: { return subcommand !== undefined && EXPLICIT_SUBCOMMANDS.has(subcommand); } (missing: @param subcommand, @returns)

### `src/tests/ensure-workspace-builds.test.ts`
- `src/tests/ensure-workspace-builds.test.ts:77` — `createFakePackage(packagesDir: string, pkgName: string, opts: { srcNewerThanDist?: boolean; missingDist?: boolean } = {})` — exported: **no** — Performs filesystem I/O. (missing: @param packagesDir, @param pkgName, @param opts)

### `src/tests/extension-model-validation.test.ts`
- `src/tests/extension-model-validation.test.ts:27` — `fakeModelRegistry(models: Array<{ provider: string; id: string }>)` — exported: **no** — Transforms a collection into a derived result. (missing: @param models, @returns)
- `src/tests/extension-model-validation.test.ts:44` — `fakeSettingsManager(initial: { provider?: string; model?: string })` — exported: **no** — Implements logic starting with: { let provider = initial.provider; let model = initial.model; let thinkingLevel = "off" as string; return { getDefaultProvider: () => provider, get… (missing: @param initial, @returns)

### `src/tests/fetch-test-helpers.ts`
- `src/tests/fetch-test-helpers.ts:6` — `normalizeHeaders(headers: HeadersInit | undefined)` — exported: **yes** — Implements logic starting with: { if (headers == null) return undefined; if (headers instanceof Headers) { const result: Record<string, string> = {}; headers.forEach((v, k) => { r… (missing: @param headers, @returns)

### `src/tests/google-search-auth.repro.test.ts`
- `src/tests/google-search-auth.repro.test.ts:32` — `mockModelRegistry(oauthJson?: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param oauthJson, @returns)

### `src/tests/google-search-oauth-shape.test.ts`
- `src/tests/google-search-oauth-shape.test.ts:52` — `makeOkSSEBody()` — exported: **no** — Serializes a value to JSON. (missing: @returns)

### `src/tests/integration/e2e-headless.test.ts`
- `src/tests/integration/e2e-headless.test.ts:47` — `runGsd(args: string[], timeoutMs = 30_000, env: NodeJS.ProcessEnv = {}, cwd: string = projectRoot)` — exported: **no** — Executes a subprocess command. (missing: @param args, @param timeoutMs, @param env, @param cwd, @returns)
- `src/tests/integration/e2e-headless.test.ts:85` — `spawnGsd(args: string[], timeoutMs = 30_000, env: NodeJS.ProcessEnv = {}, cwd: string = projectRoot)` — exported: **no** — Executes a subprocess command. (missing: @param args, @param timeoutMs, @param env, @param cwd, @returns)
- `src/tests/integration/e2e-headless.test.ts:122` — `stripAnsi(s: string)` — exported: **no** — Implements logic starting with: { // eslint-disable-next-line no-control-regex return s.replace(/\x1b\[[0-9;]*[A-Za-z]/g, ""); } (missing: @param s, @returns)
- `src/tests/integration/e2e-headless.test.ts:128` — `createTempWithGsd(prefix: string)` — exported: **no** — Implements logic starting with: { const dir = mkdtempSync(join(tmpdir(), prefix)); mkdirSync(join(dir, ".gsd", "milestones"), { recursive: true }); mkdirSync(join(dir, ".gsd", "ru… (missing: @param prefix, @returns)
- `src/tests/integration/e2e-headless.test.ts:136` — `assertNoCrashMarkers(output: string)` — exported: **no** — Implements logic starting with: { const crashMarkers = [ "SyntaxError:", "ReferenceError:", "TypeError: Cannot read", "FATAL ERROR", "ERR_MODULE_NOT_FOUND", "Error: Cannot find mo… (missing: @param output)

### `src/tests/integration/e2e-smoke.test.ts`
- `src/tests/integration/e2e-smoke.test.ts:51` — `runGsd(args: string[], timeoutMs = 8_000, env: NodeJS.ProcessEnv = {}, cwd: string = projectRoot)` — exported: **no** — Executes a subprocess command. (missing: @returns)
- `src/tests/integration/e2e-smoke.test.ts:87` — `stripAnsi(s: string)` — exported: **no** — Implements logic starting with: { // eslint-disable-next-line no-control-regex return s.replace(/\x1b\[[0-9;]*[A-Za-z]/g, ""); } (missing: @param s, @returns)

### `src/tests/integration/pack-install.test.ts`
- `src/tests/integration/pack-install.test.ts:85` — `listTarEntries(tarballPath: string)` — exported: **no** — Performs filesystem I/O. (missing: @param tarballPath, @returns)

### `src/tests/integration/web-boot-node24.test.ts`
- `src/tests/integration/web-boot-node24.test.ts:110` — `simulateConsecutive5xxDetection(events: RetryEvent[], maxConsecutive: number)` — exported: **no** — Transforms a collection into a derived result. (missing: @param events, @param maxConsecutive, @returns)

### `src/tests/integration/web-live-interaction-contract.test.ts`
- `src/tests/integration/web-live-interaction-contract.test.ts:313` — `routeEvent(state: MinimalLiveState, event: any)` — exported: **no** — Implements logic starting with: { const s = { ...state }; switch (event.type) { case "extension_ui_request": { const method = event.method; if (method === "select" || method === "… (missing: @param state, @param event, @returns)

### `src/tests/integration/web-mode-assembled.test.ts`
- `src/tests/integration/web-mode-assembled.test.ts:185` — `readSseEvents(response: Response, count: number, perReadTimeoutMs = 3_000)` — exported: **no** — Parses JSON input into a structured value. (missing: @param response, @param count, @param perReadTimeoutMs, @returns)

### `src/tests/integration/web-mode-runtime-harness.ts`
- `src/tests/integration/web-mode-runtime-harness.ts:293` — `runtimeAuthHeaders(launch: RuntimeLaunchResult)` — exported: **yes** — Implements logic starting with: { if (!launch.authToken) return {} return { Authorization: `Bearer ${launch.authToken}` } } (missing: @param launch, @returns)

### `src/tests/integration/web-project-tab-preservation.test.ts`
- `src/tests/integration/web-project-tab-preservation.test.ts:73` — `simulateViewRestoreEffect(state: ChromeState, storage: MockSessionStorage)` — exported: **no** — Implements logic starting with: { // The fix: if projectPath changed, reset viewRestored // (In React this is a separate useEffect that depends on [projectPath]) if (!state.viewRe… (missing: @param state, @param storage, @returns)
- `src/tests/integration/web-project-tab-preservation.test.ts:93` — `simulateProjectSwitch(state: ChromeState, newProjectPath: string)` — exported: **no** — Implements logic starting with: { return { ...state, projectPath: newProjectPath, viewRestored: false, // <-- THE FIX: reset so restore runs for new project }; } (missing: @param state, @param newProjectPath, @returns)
- `src/tests/integration/web-project-tab-preservation.test.ts:107` — `handleSelectProjectPreFix(_state: ChromeState, _projectPath: string)` — exported: **no** — Implements logic starting with: { // Bug: always forces dashboard return "dashboard"; } (missing: @param _state, @param _projectPath, @returns)
- `src/tests/integration/web-project-tab-preservation.test.ts:116` — `handleSelectProjectPostFix(state: ChromeState, _projectPath: string)` — exported: **no** — Implements logic starting with: { // Fix: preserve whatever view is active (restore logic handles per-project view) return state.activeView; } (missing: @param state, @param _projectPath, @returns)

### `src/tests/integration/web-terminal-preservation.test.ts`
- `src/tests/integration/web-terminal-preservation.test.ts:22` — `buggyPageHideHandler(_event: { persisted: boolean })` — exported: **no** — Implements logic starting with: { // Current code (buggy): always sends beacon regardless of event.persisted return true; // true = beacon was sent } (missing: @param _event, @returns)
- `src/tests/integration/web-terminal-preservation.test.ts:33` — `fixedPageHideHandler(event: { persisted: boolean })` — exported: **no** — Implements logic starting with: { if (event.persisted) { // Page is entering bfcache (tab switch) — do NOT shut down return false; } return true; // true = beacon was sent } (missing: @param event, @returns)
- `src/tests/integration/web-terminal-preservation.test.ts:93` — `buggyMountTerminal(tracker: TerminalSessionTracker, _projectCwd: string)` — exported: **no** — Implements logic starting with: { const sessionId = "default"; // No project namespace — always the same ID tracker.serverSessions.set(sessionId, { alive: true, projectCwd: _proje… (missing: @param tracker, @param _projectCwd, @returns)
- `src/tests/integration/web-terminal-preservation.test.ts:104` — `unmountTerminal(tracker: TerminalSessionTracker, sessionId: string)` — exported: **no** — Implements logic starting with: { tracker.clientSessions.delete(sessionId); // Server session stays alive — this is the correct behavior } (missing: @param tracker, @param sessionId)
- `src/tests/integration/web-terminal-preservation.test.ts:113` — `fixedMountTerminal(tracker: TerminalSessionTracker, projectCwd: string)` — exported: **no** — Implements logic starting with: { const sessionId = `shell:${projectCwd}:default`; // getOrCreateSession on the server: if alive, returns existing; if dead, creates new if (!track… (missing: @param tracker, @param projectCwd, @returns)
- `src/tests/integration/web-terminal-preservation.test.ts:225` — `deriveSessionId(projectCwd: string | undefined, sessionPrefix?: string, command?: string)` — exported: **no** — Implements logic starting with: { const base = sessionPrefix ?? (command ? "gsd-default" : "default"); if (!projectCwd) return base; // Stable hash-like key from the project path… (missing: @param projectCwd, @param sessionPrefix, @param command, @returns)

### `src/tests/llm-context-tavily.test.ts`
- `src/tests/llm-context-tavily.test.ts:28` — `makeTavilyLLMResponse(overrides: Partial<{ results: TavilyResult[] }> = {})` — exported: **no** — Implements logic starting with: { return overrides.results ?? [ { title: "TypeScript Handbook", url: "https://typescriptlang.org/docs/handbook", content: "TypeScript is a typed su… (missing: @param overrides, @returns)
- `src/tests/llm-context-tavily.test.ts:68` — `mockFetch(responseBody: unknown, status = 200)` — exported: **no** — Serializes a value to JSON. (missing: @param responseBody, @param status, @returns)

### `src/tests/mcp-server.test.ts`
- `src/tests/mcp-server.test.ts:14` — `distUrl(filename: string)` — exported: **no** — Implements logic starting with: { return pathToFileURL(join(projectRoot, 'dist', filename)).href } (missing: @param filename, @returns)

### `src/tests/search-loop-guard.test.ts`
- `src/tests/search-loop-guard.test.ts:39` — `makeBraveResponse()` — exported: **no** — Implements logic starting with: { return { query: { original: "test query", more_results_available: false }, web: { results: [ { title: "Result One", url: "https://example.com/one… (missing: @returns)
- `src/tests/search-loop-guard.test.ts:55` — `mockFetch(body: unknown, status = 200)` — exported: **no** — Serializes a value to JSON. (missing: @param body, @param status, @returns)
- `src/tests/search-loop-guard.test.ts:70` — `createMockPI()` — exported: **no** — Builds an in-memory lookup structure. (missing: @returns)
- `src/tests/search-loop-guard.test.ts:105` — `callSearch(execute: (...args: any[]) => Promise<any>, query: string, callId = "call-1")` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param execute, @param query, @param callId, @returns)

### `src/tests/search-tavily.test.ts`
- `src/tests/search-tavily.test.ts:27` — `makeTavilyResponse(overrides: Record<string, unknown> = {})` — exported: **no** — Implements logic starting with: { return { query: "test query", answer: null, results: [ { title: "First Result", url: "https://example.com/first", content: "Description of first… (missing: @param overrides, @returns)
- `src/tests/search-tavily.test.ts:55` — `mockFetch(responseBody: unknown, status = 200)` — exported: **no** — Serializes a value to JSON. (missing: @param responseBody, @param status, @returns)

### `src/tests/secret-scan.test.ts`
- `src/tests/secret-scan.test.ts:31` — `scanContent(content: string, filename = "test-file.ts")` — exported: **no** — Performs filesystem I/O. (missing: @param content, @param filename, @returns)

### `src/update-check.ts`
- `src/update-check.ts:22` — `compareSemver(a: string, b: string)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param a, @param b, @returns)
- `src/update-check.ts:87` — `isBunInstall(argv1: string | undefined = process.argv[1])` — exported: **yes** — Implements logic starting with: { if ('bun' in process.versions) return true if (!argv1) return false const bunBinDirs: string[] = [] if (process.env.BUN_INSTALL) bunBinDirs.push(… (missing: @param argv1, @returns)
- `src/update-check.ts:125` — `checkForUpdates(options: UpdateCheckOptions = {})` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param options)
- `src/update-check.ts:166` — `checkAndPromptForUpdates(options: UpdateCheckOptions = {})` — exported: **yes** — Executes a subprocess command. (missing: @param options, @returns)

### `src/web-mode.ts`
- `src/web-mode.ts:14` — `openBrowser(url: string)` — exported: **no** — Implements logic starting with: { if (process.platform === 'win32') { // PowerShell's Start-Process handles URLs with '&' safely; cmd /c start does not. execFile('powershell', ['-… (missing: @param url)
- `src/web-mode.ts:529` — `cleanupStaleInstance(cwd: string, stderr: WritableLike, registryPath?: string)` — exported: **no** — Implements logic starting with: { const registry = readInstanceRegistry(registryPath) const key = resolve(cwd) const stale = registry[key] if (!stale) return stderr.write(`[gsd] C… (missing: @param cwd, @param stderr, @param registryPath)

### `src/web/auto-dashboard-service.ts`
- `src/web/auto-dashboard-service.ts:48` — `isPidAlive(pid: number)` — exported: **no** — Implements logic starting with: { try { process.kill(pid, 0); return true; } catch { return false; } } (missing: @param pid, @returns)
- `src/web/auto-dashboard-service.ts:67` — `reconcileWithDiskState(data: AutoDashboardData, projectCwd: string, checkExists: (path: string) => boolean)` — exported: **no** — Performs filesystem I/O. (missing: @param data, @param projectCwd, @param checkExists, @returns)

### `src/web/bridge-service.ts`
- `src/web/bridge-service.ts:493` — `detectMonorepo(dirPath: string, checkExists?: (path: string) => boolean)` — exported: **yes** — Performs filesystem I/O. (missing: @param dirPath, @param checkExists, @returns)
- `src/web/bridge-service.ts:1807` — `resolveProjectCwd(request: Request)` — exported: **yes** — Implements logic starting with: { try { const url = new URL(request.url); const projectParam = url.searchParams.get("project"); if (projectParam) return decodeURIComponent(project… (missing: @param request, @returns)
- `src/web/bridge-service.ts:1822` — `requireProjectCwd(request: Request)` — exported: **yes** — Implements logic starting with: { const cwd = resolveProjectCwd(request); if (!cwd) { throw new NoProjectError(); } return cwd; } (missing: @param request, @returns)

### `src/web/captures-service.ts`
- `src/web/captures-service.ts:22` — `collectCapturesData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)
- `src/web/captures-service.ts:93` — `resolveCaptureAction(request: CaptureResolveRequest, projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param request, @param projectCwdOverride, @returns)

### `src/web/cleanup-service.ts`
- `src/web/cleanup-service.ts:22` — `collectCleanupData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)
- `src/web/cleanup-service.ts:108` — `executeCleanup(deleteBranches: string[], pruneSnapshots: string[], projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param deleteBranches, @param pruneSnapshots, @param projectCwdOverride, @returns)

### `src/web/doctor-service.ts`
- `src/web/doctor-service.ts:61` — `collectDoctorData(scope?: string, projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param scope, @param projectCwdOverride, @returns)
- `src/web/doctor-service.ts:111` — `applyDoctorFixes(scope?: string, projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param scope, @param projectCwdOverride, @returns)

### `src/web/export-service.ts`
- `src/web/export-service.ts:22` — `collectExportData(format: "markdown" | "json" = "markdown", projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param format, @param projectCwdOverride, @returns)

### `src/web/forensics-service.ts`
- `src/web/forensics-service.ts:24` — `collectForensicsData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/web/history-service.ts`
- `src/web/history-service.ts:22` — `collectHistoryData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/web/hooks-service.ts`
- `src/web/hooks-service.ts:23` — `collectHooksData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/web/inspect-service.ts`
- `src/web/inspect-service.ts:11` — `collectInspectData(projectCwdOverride?: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectCwdOverride, @returns)

### `src/web/knowledge-service.ts`
- `src/web/knowledge-service.ts:12` — `collectKnowledgeData(projectCwdOverride?: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectCwdOverride, @returns)
- `src/web/knowledge-service.ts:41` — `parseKnowledgeFile(content: string)` — exported: **yes** — Executes a subprocess command. (missing: @param content, @returns)

### `src/web/project-discovery-service.ts`
- `src/web/project-discovery-service.ts:35` — `readProjectProgress(projectPath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectPath, @returns)
- `src/web/project-discovery-service.ts:84` — `discoverProjects(devRootPath: string, includeProgress?: boolean)` — exported: **yes** — Implements logic starting with: { try { // ── Check if the root itself is a project/monorepo ────────────── // If the devRoot has a .git repo AND looks like a monorepo (pnpm-works… (missing: @param devRootPath, @param includeProgress, @returns)

### `src/web/settings-service.ts`
- `src/web/settings-service.ts:25` — `collectSettingsData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/web/skill-health-service.ts`
- `src/web/skill-health-service.ts:21` — `collectSkillHealthData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/web/subprocess-runner.ts`
- `src/web/subprocess-runner.ts:50` — `resolveModulePaths(packageRoot: string, options: ResolveModulePathsOptions)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param packageRoot, @param options, @returns)
- `src/web/subprocess-runner.ts:117` — `runSubprocess(options: RunSubprocessOptions)` — exported: **yes** — Parses JSON input into a structured value. (missing: @param options, @returns)

### `src/web/ts-subprocess-flags.ts`
- `src/web/ts-subprocess-flags.ts:17` — `resolveTypeStrippingFlag(packageRoot: string)` — exported: **yes** — Implements logic starting with: { const needsTransform = isUnderNodeModules(packageRoot) && supportsTransformTypes() return needsTransform ? "--experimental-transform-types" : "--… (missing: @param packageRoot, @returns)
- `src/web/ts-subprocess-flags.ts:29` — `isUnderNodeModules(filePath: string)` — exported: **yes** — Implements logic starting with: { const normalized = filePath.replace(/\\/g, "/") return normalized.includes("/node_modules/") } (missing: @param filePath, @returns)
- `src/web/ts-subprocess-flags.ts:59` — `resolveSubprocessModule(packageRoot: string, relPath: string, checkExists: (path: string) => boolean = defaultExistsSync)` — exported: **yes** — Implements logic starting with: { if (isUnderNodeModules(packageRoot)) { const jsRelPath = relPath.replace(/\.ts$/, ".js") const distPath = join(packageRoot, "dist", jsRelPath) if… (missing: @returns)
- `src/web/ts-subprocess-flags.ts:87` — `buildSubprocessPrefixArgs(packageRoot: string, resolution: SubprocessModuleResolution, tsLoaderHref: string)` — exported: **yes** — Implements logic starting with: { if (resolution.useCompiledJs) { return ["--input-type=module"] } return [ "--import", tsLoaderHref, resolveTypeStrippingFlag(packageRoot), "--inp… (missing: @param packageRoot, @param resolution, @param tsLoaderHref, @returns)
- `src/web/ts-subprocess-flags.ts:107` — `supportsTransformTypes()` — exported: **no** — Transforms a collection into a derived result. (missing: @returns)

### `src/web/undo-service.ts`
- `src/web/undo-service.ts:23` — `collectUndoInfo(projectCwdOverride?: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param projectCwdOverride, @returns)
- `src/web/undo-service.ts:119` — `executeUndo(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/web/update-service.ts`
- `src/web/update-service.ts:66` — `triggerUpdate(targetVersion?: string)` — exported: **yes** — Executes a subprocess command. (missing: @param targetVersion, @returns)

### `src/web/visualizer-service.ts`
- `src/web/visualizer-service.ts:58` — `collectVisualizerData(projectCwdOverride?: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param projectCwdOverride, @returns)

### `src/welcome-screen.ts`
- `src/welcome-screen.ts:85` — `visLen(s: string)` — exported: **no** — Implements logic starting with: { return stripAnsi(s).length } (missing: @param s, @returns)
- `src/welcome-screen.ts:90` — `rpad(s: string, w: number)` — exported: **no** — Implements logic starting with: { const clamped = clampVisible(s, w) return clamped + ' '.repeat(Math.max(0, w - visLen(clamped))) } (missing: @param s, @param w, @returns)
- `src/welcome-screen.ts:107` — `clampVisible(s: string, w: number)` — exported: **no** — Implements logic starting with: { if (w <= 0) return '' if (visLen(s) <= w) return s const plain = stripAnsi(s) return plain.slice(0, Math.max(0, w - 1)) + '…' } (missing: @param s, @param w, @returns)

### `src/wizard.ts`
- `src/wizard.ts:15` — `loadStoredEnvKeys(authStorage: AuthStorage)` — exported: **yes** — Implements logic starting with: { const providers: Array<[string, string]> = [ ['brave', 'BRAVE_API_KEY'], ['brave_answers', 'BRAVE_ANSWERS_KEY'], ['context7', 'CONTEXT7_API_KEY']… (missing: @param authStorage)

### `tests/e2e/_shared/artifacts.ts`
- `tests/e2e/_shared/artifacts.ts:27` — `artifactsFor(testSlug: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param testSlug, @returns)

### `tests/e2e/_shared/fake-llm.ts`
- `tests/e2e/_shared/fake-llm.ts:45` — `writeTranscript(turns: TranscriptTurn[])` — exported: **yes** — Performs filesystem I/O. (missing: @param turns, @returns)
- `tests/e2e/_shared/fake-llm.ts:65` — `runWithFakeLlm(transcriptPath: string, opts: FakeRunOptions)` — exported: **yes** — Implements logic starting with: { const args = [ "--print", opts.prompt, "--model", "gsd-fake-model", "--mode", opts.mode ?? "text", ...(opts.extraArgs ?? []), ]; return gsdSync(a… (missing: @param transcriptPath, @param opts, @returns)
- `tests/e2e/_shared/fake-llm.ts:90` — `parseJsonEvents(stdout: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param stdout, @returns)
- `tests/e2e/_shared/fake-llm.ts:111` — `lastAssistantText(events: Array<Record<string, unknown>>)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param events, @returns)
- `tests/e2e/_shared/fake-llm.ts:132` — `lastAssistantStopReason(events: Array<Record<string, unknown>>)` — exported: **yes** — Implements logic starting with: { for (let i = events.length - 1; i >= 0; i--) { const ev = events[i]; if (ev.type === "agent_end") { const messages = (ev as { messages?: Array<Re… (missing: @param events, @returns)

### `tests/e2e/_shared/spawn.ts`
- `tests/e2e/_shared/spawn.ts:22` — `stripAnsi(s: string)` — exported: **yes** — Implements logic starting with: { return s.replace(ANSI_REGEX, ""); } (missing: @param s, @returns)
- `tests/e2e/_shared/spawn.ts:31` — `canonicalTmpdir()` — exported: **yes** — Implements logic starting with: { try { return realpathSync(tmpdir()); } catch { return tmpdir(); } } (missing: @returns)
- `tests/e2e/_shared/spawn.ts:74` — `buildE2eEnv(extra: Record<string, string> = {})` — exported: **yes** — Implements logic starting with: { const base: NodeJS.ProcessEnv = {}; for (const [k, v] of Object.entries(process.env)) { if (k.startsWith("GSD_")) continue; base[k] = v; } // For… (missing: @param extra, @returns)
- `tests/e2e/_shared/spawn.ts:109` — `resolveGsdInvocation(args: string[], binaryOverride?: string)` — exported: **yes** — Implements logic starting with: { const binary = binaryOverride ?? process.env.GSD_SMOKE_BINARY ?? "gsd"; if (binary === "gsd") { return { command: "gsd", argv: args }; } return {… (missing: @param args, @param binaryOverride, @returns)
- `tests/e2e/_shared/spawn.ts:121` — `gsdSync(args: string[], env: E2eEnv)` — exported: **yes** — Executes a subprocess command. (missing: @param args, @param env, @returns)
- `tests/e2e/_shared/spawn.ts:159` — `gsdAsync(args: string[], env: E2eEnv, opts: SpawnOptions = {})` — exported: **yes** — Validates inputs and throws on failure. (missing: @param args, @param env, @param opts, @returns)

### `tests/e2e/_shared/tmp-project.ts`
- `tests/e2e/_shared/tmp-project.ts:33` — `createTmpProject(opts: TmpProjectOptions = {})` — exported: **yes** — Performs filesystem I/O. (missing: @param opts, @returns)

### `tests/e2e/docker/runtime.e2e.test.ts`
- `tests/e2e/docker/runtime.e2e.test.ts:41` — `packToRoot()` — exported: **no** — Validates inputs and throws on failure. (missing: @returns)

### `tests/e2e/migration.e2e.test.ts`
- `tests/e2e/migration.e2e.test.ts:65` — `seedV20Db(sqlite: SqliteModule, dbPath: string)` — exported: **no** — Executes a subprocess command. (missing: @param sqlite, @param dbPath)

### `vscode-extension/src/activity-feed.ts`
- `vscode-extension/src/activity-feed.ts:65` — `class GsdActivityFeedProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/bash-terminal.ts`
- `vscode-extension/src/bash-terminal.ts:8` — `class GsdBashTerminal` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/change-tracker.ts`
- `vscode-extension/src/change-tracker.ts:35` — `class GsdChangeTracker` — exported: **yes** — Defines an API surface. (missing: @returns)
- `vscode-extension/src/change-tracker.ts:74` — `GsdChangeTracker.get modifiedFiles()` — exported: **yes** — Implements logic starting with: { return [...this.originals.keys()]; } (missing: @returns)
- `vscode-extension/src/change-tracker.ts:79` — `GsdChangeTracker.getOriginal(filePath: string)` — exported: **yes** — Implements logic starting with: { const original = this.originals.get(filePath); return original === undefined ? undefined : original ?? ""; } (missing: @param filePath, @returns)
- `vscode-extension/src/change-tracker.ts:85` — `GsdChangeTracker.get hasChanges()` — exported: **yes** — Implements logic starting with: { return this.originals.size > 0; } (missing: @returns)
- `vscode-extension/src/change-tracker.ts:90` — `GsdChangeTracker.get checkpoints()` — exported: **yes** — Implements logic starting with: { return this._checkpoints; } (missing: @returns)
- `vscode-extension/src/change-tracker.ts:98` — `GsdChangeTracker.discardFile(filePath: string)` — exported: **yes** — Performs filesystem I/O. (missing: @param filePath, @returns)
- `vscode-extension/src/change-tracker.ts:119` — `GsdChangeTracker.discardAll()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/change-tracker.ts:133` — `GsdChangeTracker.acceptFile(filePath: string)` — exported: **yes** — Implements logic starting with: { if (this.originals.delete(filePath)) { this._onDidChange.fire([filePath]); } } (missing: @param filePath)
- `vscode-extension/src/change-tracker.ts:153` — `GsdChangeTracker.restoreCheckpoint(checkpointId: number)` — exported: **yes** — Performs filesystem I/O. (missing: @param checkpointId, @returns)

### `vscode-extension/src/chat-participant.ts`
- `vscode-extension/src/chat-participant.ts:8` — `registerChatParticipant(context: vscode.ExtensionContext, client: GsdClient)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param context, @param client, @returns)
- `vscode-extension/src/chat-participant.ts:208` — `buildFileContext(request: vscode.ChatRequest)` — exported: **no** — Performs filesystem I/O. (missing: @param request, @returns)
- `vscode-extension/src/chat-participant.ts:244` — `describeToolCall(toolName: string, input?: Record<string, unknown>)` — exported: **no** — Implements logic starting with: { if (!input) { return `Running: ${toolName}`; } switch (toolName) { case "Read": return `Reading: ${shortenPath(String(input.file_path ?? ""))}`;… (missing: @param toolName, @param input, @returns)
- `vscode-extension/src/chat-participant.ts:275` — `shortenPath(fp: string)` — exported: **no** — Implements logic starting with: { const parts = fp.replace(/\\/g, "/").split("/"); return parts.slice(-3).join("/"); } (missing: @param fp, @returns)
- `vscode-extension/src/chat-participant.ts:283` — `resolveFileUri(fp: string)` — exported: **no** — Implements logic starting with: { try { const workspaceFolders = vscode.workspace.workspaceFolders; if (!workspaceFolders || workspaceFolders.length === 0) { return null; } // Abs… (missing: @param fp, @returns)
- `vscode-extension/src/chat-participant.ts:303` — `getSelectionContext()` — exported: **no** — Implements logic starting with: { const editor = vscode.window.activeTextEditor; if (!editor || editor.selection.isEmpty) return null; const selection = editor.document.getText(ed… (missing: @returns)
- `vscode-extension/src/chat-participant.ts:318` — `getActiveDiagnosticsContext()` — exported: **no** — Transforms a collection into a derived result. (missing: @returns)

### `vscode-extension/src/checkpoints.ts`
- `vscode-extension/src/checkpoints.ts:8` — `class GsdCheckpointProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/code-lens.ts`
- `vscode-extension/src/code-lens.ts:53` — `class GsdCodeLensProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/conversation-history.ts`
- `vscode-extension/src/conversation-history.ts:23` — `class GsdConversationHistoryPanel` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/diagnostics.ts`
- `vscode-extension/src/diagnostics.ts:9` — `class GsdDiagnosticBridge` — exported: **yes** — Defines an API surface. (missing: @returns)
- `vscode-extension/src/diagnostics.ts:98` — `GsdDiagnosticBridge.addFinding(uri: vscode.Uri, range: vscode.Range, message: string, severity: vscode.DiagnosticSeverity = vscode.DiagnosticSeverity.Warning)` — exported: **yes** — Implements logic starting with: { const existing = this.collection.get(uri) ?? []; const diagnostic = new vscode.Diagnostic(range, message, severity); diagnostic.source = "GSD Age… (missing: @param uri, @param range, @param message, @param severity)

### `vscode-extension/src/file-decorations.ts`
- `vscode-extension/src/file-decorations.ts:8` — `class GsdFileDecorationProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/git-integration.ts`
- `vscode-extension/src/git-integration.ts:9` — `class GsdGitIntegration` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/gsd-client.ts`
- `vscode-extension/src/gsd-client.ts:47` — `class GsdClient` — exported: **yes** — Defines an API surface. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:218` — `GsdClient.sendPrompt(message: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message)
- `vscode-extension/src/gsd-client.ts:226` — `GsdClient.steer(message: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message)
- `vscode-extension/src/gsd-client.ts:234` — `GsdClient.followUp(message: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param message)
- `vscode-extension/src/gsd-client.ts:254` — `GsdClient.getState()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:267` — `GsdClient.setModel(provider: string, modelId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param provider, @param modelId)
- `vscode-extension/src/gsd-client.ts:275` — `GsdClient.getAvailableModels()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:284` — `GsdClient.cycleModel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:297` — `GsdClient.setThinkingLevel(level: ThinkingLevel)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param level)
- `vscode-extension/src/gsd-client.ts:305` — `GsdClient.cycleThinkingLevel()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:318` — `GsdClient.compact(customInstructions?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param customInstructions, @returns)
- `vscode-extension/src/gsd-client.ts:331` — `GsdClient.setAutoCompaction(enabled: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param enabled)
- `vscode-extension/src/gsd-client.ts:343` — `GsdClient.setAutoRetry(enabled: boolean)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param enabled)
- `vscode-extension/src/gsd-client.ts:364` — `GsdClient.runBash(command: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param command, @returns)
- `vscode-extension/src/gsd-client.ts:394` — `GsdClient.getSessionStats()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:403` — `GsdClient.exportHtml(outputPath?: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param outputPath, @returns)
- `vscode-extension/src/gsd-client.ts:416` — `GsdClient.switchSession(sessionPath: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param sessionPath)
- `vscode-extension/src/gsd-client.ts:424` — `GsdClient.setSessionName(name: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param name)
- `vscode-extension/src/gsd-client.ts:432` — `GsdClient.getMessages()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:441` — `GsdClient.getLastAssistantText()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:450` — `GsdClient.getCommands()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:463` — `GsdClient.getForkMessages()` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @returns)
- `vscode-extension/src/gsd-client.ts:472` — `GsdClient.forkSession(entryId: string)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param entryId, @returns)
- `vscode-extension/src/gsd-client.ts:485` — `GsdClient.setSteeringMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mode)
- `vscode-extension/src/gsd-client.ts:493` — `GsdClient.setFollowUpMode(mode: "all" | "one-at-a-time")` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param mode)

### `vscode-extension/src/line-decorations.ts`
- `vscode-extension/src/line-decorations.ts:8` — `class GsdLineDecorationManager` — exported: **yes** — Defines an API surface. (missing: @returns)
- `vscode-extension/src/line-decorations.ts:109` — `diffLines(originalLines: string[], currentLines: string[])` — exported: **no** — Implements logic starting with: { const added: number[] = []; const modified: number[] = []; const maxShared = Math.min(originalLines.length, currentLines.length); for (let i = 0;… (missing: @param originalLines, @param currentLines, @returns)

### `vscode-extension/src/permissions.ts`
- `vscode-extension/src/permissions.ts:18` — `class GsdPermissionManager` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/plan-viewer.ts`
- `vscode-extension/src/plan-viewer.ts:18` — `class GsdPlanViewerProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/scm-provider.ts`
- `vscode-extension/src/scm-provider.ts:12` — `class GsdScmProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/session-tree.ts`
- `vscode-extension/src/session-tree.ts:18` — `class GsdSessionTreeProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/sidebar.ts`
- `vscode-extension/src/sidebar.ts:21` — `sendViaChat(message: string)` — exported: **no** — Runs asynchronous logic and returns a promise. (missing: @param message)
- `vscode-extension/src/sidebar.ts:30` — `class GsdSidebarProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `vscode-extension/src/slash-completion.ts`
- `vscode-extension/src/slash-completion.ts:12` — `class GsdSlashCompletionProvider` — exported: **yes** — Defines an API surface. (missing: @returns)

### `web/app/api/browse-directories/route.ts`
- `web/app/api/browse-directories/route.ts:12` — `getDevRoot()` — exported: **no** — Performs filesystem I/O. (missing: @returns)
- `web/app/api/browse-directories/route.ts:31` — `getLinuxMountPoints()` — exported: **no** — Implements logic starting with: { const mountPoints: string[] = []; const home = homedir(); const standardMounts = ["/media", "/mnt", "/run/media"]; for (const mp of standardMount… (missing: @returns)
- `web/app/api/browse-directories/route.ts:55` — `getAdditionalRoots()` — exported: **no** — Implements logic starting with: { const os = platform(); if (os === "linux") { return getLinuxMountPoints(); } return []; } (missing: @returns)
- `web/app/api/browse-directories/route.ts:74` — `GET(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param request, @returns)

### `web/app/api/files/route.ts`
- `web/app/api/files/route.ts:213` — `PATCH(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param request, @returns)
- `web/app/api/files/route.ts:302` — `DELETE(request: Request)` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param request, @returns)
- `web/app/api/files/route.ts:353` — `PUT(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: @param request, @returns)

### `web/app/api/projects/route.ts`
- `web/app/api/projects/route.ts:12` — `expandTilde(p: string)` — exported: **no** — Implements logic starting with: { if (p === "~") return homedir(); if (p.startsWith("~/")) return join(homedir(), p.slice(2)); return p; } (missing: @param p, @returns)

### `web/app/api/remote-questions/route.ts`
- `web/app/api/remote-questions/route.ts:103` — `parseFrontmatter(content: string)` — exported: **no** — Implements logic starting with: { const startMarker = content.startsWith("---\r\n") ? "---\r\n" : "---\n" if (!content.startsWith(startMarker)) { return { data: {}, body: content,… (missing: @param content, @returns)
- `web/app/api/remote-questions/route.ts:128` — `writeFrontmatter(data: Record<string, unknown>, body: string)` — exported: **no** — Implements logic starting with: { const yamlStr = stringifyYaml(data, { lineWidth: 0 }).trimEnd() return `---\n${yamlStr}\n---${body}` } (missing: @param data, @param body, @returns)

### `web/app/api/switch-root/route.ts`
- `web/app/api/switch-root/route.ts:17` — `expandTilde(p: string)` — exported: **no** — Implements logic starting with: { if (p === "~") return homedir(); if (p.startsWith("~/")) return homedir() + p.slice(1); return p; } (missing: @param p, @returns)
- `web/app/api/switch-root/route.ts:32` — `POST(request: Request)` — exported: **yes** — Performs filesystem I/O. (missing: @param request, @returns)

### `web/components/gsd/chat-mode.tsx`
- `web/components/gsd/chat-mode.tsx:117` — `ChatMode({ className }: { className?: string })` — exported: **yes** — Implements logic starting with: { const state = useGSDWorkspaceState() const { sendCommand } = useGSDWorkspaceActions() const bridge = state.boot?.bridge ?? null const handleActio… (missing: @param { className }, @returns)
- `web/components/gsd/chat-mode.tsx:167` — `ChatModeHeader({ onPrimaryAction, onSecondaryAction }: ChatModeHeaderProps)` — exported: **no** — Transforms a collection into a derived result. (missing: @param { onPrimaryAction, onSecondaryAction }, @returns)
- `web/components/gsd/chat-mode.tsx:305` — `MarkdownContent({ content }: { content: string })` — exported: **no** — Executes a subprocess command. (missing: @param { content }, @returns)
- `web/components/gsd/chat-mode.tsx:485` — `TuiSelectPrompt({ prompt, onSubmit, }: { prompt: TuiPrompt onSubmit: (data: string) => void })` — exported: **no** — Transforms a collection into a derived result. (missing: @param {
  prompt,
  onSubmit,
}, @returns)
- `web/components/gsd/chat-mode.tsx:626` — `TuiTextPrompt({ prompt, onSubmit, }: { prompt: TuiPrompt onSubmit: (data: string) => void })` — exported: **no** — Logs diagnostic information. (missing: @param {
  prompt,
  onSubmit,
}, @returns)
- `web/components/gsd/chat-mode.tsx:723` — `TuiPasswordPrompt({ prompt, onSubmit, }: { prompt: TuiPrompt onSubmit: (data: string) => void })` — exported: **no** — Logs diagnostic information. (missing: @param {
  prompt,
  onSubmit,
}, @returns)
- `web/components/gsd/chat-mode.tsx:872` — `PlatformLogoIcon({ className }: { className?: string })` — exported: **no** — Implements logic starting with: { const isDark = useIsDark() return ( <Image src={isDark ? "/logo-icon-white.svg" : "/logo-icon-black.svg"} alt="" width={24} height={32} unoptimiz… (missing: @param { className }, @returns)
- `web/components/gsd/chat-mode.tsx:893` — `InlineThinking({ content, isStreaming }: { content: string; isStreaming: boolean })` — exported: **no** — Transforms a collection into a derived result. (missing: @param { content, isStreaming }, @returns)
- `web/components/gsd/chat-mode.tsx:982` — `ChatBubble({ message, onSubmitPrompt, isThinking, }: { message: ChatMessage onSubmitPrompt?: (data: string) => void isThinking?: boolean })` — exported: **no** — Transforms a collection into a derived result. (missing: @param {
  message,
  onSubmitPrompt,
  isThinking,
}, @returns)
- `web/components/gsd/chat-mode.tsx:1097` — `ChatMessageList({ messages, onSubmitPrompt, fontSize, }: { messages: ChatMessage[] onSubmitPrompt: (data: string) => void fontSize?: number })` — exported: **no** — Transforms a collection into a derived result. (missing: @param {
  messages,
  onSubmitPrompt,
  fontSize,
}, @returns)
- `web/components/gsd/chat-mode.tsx:1162` — `ChatInputBar({ onSendInput, connected, onOpenAction, }: { onSendInput: (data: string, images?: PendingImage[]) => void connected: boolean onOpenAction?: (action: GSDActionDef) => void })` — exported: **no** — Transforms a collection into a derived result. (missing: @param {
  onSendInput,
  connected,
  onOpenAction,
}, @returns)
- `web/components/gsd/chat-mode.tsx:1594` — `InlineUiRequest({ request }: { request: PendingUiRequest })` — exported: **no** — Implements logic starting with: { const { respondToUiRequest, dismissUiRequest } = useGSDWorkspaceActions() const isSubmitting = useGSDWorkspaceState().commandInFlight === "extens… (missing: @param { request }, @returns)
- `web/components/gsd/chat-mode.tsx:1893` — `ToolExecutionBlock({ tool }: { tool: CompletedToolExecution })` — exported: **no** — Transforms a collection into a derived result. (missing: @param { tool }, @returns)
- `web/components/gsd/chat-mode.tsx:2022` — `ChatPane({ className, onOpenAction }: ChatPaneProps)` — exported: **yes** — Transforms a collection into a derived result. (missing: @param { className, onOpenAction }, @returns)

### `web/components/gsd/dashboard.tsx`
- `web/components/gsd/dashboard.tsx:48` — `getProgressColor(percent: number)` — exported: **no** — Implements logic starting with: { const p = Math.max(0, Math.min(100, percent)) // Hue: 25 (red) → 85 (yellow) at 50% → 145 (green) at 100% const hue = 25 + (p / 100) * 120 return… (missing: @param percent, @returns)

### `web/components/gsd/guided-dialog.tsx`
- `web/components/gsd/guided-dialog.tsx:47` — `GuidedDialog({ open, onOpenChange, detectionKind, }: GuidedDialogProps)` — exported: **yes** — Implements logic starting with: { return ( <Dialog open={open} onOpenChange={onOpenChange}> <DialogContent className="sm:max-w-4xl h-[85vh] flex flex-col p-0 gap-0" data-testid="g… (missing: @param {
  open,
  onOpenChange,
  detectionKind,
}, @returns)

### `web/components/gsd/loading-skeletons.tsx`
- `web/components/gsd/loading-skeletons.tsx:145` — `SidebarDataSkeleton()` — exported: **yes** — Transforms a collection into a derived result. (missing: @returns)

### `web/components/gsd/onboarding/step-authenticate.tsx`
- `web/components/gsd/onboarding/step-authenticate.tsx:66` — `extractDeviceCode(flow: WorkspaceOnboardingFlowState)` — exported: **no** — Transforms a collection into a derived result. (missing: @param flow, @returns)

### `web/components/gsd/onboarding/step-provider.tsx`
- `web/components/gsd/onboarding/step-provider.tsx:41` — `groupProviders(providers: WorkspaceOnboardingProviderState[])` — exported: **no** — Transforms a collection into a derived result. (missing: @param providers, @returns)

### `web/components/gsd/scope-badge.tsx`
- `web/components/gsd/scope-badge.tsx:71` — `normalizeScopeId(raw: string)` — exported: **no** — Implements logic starting with: { return raw.replace(/([MST])0*(\d+)/g, "$1$2") } (missing: @param raw, @returns)
- `web/components/gsd/scope-badge.tsx:79` — `parseScopeLabel(label: string)` — exported: **no** — Implements logic starting with: { const m = label.match(/^(.+?)\s*—\s*(.+)$/) if (m) return { scopeId: normalizeScopeId(m[1].trim()), phase: m[2].trim() } return { scopeId: normal… (missing: @param label, @returns)
- `web/components/gsd/scope-badge.tsx:99` — `ScopeBadge({ label, size = "md", className }: ScopeBadgeProps)` — exported: **yes** — Implements logic starting with: { const { scopeId, phase } = parseScopeLabel(label) if (scopeId === "Project scope pending") { return <span className={cn("text-muted-foreground",… (missing: @param { label, size = "md", className }, @returns)
- `web/components/gsd/scope-badge.tsx:144` — `ScopeBadgeInline({ label, className }: { label: string; className?: string })` — exported: **yes** — Implements logic starting with: { const { scopeId, phase } = parseScopeLabel(label) if (scopeId === "Project scope pending") { return <span className={cn("text-muted-foreground",… (missing: @param { label, className }, @returns)

### `web/components/gsd/shell-terminal.tsx`
- `web/components/gsd/shell-terminal.tsx:428` — `uploadAndInjectImage(file: File, sessionId: string, projectCwd?: string)` — exported: **no** — Serializes a value to JSON. (missing: @param file, @param sessionId, @param projectCwd)
- `web/components/gsd/shell-terminal.tsx:482` — `deriveProjectScopedSessionId(projectCwd: string | undefined, sessionPrefix?: string, command?: string)` — exported: **no** — Implements logic starting with: { const base = sessionPrefix ?? (command ? "gsd-default" : "default") if (!projectCwd) return base return `${base}:${projectCwd}` } (missing: @param projectCwd, @param sessionPrefix, @param command, @returns)

### `web/components/gsd/visualizer-view.tsx`
- `web/components/gsd/visualizer-view.tsx:226` — `SectionLabel({ children }: { children: React.ReactNode })` — exported: **no** — Implements logic starting with: { return ( <div className="flex items-center gap-2.5"> <div className="h-3.5 w-0.5 rounded-full bg-foreground/25" /> <h3 className="text-xs font-se… (missing: @param { children }, @returns)
- `web/components/gsd/visualizer-view.tsx:238` — `EmptyState({ message, icon: Icon = AlertCircle }: { message: string; icon?: React.ComponentType<{ className?: string }> })` — exported: **no** — Implements logic starting with: { return ( <div className="flex flex-col items-center justify-center gap-4 rounded-xl border border-dashed border-border py-16 text-center"> <div c… (missing: @param { message, icon: Icon = AlertCircle }, @returns)
- `web/components/gsd/visualizer-view.tsx:250` — `StatCard({ label, value, sub, accent, }: { label: string value: string sub?: string accent?: "sky" | "emerald" | "amber" | "default" })` — exported: **no** — Implements logic starting with: { const accentClasses = { sky: "from-info/8 border-info/20", emerald: "from-success/8 border-success/20", amber: "from-warning/8 border-warning/20"… (missing: @param {
  label,
  value,
  sub,
  accent,
}, @returns)
- `web/components/gsd/visualizer-view.tsx:287` — `ProgressBar({ value, max, color = "sky", animated = false, }: { value: number max: number color?: "sky" | "emerald" | "amber" animated?: boolean })` — exported: **no** — Implements logic starting with: { const pct = max > 0 ? Math.max(1, (value / max) * 100) : 0 const barColor = { sky: "bg-info", emerald: "bg-success", amber: "bg-warning" }[color]… (missing: @param {
  value,
  max,
  color = "sky",
  animated = false,
}, @returns)

### `web/lib/__tests__/dashboard-metrics-fallback.test.ts`
- `web/lib/__tests__/dashboard-metrics-fallback.test.ts:29` — `deriveMetrics(projectTotals: ProjectTotals | null, auto: AutoDashboard | null)` — exported: **no** — Implements logic starting with: { return { elapsed: projectTotals?.duration ?? auto?.elapsed ?? 0, totalCost: projectTotals?.cost ?? auto?.totalCost ?? 0, totalTokens: projectTota… (missing: @param projectTotals, @param auto, @returns)

### `web/lib/auth-guard.ts`
- `web/lib/auth-guard.ts:14` — `verifyAuthToken(request: Request)` — exported: **yes** — Implements logic starting with: { const expectedToken = process.env.GSD_WEB_AUTH_TOKEN if (!expectedToken) { // No token configured (e.g. dev mode) — allow through return null } l… (missing: @param request, @returns)

### `web/lib/auth.ts`
- `web/lib/auth.ts:33` — `getAuthToken()` — exported: **yes** — Implements logic starting with: { if (cachedToken !== null) return cachedToken if (typeof window === "undefined") return null // 1. Try the URL fragment (initial page load from gs… (missing: @returns)
- `web/lib/auth.ts:90` — `authHeaders(extra?: Record<string, string>)` — exported: **yes** — Implements logic starting with: { const token = getAuthToken() const headers: Record<string, string> = { ...extra } if (token) { headers["Authorization"] = `Bearer ${token}` } ret… (missing: @param extra, @returns)
- `web/lib/auth.ts:107` — `authFetch(input: RequestInfo | URL, init?: RequestInit)` — exported: **yes** — Performs an HTTP request and returns a result. (missing: @param input, @param init, @returns)
- `web/lib/auth.ts:128` — `appendAuthParam(url: string)` — exported: **yes** — Implements logic starting with: { const token = getAuthToken() if (!token) return url const separator = url.includes("?") ? "&" : "?" return `${url}${separator}_token=${token}` } (missing: @param url, @returns)

### `web/lib/image-utils.ts`
- `web/lib/image-utils.ts:96` — `resizeImageInBrowser(base64: string, mimeType: string)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param base64, @param mimeType, @returns)
- `web/lib/image-utils.ts:163` — `processImageFile(file: File)` — exported: **yes** — Validates inputs and throws on failure. (missing: @param file, @returns)
- `web/lib/image-utils.ts:187` — `generateImageId()` — exported: **yes** — Implements logic starting with: { return `img-${Date.now()}-${Math.random().toString(36).slice(2, 8)}` } (missing: @returns)

### `web/lib/initial-gsd-header-filter.ts`
- `web/lib/initial-gsd-header-filter.ts:109` — `filterInitialGsdHeader(raw: string)` — exported: **yes** — Implements logic starting with: { const { plainText, rawOffsetsByPlainIndex } = indexVisibleText(raw) if (!plainText) { return { status: 'needs-more', text: '' } } const lines = p… (missing: @param raw, @returns)

### `web/lib/project-store-manager.tsx`
- `web/lib/project-store-manager.tsx:14` — `class ProjectStoreManager` — exported: **yes** — Defines an API surface. (missing: @returns)
- `web/lib/project-store-manager.tsx:43` — `ProjectStoreManager.switchProject(projectCwd: string)` — exported: **yes** — Implements logic starting with: { // Disconnect SSE on current active store if (this.activeProjectCwd && this.activeProjectCwd !== projectCwd) { const prev = this.stores.get(this.… (missing: @param projectCwd, @returns)
- `web/lib/project-store-manager.tsx:77` — `ProjectStoreManager.closeProject(projectCwd: string)` — exported: **yes** — Implements logic starting with: { const store = this.stores.get(projectCwd) if (!store) return store.dispose() this.stores.delete(projectCwd) // If we closed the active project, s… (missing: @param projectCwd)
- `web/lib/project-store-manager.tsx:101` — `ProjectStoreManager.getProjectCount()` — exported: **yes** — Implements logic starting with: { return this.stores.size } (missing: @returns)
- `web/lib/project-store-manager.tsx:106` — `ProjectStoreManager.getActiveProjectPaths()` — exported: **yes** — Implements logic starting with: { return Array.from(this.stores.keys()) } (missing: @returns)

### `web/lib/pty-chat-parser.ts`
- `web/lib/pty-chat-parser.ts:81` — `stripAnsi(s: string)` — exported: **yes** — Implements logic starting with: { // OSC: \x1b] ... (\x07 or \x1b\) s = s.replace(/\x1b\][^\x07\x1b]*(?:\x07|\x1b\\)/g, "") // DCS / PM / APC: \x1bP, \x1b^, \x1b_ ... \x1b\ s = s.… (missing: @param s, @returns)
- `web/lib/pty-chat-parser.ts:141` — `isPromptLine(line: string)` — exported: **no** — Implements logic starting with: { const trimmed = line.trim() return PROMPT_MARKERS.some((r) => r.test(trimmed)) } (missing: @param line, @returns)
- `web/lib/pty-chat-parser.ts:147` — `isSystemLine(line: string)` — exported: **no** — Implements logic starting with: { const trimmed = line.trim() if (trimmed.length === 0) return false // Short bracket-wrapped lines if (/^\[.*\]$/.test(trimmed) && trimmed.length… (missing: @param line, @returns)
- `web/lib/pty-chat-parser.ts:261` — `class PtyChatParser` — exported: **yes** — Defines an API surface. (missing: @returns)
- `web/lib/pty-chat-parser.ts:325` — `PtyChatParser.feed(chunk: string)` — exported: **yes** — Implements logic starting with: { this._lastInputAt = Date.now() // Any new content resets pending completion — we're still receiving output if (this._completionTimer) { clearTime… (missing: @param chunk)
- `web/lib/pty-chat-parser.ts:337` — `PtyChatParser.getMessages()` — exported: **yes** — Implements logic starting with: { return [...this._messages] } (missing: @returns)
- `web/lib/pty-chat-parser.ts:346` — `PtyChatParser.isAwaitingInput()` — exported: **yes** — Implements logic starting with: { return this._awaitingInput } (missing: @returns)
- `web/lib/pty-chat-parser.ts:371` — `PtyChatParser.onMessage(cb: MessageCallback)` — exported: **yes** — Implements logic starting with: { this._subscribers.add(cb) return () => this._subscribers.delete(cb) } (missing: @param cb, @returns)
- `web/lib/pty-chat-parser.ts:380` — `PtyChatParser.onCompletionSignal(cb: CompletionCallback)` — exported: **yes** — Implements logic starting with: { this._completionSubscribers.add(cb) return () => this._completionSubscribers.delete(cb) } (missing: @param cb, @returns)

### `web/lib/secure-path.ts`
- `web/lib/secure-path.ts:13` — `resolveSecurePath(requestedPath: string, root: string, options: { mustExist?: boolean } = {})` — exported: **yes** — Implements logic starting with: { if (requestedPath.startsWith("/") || requestedPath.startsWith("\\")) { return null; } if (requestedPath.includes("..")) { return null; } let root… (missing: @param requestedPath, @param root, @param options, @returns)

### `web/lib/shutdown-gate.ts`
- `web/lib/shutdown-gate.ts:107` — `isDaemonMode()` — exported: **yes** — Implements logic starting with: { return process.env.GSD_WEB_DAEMON_MODE === "1"; } (missing: @returns)
- `web/lib/shutdown-gate.ts:119` — `registerActiveStream(unsubscribe: () => void)` — exported: **yes** — Implements logic starting with: { gate.activeStreams.add(unsubscribe); return function deregister() { gate.activeStreams.delete(unsubscribe); }; } (missing: @param unsubscribe, @returns)
- `web/lib/shutdown-gate.ts:180` — `isShutdownPending()` — exported: **yes** — Implements logic starting with: { return gate.shutdownTimer !== null; } (missing: @returns)

### `web/lib/use-editor-font-size.ts`
- `web/lib/use-editor-font-size.ts:17` — `useEditorFontSize()` — exported: **yes** — Implements logic starting with: { const [fontSize, setFontSizeState] = useState<number>(() => { if (typeof window === "undefined") return DEFAULT_SIZE try { const stored = localSt… (missing: @returns)

### `web/lib/use-terminal-font-size.ts`
- `web/lib/use-terminal-font-size.ts:17` — `useTerminalFontSize()` — exported: **yes** — Implements logic starting with: { const [fontSize, setFontSizeState] = useState<number>(() => { if (typeof window === "undefined") return DEFAULT_SIZE try { const stored = localSt… (missing: @returns)

### `web/lib/use-user-mode.ts`
- `web/lib/use-user-mode.ts:41` — `getUserMode()` — exported: **yes** — Implements logic starting with: { return getSnapshot() } (missing: @returns)
- `web/lib/use-user-mode.ts:46` — `setUserMode(mode: UserMode)` — exported: **yes** — Implements logic starting with: { localStorage.setItem(STORAGE_KEY, mode) notify() } (missing: @param mode)

### `web/lib/visualizer-types.ts`
- `web/lib/visualizer-types.ts:306` — `formatCost(cost: number)` — exported: **yes** — Implements logic starting with: { const n = Number(cost) || 0 if (n < 0.01) return `$${n.toFixed(4)}` if (n < 1) return `$${n.toFixed(3)}` return `$${n.toFixed(2)}` } (missing: @param cost, @returns)
- `web/lib/visualizer-types.ts:314` — `formatTokenCount(count: number)` — exported: **yes** — Implements logic starting with: { if (count < 1000) return `${count}` if (count < 1_000_000) return `${(count / 1000).toFixed(1)}K` return `${(count / 1_000_000).toFixed(2)}M` } (missing: @param count, @returns)
- `web/lib/visualizer-types.ts:321` — `formatDuration(ms: number)` — exported: **yes** — Implements logic starting with: { const totalSeconds = Math.round(ms / 1000) if (totalSeconds < 60) return `${totalSeconds}s` const minutes = Math.floor(totalSeconds / 60) const s… (missing: @param ms, @returns)
- `web/lib/visualizer-types.ts:335` — `getCaptureStatusCounts(captures: CapturesInfo)` — exported: **yes** — Implements logic starting with: { const counts: Record<CaptureEntry["status"], number> = { pending: 0, triaged: 0, resolved: 0, } for (const entry of captures.entries) { counts[en… (missing: @param captures, @returns)

### `web/lib/workflow-action-execution.ts`
- `web/lib/workflow-action-execution.ts:20` — `executeWorkflowActionInPowerMode({ dispatch, }: { dispatch: () => Promise<unknown> })` — exported: **yes** — Runs asynchronous logic and returns a promise. (missing: @param {
  dispatch,
})

### `web/proxy.ts`
- `web/proxy.ts:15` — `proxy(request: NextRequest)` — exported: **yes** — Implements logic starting with: { const decision = evaluateWebProxyAuth({ pathname: request.nextUrl.pathname, searchParams: request.nextUrl.searchParams, headers: request.headers,… (missing: @param request, @returns)


## Already documented
- Count of functions with complete JSDoc: **313**

## Priority queue
Top 20 most impactful entries to document first (higher score = more exported + more referenced + more complex).

| Rank | File | Line | Signature | Exported | Importers | Caller-files | Complexity | Doc status | Why |
| ---: | ---- | ---: | --------- | :------: | --------: | ----------: | ---------: | --------- | --- |
| 1 | `packages/pi-tui/src/keys.ts` | 683 | `matchesKey(data: string, keyId: KeyId)` | yes | 24 | 26 | 195 | partial | exported, 24 importers, 26 caller-files, complexity 195 |
| 2 | `src/resources/extensions/gsd/doctor.ts` | 332 | `runGSDDoctor(basePath: string, options?: { fix?: boolean; dryRun?: boolean; scope?: string; fixLevel?: "task" \| "all"; isolationMode?: "none" \| "worktree" \| "branch"; includeBuild?: boolean; includeTests?: boolean })` | yes | 20 | 24 | 121 | undocumented | exported, 20 importers, 24 caller-files, complexity 121 |
| 3 | `src/resources/extensions/gsd/gsd-db.ts` | 581 | `openDatabase(path: string)` | yes | 185 | 192 | 21 | undocumented | exported, 185 importers, 192 caller-files, complexity 21 |
| 4 | `src/resources/extensions/gsd/state.ts` | 654 | `deriveStateFromDb(basePath: string)` | yes | 20 | 20 | 32 | undocumented | exported, 20 importers, 20 caller-files, complexity 32 |
| 5 | `src/resources/extensions/gsd/auto-recovery.ts` | 588 | `verifyExpectedArtifact(unitType: string, unitId: string, base: string)` | yes | 13 | 16 | 113 | partial | exported, 13 importers, 16 caller-files, complexity 113, 3 params |
| 6 | `src/resources/extensions/gsd/worktree-manager.ts` | 491 | `removeWorktree(basePath: string, name: string, opts: { deleteBranch?: boolean; force?: boolean; branch?: string } = {})` | yes | 11 | 16 | 44 | partial | exported, 11 importers, 16 caller-files, complexity 44, 3 params |
| 7 | `src/resources/extensions/gsd/schemas/parsers.ts` | 289 | `parseRoadmap(content: string)` | yes | 25 | 27 | 10 | undocumented | exported, 25 importers, 27 caller-files, complexity 10 |
| 8 | `src/resources/extensions/gsd/worktree-manager.ts` | 225 | `createWorktree(basePath: string, name: string, opts: { branch?: string; startPoint?: string; reuseExistingBranch?: boolean } = {})` | yes | 13 | 16 | 13 | partial | exported, 13 importers, 16 caller-files, complexity 13, 3 params |
| 9 | `packages/pi-ai/src/env-api-keys.ts` | 65 | `getEnvApiKey(provider: any)` | yes | 15 | 14 | 28 | undocumented | exported, 15 importers, 14 caller-files, complexity 28 |
| 10 | `packages/pi-ai/src/web-runtime-env-api-keys.ts` | 32 | `getEnvApiKey(provider: string)` | yes | 15 | 14 | 21 | undocumented | exported, 15 importers, 14 caller-files, complexity 21 |
| 11 | `packages/pi-coding-agent/src/core/tools/truncate.ts` | 69 | `truncateHead(content: string, options: TruncationOptions = {})` | yes | 16 | 16 | 13 | partial | exported, 16 importers, 16 caller-files, complexity 13 |
| 12 | `src/resources/extensions/gsd/auto-worktree.ts` | 1514 | `mergeMilestoneToMain(originalBasePath_: string, milestoneId: string, roadmapContent: string)` | yes | 12 | 13 | 128 | partial | exported, 12 importers, 13 caller-files, complexity 128, 3 params |
| 13 | `src/resources/extensions/gsd/preferences-validation.ts` | 32 | `validatePreferences(preferences: GSDPreferences)` | yes | 13 | 14 | 464 | undocumented | exported, 13 importers, 14 caller-files, complexity 464 |
| 14 | `src/resources/extensions/gsd/preferences.ts` | 149 | `loadEffectiveGSDPreferences(basePath?: string, opts?: { availableModelIds?: string[] })` | yes | 47 | 57 | 9 | undocumented | exported, 47 importers, 57 caller-files, complexity 9 |
| 15 | `src/resources/extensions/gsd/state.ts` | 298 | `deriveState(basePath: string, opts?: DeriveStateOptions)` | yes | 68 | 105 | 9 | partial | exported, 68 importers, 105 caller-files, complexity 9 |
| 16 | `src/resources/extensions/gsd/auto-dispatch.ts` | 1458 | `resolveDispatch(ctx: DispatchContext)` | yes | 12 | 24 | 8 | partial | exported, 12 importers, 24 caller-files, complexity 8 |
| 17 | `src/resources/extensions/gsd/prompt-loader.ts` | 174 | `loadPrompt(name: string, vars: Record<string, string> = {})` | yes | 17 | 30 | 8 | partial | exported, 17 importers, 30 caller-files, complexity 8 |
| 18 | `scripts/dist-test-resolve.mjs` | 32 | `resolve(specifier, context, nextResolve)` | yes | 131 | 264 | 6 | undocumented | exported, 131 importers, 264 caller-files, complexity 6, 3 params |
| 19 | `src/resources/extensions/gsd/auto-artifact-paths.ts` | 48 | `resolveExpectedArtifactPath(unitType: string, unitId: string, base: string)` | yes | 12 | 11 | 28 | partial | exported, 12 importers, 11 caller-files, complexity 28, 3 params |
| 20 | `src/resources/extensions/gsd/auto-worktree.ts` | 1199 | `teardownAutoWorktree(originalBasePath: string, milestoneId: string, opts: { preserveBranch?: boolean } = {})` | yes | 12 | 11 | 16 | partial | exported, 12 importers, 11 caller-files, complexity 16, 3 params |

## Notes
- Scope: tracked `.(js|jsx|ts|tsx|mjs|cjs)` under the requested directories, excluding `**/node_modules/**`, `**/dist/**`, `**/dist-test/**`, `pkg/dist/`, `.qogni/`, `.qogni-agent-projection/`, and `*.d.ts`.
- Classification: documented = has description + all params tagged + @returns (when non-void); partial = has some JSDoc but missing required parts; undocumented = no JSDoc.